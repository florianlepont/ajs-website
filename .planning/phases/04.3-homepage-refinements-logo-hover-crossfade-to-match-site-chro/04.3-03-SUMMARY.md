---
phase: 04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro
plan: "03"
subsystem: testing
tags: [playwright, vitest, e2e, homepage, gallery]

# Dependency graph
requires:
  - phase: 04.3-homepage-refinements (Plan 01)
    provides: HomeCarousel.astro's `.home-grid__tile` grid markup and the 'Grille'/'Carrousel' toggle buttons, unchanged by this plan
  - phase: 04.3-homepage-refinements (Plan 02)
    provides: Deletion of the standalone /galleries and /en/galleries listing pages, which this plan's specs previously depended on for discovery
provides:
  - gallery.spec.ts discovers galleries via the homepage grid instead of the deleted listing page, and verifies D-11 grid-tile clickability
  - about.spec.ts and contact.spec.ts reachability tests repointed off the deleted /galleries/ origin onto surviving BaseLayout pages (/contact/ and /about/ respectively)
  - Full green e2e (47/47) + unit (23/23) suite confirming the whole /galleries listing removal across Phase 04.3 is safe
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Grid-discovery bootstrap for e2e specs: page.goto('/') -> click getByRole('button', { name: 'Grille' }) -> page.locator('.home-grid__tile').first() — reused across gallery.spec.ts's three describe blocks, mirroring the existing pattern in homepage.spec.ts"

key-files:
  created: []
  modified:
    - tests/e2e/gallery.spec.ts
    - tests/e2e/about.spec.ts
    - tests/e2e/contact.spec.ts

key-decisions:
  - "Repurposed gallery.spec.ts's first describe block ('gallery listing') into the D-11 grid-clickability verification instead of adding a new block, per the plan's explicit instruction — no separate test added"
  - "about.spec.ts now originates from /contact/ and contact.spec.ts now originates from /about/ (cross-pointing) rather than each from its own page, since each spec needs a *different* surviving BaseLayout page than the one it's testing reachability into"
  - "This worktree's node_modules was effectively empty (npm ci had not run) and had no .env — ran `npm ci` (matches the committed, unmodified package-lock.json; not a new package install) and copied the gitignored .env from the main repo working tree to unblock `npm run build`/e2e webServer startup (Rule 3 blocking-issue fix)"

requirements-completed: []

coverage:
  - id: T-04.3-05
    description: "Reconcile e2e discovery only; keep all substantive assertions intact so the suite remains a real regression gate after the /galleries route removal"
    verification:
      - kind: e2e
        ref: "npm run test:e2e (47/47 passed) and npm run test:unit (23/23 passed) — full suite green after gallery/about/contact spec rewrites"
        status: pass
    human_judgment: false
  - id: D-11
    description: "Grid tiles remain fully clickable to their detail pages after the standalone listing removal (verification only, no code change)"
    verification:
      - kind: e2e
        ref: "tests/e2e/gallery.spec.ts#gallery listing > homepage grid renders gallery tiles, and the first tile is clickable to its detail page (D-11)"
        status: pass
    human_judgment: false

# Metrics
duration: 25min
completed: 2026-07-12
---

# Phase 04.3 Plan 03: Reconcile e2e Suite with /galleries Removal Summary

**Rewrote gallery.spec.ts's discovery to bootstrap from the homepage grid instead of the deleted /galleries listing page (also verifying D-11 grid clickability), repointed about.spec.ts/contact.spec.ts's reachability tests onto surviving BaseLayout pages, and confirmed the whole e2e (47/47) + unit (23/23) suite is green — the phase's green gate.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-07-12T08:30:00Z
- **Completed:** 2026-07-12T08:32:30Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `gallery.spec.ts`'s three describe blocks (listing/detail/lightbox) all now bootstrap via `page.goto('/')` → click `getByRole('button', { name: 'Grille' })` → `page.locator('.home-grid__tile').first()`, instead of the deleted `/galleries/` listing + `voir la galerie` link matcher.
- The "gallery listing" block was repurposed into the D-11 verification: asserts the first grid tile is visible, has a non-empty accessible name, and its href matches `/galleries/<slug>/`.
- Detail and lightbox assertions (bilingual statement diff, native `dialog[open]`, `data-role="counter"`, ArrowRight/Escape) are unchanged — only the discovery source changed.
- `about.spec.ts`'s "header nav links to the About page" test now originates from `/contact/`; `contact.spec.ts`'s "reach Contact from header nav" test now originates from `/about/` — both surviving BaseLayout pages whose headers still expose the target link.
- Confirmed via grep that no e2e spec navigates to the deleted `/galleries/` listing route anymore; remaining `galleries/` references are all `/galleries/<slug>/`-style detail URLs or explanatory comments.
- Full green gate confirmed: `npm run test:e2e` (47/47) and `npm run test:unit` (23/23) both pass.

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite gallery.spec.ts discovery via the homepage grid (+ D-11 verification)** - `795c208` (test)
2. **Task 2: Repoint about/contact reachability specs + run the full green gate** - `b59b959` (test)

**Plan metadata:** pending (this SUMMARY commit)

## Files Created/Modified
- `tests/e2e/gallery.spec.ts` - Discovery rewritten from `/galleries/` + `voir la galerie` link matcher to homepage-grid discovery (`/` → 'Grille' toggle → `.home-grid__tile`); first block repurposed as the D-11 clickability check; RED-phase header comment updated
- `tests/e2e/about.spec.ts` - "header nav links to the About page" test's origin changed from `/galleries/` to `/contact/`; stale comment updated
- `tests/e2e/contact.spec.ts` - "reach Contact from header nav" test's origin changed from `/galleries/` to `/about/`; stale comment updated

## Decisions Made
- Cross-pointed the two reachability tests (about.spec.ts → `/contact/`, contact.spec.ts → `/about/`) rather than each spec originating from its own page, since a spec testing reachability *into* a page needs to originate from a *different* surviving BaseLayout page.
- Ran `npm ci` and copied the gitignored `.env` from the main repo's working tree into this worktree before verification — this worktree's `node_modules` was effectively empty (no prior install) and had no `.env`, both of which blocked `npm run build` and the Playwright `webServer` (which runs `npm run preview` against a real build). `npm ci` reproduces the exact committed `package-lock.json` (not a new package install, so outside Rule 3's package-legitimacy exclusion); `.env` is gitignored local configuration, not a source change. This unblocked full-suite verification and incidentally confirmed that Phase 04.3 Plan 02's previously-deferred sandbox `npm run build` issue (documented in `deferred-items.md`) does not reproduce once dependencies are actually installed — it was an artifact of this specific worktree never having run `npm ci`, not the rolldown/tsconfig incompatibility originally diagnosed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Installed dependencies and restored local env config to unblock verification**
- **Found during:** Task 1 verification (`npx playwright test gallery.spec.ts`)
- **Issue:** This worktree's `node_modules` contained only cache directories (no actual packages installed), and it had no `.env` file, so `npm run build`/the Playwright `webServer` (`npm run preview`) could not start.
- **Fix:** Ran `npm ci` (installs exactly what `package-lock.json` already specifies — not a new/different package) and copied the gitignored `.env` from the main repo's working tree (`SANITY_PROJECT_ID`/`SANITY_DATASET`/`SANITY_API_READ_TOKEN`) into this worktree.
- **Files modified:** None tracked by git (`node_modules/` and `.env` are both gitignored).
- **Verification:** `npm run build` completed successfully (17 pages built); `npx playwright test gallery.spec.ts` then ran and passed.
- **Committed in:** N/A — no trackable files changed by this fix.

---

**Total deviations:** 1 auto-fixed (1 blocking-issue fix, no tracked files affected).
**Impact on plan:** None on scope/correctness — this was purely a local worktree environment setup step required to run the plan's own `<verify>` commands. No plan deliverable changed as a result.

## Issues Encountered
None beyond the worktree environment setup documented above. The previously-flagged sandbox `npm run build` failure (04.3-02's `deferred-items.md`) did not reproduce in this worktree after `npm ci` — it appears to have been caused by this worktree simply never having had `npm ci` run in it, not a real toolchain incompatibility. Flagging this as good news for the phase-level CI concern raised in 04.3-02's SUMMARY, though the authoritative signal remains GitHub Actions CI on push.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 04.3 is fully implemented and verified: HomeCarousel refinements (Plan 01), standalone gallery listing removal (Plan 02), and e2e/unit suite reconciliation (this plan) are all complete with a green full-suite gate (47 e2e + 23 unit tests passing).
- No blockers. Recommend confirming the same green result on the next GitHub Actions CI run before considering Phase 04.3 fully shippable, since this plan's local verification used a freshly-`npm ci`'d worktree rather than CI itself.

---
*Phase: 04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro*
*Completed: 2026-07-12*

## Self-Check: PASSED

All 3 modified test files and this SUMMARY.md verified present on disk; both task commit hashes (795c208, b59b959) verified present in git log.
