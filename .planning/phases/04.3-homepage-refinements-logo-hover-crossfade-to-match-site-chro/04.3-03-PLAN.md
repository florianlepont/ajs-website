---
phase: 04.3-homepage-refinements
plan: 03
type: execute
wave: 2
depends_on: [04.3-01, 04.3-02]
files_modified:
  - tests/e2e/gallery.spec.ts
  - tests/e2e/about.spec.ts
  - tests/e2e/contact.spec.ts
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "The full Playwright e2e suite and Vitest unit suite pass after the /galleries listing removal."
    - "gallery.spec.ts discovers a gallery via the homepage grid tile instead of the deleted listing page."
    - "Grid tiles are verified clickable to their detail pages (D-11 — verification only, no code change)."
    - "About/Contact reachability specs reach their targets from a surviving BaseLayout page (not the deleted /galleries route)."
  artifacts:
    - "tests/e2e/gallery.spec.ts (discovery rewritten to the homepage grid)"
    - "tests/e2e/about.spec.ts and tests/e2e/contact.spec.ts (origin page repointed off /galleries/)"
  key_links:
    - "gallery.spec discovery relies on HomeCarousel .home-grid__tile links (unchanged by 04.3-01) and the 'Grille' toggle accessible name (preserved by D-07)"
    - "about/contact reachability relies on the BaseLayout header's About/Contact links still present after D-04 removed only the Galleries link"
---

<objective>
Reconcile the e2e suite with the /galleries listing removal so the whole suite goes green again. Rewrite gallery.spec.ts's discovery step to start from the homepage grid instead of the deleted listing page (which also verifies D-11 — grid tiles remain clickable), and repoint the About/Contact reachability specs off the deleted `/galleries/` origin page onto surviving BaseLayout pages.

Purpose: The existing locked specs bootstrap through `/galleries/` + the "voir la galerie" GalleryCard accessible name, both removed in 04.3-02; without this reconciliation the suite fails. This is the phase's green-gate plan.
Output: Three updated e2e specs and a fully green e2e + unit suite.
</objective>

## Artifacts this phase produces (this plan)

- `tests/e2e/gallery.spec.ts`: discovery rewritten from `page.goto('/galleries/')` + `getByRole('link', { name: /voir la galerie/i })` to `/` → click `getByRole('button', { name: 'Grille' })` → `page.locator('.home-grid__tile').first()`; first block repurposed as the homepage-grid listing/D-11 clickability check; detail + lightbox blocks keep their assertions with only the discovery source changed.
- `tests/e2e/about.spec.ts`: "header nav links to About" origin changed from `/galleries/` to `/contact/`.
- `tests/e2e/contact.spec.ts`: "reach Contact from header nav" origin changed from `/galleries/` to `/about/`.

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md
@.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-PATTERNS.md
@tests/e2e/gallery.spec.ts
@tests/e2e/about.spec.ts
@tests/e2e/contact.spec.ts
@src/components/HomeCarousel.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite gallery.spec.ts discovery via the homepage grid (+ D-11 verification)</name>
  <files>tests/e2e/gallery.spec.ts</files>
  <read_first>
    - tests/e2e/gallery.spec.ts (all three describe blocks: listing lines 10-25, detail lines 27-53, lightbox lines 55-87 — the `/galleries/` gotos on lines 14/31/59 and the `getByRole('link', { name: /voir la galerie/i })` discovery on lines 16/33-34/61-64)
    - src/components/HomeCarousel.astro (grid markup lines 122-144: `.home-grid__tile` is an `<a href={.../galleries/${gallery.slug}}>` wrapping an `<img alt={gallery.alt}>`, a scrim, and a `<span class="home-grid__tile-title">`; grid is revealed by the 'Grille' toggle button)
    - tests/e2e/homepage.spec.ts (precedent for grid discovery: `getByRole('button', { name: 'Grille' })` click + `[data-role="home-grid"]` / `.home-grid__tile` locators)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md (code_context gallery.spec rewrite guidance; D-11)
  </read_first>
  <action>
    Replace every `/galleries/` listing bootstrap in gallery.spec.ts with a homepage-grid discovery. In each block, navigate to `/`, click the Grid toggle via `page.getByRole('button', { name: 'Grille' })`, then locate the first gallery link with `page.locator('.home-grid__tile').first()`. Read its `href` (a `/galleries/<slug>/` URL) and derive the slug with the same `/\/galleries\/([^/]+)\/?$/` regex the specs already use for `firstCardHref`.

    First describe block ("gallery listing"): repurpose it into the homepage-grid listing check. After switching to grid, assert `.home-grid__tile` is visible, the first tile has a non-empty accessible name (its title text), and its `href` matches `/\/galleries\/[^/]+\/?$/`. This block is the D-11 verification that grid tiles remain fully clickable to their detail pages — keep it verification-only, no source/component change.

    Detail block: keep the bilingual-statement assertions unchanged (navigate to the discovered `href` for FR, then `/en/galleries/${slug}/` for EN, assert both `main` innerText are non-empty and differ) — only swap the slug/href discovery to the homepage grid.

    Lightbox block: keep all existing assertions unchanged (thumbnail `getByRole('button', { name: /voir en taille réelle|view full size/i })`, native `dialog[open]`, `[data-role="counter"]`, ArrowRight advances the counter, Escape closes + returns focus) — only change the initial navigation to reach the detail page via a grid-discovered `href` instead of the listing page.

    Update the RED-phase header comment that claims the listing/detail pages "do not exist yet" so it instead states the standalone listing route was removed in Phase 04.3 and discovery now starts from the homepage grid. Do not weaken the actual assertions — only the discovery source changes.
  </action>
  <verify>
    <automated>npx playwright test gallery.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - gallery.spec.ts contains no `page.goto('/galleries/')` and no `name: /voir la galerie/i` matcher; discovery uses `.home-grid__tile` after clicking the 'Grille' toggle on `/`.
    - The first (listing) block asserts `.home-grid__tile` is a visible link whose `href` matches `/\/galleries\/[^/]+\/?$/` — satisfying the D-11 grid-clickability verification.
    - The detail block still asserts FR vs EN statements are non-empty and differ, using a slug discovered from the homepage grid.
    - The lightbox block still passes with its existing dialog/counter/ArrowRight/Escape assertions, reaching the detail page via a grid-discovered href.
    - `npx playwright test gallery.spec.ts` passes.
  </acceptance_criteria>
  <done>gallery.spec.ts fully migrated off the deleted listing route; D-11 grid clickability asserted; all three blocks green.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint about/contact reachability specs + run the full green gate</name>
  <files>tests/e2e/about.spec.ts, tests/e2e/contact.spec.ts</files>
  <read_first>
    - tests/e2e/about.spec.ts (the "header nav links to the About page" test, lines 63-75 — `page.goto('/galleries/')` on line 70, then clicks header `À propos`, expects `/about/$`)
    - tests/e2e/contact.spec.ts (the "reach Contact from header nav" test, lines 121-133 — `page.goto('/galleries/')` on line 129, then clicks `contact`, expects `/contact/`)
    - src/layouts/BaseLayout.astro (confirm the standard header still renders About + Contact nav links after D-04 removed only the Galleries link)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md (D-04 — About/Contact links stay; only Galleries removed)
  </read_first>
  <action>
    In about.spec.ts, change the "header nav links to the About page" test's `page.goto('/galleries/')` (line 70) to `page.goto('/contact/')` — a surviving BaseLayout page whose standard header still exposes the "À propos" link. Keep the click on the header's `getByRole('link', { name: 'À propos' })` and the `/about/$` URL assertion. Update the stale comment that references the homepage's "Accueil/Galeries" nav so it reflects the removed Galleries item and the new origin page.

    In contact.spec.ts, change the "reach Contact from header nav" test's `page.goto('/galleries/')` (line 129) to `page.goto('/about/')` — its header exposes the "Contact" link. Keep the click on `getByRole('link', { name: /^contact$/i })` and the `/contact/` URL assertion. Update the stale comment likewise.

    Confirm no e2e spec still navigates to the `/galleries/` listing (only `/galleries/<slug>/` detail URLs should remain). Then run the full e2e + unit suite as the phase green gate.
  </action>
  <verify>
    <automated>npm run test:e2e && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - about.spec.ts and contact.spec.ts no longer navigate to `/galleries/`; they start from `/contact/` and `/about/` respectively, and their existing header-click + URL assertions pass.
    - No e2e spec contains `goto('/galleries/')` (listing); any remaining `galleries/` reference in tests/e2e is a `/galleries/${slug}/`-style detail URL only.
    - `npm run test:e2e` passes the entire suite and `npm run test:unit` passes — the phase-level green gate.
  </acceptance_criteria>
  <done>About/Contact reachability specs migrated off the deleted route; the entire e2e + unit suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → deployed static site | e2e specs exercise the built site; no production trust boundary changes in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-05 | Repudiation / test-integrity | gallery/about/contact specs | low | mitigate | Reconcile discovery only; keep all substantive assertions intact (no weakening) so the suite remains a real regression gate after the route removal. |
| T-04.3-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No package installs in this plan (test edits only). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx playwright test gallery.spec.ts` passes after Task 1.
- `npm run test:e2e` (full Playwright suite) and `npm run test:unit` (Vitest) both pass after Task 2 — this is the phase green gate, run only after 04.3-01 and 04.3-02 have landed (Wave 2).
- No e2e spec references the deleted `/galleries/` listing route.
</verification>

<success_criteria>
The full e2e + unit suite is green with the /galleries listing removed: gallery.spec.ts discovers galleries via the homepage grid (verifying D-11), and the About/Contact reachability specs reach their targets from surviving BaseLayout pages.
</success_criteria>

<output>
Create `.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-03-SUMMARY.md` when done.
</output>
