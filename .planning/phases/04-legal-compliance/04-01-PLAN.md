---
phase: 04-legal-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/legal.spec.ts
  - src/pages/mentions-legales.astro
  - src/pages/en/mentions-legales.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [LEGAL-01]
must_haves:
  truths:
    - "Visitor can view a mentions légales page (legal name, OVH host, business status) in FR at /mentions-legales/ and EN at /en/mentions-legales/"
    - "The mentions légales page names 'Romane Lepont' as legal editor alongside the 'Atelier Jacqueline Suzanne' trade name (D-08 compliance)"
    - "A 'Mentions légales' / 'Legal notice' link appears in the footer on every page"
    - "The language switcher navigates between /mentions-legales/ and /en/mentions-legales/ at matching slugs"
    - "The full failing e2e harness (tests/e2e/legal.spec.ts) exists covering LEGAL-01/03/05 + switcher + footer"
  artifacts:
    - path: "tests/e2e/legal.spec.ts"
      provides: "RED-first e2e coverage for both legal pages, cookie absence, switcher, footer reachability"
      contains: "mentions-legales"
    - path: "src/pages/mentions-legales.astro"
      provides: "FR mentions légales page (Éditeur/Hébergement/Statut sections)"
      contains: "Romane Lepont"
    - path: "src/pages/en/mentions-legales.astro"
      provides: "EN legal notice page"
      contains: "Romane Lepont"
    - path: "src/layouts/BaseLayout.astro"
      provides: "footer-legal-nav with the Mentions légales / Legal notice link"
      contains: "footer-legal-nav"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "/mentions-legales/"
      via: "getRelativeLocaleUrl(locale, 'mentions-legales')"
      pattern: "getRelativeLocaleUrl\\(locale, 'mentions-legales'\\)"
    - from: "tests/e2e/legal.spec.ts"
      to: "/mentions-legales/"
      via: "page.goto"
      pattern: "goto\\('/mentions-legales/'\\)"
---

<objective>
Deliver the mentions légales vertical slice (LEGAL-01) and stand up the whole-phase failing e2e harness (VALIDATION.md Wave 0). After this plan a visitor can reach and read an accurate, bilingual mentions légales page from the footer of every page, and the language switcher works between its FR/EN slugs.

Purpose: LEGAL-01 is a launch blocker — a professional/commercial editorial site must publicly disclose its publisher identity, host, and business status under LCEN Article 1-1.
Output: `tests/e2e/legal.spec.ts` (RED), `src/pages/mentions-legales.astro`, `src/pages/en/mentions-legales.astro`, and a footer legal nav added to `src/layouts/BaseLayout.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** French/EU site visitor, **I want to** read the site's legal notice, privacy policy, and cookie disclosure in French or English, **so that** I know who runs the site, how my contact-form data is handled, and that no tracking cookies are used.

*(Derived from ROADMAP Phase 4 goal + its three visitor-framed success criteria. This plan delivers the legal-notice half of that story; Plan 02 delivers the privacy/cookie half.)*

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-legal-compliance/04-CONTEXT.md
@.planning/phases/04-legal-compliance/04-RESEARCH.md
@.planning/phases/04-legal-compliance/04-PATTERNS.md
@.planning/phases/04-legal-compliance/04-UI-SPEC.md

<interfaces>
<!-- Key contracts the executor needs — extracted from the codebase this session. Use directly; no exploration needed. -->

From src/layouts/BaseLayout.astro (existing frontmatter, lines 6, 36-40):
- `import { getRelativeLocaleUrl } from 'astro:i18n';`  (already imported — no new import)
- Existing pattern: `const aboutLabel = locale === 'en' ? 'About' : 'À propos';` / `const aboutHref = getRelativeLocaleUrl(locale, 'about');`
- Existing footer (lines 89-91): `<footer class="chrome-band"><p class="footer-text">{footerText}</p></footer>`
- Existing `.nav-link` style (lines 215-225): 14px/300, text-decoration none, underline on hover/focus-visible — REUSE for footer links, do not redefine its properties.

From src/lib/i18n-paths.ts (getSwitcherHref, lines 25-56):
- Strips the `/en/` prefix and re-adds it for the target locale; does NO slug translation. `hasTranslatedCounterpart` returns true for every slug except `'404'`.
- Consequence: FR and EN pages MUST share the identical slug segment (`mentions-legales`) or the switcher silently 404s (RESEARCH Pitfall 1). No change to i18n-paths.ts is needed or permitted this phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the failing legal e2e harness (Wave 0, RED)</name>
  <files>tests/e2e/legal.spec.ts</files>
  <read_first>
    - tests/e2e/about.spec.ts (page-render + lang + content + footer/nav reachability assertion patterns)
    - tests/e2e/i18n.spec.ts (switcher navigation + `context.cookies()` cookie assertion patterns; COOKIE_NAME = 'ajs_locale')
    - .planning/phases/04-legal-compliance/04-PATTERNS.md (the `tests/e2e/legal.spec.ts` section spells out each assertion to mirror)
    - .planning/phases/04-legal-compliance/04-VALIDATION.md (Per-Task Verification Map — the four `-g` groups: "mentions", "privacy", "cookie", "switcher")
  </read_first>
  <action>
    Create `tests/e2e/legal.spec.ts` mirroring the structure of `about.spec.ts` + the switcher subset of `i18n.spec.ts`. Open with a RED-first disclaimer comment (same style as about.spec.ts lines 3-8) naming LEGAL-01/LEGAL-03/LEGAL-05 and stating the pages/footer nav do not exist yet.

    Organize test names so `playwright test -g` can select these four groups exactly: "mentions", "privacy", "cookie", "switcher".

    Cover the entire phase (both pages — privacy tests stay RED until Plan 02, that is expected):
    - mentions: FR at `/mentions-legales/` — `html[lang=fr]`, `h1` contains "Mentions légales", `main` contains "Romane Lepont", "Atelier Jacqueline Suzanne", "OVH", and the FR status string "activité individuelle, non immatriculée". EN at `/en/mentions-legales/` — `html[lang=en]`, `h1` contains "Legal notice", `main` contains "Romane Lepont" and "not registered". A "copy differs between FR and EN" diff assertion.
    - privacy: FR at `/confidentialite/` — `html[lang=fr]`, `h1` contains "confidentialité" (case-insensitive), `main` contains "ajs_locale" and a contact-form-flow phrase. EN at `/en/confidentialite/` — `html[lang=en]`, `h1` contains "Privacy". A copy-differs assertion.
    - cookie: `page.goto('/mentions-legales/')` then `page.goto('/confidentialite/')` with NO switcher click; assert `context.cookies()` contains no cookie named `ajs_locale`; assert no consent-banner element exists (e.g. `page.getByRole('dialog')` / an element matching /accepter les cookies|accept cookies/i has count 0).
    - switcher: from `/mentions-legales/` click the header switcher link named "EN" → URL matches `/\/en\/mentions-legales\/$/`; reverse from `/en/mentions-legales/` clicking "FR" → `/\/mentions-legales\/$/`. Repeat the pair for `/confidentialite/` ↔ `/en/confidentialite/`.
    - footer reachability: from `/`, `page.locator('footer').getByRole('link', { name: 'Mentions légales' }).click()` → URL `/\/mentions-legales\/$/`; and `{ name: 'Confidentialité' }` → `/\/confidentialite\/$/`.

    Do not stub or weaken assertions to make them pass early.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/legal.spec.ts -g "mentions" 2>&1 | grep -Eiq "fail|error|expect" && echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/legal.spec.ts` exists and is syntactically valid (playwright loads it without a parse error).
    - Running the suite yields FAILING tests for the "mentions" group (pages do not exist yet → 404s) — RED confirmed, not skipped/empty.
    - Test names contain the substrings "mentions", "privacy", "cookie", "switcher" so `-g` selection works.
    - The cookie group asserts absence of an `ajs_locale` cookie after loading legal pages with no switcher click.
  </acceptance_criteria>
  <done>legal.spec.ts committed; full phase behavior encoded; mentions/privacy tests RED as expected.</done>
</task>

<task type="auto">
  <name>Task 2: Build the bilingual mentions légales pages + footer legal nav (GREEN for mentions)</name>
  <files>src/pages/mentions-legales.astro, src/pages/en/mentions-legales.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/pages/about.astro (FR analog — exact wrapper/typography `<style>` block to copy verbatim, renaming `.about-page` → `.legal-page`; `.placeholder` italic pattern)
    - src/pages/en/about.astro (EN analog — note import depth `../../layouts/BaseLayout.astro`)
    - src/layouts/BaseLayout.astro (the file being modified — existing header nav, frontmatter href pattern, footer, and `.nav-link` style)
    - src/components/ContactForm.astro (the existing contact email alias `contact@atelierjacquelinesuzanne.fr` to reuse — do not invent a new address)
    - .planning/phases/04-legal-compliance/04-CONTEXT.md (D-07 OVH host, D-08 legal name, D-09 address/phone placeholder, D-10 business status wording — all RESOLVED, these are the source of truth for legal copy and OVERRIDE any stale UI-SPEC placeholder wording)
    - .planning/phases/04-legal-compliance/04-RESEARCH.md ("Hosting Disclosure (OVH)", "LCEN Identity Disclosure", Pitfalls 2/3/4)
  </read_first>
  <action>
    Create `src/pages/mentions-legales.astro` (FR) and `src/pages/en/mentions-legales.astro` (EN) using the About-page Pattern 3 (hardcoded `.astro`, no Sanity fetch), wrapped in `<BaseLayout title="...">`. Copy the About `<style>` block verbatim, renaming the wrapper selector to `.legal-page`. Keep the EN slug segment identical to FR (`mentions-legales`) — RESEARCH Pitfall 1. Lead each file's frontmatter comment with "LEGAL-01" and cite "LCEN Article 1-1" (NOT the superseded "Article 6-III", Pitfall 2).

    Section order (UI-SPEC Layout Notes, Mentions Légales):
    1. `<h1>` — FR "Mentions légales" / EN "Legal notice".
    2. `<h2>` "Éditeur du site" / "Site publisher" (Heading, non-italic Body copy): per D-08 disclose "Romane Lepont, exerçant sous le nom commercial « Atelier Jacqueline Suzanne »" (EN: "Romane Lepont, trading as « Atelier Jacqueline Suzanne »"). Directeur de la publication / Publication director: Romane Lepont. E-mail: contact@atelierjacquelinesuzanne.fr. Then, per D-09, an address line and a phone line rendered with the `.placeholder` italic class as clearly-marked pending fields (e.g. FR "Adresse : à compléter." / "Téléphone : à compléter." — EN equivalents) — do NOT fabricate a real address/phone, do NOT omit the fields silently.
    3. `<h2>` "Hébergement" / "Hosting" (Body copy): name OVH per D-07. Use these researched values but VERIFY them first (see below): OVH SAS, 2 rue Kellermann, 59100 Roubaix, France; RCS Lille Métropole 424 761 419; téléphone 09 55 00 66 33.
    4. `<h2>` "Statut" / "Status" (`.placeholder` italic line, provisional per UI-SPEC): use the D-10 RESOLVED wording exactly — FR "Statut : activité individuelle, non immatriculée — activité occasionnelle en dessous du seuil imposant l'immatriculation." / EN "Status: individual activity, not registered — occasional activity below the threshold requiring business registration." Do NOT use "SIRET en cours d'attribution" (that phrasing is superseded by D-10; the stale UI-SPEC Copywriting line still shows it — ignore it).

    Verification-before-write (both are automatable, no human needed):
    - WebFetch OVH's live mentions-légales / legal page (e.g. https://www.ovhcloud.com/fr/terms-and-conditions/) to confirm the OVH SAS address / RCS number / phone above are current; update the copy to whatever OVH currently publishes if they differ (RESEARCH Assumption A2).
    - WebFetch one French micro-entrepreneur/business-formation source to sanity-check that "non immatriculée — en dessous du seuil imposant l'immatriculation" is a defensible framing for an unregistered occasional activity (D-10 requested this gut-check). If the source contradicts the framing, keep D-10's wording but record the concern in the SUMMARY for the human-verify checkpoint (Plan 03) — do not silently rewrite a user-locked decision.

    Then edit `src/layouts/BaseLayout.astro`: in frontmatter add `const legalNoticeLabel = locale === 'en' ? 'Legal notice' : 'Mentions légales';` and `const legalNoticeHref = getRelativeLocaleUrl(locale, 'mentions-legales');` (following the existing aboutLabel/aboutHref shape). Extend the footer to add a `<nav class="footer-legal-nav" aria-label="Legal">` after the `<p class="footer-text">`, containing one `<a href={legalNoticeHref} class="nav-link">{legalNoticeLabel}</a>` (the Confidentialité link is appended in Plan 02, preserving order — legal notice first per UI-SPEC). Add a minimal `.footer-legal-nav` scoped style only for layout spacing (e.g. `display: flex; gap: var(--space-xl); margin-top: var(--space-md);` per UI-SPEC Footer notes) — do not duplicate `.nav-link` properties.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/legal.spec.ts -g "mentions" && npx playwright test tests/e2e/legal.spec.ts -g "switcher" -g "mentions"</automated>
  </verify>
  <acceptance_criteria>
    - `/mentions-legales/` renders with `html[lang=fr]`, `h1` "Mentions légales", and body text containing "Romane Lepont", "Atelier Jacqueline Suzanne", "OVH", and "activité individuelle, non immatriculée".
    - `/en/mentions-legales/` renders with `html[lang=en]`, `h1` "Legal notice", and contains "Romane Lepont" and "not registered".
    - The address and phone fields render with the `.placeholder` italic class (grep the source for `class="placeholder"` on the address/phone lines); no fabricated address or phone number is present.
    - Source comments cite "LCEN Article 1-1" and never "Article 6-III" (`grep -rn "6-III" src/pages/*mentions* src/pages/en/*mentions*` returns nothing).
    - `BaseLayout.astro` frontmatter contains `getRelativeLocaleUrl(locale, 'mentions-legales')` and the footer contains `class="footer-legal-nav"` with the localized legal-notice link.
    - The "mentions" e2e group and the mentions switcher assertions pass; footer "Mentions légales" link from `/` navigates to `/mentions-legales/`.
    - The EN page slug segment is exactly `mentions-legales` (file at `src/pages/en/mentions-legales.astro`), not `legal-notice`.
  </acceptance_criteria>
  <done>Both mentions pages live and reachable from every footer; mentions + mentions-switcher e2e green; OVH/status content verified against primary sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser ← static HTML | Prerendered legal content served with no runtime logic; no untrusted input crosses (these pages have no forms). |
| published legal copy ← source-of-truth decisions | The risk is factual: shipped legal disclosures must match reality (D-08/D-09/D-10 + verified OVH details). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | mentions-legales page identity/host/status content | mitigate | Disclose real legal name per D-08; verify OVH host block via WebFetch against OVH's live legal page before writing; use D-10-locked status wording; final human-verify gate in Plan 03. |
| T-04-02 | Information Disclosure (privacy) | address/phone field on mentions légales | mitigate | Per D-09 ship address/phone as a clearly-marked `.placeholder` italic pending field — never fabricate a value, never silently omit; tracked as a known follow-up. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero new packages (RESEARCH Package Legitimacy Audit: N/A); no supply-chain surface is added. |
</threat_model>

<verification>
- `npx playwright test tests/e2e/legal.spec.ts -g "mentions"` — green after Task 2.
- `npx playwright test tests/e2e/legal.spec.ts -g "switcher"` — mentions pair green (privacy pair still RED until Plan 02).
- `grep -rn "6-III" src/pages` returns nothing (Pitfall 2).
- `npm run build` succeeds (no broken import / prerender error).
</verification>

<success_criteria>
- Bilingual mentions légales pages live at `/mentions-legales/` and `/en/mentions-legales/` with accurate LCEN Article 1-1 identity, verified OVH host, and D-10 business-status disclosure.
- Footer legal nav present on every page with the localized Mentions légales / Legal notice link.
- Whole-phase e2e harness authored; mentions + mentions-switcher green; privacy/cookie/privacy-switcher RED pending Plan 02.
</success_criteria>

<output>
Create `.planning/phases/04-legal-compliance/04-01-SUMMARY.md` when done. Record any OVH-detail deltas found during verification and any D-10 phrasing concern for the Plan 03 checkpoint.
</output>
