---
phase: 09-progressive-homepage-image-loading
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: []
autonomous: false
requirements: [HOME-09]
must_haves:
  truths:
    - "A human has confirmed the 24px/blur(50) placeholder reads as a recognizable color/shape preview of each photo (not a solid blob) across at least 2-3 real gallery covers (D-01 'Verify live')."
    - "A human has confirmed clicking the carousel/grid toggle mid-fade produces no jarring visible glitch (or the rare, accepted cosmetic coincidence is noted, not over-fixed)."
  artifacts:
    - ".planning/phases/09-progressive-homepage-image-loading/09-02-SUMMARY.md — records the live-verification outcome (approve, or tuning applied)"
  key_links:
    - "This is the only phase step that closes D-01's explicit 'Verify live' note and RESEARCH Open Questions 1 and 2 — the automated e2e suite cannot judge placeholder legibility or the View-Transition timing coincidence."
---

<objective>
Close the two visual judgments HOME-09 cannot assert automatically: (1) the blur placeholder is a recognizable-but-cheap preview at the locked D-01 values, and (2) the blur-up crossfade does not visibly fight the shipped carousel↔grid View Transition when a toggle click coincides with an in-flight fade.

Purpose: The automated suite (Plan 01) proves the mechanism is wired correctly; only a human can confirm it *looks* right on real photos and reads as the intended quick, subtle dissolve.

Output: A recorded sign-off (approve as-is, or apply the small placeholder-width/blur tuning within D-01's guidance) and a note on whether any real View-Transition artifact was observed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-progressive-homepage-image-loading/09-CONTEXT.md
@.planning/phases/09-progressive-homepage-image-loading/09-UI-SPEC.md
@.planning/phases/09-progressive-homepage-image-loading/09-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live blur-up legibility + View-Transition coincidence sign-off</name>
  <files>(review only — no files modified unless a placeholder-tuning nudge is requested within D-01's `width`/`blur` guidance in `src/lib/image.ts`)</files>
  <read_first>
    - .planning/phases/09-progressive-homepage-image-loading/09-01-SUMMARY.md (any placeholder-width/timing note the executor recorded)
    - .planning/phases/09-progressive-homepage-image-loading/09-CONTEXT.md (D-01 "Verify live" note; D-06 timing)
    - .planning/phases/09-progressive-homepage-image-loading/09-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
    Present the running homepage to the human (Florian/Romane) and collect an explicit sign-off on the two visual judgments the e2e suite cannot make (placeholder legibility, View-Transition coincidence). Do NOT auto-approve — these are visual judgments on real photos only the human can make (this checkpoint is never auto-advanced regardless of yolo mode). If tuning is requested, adjust ONLY within D-01's guidance (nudge `width`/`blur` in `blurPlaceholderUrl()`, keeping width well under any source photo's smallest dimension — Pitfall 2) and never switch away from the two-layer opacity crossfade mechanism; then re-run `npx playwright test tests/e2e/homepage.spec.ts --project=chromium` green before recording sign-off.
  </action>
  <what-built>
    Plan 01 shipped the progressive-image-loading behavior end-to-end: a `blurPlaceholderUrl()` helper producing a 24px/blur(50) Sanity CDN preview (D-01), a two-layer opacity crossfade (~260ms, D-06) on the hero (every swap, with `fetchpriority="high"` and next-photo prefetch — D-02/D-04/D-05) and on the grid tiles (kept lazy — D-03), plus a `progressive image loading (HOME-09)` e2e block that is fully green. The two remaining checks are visual judgments the automated suite cannot make.
  </what-built>
  <how-to-verify>
    Run the site locally (`npm run dev`) and open the homepage (`/` and `/en/`).
    1. **Placeholder legibility (D-01 "Verify live", RESEARCH Open Question 1):**
       - On first load, watch the hero photo resolve from blurred to sharp. Then let the carousel auto-advance (or press ArrowRight / click a progress dash) and watch the swap for at least 2-3 different galleries (e.g. Silos, Brume).
       - Confirm the blurred placeholder reads as a recognizable *color/shape* preview of the actual photo — not a solid uniform blob, and not so sharp it defeats the "emerging photo" feel.
       - Toggle to grid mode and scroll; confirm the grid tiles show the same recognizable blur-up as they come into view.
       - If any placeholder reads as a flat blob, adjust ONLY within D-01's guidance (nudge `width`/`blur` in `blurPlaceholderUrl()`), keeping the width well under any source photo's smallest dimension (Pitfall 2 — do not approach source resolution), then re-verify. Do not switch mechanisms.
    2. **View-Transition coincidence (RESEARCH Open Question 2 / Pitfall 1):**
       - Immediately after a fresh gallery swap (while a blur-up might still be in flight), click the carousel/grid toggle.
       - Confirm there is no jarring flash/pop. If the hero appears to "snap" to full opacity as the morph finishes rather than dissolving, note it — but per the 07-CONTEXT.md D-10/D-12 precedent, do NOT add defensive gating for this rare, low-severity cosmetic coincidence unless a real, reproducible artifact is observed.
    Record the outcome (approved as-is / tuning applied / artifact noted) in the plan summary.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting any tuning applied or artifact observed), or describe issues to address.</resume-signal>
  <verify>Human types "approved" (or requested tuning is applied within D-01 guidance and the quick e2e suite re-run green). This checkpoint is blocking and never auto-approvable.</verify>
  <acceptance_criteria>
    - The human has explicitly signed off on placeholder legibility across 2-3 real gallery covers, in carousel and grid modes, on `/` and `/en/`, OR requested tuning has been applied within D-01 guidance and re-verified.
    - The human has confirmed no jarring toggle-mid-fade glitch (or the accepted cosmetic coincidence is noted, not over-fixed).
    - `09-02-SUMMARY.md` records the decision.
  </acceptance_criteria>
  <done>Live blur-up legibility and the View-Transition coincidence are human-confirmed (or tuning resolved) and recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- Human confirmation obtained for placeholder legibility across 2-3 real gallery covers, in both carousel and grid modes, on `/` and `/en/`.
- Human confirmation obtained that a toggle-click-mid-fade shows no jarring glitch (or the accepted coincidence is noted, not over-fixed).
- If tuning was applied, `npx playwright test tests/e2e/homepage.spec.ts --project=chromium` remains green afterward.
</verification>

<success_criteria>
- D-01's "Verify live" note and RESEARCH Open Questions 1 and 2 are resolved with a recorded human decision.
- No mechanism change (still the two-layer opacity crossfade); at most a placeholder width/blur nudge within D-01's guidance.
</success_criteria>

## Artifacts this phase produces

- **`.planning/phases/09-progressive-homepage-image-loading/09-02-SUMMARY.md`** — records the live-verification outcome. No source symbols are created by this plan unless a placeholder-tuning nudge is applied to `blurPlaceholderUrl()` during verification (in which case it stays within D-01's `width`/`blur` guidance).

<output>
Create `.planning/phases/09-progressive-homepage-image-loading/09-02-SUMMARY.md` when done.
</output>
