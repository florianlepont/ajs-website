---
phase: 04.1-design-system-homepage-refresh
plan: 05
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/components/GalleryCard.astro
  - src/components/GalleryGrid.astro
  - src/components/Lightbox.astro
  - src/components/LanguageSwitcher.astro
  - src/pages/galleries/[slug].astro
  - src/pages/en/galleries/[slug].astro
autonomous: true
requirements: [D-01, D-06, D-07]
user_setup: []

must_haves:
  truths:
    - "GalleryCard, GalleryGrid, Lightbox, and LanguageSwitcher render on the new identity with no hardcoded old hex values"
    - "The language switcher renders its links in pink and keeps the current-locale emphasis"
    - "Gallery-detail pages show a full-bleed hero photo with a dark gradient scrim and a white Display-role title, using the transparent header variant"
    - "No superseded Phase-2 hex values remain anywhere in src/"
  artifacts:
    - path: "src/pages/galleries/[slug].astro"
      provides: "Gallery-detail hero-scrim treatment (D-07), FR"
      contains: "linear-gradient"
    - path: "src/pages/en/galleries/[slug].astro"
      provides: "Gallery-detail hero-scrim treatment (D-07), EN"
    - path: "src/components/LanguageSwitcher.astro"
      provides: "Pink switcher links via var(--color-ink)/var(--color-accent), no hardcoded #1a1a1a"
  key_links:
    - from: "src/pages/galleries/[slug].astro"
      to: "BaseLayout headerVariant='transparent'"
      via: "prop"
      pattern: "headerVariant"
    - from: "repaint sweep"
      to: "old Phase-2 palette"
      via: "grep gate returns zero"
      pattern: "F0E7E4|E4D9D0|F92D97|141213"
---

<objective>
Finish the rebrand's repaint tail: token-sweep the four "repaint only" components (GalleryCard, GalleryGrid, Lightbox, LanguageSwitcher) and add the full-bleed-hero-photo + dark-gradient-scrim + white-title treatment (D-07) to both gallery-detail pages using the transparent header variant from Plan 04.1-01.

Purpose: Complete D-01 (no split identity) and D-06 (pink links) on the remaining shipped surfaces, and deliver D-07 (gallery-detail hero-scrim), keeping the visual language consistent with the homepage carousel.
Output: Repainted components + hero-scrim gallery-detail pages (FR/EN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-RESEARCH.md

<interfaces>
<!-- Tokens available after Plan 04.1-01. Replace hardcoded values with these. -->
- Colors: --color-dominant (#FFFFFF), --color-ink (#1A1A1A), --color-accent (#FF3B94), --color-on-accent, --color-border
- Typography: --text-display-size (32px), --weight-regular (400), --weight-semibold (600), --font-display, --text-*-size/leading
- BaseLayout `headerVariant` prop: 'solid' | 'transparent' | 'none' (from Plan 04.1-01). Gallery-detail uses 'transparent' (white logo over scrim, white nav, absolutely positioned).

Repaint targets (current hardcoded values to replace):
- src/components/LanguageSwitcher.astro: `.switcher-link { color: #1a1a1a }`, `.switcher-link:focus-visible { outline: 2px solid #1a1a1a }`, `.switcher-separator { color: #1a1a1a }` (three hardcoded hex; Pitfall 3). Per D-06 links render pink: switcher links → `var(--color-accent)`, separator → `var(--color-ink)`, focus ring → single `var(--color-accent)` ring. Keep `.is-current` emphasis (weight 600 + underline). font-weight already 400/600 — fine.
- src/components/GalleryCard.astro: `.gallery-card__title` uses `clamp(2.5rem, 12cqi, 6.5rem)` + `font-weight: 900` → convert to fixed Display role (`var(--text-display-size)` 32px, `var(--weight-semibold)`, `var(--font-display)`, line-height 1.2), drop `container-type`/`cqi`. Panel keeps `var(--color-accent)` background with `var(--color-ink)` title (pink is functional; allowed). Focus ring: replace the old ink+accent box-shadow double ring with a single `var(--color-accent)` ring (matches BaseLayout's new focus style).
- src/components/GalleryGrid.astro: reads `var(--color-dominant)` only — verify no hardcoded old hex; likely no change beyond confirmation.
- src/components/Lightbox.astro: repaint any scrim/overlay/control colors to tokens; keep the native `<dialog>` architecture UNCHANGED (CONTEXT.md forbids replacing it) and keep the `‹ › ✕` glyphs.

Gallery-detail hero-scrim (D-07), both src/pages/galleries/[slug].astro and src/pages/en/galleries/[slug].astro — current header block is `.gallery-detail__header` (a pink inline-block panel with a clamp() h1). Replace with the prototype's treatment (Atelier Homepage.dc.html lines 130-141): `height:70vh; min-height:420px` hero section, full-bleed cover photo (images[0] via fullSizeUrl) + the dark gradient scrim (`linear-gradient(to top, rgba(0,0,0,0.65) 0%, rgba(0,0,0,0.15) 45%, rgba(0,0,0,0) 70%)`), a "← Retour aux galeries"/"← Back to galleries" back-link, and the project title as an absolutely-positioned bottom-left `<h1>` (Display role, fixed 32px/600, white text). Invoke `<BaseLayout ... headerVariant="transparent">`. Artist statement + thumbnail grid + Lightbox below stay structurally unchanged (repaint tokens only). fullSizeUrl/thumbnailUrl live in src/lib/image.ts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token-sweep GalleryCard, GalleryGrid, Lightbox, LanguageSwitcher (D-01/D-06)</name>
  <files>src/components/GalleryCard.astro, src/components/GalleryGrid.astro, src/components/Lightbox.astro, src/components/LanguageSwitcher.astro</files>
  <read_first>
    - src/components/LanguageSwitcher.astro (three hardcoded #1a1a1a, is-current emphasis)
    - src/components/GalleryCard.astro (clamp Display title, focus ring, pink panel)
    - src/components/GalleryGrid.astro (dominant background)
    - src/components/Lightbox.astro (scrim/control colors; native <dialog> — do NOT change architecture)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Color link states, Typography fixed Display, D-05 decorative-palette-not-here)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-RESEARCH.md (Pitfall 3 hardcoded hex, Anti-Patterns: don't replace Lightbox dialog)
  </read_first>
  <action>
    LanguageSwitcher.astro (D-06 / Pitfall 3): replace the three hardcoded `#1a1a1a` values — `.switcher-link` color → `var(--color-accent)` (pink links), `.switcher-separator` color → `var(--color-ink)`, `.switcher-link:focus-visible` outline → single `2px solid var(--color-accent)` ring (drop any ink ring). Keep `.is-current` weight-600 + underline emphasis.
    GalleryCard.astro: convert `.gallery-card__title` from `clamp(2.5rem, 12cqi, 6.5rem)` + `font-weight: 900` to fixed Display (`font-size: var(--text-display-size)`, `font-weight: var(--weight-semibold)`, `font-family: var(--font-display)`, `line-height: 1.2`), remove `container-type: inline-size`/cqi. Replace the `:focus-visible` double ring (`outline: 2px solid var(--color-ink); box-shadow: 0 0 0 4px var(--color-accent)`) with a single `outline: 2px solid var(--color-accent); outline-offset: 2px` and drop the box-shadow. Keep the pink `.gallery-card__panel` background + ink title (pink is functional, allowed) and the scale(1.02) hover.
    GalleryGrid.astro: confirm it references only `var(--color-dominant)`; if any hardcoded old hex exists, replace with the token. Otherwise leave as-is.
    Lightbox.astro: replace any hardcoded scrim/overlay/control colors with tokens (ink/white/rgba as appropriate); keep the native `<dialog>`/`showModal()` architecture, focus containment, keyboard/touch handlers, and `‹ › ✕` glyphs unchanged. Update any font-weight 300/900 to var(--weight-regular)/var(--weight-semibold).
    Across all four files, replace any surviving `font-weight: 300` with `var(--weight-regular)` and `font-weight: 900` with `var(--weight-semibold)` (rebrand weight shift, UI-SPEC Typography).
  </action>
  <verify>
    <automated>npm run build && ! grep -rEq "#1a1a1a|#141213|F0E7E4|E4D9D0|F92D97" src/components/GalleryCard.astro src/components/GalleryGrid.astro src/components/Lightbox.astro src/components/LanguageSwitcher.astro && grep -q "var(--color-accent)" src/components/LanguageSwitcher.astro && ! grep -q "12cqi" src/components/GalleryCard.astro && npx playwright test tests/e2e/gallery.spec.ts tests/e2e/i18n.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -riE "#1a1a1a|#141213|F0E7E4|E4D9D0|F92D97" src/components/{GalleryCard,GalleryGrid,Lightbox,LanguageSwitcher}.astro` returns no matches
    - LanguageSwitcher `.switcher-link` color is `var(--color-accent)`; `.is-current` emphasis preserved
    - `grep -c "12cqi\|clamp(" src/components/GalleryCard.astro` returns 0 (Display fixed 32px)
    - Lightbox still uses `<dialog>` (`grep -q "dialog" src/components/Lightbox.astro`) — architecture unchanged
    - `npx playwright test tests/e2e/gallery.spec.ts tests/e2e/i18n.spec.ts` passes (lightbox + switcher behavior intact)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The four repaint-only components render on the new identity with token-based colors, pink switcher links, fixed Display sizing, an unchanged Lightbox dialog, and green gallery/i18n e2e.</done>
</task>

<task type="auto">
  <name>Task 2: Gallery-detail hero-scrim + transparent header (D-07), FR and EN</name>
  <files>src/pages/galleries/[slug].astro, src/pages/en/galleries/[slug].astro</files>
  <read_first>
    - src/pages/galleries/[slug].astro (FR — .gallery-detail__header pink panel + clamp h1, getStaticPaths, Lightbox wiring)
    - src/pages/en/galleries/[slug].astro (EN mirror)
    - src/lib/image.ts (fullSizeUrl for the hero, thumbnailUrl for thumbs)
    - src/layouts/BaseLayout.astro (headerVariant='transparent' behavior from Plan 04.1-01)
    - .planning/design-import/homepage-prototype/Atelier Homepage.dc.html (lines 128-151 gallery-detail hero-scrim reference)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Layout Notes → Gallery-detail hero-scrim)
  </read_first>
  <action>
    In both gallery-detail pages, invoke `<BaseLayout title={...} headerVariant="transparent">`. Replace the `.gallery-detail__header` pink inline-block panel with a hero section: `height:70vh; min-height:420px; position:relative`, a full-bleed cover `<img>` (images[0] via `fullSizeUrl`, object-fit cover) + an absolutely-positioned dark gradient scrim (`linear-gradient(to top, rgba(0,0,0,0.65) 0%, rgba(0,0,0,0.15) 45%, rgba(0,0,0,0) 70%)`, pointer-events none), and the project title as an absolutely-positioned bottom-left `<h1>` (Display role: `var(--text-display-size)` 32px, `var(--weight-semibold)`, `var(--font-display)`, white text, padding `var(--space-xl)`). Add the back-link ("← Retour aux galeries" FR / "← Back to galleries" EN) positioned top-left over the scrim in white. Keep `getStaticPaths`, the artist statement paragraph, the thumbnail `GalleryGrid`, and the `<Lightbox>` wiring intact below the hero — repaint their colors to tokens only (statement text ink, back-link at the bottom can be removed or kept; the hero back-link replaces the old bottom "Retour aux galeries" link — keep exactly one back affordance). Convert any `clamp()`/font-weight 900 Display usages to the fixed 32px/600 role and drop stray `container-type`.
  </action>
  <verify>
    <automated>npm run build && grep -q 'headerVariant="transparent"' src/pages/galleries/[slug].astro && grep -q 'headerVariant="transparent"' src/pages/en/galleries/[slug].astro && grep -q "linear-gradient" src/pages/galleries/[slug].astro && grep -q "fullSizeUrl" src/pages/galleries/[slug].astro && npx playwright test tests/e2e/gallery.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both `[slug].astro` pages invoke `<BaseLayout ... headerVariant="transparent">`
    - Both render a full-bleed hero `<img>` (via `fullSizeUrl`) + the exact dark gradient scrim + a white bottom-left Display `<h1>`
    - Both include a single localized back-link over the hero ("← Retour aux galeries" / "← Back to galleries")
    - `grep -c "gallery-detail__header\|12cqi" src/pages/galleries/[slug].astro` returns 0 (old pink-panel header removed)
    - `npx playwright test tests/e2e/gallery.spec.ts` passes (statement differs fr/en; lightbox opens from thumbnails)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Gallery-detail pages show the immersive hero-scrim with a white title under a transparent header, consistent with the homepage carousel, and the gallery e2e stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build (Sanity fetch) → static gallery-detail HTML | Hero image URLs are built at build time via `src/lib/image.ts`; no client-side data fetch introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05-NA | — | repaint + gallery-detail markup | accept | Pure presentation change (CSS tokens + hero markup). No new input surface, endpoint, auth, crypto, or dependency; the Lightbox `<dialog>` security-relevant focus containment is explicitly left unchanged. No mitigation required. |
</threat_model>

<verification>
- `npm run build` succeeds.
- `npm run test:e2e` fully green (gallery + i18n + lightbox unaffected).
- Phase-wide grep gate: `grep -rniE "F0E7E4|E4D9D0|F92D97|#141213|#1a1a1a|color-destructive" src/` returns zero matches (whole-src confirmation the rebrand is complete; `#1a1a1a` may appear only as the intentional dark backdrop-chip literal in BaseLayout's logo hover — exclude BaseLayout if so, otherwise expect zero).
- Manual: confirm the gallery-detail hero title reads legibly white over the scrim.
</verification>

<success_criteria>
- The four repaint-only components render on the new identity with pink switcher links and fixed Display sizing.
- Gallery-detail pages show the hero-scrim + white title under the transparent header.
- No superseded Phase-2 palette values remain in src/.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-homepage-refresh/04.1-05-SUMMARY.md` when done.
</output>
