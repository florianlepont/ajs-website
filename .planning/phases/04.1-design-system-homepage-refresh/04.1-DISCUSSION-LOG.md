# Phase 04.1: Design System & Homepage Refresh - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-08
**Phase:** 04.1-design-system-homepage-refresh
**Areas discussed:** Rebrand scope, Homepage interaction model, Logo & imagery, New form components

---

## Rebrand scope

| Option | Description | Selected |
|--------|-------------|----------|
| Full site now | Rebrand all already-shipped pages immediately | ✓ |
| Homepage-first, rest later | Only new pages get the new identity now | |
| You decide | Planner sizes it based on effort/risk | |

**User's choice:** Full site now
**Notes:** One consistent visual identity everywhere immediately, avoids a visually split site before launch.

| Question | Options | Selected |
|---|---|---|
| Dormant Delight `@font-face` | Remove it / Keep it dormant | Remove it |
| Form error style | Adopt ink-only (no red) / Keep red | Adopt ink-only error style |
| `--color-secondary` chrome band | Remove — hairline border everywhere / Keep for other uses | Remove it |
| Decorative accent rotation scope | Homepage carousel only / Extend elsewhere | Homepage carousel only |
| Link text treatment | Full pink link text / Keep ink text + pink underline | Full pink link text |
| Hero gradient scrim on gallery-detail | Yes, adopt there too / Homepage only | Yes, adopt on gallery-detail too |

---

## Homepage interaction model

| Option | Description | Selected |
|--------|-------------|----------|
| Both, with toggle | Carousel + grid, matches prototype exactly | ✓ |
| Carousel only | Simpler, drop the grid toggle | |
| Grid only | Simpler, drop the carousel | |

**User's choice:** Both, with toggle

| Question | Options | Selected |
|---|---|---|
| Carousel autoplay | Manual only / Auto-advance with pause-on-hover | Auto-advance with pause-on-hover |
| Mobile carousel layout | Stack vertically, full-width panel / You decide | You decide (Claude's Discretion) |

---

## Logo & imagery

| Question | Options | Selected |
|---|---|---|
| Adopt AJS wordmark logo | Yes / Keep text-only header | Yes, adopt the wordmark |
| Unmigrated-gallery hero photos | Only show migrated galleries / Show all with placeholders | Only show migrated galleries (Silos, Brume) |
| Logo usage scope | Header / Favicon / Footer (multiSelect) | Header only |

**Notes:** User provided freeform follow-up (not a listed option): the current live site (atelierjacquelinesuzanne.fr) has a logo hover effect that inverts colors by swapping between two logo image variants, and asked to preserve it. Confirmed via WebFetch that the live site does use two logo images in its header (consistent with an image-swap hover). Claude matched this to the imported asset pack's `AJS_Brutalist_Black_Transparent.png` / `AJS_Brutalist_White_Transparent.png` pair — captured as D-13.

---

## New form components

| Question | Options | Selected |
|---|---|---|
| Refactor ContactForm.astro | Yes, refactor now / Leave as-is | Yes, refactor now |
| Button's first real usage | Contact form submit button / Add but don't use yet | Contact form submit button |
| Galleries empty-state refactor | Yes, refactor it / Leave as-is | Yes, refactor it |

---

## Claude's Discretion

- Mobile responsive behavior for the hero carousel layout (D-10).
- Exact pixel/spacing translation from the prototype's inline styles into real component CSS.
- Whether homepage intro/CTA copy reuses existing Sanity `siteSettings` fields or is hardcoded — not explicitly discussed.

## Deferred Ideas

- Logo usage in favicon and footer (only header adopted this phase).
- Migrating the remaining 4 unmigrated galleries' real photos — pre-existing Phase 2 deferred item, not re-opened here.
