---
phase: 04.1-design-system-homepage-refresh
plan: 03
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/components/Button.astro
  - src/components/Input.astro
  - src/components/Textarea.astro
  - src/components/EmptyState.astro
  - src/components/ContactForm.astro
  - src/pages/galleries/index.astro
  - src/pages/en/galleries/index.astro
autonomous: true
requirements: [D-03, D-14, D-15, D-16]
user_setup: []

must_haves:
  truths:
    - "Reusable Button, Input, Textarea, and EmptyState components exist, translated from the design-import references into plain Astro"
    - "The contact form renders via the new Input/Textarea/Button components and still validates + submits + blocks honeypot exactly as before"
    - "Form error states use bold ink text + a thicker ink border — no red (#dc2626) anywhere"
    - "The galleries listing empty state renders through the EmptyState component with the existing FR/EN copy"
  artifacts:
    - path: "src/components/Button.astro"
      provides: "Primary/secondary button, first used by the contact form submit"
    - path: "src/components/Input.astro"
      provides: "Labelled text input with ink-only error state"
    - path: "src/components/Textarea.astro"
      provides: "Labelled textarea with ink-only error state"
    - path: "src/components/EmptyState.astro"
      provides: "Heading + body empty-state block"
    - path: "src/components/ContactForm.astro"
      provides: "Refactored form using the new primitives, ink-only errors, unchanged validation/submit logic"
  key_links:
    - from: "src/components/ContactForm.astro"
      to: "src/lib/contact-form.ts"
      via: "unchanged import of isBlank/isValidEmail/isHoneypotTriggered"
      pattern: "isHoneypotTriggered"
    - from: "src/components/ContactForm.astro <script>"
      to: "form-status error styling"
      via: "CSS class toggle (not style.color = var(--color-destructive))"
      pattern: "contact-form__status--error"
---

<objective>
Create the four new presentational components (Button, Input, Textarea, EmptyState) by translating the design-import `.jsx` references into plain Astro, then give them their first real usages: refactor `ContactForm.astro` onto Input/Textarea/Button with the D-03 ink-only error convention, and refactor the galleries listing empty state onto EmptyState. Validation/honeypot logic must not regress.

Purpose: Deliver reusable form/feedback primitives and remove the last `--color-destructive` references from the codebase (D-03, D-14, D-15, D-16).
Output: Four new components plus refactored `ContactForm.astro` and both galleries listing pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-RESEARCH.md

<interfaces>
<!-- Token names available after Plan 04.1-01. Components read these; no hardcoded hex. -->
- Colors: --color-dominant, --color-ink, --color-accent, --color-on-accent, --color-border
- Typography: --font-sans, --font-display, --weight-regular (400), --weight-semibold (600), --text-label-size/weight/leading, --text-body-size/weight/leading, --text-heading-size/weight/leading/family
- Spacing/shape: --space-xs..3xl, --radius-none, --radius-sm, --border-hairline (1px), --border-focus (2px), --tap-target-min (44px)

Component contracts (translate from .planning/design-import/design-system/components/):
- Button.astro props: variant ('primary'|'secondary'=primary), size ('sm'|'md'=md), disabled (bool), type ('button'|'submit'|'reset'='button'). Must forward arbitrary attributes (e.g. data-role) via `...rest` so the contact form can attach data-role="submit-button". Primary: fill var(--color-accent), text var(--color-on-accent), 1px accent border. Secondary: transparent fill, accent text/border. Hover (non-disabled): opacity 0.8. Disabled: opacity 0.4, cursor not-allowed. min-height var(--tap-target-min); radius var(--radius-sm). <slot /> for label.
- Input.astro props: label, id, name, type ('text' default), required (bool), error (string?). Renders label + input + optional `<p role="alert">` error. Default border var(--border-hairline) solid var(--color-border). Error: border var(--border-focus) solid var(--color-ink) + bold ink error text. No red. Must forward extra attributes to the <input>. font-size var(--text-body-size) (16px — avoids iOS zoom).
- Textarea.astro: same as Input plus rows (default 5), resize: vertical, min-height for multi-line.
- EmptyState.astro props: heading, body. Centered block, heading = Heading role (var(--text-heading-*), var(--font-display)), body = Body role. Do NOT bake in the .jsx's French-only default copy — heading/body come from props.

Existing ContactForm.astro (src/components/ContactForm.astro): data-role attributes the <script> queries — data-role="contact-form", "submit-button", "error-name"/"error-email"/"error-message", "honeypot", "form-status". The <script> imports isBlank/isHoneypotTriggered/isValidEmail from '../lib/contact-form' (UNCHANGED). renderSubmissionError() currently does `statusEl.style.color = 'var(--color-destructive)'` (line ~134) and renderSuccess() does `style.color = 'var(--color-ink)'` — these + the <style> block's `.contact-form__error { color: var(--color-destructive) }` (line ~254) and `.contact-form__input border: 1px solid var(--color-secondary)` are the D-03/Pitfall-2 targets.

Existing galleries listing empty-state markup: src/pages/galleries/index.astro (lines 25-31, `<div class="empty-state">` with h1 "Galeries à venir" + p) and src/pages/en/galleries/index.astro ("Galleries coming soon"). Copy is verbatim-reused via EmptyState.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Button, Input, Textarea, EmptyState components</name>
  <files>src/components/Button.astro, src/components/Input.astro, src/components/Textarea.astro, src/components/EmptyState.astro</files>
  <read_first>
    - .planning/design-import/design-system/components/ (Button.jsx, Input.jsx, Textarea.jsx, EmptyState.jsx and their .prompt.md companions)
    - .planning/design-import/design-system/tokens/spacing.css, typography.css, colors.css
    - src/components/ContactForm.astro (current field/label/input styling to match roles)
    - src/components/LanguageSwitcher.astro (Astro component + scoped-style precedent)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-RESEARCH.md (Pattern 2, Code Examples for EmptyState/Input)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Component Inventory, Typography role assignment)
  </read_first>
  <behavior>
    - Button primary renders `background: var(--color-accent)`, `color: var(--color-on-accent)`, min-height 44px; disabled sets cursor not-allowed + opacity 0.4.
    - Button forwards a `data-role` attribute passed by the caller onto the rendered `<button>`.
    - Input in error state applies a 2px ink border and renders a `role="alert"` element with bold ink text; no red is present.
    - EmptyState renders the `heading` and `body` props (not hardcoded copy).
  </behavior>
  <action>
    Create `src/components/Button.astro`, `Input.astro`, `Textarea.astro`, `EmptyState.astro` as plain Astro components per the contracts in the interfaces block. Translate the design-import `.jsx` references mechanically: props → `Astro.props`, inline `style` objects → scoped `<style>` blocks reading the CSS custom properties from Plan 04.1-01, `{children}` → `<slot />`, `{...rest}` → Astro attribute spreading so extra attributes (notably `data-role`) reach the underlying element. Do NOT import React or any `.jsx`, and do NOT reference `_ds_bundle.js`/`support.js`/`image-slot.js` (RESEARCH.md Pitfall 5). No `client:*` directive — these are inert presentational components. Use `var(--radius-sm)` for Button/Input/Textarea and `var(--radius-none)` nowhere in these three; error borders use `var(--border-focus) solid var(--color-ink)` (ink-only, D-03). No hardcoded hex.
  </action>
  <verify>
    <automated>npm run build && for f in Button Input Textarea EmptyState; do test -f src/components/$f.astro || exit 1; done && ! grep -rniq "dc2626\|color-destructive\|react\|from 'react'" src/components/Button.astro src/components/Input.astro src/components/Textarea.astro src/components/EmptyState.astro</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under src/components/
    - `grep -c "color-destructive\|dc2626" src/components/Input.astro src/components/Textarea.astro` returns 0
    - Button.astro spreads extra props so `data-role` reaches the `<button>` (e.g. an `{...rest}` / attribute spread is present)
    - Input.astro/Textarea.astro error state uses `var(--border-focus)` + `var(--color-ink)` (grep matches) and a `role="alert"` element
    - EmptyState.astro renders `Astro.props` heading/body, not a hardcoded French default
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Four reusable, dependency-free Astro components exist, all reading Plan 04.1-01 tokens, with ink-only error styling and prop forwarding, and the build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ContactForm onto Input/Textarea/Button with ink-only errors (D-03/D-14/D-15)</name>
  <files>src/components/ContactForm.astro</files>
  <read_first>
    - src/components/ContactForm.astro (full file — markup, <script>, <style>)
    - src/lib/contact-form.ts (isBlank/isValidEmail/isHoneypotTriggered — must stay unchanged)
    - src/components/Input.astro, Textarea.astro, Button.astro (from Task 1)
    - tests/e2e/contact.spec.ts (the regression contract this refactor must keep green)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Layout Notes → Contact form; Copywriting Contract error strings)
  </read_first>
  <action>
    Replace the hand-rolled `<input>`/`<textarea>`/`<button>` markup in ContactForm.astro with the new Input, Textarea, and Button components (Button primary variant for submit — D-15's first real Button usage). Preserve every `data-role` the `<script>` queries: pass `data-role="submit-button"` to Button, and keep the per-field error `<p data-role="error-name|error-email|error-message" role="alert">` and `data-role="form-status"` elements resolving against whatever the new components render (attach data-role via prop forwarding or keep the error `<p>`s as sibling markup owned by ContactForm, not inside Input). Keep the honeypot field, all `data-*` localized-copy attributes on the `<form>`, and the entire `<script>` validation/submit flow behaviorally identical — the import of `isBlank`/`isValidEmail`/`isHoneypotTriggered` from `../lib/contact-form` is unchanged (V5 Input Validation must not regress).
    Apply D-03 ink-only errors: in the `<style>` block replace `.contact-form__error { color: var(--color-destructive) }` with bold ink (`color: var(--color-ink); font-weight: var(--weight-semibold)`), and give error inputs the thicker ink border. In the `<script>` (Pitfall 2), replace `renderSubmissionError()`'s `statusEl.style.color = 'var(--color-destructive)'` with a CSS class toggle: add/remove a `.contact-form__status--error` class (bold ink text) on the status element, and clear it in `renderSuccess()`; update the `.contact-form__input`/`.contact-form__textarea` border from `1px solid var(--color-secondary)` to `var(--border-hairline) solid var(--color-border)`. Update remaining font-weight 300/900 values in this file to var(--weight-regular)/var(--weight-semibold). After the refactor, no `--color-destructive` reference may survive in the file's markup, `<style>`, or `<script>`.
  </action>
  <verify>
    <automated>npm run build && grep -c "color-destructive" src/components/ContactForm.astro | grep -q '^0$' && grep -q "contact-form__status--error" src/components/ContactForm.astro && npx playwright test tests/e2e/contact.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "color-destructive" src/components/ContactForm.astro` returns 0 (markup + style + script)
    - ContactForm imports/uses Input, Textarea, and Button components
    - The `<script>` still imports `isBlank`, `isValidEmail`, `isHoneypotTriggered` from `../lib/contact-form` (unchanged)
    - Submission-error styling is applied via a `.contact-form__status--error` class toggle, not `style.color`
    - `npx playwright test tests/e2e/contact.spec.ts` passes (honeypot, per-field validation, submit flow intact)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The contact form renders through the new primitives, shows ink-only errors with no red, keeps its validation/honeypot/submit behavior, and the existing contact e2e stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Adopt EmptyState on the galleries listing (D-16) + repaint listing Display</name>
  <files>src/pages/galleries/index.astro, src/pages/en/galleries/index.astro</files>
  <read_first>
    - src/pages/galleries/index.astro (FR — empty-state block lines 25-31, .empty-state + h1 styles)
    - src/pages/en/galleries/index.astro (EN mirror)
    - src/components/EmptyState.astro (from Task 1)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Copywriting Contract empty-state strings; Typography — Display is fixed 32px, not clamp())
  </read_first>
  <action>
    In `src/pages/galleries/index.astro`, replace the inline `<div class="empty-state">…</div>` branch with `<EmptyState heading="Galeries à venir" body="De nouvelles œuvres seront bientôt visibles ici. Revenez prochainement." />` (verbatim existing FR copy — D-16). Do the same in `src/pages/en/galleries/index.astro` with the existing EN copy ("Galleries coming soon" / "New work will appear here shortly. Check back soon."). Import EmptyState and delete the now-unused `.empty-state` CSS. While in these files, convert the `.galleries-page h1` Display treatment from `clamp(2.5rem, 12cqi, 6.5rem)` + `font-weight: 900` to the fixed Display role (`font-size: var(--text-display-size)` = 32px, `font-weight: var(--weight-semibold)`, `font-family: var(--font-display)`, `line-height: 1.2`), removing the `container-type`/`cqi` clamp mechanism (UI-SPEC Typography: Display is a site-wide fixed 32px in this identity).
  </action>
  <verify>
    <automated>npm run build && grep -q "EmptyState" src/pages/galleries/index.astro && grep -q "EmptyState" src/pages/en/galleries/index.astro && ! grep -q "12cqi" src/pages/galleries/index.astro && ! grep -q "12cqi" src/pages/en/galleries/index.astro && npx playwright test tests/e2e/gallery.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both galleries listing pages import and render `<EmptyState ... />` with the existing FR/EN copy
    - The inline `.empty-state` div + its CSS are removed from both files
    - `grep -c "12cqi\|clamp(" src/pages/galleries/index.astro` returns 0 (Display now fixed 32px)
    - `npx playwright test tests/e2e/gallery.spec.ts` passes (listing still renders cards)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The galleries listing empty state renders through EmptyState with unchanged copy, the listing Display heading uses the fixed 32px role, and the gallery e2e stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → contact form | Untrusted name/email/message input + honeypot cross into client-side validation before the Web3Forms fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-03-IV | Tampering / Spoofing | ContactForm validation + honeypot | mitigate | The refactor changes markup only; `src/lib/contact-form.ts` (isBlank/isValidEmail/isHoneypotTriggered) is untouched and the honeypot short-circuit still runs before any network call. Regression-guarded by `tests/e2e/contact.spec.ts` (V5 Input Validation, RESEARCH.md Security Domain). |
| T-04.1-03-NA | — | Button/Input/Textarea/EmptyState | accept | Inert presentational components, no input handling of their own, no new dependency. No mitigation required. |
</threat_model>

<verification>
- `npm run build` succeeds.
- `npx playwright test tests/e2e/contact.spec.ts tests/e2e/gallery.spec.ts` green.
- `grep -rn "color-destructive" src/` — after this plan, the only remaining source of this token is gone (BaseLayout cleared in 04.1-01, ContactForm cleared here); expect zero matches across `src/`.
</verification>

<success_criteria>
- Button, Input, Textarea, EmptyState exist and are used (contact form, galleries listing).
- Contact form validates/submits/honeypots unchanged with ink-only errors and no red.
- `--color-destructive` is fully eliminated from `src/`.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-homepage-refresh/04.1-03-SUMMARY.md` when done.
</output>
