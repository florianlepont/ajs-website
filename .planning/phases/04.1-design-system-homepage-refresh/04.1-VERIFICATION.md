---
phase: 04.1-design-system-homepage-refresh
verified: 2026-07-10T15:45:00Z
status: human_needed
score: 15/15 must-haves verified
overrides_applied: 0
human_verification:
  - test: "EmptyState renders correctly on the galleries listing page"
    expected: "Heading + body render via the new EmptyState component with existing FR/EN copy when getGalleries() returns zero entries"
    why_human: "Not reachable via real content today — both migrated galleries (Silos, Brume) plus the newly-live Paysage gallery are all published; would require temporarily unpublishing all gallery documents in Sanity Studio to observe. Flagged as manual-only in 04.1-VALIDATION.md itself; component code was verified to exist, be wired, and use prop-driven (not hardcoded) copy, but the actual rendered visual was not observed."
  - test: "D-13 logo hover-invert backdrop-chip visual mechanics, compared against the current live site"
    expected: "04.1-UI-SPEC.md explicitly calls this out as an unverified assumption (RESEARCH.md Open Question 1 / Assumption A3): 'before shipping, do a 30-second check of atelierjacquelinesuzanne.fr's current header hover behavior and adjust the chip's size/color only if it visibly conflicts.' No SUMMARY.md across all 6 plans mentions this check was performed."
    why_human: "Requires opening the live external site and visually comparing hover behavior — not verifiable via grep/static analysis. The implemented fallback (dark ink backdrop chip + PNG cross-fade) is code-complete and functionally sound regardless, but the explicit pre-ship visual-parity check from the UI-SPEC was not confirmed as done."
---

# Phase 04.1: Design System & Homepage Refresh Verification Report

**Phase Goal:** The site adopts the imported design system's monochrome + pop-pink visual identity (superseding Phase 2's Dawn Pink/Wild Strawberry palette) and the homepage becomes a real hero-carousel/grid-toggle gallery entry point, replacing the current placeholder "under construction" page.
**Verified:** 2026-07-10T15:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Full-site rebrand: white/#FFFFFF, ink/#1A1A1A, pop-pink/#FF3B94 identity replaces the old Dawn Pink/Woodsmoke/Wild Strawberry palette everywhere, not just new work (D-01) | ✓ VERIFIED | `grep -rniE "F0E7E4\|E4D9D0\|F92D97\|#141213\|color-destructive\|font-weight:\s*900\|font-weight:\s*300\|clamp\(" src/` returns zero matches. `BaseLayout.astro` `:root` block defines the new token set. |
| 2 | About, both legal pages, Contact, and 404 (already-shipped pages that expanded the D-01 scope across 3 plan-checker revision rounds) are actually repainted, not just referenced | ✓ VERIFIED | Directly inspected all 9 files (about.astro×2, mentions-legales.astro×2, confidentialite.astro×2, contact.astro×2, 404.astro) — all use `var(--text-display-size)`/`var(--text-heading-size)`/`var(--text-body-size)`/`var(--weight-semibold)`/`var(--weight-regular)`; 404.astro's link color is `var(--color-accent)`, not a hardcoded ink hex. |
| 3 | Links render as full pink text site-wide, not ink-text-plus-underline (D-06) | ✓ VERIFIED | `BaseLayout.astro` global `a { color: var(--color-accent); }`; `LanguageSwitcher.astro` line 42 confirms pink switcher links; 404.astro fixed in Plan 06. |
| 4 | Header/footer are white-on-white, delineated only by a 1px hairline border (D-04) | ✓ VERIFIED | `--color-secondary: var(--gray-0)` (alias of dominant); `.site-header--solid { background-color: var(--color-dominant); border-bottom: 1px solid var(--color-border); }`; footer mirrors with `border-top`. |
| 5 | Real AJS wordmark PNG logo in header, hover-inverts black↔white (D-11/D-13) | ✓ VERIFIED | `BaseLayout.astro` renders both PNGs with a `.logo-mark__chip` dark backdrop and opacity cross-fade on `:hover`/`:focus-within`. Both asset files exist on disk (`public/logos/AJS_Brutalist_{Black,White}_Transparent.png`). |
| 6 | Archivo Black self-hosted (no Google Fonts CDN call), dormant Delight @font-face removed (D-02) | ✓ VERIFIED | `import '@fontsource/archivo-black'` in BaseLayout frontmatter; `npm view @fontsource/archivo-black` independently confirms a legitimate package (`github.com/fontsource/font-files`, v5.2.8, matches package.json). `grep -c "Delight"` returns 0. |
| 7 | headerVariant prop (solid/transparent/none) exists and is consumed by downstream pages | ✓ VERIFIED | `BaseLayout.astro` Props interface + conditional header render; `galleries/[slug].astro` passes `headerVariant="transparent"`; `index.astro` passes `headerVariant="none"`. |
| 8 | Decorative secondary palette is homepage-hero-carousel-only, never used elsewhere (D-05) | ✓ VERIFIED | `grep -rln "palette-purple\|palette-teal\|palette-lime\|palette-plum" src/` returns only `BaseLayout.astro` (token definition) and `HomeCarousel.astro` (consumption). No other file references it. |
| 9 | Ink-only error convention, `--color-destructive`/#dc2626 fully removed (D-03) | ✓ VERIFIED | `grep -rn "color-destructive\|#dc2626" src/` returns zero matches. `ContactForm.astro` uses a `.contact-form__status--error` class toggle instead of the old inline `style.color` assignment. |
| 10 | New Button/Input/Textarea/EmptyState components exist with real first usages (D-14/D-15/D-16) | ✓ VERIFIED | All 4 files exist in `src/components/`. `ContactForm.astro` imports and uses Input/Textarea/Button. `galleries/index.astro` (FR+EN) import and render `<EmptyState>` with prop-driven (not hardcoded-default) copy. |
| 11 | Homepage is a real hero carousel + 2-column grid toggle with rotating decorative accent, replacing the placeholder (D-08) | ✓ VERIFIED | `HomeCarousel.astro` implements both `.home-hero` (carousel) and `.home-grid` (2-col grid, `grid-template-columns: repeat(2,1fr); gap:0`) with a working `show-carousel`/`show-grid` toggle. `index.astro`/`en/index.astro` fully replace the Phase 1 placeholder. |
| 12 | Carousel auto-advances every 6s, pauses on hover/focus, never permanently (D-09) | ✓ VERIFIED | `setInterval(..., 6000)`; `mouseenter`/`focusin` → `stopAutoAdvance()`; `mouseleave`/`focusout` → `startAutoAdvance()`. Playwright test uses `page.clock` to assert this deterministically — passes. |
| 13 | Only galleries with real migrated images appear on the homepage, filter is content-driven not a hardcoded slug list (D-12) | ✓ VERIFIED | `index.astro`: `.filter((gallery) => gallery.images.length > 0)` — no slug denylist anywhere in `index.astro`, `en/index.astro`, or `HomeCarousel.astro` (grep for `silos\|brume\|rebut\|adults\|victorian\|paysage` returns nothing in these files). SUMMARY documents this filter correctly and automatically picked up a third real gallery (`adults`/"Paysage") added mid-phase outside this phase's own work — direct evidence the filter is genuinely generic, not coincidentally matching 2 hardcoded galleries. |
| 14 | Gallery-detail pages get the same full-bleed hero-photo + dark-gradient-scrim + white Display title treatment as the homepage (D-07) | ✓ VERIFIED | Both `galleries/[slug].astro` and `en/galleries/[slug].astro` use `headerVariant="transparent"` and the identical `linear-gradient(to top, rgba(0,0,0,.65)...)` scrim used in `HomeCarousel.astro`. |
| 15 | Homepage's own header intentionally omits About/Contact nav (UI-SPEC-backed, not scope reduction); About/Contact remain reachable from elsewhere | ✓ VERIFIED | `HomeCarousel.astro`'s `.home-nav` only renders Home/Galleries links, matching UI-SPEC Layout Notes verbatim. `about.spec.ts`/`contact.spec.ts` were updated (not deleted) to assert reachability from `/galleries/` instead of `/`, with inline comments explaining this is a locked UI-SPEC decision, and the standard `BaseLayout` header (with About/Contact links) still renders on every other page. |

**Score:** 15/15 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/layouts/BaseLayout.astro` | New token block, Archivo Black import, logo hover-invert, headerVariant prop, white-on-white chrome | ✓ VERIFIED | All present and wired (see truths 1-9 above) |
| `public/logos/AJS_Brutalist_{Black,White}_Transparent.png` | Header logo assets | ✓ VERIFIED | Both present on disk |
| `src/components/{Button,Input,Textarea,EmptyState}.astro` | New reusable components | ✓ VERIFIED | All present, all wired to real usages |
| `src/components/HomeCarousel.astro` | Vanilla-JS carousel/grid island | ✓ VERIFIED | Present, no framework dependency, wired to `index.astro`/`en/index.astro` |
| `src/pages/{index,en/index}.astro` | Real homepage replacing placeholder | ✓ VERIFIED | Both fully rebuilt, content-driven gallery filter |
| `src/pages/galleries/{index,[slug]}.astro` (+EN) | EmptyState adoption, hero-scrim | ✓ VERIFIED | Both present and correct |
| `tests/e2e/homepage.spec.ts` | Wave 0 RED→GREEN contract | ✓ VERIFIED | 5 tests, all GREEN against final build |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `BaseLayout.astro` | `@fontsource/archivo-black` | frontmatter import | ✓ WIRED | Confirmed via grep + independent npm registry check |
| `BaseLayout.astro :root` | every component's `var(--color-*)` | CSS custom properties | ✓ WIRED | No component defines its own conflicting color values; grep confirms zero old-palette leftovers |
| `ContactForm.astro` | `src/lib/contact-form.ts` | unchanged `isHoneypotTriggered`/`isBlank`/`isValidEmail` imports | ✓ WIRED | `contact.spec.ts` (6/6) passes, validation logic untouched |
| `index.astro` frontmatter | `getGalleries()` filtered `images.length > 0` | build-time Sanity fetch | ✓ WIRED | Confirmed in source; content-drift (3rd gallery) proves it's live, not stubbed |
| `HomeCarousel.astro <script>` | embedded gallery data | `data-*` attributes read at runtime, no Sanity import in script | ✓ WIRED | Confirmed no `src/lib/sanity` import in the component's client script (threat-model note in file header) |
| `galleries/[slug].astro` | `BaseLayout headerVariant="transparent"` | prop | ✓ WIRED | Confirmed both FR/EN |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `index.astro` → `HomeCarousel` | `galleries` (filtered array) | `getGalleries()` (Sanity, build-time) | Yes — confirmed by content-drift finding (a 3rd real gallery, "Paysage", was picked up automatically) | ✓ FLOWING |
| `HomeCarousel.astro` client script | `carouselIndex`, `ACCENTS[index % length]` | Client-side state machine, generic modulo cycling | Yes — not hardcoded to N=2, verified in source | ✓ FLOWING |
| `galleries/index.astro` EmptyState branch | `getGalleries().length === 0` | Sanity query | Cannot observe rendered output today — both real galleries are published (no negative-case content exists) | ⚠️ UNTESTABLE TODAY (by design, not a defect — flagged in VALIDATION.md as manual-only) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full production build succeeds | `npm run build` | 19 pages built, 0 errors | ✓ PASS |
| Unit test suite green | `npm run test:unit` | 23/23 passed | ✓ PASS |
| Full e2e suite green (homepage, about, contact, gallery, i18n, legal) | `npx playwright test` | 40/40 passed | ✓ PASS |
| `@fontsource/archivo-black` is a legitimate package | `npm view @fontsource/archivo-black repository.url version` | `github.com/fontsource/font-files`, v5.2.8 — matches package.json and the SUMMARY's claimed audit findings | ✓ PASS |
| No old-palette / weight-900 / weight-300 / clamp() / destructive-red leftovers anywhere in src/ | `grep -rniE "F0E7E4\|E4D9D0\|F92D97\|#141213\|color-destructive\|font-weight:\s*900\|font-weight:\s*300\|clamp\(" src/` | zero matches | ✓ PASS |
| No React/framework introduced (constraint compliance) | `grep -rn "client:load\|client:idle\|client:visible\|from 'react'" src/` | zero matches | ✓ PASS |

### Probe Execution

Not applicable — this phase is a design-system/UI rebuild, not a migration/tooling phase. No `scripts/*/tests/probe-*.sh` files exist in this repo, and neither PLAN nor SUMMARY files reference probe-based verification. Playwright e2e specs serve this phase's equivalent role and were executed directly (see Behavioral Spot-Checks).

### Requirements Coverage

This is an inserted phase with no formal REQ-IDs (confirmed via `.planning/REQUIREMENTS.md` — no Phase 04.1 entries exist, consistent with 04.1-CONTEXT.md's explicit note "no REQ-IDs currently mapped to this phase"). Coverage is tracked instead via locked decisions D-01 through D-16, all of which are covered in the Observable Truths table above. No orphaned requirements found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` markers found in any of the 25 files modified across this phase's 6 plans | — | None — clean |
| `.planning/phases/04.1-design-system-homepage-refresh/04.1-VALIDATION.md` | 4-6 | Frontmatter still reads `status: draft`, `nyquist_compliant: false`, `wave_0_complete: false` despite Wave 0 (homepage.spec.ts) and the full suite being complete and green | ℹ️ Info | Documentation staleness only — does not affect shipped code or test results, which were independently re-run and confirmed green in this verification session |

### Human Verification Required

### 1. EmptyState visual render on galleries listing

**Test:** Temporarily unpublish all gallery documents in Sanity Studio, load `/galleries/` (and `/en/galleries/`), confirm the EmptyState component renders the existing FR/EN copy correctly, then re-publish.
**Expected:** Heading ("Galeries à venir"/"Galleries coming soon") and body copy render via the `EmptyState.astro` component with correct spacing/typography.
**Why human:** Not reachable via real content today — all three real galleries (Silos, Brume, and the newly-live Paysage) are currently published. This is explicitly documented as manual-only in `04.1-VALIDATION.md`. Code-level verification (component exists, is wired, uses prop-driven not hardcoded copy) was completed; the actual rendered visual was not observed in this session.

### 2. D-13 logo hover-invert visual-parity check against the live site

**Test:** Load `atelierjacquelinesuzanne.fr`'s current header, hover the logo, observe the current hover treatment; compare against the newly-implemented dark-backdrop-chip + black/white PNG cross-fade.
**Expected:** `04.1-UI-SPEC.md` explicitly flags this as an unverified assumption (RESEARCH.md Open Question 1 / Assumption A3) and instructs: "before shipping, do a 30-second check ... and adjust the chip's size/color only if it visibly conflicts."
**Why human:** Requires visually comparing two live-rendered pages — not verifiable via static analysis. None of the 6 plan SUMMARY.md files mention this check was performed. The shipped implementation is code-complete and functionally sound (both PNG assets exist, cross-fade CSS is correct) regardless of the outcome of this check, so this is a non-blocking but genuinely open verification item, not a defect.

### Gaps Summary

No blocking gaps. All 15 derived observable truths (spanning D-01 through D-16, the full-site rebrand scope expansion documented in execution notes, the homepage rebuild, and the new component library) are verified directly against the codebase — not merely claimed in SUMMARY.md. Build is green (19 pages), unit tests are green (23/23), and the full e2e suite is green (40/40), independently re-run in this verification session rather than trusted from prior SUMMARY claims.

Two items require human/manual verification before full confidence: (1) the EmptyState empty-content visual, which is genuinely unreachable with current live content and was already flagged as manual-only in the phase's own VALIDATION.md, and (2) an explicit pre-ship visual-parity check for the D-13 logo hover mechanism against the live external site, which no plan's SUMMARY confirms was performed. Neither blocks phase completion — both are visual sanity checks on an already-correct, already-tested code implementation — but per the escalation-gate pattern they are surfaced for a human decision rather than silently marked passed.

One non-blocking documentation issue: `04.1-VALIDATION.md`'s frontmatter (`status: draft`, `wave_0_complete: false`) was never updated to reflect the phase's actual completed state — cosmetic only, does not affect code or test outcomes.

---

*Verified: 2026-07-10T15:45:00Z*
*Verifier: Claude (gsd-verifier)*
