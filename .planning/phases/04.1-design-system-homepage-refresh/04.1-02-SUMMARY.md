---
phase: 04.1-design-system-homepage-refresh
plan: 02
subsystem: testing
tags: [playwright, e2e, tdd-red, homepage, carousel]

# Dependency graph
requires: []
provides:
  - "tests/e2e/homepage.spec.ts — RED Wave 0 e2e contract for D-08 (carousel/grid toggle), D-09 (auto-advance/pause via mocked clock), D-12 (only migrated galleries), plus an i18n header non-regression guard"
affects: ["04.1-04 (homepage build — must turn this spec GREEN)"]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Playwright page.clock.install()/fastForward() for deterministic setInterval-based UI testing, mirroring the existing gallery.spec.ts RED-authoring precedent"]

key-files:
  created: ["tests/e2e/homepage.spec.ts"]
  modified: []

key-decisions:
  - "Asserted the carousel index label via a generic /^\\d{2} \\/ \\d{2}$/ regex scoped to [data-role=\"home-carousel\"] rather than a more specific selector, since the exact DOM structure is not yet built (only the interface contract in the plan's <interfaces> block is known)"
  - "Used page.clock.install() + page.clock.fastForward(6000) (not real wall-clock waits) to make the D-09 auto-advance/pause assertions deterministic, per RESEARCH.md's Validation Architecture guidance"
  - "Grid-toggle assertion checks for both migrated gallery titles becoming simultaneously visible (vs. carousel's one-at-a-time) as a proxy for '2-column grid', since no grid-specific data-role is defined in the plan's interface contract"

requirements-completed: [D-08, D-09, D-12]

# Metrics
duration: 8min
completed: 2026-07-09
---

# Phase 04.1 Plan 02: RED Homepage E2E Spec Summary

**Wave 0 failing Playwright spec (`tests/e2e/homepage.spec.ts`) pins the homepage hero-carousel/grid contract (D-08/D-09/D-12) before Plan 04.1-04 builds it, using `page.clock` to deterministically test the 6000ms auto-advance/pause behavior.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-09T12:14:00Z
- **Completed:** 2026-07-09T12:22:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Authored `tests/e2e/homepage.spec.ts` with 5 test cases covering: carousel render + first migrated gallery (Silos), D-12 only-migrated-galleries (Rebut/Adults/Victorian Tea Room/Paysages must never appear), D-08 carousel↔grid toggle, D-09 auto-advance + pause-on-hover via a mocked Playwright clock, and an i18n header non-regression guard matching `i18n.spec.ts`'s pattern
- Confirmed the spec is genuinely RED for the correct reason: it fails against the current Phase 1 placeholder homepage ("Bienvenue... en cours de construction") because no `[data-role="home-carousel"]` element exists yet — not due to a typo, misconfiguration, or unrelated error
- Verified zero regression: the full pre-existing e2e suite (33 tests across about/contact/gallery/i18n/legal specs) and the unit suite (23 tests) both remain green

## Task Commits

Each task was committed atomically:

1. **Task 1: Author tests/e2e/homepage.spec.ts (RED)** - `fbc4ca6` (test)

**Plan metadata:** (this commit, docs)

## Files Created/Modified
- `tests/e2e/homepage.spec.ts` - New RED Wave 0 spec: 5 test cases (carousel render, D-12 gallery filter, D-08 toggle, D-09 mocked-clock auto-advance/pause, i18n header guard)

## Decisions Made
- See `key-decisions` in frontmatter — selector choices were necessarily interface-contract-based (not implementation-based) since the homepage doesn't exist yet; they were derived directly from the plan's `<interfaces>` block and 04.1-UI-SPEC.md's Layout Notes (index label format "01 / 02", toggle button labels "Carrousel"/"Grille", `data-role="home-carousel"`).

## Deviations from Plan

None - plan executed exactly as written. One environment gap was resolved without deviating from the plan's scope: this worktree had no local `.env` (Sanity credentials), which is a pre-existing project setup requirement (not part of this plan's task) needed to run `astro build`/`npm run preview` at all — copied from the main repo checkout's existing `.env` (already gitignored, not committed) to make the Playwright `webServer` able to start and actually execute the RED verification.

## Issues Encountered
- Initial `npx playwright test` run failed at the `webServer` startup step because no `dist/` build existed and no `.env` was present in this git worktree (Sanity env vars are required even for build failure paths to resolve cleanly). Resolved by copying the existing `.env` from the main repository checkout (a local dev-machine file, gitignored, never committed) and running `npm run build` once before invoking Playwright. This is standard local dev setup, not a plan deviation — the spec itself needed no changes.

## User Setup Required

None - no external service configuration required. (The `.env` copy above was a pre-existing project setup dependency for running the build locally in this worktree, not a new external service.)

## Next Phase Readiness
- `tests/e2e/homepage.spec.ts` is ready to serve as Plan 04.1-04's GREEN target — building the real hero carousel + grid toggle homepage should turn all 5 currently-failing assertions green without further spec changes.
- No blockers for Plan 04.1-04.

---
*Phase: 04.1-design-system-homepage-refresh*
*Completed: 2026-07-09*

## Self-Check: PASSED

- FOUND: tests/e2e/homepage.spec.ts
- FOUND: .planning/phases/04.1-design-system-homepage-refresh/04.1-02-SUMMARY.md
- FOUND commit: fbc4ca6 (test task commit)
- FOUND commit: bf6618f (docs/summary commit)
