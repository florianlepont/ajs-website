---
phase: 04.1-design-system-homepage-refresh
plan: 06
subsystem: ui
tags: [astro, css-custom-properties, typography, design-tokens, d-01, d-06]

# Dependency graph
requires:
  - phase: 04.1-design-system-homepage-refresh
    provides: "04.1-01's new :root token block in BaseLayout.astro (--text-display-size, --text-heading-size, --text-body-size, --weight-regular, --weight-semibold, --font-display, --color-accent)"
provides:
  - "About page (FR/EN), both legal pages (mentions-légales + confidentialité, FR/EN), and the Contact page-wrapper (FR/EN) repainted onto the fixed Display/Heading/Body type roles — closing the D-01 full-site-rebrand gap left open by Plans 03/05"
  - "404 page's return-home links repainted to var(--color-accent) — closing the D-06 pink-link gap on the one already-shipped page that hardcoded its link color"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Type-role conversion pattern (fixed var(--text-*-size)/var(--weight-*)/var(--font-display), no container-type/clamp()/letter-spacing) applied identically across .about-page, .legal-page, and .contact-page selectors — same pattern Plan 04.1-03 established for galleries/index.astro"

key-files:
  created:
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-06-SUMMARY.md
  modified:
    - src/pages/about.astro
    - src/pages/en/about.astro
    - src/pages/mentions-legales.astro
    - src/pages/en/mentions-legales.astro
    - src/pages/confidentialite.astro
    - src/pages/en/confidentialite.astro
    - src/pages/contact.astro
    - src/pages/en/contact.astro
    - src/pages/404.astro

key-decisions:
  - "Ran npm install and copied .env from the main repo checkout into this worktree before any edits — this worktree had neither node_modules nor a .env file, and npm run build failed immediately on both (missing @fontsource/archivo-black from Wave 1's package.json changes, then missing SANITY_PROJECT_ID). Same pattern Plan 04.1-01 already used for the .env copy; the npm install was flagged explicitly in the orchestrator's worktree note."

requirements-completed: [D-01, D-06]

# Metrics
duration: ~20min
completed: 2026-07-09
---

# Phase 04.1 Plan 06: About/Legal/Contact Typography + 404 Link-Color Summary

**Repainted the eight already-shipped content pages' `<style>` blocks (About FR/EN, mentions-légales FR/EN, confidentialité FR/EN, Contact FR/EN) from the superseded fluid-clamp/900/300 type system onto the fixed 32px/20px/16px Display/Heading/Body roles, and fixed the 404 page's one hardcoded ink-hex link-color declaration to the accent token — closing the last two D-01/D-06 gaps across every shipped page.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-09
- **Tasks:** 4 (all auto)
- **Files modified:** 9 (8 typography repaints + 1 single-declaration color fix)

## Accomplishments
- Converted `.about-page h1/h2/p` (FR + EN) from `clamp(2.5rem, 12cqi, 6.5rem)`/`font-weight: 900`/`container-type: inline-size` to `var(--text-display-size)`/`var(--weight-semibold)`/`var(--font-display)` (h1), `var(--text-heading-size)` (h2), and `var(--text-body-size)`/`var(--weight-regular)` (p)
- Applied the identical conversion to `.legal-page h1/h2/p` across all four legal files (mentions-légales + confidentialité, FR + EN)
- Applied the identical conversion to `.contact-page h1` and `.contact-page__lead` (FR + EN) — the Contact page-wrapper only, `ContactForm.astro`'s island untouched
- Fixed the 404 page's single `.not-found a { color: #1a1a1a; /* Accent */ }` declaration to `color: var(--color-accent);` — the page's h1/h2/p were already correct; this was the only remaining D-06 gap
- All eight content pages now match the type system Plans 03/05 already applied to galleries — no visually split site remains (D-01 complete across every shipped page)

## Task Commits

Each task was committed atomically:

1. **Task 1: Repaint About page typography (FR + EN)** - `126f1f5` (feat)
2. **Task 2: Repaint legal-page typography — mentions-légales + confidentialité (FR + EN)** - `c711d07` (feat)
3. **Task 3: Repaint Contact page-wrapper typography (FR + EN)** - `9ab9f4a` (feat)
4. **Task 4: Repaint 404 page link color to the accent token (D-06)** - `c527362` (fix)

## Files Created/Modified
- `src/pages/about.astro` / `src/pages/en/about.astro` - `.about-page` h1/h2/p converted to fixed Display/Heading/Body roles
- `src/pages/mentions-legales.astro` / `src/pages/en/mentions-legales.astro` - `.legal-page` h1/h2/p converted
- `src/pages/confidentialite.astro` / `src/pages/en/confidentialite.astro` - `.legal-page` h1/h2/p converted
- `src/pages/contact.astro` / `src/pages/en/contact.astro` - `.contact-page` h1 and `.contact-page__lead` converted
- `src/pages/404.astro` - `.not-found a` color declaration repainted to `var(--color-accent)`

## Decisions Made
- Ran `npm install` at the start of execution (this worktree had no `node_modules` at all) and copied `.env` from the main repo checkout into the worktree (gitignored, not committed) — both were prerequisites for a working `npm run build`, unrelated to this plan's own scope, mirroring the exact same fix Plan 04.1-01 already documented for the `.env` gap. The orchestrator's worktree note explicitly anticipated the `npm install` requirement.

## Deviations from Plan

### Auto-fixed Issues

None — the plan executed exactly as written for all four tasks. Each task's action section was applied verbatim; no bugs, missing functionality, or blocking issues were discovered in the target files beyond the two documented setup steps below (which are environment/worktree bootstrapping, not code deviations).

**Setup-only, not a Rule 1-4 deviation:**
- `npm install` (missing `node_modules` in this fresh worktree, not yet synced with Wave 1's `package.json` changes) and copying `.env` from the main repo checkout (same gitignored, build-time-only Sanity read credential Plan 04.1-01 already used) were both required before `npm run build` could run at all. Neither touched any file this plan is responsible for and neither is a plan deviation — they are the documented worktree-bootstrapping steps called out in the orchestrator's own instructions.

## Issues Encountered

**Homepage e2e tests fail in this worktree — expected, out of scope.** Running the full `npm run test:e2e` suite shows 4 failures, all in `tests/e2e/homepage.spec.ts` (carousel root not found, grid toggle, auto-advance). This worktree only has Wave 1 (Plan 04.1-01's BaseLayout foundation) merged as its base — Plan 04.1-04's `HomeCarousel` component and homepage rewrite live in a separate parallel worktree not yet merged here, so `index.astro` in this worktree is still the placeholder homepage from Phase 1. None of this plan's 9 modified files touch `index.astro` or `HomeCarousel.astro`. The 35 other e2e tests (about, contact, gallery, i18n, legal specs — the ones actually relevant to this plan's changes) all pass. This is a wave-parallelism artifact, not a Plan 06 defect; it will resolve automatically once the orchestrator merges all Wave 2 worktrees together.

## User Setup Required
None — no external service configuration required. (`.env` values used in this worktree were copied from the already-provisioned main-repo checkout, not newly created.)

## Next Phase Readiness
- All eight already-shipped content pages (About, legal x2, Contact — FR/EN each) now render on the same fixed Display/Heading/Body type roles as the galleries pages (Plans 03/05) — D-01's full-site-rebrand scope is complete across every shipped page.
- The 404 page's return-home links now render in full pink text via `var(--color-accent)` — D-06's site-wide pink-link rule is complete across every shipped page, with no hardcoded ink hex remaining anywhere in `src/pages/404.astro`.
- No blockers for the orchestrator's Wave 2 merge; once all parallel worktrees (02 through 06) are merged together, the full `homepage.spec.ts` suite should pass against the complete rebrand + new homepage.

---
*Phase: 04.1-design-system-homepage-refresh*
*Completed: 2026-07-09*

## Self-Check: PASSED

All nine claimed modified files verified present on disk; all four task commit hashes (`126f1f5`, `c711d07`, `9ab9f4a`, `c527362`) verified present in git history. No missing items.
