---
phase: 04.1
slug: design-system-homepage-refresh
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-08
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.9 (unit) + Playwright 1.61.1 (e2e) |
| **Config file** | `vitest.config.ts` (uses `astro/config getViteConfig`), `playwright.config.ts` |
| **Quick run command** | `npm run test:unit` |
| **Full suite command** | `npm run test:unit && npm run test:e2e` (Playwright's `webServer` auto-runs `npm run preview` against a production build) |
| **Estimated runtime** | ~90 seconds (unit: ~10s, e2e: ~80s against production preview build) |

---

## Sampling Rate

- **After every task commit:** Run `npm run test:unit`
- **After every plan wave:** Run `npm run test:unit && npm run test:e2e`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04.1-01-01 | 01 | 0 | D-08/D-09/D-12 | — | N/A | e2e, NEW | `npx playwright test tests/e2e/homepage.spec.ts` | ❌ W0 | ⬜ pending |
| 04.1-0X-0X | TBD | 1 | D-01–D-06 (token rebrand) | — | N/A | smoke (existing suite unchanged) | `npm run test:e2e` | ✅ (about/contact/gallery/i18n/legal specs) | ⬜ pending |
| 04.1-0X-0X | TBD | 1 | D-14 (ContactForm refactor) | V5 Input Validation | Existing `isBlank`/`isValidEmail`/`isHoneypotTriggered` behavior preserved | e2e, EXISTING (re-run) | `npx playwright test tests/e2e/contact.spec.ts` | ✅ | ⬜ pending |
| 04.1-0X-0X | TBD | 1 | New dependency: `@fontsource/archivo-black` | V14 Configuration / Tampering | `checkpoint:human-verify` gate before `npm install` (package legitimacy audit) | manual | — | — | ⬜ pending |
| 04.1-0X-0X | TBD | 1 | D-16 (EmptyState on galleries listing) | — | N/A | manual-only | — | — | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Exact task IDs are assigned by the planner; this table's rows are seeded from RESEARCH.md's Phase Requirements → Test Map and must be reconciled against the final PLAN.md task IDs before sign-off.*

---

## Wave 0 Requirements

- [ ] `tests/e2e/homepage.spec.ts` — new file covering D-08/D-09/D-12: carousel renders, grid toggle works, only migrated galleries appear (never Rebut/Adults/Victorian Tea Room/Paysages), auto-advance is observable/pausable via Playwright's `page.clock.install()` clock mocking (deterministic, no real wall-clock waits)
- [ ] No new fixtures/conftest-equivalent needed — Playwright tests hit the real built site directly; the 2 real migrated galleries serve as test fixtures, same pattern as existing `gallery.spec.ts`

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| EmptyState renders on galleries listing | D-16 | Not reachable via real content today — all real galleries are published; would require temporarily un-publishing all galleries in Sanity to observe | Temporarily unpublish all gallery documents in Sanity Studio, load `/galleries`, confirm EmptyState component renders, then re-publish |
| `@fontsource/archivo-black` package legitimacy audit | V14 Configuration / Tampering (STRIDE) | Supply-chain checkpoint requiring human judgment before installing a new npm dependency, consistent with Phase 2's precedent (`@sanity/orderable-document-list`) | Review package on npmjs.com: publisher, download count, repo link (`github.com/fontsource/font-files`), version history; confirm no typosquat risk before `npm install` |
| D-13 logo hover-invert visual mechanics | D-13 | Open question flagged in RESEARCH.md — exact live-site treatment (dark-chip backdrop vs. other) needs visual confirmation before implementation, not automatable | Load current live site header, hover the logo, observe current treatment; compare against design-import's brand logo asset variants |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`tests/e2e/homepage.spec.ts`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
