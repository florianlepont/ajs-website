---
phase: 02-portfolio-galleries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/gallery.spec.ts
  - tests/unit/gallery-query.test.ts
  - sanity/schemas/objects/galleryImage.ts
  - sanity/schemas/gallery.ts
  - sanity/schemas/index.ts
  - sanity/schemas/structure.ts
  - sanity/schemas/siteSettings.ts
  - sanity/package.json
  - src/lib/sanity.ts
  - src/lib/image.ts
autonomous: false
requirements: [CMS-01, PORT-01, PORT-03]
user_setup: []

must_haves:
  truths:
    - "Romane can create a new gallery document in Sanity Studio and drag-reorder galleries in the document list"
    - "Every gallery image requires a French and English alt text before it can be published"
    - "Every downstream gallery page can render Romane's chosen manual order without additional code"
  artifacts:
    - path: "sanity/schemas/gallery.ts"
      provides: "gallery document schema (title, slug, statement, images[], orderRank)"
      contains: "defineType"
    - path: "sanity/schemas/objects/galleryImage.ts"
      provides: "reusable image+bilingual-alt object type"
      contains: "galleryImage"
    - path: "src/lib/sanity.ts"
      provides: "getGalleries/getGallery typed query functions"
      exports: ["getGalleries", "getGallery", "Gallery", "GalleryImage"]
    - path: "src/lib/image.ts"
      provides: "Sanity CDN image URL builders"
      exports: ["thumbnailUrl", "fullSizeUrl"]
    - path: "tests/e2e/gallery.spec.ts"
      provides: "failing e2e coverage for listing order, lightbox, statement"
    - path: "tests/unit/gallery-query.test.ts"
      provides: "query-function null-safety unit coverage"
  key_links:
    - from: "sanity/schemas/index.ts"
      to: "gallery + galleryImage schemas"
      via: "schemaTypes array registration"
      pattern: "schemaTypes = \\[siteSettings, gallery, galleryImage\\]"
    - from: "sanity/schemas/structure.ts"
      to: "@sanity/orderable-document-list"
      via: "orderableDocumentListDeskItem for 'gallery'"
      pattern: "orderableDocumentListDeskItem"
    - from: "src/lib/sanity.ts"
      to: "gallery documents"
      via: "GROQ order(orderRank) query"
      pattern: "order\\(orderRank\\)"
---

<objective>
Establish the gallery data contract: a new Sanity `gallery` document type with an inline array of bilingual-alt image objects (D-01/D-02/D-03), drag-to-reorder gallery ordering via the official `@sanity/orderable-document-list` plugin (D-10/CMS-01), and the typed build-time read layer (`getGalleries`/`getGallery`, image URL builders) every downstream page consumes. Also lays down the RED Wave 0 tests.

Purpose: This is the write+read contract for the whole phase. Romane gains a working Studio editing surface (the CMS-01 self-serve slice) and downstream plans get a stable, typed interface to build against — no codebase spelunking required.
Output: gallery/galleryImage schemas, Studio ordering wiring, `getGalleries`/`getGallery`, `src/lib/image.ts`, `navLabels.galleries` field, and two failing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-portfolio-galleries/02-CONTEXT.md
@.planning/phases/02-portfolio-galleries/02-RESEARCH.md
@.planning/phases/02-portfolio-galleries/02-PATTERNS.md
@.planning/phases/02-portfolio-galleries/02-VALIDATION.md

<interfaces>
<!-- Contracts the executor must replicate. Extracted from the codebase. Use directly. -->

Existing locale-object helpers to REUSE (sanity/schemas/siteSettings.ts, lines 7-29):
- `localeStringField(name, title)` — object with required fr/en `string` sub-fields.
- `localeTextField(name, title)` — object with required fr/en `text` (rows:3) sub-fields.
These are module-local functions in siteSettings.ts (not exported). Replicate the same shape inline in galleryImage.ts for `alt`, and copy `localeTextField` into gallery.ts for `statement`.

Existing SiteSettings read contract to EXTEND (src/lib/sanity.ts, lines 31-63):
- `export interface LocaleString { fr: string; en: string }`  ← reuse for alt/statement typing
- `export const sanityClient = createClient({...})`  ← import and reuse; never create a second client
- `getSiteSettings()` returns `SiteSettings | null` via `result ?? null` — copy this exact fetch-then-null-coalesce shape for getGalleries/getGallery.
- `SiteSettings.navLabels` is currently `{ home: LocaleString }` — add `galleries: LocaleString`.

Existing Studio desk structure to EXTEND (sanity/schemas/structure.ts, full file, 19 lines):
- `StructureResolver` currently takes only `S`; the plugin needs `(S, context)`.
- The trailing `S.documentTypeListItems().filter(... !== 'siteSettings')` must also exclude `'gallery'`.

Existing schema registry (sanity/schemas/index.ts): `export const schemaTypes = [siteSettings]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the RED Wave 0 test files</name>
  <files>tests/e2e/gallery.spec.ts, tests/unit/gallery-query.test.ts</files>
  <read_first>
    - tests/e2e/i18n.spec.ts (exact Playwright describe/goto/locator idiom to copy, incl. the RED header-comment convention)
    - tests/unit/i18n-paths.test.ts (exact Vitest describe/it idiom + the RED "does not exist yet" header comment)
    - .planning/phases/02-portfolio-galleries/02-VALIDATION.md (Per-Task Verification Map — the binding Requirement/Command columns)
    - src/lib/sanity.ts (the getGalleries/getGallery signatures the unit test imports)
  </read_first>
  <action>
    Create tests/e2e/gallery.spec.ts as a Playwright suite grouped by `test.describe` into 'gallery listing', 'gallery detail', and 'lightbox', mirroring i18n.spec.ts's structure and its RED header comment (these assertions target contracts built in Plans 03/04 and MUST fail until then — do not weaken them). Cover: (a) listing order — visiting `/galleries/` renders gallery cards; the first card's title matches the manually-ordered first gallery; (b) statement — `/galleries/{slug}` and `/en/galleries/{slug}` each render the artist statement text and differ between locales; (c) lightbox — clicking a detail-page thumbnail opens a `dialog[open]`, ArrowRight advances the counter, Escape closes it and focus returns to the triggering thumbnail. Use resilient locators (roles, data attributes) not brittle nth-child chains where avoidable.
    Create tests/unit/gallery-query.test.ts as a Vitest suite that mocks the `@sanity/client` module (via `vi.mock`) so `sanityClient.fetch` is stubbed, then asserts: `getGallery(slug)` returns `null` when fetch resolves `null` or `undefined` (WR-03 null-safety), `getGalleries()` returns the array fetch resolves, and the GROQ strings passed to fetch contain `order(orderRank)` (list) and `slug.current == $slug` (detail). Add the RED header comment noting src/lib/sanity.ts's gallery exports do not exist yet. If import of sanity.ts requires SANITY_PROJECT_ID/SANITY_DATASET env at module load, set them to dummy values inside the test (e.g. via `vi.stubEnv` or a top-of-file assignment) so the import does not throw.
    Do NOT create src/lib/sanity.ts gallery exports or any pages in this task — the tests are meant to be RED now.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/gallery.spec.ts --list &amp;&amp; npx vitest run tests/unit/gallery-query.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/gallery.spec.ts` exists and contains three `test.describe` blocks titled 'gallery listing', 'gallery detail', 'lightbox'.
    - `tests/unit/gallery-query.test.ts` exists, imports `getGalleries` and `getGallery` from `../../src/lib/sanity`, and uses `vi.mock`.
    - Running the unit spec fails RED (import/assertion failure) — this is the required initial state, not a defect.
    - `npx playwright test tests/e2e/gallery.spec.ts --list` enumerates the tests without a config error (list-only, no run needed here).
  </acceptance_criteria>
  <done>Both test files exist, are shaped after the Phase 1 analogs, and fail RED because their production targets are not built yet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Verify @sanity/orderable-document-list package legitimacy</name>
  <read_first>
    - .planning/phases/02-portfolio-galleries/02-RESEARCH.md (Package Legitimacy Audit — the [ASSUMED] provenance note and the slopcheck-install side-effect warning)
  </read_first>
  <action>Before the Task 3 install, a human confirms `@sanity/orderable-document-list` is a legitimate package on npmjs.com (published by the `sanity-io` org, repo github.com/sanity-io/orderable-document-list, version ~2.0.8, peers `sanity ^5 || ^6.0.0-0` / `react ^19.2`). This is a blocking supply-chain gate (T-02-01-SC); do not auto-approve. Do NOT run `slopcheck install` (mutating side effect).</action>
  <acceptance_criteria>
    - Human has confirmed the package on npmjs.com under the sanity-io org and typed "approved", OR reported a discrepancy that halts the install.
  </acceptance_criteria>
  <what-built>Nothing yet — this gate precedes the package install in Task 3. `@sanity/orderable-document-list` was tagged `[ASSUMED]` for name-provenance in 02-RESEARCH.md (discovered via WebSearch; README fetched from the official `sanity-io` GitHub org; passed slopcheck `[OK]`). Threat T-02-01-SC requires a human confirmation before any package-manager install (supply-chain tampering).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@sanity/orderable-document-list in a browser.
    2. Confirm the package is published by the `sanity-io` org and links to repo `github.com/sanity-io/orderable-document-list`.
    3. Confirm a current version around `2.0.8` exists and `peerDependencies` list `sanity ^5 || ^6.0.0-0` and `react ^19.2` (this project's Studio runs `sanity ^6.3.0` / `react ^19.2.4`).
    4. Do NOT run `slopcheck install` (02-RESEARCH.md notes it performs a real, mutating `npm install` side effect) — a manual npmjs.com glance is the intended check.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the Task 3 install, or describe any discrepancy found.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Gallery schema, Studio ordering, read layer, and navLabels.galleries</name>
  <files>sanity/schemas/objects/galleryImage.ts, sanity/schemas/gallery.ts, sanity/schemas/index.ts, sanity/schemas/structure.ts, sanity/schemas/siteSettings.ts, sanity/package.json, src/lib/sanity.ts, src/lib/image.ts</files>
  <read_first>
    - sanity/schemas/siteSettings.ts (localeStringField/localeTextField helpers + navLabels.home field shape to mirror for navLabels.galleries)
    - sanity/schemas/structure.ts (current StructureResolver to extend with the context param + gallery filter)
    - sanity/schemas/index.ts (schemaTypes array to extend)
    - sanity/sanity.config.ts (confirms structureTool({structure}) + schema.types wiring so registration lands correctly)
    - src/lib/sanity.ts (sanityClient singleton, LocaleString, SiteSettings interface, getSiteSettings fetch+null-coalesce shape to replicate)
    - .planning/phases/02-portfolio-galleries/02-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 3, Pitfall 4, Code Examples)
    - .planning/phases/02-portfolio-galleries/02-PATTERNS.md (concrete starting-point snippets for gallery.ts, structure.ts, sanity.ts, image.ts)
  </read_first>
  <action>
    First run `npm install @sanity/orderable-document-list` inside the `sanity/` directory only (never in the frontend package.json) — this updates sanity/package.json and sanity/package-lock.json.
    Create sanity/schemas/objects/galleryImage.ts as a `defineType` object type named `galleryImage`: an `image` field (`type: 'image'`, `options: {hotspot: true}`, `validation: required`) and an `alt` object field with required fr/en `string` sub-fields (same shape as localeStringField, both required, and the alt object itself required per D-02). Add `preview: {select: {media: 'image', title: 'alt.fr'}}`.
    Create sanity/schemas/gallery.ts as a `defineType` document named `gallery` with fields in order: `title` (`type: 'string'`, required — NOT locale-aware per D-04); `slug` (`type: 'slug'`, `options: {source: 'title'}`, required — a single non-locale field per Pitfall 3); `statement` via a `localeTextField('statement', 'Artist Statement')` helper copied from siteSettings.ts; `images` (`type: 'array'`, `of: [{type: 'galleryImage'}]`, `options: {layout: 'grid'}`, `validation: rule.min(1)` with the message that a gallery needs at least one image because the first image is the cover, D-09); and `orderRankField({type: 'gallery'})` imported from `@sanity/orderable-document-list`, spread with `hidden: true` so the raw fractional-index field never shows in Romane's edit form (Pitfall 4). Add `preview: {select: {title: 'title', media: 'images.0.image'}}`.
    Modify sanity/schemas/index.ts to import `gallery` and `galleryImage` and export `schemaTypes = [siteSettings, gallery, galleryImage]` (both must be registered — Sanity requires every referenced object type in the array).
    Modify sanity/schemas/structure.ts: import `orderableDocumentListDeskItem` from `@sanity/orderable-document-list`, change the resolver signature to `(S, context)`, add `orderableDocumentListDeskItem({type: 'gallery', S, context})` as a list item after the Site Settings item, and change the trailing filter to exclude both `'siteSettings'` and `'gallery'` from `S.documentTypeListItems()` (guard `listItem.getId() ?? ''`).
    Modify sanity/schemas/siteSettings.ts: add a `galleries` sub-field to the `navLabels` object, an object with required fr/en `string` sub-fields, identical in shape to the existing `home` sub-field (lines 45-53).
    Modify src/lib/sanity.ts: add `galleries: LocaleString` to `SiteSettings.navLabels`; add `navLabels` in the SITE_SETTINGS_QUERY already returns the whole object so no query change is needed there; add `export interface GalleryImage { image: {asset: {_ref: string}; hotspot?: {x:number;y:number;height:number;width:number}}; alt: LocaleString }` and `export interface Gallery { title: string; slug: string; statement: LocaleString; images: GalleryImage[] }`; add `GALLERIES_QUERY` (`*[_type == "gallery"] | order(orderRank){title, "slug": slug.current, statement, images}`) and `GALLERY_BY_SLUG_QUERY` (`*[_type == "gallery" && slug.current == $slug][0]{title, "slug": slug.current, statement, images}`); add `getGalleries(): Promise<Gallery[]>` returning `sanityClient.fetch(GALLERIES_QUERY)` and `getGallery(slug): Promise<Gallery | null>` returning `(await sanityClient.fetch(...{slug})) ?? null`, mirroring getSiteSettings.
    Create src/lib/image.ts: import `createImageUrlBuilder` from `@sanity/image-url`, import `sanityClient` from `./sanity` and type `GalleryImage` from `./sanity`; construct one `builder`; export `thumbnailUrl(img: GalleryImage['image'], size = 600)` returning a `.width(size).height(size).fit('crop').auto('format').url()` string (1:1 crop for grids) and `fullSizeUrl(img: GalleryImage['image'], maxWidth = 2000)` returning `.width(maxWidth).fit('max').auto('format').url()` (uncropped, for the lightbox). This module is build-time only (imports the read-token client) — never import it from a client `<script>`.
  </action>
  <verify>
    <automated>cd sanity &amp;&amp; npx sanity schema validate 2>/dev/null || npx tsc --noEmit -p . ; cd - &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run tests/unit/gallery-query.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sanity/package.json` lists `@sanity/orderable-document-list` under dependencies; the frontend root `package.json` does NOT.
    - `sanity/schemas/gallery.ts` defines a document with a `title` string field (no fr/en sub-fields), a `slug` field, a `localeText`-shaped `statement`, an `images` array of `galleryImage` with `validation` calling `.min(1)`, and an `orderRankField` spread with `hidden: true`.
    - `grep -c "orderableDocumentListDeskItem" sanity/schemas/structure.ts` returns 1, and the trailing filter excludes both `siteSettings` and `gallery`.
    - `grep -E "getGalleries|getGallery" src/lib/sanity.ts` shows both exported functions; `grep "order(orderRank)" src/lib/sanity.ts` matches.
    - `src/lib/image.ts` exports `thumbnailUrl` and `fullSizeUrl`.
    - `npx tsc --noEmit` passes for the frontend; `tests/unit/gallery-query.test.ts` now passes GREEN.
  </acceptance_criteria>
  <done>The Studio has a drag-orderable `gallery` type with required bilingual alt text, the typed read layer and image builders exist, navLabels.galleries is declared, and the gallery-query unit test is GREEN. The e2e spec stays RED (no pages yet).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Romane → Sanity Studio | Authenticated content editing; the only write path. Sanity's project-level auth (Phase 1) gates it. |
| npm registry → sanity/ install | Supply-chain surface: one new package pulled at build/dev time. |
| Sanity Content Lake → Astro build | Build-time read via a read-only token that is never shipped to the browser (unchanged from Phase 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-SC | Tampering | `npm install @sanity/orderable-document-list` (sanity/) | mitigate | Blocking human legitimacy checkpoint (Task 2) verifying the package on npmjs.com under the `sanity-io` org before install; `[ASSUMED]` provenance per 02-RESEARCH.md. |
| T-02-01-IV | Tampering (Input) | Studio-authored gallery/image content | mitigate | Schema validation: `alt` fr/en `required()` (D-02), `images` `.min(1)` (D-09), `title`/`slug` `required()` — enforced Studio-side (ASVS V5, the only input surface this phase). |
| T-02-01-IE | Information Disclosure | `SANITY_API_READ_TOKEN` | accept | Build-time-only client in src/lib/sanity.ts / src/lib/image.ts; header comment forbids client-side import. No new exposure vs. Phase 1; frontend ships zero token. |
</threat_model>

<verification>
- Studio starts (`cd sanity && npx sanity dev` boots without schema errors) and the "Gallery" list appears in the desk with drag handles.
- `npx tsc --noEmit` clean; `tests/unit/gallery-query.test.ts` GREEN; `tests/e2e/gallery.spec.ts` still RED.
</verification>

<success_criteria>
- A `gallery` document type with bilingual-alt images and drag-reorder ordering exists in Studio (CMS-01 write path).
- `getGalleries`/`getGallery`/`thumbnailUrl`/`fullSizeUrl` are exported and typed; downstream plans can build against them without exploring the codebase.
</success_criteria>

<output>
Create `.planning/phases/02-portfolio-galleries/02-01-SUMMARY.md` when done.
</output>
