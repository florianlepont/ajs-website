---
phase: 02-portfolio-galleries
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/index.astro
  - src/pages/en/index.astro
  - public/fonts/
autonomous: false
requirements: [PORT-01]
user_setup: []

must_haves:
  truths:
    - "Every page renders on the Dawn Pink background with Woodsmoke ink and the Wild Strawberry accent, replacing Phase 1's grayscale"
    - "A 'Galeries'/'Galleries' nav link appears in the header, its label sourced from Sanity"
    - "The homepage welcome heading renders at the fluid oversized Display role"
    - "Links show a Wild Strawberry underline and focus-visible shows the double-ring treatment"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "site-wide design tokens, font-face, galleries nav link, sr-only utility"
      contains: "--color-ink"
  key_links:
    - from: "src/layouts/BaseLayout.astro header"
      to: "siteSettings.navLabels.galleries"
      via: "localized nav link with fallback"
      pattern: "navLabels\\?\\.galleries"
    - from: "src/layouts/BaseLayout.astro :root"
      to: "Wild Strawberry accent + Woodsmoke ink"
      via: "CSS custom properties"
      pattern: "#F92D97|#141213"
---

<objective>
Apply the real visual identity from 02-UI-SPEC.md site-wide: replace Phase 1's provisional grayscale/system-font tokens in BaseLayout with the Dawn Pink / Woodsmoke / Wild Strawberry palette, the Delight variable-font `@font-face` (with graceful system fallback), the extreme Light(300)/Black(900) two-weight system, the fluid `clamp()` Display role, the accessible link + double-ring focus treatments, an `.sr-only` utility, and a new "Galeries"/"Galleries" header nav link. Update the two homepage files so their heading adopts the new Display role.

Purpose: Every gallery component built in Plan 03 references the NEW token names (`--color-ink`, Wild Strawberry `--color-accent`, Display `clamp()`), so the token migration must land first. The identity change is site-wide and user-facing on its own — after this plan the homepage and 404 already look like the new brand.
Output: rebranded BaseLayout, updated homepages, self-hosted Delight font file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-galleries/02-UI-SPEC.md
@.planning/phases/02-portfolio-galleries/02-PATTERNS.md

<interfaces>
<!-- Current BaseLayout contracts to modify. Extracted from the codebase. -->

src/layouts/BaseLayout.astro currently:
- `:root` (lines 79-92): `--color-dominant:#ffffff`, `--color-secondary:#f5f4f2`, `--color-accent:#1a1a1a`, `--color-destructive:#dc2626`, plus the `--space-*` scale (KEEP the spacing scale unchanged).
- body (lines 104-114): system font stack, `background: var(--color-dominant)`, `color: var(--color-accent)`.
- `a`/`:focus-visible` (lines 116-124): single 2px accent outline.
- `.site-title` (146-151): 20px / weight 600. `.nav-link` (158-163): 14px / weight 400.
- header nav (64-66): a single `<a href={homeHref} class="nav-link">{homeLabel}</a>`.
- `homeLabel` derived at line 25 from `siteSettings?.navLabels?.home?.[locale]`.

siteSettings read contract (src/lib/sanity.ts, updated in Plan 02-01): `SiteSettings.navLabels` now includes `galleries: LocaleString`; the SITE_SETTINGS_QUERY already returns the full navLabels object.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand BaseLayout tokens, typography, links, nav link, and homepages</name>
  <files>src/layouts/BaseLayout.astro, src/pages/index.astro, src/pages/en/index.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (the :root block, global styles, header nav, and scoped chrome styles to modify)
    - .planning/phases/02-portfolio-galleries/02-UI-SPEC.md (Color, Typography incl. the Display clamp mechanism and container-type requirement, Copywriting Contract nav label, focus-contrast resolution)
    - src/pages/index.astro and src/pages/en/index.astro (current .welcome h1 at 32px/600 to migrate to Display)
    - src/lib/sanity.ts (SiteSettings.navLabels.galleries shape for the fallback read)
  </read_first>
  <action>
    In src/layouts/BaseLayout.astro `:root`: set `--color-dominant: #F0E7E4` (Dawn Pink), `--color-secondary: #E4D9D0`, `--color-accent: #F92D97` (Wild Strawberry), add `--color-ink: #141213` (Woodsmoke), keep `--color-destructive: #dc2626`, and keep the entire `--space-*` scale exactly as-is.
    Update the global `body` rule: `background-color: var(--color-dominant)`, `color: var(--color-ink)` (NOT accent — accent is now the vivid pink and must never be body text), and set `font-family: 'Delight', -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif;` with a base `font-weight: 300`.
    Add a single variable `@font-face` for `'Delight'` in the global style block: `font-weight: 100 900; font-display: swap;` with `src: url('/fonts/delight.woff2') format('woff2');` (path under public/, filename provided by Task 2 — coordinate exact filename). The system fallback in the stack keeps the site fully functional until the file lands.
    Update the global `a` rule: keep the glyph color `var(--color-ink)` (Wild Strawberry as text fails contrast, ≈2.95:1 per UI-SPEC), and give links a permanent Wild Strawberry accent cue via `text-decoration-color: var(--color-accent)` with `text-underline-offset` for legibility (underline present at rest). Replace the `:focus-visible` rule (for `a` and `button`) with the co-styled double ring: `outline: 2px solid var(--color-ink); outline-offset: 2px; box-shadow: 0 0 0 4px var(--color-accent);`.
    Add a global `.sr-only` utility (visually-hidden but screen-reader-available: position absolute, 1px clip, no wrap) — this is introduced here for reuse by GalleryCard in Plan 03.
    Update scoped chrome styles: `.site-title` weight 900 (Heading role, keep 20px); `.nav-link` weight 300 (Label role, keep 14px); `.footer-text` weight 300. Header `.chrome-band` background stays `var(--color-secondary)`.
    In the header nav (`<nav class="site-nav">`), add a second `<a class="nav-link">` for galleries AFTER the home link. Its href is `getRelativeLocaleUrl(locale, 'galleries')` and its label reads `siteSettings?.navLabels?.galleries?.[locale] ?? (locale === 'en' ? 'Galleries' : 'Galeries')` (defensive fallback per WR-03). Derive both in frontmatter alongside the existing `homeLabel`/`homeHref`.
    In src/pages/index.astro and src/pages/en/index.astro: change `.welcome h1` to the Display role — `font-size: clamp(2.5rem, 12cqi, 6.5rem); font-weight: 900; line-height: 1.0; letter-spacing: -0.02em;` — and add `container-type: inline-size;` to the `.welcome` wrapper so `cqi` resolves against the panel width (required, per UI-SPEC Typography mechanism). Change `.welcome p` weight to 300 (Body role). Remove the old `font-weight: 600` on the heading. Keep the plain-text treatment (no color panel on the homepage — panels are gallery-only).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -q "#F92D97" dist/index.html 2>/dev/null; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\-\-color-ink: #141213" src/layouts/BaseLayout.astro` returns 1 and `--color-accent: #F92D97` and `--color-dominant: #F0E7E4` are present.
    - `grep "container-type: inline-size" src/pages/index.astro` and `src/pages/en/index.astro` each match.
    - `grep "clamp(2.5rem, 12cqi, 6.5rem)" src/pages/index.astro` matches (Display role).
    - BaseLayout header renders two `.nav-link` anchors; the galleries one reads `navLabels?.galleries?.[locale]` with a literal 'Galeries'/'Galleries' fallback.
    - `.sr-only` global utility class exists in BaseLayout.
    - `:focus-visible` rule contains both `outline: 2px solid var(--color-ink)` and `box-shadow: 0 0 0 4px var(--color-accent)`.
    - `npm run build` succeeds and `npx tsc --noEmit` is clean.
  </acceptance_criteria>
  <done>The whole site renders in the new Dawn Pink/Woodsmoke/Wild Strawberry identity with the two-weight Delight system (system-font fallback active until Task 2), a galleries nav link, and the fluid Display homepage heading.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Source and self-host the Delight variable font</name>
  <read_first>
    - .planning/phases/02-portfolio-galleries/02-UI-SPEC.md (Font section — sourcing dependency + license requirement)
    - src/layouts/BaseLayout.astro (the @font-face src path declared in Task 1)
  </read_first>
  <action>A human sources the Delight variable font file (Behance release by Rajesh Rajput — not on any CDN/npm), confirms a self-hosted commercial web-embedding license, and places it at public/fonts/delight.woff2 (or updates the @font-face src to the actual filename). Claude cannot download it programmatically. The system-font fallback keeps the site functional until this lands.</action>
  <acceptance_criteria>
    - public/fonts/delight.woff2 (or the referenced filename) exists and loads with no console 404, OR the human explicitly chooses "fallback-only" to ship on the system font for now.
    - License permitting web embedding has been confirmed before shipping the font file.
  </acceptance_criteria>
  <what-built>Task 1 declared a `@font-face` for `'Delight'` pointing at `/fonts/delight.woff2` and left the system-font fallback active. The actual font file must be human-sourced — "Delight" is a free Behance release by Rajesh Rajput, not on Google Fonts / any CDN / npm, and only reachable as image-only project assets. Claude cannot download it programmatically.</what-built>
  <how-to-verify>
    1. Source the Delight variable font file(s) (ideally a single variable `.woff2`) from the Behance project or the designer directly.
    2. Confirm the license permits self-hosted commercial web embedding — a free Behance release does not automatically grant a web-embedding license. Do NOT ship without this confirmation.
    3. Place the file at `public/fonts/delight.woff2` (or update the `@font-face` `src` in BaseLayout to the exact filename you use).
    4. Run `npm run dev`, load `/`, and confirm the heading renders in Delight (not the system fallback) with no console 404 for the font.
  </how-to-verify>
  <resume-signal>Type "approved" once the licensed font file is in public/fonts/ and rendering, or "fallback-only" to intentionally ship on the system-font fallback for now (font sourcing tracked as follow-up).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External font source → repo | A binary font asset is added to the repo/`public/`. |
| Sanity → build | navLabels.galleries read at build time (unchanged read boundary from Phase 1). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-LIC | Repudiation/Legal | Self-hosted Delight font file | mitigate | Blocking human checkpoint (Task 2) requires explicit web-embedding license confirmation before the file ships. |
| T-02-02-IV | Tampering (Input) | siteSettings.navLabels.galleries | accept | Studio-side `required()` fr/en validation (added in 02-01); frontend read is null-safe with literal fallback. No runtime input surface. |
</threat_model>

<verification>
- `npm run build` produces pages on the Dawn Pink background with the pink accent; 404 and homepage inherit the new identity automatically.
- Focus-visible on a link shows the ink outline + pink ring; links carry a pink underline at rest.
</verification>

<success_criteria>
- Site-wide identity matches 02-UI-SPEC.md (color, two-weight typography, Display clamp, accessible links/focus).
- Galleries nav link present and Sanity-sourced; homepage heading is Display role with container-query sizing.
</success_criteria>

<output>
Create `.planning/phases/02-portfolio-galleries/02-02-SUMMARY.md` when done.
</output>
