---
phase: 03-about-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/about.spec.ts
  - src/pages/about.astro
  - src/pages/en/about.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [ABOUT-01, ABOUT-02]

must_haves:
  truths:
    - "Visitor can open /about/ (FR) and read a bio section and an atelier/practice section"
    - "Visitor can open /en/about/ (EN) and read the same content in English"
    - "The atelier/practice section shows the exact locked D-06 placeholder copy for medium/technique"
    - "Visitor can reach the About page from a nav link in the site header, in both locales"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "FR About page — hardcoded bilingual-neutral French bio + atelier/practice + placeholders"
      contains: "À propos"
    - path: "src/pages/en/about.astro"
      provides: "EN About page mirror"
      contains: "About"
    - path: "src/layouts/BaseLayout.astro"
      provides: "About nav link added to the primary <nav>"
      contains: "getRelativeLocaleUrl(locale, 'about')"
    - path: "tests/e2e/about.spec.ts"
      provides: "Playwright locale-pair content assertions for the About page"
  key_links:
    - from: "src/layouts/BaseLayout.astro nav"
      to: "/about/ and /en/about/ routes"
      via: "getRelativeLocaleUrl(locale, 'about')"
      pattern: "getRelativeLocaleUrl\\(locale, 'about'\\)"
---

<objective>
Ship the About page as a complete vertical slice: a visitor can navigate to `/about/` (FR) or `/en/about/` (EN) from the header nav and read Romane's bio and atelier/practice information, satisfying ABOUT-01 and ABOUT-02.

Because live-site verification found NO existing bio/practice text anywhere (03-RESEARCH.md Open Question 1, resolved by 03-CONTEXT.md Post-Research Amendment), this page ships with clearly-marked italic placeholder copy — same treatment as the locked D-06 medium/technique placeholder — for Romane to replace before launch. This is the approved resolution, NOT a scope reduction: the page, routes, nav, and bilingual structure are all real and complete; only the prose is placeholder pending the artist.

Purpose: Delivers success criteria 1 and 2 of Phase 3 (visitor can read bio + atelier/practice info in FR and EN).
Output: `src/pages/about.astro`, `src/pages/en/about.astro`, an About nav link in `BaseLayout.astro`, and a passing `tests/e2e/about.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** prospective visitor or collaborator, **I want to** read about Romane's background and artistic/atelier practice on the site, **so that** I understand who she is before reaching out or considering her work.

<context>
@.planning/ROADMAP.md
@.planning/phases/03-about-contact/03-CONTEXT.md
@.planning/phases/03-about-contact/03-UI-SPEC.md
@.planning/phases/03-about-contact/03-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase — the executor should use these directly, no exploration needed. -->

From src/pages/galleries/index.astro (the exact static-page analog — copy the Display <h1> style block verbatim, rename selector):
- Frontmatter is just `import BaseLayout from '../../layouts/BaseLayout.astro';` — but About pages are TOP-LEVEL (like src/pages/index.astro), so import depth is `../layouts/BaseLayout.astro` for src/pages/about.astro and `../../layouts/BaseLayout.astro` for src/pages/en/about.astro. Verify against src/pages/index.astro depth, NOT the galleries example.
- Locked Display-role <h1> style block to reuse (rename `.galleries-page` → `.about-page`):
  container-type: inline-size; font-size: clamp(2.5rem, 12cqi, 6.5rem); font-weight: 900; line-height: 1; letter-spacing: -0.02em; margin: 0 0 var(--space-xl);
- Reading-width precedent: max-width: 640px; margin: 0 auto; (from the `.empty-state` block, lines 48-53).

From src/layouts/BaseLayout.astro (the nav to extend — lines 26-33, 70-73):
- `const locale = Astro.currentLocale === 'en' ? 'en' : 'fr';` already defined.
- Existing hardcoded-fallback nav pattern (mirror it exactly for About):
  `const galleriesLabel = siteSettings?.navLabels?.galleries?.[locale] ?? (locale === 'en' ? 'Galleries' : 'Galeries');`
  `const galleriesHref = getRelativeLocaleUrl(locale, 'galleries');`
  In the <nav aria-label="Primary">: `<a href={galleriesHref} class="nav-link">{galleriesLabel}</a>`
- Design tokens available (do NOT redefine): --color-dominant, --color-ink, --space-md, --space-2xl, --space-xl. Site-wide `a:focus-visible` double-ring already global.

From src/lib/i18n-paths.ts (switcher — NO change needed):
- `hasTranslatedCounterpart` returns true for all non-'404' slugs, so the switcher already maps `/about/` ↔ `/en/about/` correctly once both pages exist. Do not modify this file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — Playwright locale-pair content assertions for the About page</name>
  <files>tests/e2e/about.spec.ts</files>
  <read_first>
    - tests/e2e/i18n.spec.ts (exact structure analog: imports, test.describe, `RED (Wave 0)` comment convention, locale-pair copy-differs assertion style)
    - .planning/phases/03-about-contact/03-UI-SPEC.md (Layout Notes → About Page: exact locked copy strings this test must assert)
    - .planning/phases/03-about-contact/03-PATTERNS.md (tests/e2e/about.spec.ts section — locator/assertion guidance)
  </read_first>
  <action>
    Create tests/e2e/about.spec.ts following tests/e2e/i18n.spec.ts's exact scaffolding (`import { test, expect } from '@playwright/test';`, a top `RED (Wave 0)` comment explaining the file is expected to FAIL until Task 2 builds about.astro / en/about.astro, one `test.describe('about page content', ...)` block). Write these tests (they MUST fail now because the routes 404):
    (1) FR: `page.goto('/about/')`; assert `html[lang="fr"]`; assert the `<h1>` contains "À propos"; assert body text contains the D-06 locked FR placeholder "Précisions sur le médium et la technique à venir — en attente de confirmation avec l'artiste."; assert the atelier subheading "Atelier & pratique" is visible.
    (2) EN: `page.goto('/en/about/')`; assert `html[lang="en"]`; assert `<h1>` contains "About"; assert body contains the D-06 locked EN placeholder "Details on medium and technique coming soon — pending confirmation with the artist."; assert the atelier subheading "Studio & practice" is visible.
    (3) Copy-differs-between-locales: navigate to `/about/` and capture `main` innerText, navigate to `/en/about/` and capture `main` innerText, assert they are not equal (mirrors i18n.spec.ts's header-copy-differs assertion).
    (4) Reachability: `page.goto('/')`, assert the header `<nav>` contains a link whose href resolves to the About route and clicking it lands on `/about/`.
    Use `getByRole`/`locator` idioms from the analog. Do NOT weaken assertions to pass early.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/about.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npx playwright test tests/e2e/about.spec.ts` RUNS (compiles, no syntax error) and reports FAILING assertions (routes 404 / nav link absent) — this is the intended RED state, not an error state.
    - Source: file asserts both the FR and EN D-06 placeholder copy strings byte-exact as quoted in 03-UI-SPEC.md.
    - Source: file contains a nav-reachability test that clicks the About link from `/`.
  </acceptance_criteria>
  <done>tests/e2e/about.spec.ts exists, is syntactically valid, and fails only because the About pages and nav link do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — Build FR/EN About pages + add About nav link to BaseLayout</name>
  <files>src/pages/about.astro, src/pages/en/about.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/pages/galleries/index.astro (static-page shell + locked Display <h1> style block to copy verbatim)
    - src/pages/index.astro (correct top-level import depth: `../layouts/...` for the FR page)
    - src/layouts/BaseLayout.astro (nav to extend — replicate the galleriesLabel/galleriesHref hardcoded-fallback pattern for About)
    - .planning/phases/03-about-contact/03-UI-SPEC.md (Layout Notes → About Page: structure, spacing tokens, exact copy)
    - .planning/phases/03-about-contact/03-PATTERNS.md (about.astro section — placeholder/italic styling guidance)
  </read_first>
  <action>
    Build the FR page src/pages/about.astro and its EN mirror src/pages/en/about.astro (per D-02: single flowing page, no tabs/sub-nav/boxed panels; per D-03: no portrait/photo). Frontmatter: only the BaseLayout import at the correct depth (`../layouts/BaseLayout.astro` for FR, `../../layouts/BaseLayout.astro` for EN) — no Sanity fetch (Pattern 3: hardcoded content). Add the file-level header comment citing ABOUT-01/ABOUT-02 and the D-01/D-04 amendment (placeholder copy because no source content exists on the live site).

    Page structure (Display <h1>, plain, NO Wild-Strawberry panel — the panel is reserved for gallery titles only):
    - `<h1>` — FR "À propos" / EN "About".
    - Bio section: one Body-role paragraph, `font-style: italic` (placeholder marker per the amendment). FR: "Le texte de présentation de Romane sera bientôt disponible ici — en attente de sa version définitive." EN: "Romane's biography will be available here soon — pending her final text."
    - Atelier subheading (Heading role, 20px/900): FR "Atelier & pratique" / EN "Studio & practice".
    - Atelier/practice placeholder paragraph (Body, italic) covering studio/where-she-works: FR "Informations sur l'atelier et la pratique de Romane à venir prochainement." EN "Information about Romane's studio and practice is coming soon."
    - D-06 medium/technique placeholder paragraph (Body, italic) — EXACT locked copy: FR "Précisions sur le médium et la technique à venir — en attente de confirmation avec l'artiste." EN "Details on medium and technique coming soon — pending confirmation with the artist." Do NOT infer analog/film/darkroom from the work.

    Scoped `<style>`: reuse the locked Display <h1> block from galleries/index.astro (rename selector to `.about-page h1`); wrapper `.about-page { max-width: 640px; margin: 0 auto; padding: var(--space-2xl) var(--space-md); }`; `var(--space-2xl)` gap before the atelier subheading; `var(--space-md)` gap between subheading and its paragraph; `.about-page .placeholder { font-style: italic; }` (or apply italic inline via a class) — no box/border/background on placeholders (D-02). Reference existing tokens only; never hardcode a hex/px that has a token. FR and EN files must be byte-identical except copy strings and import depth.

    In src/layouts/BaseLayout.astro, add an About nav link mirroring the galleries pattern exactly: compute `const aboutLabel = locale === 'en' ? 'About' : 'À propos';` and `const aboutHref = getRelativeLocaleUrl(locale, 'about');`, then add `<a href={aboutHref} class="nav-link">{aboutLabel}</a>` inside `<nav aria-label="Primary">` after the galleries link. (About content is not in CMS scope — CMS-01 is galleries-only — so the label is a hardcoded locale conditional, not a Sanity navLabels field.)
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/about.spec.ts && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npx playwright test tests/e2e/about.spec.ts` passes all assertions (both locales render, D-06 copy present, copy differs FR/EN, nav link reaches `/about/`).
    - Behavior: full `npm run test:e2e` (existing i18n/gallery suites) stays green — the added nav link must not break the switcher or existing header assertions.
    - Source: `grep -q "getRelativeLocaleUrl(locale, 'about')" src/layouts/BaseLayout.astro` succeeds.
    - Source: src/pages/about.astro and src/pages/en/about.astro contain the byte-exact D-06 placeholder strings for their locale; neither contains any inferred medium/technique claim.
    - Source: about.astro frontmatter has no `getGalleries`/`sanity` import (hardcoded-content Pattern 3).
  </acceptance_criteria>
  <done>Both About pages render in FR and EN with bio + atelier/practice placeholder copy and the D-06 locked text; the header nav links to About in both locales; the About e2e suite and the full e2e suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → static HTML | All About content is hardcoded and prerendered at build; there is no request-time compute and no untrusted input crosses any boundary on this page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | about.astro / en/about.astro content | accept | Content is intentionally public marketing/bio copy; placeholder text contains no PII or secrets. No action needed. |
| T-03-01-02 | Tampering | src/layouts/BaseLayout.astro (shared chrome) | mitigate | Nav change is additive (one static `<a>`); full e2e suite gate confirms existing switcher/cookie/header contracts still pass, preventing regression of the shared layout. |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (per 03-RESEARCH.md: zero new dependencies this phase). Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `npm run test:e2e` green (about.spec.ts + existing i18n/gallery suites).
- `npm run build` succeeds (static output, no Sanity fetch added for About).
- Manual spot-check optional: `/about/` and `/en/about/` render with header nav link present.
</verification>

<success_criteria>
- ABOUT-01: Visitor can read a bio section on the About page in both FR and EN (placeholder copy, clearly marked, pending Romane's final text).
- ABOUT-02: About page includes an atelier/practice section (studio + medium/technique) in both locales, using the locked D-06 placeholder copy.
- About page is reachable via a header nav link in both locales.
- All About e2e assertions pass; existing suites unaffected.
</success_criteria>

<output>
Create `.planning/phases/03-about-contact/03-01-SUMMARY.md` when done.
</output>
