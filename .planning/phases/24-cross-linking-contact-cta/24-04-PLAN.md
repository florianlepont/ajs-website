---
phase: 24-cross-linking-contact-cta
plan: 04
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - src/components/EditionDetailBody.astro
  - src/components/EditionDetailPage.astro
  - tests/e2e/edition.spec.ts
autonomous: true
requirements: [CONT-04, UI-03]

must_haves:
  truths:
    - "A visitor reaching the end of any édition's photo sequence sees the contact CTA and can click through to the contact page (CONT-04, D-04, D-07)."
    - "The CTA renders identically in structure, copy, and styling to the gallery-page CTA — one shared string, no per-page contextual wording (D-06)."
    - "On an édition page carrying BOTH links, the existing top related-gallery link keeps its shipped subtle bordered treatment unchanged while the new bottom CTA reads heavier (D-09)."
    - "Both render correctly at phone widths (390px) and desktop widths (1280px), with the existing édition layout unchanged (UI-03)."
  artifacts:
    - "src/components/EditionDetailBody.astro — .edition-detail__contact-cta-zone/-rule/-arrow markup+CSS, two new Props"
    - "src/components/EditionDetailPage.astro — contactCtaHref / contactCtaLabel prop threading"
    - "tests/e2e/edition.spec.ts — new describe block for CONT-04 at both viewports plus the D-09 no-style-bleed guard"
  key_links:
    - "GalleryGrid close -> contact CTA: the CTA must be the LAST child of .edition-detail__content, after the grid, with no change to GalleryGrid.astro itself."
    - "The existing .edition-detail__related rules (EditionDetailBody.astro lines 119-161) must survive byte-identical — REQUIREMENTS.md puts any change to the shipped forward cross-link out of scope."
---

<objective>
Add CONT-04's contact CTA to the end of every édition detail page's photo sequence, and prove with e2e that its arrival does not disturb the shipped EDN-08 related-gallery link sitting at the top of the same page.

Purpose: CONT-04 applies universally to both page types. This plan carries the édition half. It is deliberately independent of EDN-12's data layer — an édition page needs no new Sanity field, so this plan runs in parallel with the gallery-side render work in plan 24-03.

Output: an édition detail page that closes with the same pink-hairline CTA as a gallery page, with D-09's two-weight relationship verified rather than assumed.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cross-linking-contact-cta/24-CONTEXT.md
@.planning/phases/24-cross-linking-contact-cta/24-UI-SPEC.md
@.planning/phases/24-cross-linking-contact-cta/24-PATTERNS.md
@.planning/phases/24-cross-linking-contact-cta/24-VALIDATION.md
@.planning/phases/24-cross-linking-contact-cta/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the CONT-04 contact CTA at the end of the édition photo sequence</name>

  <files>src/components/EditionDetailBody.astro, src/components/EditionDetailPage.astro</files>

  <read_first>
    - `src/components/EditionDetailBody.astro` — the whole 162-line file: the header comment stating this body holds no locale strings of its own (lines 2-8), the `Props` interface (14-35), the destructure (37-54), the `.edition-detail__content` div (78-106) and specifically its closing structure at lines 105-106, and the entire `<style>` block (110-162) including the shipped `.edition-detail__related` rules at 119-161.
    - `.planning/phases/24-cross-linking-contact-cta/24-UI-SPEC.md` — "Component Contract: Contact CTA (CONT-04)" in full: the markup block, the CSS block, the States section, the Motion note, and the note explaining Astro scopes `<style>` per component so identical inner class names in the two body components cannot collide and no shared extraction is wanted.
    - `.planning/sketches/018-gallery-edition-contact-cta/index.html` — Variant B (`.cta-zone--b`, `.cta--b-rule`, `.cta--b`, `.arrow`) as the confirmed visual reference; note its inline comment saying the mobile hard line break must NOT be reproduced in the real implementation.
    - `src/components/EditionDetailPage.astro` — the whole file: the `<EditionDetailBody ... />` prop list to extend.
  </read_first>

  <action>
    In `src/components/EditionDetailBody.astro`:

    Add `contactCtaHref: string;` and `contactCtaLabel: string;` to the `Props` interface as the last two fields before `locale`, and add both to the destructure.

    Render the CTA as the LAST child of `.edition-detail__content`, immediately after the existing `GalleryGrid` line (105) and before that div closes (106) — D-07, literally the end of the photo sequence, in the same relative position as the gallery-page CTA. It renders UNCONDITIONALLY on every édition: no truthiness guard, no sold-state variant. Structure per UI-SPEC.md: an outer `div` with class `edition-detail__contact-cta-zone`, containing first an empty `div` with class `edition-detail__contact-cta-rule` (the hairline), then an anchor with class `edition-detail__contact-cta` whose `href` is the `contactCtaHref` prop, whose text child is the `contactCtaLabel` prop, and which ends with a `span` of class `edition-detail__contact-cta-arrow` carrying `aria-hidden="true"` and the right-arrow glyph. Do not hardcode any visible string and do not hardcode the contact path — both arrive as props, per this component's stated contract. Do not reproduce the sketch's hard line break; let text wrapping happen naturally.

    Add the CSS at the END of the existing `<style>` block, after the `@media (max-width: 767px)` block, copying UI-SPEC.md's CONT-04 block verbatim with the `edition-detail__` class prefix: the zone rule (`padding-top: var(--space-2xl)`), the rule element (`border-top: 1px solid var(--pink-600)`, `width: 48px`, `margin-bottom: var(--space-md)`), the anchor (`inline-flex`, `align-items: center`, `gap: 10px`, `color: var(--color-ink)`, `font-family: var(--font-display)`, `font-weight: 600`, `font-size: 20px`, `line-height: 1.25`, `text-decoration: none`, `border-bottom: 1px solid transparent`), the arrow (`color: var(--pink-600)`, `font-size: 20px`, `transition: transform 0.15s ease`), a combined `:hover, :focus-visible` rule on the anchor setting `border-bottom-color: var(--color-ink)`, and a combined `:hover .arrow, :focus-visible .arrow` rule setting `transform: translateX(4px)`.

    Four fidelity constraints: (a) use the literal `20px` and `600` and `1.25` values, not `var(--text-heading-size)` / `var(--weight-semibold)` / `var(--text-heading-leading)` — UI-SPEC.md pins the sketch's own literals and notes the tokens are numerically coincidental today, not semantically the same; (b) include the `:focus-visible` half of both combined selectors — the raw sketch had `:hover` only, and this project pairs the two everywhere; (c) add no `prefers-reduced-motion` guard for the 4px arrow nudge, and add no background-color, border-radius, or padding to the anchor — this is a text link with an arrow, not a filled or pill button (D-08); (d) D-09 is a hard no-touch boundary — every existing rule in the `.edition-detail__related` selector family and the existing mobile media-block content must remain byte-identical. Do not make the top link heavier, do not add differentiating chrome to it, and do not introduce any selector that could cascade onto it. Position and weight alone are the intended differentiation, confirmed by sketch 018.

    In `src/components/EditionDetailPage.astro`: pass `contactCtaHref={model.contactCtaHref}` and `contactCtaLabel={model.contactCtaLabel}` into `<EditionDetailBody />`, positioned as the last two props before `locale={locale}`.
  </action>

  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run build` exits 0.
    - `grep -c "edition-detail__contact-cta" src/components/EditionDetailBody.astro` is at least 10 (4 markup classes + 6 CSS selectors).
    - `grep -n "border-top: 1px solid var(--pink-600)" src/components/EditionDetailBody.astro` matches exactly 1 line.
    - `grep -n "translateX(4px)" src/components/EditionDetailBody.astro` matches exactly 1 line.
    - `grep -n "contactCtaHref\|contactCtaLabel" src/components/EditionDetailPage.astro` matches exactly 2 lines.
    - `grep -n "transition: transform 0.15s ease" src/components/EditionDetailBody.astro` matches exactly 1 line, and that rule is NOT nested inside any `@media` block — the arrow nudge is unconditional, per UI-SPEC.md's Motion note.
    - `git diff -U0 src/components/EditionDetailBody.astro` shows ZERO deleted or modified lines between the start of the `.edition-detail__related` rule and the end of the existing `@media (max-width: 767px)` block — the D-09 no-touch boundary. Only additions elsewhere in the file.
    - In the built artifact, `grep -rl "edition-detail__contact-cta" dist/editions/` returns at least one file.
    - `git diff --name-only` lists no `src/components/GalleryGrid.astro` and no `src/components/GalleryDetailBody.astro`.
  </acceptance_criteria>

  <done>
    Every édition detail page ends its photo sequence with the same pink-hairline-topped 20px Unbounded contact CTA the gallery pages use, and the shipped EDN-08 related-gallery link above it is untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-viewport e2e coverage for the édition CTA and the D-09 two-weight relationship</name>

  <files>tests/e2e/edition.spec.ts</files>

  <read_first>
    - `tests/e2e/edition.spec.ts` — the whole file, and specifically the "editions related-gallery cross-link (EDN-08)" describe block at lines 623-670: it shows the content-fragility-safe navigation idiom (go to `/editions/`, read `.editions-index__row` hrefs at runtime via `evaluateAll`), the walk-until-found loop, the `toHaveAttribute` href-pattern assertion, the `getComputedStyle` evaluate idiom, and the two-viewport structure to mirror.
    - `tests/unit/e2e-content-fragility.test.ts` — lines 1-60: the CI guard that FAILS the unit suite if any e2e spec embeds a literal `galleries/<slug>` or `editions/<slug>` route. Every édition href in the new tests must be discovered at runtime.
    - `src/layouts/BaseLayout.astro` — line 309: `--pink-600: #D6327C`, which resolves to `rgb(214, 50, 124)` in computed styles; line 347: `--text-label-size: 14px`, the size the existing related link must still report.
    - `playwright.config.ts` — the configured projects (`chromium`, `webkit-mobile`).
  </read_first>

  <action>
    Add one new `test.describe` block to `tests/e2e/edition.spec.ts`, titled for CONT-04, placed after the existing EDN-08 block.

    **Navigation:** discover édition hrefs at runtime exactly the way the EDN-08 block does — go to `/editions/`, read every `.editions-index__row` href via `evaluateAll`. Never write an édition slug as a literal anywhere in the file; the content-fragility guard in `tests/unit/e2e-content-fragility.test.ts` fails the unit suite on that pattern.

    **CTA universality (CONT-04):** walk EVERY discovered édition href at 390x844 and assert `.edition-detail__contact-cta` has count 1 on each — this proves the "applies universally, no conditional logic" requirement, not just that it works on one page.

    **CTA correctness:** for the first discovered href, at 390x844 and again at 1280x900, assert the anchor is visible; its `href` attribute matches a contact-route pattern allowing an optional base prefix and optional trailing slash; its text contains the French CTA fragment `Contactez-nous`; and its computed styles are `fontSize: '20px'`, `fontWeight: '600'`, `fontFamily` containing `Unbounded`, and `backgroundColor: 'rgba(0, 0, 0, 0)'` (the D-08 "no filled button" guard). Separately assert `.edition-detail__contact-cta-rule` has computed `borderTopWidth: '1px'` and `borderTopColor: 'rgb(214, 50, 124)'`, and that `.edition-detail__contact-cta-arrow` has computed `color: 'rgb(214, 50, 124)'`. Add a DOM-order assertion proving D-07: evaluate `compareDocumentPosition` between `.gallery-grid` and `.edition-detail__contact-cta` and assert the CTA follows the grid. Add one English-locale check: navigate to the `/en/` counterpart of the discovered href (derive it programmatically, not by writing a literal slug) and assert the CTA text contains `Get in touch`.

    **D-09 two-weight guard:** walk the discovered hrefs until one is found whose page has BOTH `.edition-detail__related` and `.edition-detail__contact-cta` present. On that page, at 1280x900, read the computed styles of both elements in a single evaluate and assert the pair reads as two deliberately different weights rather than a styling inconsistency: the top related link still reports `fontSize: '14px'` and `borderTopWidth: '2px'` (its shipped subtle bordered treatment, unchanged), while the CTA reports `fontSize: '20px'` and a `fontFamily` containing `Unbounded`. Assert the two elements' computed `fontSize` values are not equal. Also assert the related link's computed `fontFamily` does NOT contain `Unbounded`, proving no style bleed from the new display-font rules onto the shipped link.

    Head the block with a comment citing CONT-04, D-06 (one shared string across both page types), D-07, D-08, D-09, and UI-03's dual-viewport requirement. Note in that comment that the CTA's French copy asserted here is the exact same string the gallery-side spec asserts, which is what makes a future divergence visible.
  </action>

  <verify>
    <automated>npx playwright test tests/e2e/edition.spec.ts &amp;&amp; npx vitest run tests/unit/e2e-content-fragility.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx playwright test tests/e2e/edition.spec.ts` exits 0 across both configured projects.
    - `npx vitest run tests/unit/e2e-content-fragility.test.ts` exits 0 (no hardcoded slug introduced).
    - `grep -c "CONT-04" tests/e2e/edition.spec.ts` is at least 1 and `grep -c "D-09" tests/e2e/edition.spec.ts` is at least 1.
    - `grep -n "rgb(214, 50, 124)" tests/e2e/edition.spec.ts` matches at least 2 lines (hairline rule color + arrow color).
    - `grep -n "compareDocumentPosition" tests/e2e/edition.spec.ts` matches at least 1 line (D-07 DOM-order proof).
    - `grep -n "390" tests/e2e/edition.spec.ts` and `grep -n "1280" tests/e2e/edition.spec.ts` each match inside the new block (UI-03 dual-viewport).
    - The pre-existing "editions related-gallery cross-link (EDN-08)" describe block is unmodified: `git diff tests/e2e/edition.spec.ts` shows only additions, no deletions inside it.
  </acceptance_criteria>

  <done>
    Automated e2e proves the CTA renders on EVERY published édition, after the grid, at both viewport classes in both locales, and proves the shipped top related link keeps its 14px bordered treatment with no display-font bleed from the new CTA rules.
  </done>
</task>

</tasks>

## Artifacts this phase produces

Created by THIS plan:

| Symbol / Path | Kind | Notes |
|---|---|---|
| `EditionDetailBody` prop `contactCtaHref` | Astro component prop | `string` |
| `EditionDetailBody` prop `contactCtaLabel` | Astro component prop | `string` |
| `.edition-detail__contact-cta-zone` | CSS class + markup | Unconditional CTA wrapper, last child of `.edition-detail__content` |
| `.edition-detail__contact-cta-rule` | CSS class + markup | 48px pink hairline |
| `.edition-detail__contact-cta` | CSS class + markup | The anchor |
| `.edition-detail__contact-cta-arrow` | CSS class + markup | `aria-hidden` decorative arrow span |
| `tests/e2e/edition.spec.ts` → CONT-04 describe block | Playwright describe block | Includes the D-09 two-weight guard |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render model → emitted static HTML | CTA copy and destination cross into a public page |
| Visitor browser → static page | Public, unauthenticated read-only surface; no form, no fetch, no client JS added by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-10 | Spoofing | `.edition-detail__contact-cta` destination | low | mitigate | The `href` comes only from `model.contactCtaHref`, built in plan 24-02 from a hardcoded internal route segment via Astro's base-aware URL builder. No editor-controlled or visitor-controlled value reaches it, so the CTA cannot be redirected off-site through content. Task 2 asserts the emitted href matches a contact-route pattern on every viewport and locale checked. |
| T-24-11 | Tampering | New CSS rules cascading onto the shipped `.edition-detail__related` element | medium | mitigate | Task 1 forbids editing any existing rule and forbids selectors that could cascade onto the shipped link; task 2 adds a computed-style guard asserting the related link still reports 14px with a 2px border and no `Unbounded` family. Task 1's acceptance criteria additionally require a zero-deletion `git diff -U0` across the shipped rule range. |
| T-24-12 | Repudiation | New anchor bypassing keyboard/assistive access | low | mitigate | A real `<a href>` in DOM order, no `tabindex` manipulation, decorative arrow `aria-hidden`, explicit `:focus-visible` state on top of the site's global accent focus ring. Covered by the existing `tests/e2e/accessibility.spec.ts` regression pass. |
| T-24-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs zero packages (RESEARCH.md Standard Stack). Legitimacy gate not applicable. |

ASVS L1 scope note: V2/V3/V4/V6 not applicable. V5 unaffected — this plan constructs no queries and accepts no visitor input. No `high`-severity threat, so `security_block_on: high` is not tripped.
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npm run lint` exits 0.
- `npm run test:unit` exits 0.
- `npm run test:e2e` exits 0 (full Playwright suite — this is also the UI-03 regression pass for the édition surface).
- `npm run build && npm run test:artifact` exits 0.
- `git diff --name-only` lists only the three files in `files_modified`.
</verification>

<success_criteria>
- Every published édition detail page ends with the contact CTA, after the grid, in both locales.
- The CTA's structure, copy, and computed styling match the gallery-page CTA.
- The shipped `.edition-detail__related` link is byte-identical and still reports its 14px bordered computed styles.
- `GalleryGrid.astro` and `GalleryDetailBody.astro` are untouched.
- The full existing édition e2e suite still passes (UI-03 no-regression).
</success_criteria>

<output>
Create `.planning/phases/24-cross-linking-contact-cta/24-04-SUMMARY.md` when done.
</output>
