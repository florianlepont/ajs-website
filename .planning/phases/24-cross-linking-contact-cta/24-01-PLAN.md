---
phase: 24-cross-linking-contact-cta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/schemas/gallery.ts
  - src/lib/sanity.ts
  - src/lib/sanity-validation.ts
  - src/lib/related-edition.ts
  - tests/unit/gallery-query.test.ts
  - tests/unit/sanity-validation.test.ts
  - tests/unit/related-edition.test.ts
autonomous: true
requirements: [EDN-12]

must_haves:
  truths:
    - "A gallery document in Sanity can carry an optional reference to one édition document (D-01)."
    - "A published gallery fetched by getGalleries() carries its dereferenced relatedEdition {title, slug} intact."
    - "A gallery whose relatedEdition is absent, null, or dereferences to partial data yields no link data at all — never partial/broken data (D-02)."
    - "An édition fetched by getEditions() never carries a stray relatedEdition field (shared-base sanitizer stays leak-free)."
  artifacts:
    - "sanity/schemas/gallery.ts — relatedEdition reference field + relatedCollection group"
    - "src/lib/sanity.ts — Gallery.relatedEdition type + relatedEdition projection in GALLERIES_QUERY and GALLERY_BY_SLUG_QUERY"
    - "src/lib/sanity-validation.ts — relatedEdition sanitization in sanitizeGalleryDocument + exclusion in sanitizeEditionDocument"
    - "src/lib/related-edition.ts — getRelatedEditionLink() + RelatedEditionLink interface"
    - "tests/unit/related-edition.test.ts"
  key_links:
    - "GALLERIES_QUERY -> sanitizeGalleryDocument -> Gallery.relatedEdition: the load-bearing chain (gallery detail pages build from getGalleries() via getStaticPaths, NOT getGallery())."
    - "sanitizeGalleryDocument -> sanitizeEditionDocument destructure exclusion: adding a gallery-only field to the shared base MUST be paired with an exclusion, or every Edition silently gains a stray field."
---

<objective>
Build the complete EDN-12 data layer for the reverse gallery→édition cross-link: a new optional Sanity reference field on the `gallery` document type, its GROQ projection and TypeScript type, its defensive sanitization (plus the paired édition-side exclusion), and a pure locale-aware link-building helper.

Purpose: this is the entire "mirror the existing forward pattern" content-model half of EDN-12 (D-01). Everything downstream (render model, component render, e2e) depends on this chain resolving correctly. It ships with zero UI change, so it is independently verifiable by unit tests alone.

Output: `relatedEdition` reachable end-to-end from Sanity Studio to a typed, sanitized `Gallery.relatedEdition` value, plus `getRelatedEditionLink()` ready for the render model to call.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cross-linking-contact-cta/24-CONTEXT.md
@.planning/phases/24-cross-linking-contact-cta/24-RESEARCH.md
@.planning/phases/24-cross-linking-contact-cta/24-PATTERNS.md
@.planning/phases/24-cross-linking-contact-cta/24-VALIDATION.md
</context>

<discretion_resolutions>
CONTEXT.md left one implementation choice to Claude's Discretion; it is resolved here and is binding on every downstream plan:

**Parallel file, not generalization.** EDN-12's reverse helper ships as a NEW file `src/lib/related-edition.ts` exporting `getRelatedEditionLink()` and `RelatedEditionLink`. `src/lib/related-gallery.ts` and its exported `RelatedGalleryLink` type are NOT renamed, NOT generalized, and NOT edited.

Rationale: `.planning/REQUIREMENTS.md` "Out of Scope" states any change to the existing forward `relatedGallery` link is out of scope ("Already shipped and correct"). Generalizing would require renaming a shipped exported type and editing `page-models.ts`'s existing `EditionDetailModel.relatedLink` annotation — a non-additive diff against shipped, tested code for no user-visible gain. RESEARCH.md Pitfall 5 explicitly sanctions the parallel-file option and notes the duplication cost (~30 lines) is low.
</discretion_resolutions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the relatedEdition reference field, type, and GROQ projections</name>

  <files>sanity/schemas/gallery.ts, src/lib/sanity.ts, tests/unit/gallery-query.test.ts</files>

  <read_first>
    - `sanity/schemas/gallery.ts` — the file being modified; note its existing `groups` array (lines 23-29) has no `relatedCollection` group.
    - `sanity/schemas/edition.ts` — lines 12-21 (its `groups` array, which DOES declare `relatedCollection`) and lines ~72-85 (the `relatedGallery` field definition being mirrored).
    - `src/lib/sanity.ts` — lines 140-230: the `Gallery` interface, `GALLERIES_QUERY`, `GALLERY_BY_SLUG_QUERY`, the `Edition` interface's `relatedGallery` field + doc comment, and `EDITIONS_QUERY`/`EDITION_BY_SLUG_QUERY` (the projections being mirrored).
    - `tests/unit/gallery-query.test.ts` — the whole file; note the `vi.mock('@sanity/client')` harness, the `describe('getGalleries')` / `describe('getGallery')` blocks, and the existing `expect(fetchMock).toHaveBeenCalledWith(expect.stringContaining(...))` projection-assertion idiom.
    - `tests/unit/edition-query.test.ts` — lines 180-250: the four existing `relatedGallery` assertions to mirror (`projects relatedGallery`, populated-fixture passthrough, absent/null passthrough).
  </read_first>

  <behavior>
    - `GALLERIES_QUERY` contains the substring `relatedEdition` (load-bearing: gallery detail pages build from `getGalleries()` via `getStaticPaths`, per RESEARCH.md Pitfall 2).
    - `GALLERY_BY_SLUG_QUERY` contains the substring `relatedEdition` (API parity; `getGallery()` has zero call sites today).
    - `getGalleries()` returns a fixture gallery with `relatedEdition: {title: 'Rebut', slug: 'rebut'}` intact.
    - `getGalleries()` resolves without error for a fixture gallery with `relatedEdition: null`.
    - `getGallery(slug)` still binds `slug` as a GROQ parameter, never string-interpolated (ASVS V5, unchanged).
  </behavior>

  <action>
    Implement three coordinated changes (D-01 — mirror the shipped forward pattern exactly, reversed).

    1. `sanity/schemas/gallery.ts`: add a `relatedCollection` group to the `groups` array (title `'Édition liée'`), placed after the existing `content` group entry. Then add a `defineField` for `relatedEdition` — `type: 'reference'`, `group: 'relatedCollection'`, `to: [{type: 'edition'}]`, NO `validation` rule (the field is optional, D-02), `title: 'Édition liée (optionnel)'`, and a French editorial `description` written for Romane that mirrors the tone and structure of `edition.ts`'s `relatedGallery` description but reversed: it explains that this links a photo collection to the printed édition presenting the same work, gives the Rebut collection/édition pairing as the example, states that filling it shows a link to the édition on the collection's public page, and says to leave it empty when no matching édition exists. Place the field definition after the `statement` locale-text field and before `showOnHomePage`, so it lands in editorial reading order. Do NOT add it to the `preview.select` map.

    2. `src/lib/sanity.ts`: add `relatedEdition?: { title: string; slug: string } | null` to the `Gallery` interface, immediately after the `seo` field and before `images`. Give it a doc comment mirroring the shape of the `Edition.relatedGallery` comment: cite EDN-12, state it is an optional unidirectional cross-link target dereferenced from the `relatedEdition` reference field in `sanity/schemas/gallery.ts`, and note it is null/absent for galleries with no linked édition (the common case). Then append `, relatedEdition->{title, "slug": slug.current}` to the projection field list of BOTH `GALLERIES_QUERY` and `GALLERY_BY_SLUG_QUERY`, positioned last, immediately after the interpolated `${IMAGES_WITH_DIMENSIONS_PROJECTION}`. Do not otherwise reorder or reformat either query. `GALLERY_BY_SLUG_QUERY`'s `slug.current == $slug` parameter binding stays exactly as-is.

    3. `tests/unit/gallery-query.test.ts`: add four tests mirroring `edition-query.test.ts` lines 188-246. In `describe('getGalleries')`: (a) a projection test asserting `fetchMock` was called with a string containing `relatedEdition`, carrying a comment explaining this is the load-bearing projection because gallery detail pages receive their `gallery` prop from `getGalleries()` via `getStaticPaths`, not from `getGallery()`; (b) a populated-fixture passthrough test whose fixture is a minimal renderable gallery (title, slug, statement fr/en, one valid image matching the file's existing image fixture shape) plus `relatedEdition: {title: 'Rebut', slug: 'rebut'}`, asserting the returned `result[0].relatedEdition` equals that object; (c) a null-case test with `relatedEdition: null` asserting `getGalleries()` resolves without throwing. In `describe('getGallery')`: (d) a parity projection test asserting the by-slug query string also contains `relatedEdition`.

    The Studio schema change is editorial config with no runtime test surface of its own; its correctness is proven downstream by task 2's sanitizer tests and by the phase's Studio checkpoint in plan 24-05.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/gallery-query.test.ts tests/unit/edition-query.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/unit/gallery-query.test.ts tests/unit/edition-query.test.ts` exits 0.
    - `grep -c 'relatedEdition' src/lib/sanity.ts` is at least 3 (interface field + two query projections).
    - `grep -n 'relatedEdition->{title, "slug": slug.current}' src/lib/sanity.ts` matches on exactly 2 lines.
    - `grep -n "slug.current == \$slug" src/lib/sanity.ts` still matches inside `GALLERY_BY_SLUG_QUERY` (ASVS V5 parameter binding preserved).
    - `grep -c "name: 'relatedEdition'" sanity/schemas/gallery.ts` equals 1, and `grep -c "to: \[{type: 'edition'}\]" sanity/schemas/gallery.ts` equals 1.
    - `grep -c "relatedCollection" sanity/schemas/gallery.ts` is at least 2 (group declaration + field `group:` reference).
    - `npm --prefix sanity run typecheck` exits 0.
    - `npm --prefix sanity run lint` exits 0.
  </acceptance_criteria>

  <done>
    A gallery document type exposes an optional "Édition liée" reference in Studio; both gallery GROQ queries dereference it to `{title, slug}`; `Gallery.relatedEdition` is typed; four new unit tests cover the projection, the populated passthrough, and the null case.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sanitize relatedEdition on galleries and exclude it from éditions</name>

  <files>src/lib/sanity-validation.ts, tests/unit/sanity-validation.test.ts</files>

  <read_first>
    - `src/lib/sanity-validation.ts` — lines 190-310: `issue()`, `sanitizeGalleryDocument()` (the function being extended), and `sanitizeEditionDocument()` (which calls the gallery sanitizer as its base, at line 257, and destructures gallery-only fields out at lines 292-297). Note `sanitizeGalleryDocument` builds an EXPLICIT return object — an unhandled field is dropped silently, which is why this task is required for the data to reach the render layer at all.
    - `src/lib/sanity-validation.ts` — lines 270-290: the existing `relatedGallery` defensive-dereference block and its `issue('relatedGallery.removed', value)` push — the exact tri-state pattern to mirror.
    - `tests/unit/sanity-validation.test.ts` — lines 1-20 (imports: only the exported `sanitizeGallery`/`sanitizeEdition`/`sanitizeGalleries`/`sanitizeEditions` wrappers are reachable, the inner `*Document` functions are module-private) and lines 112-181 (`describe('gallery and edition sanitizers')`, the block to extend, plus the `renderableImage` fixture at lines 15-18).
  </read_first>

  <behavior>
    - `sanitizeGallery({...valid, relatedEdition: {title: 'Rebut', slug: 'rebut'}}).value.relatedEdition` equals `{title: 'Rebut', slug: 'rebut'}`.
    - `sanitizeGallery({...valid, relatedEdition: null}).value.relatedEdition` is `null`.
    - `sanitizeGallery(valid).value` has no `relatedEdition` key at all when the field is absent from the input.
    - `sanitizeGallery({...valid, relatedEdition: {title: 'Rebut'}}).value.relatedEdition` is `undefined` (key absent) AND `.issues` contains a `relatedEdition.removed` entry — a partially-dereferenced reference is dropped, never rendered.
    - `sanitizeEdition({...validEdition, relatedEdition: {title: 'X', slug: 'x'}}).value` has NO `relatedEdition` key (shared-base leak guard, RESEARCH.md Pitfall 1).
  </behavior>

  <action>
    Two coordinated edits that MUST land in the same commit (RESEARCH.md Pitfall 1 — the highest-risk correctness pattern in this phase).

    1. In `sanitizeGalleryDocument()` (`src/lib/sanity-validation.ts`, starts line 201): after the existing `seo` handling block (lines 231-234) and before the `return`, add a `relatedEdition` block mirroring `sanitizeEditionDocument`'s `relatedGallery` block verbatim in structure — read `record.relatedEdition` through `asRecord()`, pull `title` and `slug` through `nonEmptyString()`, and resolve the same tri-state: an object `{title, slug}` when both are non-empty; `null` when the raw input is exactly `null`; otherwise `undefined`. Then push `issue('relatedEdition.removed', value)` when the raw input is neither `undefined` nor `null` yet failed to resolve — matching the existing `relatedGallery.removed` push condition exactly. Add the resolved value to the returned object literal as `...(relatedEdition !== undefined ? {relatedEdition} : {})`, placed last, after the existing `...(seo ? {seo} : {})` spread.

    2. In `sanitizeEditionDocument()` (starts line 256): add `relatedEdition: _relatedEdition` to the existing gallery-only-field destructure at lines 292-293, and add a matching `void _relatedEdition` line alongside the existing `void` statements. Without this, `...shared` spreads the new gallery-only field onto every `Edition`, which has no such field in its schema or its interface.

    3. In `tests/unit/sanity-validation.test.ts`, extend `describe('gallery and edition sanitizers')` with the five behaviors above. Build gallery fixtures on the existing `renderableImage` constant and the same minimal shape the neighbouring tests use (`{title, slug, statement, images}`); build the édition fixture on the shape already used by the existing "applies the same renderability contract to edition detail and collection data" test. Name the leak-guard test so it states what it protects, and give it a comment citing RESEARCH.md Pitfall 1 and the fact that the gallery and édition sanitizers share a base function. Assert the absent-key cases with `expect(result.value).not.toHaveProperty('relatedEdition')`, not with an `undefined` equality check.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/sanity-validation.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/unit/sanity-validation.test.ts` exits 0.
    - `grep -c 'relatedEdition' src/lib/sanity-validation.ts` is at least 6 (record read, title, slug, tri-state binding, issue push, return spread, edition destructure, void).
    - `grep -n "relatedEdition: _relatedEdition" src/lib/sanity-validation.ts` matches exactly 1 line, and `grep -n "void _relatedEdition" src/lib/sanity-validation.ts` matches exactly 1 line.
    - `grep -n "relatedEdition.removed" src/lib/sanity-validation.ts` matches exactly 1 line.
    - `tests/unit/sanity-validation.test.ts` contains a test asserting `sanitizeEdition(...).value` does not have a `relatedEdition` property, and that test passes.
    - `npm run typecheck` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>

  <done>
    A published gallery's dereferenced `relatedEdition` survives sanitization as `{title, slug}`, a partial dereference is dropped with a diagnostic issue, and no `Edition` ever carries a stray `relatedEdition` field.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the pure getRelatedEditionLink helper</name>

  <files>src/lib/related-edition.ts, tests/unit/related-edition.test.ts</files>

  <read_first>
    - `src/lib/related-gallery.ts` — the whole 45-line file: the exact structure, doc comments, `getRelativeLocaleUrl` import, defensive guards, and fr/en copy construction being mirrored. Note line 41's English string uses TYPOGRAPHIC curly double quotation marks around the interpolated title, not straight ASCII quotes.
    - `tests/unit/related-gallery.test.ts` — the whole 38-line file: the six-case coverage shape (fr populated, en populated, null, undefined, empty slug, empty title) to mirror.
    - `.planning/phases/24-cross-linking-contact-cta/24-UI-SPEC.md` — the "Copywriting Contract" table, rows for the EDN-12 reverse cross-link (fr/en).
  </read_first>

  <behavior>
    - `getRelatedEditionLink({title: 'Rebut', slug: 'rebut'}, 'fr')` returns `href` matching `/\/editions\/rebut\/?$/` and `text` exactly `Voir l'édition « Rebut »`.
    - `getRelatedEditionLink({title: 'Rebut', slug: 'rebut'}, 'en')` returns `href` matching `/\/en\/editions\/rebut\/?$/` and `text` exactly `View the “Rebut” edition` (typographic curly quotes).
    - Returns `null` for `null`, for `undefined`, for an empty `slug`, and for an empty `title`.
  </behavior>

  <action>
    Create `src/lib/related-edition.ts` as a direct structural mirror of `src/lib/related-gallery.ts` (see `<discretion_resolutions>` — a parallel file, NOT a generalization; do not edit `related-gallery.ts`).

    Export an interface `RelatedEditionLink` with `href: string` and `text: string`. Export a function `getRelatedEditionLink(relatedEdition, locale)` typed against a local `RelatedEdition = { title: string; slug: string } | null | undefined` alias and a `'fr' | 'en'` locale, returning `RelatedEditionLink | null`. Import `getRelativeLocaleUrl` from `astro:i18n` and build the href as `getRelativeLocaleUrl(locale, \`editions/${slug}\`)` — never by manual string concatenation, since only this import applies Astro's configured `base` and keeps the href passing CI's un-prefixed-link grep guard on GitHub Pages.

    Apply the identical defensive guards (D-02): return `null` immediately when the argument is falsy, and return `null` when either `title` or `slug` is missing or whitespace-only. Build the visible text per UI-SPEC.md's Copywriting Contract: French uses guillemets around the title (`Voir l'édition « Rebut »`); English uses TYPOGRAPHIC curly double quotation marks around the title, matching `related-gallery.ts` line 41's existing convention rather than straight ASCII quotes.

    Head the file with a doc comment citing EDN-12, stating it is the reverse direction of `related-gallery.ts`'s EDN-08 helper, explaining why it is a parallel file rather than a generalization (keeps the shipped forward helper and its exported type untouched), and explaining the defensive-null contract: a reference to an unpublished or archived édition can dereference to partial data under Sanity's published perspective, and rendering nothing is correct.

    Create `tests/unit/related-edition.test.ts` covering the six behaviors above, mirroring `tests/unit/related-gallery.test.ts`'s structure and its fixture-based comment explaining that no live Sanity dataset write occurs.
  </action>

  <verify>
    <automated>npx vitest run tests/unit/related-edition.test.ts tests/unit/related-gallery.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/unit/related-edition.test.ts tests/unit/related-gallery.test.ts` exits 0 with 6 passing tests in the new file.
    - `src/lib/related-edition.ts` exports both `RelatedEditionLink` and `getRelatedEditionLink`.
    - `grep -c "getRelativeLocaleUrl" src/lib/related-edition.ts` is at least 2 (import + call).
    - `git diff --name-only` does NOT list `src/lib/related-gallery.ts` or `tests/unit/related-gallery.test.ts`.
    - `npm run typecheck` exits 0.
    - `npm run lint` exits 0.
  </acceptance_criteria>

  <done>
    `getRelatedEditionLink()` produces base-path-safe, locale-correct hrefs and copy for a populated reference, and `null` for every absent or malformed input — with the shipped forward helper untouched.
  </done>
</task>

</tasks>

## Artifacts this phase produces

Created by THIS plan:

| Symbol / Path | Kind | Notes |
|---|---|---|
| `sanity/schemas/gallery.ts` → `relatedEdition` field | Sanity schema field | `type: 'reference'`, `to: [{type: 'edition'}]`, optional, group `relatedCollection` |
| `sanity/schemas/gallery.ts` → `relatedCollection` group | Sanity schema group | Title `'Édition liée'` |
| `src/lib/sanity.ts` → `Gallery.relatedEdition` | TypeScript interface field | `{ title: string; slug: string } \| null \| undefined` |
| `src/lib/sanity.ts` → `relatedEdition->{...}` projection | GROQ projection | Added to `GALLERIES_QUERY` (load-bearing) and `GALLERY_BY_SLUG_QUERY` (parity) |
| `src/lib/sanity-validation.ts` → `relatedEdition` sanitization | internal logic | Tri-state in `sanitizeGalleryDocument`; exclusion in `sanitizeEditionDocument` |
| `src/lib/sanity-validation.ts` → `relatedEdition.removed` | diagnostic issue code | Pushed on partial dereference |
| `src/lib/related-edition.ts` | new file | — |
| `RelatedEditionLink` | exported interface | `{ href: string; text: string }` |
| `getRelatedEditionLink(relatedEdition, locale)` | exported function | Returns `RelatedEditionLink \| null` |
| `tests/unit/related-edition.test.ts` | new file | — |

Consumed by later plans: `24-02` imports `getRelatedEditionLink` and reads `Gallery.relatedEdition`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Content Lake → Astro build | Untrusted-shaped JSON crosses into the build; document shape is not guaranteed by the client |
| Sanity Studio editor → Content Lake | Editor-authored reference target; the referenced document may be unpublished, archived, or deleted after the reference is set |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | `GALLERY_BY_SLUG_QUERY` in `src/lib/sanity.ts` (ASVS V5) | medium | mitigate | The `relatedEdition->{title, "slug": slug.current}` addition is a static literal appended to the existing template; `getGallery(slug)` keeps binding `$slug` as a GROQ parameter (never string-interpolated). Task 1 acceptance criteria greps for the surviving `slug.current == $slug` binding. |
| T-24-02 | Information Disclosure | `relatedEdition` GROQ dereference | low | mitigate | Projection selects only `title` and `slug` — no statement, images, or publication metadata of the target document is pulled. A dereference to an unpublished document yields partial/empty fields, which task 2's tri-state and task 3's guards reduce to "render nothing". |
| T-24-03 | Tampering | `sanitizeEditionDocument` shared-base spread | medium | mitigate | Task 2 pairs the new gallery-only field with an explicit destructure exclusion in the same commit, proven by a dedicated leak-guard unit test (RESEARCH.md Pitfall 1). |
| T-24-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs zero packages (RESEARCH.md Standard Stack: "This phase adds zero dependencies"). No package-manager install task exists, so the legitimacy gate is not applicable and no `[ASSUMED]`/`[SUS]` checkpoint is required. |

ASVS L1 scope note (from RESEARCH.md Security Domain): V2/V3/V4/V6 are not applicable — no auth, session, access-control, or cryptography surface is touched. V5 applies narrowly and is covered by T-24-01. No threat is rated `high`, so the configured `security_block_on: high` gate is not tripped.
</threat_model>

<verification>
- `npm run typecheck` exits 0 (astro check).
- `npm run lint` exits 0.
- `npm run test:unit` exits 0 (full Vitest suite, including coverage-adjacent existing specs).
- `npm --prefix sanity run typecheck` and `npm --prefix sanity run lint` exit 0 (Studio schema is a CI blocking gate).
- `git diff --name-only` lists only the seven files in `files_modified`.
</verification>

<success_criteria>
- A gallery in Sanity Studio can reference exactly one édition through an optional "Édition liée" field.
- `getGalleries()` returns galleries whose `relatedEdition` is either a complete `{title, slug}`, `null`, or absent — never partial.
- `getEditions()` returns éditions with no `relatedEdition` key.
- `getRelatedEditionLink()` returns a base-path-safe locale URL and correct fr/en copy, or `null`.
- `src/lib/related-gallery.ts` is byte-identical to its pre-plan state.
</success_criteria>

<output>
Create `.planning/phases/24-cross-linking-contact-cta/24-01-SUMMARY.md` when done.
</output>
