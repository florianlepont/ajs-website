---
phase: 01-foundation-bilingual-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - astro.config.mjs
  - tsconfig.json
  - .nvmrc
  - .gitignore
  - src/env.d.ts
  - src/pages/index.astro
  - playwright.config.ts
  - vitest.config.ts
  - tests/unit/i18n-paths.test.ts
  - tests/e2e/i18n.spec.ts
autonomous: true
requirements: [I18N-01, I18N-02]

must_haves:
  truths:
    - "`npm run build` produces a static `dist/` with a French page at `/` (Astro i18n configured, no adapter)"
    - "The unit test file imports `getSwitcherHref` from `src/lib/i18n-paths.ts` and FAILS (module not built yet — RED)"
    - "The e2e test file asserts FR-at-`/`, EN-at-`/en/`, switcher navigation and cookie persistence, and FAILS (implementation not built yet — RED)"
    - "`npx vitest run` and `npx playwright test` both execute (harness wired) rather than erroring on missing config"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro i18n config: defaultLocale fr, locales [fr,en], prefixDefaultLocale false (D-01), output static default (no adapter)"
      contains: "prefixDefaultLocale"
    - path: "vitest.config.ts"
      provides: "Vitest runner config for the pure-TS utility unit test"
    - path: "playwright.config.ts"
      provides: "Playwright e2e config with webServer serving the built static output"
      contains: "webServer"
    - path: "tests/unit/i18n-paths.test.ts"
      provides: "Failing unit test for the shared-slug switcher utility"
      contains: "getSwitcherHref"
    - path: "tests/e2e/i18n.spec.ts"
      provides: "Failing e2e smoke test covering I18N-01 and I18N-02"
  key_links:
    - from: "playwright.config.ts"
      to: "dist/ (built static output)"
      via: "webServer command (astro preview)"
      pattern: "webServer"
---

<objective>
Scaffold the Astro static-site project with built-in bilingual i18n routing (French at root, English under `/en/`, no adapter — per D-01 and OVH static-hosting constraint), stand up the Playwright + Vitest test harness, and author the failing tests that later plans make green. This is the Walking Skeleton's foundation: nothing renders real chrome yet, but the project builds, the i18n routing config is in place, and the Nyquist test harness exists in a RED state so every subsequent plan has an automated gate.

Purpose: Establish the first patterns (config, structure, test harness) every later phase copies, and satisfy the MVP rule that a failing end-to-end test for the happy path exists before implementation.
Output: A buildable Astro project + configured test runners + RED unit and e2e tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-PATTERNS.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-VALIDATION.md

<interfaces>
<!-- Contracts this plan establishes for downstream plans. Executor authors the RED tests against these signatures. -->
The switcher utility that Plan 04 implements (do NOT implement it here — only test against it):

  // src/lib/i18n-paths.ts  (built in Plan 04)
  export function getSwitcherHref(currentPath: string, targetLocale: 'fr' | 'en'): string;
  // Given a current pathname, returns the equivalent page URL in targetLocale.
  // '/'         + 'en' -> '/en/'
  // '/en/'      + 'fr' -> '/'
  // '/rebut'    + 'en' -> '/en/rebut'     (shared-slug convention, forward-looking)
  // '/en/rebut' + 'fr' -> '/rebut'

Astro i18n config target (from RESEARCH.md Pattern 1, lines 202-219):
  defaultLocale: 'fr', locales: ['fr','en'], routing.prefixDefaultLocale: false
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project with i18n routing config</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Pattern 1 lines 202-219; Standard Stack lines 60-102; Pitfall 4 line 302 Node pin; Anti-Patterns line 267 no adapter; Environment Availability lines 409-429)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-PATTERNS.md (astro.config.mjs assignment lines 42-58; Recommended Project Structure)
    - CLAUDE.md (Technology Stack: Astro 7.0.6, prefixDefaultLocale:false recommendation)
  </read_first>
  <action>
    Initialize an Astro project at the repo root (greenfield — no `src/` or `package.json` exists yet). Install `astro@7.0.6` as the only runtime dependency (do NOT install `@astrojs/cloudflare`, `wrangler`, or any adapter — `output: 'static'` is the framework default and correct for OVH static hosting; installing an adapter here is actively wrong per RESEARCH.md Anti-Patterns).
    Create `astro.config.mjs` with an `i18n` block: `defaultLocale: 'fr'`, `locales: ['fr', 'en']`, `routing: { prefixDefaultLocale: false }` (D-01: French at root, English under `/en/`). Do not add Accept-Language detection or auto-redirect (explicitly excluded by D-02).
    Create `.nvmrc` pinning Node `22` (Astro 7.0.6 requires `>=22.12.0`, RESEARCH.md Pitfall 4). Create `tsconfig.json` extending Astro's strict TS preset. Create `src/env.d.ts` with the Astro client types reference. Create `.gitignore` covering `node_modules/`, `dist/`, `.astro/`, and env files.
    Create a minimal `src/pages/index.astro` containing only a bare French placeholder string (e.g. a single `<h1>` with a French welcome word) so `npm run build` produces a non-empty `dist/`. This is a temporary build-enabling stub; Plan 04 replaces it with the real BaseLayout-wrapped, Sanity-sourced homepage. Do NOT add nav/footer/switcher here — those belong to Plan 04.
    Add `build`, `dev`, and `preview` scripts to `package.json` (Astro's standard `astro build` / `astro dev` / `astro preview`).
  </action>
  <verify>
    <automated>npm run build && test -f dist/index.html && grep -q "prefixDefaultLocale" astro.config.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` contains `defaultLocale: 'fr'`, `locales: ['fr', 'en']`, and `prefixDefaultLocale: false`.
    - `package.json` lists `astro` at `7.0.6` and NO `@astrojs/cloudflare` or `wrangler` dependency (`grep -c "cloudflare\|wrangler" package.json` returns 0).
    - `.nvmrc` file content is `22`.
    - `npm run build` exits 0 and produces `dist/index.html`.
    - No adapter is configured: `grep -c "adapter" astro.config.mjs` returns 0.
  </acceptance_criteria>
  <done>Astro project builds to static `dist/` with FR-at-root i18n routing configured and no SSR adapter.</done>
</task>

<task type="auto">
  <name>Task 2: Install and configure Playwright + Vitest test harness</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-VALIDATION.md (Test Infrastructure, Wave 0 Requirements lines 52-58)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Validation Architecture lines 431-462; Testing stack lines 78-83)
  </read_first>
  <action>
    Install dev dependencies `@playwright/test@1.61.1` and `vitest@4.1.9`, then run `npx playwright install --with-deps chromium` (RESEARCH.md-verified versions; both Approved in the Package Legitimacy Audit — no legitimacy checkpoint required).
    Create `vitest.config.ts` — minimal config targeting `tests/unit/**/*.test.ts`, node environment (the utility under test is pure TS, no DOM), no watch mode.
    Create `playwright.config.ts` — `testDir: './tests/e2e'`, a `webServer` block whose `command` is `npm run preview` (Astro's built-in static preview server) with `url: 'http://localhost:4321'` and `reuseExistingServer: !process.env.CI`, `use.baseURL: 'http://localhost:4321'`, chromium project only. Do NOT use any `--watch` flag anywhere.
    Add npm scripts `test:unit` (`vitest run`) and `test:e2e` (`playwright test`) to `package.json`.
  </action>
  <verify>
    <automated>test -f playwright.config.ts && test -f vitest.config.ts && grep -q "webServer" playwright.config.ts && npx vitest run --dir tests/unit --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `playwright.config.ts` exists and contains a `webServer` block referencing `npm run preview` and baseURL `http://localhost:4321`.
    - `vitest.config.ts` exists and contains no `watch` enablement.
    - `package.json` devDependencies include `@playwright/test` and `vitest`.
    - `npx vitest run --passWithNoTests` exits 0 (runner is wired even before tests exist).
    - `grep -rc "\-\-watch" playwright.config.ts vitest.config.ts package.json` returns 0.
  </acceptance_criteria>
  <done>Both test runners are installed, configured, and execute against the project; Playwright serves the built static output via `astro preview`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author failing unit + e2e tests (RED)</name>
  <behavior>
    Unit (tests/unit/i18n-paths.test.ts) — asserts getSwitcherHref:
      - '/' + 'en' returns '/en/'
      - '/en/' + 'fr' returns '/'
      - '/rebut' + 'en' returns '/en/rebut'
      - '/en/rebut' + 'fr' returns '/rebut'
    E2E (tests/e2e/i18n.spec.ts):
      - "locale content" group: GET '/' renders French nav/footer chrome + French placeholder homepage; GET '/en/' renders English chrome + English homepage; both pull site-title/nav/footer copy that differs by locale.
      - "switcher" group: clicking the "FR | EN" switcher from '/' lands on '/en/' (and the reverse); after clicking to EN, the `ajs_locale` cookie equals 'en'; with `ajs_locale=en` pre-set in a fresh browser context, visiting '/' redirects to '/en/'.
  </behavior>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-VALIDATION.md (Per-Task Verification Map lines 37-48)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Phase Requirements → Test Map lines 442-448; Pattern 2 lines 221-238; Pattern 3 lines 240-263)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-UI-SPEC.md (Copywriting Contract — switcher "FR | EN", 404 copy)
    - tests/unit and tests/e2e directory (empty; confirm no pre-existing tests)
  </read_first>
  <action>
    Write `tests/unit/i18n-paths.test.ts` importing `getSwitcherHref` from `../../src/lib/i18n-paths` and asserting the four mappings in the behavior block. This MUST fail now because `src/lib/i18n-paths.ts` does not exist yet (Plan 04 builds it) — that RED state is the intended outcome, not a bug.
    Write `tests/e2e/i18n.spec.ts` with two `test.describe` groups named exactly `locale content` and `switcher` (the VALIDATION.md `-g` filters target these names). Assert: French chrome at `/`, English chrome at `/en/`, switcher click navigation to the equivalent locale page, `document.cookie` contains `ajs_locale=en` after switching, and cookie-driven `/` -> `/en/` redirect with a pre-set cookie. Reference the "FR | EN" switcher text and locale-differentiated site title/nav/footer copy per UI-SPEC. These MUST fail now (no switcher/chrome/pages built yet).
    Do NOT stub or weaken assertions to make them pass — they are deliberately RED until Plans 03/04 implement the utility, chrome, and pages.
  </action>
  <verify>
    <automated>test -f tests/unit/i18n-paths.test.ts && test -f tests/e2e/i18n.spec.ts && ! npx vitest run tests/unit/i18n-paths.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/i18n-paths.test.ts` imports from `src/lib/i18n-paths` and exercises `getSwitcherHref` with the four mappings.
    - `tests/e2e/i18n.spec.ts` contains `test.describe('locale content'` and `test.describe('switcher'` groups.
    - `npx vitest run tests/unit/i18n-paths.test.ts` FAILS (non-zero exit) due to the missing `src/lib/i18n-paths.ts` module — confirming RED.
    - Assertions reference the literal `ajs_locale` cookie name and the `FR | EN` switcher text.
  </acceptance_criteria>
  <done>Unit and e2e test files exist, target the real contracts (not stubs), and are confirmed RED — the Nyquist gate is armed for later plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local/CI install | Third-party package code is pulled and executed during install/build |
| test webServer → localhost | Playwright starts a local preview server bound to 127.0.0.1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (astro, @playwright/test, vitest) | mitigate | All packages pre-vetted and Approved in RESEARCH.md Package Legitimacy Audit (astro, @playwright/test, vitest — no [ASSUMED]/[SUS]/[SLOP], no postinstall scripts). Pin exact versions in package.json; commit package-lock.json. No new/unaudited package introduced in this plan → no blocking human-verify checkpoint required. |
| T-01-01 | Information Disclosure | Playwright webServer | accept | Preview server binds to localhost only, serves static non-sensitive placeholder output, runs only during test execution. |
</threat_model>

<verification>
- `npm run build` produces `dist/index.html` (static output, no adapter).
- `npx vitest run tests/unit/i18n-paths.test.ts` fails RED (missing module by design).
- Playwright and Vitest configs exist and execute; no watch-mode flags anywhere.
</verification>

<success_criteria>
- Astro project scaffolded with i18n config (D-01) and no SSR adapter.
- Node pinned to 22 via `.nvmrc`.
- Playwright + Vitest installed and configured; Playwright serves built output via `astro preview`.
- Unit + e2e tests authored and confirmed RED against the real contracts.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bilingual-infrastructure/01-01-SUMMARY.md` when done.
</output>
