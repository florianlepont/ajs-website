---
phase: 01-foundation-bilingual-infrastructure
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-04"]
files_modified:
  - .github/workflows/deploy.yml
autonomous: false
requirements: [I18N-01]
user_setup:
  - service: GitHub Actions Secrets
    why: "CI needs Sanity project/token to build; the deploy target itself (GitHub Pages) uses the built-in GITHUB_TOKEN, not a manual secret"
    env_vars:
      - name: SANITY_PROJECT_ID
        source: "Sanity project (Plan 03)"
      - name: SANITY_DATASET
        source: "Sanity project (Plan 03)"
      - name: SANITY_API_READ_TOKEN
        source: "Sanity Viewer token (Plan 03) — never committed"
    dashboard_config:
      - task: "Add the three Sanity repo secrets above to GitHub → Settings → Secrets and variables → Actions"
        location: "GitHub repository settings (or `gh secret set`)"
      - task: "Create a fine-grained GitHub PAT (this repo only, Actions:write + Contents:read) and configure a Sanity webhook to POST repository_dispatch on publish"
        location: "GitHub → Settings → Developer settings → Fine-grained tokens; Sanity → API → Webhooks"

must_haves:
  truths:
    - "Pushing to `main` triggers a GitHub Actions run that installs deps on Node 22, builds the Astro static output with ASTRO_BASE=/ajs-website/, runs Playwright + Vitest as a BLOCKING gate, and only then deploys `dist/` to GitHub Pages"
    - "The staging site at https://florianlepont.github.io/ajs-website/ serves the built bilingual site (FR at the base path, EN at `/en/`) over automatic HTTPS"
    - "Publishing content in Sanity triggers a rebuild via a `repository_dispatch` event (webhook → GitHub API), so the static site stays in sync without manual redeploys"
    - "No secret (Sanity token, PAT) appears in the repository or in workflow logs"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Build+test+deploy CI pipeline to GitHub Pages, with repository_dispatch rebuild trigger"
      contains: "deploy-pages"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GitHub Pages"
      via: "actions/upload-pages-artifact + actions/deploy-pages"
      pattern: "deploy-pages"
    - from: "Sanity webhook"
      to: "GitHub Actions (repository_dispatch)"
      via: "POST api.github.com/repos/florianlepont/ajs-website/dispatches with fine-grained PAT"
      pattern: "repository_dispatch"
---

<objective>
Wire the only compute tier in this architecture — GitHub Actions — to build, test-gate, and deploy the site to GitHub Pages on every push to `main`, and to rebuild on Sanity content publishes via `repository_dispatch`. Originally scoped to deploy to an OVH staging subdomain over FTPS — rescoped per 01-CONTEXT.md D-12 because OVH's Free hosting tier cannot host a staging subdomain at all. This closes the Walking Skeleton loop: code + CMS + hosting + TLS all connected, with a live public staging URL proving the full stack end-to-end. Uses the GitHub Pages enablement + astro.config.mjs base wiring from Plan 02 and the Sanity project from Plan 03. OVH remains the target for the real production cutover in a later Phase 5 plan, using the SFTP facts Plan 02 recorded.

Purpose: Deliver Phase 1 success criterion #3 — the site is built static and deployed to a public HTTPS URL, connected to the live Sanity CMS — and prove the content-change→rebuild pipeline.
Output: A working CI/CD deploy workflow to GitHub Pages, three configured Sanity GitHub secrets, a Sanity→GitHub rebuild webhook, and a verified live staging site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-PATTERNS.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-02-SUMMARY.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the build+test+deploy GitHub Actions workflow</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Pitfall 4 Node pin lines 302-306; Open Question 3 hard-gate lines 404-407; Known Threat Patterns lines 480-484 — note: the OVH/FTP-specific examples no longer apply, see 01-CONTEXT.md D-12)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-02-SUMMARY.md (confirms GitHub Pages is enabled and the ASTRO_BASE convention from astro.config.mjs)
  </read_first>
  <action>
    Create `.github/workflows/deploy.yml`. Trigger on `push` to `main` AND `repository_dispatch` with `types: [sanity-content-published]`. Grant the job `permissions: { contents: read, pages: write, id-token: write }` (required by `actions/deploy-pages`). One job `build-and-deploy` on `ubuntu-latest`, with `environment: { name: github-pages, url: ${{ steps.deployment.outputs.page_url }} }`:
    1. `actions/checkout@v4`.
    2. `actions/setup-node@v4` with `node-version: '22'` (Astro 7.0.6 requires >=22.12.0 — Pitfall 4; do NOT pin 18/20).
    3. `npm ci`.
    4. `npm run build` (outputs `dist/`), passing `SANITY_PROJECT_ID`, `SANITY_DATASET`, `SANITY_API_READ_TOKEN` from `secrets` as env, AND `ASTRO_BASE: /ajs-website/` as env so the build emits GitHub Pages project-page-correct links.
    5. `npx playwright install --with-deps chromium`.
    6. `npx playwright test` then `npx vitest run` — BLOCKING gate, placed BEFORE the deploy steps in the same job so a test failure fails the run and prevents deploy (Open Question 3 hard-gate).
    7. `actions/upload-pages-artifact@v3` with `path: ./dist`.
    8. `actions/deploy-pages@v4` with `id: deployment` — this publishes to GitHub Pages using the job's built-in `GITHUB_TOKEN` permissions, no manual credentials needed.
    Do NOT use any FTP/OVH deploy action here — that path is deferred to Phase 5's separate production-cutover plan (see 01-CONTEXT.md D-12).
  </action>
  <verify>
    <automated>test -f .github/workflows/deploy.yml && grep -q "node-version: '22'" .github/workflows/deploy.yml && grep -q "deploy-pages" .github/workflows/deploy.yml && grep -q "upload-pages-artifact" .github/workflows/deploy.yml && grep -q "ASTRO_BASE" .github/workflows/deploy.yml && grep -q "repository_dispatch" .github/workflows/deploy.yml && ! grep -qi "FTP-Deploy-Action\|pitscher\|ovh-deploy-hosting" .github/workflows/deploy.yml</automated>
  </verify>
  <acceptance_criteria>
    - Workflow triggers on both `push: branches: [main]` and `repository_dispatch: types: [sanity-content-published]`.
    - `actions/setup-node@v4` pins `node-version: '22'`.
    - Test steps (`playwright test`, `vitest run`) appear BEFORE the `FTP-Deploy-Action` step in the job (grep line-order check).
    - `ASTRO_BASE: /ajs-website/` is set as a build-step env var; `upload-pages-artifact` and `deploy-pages` steps are present; job `permissions` includes `pages: write` and `id-token: write`.
    - No FTP/FTPS/OVH deploy action is referenced.
    - Official `actions/*` are used at pinned major versions (supply-chain hygiene).
  </acceptance_criteria>
  <done>The CI workflow builds on Node 22, hard-gates on Playwright+Vitest, and deploys `dist/` to GitHub Pages, with a Sanity rebuild trigger.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configure Sanity GitHub secrets + rebuild webhook</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Sanity webhook config lines 353-370; Security Domain lines 464-484 PAT scoping)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-03-SUMMARY.md (the actual Sanity credential values)
  </read_first>
  <what-built>
    Claude can set the repo secrets non-interactively via `gh secret set` if `gh` is authenticated (automation-first). The parts Claude cannot do: create the Sanity credential values themselves (done in Plan 03) and create the fine-grained GitHub PAT + configure the Sanity webhook in Sanity's dashboard. Attempt `gh secret set` first; fall back to guiding Florian through the GitHub UI only if `gh` auth is unavailable. GitHub Pages itself needs no secret — `deploy-pages` uses the job's automatic `GITHUB_TOKEN`.
  </what-built>
  <how-to-verify>
    1. Set the three Sanity repo secrets (`SANITY_PROJECT_ID`, `SANITY_DATASET`, `SANITY_API_READ_TOKEN`) using values from the Plan 03 SUMMARY — prefer `gh secret set <NAME>` reading from a prompt (never echo values into shell history or logs).
    2. Create a fine-grained GitHub PAT scoped to THIS repo only, with `Actions: write` + `Contents: read` and nothing else (RESEARCH.md Elevation-of-Privilege mitigation — never a classic all-repos PAT).
    3. In Sanity → API → Webhooks, configure a webhook that fires on document publish and POSTs to `https://api.github.com/repos/florianlepont/ajs-website/dispatches` with header `Authorization: Bearer <the fine-grained PAT>`, `Accept: application/vnd.github+json`, body `{"event_type": "sanity-content-published"}`.
    4. Confirm none of these secret values are committed anywhere in the repo.
  </how-to-verify>
  <acceptance_criteria>
    - `gh secret list` (or the GitHub UI) shows all three Sanity secrets present.
    - The Sanity webhook targets the `/dispatches` endpoint with `event_type: sanity-content-published` and uses a fine-grained, repo-scoped PAT (Actions:write + Contents:read only).
    - `git grep -iE "SANITY_API_READ_TOKEN=|ghp_|github_pat_"` returns no committed secret values.
  </acceptance_criteria>
  <resume-signal>Reply "secrets + webhook configured" once all three secrets and the Sanity webhook (with fine-grained PAT) are in place.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live staging deploy end-to-end</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-VALIDATION.md (Manual-Only Verifications lines 62-68)
  </read_first>
  <what-built>
    Claude pushes to `main` (or re-runs the workflow) to trigger the CI build+test+deploy, and monitors the run via `gh run watch`. Once the run is green, the human confirms the live public URL.
  </what-built>
  <how-to-verify>
    1. After a push to `main`, confirm the GitHub Actions run is green (build + tests + Pages deploy all succeeded) via `gh run watch` / the Actions tab.
    2. Visit `https://florianlepont.github.io/ajs-website/` — confirm valid automatic HTTPS, French chrome + French placeholder homepage from Sanity.
    3. Visit `https://florianlepont.github.io/ajs-website/en/` — confirm English chrome + homepage.
    4. Click "FR | EN" — confirm it lands on the equivalent page in the other locale; confirm the `ajs_locale` cookie is set; reload the base path and confirm the cookie-driven redirect to `/en/`.
    5. Visit a nonexistent path under the base — confirm the bilingual 404 renders, not GitHub's generic 404.
    6. Confirm the deployed files are the built `dist/` (no `astro.config.mjs`/`package.json`/`node_modules` visible at the URL).
  </how-to-verify>
  <acceptance_criteria>
    - The GitHub Actions run for the latest `main` push is green through the deploy step.
    - `https://florianlepont.github.io/ajs-website/` and `/en/` load over valid HTTPS with locale-correct Sanity-sourced content.
    - Switcher navigation + cookie persistence + cookie-driven redirect all work on the live site.
    - Custom bilingual 404 serves for unknown paths; no source/config files are publicly reachable.
  </acceptance_criteria>
  <resume-signal>Reply "staging verified" if all checks pass, or describe which check failed (with the URL/step) so it can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → GitHub Pages | Deploy uses the job's automatic, scoped GITHUB_TOKEN |
| Sanity webhook → GitHub REST API | External service triggers CI using a PAT |
| GitHub Secrets → workflow env | Secrets are injected into the build job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-PAGES | Elevation of Privilege | GitHub Pages deploy token | mitigate | `deploy-pages` uses the ephemeral, job-scoped `GITHUB_TOKEN` (via `id-token: write` OIDC), never a long-lived PAT — narrower blast radius than the originally-planned OVH FTPS credential. |
| T-01-PAT | Elevation of Privilege | GitHub PAT used by Sanity webhook | mitigate | Fine-grained PAT scoped to this one repo, `Actions: write` + `Contents: read` only; stored only in Sanity's webhook config, never in the repo (RESEARCH.md line 482). |
| T-01-SEC | Information Disclosure | secrets in CI logs | mitigate | Use `secrets.*` context (auto-masked by Actions); no `echo`/`env` dumps of secret values; `git grep` gate for committed secrets in Task 2. |
| T-01-SC | Tampering | third-party Actions (setup-node, upload/deploy-pages) | mitigate | Use official `actions/*` at pinned major versions only. |
| T-01-SPOOF | Spoofing | repository_dispatch trigger | accept | The dispatch endpoint requires the fine-grained PAT to invoke; worst case of a spurious trigger is an extra idempotent rebuild-and-deploy of already-published content (no data mutation). Low risk. |
</threat_model>

<verification>
- `gh run watch` shows the latest `main` run green through deploy.
- Manual: `https://florianlepont.github.io/ajs-website/` and `/en/` serve locale-correct Sanity content over valid HTTPS; switcher + cookie + 404 work.
- `git grep` finds no committed secrets.
</verification>

<success_criteria>
- CI builds on Node 22, hard-gates on Playwright+Vitest, and deploys `dist/` to GitHub Pages on push to `main` (D-05, rescoped by D-12).
- Sanity publish triggers a rebuild via `repository_dispatch`.
- Live staging URL serves the bilingual site over HTTPS (Phase 1 success criterion #3).
- No secret is committed or logged.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bilingual-infrastructure/01-05-SUMMARY.md` when done.
</output>
