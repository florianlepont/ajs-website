---
phase: 01-foundation-bilingual-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [I18N-01]
user_setup:
  - service: OVH Web Hosting
    why: "Static hosting target for the staging site (D-05/D-07/D-08); credentials and plan-tier details are only visible in the OVH control panel"
    dashboard_config:
      - task: "Identify hosting plan tier and available deploy protocol (FTP vs FTPS vs SSH), and the site's document root path (D-06)"
        location: "OVH Control Panel → Web Cloud → Hosting"
      - task: "Add DNS record for staging.atelierjacquelinesuzanne.fr and create the OVH multisite entry pointing it at a folder (D-07)"
        location: "OVH Control Panel → Web Cloud → Domains / Hosting → Multisite"
      - task: "Enable free Let's Encrypt HTTPS for the staging subdomain and wait for cert issuance (D-08)"
        location: "OVH Control Panel → Hosting → SSL certificates"

must_haves:
  truths:
    - "Florian has confirmed the exact OVH hosting plan tier and which deploy protocol it supports (FTP-only vs FTP+SSH) and the document-root path the deploy must target"
    - "A DNS record for staging.atelierjacquelinesuzanne.fr resolves, separate from the live production domain's records"
    - "The staging subdomain is served over HTTPS with a valid (non-warning) Let's Encrypt certificate"
    - "The FTPS credentials (host, user, password) needed by CI are captured for secure entry into GitHub Secrets in Plan 05 — never committed to the repo"
  artifacts: []
  key_links:
    - from: "staging.atelierjacquelinesuzanne.fr (DNS)"
      to: "OVH hosting folder (multisite entry)"
      via: "OVH multisite subdomain→folder mapping"
      pattern: "N/A (control-panel configuration, verified via HTTPS reachability)"
---

<objective>
Resolve the single biggest unknown in Phase 1 — the OVH deployment target — before any CI wiring is attempted. This plan is human-driven control-panel work (D-06 explicitly schedules it as the first execution task): confirm the OVH plan tier and protocol, create the `staging.atelierjacquelinesuzanne.fr` subdomain with a DNS record, and provision Let's Encrypt HTTPS. It runs in Wave 1 (parallel with the code scaffold in Plan 01) specifically so the "several hours" Let's Encrypt cert-provisioning delay (RESEARCH.md Pitfall 5) is absorbed early rather than blocking the deploy plan.

Purpose: De-risk the deploy pipeline by turning D-06's UNVERIFIED protocol/plan question into a confirmed fact, and by having a live, HTTPS-secured staging URL ready before Plan 05 wires CI.
Output: A confirmed OVH protocol + doc-root, a resolving staging subdomain, a valid HTTPS cert, and captured (not committed) FTPS credentials.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm OVH plan tier, deploy protocol, and document root (D-06)</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Pitfall 3 lines 296-300; Open Questions 1-2 lines 394-402; Assumptions A1-A2 lines 386-388; Alternatives Considered lines 87-92)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md (D-05 through D-08)
  </read_first>
  <what-built>
    Nothing is automated here — this is genuinely control-panel-only investigation that Claude cannot perform (requires Florian's OVH account login). Claude's role is to have prepared the exact questions below so the answers directly parameterize Plan 05's CI workflow.
  </what-built>
  <how-to-verify>
    Log into the OVH Control Panel (Web Cloud → Hosting) and record, for the reply:
    1. The exact hosting plan name/tier (e.g. Personal / Professional / Performance).
    2. Which deploy protocol that tier supports: FTP-only, or FTP+SSH? (Personal is typically FTP-only; SSH requires Professional/Performance — RESEARCH.md Pitfall 3.) The default CI path uses FTPS regardless; SSH+rsync is only an option if the tier includes it.
    3. Whether the host supports explicit FTPS (encrypted) — needed to use `protocol: ftps` in the deploy action rather than plaintext FTP.
    4. The FTP/FTPS connection details: host, username, and the document-root/server-dir path where the staging subdomain's files must land.
    5. Where the DNS zone for `atelierjacquelinesuzanne.fr` is managed — at OVH itself, or an external registrar/DNS provider (A2 — this changes how the staging subdomain record is created in Task 2).
    Do NOT paste the FTP password into chat or any file — note only that it is captured, ready for GitHub Secrets in Plan 05.
  </how-to-verify>
  <acceptance_criteria>
    - Plan tier name is recorded in the reply and in the plan SUMMARY.
    - Protocol decision recorded: FTPS is available (preferred) OR document why plain FTP is the only option.
    - Document-root/server-dir path for the staging subdomain is recorded (this becomes `server-dir` in Plan 05's deploy.yml).
    - DNS-zone location (OVH vs external) is recorded (drives Task 2 procedure).
  </acceptance_criteria>
  <resume-signal>Reply with plan tier, available protocol (FTPS/SSH), document-root path, and DNS-zone location — or describe any blockers found in the panel.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision staging subdomain + Let's Encrypt HTTPS (D-07, D-08)</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Pitfall 5 lines 308-312 cert delay; Architecture Patterns; Environment Availability lines 419)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md (D-07 staging subdomain, D-08 HTTPS)
  </read_first>
  <what-built>
    Control-panel provisioning only (no repo code). Uses the DNS-zone location answer from Task 1 to choose the correct procedure.
  </what-built>
  <how-to-verify>
    1. Create a DNS record for `staging.atelierjacquelinesuzanne.fr` — at OVH if the zone is OVH-managed (native multisite flow), or at the external DNS provider if not (A2). This must be a NEW record that does not disturb the live production domain's existing records (the real cutover is Phase 5, not now).
    2. In OVH Hosting → Multisite, add an entry mapping `staging.atelierjacquelinesuzanne.fr` to a folder (the document root confirmed in Task 1).
    3. Enable the free Let's Encrypt certificate for the subdomain (OVH Hosting → SSL). Then WAIT — OVH's own docs note issuance "may take several hours" (Pitfall 5). Do not treat a cert warning during this window as a pipeline bug.
    4. Once issued, confirm in a browser that `https://staging.atelierjacquelinesuzanne.fr/` loads over HTTPS with NO certificate warning (an empty/placeholder page or OVH default page is fine at this stage — content arrives via Plan 05's deploy).
  </how-to-verify>
  <acceptance_criteria>
    - `staging.atelierjacquelinesuzanne.fr` resolves in DNS (verifiable via `dig staging.atelierjacquelinesuzanne.fr` or an online DNS lookup) and is distinct from production records.
    - Visiting `https://staging.atelierjacquelinesuzanne.fr/` in a browser shows a valid certificate (no browser security warning).
    - The OVH multisite entry points the subdomain at the confirmed document-root folder.
    - The production site/domain records were not modified.
  </acceptance_criteria>
  <resume-signal>Reply "staging live over HTTPS" once the subdomain resolves and shows a valid cert — or describe cert/DNS issues (note: several-hours cert delay is expected, not a failure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → staging site | Anyone can reach the staging subdomain once DNS resolves |
| developer → OVH control panel | Privileged account operations (DNS, SSL, FTP creds) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-TLS | Information Disclosure | staging subdomain transport | mitigate | Provision Let's Encrypt HTTPS (D-08) and treat "valid cert, no warning" as the done condition — no HTTP-only staging. |
| T-01-CRED | Information Disclosure | OVH FTP/FTPS password | mitigate | Password is never pasted into chat, the repo, or any planning file; it is entered directly into GitHub Secrets in Plan 05. Prefer FTPS (encrypted) over plaintext FTP for the eventual transfer. |
| T-01-DNS | Tampering | production domain records | accept | Only a new `staging.` record is added; production records are explicitly left untouched (real cutover deferred to Phase 5). Low risk given scoped, additive change. |
</threat_model>

<verification>
- `dig staging.atelierjacquelinesuzanne.fr` (or online lookup) resolves the new subdomain.
- Browser visit to `https://staging.atelierjacquelinesuzanne.fr/` shows a valid Let's Encrypt cert, no warning.
- Plan tier, protocol, and document-root path are recorded in the SUMMARY for Plan 05 to consume.
</verification>

<success_criteria>
- OVH plan tier + deploy protocol + document root confirmed (D-06 resolved).
- Staging subdomain resolves and is served over valid HTTPS (D-07, D-08).
- FTPS credentials captured out-of-band for Plan 05, never committed.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bilingual-infrastructure/01-02-SUMMARY.md` when done — record the confirmed protocol, document-root path, and DNS-zone location (these parameterize Plan 05).
</output>
