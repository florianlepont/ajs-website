---
phase: 15-about-page-editorial-redesign
plan: 04
type: execute
wave: 4
depends_on: [15-03]
files_modified: []
autonomous: false
requirements: [ABOUT-03, ABOUT-04]
tags: [about, verification, uat, accessibility, editorial]

must_haves:
  truths:
    - "The full CI-equivalent gate is green on the redesigned About page: astro check (0 errors), Vitest unit + coverage, and Playwright e2e (chromium + webkit-mobile)."
    - "A human has confirmed the live About page — in both French and English — reads as one coherent editorial composition matching the Contact/Éditions identity, not the old layout with a new title bolted on."
    - "All five ROADMAP Phase 15 success criteria are demonstrably satisfied."
  artifacts:
    - ".planning/phases/15-about-page-editorial-redesign/15-04-SUMMARY.md (final verification result against all 5 criteria)"
  key_links:
    - "This plan is the phase acceptance gate — it verifies the integrated result of 15-01 (sketch), 15-02 (composition), and 15-03 (motion) together, across both locales and all motion states."
    - "Criterion 2 (coherent editorial composition) is inherently visual and requires the human-verify checkpoint; the automated gate cannot assert 'reads as coherent'."
---

<objective>
Close Phase 15 with a single integrated verification pass over the redesigned About page: run the full CI-equivalent gate, then a human visual/UAT confirmation across both locales and all motion states, checked against every ROADMAP Phase 15 success criterion.

This is a deliberately separate, non-autonomous plan so the implementation waves (15-02, 15-03) stayed autonomous and re-runnable, and so the human review sees the fully-integrated result (composition + motion + both locales) rather than a mid-build slice.

Purpose: confirm the About page adopts the giant-title editorial identity coherently, preserves all copy, and works in FR/EN before the phase is marked complete.
Output: `15-04-SUMMARY.md` recording the gate result and human sign-off against all 5 criteria.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-about-page-editorial-redesign/15-CONTEXT.md
@.planning/phases/15-about-page-editorial-redesign/15-UI-SPEC.md
@.planning/phases/15-about-page-editorial-redesign/15-01-SUMMARY.md
@.planning/phases/15-about-page-editorial-redesign/15-02-SUMMARY.md
@.planning/phases/15-about-page-editorial-redesign/15-03-SUMMARY.md
@.planning/sketches/014-about-page-composition/README.md
@src/components/AboutPageBody.astro
</context>

## Artifacts this phase produces (this plan)

- New file `.planning/phases/15-about-page-editorial-redesign/15-04-SUMMARY.md` — the phase verification record: gate result + human sign-off mapped to ROADMAP criteria 1-5. No source symbols are created or modified by this plan.

<tasks>

<task type="auto">
  <name>Task 1: Run the full CI-equivalent gate on the redesigned About page</name>
  <files>(verification only — no files modified)</files>
  <read_first>
    - CLAUDE.md (the documented GitHub Actions gate order: typecheck → build → e2e → unit coverage; the two Playwright projects chromium + webkit-mobile)
    - .planning/phases/15-about-page-editorial-redesign/15-02-SUMMARY.md and 15-03-SUMMARY.md (any recorded env/e2e caveats to resolve or escalate)
    - tests/e2e/about.spec.ts (the full About suite that must pass)
  </read_first>
  <action>
    Run the project's blocking gates in order and capture results: `npm run typecheck` (astro check, 0 errors), `npm run test:e2e -- about.spec.ts` (both `chromium` and `webkit-mobile` projects), and `npm run test:unit` (plus `npm run test:coverage` if coverage thresholds are part of the gate). Confirm the accessibility-relevant expectations hold: any interactive element the D-05 reveal introduced meets the 44px `--tap-target-min` and carries focus-visible treatment (cross-check with tests/e2e/accessibility.spec.ts if it covers About).

    If the worktree lacks the Sanity build env and e2e cannot build/serve (a known risk noted in earlier SUMMARYs and in project memory), do NOT weaken any test — record the exact blocked command and hand off to independent orchestrator verification per the project's established pattern (the same way the Contact redesign was verified when the executor's worktree had no `.env`).
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:e2e -- about.spec.ts && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passes with 0 errors.
    - `npm run test:e2e -- about.spec.ts` passes on both `chromium` and `webkit-mobile` (structure, content-preservation, FR≠EN, and the three motion-state assertions).
    - `npm run test:unit` passes (and coverage thresholds hold if run).
    - Any blocked gate (missing build env) is recorded verbatim in the SUMMARY with an explicit escalation to independent verification — never silently skipped or weakened.
  </acceptance_criteria>
  <done>The full CI-equivalent gate is green for the redesigned About page (or any environmental block is explicitly escalated, not hidden).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human confirms the redesigned About page against all 5 ROADMAP criteria</name>
  <action>Present the fully-integrated, redesigned About page to the user for visual/UAT confirmation across both locales and all motion states, walking through the how-to-verify steps below. Block until the user approves or reports issues — do not self-approve the visual criteria.</action>
  <what-built>
    The About page redesign is complete: the title now renders via the shared `PageTitleHeader` (giant display title, pulsing-dot eyebrow, drifting halftone, hairline divider — matching Contact/Éditions); the biography is a standalone lead paragraph; the portrait is a small circular accent; the numbered sections follow the sketch-014 winner's structure; and the exhibition photo is a pinned, scroll-scrubbed reveal on desktop that settles statically on reduced-motion and mobile. All existing bio/practice/medium copy is preserved verbatim in both locales.
  </what-built>
  <how-to-verify>
    1. Build/serve the site locally (`npm run build && npm run preview`, or `npm run dev`) and open `/about/` (French).
    2. Confirm criterion 1 — the title reads with the SAME giant-display treatment as Contact (`/contact/`) and Éditions (`/editions/`): same font, size feel, pulsing pink eyebrow dot, halftone texture, hairline divider.
    3. Confirm criterion 2 — scroll down: the composition (lead bio → circular portrait accent → pinned hero photo shrink/reveal → numbered sections) reads as ONE coherent modern editorial page, not the old layout with a new title bolted on. Watch the hero-photo reveal resolve into the sketch-014 winner's D-05 target.
    4. Confirm criterion 4 — the bio, "Atelier & pratique", and "Médium & technique" text is all present and unchanged.
    5. Confirm criterion 5 — repeat at `/en/about/` (English): same layout, English copy ("About", "Studio & practice", "Medium & technique").
    6. Motion/accessibility spot-checks: enable OS "reduce motion" and reload `/about/` on desktop — the hero shows its settled end-state immediately with no pin/scroll motion. Resize to a mobile width (~375px) — the hero is a plain static band and the portrait sits as a ~72px circular accent. Both should look intentional, not broken.
    7. Confirm the empty-`intro` right-column area (beside the giant title, ≥760px) reads intentionally per the sketch winner's resolution — no orphaned floating divider.
  </how-to-verify>
  <resume-signal>Type "approved" if all five ROADMAP criteria hold in both locales, or describe any issues (composition, copy, motion, mobile, or the title-header right column) to feed a follow-up.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan runs tests and performs human visual review. It modifies no source and no runtime surface — it only reads and verifies the artifacts produced by 15-02/15-03. No trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04-VERIFY | Repudiation (unverified claim of done) | phase acceptance | low | mitigate | The phase is only marked complete after both the automated gate is green AND a human confirms all 5 criteria; any environmental block on the gate is escalated to independent verification rather than silently passed. This closes the "shipped but not actually verified" gap the Contact redesign surfaced. |
| T-15-SC | Supply chain (npm/pip/cargo) | — | n/a | accept | No package installs — verification only. No legitimacy gate applies. |

ASVS L1, block-on-high: no high-severity threats — nothing blocks. Verification-only plan; trivial risk profile, documented.
</threat_model>

<verification>
- Automated: `npm run typecheck`, `npm run test:e2e -- about.spec.ts` (chromium + webkit-mobile), `npm run test:unit` all green (or any env block explicitly escalated).
- Human: all five ROADMAP Phase 15 criteria confirmed on the live page in both FR and EN, across desktop-motion, reduced-motion, and mobile.
</verification>

<success_criteria>
All five ROADMAP Phase 15 success criteria are demonstrably satisfied: (1) title via PageTitleHeader, (2) coherent editorial composition, (3) chosen from multiple sketches [15-01], (4) copy preserved verbatim, (5) renders correctly in FR + EN. ABOUT-03 and ABOUT-04 are complete.
</success_criteria>

<output>
Create `.planning/phases/15-about-page-editorial-redesign/15-04-SUMMARY.md` when done, recording the gate result (or escalated env block), the human sign-off, and an explicit pass/fail mapping to ROADMAP criteria 1-5.
</output>
