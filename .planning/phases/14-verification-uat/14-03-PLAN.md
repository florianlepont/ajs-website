---
phase: 14-verification-uat
plan: 03
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - .planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md
autonomous: true
requirements: []   # Cross-cutting verification phase — owns no primary REQ. Implements locked decision D-01 + the REQUIREMENTS.md re-verify discretion item.

must_haves:
  truths:
    - "An itemized closure-audit doc (`14-CLOSURE-AUDIT.md`) maps EACH of PITFALLS.md's 7 'Looks Done But Isn't' checklist items to specific, freshly-re-run evidence (build output, test-run results, greps of built HTML/sitemap), satisfying ROADMAP success criteria #1, #2, and #4 without duplicating the test suites Phases 11-13 already built (D-01)."
    - "The audit re-runs the direct checks ITSELF — `npm run build`, `npm run test:unit`, `npm run test:e2e`, `npm run test:artifact`, plus targeted greps for the sitemap URLs and the localized nav link on BOTH homepage `<SiteHeader>` call sites in both locales — and records real command output, not citations of prior SUMMARY/VERIFICATION claims (the project's direct-evidence discipline)."
    - "The audit records that `.planning/REQUIREMENTS.md`'s EDN-01..07 + CMS-04 checkboxes (lines ~79-89) and the traceability table (lines ~198-205) are ALREADY `[x]`/Complete — resolved by commit 2440468 — confirmed by grep, with NO edit performed (the bookkeeping lag CONTEXT.md described was already fixed)."
    - "The audit explicitly notes that PITFALLS item 5 (Studio editorial parity: draft/publish + drag-reorder) is closed for real by Plan 14-04's Romane UAT — the drag-reorder that `11-UAT.md` waived for lack of a 2nd document — and marks it pending that blocking checkpoint (ROADMAP SC #3)."
  artifacts:
    - path: ".planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md"
      provides: "7-item PITFALLS 'Looks Done But Isn't' closure map with re-run evidence per item + REQUIREMENTS.md re-verify note"
      min_lines: 40
  key_links:
    - from: ".planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md (item 5, Studio parity)"
      to: ".planning/phases/14-verification-uat/14-ROMANE-UAT.md (Plan 14-04 blocking checkpoint)"
      via: "audit references the Romane create/edit/publish/drag-reorder pass as the closing evidence for the waived-in-11-UAT drag-reorder"
      pattern: "14-04|Romane|drag-reorder"
---

<objective>
Implement locked decision **D-01**: produce Phase 14's core deliverable — a genuine closure audit, NOT a new duplicate test suite. Phases 11-13 already built and independently re-verified `edition.spec.ts`, `site-header.spec.ts`, `seo.spec.ts`, `edition-query.test.ts`, `static-routes.test.ts`, and the `test:artifact` guard. This plan writes an itemized checklist that maps every one of PITFALLS.md's 7 "Looks Done But Isn't" items to specific existing evidence, RE-RUNNING the direct checks itself (build, unit, e2e, artifact guard, sitemap + nav greps) so the closure is proven, not assumed — which is exactly what ROADMAP success criterion #4 demands ("explicitly checked off, not just assumed from a single happy-path pass").

Also handles the REQUIREMENTS.md bookkeeping discretion item: RE-VERIFY (do not re-edit) that EDN-01..07/CMS-04 checkboxes and the traceability table are already `[x]`/Complete — the pattern-mapper confirmed live that commit 2440468 already flipped them, so the audit records "already resolved, no action needed" rather than re-doing a no-op edit.

Purpose: satisfies ROADMAP SC #1 (both locales' pages render + localized nav on every page incl. both homepage `<SiteHeader>` call sites + sitemap URLs), SC #2 (negative commerce check), and SC #4 (every omission-class risk explicitly checked off).
Output: `14-CLOSURE-AUDIT.md` with a 7-item mapping and real re-run command output.

MVP-mode note (judgment per orchestrator): this is the closure/audit pass itself — there is no user-facing vertical slice to build; the deliverable is the evidence-backed audit document.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-verification-uat/14-CONTEXT.md
@.planning/phases/14-verification-uat/14-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/11-schema-content-model/11-VERIFICATION.md
@.planning/phases/11-schema-content-model/11-UAT.md
@.planning/phases/12-data-fetch-layer-routes/12-VERIFICATION.md
@.planning/phases/13-nav-integration/13-VERIFICATION.md
@.planning/REQUIREMENTS.md
</context>

## Artifacts this phase produces

New symbols introduced by this plan (downstream verification reference):

- **`.planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md`** — the closure audit. Contains: (1) a table with one row per PITFALLS.md "Looks Done But Isn't" item (7 rows) → status → the specific re-run command + result + file/line/test that closes it; (2) a mapping of the 7 items and the re-run evidence to ROADMAP SC #1/#2/#4; (3) a REQUIREMENTS.md re-verification note (already `[x]`/Complete via 2440468, no edit); (4) an explicit "PITFALLS item 5 (Studio parity / drag-reorder) closed for real by Plan 14-04's Romane UAT — pending that blocking checkpoint (SC #3)" note.
- **Explicitly NOT produced**: no new `editions.spec.ts`/`edition-query.test.ts` or any duplicate consolidated test suite (D-01 forbids this — new automated coverage lives only in Plans 14-01/14-02); no edit to `REQUIREMENTS.md` (re-verify only); no source-code change.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-run the direct checks and write the 7-item PITFALLS closure audit (D-01)</name>
  <files>.planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md</files>
  <read_first>
    - .planning/research/PITFALLS.md (the "Looks Done But Isn't Checklist" — the 7 items to map, verbatim, as the audit's rows)
    - .planning/phases/12-data-fetch-layer-routes/12-VERIFICATION.md (evidence for items 1/3/6: route+locale render, sitemap URLs, no-commerce build guard — cite but ALSO re-run)
    - .planning/phases/13-nav-integration/13-VERIFICATION.md (evidence for items 2/4: nav link wired end-to-end + mobile 4-link single-row fit both variants both locales)
    - .planning/phases/11-schema-content-model/11-VERIFICATION.md (evidence for items 5/7: publicationStatus + orderRank + structure.ts desk item; printRun/pageCount/dimensions typed + commerce-path comment)
    - .planning/phases/11-schema-content-model/11-UAT.md (the WAIVED drag-reorder — item 5's still-open sub-part that Plan 14-04 closes for real)
    - .planning/phases/14-verification-uat/14-PATTERNS.md (the "Direct-evidence verification discipline" shared pattern — re-run, don't trust)
  </read_first>
  <action>
    First RE-RUN the direct checks (do not trust prior reports) and capture their real output: `npm run build`; `npm run test:unit`; `npm run test:e2e`; `npm run test:artifact`; then targeted greps against the fresh `dist/` — the sitemap contains the four Éditions URLs (`/editions/`, `/en/editions/`, and per-slug FR/EN), and the localized "Éditions" nav link renders as the first `.nav-link` in the built HTML of representative pages in BOTH locales AND on the homepage specifically (the homepage renders `<SiteHeader>` from two independent call sites — confirm the built `dist/index.html` and `dist/en/index.html` carry the link, per SC #1's "both homepage call sites" requirement).
    Then write `.planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md` as an itemized checklist with one row per PITFALLS "Looks Done But Isn't" item (all 7): overview+detail render in both locale dirs; nav item wired end-to-end (schema→type→resolveSiteCopy→BaseLayout→SiteHeader, Studio-editable); sitemap/SEO discoverability; mobile nav with 4 links (both variants, both locales); Studio editorial parity (draft/publish + drag-reorder); no stray commerce UI (built HTML AND schema — cite Plan 14-02's extension); schema extensibility groundwork (typed fields + commerce-path comment). For EACH item record: the re-run command, its real result, and the specific file/line/test that closes it. Map the items to ROADMAP SC #1/#2/#4. For item 5, mark the draft/publish + code-level ordering parts closed by evidence, and mark the actual human drag-reorder as closed-for-real by Plan 14-04's Romane UAT (SC #3) — pending that blocking checkpoint. Note that Plan 14-01's null-safety guards and Plan 14-02's schema commerce scan are part of the closing evidence for the resilience and no-commerce items respectively. Do NOT write a new duplicate test suite (D-01). Do NOT re-state any commerce token literal in the audit prose — describe the check by referencing `npm run test:artifact` and Plan 14-02, not by listing the words.
  </action>
  <verify>
    <automated>npm run build && npm run test:unit && npm run test:e2e && npm run test:artifact && test -f .planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md && grep -c "Looks Done But Isn" .planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `14-CLOSURE-AUDIT.md` exists and contains one explicit row/entry for each of PITFALLS.md's 7 "Looks Done But Isn't" items, each with a re-run command, its real result, and the closing file/line/test.
    - The full re-run suite (`build`, `test:unit`, `test:e2e`, `test:artifact`) passes, and the doc records the sitemap-URL grep and the both-locales/both-homepage-call-sites nav-link grep results.
    - The doc maps the 7 items to ROADMAP SC #1/#2/#4 and marks item 5's human drag-reorder as pending Plan 14-04's blocking Romane UAT (SC #3).
    - No new consolidated/duplicate test suite file was created (D-01); no commerce token literal is re-stated in the audit prose.
  </acceptance_criteria>
  <done>An evidence-backed closure audit maps all 7 PITFALLS omission-class items to freshly-re-run direct checks, satisfying ROADMAP SC #1/#2/#4 without duplicate test code, and flags the one remaining item (SC #3, human drag-reorder) as owned by Plan 14-04.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-verify REQUIREMENTS.md is already Complete and record it (discretion item)</name>
  <files>.planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines ~79-89 for the EDN-01..07/CMS-04 checkboxes; lines ~198-205 for the traceability table)
    - .planning/phases/14-verification-uat/14-PATTERNS.md (the "REQUIREMENTS.md bookkeeping is already complete, not pending" finding — cites the exact current `[x]`/Complete lines and commit 2440468)
    - .planning/phases/14-verification-uat/14-CONTEXT.md (the "Claude's Discretion → REQUIREMENTS.md bookkeeping lag" item)
  </read_first>
  <action>
    Grep/read `.planning/REQUIREMENTS.md` to confirm EDN-01 through EDN-07 and CMS-04 are already `- [x]` (checked) at lines ~79-89, and that the traceability table (lines ~198-205) shows them as "Complete" mapped to Phases 11/12/13. Append a short section to `14-CLOSURE-AUDIT.md` recording this: the bookkeeping lag that `12-VERIFICATION.md` and `13-VERIFICATION.md` flagged was ALREADY resolved by commit 2440468 ("docs(phase-13): complete phase execution") — so NO edit is performed here. Explicitly state "already resolved, no action needed" with the confirming grep output, so the audit does not claim to fix something already fixed. Do NOT modify `REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>grep -cE "^- \[x\] \*\*(EDN-0[1-7]|CMS-04)\*\*" .planning/REQUIREMENTS.md && grep -c "already resolved" .planning/phases/14-verification-uat/14-CLOSURE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - The grep confirms all 8 IDs (EDN-01..07, CMS-04) are already `- [x]` in `REQUIREMENTS.md` (count = 8).
    - `14-CLOSURE-AUDIT.md` contains a section stating the REQUIREMENTS.md bookkeeping is already resolved by commit 2440468, with the confirming evidence and "no action needed".
    - `.planning/REQUIREMENTS.md` is NOT modified by this plan (git shows no change to it).
  </acceptance_criteria>
  <done>The audit records — with confirming grep evidence — that the REQUIREMENTS.md checkbox/traceability bookkeeping was already fixed upstream, so no redundant edit is made.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (No new boundary) | This plan re-runs existing build/test commands and writes a documentation artifact. It touches no source, adds no request-time surface (static site, zero compute), no user input, no auth, no data flow. A full STRIDE sweep would fabricate inapplicable threats. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-R | Repudiation / verification integrity | The closure audit's evidence claims | low | mitigate | The audit is required to RE-RUN the direct checks and record real output rather than citing prior SUMMARY/VERIFICATION claims (direct-evidence discipline) — this prevents a "looks verified but wasn't re-checked" gap, the exact omission-class risk this phase exists to close. |
| T-14-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs; no dependency change; documentation + re-run of existing scripts only. No `[ASSUMED]`/`[SUS]` packages. |

No high-severity threat exists for this plan (ASVS L1, block-on-high).
</threat_model>

<verification>
- `npm run build && npm run test:unit && npm run test:e2e && npm run test:artifact` all pass (the audit's re-run evidence).
- `14-CLOSURE-AUDIT.md` exists with all 7 PITFALLS items mapped to re-run evidence and to ROADMAP SC #1/#2/#4.
- The 8 requirement IDs are confirmed already `[x]` in REQUIREMENTS.md and the audit records this without editing the file.
- `git status` shows `.planning/REQUIREMENTS.md` unchanged.
</verification>

<success_criteria>
Every omission-class "Looks Done But Isn't" risk is explicitly checked off with freshly-re-run direct evidence (SC #1/#2/#4), the REQUIREMENTS.md bookkeeping is confirmed already-resolved (no redundant edit), and the one item requiring a human (drag-reorder, SC #3) is clearly delegated to Plan 14-04 — all without writing duplicate test code (D-01).
</success_criteria>

<output>
Create `.planning/phases/14-verification-uat/14-03-SUMMARY.md` when done.
</output>
