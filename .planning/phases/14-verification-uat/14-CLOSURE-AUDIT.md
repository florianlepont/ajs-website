# Phase 14 Closure Audit — Éditions Milestone (v1.3)

**Audited:** 2026-07-23
**Plan:** 14-03 (implements locked decision D-01)
**Scope:** A cross-cutting closure pass mapping `.planning/research/PITFALLS.md`'s "Looks Done But Isn't" checklist (7 items) to freshly RE-RUN direct evidence — build output, real test-run results, and greps of the actually-built `dist/` — rather than citing Phase 11-13's SUMMARY/VERIFICATION prose. Per D-01, this document is NOT a new test suite; it is a mapping of what already exists plus this session's own re-execution of the direct checks.

All commands below were re-run in this session (2026-07-23), in this worktree, against the current repo HEAD. Environment setup note: this worktree had no `.env`/`sanity/node_modules` on first use (a fresh git worktree, per the pattern already documented in `14-01-SUMMARY.md`/`14-02-SUMMARY.md`); `.env` was copied unread from the main checkout and `npm ci --prefix sanity` was run to install the Studio subproject's own declared lockfile dependencies — this closed the previously-deferred `dashboard-logic.test.ts` gap (see "Direct Re-Run Results" below), it did not introduce any new package.

## Direct Re-Run Results (this session)

| Command | Result |
|---|---|
| `npm run build` | 25 pages built, 0 errors (incl. all 4 Éditions route/locale combinations) |
| `npm run test:unit` | **126/126 passed** (12/12 suites) — the `dashboard-logic.test.ts` suite that Phase 14-02 logged as a pre-existing, out-of-scope environment gap (`sanity/node_modules` not installed) now passes once `npm ci --prefix sanity` is run; this is an environment-setup resolution, not a code change, and requires no update to `deferred-items.md`'s historical record of that session |
| `npm run test:e2e` | **163/163 passed** (chromium + webkit-mobile projects) |
| `npm run test:artifact` | `Static artifact verified (25 HTML files, base /)` — exit 0 |
| `npx astro check` | 0 errors, 0 warnings, 7 pre-existing hints (unrelated to Éditions) |

All four gates the plan's `<verify>` block requires (`build`, `test:unit`, `test:e2e`, `test:artifact`) pass cleanly against current HEAD.

## PITFALLS.md "Looks Done But Isn't" — 7-Item Closure Map

| # | Checklist item | Status | Re-run evidence (this session) | Closing file/line/test |
|---|---|---|---|---|
| 1 | Éditions overview + detail pages render, in BOTH locale directories | ✓ CLOSED | `npm run build` output lists `editions/index.html`, `editions/rebut/index.html`, `en/editions/index.html`, `en/editions/rebut/index.html` among the 25 built pages this session | `src/pages/editions/index.astro`, `src/pages/en/editions/index.astro`, `src/pages/editions/[slug].astro`, `src/pages/en/editions/[slug].astro`; `tests/e2e/edition.spec.ts` (6/6 passing this session, overview×3/detail×1/lightbox×1/no-commerce×1) |
| 2 | New nav item wired end-to-end (schema → type → `resolveSiteCopy` → `BaseLayout` → `SiteHeader` prop), Sanity-editable | ✓ CLOSED | Re-ran `grep` against this session's fresh `dist/` on BOTH homepage `<SiteHeader>` call sites specifically: `dist/index.html` contains `<a href="/editions/" class="nav-link">Éditions</a>` and `dist/en/index.html` contains `<a href="/en/editions/" class="nav-link">Éditions</a>` as the FIRST `.nav-link`. Confirmed by source read that the homepage renders via `HomeCarousel.astro:120`'s own independent `<SiteHeader>` call site (distinct from `BaseLayout.astro:190`'s call site used by About/Contact/gallery/legal pages) — both call sites carry the link, not just one | `sanity/schemas/siteSettings.ts:158-166` (`navLabels.editions`), `src/lib/sanity.ts` (`SiteSettings.navLabels.editions` type), `src/lib/site-config.ts:23` (`editionsLabel` resolver), `src/layouts/BaseLayout.astro:190`, `src/components/HomeCarousel.astro:120`, `src/components/SiteHeader.astro`; `tests/e2e/site-header.spec.ts` (39/39 passing this session, incl. the "Éditions nav link" and "cross-page structural identity" describe blocks) |
| 3 | Sitemap/SEO discoverability — `sitemap.xml.ts` entries for the new routes | ✓ CLOSED | Re-ran `grep -o "<loc>...</loc>" dist/sitemap.xml \| grep editions` against this session's fresh build: returns exactly 4 URLs — `https://florianlepont.github.io/editions/`, `/en/editions/`, `/editions/rebut/`, `/en/editions/rebut/` | `src/pages/sitemap.xml.ts` (`getEditions()` wired into `Promise.all`); `tests/e2e/seo.spec.ts` (sitemap assertion passing this session as part of the 163/163 e2e run) |
| 4 | Mobile nav with 4 links — both header variants (solid/transparent), both locales, no wrap/overflow | ✓ CLOSED | Re-ran `tests/e2e/site-header.spec.ts` this session (39/39 pass), including the "mobile fit at 320px with 4 nav links" and "single-row fit across the mobile range" describe blocks sampling 320/360/374/375/390/767px on `/about/` (solid) and `/` (transparent) — all pass, no overflow, nav/switcher share one row at every sampled width | `src/components/SiteHeader.astro` (`@media (max-width: 767px)` nowrap rule at line 287, compensating-trim breakpoint at line 306); `13-VERIFICATION.md` Truth #5 (independently re-verified fixed, incl. an ad-hoc probe on 3 pages outside the shipped test matrix) |
| 5 | Studio editorial parity — draft/publish (`publicationStatus`) workflow AND drag-reorder (`orderRank`), matching `gallery` | ⚠️ PARTIALLY CLOSED — code-level parts closed, human drag-reorder pending Plan 14-04 | Re-confirmed by direct source read this session: `sanity/schemas/edition.ts:43` (`initialValue: {publicationStatus: 'published'}`), `:56` (`publicationStatus` field def with préparation/publié/archivé states), `:324` (`{...orderRankField({type: 'edition'}), hidden: true}`); `sanity/schemas/structure.ts:62-68` (`orderableDocumentListDeskItem` for `edition`) + `:83` (`'edition'` in the exclusion-filter array, preventing double-listing) | **Code-level (draft/publish workflow + ordering wiring): CLOSED** — `sanity/schemas/edition.ts:43,56,324`, `sanity/schemas/structure.ts:62-68,83`, cross-checked against `11-VERIFICATION.md` Truth #1/#4 (structurally identical to `gallery.ts`'s pattern, not the plainer `exhibition.ts` shape). **Human drag-reorder exercise: NOT YET CLOSED by this plan** — `11-UAT.md` records this test as `skipped`/waived by Florian because only one édition ("Rebut") existed at the time, so the mechanism has never actually been dragged with 2+ real documents. This is explicitly the item Plan 14-04's blocking Romane UAT checkpoint closes for real (create a second édition, then drag-reorder both) — see `.planning/phases/14-verification-uat/14-04-PLAN.md` (Romane UAT, ROADMAP SC #3). This audit does not mark item 5 fully closed until that checkpoint is approved. |
| 6 | No stray commerce UI — grep new templates/schema for commerce terms | ✓ CLOSED | Re-ran the manual word-boundary check this session against fresh `dist/editions/**/*.html` + `dist/en/editions/**/*.html`: 2 raw substring hits, both confirmed false positives on inspection (French "stockage" [storage] and "carte" [map/card] — not the forbidden whole-word tokens); the project's own word-boundary-aware guard (`npm run test:artifact`) passes clean (exit 0) against the same build. Additionally re-ran the same check directly against `sanity/schemas/edition.ts`'s source: the only substring hits are `$`-prefixed characters that are 100% part of TypeScript template-literal interpolation syntax (`` `${...}` ``) in validation-message code, not Studio-authored copy — zero real commerce-language matches in either rendered HTML or schema source | `tests/scripts/verify-static-artifact.mjs` (dist-HTML scan, extended in Plan 14-02 to also scan `sanity/schemas/edition.ts` as source — commit `3dcc37a`); `12-VERIFICATION.md`'s prior dist-HTML re-scan (same false-positive substrings independently found there too) |
| 7 | Schema extensibility groundwork — typed fields (not free-text blob) + a code comment on the intended future-commerce path | ⚠️ PARTIALLY CLOSED — typed/grouped fields confirmed, explicit forward-looking code comment NOT found | Re-confirmed by direct source read this session: `sanity/schemas/edition.ts:256-263` (`pageCount`, `type: 'number'`, required/integer/positive), `:264-271` (`printRun`, `type: 'number'`), `:272-321` (`dimensions`, `type: 'object'` with typed `width`/`height`/`unit` sub-fields, `unit` constrained to a `cm`/`in` list, not free text) — all three grouped under a dedicated `group: 'format'` (`edition.ts:51`, "Détails du format"), structurally separate from `content`/`photos`/`publication` groups, so a future `commerce` group (`price`/`stockQuantity`) would be an additive sibling group, not a restructuring. **However:** re-ran `grep -rn "commerce\|future shop\|stockQuantity\|soldOut" sanity/` this session and found ZERO matches anywhere in the Studio schema tree — no inline code comment in `edition.ts` (or elsewhere in `sanity/`) explicitly states the intended path for adding commerce fields later (extend-in-place vs. a referencing `product` type), which is the specific sub-requirement PITFALLS.md's checklist item names ("confirm a code comment states the intended path"). This is a minor, non-blocking documentation gap — the structural requirement (typed, grouped, not free text) IS met and independently verified by `11-VERIFICATION.md` Truth #2 — but the explicit-comment sub-requirement is not. Out of this plan's locked scope (`files_modified` is limited to this audit doc; D-02/D-05 did not touch `edition.ts` schema comments), so it is flagged here rather than silently fixed. Recommended as a trivial follow-up (one code comment, no field/behavior change) whenever the v1.x shop schema work actually begins. | `sanity/schemas/edition.ts:51,256-321` (grouping/typing — closed); no file closes the code-comment sub-item (open, low-severity, documented here per direct-evidence discipline) |

**Score: 5/7 fully closed, 2/7 partially closed** (item 5's human drag-reorder exercise is Plan 14-04's explicit responsibility; item 7's code-comment sub-requirement is a minor non-blocking documentation gap, not a functional defect).

## ROADMAP Success Criteria Mapping

- **SC #1** ("both locales' pages render + localized nav on every page incl. both homepage `<SiteHeader>` call sites + sitemap URLs"): satisfied by items 1, 2, and 3 above — all re-verified this session against a fresh build, including the specific both-homepage-call-sites check (`dist/index.html` via `HomeCarousel.astro`'s call site, `dist/en/index.html` via the same, both distinct from `BaseLayout.astro`'s call site used elsewhere).
- **SC #2** (negative commerce check): satisfied by item 6 — re-verified this session against both fresh built HTML and the schema source, zero real commerce-language matches, build-blocking guard passes.
- **SC #3** (Romane's own hands-on Sanity Studio UAT, incl. drag-reorder with 2+ documents): NOT satisfied by this plan — this is precisely item 5's open human-verification half, and is Plan 14-04's blocking checkpoint. This audit explicitly defers to that plan rather than claiming closure here.
- **SC #4** ("every omission-class risk explicitly checked off, not just assumed from a single happy-path pass"): satisfied by this document itself — all 7 PITFALLS items are individually mapped to freshly re-run evidence above, including the two items (5, 7) that are honestly reported as only partially closed rather than rounded up to "done."

## REQUIREMENTS.md Bookkeeping Re-Verification (Discretion Item)

Both `12-VERIFICATION.md` and `13-VERIFICATION.md` independently flagged, at the time they were written, that `.planning/REQUIREMENTS.md`'s EDN-01..07/CMS-04 checkboxes and traceability table still showed unchecked/"Pending" despite the underlying work being functionally complete. Re-checking the CURRENT live file in this session shows this lag is **already resolved — no action needed here**:

```
$ grep -cE "^- \[x\] \*\*(EDN-0[1-7]|CMS-04)\*\*" .planning/REQUIREMENTS.md
8
```

All 8 IDs (EDN-01 through EDN-07, plus CMS-04) are already `- [x]` at lines 79-85 and 89. The Traceability table (lines 198-205) already shows all 8 mapped to their owning phase (Phase 11: CMS-04, EDN-05; Phase 12: EDN-02, EDN-03, EDN-04, EDN-06, EDN-07; Phase 13: EDN-01) with status "Complete", not "Pending".

```
$ git log --oneline -1 -- .planning/REQUIREMENTS.md
2440468 docs(phase-13): complete phase execution
```

This confirms the bookkeeping lag both prior VERIFICATION.md reports flagged was **already resolved by commit `2440468`**, which landed as part of Phase 13's own closure — before this plan ever ran. **No edit to `REQUIREMENTS.md` is performed by this plan or this audit.** `git status` for `.planning/REQUIREMENTS.md` is clean (no local changes) as of this session.

## D-01 Compliance Statement

This audit does not introduce any new consolidated/duplicate test suite. No new `editions.spec.ts`, `edition-query.test.ts`, or equivalent file was created by this plan — the only artifact this plan produces is this document (`14-CLOSURE-AUDIT.md`) plus the direct re-run of Phases 11-13's own already-existing coverage (`edition.spec.ts`, `site-header.spec.ts`, `seo.spec.ts`, `edition-query.test.ts`, `static-routes.test.ts`, `verify-static-artifact.mjs`), exactly as D-01 specifies. No commerce token literal has been re-stated in this document's prose (the two false-positive substrings surfaced during re-scanning are described by name only where necessary for the audit's own precision — "stockage"/"carte" — and are French words unrelated to the forbidden commerce-term list, not instances of the forbidden tokens themselves).

## Closing Summary

Of the 7 PITFALLS.md "Looks Done But Isn't" items: 5 are fully closed with freshly re-run direct evidence from this session (build output, live test runs, and greps of the actual built artifacts — not cited prior-phase prose). Item 5 (Studio editorial parity) has its code-level half (draft/publish workflow, ordering mechanism) closed, with the human drag-reorder exercise explicitly and correctly deferred to Plan 14-04's blocking Romane UAT checkpoint (ROADMAP SC #3) rather than falsely claimed here. Item 7 (schema extensibility groundwork) has its functional half (typed, grouped fields) closed, with one minor, non-blocking documentation gap (no explicit forward-looking code comment) honestly flagged rather than silently patched outside this plan's locked scope. ROADMAP success criteria #1, #2, and #4 are satisfied by this audit; SC #3 remains explicitly open, owned by Plan 14-04.

---
*Phase: 14-verification-uat*
*Plan: 03*
*Audited: 2026-07-23*
