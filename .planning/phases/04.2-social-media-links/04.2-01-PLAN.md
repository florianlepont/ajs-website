---
phase: 04.2-social-media-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - src/pages/about.astro
  - src/pages/en/about.astro
  - src/pages/contact.astro
  - src/pages/en/contact.astro
  - tests/e2e/social-links.spec.ts
autonomous: true
requirements: [SOCIAL-01]
# SOCIAL-01 is a synthetic ID for this inserted phase (ROADMAP lists "Requirements: TBD",
# phase_req_ids is null). It maps 1:1 to the ROADMAP 04.2 goal: "Visitors can find and
# follow Romane's Instagram (@ajs_romanelepont) from the site footer (site-wide) and from
# the About/Contact page."

must_haves:
  truths:
    - "On every page, the footer shows an 'Instagram' link pointing to https://www.instagram.com/ajs_romanelepont/"
    - "The About page (FR at /about/, EN at /en/about/) shows a sentence linking @ajs_romanelepont to Instagram"
    - "The Contact page (FR at /contact/, EN at /en/contact/) shows a sentence linking @ajs_romanelepont to Instagram, placed above the contact form"
    - "Every Instagram link opens in a new tab with target=\"_blank\" and rel=\"noopener noreferrer\""
    - "Every Instagram link carries a visually-hidden new-tab hint so its accessible name ends with '(opens in new tab)' (EN) / '(nouvelle fenetre)' (FR)"
    - "All introduced copy is localized FR/EN; the handle @ajs_romanelepont and the word 'Instagram' are identical across locales"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Site-wide footer Instagram link in a second footer-social-nav"
      contains: "footer-social-nav"
    - path: "src/pages/about.astro"
      provides: "FR About-page Instagram sentence"
      contains: "ajs_romanelepont"
    - path: "src/pages/en/about.astro"
      provides: "EN About-page Instagram sentence"
      contains: "ajs_romanelepont"
    - path: "src/pages/contact.astro"
      provides: "FR Contact-page Instagram sentence + contact-page__social rule"
      contains: "contact-page__social"
    - path: "src/pages/en/contact.astro"
      provides: "EN Contact-page Instagram sentence + contact-page__social rule"
      contains: "contact-page__social"
    - path: "tests/e2e/social-links.spec.ts"
      provides: "E2E coverage for all three surfaces in both locales"
      contains: "instagram.com/ajs_romanelepont"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "https://www.instagram.com/ajs_romanelepont/"
      via: "footer anchor href"
      pattern: "instagram\\.com/ajs_romanelepont"
    - from: "all 5 Instagram anchors"
      to: "safe outbound new-tab behavior"
      via: "target + rel attributes"
      pattern: "rel=\"noopener noreferrer\""
---

<objective>
Add a single outbound Instagram link (@ajs_romanelepont) to three surfaces so visitors can find
and follow Romane on Instagram: the site-wide footer (one link, every page), the About page (FR/EN),
and the Contact page (FR/EN). Markup-and-copy only — zero new components, zero new dependencies,
zero client JS.

Purpose: Satisfies the Phase 04.2 ROADMAP goal. The footer instance covers the "site-wide" half;
the About/Contact instances cover the "on the About/Contact page" half.
Output: 5 modified files + 1 new e2e spec proving all three surfaces in both locales.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-social-media-links/04.2-UI-SPEC.md
@.planning/phases/04.2-social-media-links/04.2-PATTERNS.md

<interfaces>
<!-- Everything the executor needs already exists in the codebase; this phase creates no new interface. -->
<!-- Reused as-is (DO NOT modify these): -->
<!--   .nav-link           — src/layouts/BaseLayout.astro:362-372 (Label role, 14px/400/1.5) -->
<!--   .sr-only            — src/layouts/BaseLayout.astro:245-255 (visually-hidden utility) -->
<!--   global `a` rules    — src/layouts/BaseLayout.astro:221-241 (pink color + hover/focus underline + focus outline) -->
<!--   .about-page p       — src/pages/about.astro:55-60 / en/about.astro:52-57 (Body role) -->
<!--   .contact-page__lead — src/pages/contact.astro:37-42 / en/contact.astro:33-38 (Body role; template for the new .contact-page__social rule) -->
<!-- Existing footer frontmatter-const convention (locale ternary, hardcoded, not CMS): -->
<!--   src/layouts/BaseLayout.astro:57 — const legalNoticeLabel = locale === 'en' ? 'Legal notice' : 'Mentions légales'; -->
<!-- External-URL note: the Instagram href is an absolute external URL — it does NOT go through -->
<!-- getRelativeLocaleUrl (that helper is for internal/site-relative routes only). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the site-wide footer Instagram link</name>
  <files>src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro — frontmatter consts (lines 52-60), the `<footer class="chrome-band">` block (lines 122-128), the `.footer-legal-nav` CSS rule (lines 386-390), and the existing `.nav-link` / `.sr-only` / global `a` rules (do NOT modify any of these).
    - .planning/phases/04.2-social-media-links/04.2-PATTERNS.md — "BaseLayout.astro — footer" section (exact markup, frontmatter, and CSS to copy).
    - .planning/phases/04.2-social-media-links/04.2-UI-SPEC.md — "Footer (site-wide)" Layout Notes and the Copywriting Contract "Footer link" row.
  </read_first>
  <action>
    Three edits, all inside src/layouts/BaseLayout.astro, matching the existing hardcoded locale-conditional convention.

    1. Frontmatter — add one const alongside the existing legal-label consts (near line 57-60):
       const instagramNewTabHint = locale === 'en' ? ' (opens in new tab)' : ' (nouvelle fenêtre)';
       The leading space is intentional so the accessible name reads "Instagram (opens in new tab)".
       Do NOT route the Instagram URL through getRelativeLocaleUrl — it is an absolute external URL.

    2. Markup — inside <footer class="chrome-band">, immediately AFTER the closing </nav> of the existing .footer-legal-nav (do not merge into it, do not reorder the legal links), append a second, separate nav. Anchor attributes exactly: href="https://www.instagram.com/ajs_romanelepont/", target="_blank", rel="noopener noreferrer", class="nav-link". Visible text "Instagram" (identical both locales, a brand name, not translated), immediately followed by <span class="sr-only">{instagramNewTabHint}</span> as the last child inside the anchor. Wrap the anchor in <nav class="footer-social-nav" aria-label="Social"> — aria-label hardcoded/unlocalized, matching the sibling aria-label="Legal".

    3. CSS — add a .footer-social-nav rule whose declarations are identical to the existing .footer-legal-nav rule so spacing matches exactly: display: flex; gap: var(--space-xl); margin-top: var(--space-md);. Reuse .nav-link as-is for the anchor; add no other CSS (color/hover/focus come from the global `a` rules).
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website/.claude/worktrees/gsd-resume-work-b448d2 && grep -q 'footer-social-nav' src/layouts/BaseLayout.astro && grep -q 'instagram.com/ajs_romanelepont/' src/layouts/BaseLayout.astro && grep -q 'rel="noopener noreferrer"' src/layouts/BaseLayout.astro && grep -q 'instagramNewTabHint' src/layouts/BaseLayout.astro && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/BaseLayout.astro contains a second <nav class="footer-social-nav" aria-label="Social"> placed AFTER the existing .footer-legal-nav, not merged into it; the legal links keep their original DOM order.
    - The footer anchor has href="https://www.instagram.com/ajs_romanelepont/", target="_blank", rel="noopener noreferrer", class="nav-link", visible text "Instagram", and a trailing <span class="sr-only">{instagramNewTabHint}</span>.
    - instagramNewTabHint frontmatter const exists with the FR/EN ternary and leading space.
    - A .footer-social-nav CSS rule exists with display: flex; gap: var(--space-xl); margin-top: var(--space-md); no changes to .nav-link, .sr-only, .footer-legal-nav, or the global `a` rules.
    - `npm run build` completes successfully.
  </acceptance_criteria>
  <done>The footer of every rendered page shows a pink "Instagram" link opening instagram.com/ajs_romanelepont in a new tab, with a screen-reader new-tab hint, and the site builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add About + Contact page Instagram mentions and prove all surfaces with an e2e spec</name>
  <files>src/pages/about.astro, src/pages/en/about.astro, src/pages/contact.astro, src/pages/en/contact.astro, tests/e2e/social-links.spec.ts</files>
  <read_first>
    - src/pages/about.astro and src/pages/en/about.astro — the tail of .about-page (the placeholder paragraphs) and the .about-page p CSS rule.
    - src/pages/contact.astro and src/pages/en/contact.astro — the .contact-page__lead paragraph, the <ContactForm locale={locale} /> mount, and the .contact-page__lead CSS rule (template for the new rule).
    - .planning/phases/04.2-social-media-links/04.2-PATTERNS.md — the four page-specific sections (exact insertion markup per file) and "Shared Patterns" (.sr-only new-tab hint, external-link mechanics).
    - .planning/phases/04.2-social-media-links/04.2-UI-SPEC.md — Copywriting Contract rows for About and Contact, and the "About page" / "Contact page" Layout Notes.
    - tests/e2e/about.spec.ts and tests/e2e/contact.spec.ts — existing Playwright conventions (page.goto('/about/'), main locator, toContainText). Playwright config: baseURL http://localhost:4321, webServer runs `npm run preview` (serves the built dist — the site must be built before e2e runs).
  </read_first>
  <action>
    Shared link mechanics for ALL four page anchors: href="https://www.instagram.com/ajs_romanelepont/", target="_blank", rel="noopener noreferrer". The handle @ajs_romanelepont is the link text, immediately followed by an inline <span class="sr-only"> new-tab hint (literal per-locale copy, not a frontmatter const — matches how each page's prose is hardcoded in the template). Do NOT add class="placeholder" to any new paragraph — this is real shipped copy.

    1. src/pages/about.astro — after the LAST existing .placeholder paragraph inside .about-page, add a new plain <p> (no class, reuses .about-page p): text "Suivez le travail de Romane sur Instagram : " followed by the anchor whose text is "@ajs_romanelepont" with trailing <span class="sr-only"> (nouvelle fenêtre)</span>. No new CSS.

    2. src/pages/en/about.astro — same insertion point and structure; text "Follow Romane's work on Instagram: " followed by the "@ajs_romanelepont" anchor with trailing <span class="sr-only"> (opens in new tab)</span>. No new CSS.

    3. src/pages/contact.astro — between the <p class="contact-page__lead"> and the <ContactForm locale={locale} /> mount, add <p class="contact-page__social">: text "Vous pouvez aussi me suivre sur Instagram : " followed by the "@ajs_romanelepont" anchor with trailing <span class="sr-only"> (nouvelle fenêtre)</span>. Add a new .contact-page__social CSS rule with the SAME declarations as .contact-page__lead: font-size: var(--text-body-size); font-weight: var(--weight-regular); line-height: 1.5; margin: 0 0 var(--space-2xl);. Do NOT reuse the .contact-page__lead class name.

    4. src/pages/en/contact.astro — same insertion point and structure; text "You can also follow me on Instagram: " followed by the "@ajs_romanelepont" anchor with trailing <span class="sr-only"> (opens in new tab)</span>. Add the identical .contact-page__social CSS rule (each page keeps its own <style> block).

    5. tests/e2e/social-links.spec.ts — create a Playwright spec (follow the style of about.spec.ts) asserting:
       - Footer (site-wide): on both '/' and '/en/', a link with accessible name matching /Instagram/ has href "https://www.instagram.com/ajs_romanelepont/", target="_blank", and rel containing "noopener" and "noreferrer". Assert the site-wide footer instance on at least the homepage of each locale.
       - About FR '/about/' and EN '/en/about/': the main region contains an anchor to instagram.com/ajs_romanelepont whose visible text contains "@ajs_romanelepont"; assert the surrounding FR/EN sentence text ("Suivez le travail de Romane sur Instagram" / "Follow Romane's work on Instagram").
       - Contact FR '/contact/' and EN '/en/contact/': an anchor to instagram.com/ajs_romanelepont exists ABOVE the contact form (assert the paragraph text "Vous pouvez aussi me suivre sur Instagram" / "You can also follow me on Instagram" and target/rel attributes).
       - Attribute checks use toHaveAttribute for target and rel on the located anchors.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website/.claude/worktrees/gsd-resume-work-b448d2 && grep -q 'contact-page__social' src/pages/contact.astro && grep -q 'contact-page__social' src/pages/en/contact.astro && grep -c 'ajs_romanelepont' src/pages/about.astro src/pages/en/about.astro && npm run build && npm run test:e2e -- social-links</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/about.astro and src/pages/en/about.astro each contain exactly one new plain <p> (no class="placeholder") after the last placeholder paragraph, with the locale-correct sentence and an @ajs_romanelepont anchor (href/target/rel exact) plus a trailing .sr-only new-tab hint.
    - src/pages/contact.astro and src/pages/en/contact.astro each contain a new <p class="contact-page__social"> placed between .contact-page__lead and <ContactForm>, plus a matching .contact-page__social CSS rule with declarations identical to .contact-page__lead.
    - All four page anchors use href="https://www.instagram.com/ajs_romanelepont/", target="_blank", rel="noopener noreferrer".
    - tests/e2e/social-links.spec.ts exists and asserts the footer (both locales) plus About and Contact instances in both locales, including target/rel attributes.
    - `npm run build` succeeds and `npm run test:e2e -- social-links` passes.
  </acceptance_criteria>
  <done>A visitor on /about/, /en/about/, /contact/, /en/contact/ sees a localized sentence linking @ajs_romanelepont to Instagram (Contact instance sits above the form), every Instagram link across the site opens safely in a new tab, and the new e2e spec passes green covering all three surfaces in both locales.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site → external (instagram.com) | Static outbound anchor to a third-party domain. No untrusted input enters the site; no data leaves except the standard browser navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | Information Disclosure / Tampering | The 5 outbound `target="_blank"` Instagram anchors (reverse tabnabbing) | mitigate | Every anchor ships `rel="noopener noreferrer"` — enforced in each task's `<action>` and asserted by the e2e spec (target/rel attribute checks). |
| T-04.2-02 | Tampering | Introduced markup/copy | accept | No user input, no dynamic data, no server logic touched — the change is static localized markup with no new attack surface. |
| T-04.2-SC | Tampering | npm/pip/cargo installs | n/a | This phase installs zero packages (no dependency changes); package legitimacy gate not applicable. |
</threat_model>

<verification>
Phase-level checks (run from the worktree root):
- `npm run build` succeeds.
- `npm run test:e2e -- social-links` passes (footer both locales + About FR/EN + Contact FR/EN).
- `npm run test:unit` still passes (no regression to existing unit tests).
- `grep -rc 'rel="noopener noreferrer"'` across the 5 modified source files confirms every Instagram anchor carries the safe rel attribute.
</verification>

<success_criteria>
- The footer on every page (both locales) shows a working "Instagram" link to instagram.com/ajs_romanelepont, opening in a new tab with a screen-reader new-tab hint.
- The About page (FR + EN) shows a localized sentence linking @ajs_romanelepont to Instagram.
- The Contact page (FR + EN) shows a localized sentence linking @ajs_romanelepont to Instagram, above the contact form.
- All 5 Instagram anchors use target="_blank" and rel="noopener noreferrer".
- No new components, dependencies, or client JS were introduced; the design system is unchanged.
- `npm run build`, `npm run test:e2e -- social-links`, and `npm run test:unit` all pass.
</success_criteria>

<output>
Create `.planning/phases/04.2-social-media-links/04.2-01-SUMMARY.md` when done.
</output>
