---
phase: 11-schema-content-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/schemas/edition.ts
  - sanity/schemas/index.ts
  - sanity/schemas/structure.ts
  - .planning/PROJECT.md
autonomous: true
requirements: [CMS-04, EDN-05]
must_haves:
  truths:
    - "A new `edition` document type renders a create/edit form in Sanity Studio with groups Publication / Présentation / Photos / Détails du format."
    - "Format details are typed fields: pageCount (number), printRun (number, not free text), dimensions (object with numeric width/height + unit) — grouped under 'Détails du format' (EDN-05, D-06, success criterion #3)."
    - "A dedicated `leadPhoto` field exists, separate from the `images` photo-shoot array (D-04)."
    - "An 'Éditions' drag-reorderable list appears in the Studio desk, mirroring galleries (D-12/D-14, success criterion #2)."
    - "The Rebut gallery↔édition naming overlap is recorded as Confirmed in PROJECT.md Key Decisions (success criterion #5, D-01/D-02)."
  artifacts:
    - sanity/schemas/edition.ts
    - sanity/schemas/index.ts
    - sanity/schemas/structure.ts
    - .planning/PROJECT.md
  key_links:
    - "`edition` imported into `schemaTypes` in index.ts — else the type does not exist in Studio."
    - "`orderRankField({type:'edition'})` in edition.ts AND `orderableDocumentListDeskItem({type:'edition'})` in structure.ts — both halves required together for drag-reorder (D-12/D-14)."
    - "`'edition'` added to the structure.ts exclusion filter — else the type double-lists in the generic document-type list."
    - "Parent-level `rule.custom()` on `dimensions` + `rule.required().assetRequired()` on `leadPhoto` — else incomplete format data or an asset-less lead photo can publish (Pitfall B)."
---

<objective>
Create the new `edition` Sanity document type (mirroring `gallery.ts`'s editorial workflow, plus a dedicated `leadPhoto` field and a structured `format` group), wire it into the schema registry and Studio desk structure so it is fully editable and drag-reorderable in the local Studio, and record the "Rebut" gallery↔édition naming resolution in PROJECT.md.

Purpose: Every field name chosen here is the contract Phase 12 fetches against via GROQ. Getting the type, wiring, and typed format fields right now is what lets Phase 12 build real routes and lets Romane self-serve éditions (CMS-04, EDN-05). This is the first slice of the phase's vertical goal — after this plan the type exists and works in `sanity dev`; Plan 02 deploys it to the hosted Studio and seeds real content.

Output: `sanity/schemas/edition.ts` (new), edits to `index.ts` and `structure.ts`, and a Confirmed Key Decisions row in `.planning/PROJECT.md`.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-schema-content-model/11-CONTEXT.md
@.planning/phases/11-schema-content-model/11-RESEARCH.md
@.planning/phases/11-schema-content-model/11-PATTERNS.md
@sanity/schemas/gallery.ts
@sanity/schemas/imageRights.ts
@sanity/schemas/index.ts
@sanity/schemas/structure.ts
</context>

## Artifacts this phase produces

This section is the canonical symbol list for downstream phases (Phase 12 fetches against these exact names).

- **New file:** `sanity/schemas/edition.ts`
- **New Sanity document type:** `edition` (title "Édition")
- **New fields on `edition`:**
  - `publicationStatus` (string radio: `preparation` / `published` / `archived`)
  - `title` (plain string)
  - `slug` (slug, sourced from `title`)
  - `statement` (object `{fr, en}` via inline `localeTextField`)
  - `leadPhoto` (image + nested bilingual `alt` object; `required().assetRequired()`)
  - `images` (array of image members, each with bilingual `alt` + `rights` of type `imageRights`)
  - `pageCount` (number)
  - `printRun` (number)
  - `dimensions` (object: `width` number, `height` number, `unit` string)
  - `orderRank` (hidden fractional-index field via `orderRankField({type:'edition'})`)
- **New Studio field groups on `edition`:** `publication`, `content`, `photos`, `format` ("Détails du format")
- **New desk-structure list item:** "Éditions" (orderable, `BookIcon`) in `sanity/schemas/structure.ts`
- **Registry change:** `edition` added to `schemaTypes` in `sanity/schemas/index.ts`
- **Exclusion-filter change:** `'edition'` added to the generic-type exclusion list in `structure.ts`
- **Documentation:** Confirmed "Rebut" naming-resolution row in `.planning/PROJECT.md` Key Decisions
- **Deliberately NOT created (out of scope this phase):** no `showOnHomePage`/`heroColor` (D-13), no `seo` field/group (Claude's Discretion — omitted; Phase 12 may add), no `publishedPageLinks` field (Pitfall D — would 404 before Phase 12), no `reference`/cross-link field to `gallery` (D-03/EDN-08 deferred)

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the `edition` schema type</name>
  <files>sanity/schemas/edition.ts</files>
  <read_first>
    - sanity/schemas/gallery.ts (the exact structural mirror target — field shapes, localeTextField helper, images-array custom validator, preview block)
    - sanity/schemas/imageRights.ts (the `rights` field type reused verbatim on each image)
    - .planning/phases/11-schema-content-model/11-PATTERNS.md (lines 19-218 — per-field excerpts with line numbers for every field to mirror or add)
    - .planning/phases/11-schema-content-model/11-RESEARCH.md (Patterns 1-3 for leadPhoto + format group; Pitfall B for the nested-object/image-asset validation gaps)
    - .planning/phases/11-schema-content-model/11-CONTEXT.md (D-04 through D-13 — the locked field decisions)
  </read_first>
  <action>
    Create `sanity/schemas/edition.ts` exporting `const edition = defineType({name:'edition', title:'Édition', type:'document', ...})`. Mirror `gallery.ts` structurally per 11-PATTERNS.md lines 19-218 and 11-RESEARCH.md Patterns 1-3; do not re-derive from scratch.

    Imports: `defineArrayMember, defineField, defineType` from 'sanity' and `orderRankField` from '@sanity/orderable-document-list'. Do NOT import `HeroColorInput`/`HERO_COLOR_OPTIONS` or `PublishedPageLinks` — no heroColor/showOnHomePage (D-13), and omit PublishedPageLinks so no Studio "Voir sur le site" link 404s before Phase 12 ships the route (Pitfall D; Claude's Discretion resolved as omit).

    Copy the `localeTextField(name, title, group)` helper inline verbatim from gallery.ts lines 22-47, keeping the same "duplicated intentionally — no shared schema-lib module exists yet" comment (project convention).

    Document shell: `initialValue: {publicationStatus: 'published'}` (drop showOnHomePage, D-13). groups in order: `publication` (default:true), `content`, `photos`, `format` (title "Détails du format"). No `homepage` group (D-13). No `seo` group (Claude's Discretion — omitted this phase; note in a comment that Phase 12 may add SEO).

    Fields, in order:
    - `publicationStatus`: copy gallery lines 62-79 verbatim — string, radio, list of preparation/published/archived, initialValue 'published', required. Reword "collection" wording to "édition" (D-07).
    - `title`: plain string, group 'content', required — proper noun shared across locales, NOT a locale object (D-08).
    - `slug`: type 'slug', group 'content', `options:{source:'title'}`, required, same "Cliquer sur « Générer »" description as gallery (D-09).
    - `statement`: `localeTextField('statement', 'Texte de présentation', 'content')` — fr/en both required (D-10, satisfies CMS-04's bilingual statement).
    - `leadPhoto`: per 11-RESEARCH.md Pattern 3 (lines 251-283) — type 'image', group 'photos', `options:{hotspot:true}`, one nested `alt` object field (bilingual `fr`/`en` string, each required, the alt object itself required), and a field-level validation of `rule.required().assetRequired()` with message "Choisir une photo principale." This is a dedicated cover image separate from `images`; do NOT derive it from `images[0]` (D-04).
    - `images`: array, group 'photos', mirror gallery lines 135-226 — `of:[defineArrayMember({type:'image', options:{hotspot:true}, fields:[ alt object bilingual required, rights of type 'imageRights' with the same initialValue object gallery uses ]})]`, `options:{layout:'grid'}`, and the parent-level `rule.custom(...)` validator adapted from gallery's images validator. Per D-11, each image carries a bilingual (fr/en, both required) `alt` plus a `rights` field reusing the existing `imageRights` object type verbatim — no new rights modeling. Reword the field title/description to describe photos of the printed object — e.g. "Photos de l'objet imprimé — couverture, pages intérieures, détail de reliure/impression" (D-05). Keep `alt` fields attached directly on the `image` array member (not a wrapper object) to preserve Studio multi-file drag-and-drop.
    - `pageCount`: number, group 'format', title "Nombre de pages", validation `rule.required().integer().positive()` (D-06/EDN-05).
    - `printRun`: number, group 'format', title "Tirage", validation `rule.required().integer().positive()` — stored as a number, not free text (D-06, success criterion #3).
    - `dimensions`: object, group 'format', `options:{columns:3}`, fields `width` (number, "Largeur", `required().positive()`), `height` (number, "Hauteur", `required().positive()`), `unit` (string, "Unité", `initialValue:'cm'`, required — fixed default, no select list per Claude's Discretion). Add a PARENT-level `rule.custom()` on the dimensions object per 11-RESEARCH.md Pattern 2 (lines 240-247) that returns an error string unless width, height, and unit are all present — this closes Sanity's nested-object validation gap (Pitfall B) using the same technique gallery already applies to its images array.
    - `orderRank`: spread `{...orderRankField({type:'edition'}), hidden: true}` (D-12).

    `preview`: adapt gallery's preview per 11-PATTERNS.md lines 194-214 — `select` id/`title`/`media:'leadPhoto'`/`publicationStatus`; `prepare` returns title (fallback 'Édition sans nom'), a subtitle status label (archived → 'Archivée', preparation → 'En préparation', drafts.-prefixed id → 'Modifications non publiées', else 'Publiée'), and `media`. No heroColor/showOnHomePage/isVisible logic.

    Do NOT add any `reference` or cross-link field to `gallery` — EDN-08 is deferred (D-03); just keep the schema shape open so an optional reference field can be added later without restructuring.
  </action>
  <verify>
    <automated>npm --prefix sanity run lint && npm --prefix sanity run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix sanity run lint` exits 0 and `npm --prefix sanity run build` exits 0 (the Studio schema compiles with the new `edition` type).
    - `sanity/schemas/edition.ts` defines fields `publicationStatus`, `title`, `slug`, `statement`, `leadPhoto`, `images`, `pageCount`, `printRun`, `dimensions`, and a hidden `orderRank`, grouped under `publication`/`content`/`photos`/`format`.
    - `printRun` and `pageCount` are `type: 'number'` (not string/free text); `dimensions` is an object with numeric `width`/`height` and a `unit` string.
    - `leadPhoto` uses `required().assetRequired()`; `dimensions` has a parent-level `rule.custom()` completeness check.
    - No `heroColor`, `showOnHomePage`, `seo`, `publishedPageLinks`, or `reference` field is present (D-03/D-13; SEO/PublishedPageLinks omitted by discretion).
  </acceptance_criteria>
  <done>`edition.ts` compiles (lint + build green) with the full typed field set, grouped format fields, dedicated leadPhoto, and the Pitfall B validators.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Register `edition` and wire the Studio desk structure</name>
  <files>sanity/schemas/index.ts, sanity/schemas/structure.ts</files>
  <read_first>
    - sanity/schemas/index.ts (current registry — gallery/exhibition entries to mirror)
    - sanity/schemas/structure.ts (current desk structure — gallery orderable item lines 54-60, exclusion filter lines 67-78, existing @sanity/icons imports lines 3-11)
    - .planning/phases/11-schema-content-model/11-PATTERNS.md (lines 222-309 — the exact index.ts + structure.ts edits, including the TagsIcon correction)
    - .planning/phases/11-schema-content-model/11-CONTEXT.md (D-14 — desk-structure decision)
  </read_first>
  <action>
    In `sanity/schemas/index.ts`: add `import {edition} from './edition'` immediately after the `gallery` import (current line 3), and insert `edition,` into the `schemaTypes` array immediately after `gallery,` (current line 15). Matches 11-PATTERNS.md lines 222-248.

    In `sanity/schemas/structure.ts` (D-14): add a distinct, not-already-used `@sanity/icons` export to the existing icon import block (lines 3-11) — use `BookIcon` (apt for éditions/books; if unavailable, `DocumentIcon`). Do NOT reuse `TagsIcon` (already used by the "Crédits et droits" list item, line 64) or `ImagesIcon` (used by galleries) — see the correction in 11-PATTERNS.md lines 297-308. Insert an `orderableDocumentListDeskItem({type:'edition', title:'Éditions', icon: BookIcon, S, context})` block immediately after the gallery `orderableDocumentListDeskItem` block (lines 54-60). Add the string `'edition'` to the exclusion-filter array (lines 69-77) so `S.documentTypeListItems()` does not double-list the type. Do NOT use the plain `S.documentTypeListItem('edition')` form (that is the non-orderable exhibition pattern — édition follows the orderable gallery pattern, D-14).
  </action>
  <verify>
    <automated>npm --prefix sanity run lint && npm --prefix sanity run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm --prefix sanity run lint` and `npm --prefix sanity run build` both exit 0.
    - `sanity/schemas/index.ts` imports `edition` and includes it in `schemaTypes`.
    - `sanity/schemas/structure.ts` contains an `orderableDocumentListDeskItem({type:'edition', ...})` with a non-`TagsIcon`/non-`ImagesIcon` icon, and `'edition'` appears in the exclusion-filter array.
    - Running `sanity dev` locally shows a single "Éditions" list item (not double-listed) with drag handles.
  </acceptance_criteria>
  <done>`edition` is registered in `index.ts` and appears once as an orderable "Éditions" desk item in `structure.ts`; lint + build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the "Rebut" naming resolution in PROJECT.md (success criterion #5)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (Context "Open item (v1.3)" line 86; Key Decisions table lines 102-123)
    - .planning/phases/11-schema-content-model/11-CONTEXT.md (D-01, D-02, D-03 — the locked resolution wording)
  </read_first>
  <action>
    Record the "Rebut" gallery↔édition naming resolution so success criterion #5 is satisfied. Per D-02 this was ALREADY confirmed directly with Romane — this is a documentation task, NOT a new human sign-off checkpoint.

    Add a new row to the Key Decisions table in `.planning/PROJECT.md` (after the most recent row, currently line 123): Decision = the Portfolio gallery "Rebut" and the new "Rebut" édition are the SAME underlying subject presented as two distinct objects (the gallery shows the photographs; the édition is the printed book/zine of that photo collection); Rationale = they stay as two separate documents/pages — the gallery is not moved, renamed, or merged into Éditions (D-01); Outcome = "Confirmed — raised with and confirmed by Romane during Phase 11 (D-01/D-02)". In the Decision/Rationale text note that an optional future gallery↔édition cross-link is tracked as EDN-08 and deferred to a future milestone (D-03) — no reference field is added now.

    Also update the Context "Open item (v1.3)" bullet (line 86) so it reads as RESOLVED (reference the new Key Decisions row) rather than "unconfirmed / needs a direct check with Romane."

    Use the Edit tool for scoped replacements — do NOT rewrite the whole PROJECT.md file.
  </action>
  <verify>
    <automated>rg -n "Rebut.*Confirm" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` Key Decisions table contains a new row about the Rebut gallery/édition subject whose Outcome column is marked Confirmed (the `rg` command above returns at least one match).
    - The row states the gallery and édition are kept as two separate documents (gallery not moved/merged) and notes EDN-08 (cross-link) as deferred.
    - The Context "Open item (v1.3)" bullet no longer describes the naming overlap as unconfirmed / needing a check.
    - No unrelated PROJECT.md content was removed (scoped Edit only).
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions has a Confirmed Rebut resolution row and the Context open item is marked resolved (success criterion #5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio editor form → Sanity Content Lake | Editor-entered field values (dimensions, alt, images) cross into stored documents that Phase 12 fetches at build time — incomplete/invalid data here becomes a downstream build/render risk. |
| Local schema source → schema registry/desk | New `edition.ts` must be correctly registered/wired or the type silently does not appear (functional, not security, risk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | `edition.dimensions` (nested object) + `edition.leadPhoto` (image field) validation | medium | mitigate | Parent-level `rule.custom()` on `dimensions` checking width/height/unit all present (Pattern 2); `rule.required().assetRequired()` on `leadPhoto` — closes Sanity's documented nested-object + image-asset validation gaps so incomplete data cannot publish and break Phase 12's build-time fetch (Pitfall B). |
| T-11-SC | Tampering | npm installs | low | accept | No new packages are installed this phase — reuses the already-vetted `@sanity/orderable-document-list` (legitimacy-checked in Phase 2) and in-repo modules (`imageRights`, inline `localeTextField`). No new supply-chain surface. |
</threat_model>

<verification>
- `npm --prefix sanity run lint && npm --prefix sanity run build` green after each task (schema + wiring compile).
- Local `sanity dev`: an "Éditions" list appears once, is drag-reorderable, and the create form shows all four field groups with typed format fields.
- `rg -n "Rebut.*Confirm" .planning/PROJECT.md` returns the new Confirmed decision row.
- All of D-01 through D-14 are implemented or explicitly omitted-by-decision in this plan (D-15 seeding is Plan 02).
</verification>

<success_criteria>
- `sanity/schemas/edition.ts` exists, compiles, and defines the full field set with typed/grouped format fields (EDN-05, success criterion #3).
- `edition` is registered in `index.ts` and wired as an orderable desk item in `structure.ts` (success criterion #2 groundwork).
- The Rebut naming resolution is recorded as Confirmed in PROJECT.md Key Decisions (success criterion #5).
- Studio lint + build pass; the type is usable in local `sanity dev`.
</success_criteria>

<output>
Create `.planning/phases/11-schema-content-model/11-01-SUMMARY.md` when done.
</output>
