---
phase: 20-mobile-navigation-accent-color
plan: 06
type: execute
wave: 5
depends_on: ['20-03', '20-04', '20-05']
files_modified:
  - src/components/MobileNavPanel.astro
  - src/components/SiteHeader.astro
  - tests/e2e/mobile-nav.spec.ts
autonomous: true
gap_closure: true
requirements: [HOME-13]

must_haves:
  truths:
    - "Opening the mobile nav panel on a phone-width homepage shows exactly THREE big Display-size links in the centred stack (Éditions / About / Contact) — the language switcher is no longer one of them (reverses D-04's switcher clause per 20-UAT.md Test 2) — automated: npm run build && npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium"
    - "The language switcher renders in the panel's bottom secondary tier at the SAME small size and colour as the Instagram line: 14px / weight 400 / non-Unbounded family / ink (#1A1A1A) — automated: same spec"
    - "The switcher line and the Instagram line occupy two separate stacked rows, switcher directly above Instagram, both horizontally centred — not one shared row (user-confirmed during diagnosis) — automated: same spec"
    - "The Instagram line carries the same inline Instagram glyph the desktop header uses, sized to match the switcher's globe, beside the @handle, in ink — automated: same spec"
    - "Both secondary lines keep a >= 44px tap target and the switcher still navigates to the other locale and sets the ajs_locale cookie from inside the focus-trapped panel — automated: same spec"
    - "The Instagram line stays the bottom-most element, 48px clear of the panel's bottom edge (UI-SPEC 2xl offset), and its href/target/rel contract is unchanged — automated: same spec"
    - "Every other page's header, the desktop homepage header, the panel's motion, halftone, focus containment and script-count tripwire are all unchanged — automated: npm run test:e2e plus npm run test:coverage"
  artifacts:
    - "src/components/MobileNavPanel.astro — LanguageSwitcher relocated OUT of .mobile-nav-panel__nav to a direct-child position immediately before the .mobile-nav-panel__secondary anchor; the Instagram glyph inlined inside that anchor"
    - "src/components/SiteHeader.astro — the Display-role .switcher-link override block replaced by secondary-tier layout plus an ink colour rule; a gap declaration on .mobile-nav-panel__secondary for the icon/label pair; three stale D-04 comments corrected"
    - "tests/e2e/mobile-nav.spec.ts — the Display-size switcher assertion rewritten to Label size in ink, plus new structural (DOM position, icon count) and geometric (two stacked rows, bottom offset, tap targets) assertions, all tagged (20-06)"
  key_links:
    - "`.mobile-nav-panel > .language-switcher` only matches once the markup move in Task 2 actually makes the switcher a DIRECT child of the dialog — leave it nested inside .mobile-nav-panel__nav and the layout rule silently never applies (the selector-shape failure class documented in 19-PATTERNS.md / 20-RESEARCH.md Pitfall 2). Task 1's computed-style assertions are the gate that catches this."
    - "Deleting the `.switcher-link svg { width: 22px; height: 22px }` override is what lets the globe fall back to its own 16x16 attribute size, which is what makes it match the new 16x16 Instagram glyph — leave it in place and the two stacked lines carry visibly mismatched icons."
    - "NOT re-introducing `padding: 0` on the switcher link is what preserves LanguageSwitcher.astro's own `padding: 8px` + `min-height: 44px` WCAG 2.5.5 tap target in the new position — the panel is a focus-trapped modal, so a sub-44px locale control there is a real dead end (T-20-06-03)."
    - "`margin-bottom: var(--space-2xl)` stays on `.mobile-nav-panel__secondary` (the bottom-most line). Moving it to a shared wrapper would break the already-green assertion at mobile-nav.spec.ts lines 305-332 AND change what that assertion means."
    - "src/components/LanguageSwitcher.astro is deliberately NOT in files_modified — its own scoped `.switcher-link { color: var(--color-accent) }` default (D-06 pink) must keep applying on every other page."
---

<objective>
Close the two major gaps 20-UAT.md Test 2 recorded from the user's live on-phone test: move the language switcher out of the panel's big primary list into a small secondary line stacked directly above the Instagram link, and give that Instagram link the site's own Instagram glyph.

Purpose: neither gap is a code defect. Plans 20-03/20-04 implemented 20-CONTEXT.md's D-04 faithfully ("the language switcher becomes one more full-size stacked item"), and the user has now seen that live and reversed the switcher half of D-04 — the switcher belongs at the bottom at the Instagram line's size, not as a fourth 32px item. The Instagram glyph was simply never carried over from SiteHeader.astro when MobileNavPanel.astro was authored as a separate file, because this codebase has no shared icon component. Both land in the same bottom region of the same file pair, and the first fix changes the markup the second one targets, so they are closed together in one ordered plan.
Output: a three-primary-link panel with a two-line secondary tier (switcher above an icon-bearing Instagram handle), and an e2e contract that encodes the reversed hierarchy so it cannot silently drift back.
</objective>

## What reverses, and what explicitly does NOT

| Decision | Status after this plan |
|----------|------------------------|
| D-04 primary tier — the big equal-weight Display-size stack of Éditions / About / Contact | **UNCHANGED.** Still 32px / 600 / Unbounded / ink, still centred, still `.mobile-nav-panel__link`. |
| D-04 primary tier — "and the language switcher" as a fourth equal-weight item | **REVERSED** by 20-UAT.md Test 2 (severity major, user's live phone test). The switcher becomes a secondary-tier line. |
| D-04 secondary tier — the Instagram link small, near the bottom | **UNCHANGED in position and size**, extended with the header's Instagram glyph. Still the bottom-most element, still 48px clear of the panel edge. |
| 20-UI-SPEC.md Colour — every item inside the panel renders in `--color-ink`, not accent pink | **UNCHANGED and still enforced**, including for the switcher. Only its SIZE reverses, not its colour. |
| D-06 — switcher links render accent pink everywhere else on the site | **UNCHANGED.** `LanguageSwitcher.astro` is not edited. |
| D-01 / D-02 / D-03 / D-05 / HOME-16 | **UNCHANGED.** No motion, halftone, hamburger, prop-gating or accent-colour behaviour is touched. |

## Artifacts this plan produces

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| markup (moved) | `<LanguageSwitcher />` — from 4th child of `.mobile-nav-panel__nav` to direct child of `dialog#mobile-nav`, immediately before `.mobile-nav-panel__secondary` | logic, copy, href and cookie script all unchanged |
| markup (new) | inline Instagram `<svg>` inside `.mobile-nav-panel__secondary`, before `{instagramLabel}` | duplicated from `SiteHeader.astro` lines 106-118, resized 20 to 16 |
| CSS rule (new) | `.mobile-nav-panel > .language-switcher` | secondary-tier layout plus halftone stacking |
| CSS rule (new) | `.mobile-nav-panel > .language-switcher .switcher-link` | ink colour only |
| CSS rules (deleted) | the three-rule Display-role override block, `SiteHeader.astro` lines 777-793 | this is what currently forces 32px / 600 / Unbounded / 22px-globe onto the switcher |
| CSS declaration (new) | `gap` on `.mobile-nav-panel__secondary` | separates the new glyph from the @handle |
| test (rewritten) | the Display-size switcher assertion, `mobile-nav.spec.ts` lines 252-303 | now asserts Label size in ink |
| test blocks (new) | DOM-position + icon-count structural assertions; two-stacked-rows geometry assertions | all titled with a `(20-06)` marker |

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-mobile-navigation-accent-color/20-UAT.md
@.planning/debug/mobile-nav-switcher-hierarchy.md
@.planning/debug/mobile-nav-instagram-icon.md
@.planning/phases/20-mobile-navigation-accent-color/20-CONTEXT.md
@.planning/phases/20-mobile-navigation-accent-color/20-UI-SPEC.md
@.planning/phases/20-mobile-navigation-accent-color/20-PATTERNS.md
@.planning/phases/20-mobile-navigation-accent-color/20-03-SUMMARY.md
@.planning/phases/20-mobile-navigation-accent-color/20-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the reversed hierarchy and the icon in the e2e contract (RED first)</name>
  <files>tests/e2e/mobile-nav.spec.ts</files>
  <read_first>
    - `tests/e2e/mobile-nav.spec.ts` lines 1-30 — the file's own header comment. It records that this file was written and proven green BEFORE the source change it guards, per the v1.5 retrospective's regression-net-first pattern, and it explicitly forbids weakening or removing the four net describe blocks that follow (lines 31-154). This task adds to and rewrites only blocks BELOW line 154. Do not touch anything above it, and do not change `EXPECTED_SCRIPT_COUNT` (still 4).
    - The same file lines 208-234, the `the panel holds the D-04 two-tier contents` test. Every assertion in it stays TRUE after this plan (3 `.mobile-nav-panel__link`, 1 `.switcher-link`, 1 `.mobile-nav-panel__secondary` with its href/target/rel). Leave it byte-for-byte intact and add a NEW sibling test rather than editing it.
    - The same file lines 252-303, `the primary list and switcher render at Display size in ink`. This is the ONE existing test that directly encodes the hierarchy being reversed and it MUST be rewritten. Note its `dialog.style.display = 'flex'` / restore-previous-display trick for reading resolved styles off a closed dialog, and reuse that trick verbatim.
    - The same file lines 305-332, `the secondary line renders at Label size` — the reference shape the switcher's new assertions must mirror (14px / 400 / marginBottom 48px). Leave this test intact; it stays green and keeps its meaning because the Instagram anchor remains the bottom-most line.
    - The same file lines 381-407 and 516-527 — the behaviour describe block's `openPanel(page)` helper and the existing switcher-navigates-and-sets-cookie test. The new geometry test belongs in this block because bounding boxes require a genuinely visible panel.
    - `.planning/phases/20-mobile-navigation-accent-color/20-UAT.md` Gaps section in full — the two gaps' `missing` lists are the acceptance surface this contract must cover.
  </read_first>
  <behavior>
    New/changed expectations, all against `/` and `/en/` at a 393x852 viewport. Every new or retitled test carries the literal marker `(20-06)` in its title so the whole changed set can be selected with a single Playwright title filter.

    In the structural describe block (closed dialog, counts and attributes only) — a NEW test, `${path}: the switcher sits in the panel's bottom secondary tier, stacked above an icon-bearing Instagram line (20-06)`:
    - `dialog#mobile-nav .mobile-nav-panel__nav .switcher-link` has count 0 — the switcher is no longer inside the primary list.
    - `dialog#mobile-nav > .language-switcher` has count 1 — it is a direct child of the dialog (the direct-child form is what the new CSS rule depends on, so assert the same shape the CSS asserts).
    - Read `nextElementSibling` off that `.language-switcher` in the page and assert its class list contains `mobile-nav-panel__secondary` — the switcher line sits immediately ABOVE the Instagram line in source order.
    - `dialog#mobile-nav .mobile-nav-panel__secondary svg` has count exactly 1, and that svg has attribute `aria-hidden` equal to `"true"` (decorative — the anchor's own aria-label carries the accessible name).
    - `dialog#mobile-nav .mobile-nav-panel__nav > *` has count 3 — nothing else crept into the primary list.

    Rewriting the existing lines 252-303 test, retitled `${path}: the primary list renders at Display size and the switcher renders at Label size in ink (20-06)`:
    - The `.mobile-nav-panel__link` half is UNCHANGED: fontSize `32px`, fontWeight `600`, fontFamily contains `Unbounded`, color `rgb(26, 26, 26)`.
    - The `.switcher-link` half INVERTS: fontSize `14px`, fontWeight `400`, fontFamily does NOT contain `Unbounded`, color `rgb(26, 26, 26)`.
    - Extend the same `page.evaluate` to also read the computed `width` of the switcher's own globe svg and of the Instagram svg inside `.mobile-nav-panel__secondary`, and assert both are `16px` — the two stacked secondary lines carry same-size icons.

    In the behaviour describe block (panel genuinely open via `openPanel(page)`) — a NEW test, `the switcher and Instagram lines are two stacked rows near the panel's bottom edge (20-06)`:
    - Take bounding boxes for `dialog#mobile-nav .switcher-link`, `dialog#mobile-nav .mobile-nav-panel__secondary` and `dialog#mobile-nav` itself; none may be null.
    - Stacked, switcher above: `switcherBox.y + switcherBox.height` is less than or equal to `secondaryBox.y + 1` (the 1px slack absorbs subpixel layout). This is the assertion that fails if the two ever end up sharing a row.
    - Both horizontally centred: each box's own centre x is within 2px of `panelBox.x + panelBox.width / 2`.
    - Instagram stays the bottom-most line, 48px clear: `panelBox.y + panelBox.height - (secondaryBox.y + secondaryBox.height)` is between 44 and 56 inclusive.
    - Tap targets survive the move: both boxes report `height` greater than or equal to 44.
  </behavior>
  <action>
    Apply the changes described in the behavior block above to `tests/e2e/mobile-nav.spec.ts` only — no source file is edited in this task. This is the RED half of the cycle: after this task the spec MUST fail, and it must fail for the intended reasons.

    Comment each new block with WHY it exists, in this file's established style: name 20-UAT.md Test 2 as the origin, state that D-04's switcher clause was reversed by the user's live on-phone test after the phase shipped, and record that the stacked (not same-row) arrangement and the switcher-above-Instagram order are the user-confirmed shape. A future reader must be able to tell that the inverted expectation is deliberate, not a regression someone codified by accident.

    Hard prohibitions:
    - Do NOT weaken, retitle or delete anything in the four net describe blocks at lines 31-154, and do NOT change `EXPECTED_SCRIPT_COUNT`. Plan 20-02 built those specifically to catch this component breaking unrelated pages, which it has done twice before.
    - Do NOT delete the `the secondary line renders at Label size` test at lines 305-332 and do NOT relax its `marginBottom` expectation. The Instagram anchor keeps that 48px bottom offset in Task 2 precisely so this stays true.
    - Do NOT add a `.language-switcher` locator that is scoped to `[data-role="site-header"]` in these new blocks — every existing switcher locator in the repo's e2e suite is header-scoped, which is exactly why relocating the panel's switcher breaks none of them. Keep the new assertions dialog-scoped.

    After editing, run the RED gate below. Read the failure output and confirm the failures are the INTENDED assertion mismatches — received `32px` where `14px` is expected, `Unbounded` present where it should be absent, svg count 0 where 1 is expected, `.mobile-nav-panel__nav .switcher-link` count 1 where 0 is expected, `> .language-switcher` count 0 where 1 is expected. If any failure is instead a syntax error, a strict-mode violation or a null bounding box, fix the test code and re-run until the only failures are the intended contract mismatches. Record the exact RED failure list in the SUMMARY.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; if npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium -g "20-06"; then echo "UNEXPECTED PASS - the new assertions did not actually land"; exit 1; else echo "RED as expected"; fi</automated>
  </verify>
  <acceptance_criteria>
    - The RED gate command above exits 0 (i.e. the `-g "20-06"` selection genuinely fails against unmodified source).
    - `npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium -g "20-06" --list` lists 5 tests: 2 structural (one per locale path), 2 restyled typography (one per locale path), 1 geometry.
    - `grep -c 'EXPECTED_SCRIPT_COUNT = 4' tests/e2e/mobile-nav.spec.ts` returns 1.
    - `git diff --stat tests/e2e/mobile-nav.spec.ts` shows no deletions inside lines 1-154 (verify by inspecting the diff hunks' line ranges and record them in the SUMMARY).
    - The SUMMARY lists every RED failure message observed, each mapped to the gap (20-UAT.md gap 1 or gap 2) it encodes.
  </acceptance_criteria>
  <done>The spec now describes the panel the user asked for — three big primary links, a Label-size ink switcher stacked above an icon-bearing Instagram line — and fails against the current source for exactly those reasons and no others.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move the switcher to the secondary tier and give Instagram its glyph (GREEN)</name>
  <files>src/components/MobileNavPanel.astro, src/components/SiteHeader.astro</files>
  <read_first>
    - `src/components/MobileNavPanel.astro` lines 1-24 (the header comment recording the hard structural constraints and the "no styles of its own" contract), lines 86-95 (the primary `<nav>` with the switcher as its 4th child and the D-04 comment above it), and lines 96-105 (the secondary Instagram anchor).
    - `src/components/SiteHeader.astro` lines 106-118 — the source-of-truth Instagram glyph to duplicate: a 20x20 `<svg>` with `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `stroke-width="1.8"`, `aria-hidden="true"`, containing a rounded-square outline `<rect x="2.5" y="2.5" width="19" height="19" rx="5.5" />`, an outline `<circle cx="12" cy="12" r="4.6" />` and a solid dot `<circle cx="17.4" cy="6.6" r="1.1" fill="currentColor" stroke="none" />`. Note it is entirely `currentColor`-driven, so it needs no colour wiring of its own.
    - `src/components/SiteHeader.astro` lines 634-641 — the comment explaining the explicit `position: relative; z-index: 1` stacking pattern, and WHY it is required: an absolutely-positioned element with `z-index: 0` still paints above non-positioned in-flow content in the same stacking context, so every content-layer child of the dialog needs its own positioned stacking. The relocated switcher becomes such a child and needs the same treatment.
    - `src/components/SiteHeader.astro` lines 736-748 — `.mobile-nav-panel__nav`'s rule and its now-stale "all four items (three links + the language switcher) read as equal weight" comment.
    - `src/components/SiteHeader.astro` lines 767-793 — the three-rule override block to delete, and its comment explaining the `is:global` reach. Understand exactly what each rule does before removing it: the `font-size: inherit` on `.language-switcher` neutralises that component's own 14px, the `.switcher-link` block forces Display typography plus `padding: 0`, and the `svg` rule scales the globe to 22px. Removing all three is what restores the component's own 14px / 400 / `padding: 8px` / 16px-globe defaults.
    - `src/components/SiteHeader.astro` lines 795-816 — `.mobile-nav-panel__secondary`'s complete rule, the styling target the switcher must visually match, and its `margin-bottom: var(--space-2xl)`.
    - `src/components/LanguageSwitcher.astro` lines 18-36 and 38-67 — the markup it renders (`<nav class="language-switcher">` wrapping `<a class="switcher-link">` with a 16x16 globe svg) and its own scoped styling (`.language-switcher` 14px/400/1.5; `.switcher-link` `gap: var(--space-xs)`, `min-height: 44px`, `padding: 8px`, `color: var(--color-accent)`). This file is READ-ONLY in this plan.
    - `src/components/SiteHeader.astro` lines 450-471 and 509-513 — the two pre-existing `.language-switcher` / `.switcher-link` rules that are `.site-header`-scoped. Confirm for yourself that neither can reach the panel's switcher (the dialog is a sibling of `<header>`, never a descendant), so relocating it inside the dialog cannot pick up the header's hide-at-phone-width or 400px 12px trim rules.
    - `.planning/phases/20-mobile-navigation-accent-color/20-UI-SPEC.md` Colour section — the rule that ALL items inside the panel render in `--color-ink`, which still holds for the switcher; only its typographic role changes.
  </read_first>
  <action>
    Two files, one atomic change. The DOM move and its styling are inseparable — landing only one of them produces a visibly broken panel, so both happen in this task.

    **`MobileNavPanel.astro` — markup.**
    1. Delete `<LanguageSwitcher />` and the three-line D-04 comment above it from inside `<nav class="mobile-nav-panel__nav">`. That `<nav>` must be left holding exactly the three `.mobile-nav-panel__link` anchors and nothing else.
    2. Re-insert `<LanguageSwitcher />` as a DIRECT child of the `<dialog>`, positioned after the closing `</nav>` and immediately BEFORE the `.mobile-nav-panel__secondary` anchor. Comment the new position with: 20-UAT.md Test 2 reversed D-04's switcher clause after the user's live on-phone test; the switcher is now the upper of two stacked secondary lines; the switcher goes ABOVE Instagram so the Instagram anchor stays the bottom-most element and keeps owning the panel's 48px bottom offset; the import, the component's copy, its href logic and its cookie script are all untouched.
    3. Inside the `.mobile-nav-panel__secondary` anchor, insert the Instagram glyph before the `{instagramLabel}` expression, duplicating `SiteHeader.astro` lines 106-118 verbatim with ONE change: `width` and `height` become `16` instead of `20`. Keep `viewBox="0 0 24 24"`, `fill="none"`, `stroke="currentColor"`, `stroke-width="1.8"`, `aria-hidden="true"` and all three child primitives exactly as they are in the header. Sizing via the attributes (not a CSS rule) matches how `LanguageSwitcher.astro` sizes its own 16x16 globe, so the two stacked lines get matching icons with no extra rule. Comment that 16 rather than 20 is deliberate — it matches the sibling switcher line's globe at the same 14px Label size — and that this codebase has no shared icon component, so inlining the same glyph per component is the existing convention, not a refactor debt (see the mobile-nav-instagram-icon debug session).
    4. Leave `aria-label={`Instagram ${instagramLabel}`}`, the `sr-only` new-tab hint span, `target="_blank"` and `rel="noopener noreferrer"` exactly as they are. The svg is decorative and `aria-hidden`, so the accessible name must not change.
    5. Update the file's own header comment so it no longer describes the switcher as a primary item, and record the UAT reversal there. Do not touch any of the four hard structural constraints listed in that comment, and do not touch the client script.

    **`SiteHeader.astro` — the `<style is:global>` block.**
    6. Delete all three rules in the block at lines 777-793: the `font-size: inherit` rule on `.mobile-nav-panel .language-switcher`, the Display-role `.mobile-nav-panel .switcher-link` block, and the `.mobile-nav-panel .switcher-link svg` 22px rule. Removing them is the entire mechanism by which the switcher falls back to `LanguageSwitcher.astro`'s own 14px / 400 / 1.5 / `padding: 8px` / `min-height: 44px` / 16px-globe defaults. Do NOT keep `padding: 0` in any form — that declaration existed only to stop the 8px padding disrupting the 32px stack, and re-introducing it would collapse the tap target the panel's focus-trapped modal depends on.
    7. In its place add `.mobile-nav-panel > .language-switcher` with `position: relative`, `z-index: 1` and `align-self: center`. The first two mirror the documented stacking pattern (lines 634-641) now that the switcher is a positioned-content-layer child of the dialog in its own right rather than inheriting `.mobile-nav-panel__nav`'s stacking; `align-self: center` matches `.mobile-nav-panel__secondary` so the two secondary lines share one centre axis. Add no `margin` and no `gap` between the two lines: each is a 44px-tall box holding centred 14px text, which already yields roughly 23px of optical separation, and any margin here would change the Instagram anchor's 48px bottom-offset relationship.
    8. Add `.mobile-nav-panel > .language-switcher .switcher-link` setting `color: var(--color-ink)` and nothing else. Comment why the chain includes the intermediate class: it makes the selector three classes deep, which beats `LanguageSwitcher.astro`'s own scoped `.switcher-link` accent-pink rule on specificity alone rather than relying on stylesheet order between an `is:global` block and another component's scoped block. Comment also that this ink override is 20-UI-SPEC.md's Colour requirement for the panel (every panel item renders in ink) and is NOT part of the reversal — only the typographic size reverses.
    9. Add a `gap: var(--space-xs)` declaration to the existing `.mobile-nav-panel__secondary` rule so the new glyph does not butt against the @handle. `--space-xs` is the same 4px token `LanguageSwitcher.astro` uses between its globe and its label, which keeps the two stacked lines' icon/label rhythm identical. Change nothing else in that rule — in particular `margin-bottom: var(--space-2xl)` stays, because it is what keeps the Instagram line 48px clear of the panel's bottom edge and what the already-green assertion at `mobile-nav.spec.ts` lines 305-332 measures.
    10. Correct three now-stale comments: `.mobile-nav-panel__nav`'s (lines 736-738) so it describes three equal-weight links rather than four items; the stacking comment (lines 637-638) so its list of mirrored content-layer elements includes the relocated switcher; and `.mobile-nav-panel__secondary`'s (lines 795-796) so it describes the bottom-most of TWO stacked secondary lines. Each corrected comment should name 20-UAT.md Test 2 as the reason, so the next reader does not "fix" the code back toward the original D-04 wording.

    **Hard prohibitions.**
    - Do NOT edit `src/components/LanguageSwitcher.astro`. Its accent-pink `.switcher-link` default (D-06) must keep applying in the desktop header on every page; changing it there would leak this panel-only treatment site-wide, which is exactly the failure mode 20-PATTERNS.md documents for this file.
    - Do NOT wrap the switcher and the Instagram link in a shared container element. That would move ownership of the 48px bottom offset off `.mobile-nav-panel__secondary` and invalidate a currently-green assertion, and it is not needed — two `align-self: center` flex children of the dialog already produce the stacked arrangement.
    - Do NOT add any styles to `MobileNavPanel.astro`. That component's stated contract is that all its CSS lives in `SiteHeader.astro`'s `is:global` sheet.
    - Do NOT add an `overflow` declaration anywhere, and do NOT set `position` on `.mobile-nav-panel` (a modal dialog's UA `position: fixed` is what makes the full-screen takeover and the halftone containment work — see plan 20-05's own prohibitions).
    - Do NOT touch the client script, the motion CSS, the halftone rules, the hamburger, or anything gated on the `mobileNav` prop.

    Then run the GREEN gates. If a Task 1 assertion still fails, fix the SOURCE, never the assertion.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium</automated>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/accessibility.spec.ts tests/e2e/site-header.spec.ts tests/e2e/i18n.spec.ts tests/e2e/homepage-carousel-core.spec.ts tests/e2e/homepage-mobile-responsive.spec.ts tests/e2e/visual.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - The full `tests/e2e/mobile-nav.spec.ts` run passes under `--project=chromium`, including all 5 `(20-06)` tests from Task 1 and every pre-existing test in the file, with zero skips.
    - `grep -c 'LanguageSwitcher' src/components/MobileNavPanel.astro` returns 2 (the import and the single usage) — the switcher was moved, not duplicated.
    - `grep -c 'switcher-link' src/components/LanguageSwitcher.astro` and `git diff --stat src/components/LanguageSwitcher.astro` confirm that file is completely unmodified (empty diff).
    - `grep -c 'mobile-nav-panel > .language-switcher' src/components/SiteHeader.astro` returns at least 2 (the layout rule and the colour rule).
    - `grep -c 'space-2xl' src/components/SiteHeader.astro` is unchanged from before this task, and `.mobile-nav-panel__secondary` still carries its `margin-bottom` (record both observations in the SUMMARY).
    - `tests/e2e/visual.spec.ts` passes with `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` empty — the `/about/` header baseline is untouched, proving no desktop/other-page leak.
    - `tests/e2e/accessibility.spec.ts` still reports zero serious or critical axe violations for the open panel at 393x852 in both locales, with no rule exclusion added.
    - `git diff src/components/MobileNavPanel.astro` shows no change inside the `<script>` block.
  </acceptance_criteria>
  <done>On a phone-width homepage the open panel shows three big primary links, then a small ink language-switcher line, then an icon-bearing Instagram handle 48px above the bottom edge — and every Task 1 assertion plus every pre-existing panel, header, i18n, a11y and visual assertion is green.</done>
</task>

<task type="auto">
  <name>Task 3: Full CI-order regression sweep and phase re-close</name>
  <files>tests/e2e/mobile-nav.spec.ts</files>
  <read_first>
    - `.github/workflows/deploy.yml` — the blocking-gate order this task reproduces locally: Sanity Studio lint + build, `npm run typecheck` BEFORE the first build, the root-base build, `npm run test:artifact`, the Playwright e2e run, `npm run test:coverage`, then the rebuild with `ASTRO_BASE: /ajs-website/`, the un-prefixed-link grep guard over `dist/`, and finally `npm run test:artifact` with `EXPECTED_BASE: /ajs-website/`.
    - `CLAUDE.md` "GitHub Actions pipeline, in actual step order" — the prose restatement of the same sequence.
    - `.planning/phases/20-mobile-navigation-accent-color/20-05-SUMMARY.md` — the recorded end-of-phase e2e and unit counts and coverage percentages this sweep must meet or exceed, and the phase-gate cross-check block plan 20-05 appended at `mobile-nav.spec.ts` lines 613-672.
    - `.planning/phases/20-mobile-navigation-accent-color/20-VERIFICATION.md` — the truths already marked satisfied, so the SUMMARY can state which ones this plan re-proved rather than re-deriving them.
  </read_first>
  <action>
    Run the phase gate locally in the CI's real order. Fix any fallout at the source; never relax a gate to make it pass.
    1. `npm --prefix sanity run lint`, then `npm --prefix sanity run build`. The Studio is untouched by this plan, so both must pass unchanged; a failure means unrelated drift and should be reported, not worked around.
    2. `npm run typecheck`.
    3. `npm run build` (root base), then `npm run test:artifact`.
    4. `npm run test:e2e` — the full suite under both configured Playwright projects, `chromium` and `webkit-mobile`. `tests/e2e/critical.smoke.spec.ts` is the one spec the `webkit-mobile` project matches and it exercises the mobile panel's open path, so this is where a WebKit-specific layout or focus consequence of the switcher move would surface.
    5. `npm run test:coverage` — the Vitest thresholds. No unit-testable logic changed in this plan, so the percentages must not drop.
    6. `ASTRO_BASE=/ajs-website/ npm run build`, then reproduce the workflow's un-prefixed-link guard against `dist/` using the same `grep -rE` pattern the workflow uses, then `EXPECTED_BASE=/ajs-website/ npm run test:artifact`. The relocated switcher still renders a `getSwitcherHref`-produced href and the Instagram anchor is absolute, so this guard must stay silent; if it fires, a literal path was hardcoded during the markup move.

    Then extend the existing `Phase 20 — phase gate cross-checks` describe block at `mobile-nav.spec.ts` lines 613-672 with one durable assertion that outlives this session: at 393x852 on `/` and `/en/`, the panel's `.switcher-link` href still matches the header's own `.language-switcher .switcher-link` href (the block's existing anchor cross-check already covers every dialog anchor generically — add an explicit, named assertion for the switcher specifically, commented as the guard against a future edit that relocates the switcher again and accidentally re-renders it with a stale or non-base-aware href). Keep it inside the existing describe block; do not add a new one.

    Record in the SUMMARY: every command with its outcome, the final full-suite e2e count broken down by project, the unit count, the four coverage percentages, the confirmed `EXPECTED_SCRIPT_COUNT`, and an explicit statement that `src/components/LanguageSwitcher.astro` has an empty diff. Then state, gap by gap, how each of the two 20-UAT.md gaps' `missing` items was closed, so `/gsd-verify-work` can re-run Test 2 against a concrete claim.
  </action>
  <verify>
    <automated>npm --prefix sanity run lint &amp;&amp; npm --prefix sanity run build &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run test:artifact &amp;&amp; npm run test:e2e &amp;&amp; npm run test:coverage</automated>
    <automated>ASTRO_BASE=/ajs-website/ npm run build &amp;&amp; EXPECTED_BASE=/ajs-website/ npm run test:artifact</automated>
    <human-check>
      Open the homepage on a real phone (or device emulation at 393x852) and open the menu, then confirm against `.planning/phases/20-mobile-navigation-accent-color/20-mobile-menu-reference.png`:
      1. Only three big stacked links remain in the centre (Éditions / About / Contact) — the EN/FR switcher is no longer one of them.
      2. Near the bottom there are two small lines, the language switcher directly above the Instagram handle, at the same small size and the same black as each other.
      3. The Instagram line now shows the Instagram glyph beside the @handle, at the same size as the switcher's globe.
      4. The vertical spacing between those two small lines reads as a deliberate pair rather than two unrelated items — this is the one judgement call the automated geometry gate cannot make, so say if it wants tightening or opening up.
      5. Tapping the switcher still changes language, and tapping the Instagram line still opens Instagram in a new tab.
      6. Open and close motion, the hamburger-to-X morph and the corner halftone are unchanged from before this fix.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Every command in both `<automated>` chains exits 0, run in the listed order.
    - The un-prefixed-link guard produces no output against the `ASTRO_BASE=/ajs-website/` build.
    - `npm run test:e2e` passes under both `chromium` and `webkit-mobile`, with a total test count greater than or equal to the count recorded in `20-05-SUMMARY.md` plus the 5 tests added in Task 1 plus the 2 added here (one per locale path).
    - `npm run test:coverage` meets or exceeds the thresholds and the percentages recorded in `20-05-SUMMARY.md`.
    - `git diff --stat src/components/LanguageSwitcher.astro` is empty.
    - `grep -c 'EXPECTED_SCRIPT_COUNT = 4' tests/e2e/mobile-nav.spec.ts` returns 1, and the script-count net tests pass.
    - The SUMMARY maps each `missing` item from both 20-UAT.md gaps to the change that closed it and the assertion that now guards it.
  </acceptance_criteria>
  <done>The whole phase passes the CI blocking-gate sequence in its real order after the reversal, no other page or viewport regressed, and both UAT gaps have a documented closure plus a durable automated guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author markup → static HTML artifact | Every value in the changed markup is an author literal or a prop already threaded from `resolveSiteCopy()` / `getSwitcherHref`. This is a 100% static Astro build with zero request-time compute, so no untrusted input can cross into the rendered panel. |
| panel → third-party origin (instagram.com) | The secondary anchor navigates the visitor off-origin into a new browsing context. |
| panel (focus-trapped modal) → visitor's only locale control at phone width | At phone widths the header's inline switcher is hidden, so the in-panel switcher is the sole locale affordance, reachable only from inside a modal that traps focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06-01 | Tampering | the inline Instagram `<svg>` duplicated into `MobileNavPanel.astro` | low | accept | Author-literal SVG: no interpolated expression, no `set:html`, no CMS- or URL-sourced attribute, so a static build cannot render attacker content through it. Task 1's structural assertion (exactly one `svg` inside `.mobile-nav-panel__secondary`, `aria-hidden="true"`) detects accidental drift toward a dynamic source. |
| T-20-06-02 | Information Disclosure | `.mobile-nav-panel__secondary` outbound link to instagram.com | low | mitigate | `rel="noopener noreferrer"` stays paired with `target="_blank"` so the third-party tab gets no `window.opener` handle and no referrer. Task 2 explicitly forbids touching those attributes while inserting the glyph, and `mobile-nav.spec.ts` lines 227-231 assert them; Task 3's full sweep re-runs that assertion. |
| T-20-06-03 | Denial of Service | the relocated `.switcher-link` inside the focus-trapped `dialog#mobile-nav` | medium | mitigate | Deleting the old override's `padding: 0` restores `LanguageSwitcher.astro`'s own `padding: 8px` + `min-height: 44px`, keeping the WCAG 2.5.5 tap target in the new position. Task 1's geometry assertion requires both secondary lines to report `height` >= 44, and the existing focus-containment (spec lines 409-436) and switcher-navigates-and-sets-cookie (lines 516-527) tests must stay green — together these prevent a phone visitor being left inside the modal with an unhittable or unreachable locale control. |
| T-20-06-04 | Elevation of Privilege | the `ajs_locale` cookie written by `LanguageSwitcher.astro`'s own client script | low | accept | Out of scope by construction: this plan does not edit that file, so the cookie keeps its existing `SameSite=Lax; Secure` flags and its `import.meta.env.BASE_URL`-scoped `path` (which is what stops it leaking to sibling projects on the shared `github.io` staging domain). The script binds via `querySelectorAll`, so relocating the one panel instance neither adds a second writer nor changes the scope; Task 2's acceptance criteria require an empty diff on the file to prove it. |
| T-20-06-05 | Spoofing | the panel's accessible naming after the glyph insertion | low | mitigate | The new `<svg>` is `aria-hidden="true"` and the anchor keeps its explicit `aria-label` naming Instagram, so the link's accessible name is unchanged and cannot be silently emptied into an unlabelled control. Enforced by Task 1's `aria-hidden` attribute assertion and by `accessibility.spec.ts`'s open-panel axe scan in Task 2's verify chain. |

**Package Legitimacy Gate: not applicable.** This plan installs nothing — no `npm`/`pip`/`cargo` install task exists in it, and the Instagram glyph is duplicated inline precisely because this codebase has no icon dependency and no shared icon component. There is therefore no `T-20-06-SC` supply-chain row and no legitimacy checkpoint.
</threat_model>

<verification>
Phase-level checks after all three tasks:

1. `npm run typecheck` exits 0.
2. `npm run test:e2e` passes under both configured Playwright projects (`chromium` and `webkit-mobile`), with no test skipped, weakened or deleted — specifically including the four Phase 20 net describe blocks at `mobile-nav.spec.ts` lines 31-154 and the `EXPECTED_SCRIPT_COUNT = 4` tripwire.
3. `npm run test:coverage` meets or exceeds the thresholds and the percentages recorded in `20-05-SUMMARY.md`.
4. `npm run test:artifact` passes for both the root-base build and the `ASTRO_BASE=/ajs-website/` build, and the workflow's un-prefixed-link grep guard stays silent.
5. `tests/e2e/visual.spec.ts` passes with no snapshot baseline updated — proof that `/about/`'s shared header, and by extension every non-homepage page, did not shift.
6. `tests/e2e/accessibility.spec.ts` reports zero serious or critical axe violations for the open panel at 393x852 in both locales, with no rule exclusion added.
7. `git diff --stat src/components/LanguageSwitcher.astro` is empty — the D-06 accent-pink switcher is intact everywhere else on the site.
8. The end-of-phase `<human-check>` in Task 3 is answered against a real phone-width viewport, covering the reference image comparison that 20-UAT.md Test 2 failed on.
</verification>

<success_criteria>
1. 20-UAT.md gap 1 closed: the language switcher renders in the panel's bottom secondary tier at 14px / weight 400 / non-Unbounded / ink, as its own line stacked directly above the Instagram link, and `.mobile-nav-panel__nav` holds exactly the three primary links.
2. 20-UAT.md gap 2 closed: `.mobile-nav-panel__secondary` renders the header's Instagram glyph (16x16, `currentColor`, `aria-hidden`) beside the @handle, with its href/target/rel/aria-label contract unchanged.
3. Both closures are guarded by automated assertions that fail if the hierarchy drifts back — the rewritten Label-size/ink typography test, the DOM-position and icon-count structural test, and the two-stacked-rows geometry test.
4. ROADMAP Phase 20 success criteria 1, 2 and 4 still hold: the hamburger still replaces the desktop bar at phone width, the switcher is still reachable from inside the menu, and desktop/tablet plus every other page are byte-for-byte unchanged.
5. HOME-16 and every unchanged Phase 20 decision (D-01, D-02, D-03, D-05, D-06, and D-04's primary-tier clause) are demonstrably untouched.
6. Every threat in the register has a disposition, and the two `mitigate` rows point at named assertions that are green.
</success_criteria>

<output>
Create `.planning/phases/20-mobile-navigation-accent-color/20-06-SUMMARY.md` when done.

It must record: the RED failure list from Task 1 mapped to the two gaps; the exact rules deleted from and added to `SiteHeader.astro`; the confirmation that `LanguageSwitcher.astro` has an empty diff; the full CI-order command log with outcomes; final e2e counts per Playwright project, unit count and the four coverage percentages; the `<human-check>` answers, including the spacing judgement in item 4; and a gap-by-gap mapping from each 20-UAT.md `missing` item to the change that closed it and the assertion that now guards it.
</output>
