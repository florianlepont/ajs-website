---
phase: 10-unified-header-simplified-language-switcher
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage.spec.ts
  - tests/e2e/i18n.spec.ts
  - tests/e2e/site-header.spec.ts
autonomous: true
requirements: [HOME-10]
must_haves:
  truths:
    - "The homepage header renders from the same <SiteHeader> component as About/Contact (via HomeCarousel), not a parallel .home-header implementation — D-01/D-05."
    - "In carousel mode the homepage header looks transparent (white logo/text over the scrim); in grid mode it looks solid (ink logo/text, hairline) — driven by [data-display-mode] CSS re-skinning a fixed variant='transparent' render, never a prop change — D-02/Pattern 1."
    - "The carousel/grid mode-toggle renders inside SiteHeader's extra slot on the homepage only, still toggles modes, and does NOT appear on About/Contact — D-04."
    - "The .nav-link count and order match between / and /about/ (Logo, About, Contact, Instagram, switcher) — HOME-10 success criterion #1/#2."
  artifacts:
    - "src/components/HomeCarousel.astro rendering <SiteHeader> with a slot=extra toggle"
    - "tests/e2e/site-header.spec.ts cross-page structural-identity assertion"
  key_links:
    - "HomeCarousel passes variant='transparent' (fixed, rendered once) and the homepage's [data-display-mode='grid'] CSS re-skins .site-header--transparent to look solid (Pattern 1)."
    - "The toggle <button> is a direct child of <SiteHeader> carrying slot='extra' (named slots must be an immediate child — Pattern 2)."
---

<objective>
Make the homepage render its header from the shared `<SiteHeader>` (created in Plan 01) instead of HomeCarousel's parallel `.home-header` markup (D-01), passing the carousel/grid mode-toggle through the `extra` slot (D-04), and retire the `.home-header`/`.home-nav`/`.home-logo`/`[data-role="home-header"]` class+attribute set entirely in favour of the unified names (D-05). Grid-mode's solid look is achieved by re-scoping the existing `[data-display-mode]` CSS overrides onto `.site-header` (D-02, Pattern 1) — never by changing the fixed `variant="transparent"` prop. This completes HOME-10.

Purpose: Deliver the actual "one shared header everywhere" invariant — the homepage stops being a second, independently-styled header implementation.
Output: A rewired `HomeCarousel.astro`, the D-05 selector rename applied across its markup + CSS + the two Phase 7 e2e files (D-06), and a new cross-page structural-identity assertion proving `/` and `/about/` share the same header DOM.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-CONTEXT.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-RESEARCH.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-PATTERNS.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-UI-SPEC.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-01-SUMMARY.md
@src/components/SiteHeader.astro
@src/components/HomeCarousel.astro
</context>

<phase_goal>
**As a** bilingual visitor, **I want to** see the homepage's header match the header on every other page — same logo, nav, Instagram, and switcher, in the same positions — **so that** moving between the homepage and About/Contact feels like one coherent site, not two.
</phase_goal>

<mvp_slice_note>
This plan's user-facing slice: after it ships, the homepage's header IS the shared component — a header-level change made once (Plan 01's SiteHeader) is now reflected on the homepage too, and the mode-toggle still works. It builds directly on Plan 01's real, shipped SiteHeader; it is not scaffolding.
</mvp_slice_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — selector rename + cross-page structural-identity assertion (RED)</name>
  <files>tests/e2e/homepage.spec.ts, tests/e2e/i18n.spec.ts, tests/e2e/site-header.spec.ts</files>
  <read_first>
    - tests/e2e/homepage.spec.ts lines 90-104 (i18n non-regression guard), 423-432 (`.home-nav > a` DOM-order), 434-448 (`.home-nav` 393px scrollWidth), 456-464 (`.home-nav` sr-only), 550-605 and 613-623 (`[data-role="home-header"]` / `.home-nav` references).
    - tests/e2e/i18n.spec.ts lines 15-40 (`[data-role="home-header"]` references — note lines 17 and 26 also assert the two-code switcher text, which stays valid until Plan 03; do NOT touch those text assertions here).
    - tests/e2e/site-header.spec.ts (created in Plan 01) — where the new `/` vs `/about/` identity assertion will be appended.
    - 10-CONTEXT.md D-05/D-06 (rename map; `[data-role="mode-toggle"]` and `.home-toggle*` are EXCLUDED from the rename) and 10-UI-SPEC.md "Class/attribute rename map".
  </read_first>
  <behavior>
    - Every homepage.spec.ts and i18n.spec.ts locator that currently targets `[data-role="home-header"]` resolves to `[data-role="site-header"]`; every `.home-nav` / `.home-nav-link` / `.home-logo*` locator resolves to `.site-nav` / `.nav-link` / `.logo-mark*`.
    - `[data-role="mode-toggle"]`, `.home-toggle`, and `.home-toggle__box` locators are left unchanged (D-05 excludes them).
    - A new assertion in site-header.spec.ts proves the header is the SAME component on `/` and `/about/`: the ordered list of `.site-nav .nav-link` hrefs/structure on `/` matches the About+Contact+Instagram set on `/about/` (same count and order), i.e. not two divergent implementations.
  </behavior>
  <action>
    Apply the D-05/D-06 selector rename across the two Phase 7 test files: in homepage.spec.ts and i18n.spec.ts, replace `[data-role="home-header"]` → `[data-role="site-header"]`, `.home-nav` → `.site-nav`, `.home-nav-link` → `.nav-link`, and any `.home-logo*` → `.logo-mark*`. Leave `[data-role="mode-toggle"]`, `.home-toggle`, and `.home-toggle__box` untouched (D-05 explicitly excludes the mode-toggle). In i18n.spec.ts, rename the selector on lines 15/24/31/35 but DO NOT alter the two-language-code switcher text assertions on lines 17/26 — those remain valid until Plan 03 simplifies the switcher, and changing them here would be premature. Then append a cross-page structural-identity test to tests/e2e/site-header.spec.ts: load `/` and `/about/`, read the ordered `.site-nav .nav-link` set on each, and assert the homepage's nav exposes the same About + Contact + Instagram links in the same order as About's (proving one shared component, the gap 10-RESEARCH.md flags as untested). This new identity assertion and the renamed homepage selectors are expected to FAIL RED now because the homepage still renders `.home-header`/`.home-nav` and has no `[data-role="site-header"]` — do not weaken them.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/homepage.spec.ts tests/e2e/i18n.spec.ts tests/e2e/site-header.spec.ts --project=chromium 2>&1 | grep -qE "failed|error" && echo "RED as expected" || (echo "ERROR: renamed selectors should be RED before implementation" && exit 1)</automated>
  </verify>
  <done>The renamed unified selectors and the new /-vs-/about/ identity test are RED against the homepage's still-old .home-header markup.</done>
  <acceptance_criteria>
    homepage.spec.ts and i18n.spec.ts reference only the unified selectors (no `[data-role="home-header"]`, `.home-nav`, `.home-nav-link`, or `.home-logo` remain except the excluded `.home-toggle*`); the two-code switcher text assertions in i18n.spec.ts are unchanged; site-header.spec.ts has a `/`-vs-`/about/` identity test; the suite is RED on the homepage's still-old markup.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Render SiteHeader from HomeCarousel with the toggle in the extra slot; apply the D-05 rename to markup + CSS</name>
  <files>src/components/HomeCarousel.astro</files>
  <read_first>
    - src/components/HomeCarousel.astro lines 99-162 (the `.home-header` markup to replace, incl. the `.home-toggle` button to move into the slot), lines 700-1029 (header CSS: base `.home-header`/`.home-nav`/`.home-logo` rules, the `[data-display-mode]` overrides, the switcher-link color overrides at 860-867, and the `.home-toggle*` rules), and lines 1524-1571 (mobile `@media` block).
    - src/components/SiteHeader.astro (Plan 01) — its Props contract and that its `is:global` stylesheet now OWNS the logo-hover-crossfade + base `.site-header`/`.site-nav`/`.nav-link` + ported mobile CSS; HomeCarousel must NOT duplicate those, only layer its `[data-display-mode]` deltas on top.
    - 10-PATTERNS.md "src/components/HomeCarousel.astro (MODIFY)" — the exact change pattern, the CSS override re-scoping example, and the delete list; 10-RESEARCH.md Pattern 1 (fixed prop, re-skin via attribute selector) and Pattern 2 (named slot immediate-child rule).
  </read_first>
  <action>
    Replace HomeCarousel's inline `<header class="home-header" data-role="home-header">...</header>` (lines 99-162) with a `<SiteHeader variant="transparent" ...>` call (import SiteHeader from './SiteHeader.astro'), passing `siteTitle={wordmark}`, `homeHref`, `aboutLabel`, `aboutHref`, `contactLabel`, `contactHref`, `instagramUrl={siteCopy.instagramUrl}`, `instagramLabel={siteCopy.instagramLabel}`, `instagramNewTabHint`, `logoBlackSrc`, `logoWhiteSrc`. `variant="transparent"` is FIXED and rendered once, matching the initial carousel state (D-02, Pattern 1) — it is never swapped by JS. Move the existing mode-toggle `<button>` (lines 141-160, keeping its `data-role="mode-toggle"`, `data-action`, `data-label-*`, `aria-label`, and its `.home-toggle`/`.home-toggle__box`/`.home-toggle__morph*` inner markup verbatim) to be a DIRECT child of `<SiteHeader>` carrying `slot="extra"` (D-04, Pattern 2 — a named-slot child must be an immediate child, not nested in a wrapper). Remove HomeCarousel's now-unused direct `import LanguageSwitcher` and its inline `<LanguageSwitcher />` (SiteHeader renders it now) and remove the inline Instagram nav-link markup (SiteHeader owns it now, D-03). In HomeCarousel's `<style>`, apply the D-05 rename to every header selector across lines 700-1029 and 1547-1571: `.home-header` → `.site-header`, `.home-nav` → `.site-nav`, `.home-nav-link` → `.nav-link`, `.home-logo*` → `.logo-mark*`; keep `.home-toggle`, `.home-toggle__box`, `.home-toggle__morph*`, and `[data-role="mode-toggle"]` unchanged (D-05 exclusion). DELETE from HomeCarousel the base header/nav/logo rules that now live in SiteHeader's `is:global` stylesheet — the full logo hover-crossfade block (lines 769-845), the base `.home-header` layout rule (700-706, minus its `view-transition-name`/`z-index` — see next sentence), the base `.home-nav` (847-852), and the shared `.home-nav-link, .home-toggle` typographic rule's nav-link half — keeping ONLY the homepage-specific deltas: the `[data-display-mode='carousel'|'grid'] .site-header` position/background/color re-skins (re-scoped from `.home-header` to `.site-header--transparent` per Pattern 1), the `[data-display-mode]` logo default/hover swap deltas re-scoped onto `.logo-mark`, the switcher-link color overrides (lines 860-867, re-scoped to `.site-header`), the toggle rules, and the homepage-specific mobile trims. PRESERVE the header's `view-transition-name: ajs-header` + `z-index: 5` (lines 706-726) by re-declaring them on `.site-header` within HomeCarousel's own stylesheet (or on the `.home[data-display-mode] .site-header` override) — this is a homepage-only morph concern, not part of the shared component. Re-measure the mobile pixel budget live at 393px per Pitfall 2 (the switcher width is unchanged in this plan — it shrinks in Plan 03 — so the homepage's existing 5-item budget still applies): confirm the row still fits after the markup move; adjust the homepage-scoped mobile trims only if a live `boundingBox()` measurement shows overflow.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/homepage.spec.ts tests/e2e/i18n.spec.ts tests/e2e/site-header.spec.ts --project=chromium</automated>
    <automated>npm run test:e2e && npm run test:unit</automated>
    <human-check>Load `/` in both locales, toggle carousel↔grid, and confirm the header re-skins correctly: carousel mode = white logo/nav over the photo scrim; grid mode = ink logo/nav on white with the hairline; the logo hover-crossfade works in both modes; the mode-toggle sits between the Instagram link and the switcher and still animates its morph.</human-check>
  </verify>
  <done>The homepage renders <SiteHeader variant="transparent"> with the toggle in the extra slot; .home-* header selectors are retired; full test:e2e + test:unit suite GREEN; build succeeds.</done>
  <acceptance_criteria>
    The homepage renders `<SiteHeader variant="transparent">` with `[data-role="site-header"]`; `.home-header`/`.home-nav`/`.home-nav-link`/`.home-logo` and `[data-role="home-header"]` no longer appear in HomeCarousel or any test; the mode-toggle renders via the `extra` slot on the homepage only (absent on About/Contact); carousel/grid visual swap works via `[data-display-mode]` CSS with a fixed `variant="transparent"`; homepage.spec.ts, i18n.spec.ts, site-header.spec.ts are GREEN and the full `npm run test:e2e && npm run test:unit` suite passes. `npm run build` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time render → static HTML → browser | The homepage's header hrefs/labels remain build-time `siteCopy` values; the mode-toggle is a plain client `<button>` whose handler already exists unchanged. No new user-controlled input crosses into the header. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | HomeCarousel `[data-display-mode]` client re-skin | low | accept | The carousel/grid swap continues to mutate a single `data-display-mode` attribute read by CSS (Pattern 1) — no new script, no DOM injection, no eval; the same already-shipped mechanism, only its CSS selector target changes. |
| T-10-04 | Information Disclosure | HomeCarousel → SiteHeader render | low | accept | HomeCarousel still never imports `src/lib/sanity.ts`; passing labels/hrefs into `<SiteHeader>` as props introduces no new data path to the client. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (10-RESEARCH.md Package Legitimacy Audit: zero new dependencies). |

No high-severity threats (security_block_on=high) — nothing blocks.
</threat_model>

<artifacts>
## Artifacts this plan produces
- **Modified:** `src/components/HomeCarousel.astro` — now renders `<SiteHeader variant="transparent">` with the mode-toggle in `slot="extra"`; no longer imports/renders `LanguageSwitcher` directly; no longer contains inline Instagram markup or duplicated logo/nav/header CSS.
- **Retired symbols:** `.home-header`, `.home-nav`, `.home-nav-link`, `.home-logo`, `.home-logo__chip`, `.home-logo__img(--default|--hover)`, `[data-role="home-header"]` (removed from source and all tests).
- **Preserved on `.site-header` (homepage-scoped):** `view-transition-name: ajs-header`, `z-index: 5`, and the `[data-display-mode]` carousel/grid re-skin + switcher-link color overrides.
- **New test:** cross-page structural-identity assertion in `tests/e2e/site-header.spec.ts` (`/` vs `/about/`).
- **Unchanged/excluded:** `[data-role="mode-toggle"]`, `.home-toggle`, `.home-toggle__box`, `.home-toggle__morph*`.
</artifacts>

<verification>
- `npx playwright test tests/e2e/homepage.spec.ts tests/e2e/i18n.spec.ts tests/e2e/site-header.spec.ts --project=chromium` — GREEN.
- `npm run test:e2e && npm run test:unit` — full suite GREEN (per-wave gate).
- `npm run build` — succeeds.
- Live: homepage header re-skins correctly carousel↔grid in both locales; toggle + logo hover work.
</verification>

<success_criteria>
- The homepage header is rendered from `<SiteHeader>` (HOME-10 success criterion #1) — one shared component, verified identical to About's nav by the new e2e assertion (#2).
- The mode-toggle renders on the homepage only, via the `extra` slot, and still functions (#3).
- The `.home-*` header class/attribute set is fully retired (D-05); the Phase 7 tests reference the unified selectors (D-06).
- Carousel/grid visual swap works with a fixed `variant="transparent"` re-skinned by `[data-display-mode]` CSS (D-02, Pattern 1) — no prop mutation.
</success_criteria>

<output>
Create `.planning/phases/10-unified-header-simplified-language-switcher/10-02-SUMMARY.md` when done.
</output>
