---
phase: 10-unified-header-simplified-language-switcher
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/galleries/[slug].astro
  - src/pages/en/galleries/[slug].astro
autonomous: true
gap_closure: true
requirements: [HOME-10]
must_haves:
  truths:
    - "On both /galleries/{slug}/ and /en/galleries/{slug}/, no text link overlaps the SiteHeader logo in the top-left corner of the hero."
    - "The SiteHeader logo remains the sole 'return home' affordance on gallery-detail pages and still navigates to the site root."
    - "The site builds cleanly — no unused-import or unused-variable errors from the blocking astro-check / ESLint CI gates."
  artifacts:
    - "src/pages/galleries/[slug].astro (duplicate back-link markup + CSS + dead import/const removed)"
    - "src/pages/en/galleries/[slug].astro (duplicate back-link markup + CSS + dead import/const removed)"
  key_links:
    - "SiteHeader logo href -> site root (shipped in 10-01/10-02) provides the home navigation the removed link duplicated"
---

<objective>
Close the single Phase 10 UAT gap (Test 5, major): on gallery-detail pages a legacy page-local "← Back home" / "← Retour à l'accueil" link visually overlaps the shared SiteHeader logo. The link is a functional duplicate — the SiteHeader logo added in Plans 10-01/10-02 already navigates to the site root — so it is removed entirely, per the user's UAT instruction ("this one can be removed as the click on logo has the same behavior").

Root cause (from `.planning/debug/header-backhome-overlap-logo.md`): both gallery-detail templates render `.gallery-detail__hero-back`, absolutely positioned at `top/left: var(--space-xl)`, `z-index: 3`, relative to a hero box that starts at page y=0 because the transparent SiteHeader variant is `position: absolute` (removed from flow). This lands the link at nearly the same top-left coordinates as the logo and paints its text over the logo graphic.

Purpose: Restore the "header fits on one row, no overlap" behavior HOME-10 promised, using the SiteHeader logo as the single home-navigation affordance.
Output: Both gallery-detail locale templates with the duplicate link, its dead CSS, and the now-unused import/const removed; a clean build.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The full diagnosis for this gap (root cause + exact artifacts/lines)
@.planning/debug/header-backhome-overlap-logo.md

# The two mirror templates being edited (identical structure, only locale key + import depth differ)
@src/pages/galleries/[slug].astro
@src/pages/en/galleries/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the duplicate gallery-detail back-home link from both locale templates</name>
  <files>src/pages/galleries/[slug].astro, src/pages/en/galleries/[slug].astro</files>
  <action>
    Apply the SAME removal to both mirror files (FR: src/pages/galleries/[slug].astro, EN: src/pages/en/galleries/[slug].astro). The two files are byte-for-byte identical in the affected regions except for the visible link text ("← Retour à l'accueil" vs "← Back home") and import depth.

    In each file, delete every trace of the duplicate back-home affordance:

    1. Remove the anchor element carrying class `gallery-detail__hero-back` from inside the `.gallery-detail__hero` block (FR ~line 80, EN ~line 77). This is the link whose text overlaps the logo; the SiteHeader logo (10-01/10-02) already links to the same destination.

    2. Remove BOTH CSS rule blocks that target that class from the `<style>` block: the base positioning rule (FR ~lines 143-153, EN ~lines 140-150) and the `:hover, :focus-visible` rule that follows it (FR ~lines 155-158, EN ~lines 152-155). Leave every other `.gallery-detail__hero*` rule (scrim, img, title, content) untouched.

    3. Remove the now-unused `backHref` constant in the frontmatter (`const backHref = getRelativeLocaleUrl(locale, '');` — FR ~line 35, EN ~line 32). It has no remaining consumer once the link is gone.

    4. Remove the now-unused `getRelativeLocaleUrl` import from `'astro:i18n'` (FR ~line 9, EN ~line 6). Confirmed sole usage was `backHref`; leaving the dead import would fail the BLOCKING `astro check` type-check step and ESLint gate added to CI in quick task 260720-dzi.

    5. Update the stale prose in the `.gallery-detail__hero` CSS comment so it no longer describes a "white top-left back-link" — that element no longer exists. Keep the rest of the comment (full-bleed hero photo + scrim + white bottom-left Display title under the transparent header).

    Do NOT alter the hero image, scrim, title, thumbnails, Lightbox, `BaseLayout` props, or `headerVariant="transparent"`. This is a pure removal of one duplicated element and its dead code — no styling of remaining elements changes.

    Reference: the exact lines and CSS are catalogued in the debug session artifacts list. The SiteHeader logo home-navigation this replaces is described in 10-01-SUMMARY.md / 10-02-SUMMARY.md.

    <!-- planner-discipline-allow: hero-back -->
    <!-- planner-discipline-allow: backHref -->
    <!-- planner-discipline-allow: getRelativeLocaleUrl -->
  </action>
  <verify>
    <automated>grep -rn "hero-back\|backHref" "src/pages/galleries/[slug].astro" "src/pages/en/galleries/[slug].astro" ; test -z "$(grep -rn "hero-back\|backHref" "src/pages/galleries/[slug].astro" "src/pages/en/galleries/[slug].astro")" && npm run build</automated>
    <human-check>Open a gallery-detail page (e.g. /galleries/{slug}/ and its /en/ counterpart) at desktop and ~390px mobile widths; confirm no text link overlaps the logo, and clicking the logo still returns to the homepage.</human-check>
  </verify>
  <done>Neither template contains the `hero-back` class, the `backHref` const, or the `getRelativeLocaleUrl` import; `npm run build` succeeds with no unused-import/variable error; the header top-left shows only the SiteHeader logo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Static, build-time-only change. No new runtime input, endpoint, or data flow is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-01 | Denial of Service | gallery-detail build (dead import left behind) | low | mitigate | Removing the unused `getRelativeLocaleUrl` import/`backHref` const keeps the blocking `astro check` + ESLint CI gates green so a broken build cannot ship. |
| T-10-04-02 | Information Disclosure | removed markup/CSS | low | accept | Removal-only diff on two static templates; no data path, no secret, no auth surface touched. No package installs in this plan (no supply-chain vector). |
</threat_model>

<verification>
- `grep -rn "hero-back\|backHref\|getRelativeLocaleUrl" src/pages/galleries/[slug].astro src/pages/en/galleries/[slug].astro` returns no matches in either file.
- `npm run build` succeeds (proves no dead import/variable error under the blocking type-check + lint gates).
- `npm run test:e2e` stays green (no test referenced the removed link, confirmed at plan time — this guards against unintended regression).
- Human re-UAT: on both gallery-detail locales, no link overlaps the logo at desktop and ~390px; the logo alone returns home.
</verification>

<success_criteria>
- The `.gallery-detail__hero-back` link, its two CSS rule blocks, the `backHref` const, and the `getRelativeLocaleUrl` import are absent from both gallery-detail templates.
- The build passes cleanly; e2e suite remains green.
- Gallery-detail pages present the SiteHeader logo as the single, non-overlapping home-navigation affordance (UAT Test 5 now passes).
</success_criteria>

<output>
Create `.planning/phases/10-unified-header-simplified-language-switcher/10-04-SUMMARY.md` when done.
</output>
