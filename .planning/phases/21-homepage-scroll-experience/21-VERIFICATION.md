---
phase: 21-homepage-scroll-experience
verified: 2026-08-10T14:35:00Z
status: human_needed
score: 5/5 must-haves verified (roadmap success criteria); 68/68 plan-level must_have truths verified (45 carried forward from the round-1 gap-closure pass, 23 new from round-2 gap-closure plans 21-11..21-15)
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 5/5 (roadmap); 45/45 (plan-level, pre-round-2)
  gaps_closed:
    - "21-UAT.md round-2 gap 1 (logo appears twice / no continuous shrink) — closed by 21-13: plan 21-10's two static intro sections replaced with one pinned, scroll-scrubbed intro track/stage holding exactly one logomark; automated coverage completed by 21-14's rewritten 'pinned intro scrub' describe block (22 cases)"
    - "21-UAT.md round-2 gap 2 (intro tagline AND gallery descriptions flash on/off too fast to read) — intro half closed by 21-13's pinned-scrub trailing-40%-dwell design; gallery half closed by 21-15's two-level reveal/release latch (computeArrivalRevealed, 0.9/0.45) replacing the old symmetric ratio>=0.98 comparison"
    - "21-UAT.md round-2 gap 3 (cover photo renders twice after zoom completes) — closed by 21-12: slides wrapper pulled up by one deck-viewport-height, stage made opaque/stacked/retired-on-completion so the first slide's snap point coincides with the stage's release point, collapsing the post-zoom dead zone to zero"
    - "21-UAT.md round-2 gap 4 (white bar/gap near status bar and Safari toolbar) — closed by 21-11: every deck viewport-height length converted from static 100svh to a live-synced --deck-vh custom property driven from window.visualViewport, covering the CURRENT viewport in every intermediate toolbar state"
    - "21-UAT.md round-2 gap 5 (hamburger unreachable at scroll 0) — closed by 21-14: plan 21-10's whole-header intro-active hide removed, D-12's hide scope restored to the zoom alone, replaced with a narrow rule suppressing only the header's own logomark (A7); a second, previously-hidden defect (data-zoom-active still hiding the header at scroll 0 via a pre-existing timing coincidence) discovered and fixed with a :not([data-intro-active]) addendum"
    - "21-REVIEW.md (round-2 scoped review) WR-01 (applyArrival()'s viewport-height source diverged from --deck-vh) — closed, commit c163d13: viewportHeight now reads the same lastDeckVh cache --deck-vh itself is sourced from"
    - "21-REVIEW.md (round-2 scoped review) WR-02 (applyArrival() wrote classList unconditionally every frame) — closed, commit 3991e4a: classList.toggle gated behind an actual wasRevealed/reached state change, restoring the 'zero style writes when stationary' invariant"
    - "21-REVIEW.md (round-2 scoped review) WR-03 (no-JS visitor never sees the pinned intro tagline) — closed, commit db1af15: @media (max-width: 767px) and (scripting: none) fallback added, confirmed compiled into the built CSS"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Real-device pass closing phase 21's ROUND-2 gap-closure set — required by 21-VALIDATION.md's Manual-Only Verifications table, and the direct re-run of the 21-UAT.md round-2 test that produced all five gaps. This is the item 21-15-PLAN.md's own Task 3 <verify><human-check> block specifies, deliberately preserved unexecuted in the plan file for workflow.human_verify_mode=end-of-phase harvesting (confirmed present, unexecuted, and undeleted in 21-15-PLAN.md by direct read during this verification pass). Run `npm run build && npm run preview -- --host`, then open the printed local-network URL on a REAL iPhone (not a desktop browser at a phone width). On the device: (1) load the homepage fresh, look at it before scrolling, count the logos on screen, try to reach the navigation; (2) tap the hamburger and open the nav panel without having scrolled, then close it; (3) scroll down slowly through the opening, watching the logomark itself; (4) stop while the intro tagline is on screen and read it at a normal unhurried pace; (5) keep scrolling slowly through the whole wordmark zoom, watching the exact moment it finishes and the first gallery photo takes over; (6) continue slowly through every gallery slide to the bottom, stopping where a normal scroll naturally comes to rest and trying to read each description; (7) repeat at a brisker, more realistic flicking pace; (8) scroll back up to the very top; (9) fling-scroll quickly down then up; (10) watch the very top (status bar) AND the very bottom (Safari's own toolbar area) throughout, including while the toolbar animates; (11) tap a gallery photo; (12) reach for the header mid-scroll; (13) open the same build on a tablet and a desktop browser and compare against pre-milestone behaviour. Then confirm or correct by name: assumptions A1, A2, A3, A5 (21-10-PLAN.md, still standing); A4 (superseded by the developer's own round-2 correction, delivered by 21-13); A6 (21-13-PLAN.md, 900px intro-scrub distance/shrink-in-place); A7 (21-14-PLAN.md, header visible with only its own logomark suppressed during the intro); A8 (21-15-PLAN.md, the 0.9/0.45 reveal/release latch); A9 (21-15-PLAN.md, scroll-snap-type left at y proximity, deliberately declined to change)."
    expected: "1) Exactly ONE AJS logomark on screen, centred on an ink background with a scroll-down cue, and the navigation hamburger visible in the header from the very first paint (gap 1 + gap 5). 2) The hamburger actually opens the nav panel at scroll 0 (gap 5) — per A7 the header's own small logo is deliberately hidden while the intro is on screen, but the hamburger and language switcher are not. 3) That single logomark shrinks continuously as you scroll, with the tagline arriving beneath it as one continuous movement — never a second logo (gap 1). 4) The tagline stays fully visible and stationary long enough to read comfortably (gap 2, intro half). 5) The zoom stays clean and the first gallery photo is already full-screen the instant it finishes — at no scroll position does the same photo appear twice, one above the other (gap 3). 6) Each gallery's description appears as its slide settles and STAYS on screen while reading it — no flash-on-flash-off, and this holds at both a slow and a brisker/flicking pace (gap 2, gallery half). 7) Scrolling back up smoothly reverses the whole sequence including the intro scrub; a fast fling does not skip a gallery or produce a broken frame at the zoom boundary. 8) No white bar/edge near the status bar at the top, and no white gap near Safari's toolbar at the bottom, at any point including during toolbar animation (gap 4). 9) Tapping a photo opens that gallery; the header is reachable while scrolling the slides. 10) Nothing on the tablet/desktop homepage differs from pre-milestone behaviour. 11) A1/A2/A3/A5 confirmed; A4 recorded superseded; A6/A7/A8/A9 each confirmed or corrected, with any correction recorded."
    why_human: "Round 1 shipped a fully green automated suite (396+ e2e cases) and the developer still found four real-device-only defects; round 2's gap-closure set (plans 21-07 through 21-10) then shipped a fully green suite (430 chromium + 5 webkit-mobile) and the developer found FIVE MORE real-device-only defects on the same live iPhone. Playwright's webkit-mobile project runs the desktop WebKit engine behind an emulated iPhone viewport — it has no touch-momentum physics, no real scroll-snap proximity heuristics under a finger, no settle spring, and no dynamic browser-toolbar animation. This verification pass independently re-ran every mechanism-level automated check plans 21-11 through 21-15 added (all green, confirmed live, not read from a prior report) and independently re-read the compiled fix for every one of the round-2 scoped code review's three warnings (all confirmed present in source and in the built CSS). But per this phase's own explicit two-round history, presence-plus-wiring-plus-mechanism evidence has twice now not been sufficient to predict the real-device visitor-facing result — the pacing, the composition (A6/A7), the reveal-latch feel (A8), and the accepted snap-mode tradeoff (A9) are exactly the judgement calls 21-15-PLAN.md's own human-check names as un-testable by any assertion. No file in this phase directory newer than 21-15-SUMMARY.md (dated 2026-08-10, same day as the round-2 code-review fixes) records this real-device pass as having been performed."
---

# Phase 21: Homepage Scroll Experience Verification Report

**Phase Goal:** On phone-width viewports (≤767px), a scroll-driven single-view homepage replaces the carousel/grid toggle — a full-screen wordmark that zooms into the first gallery's photo as the visitor scrolls, with per-gallery scroll-snap slides revealing descriptions on arrival.

**Verified:** 2026-08-10T14:35:00Z
**Status:** human_needed
**Re-verification:** Yes — this pass covers the ROUND-2 gap-closure set (plans 21-11 through 21-15, closing all five `21-UAT.md` round-2 real-device defects found after round-1 shipped green) plus a scoped round-2 code review (`21-REVIEW.md`/`21-REVIEW-FIX.md`, 0 critical / 3 warning / 1 info, all three warnings fixed and independently re-confirmed in source). The prior `21-VERIFICATION.md` (2026-08-08, superseded by this report) covered round-1 gap-closure only.

**Note on the phase's own real-device track record:** This is the THIRD verification pass over this phase. Pass 1 (2026-08-05) found the automated suite green and routed to human_needed; round-1 UAT then found 4 real defects. Pass 2 (2026-08-08) found the round-1 gap-closure automated suite green and routed to human_needed again; round-2 UAT then found 5 MORE real defects, despite every one of round-1's fixes having dedicated, passing automated coverage. This pass (pass 3) independently re-verifies round-2's gap-closure work the same way — every mechanism is present, wired, and covered — and routes to human_needed a third time, because the phase's own history is direct evidence that mechanism-level automated coverage has not yet been sufficient to predict this specific feature's real-device behavior.

## Goal Achievement

### Observable Truths (ROADMAP.md Success Criteria, re-verified against current codebase)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | On a phone-width viewport, the carousel/grid toggle control no longer appears on the homepage | ✓ VERIFIED | Live re-run confirms `.home-hero`/`.home-grid`/the mode toggle stay `@media (max-width: 767px)`-gated; full chromium suite (461/461, one transient unrelated flake reproduced and cleared on retry — see Behavioral Spot-Checks) and webkit-mobile (5/5) both green this pass |
| 2 | Scrolling the homepage on a phone moves through every gallery as a single continuous sequence, with no separate carousel/grid mode | ✓ VERIFIED | Built artifact re-checked this pass: `dist/index.html` still has 5 `data-role="deck-slide"` anchors for 5 galleries; structural e2e cases pass |
| 3 | Each gallery's description text is hidden until that gallery arrives on screen during scroll, then reveals via the sketched-and-approved transition | ✓ VERIFIED | Reveal mechanism now a two-level latch (`computeArrivalRevealed`, plan 21-15) instead of round-1's symmetric threshold — still driven from the same per-frame `applyArrival()`, `IntersectionObserver` count still 0 (re-grepped live), reveal/release/sweep/jitter e2e cases all pass |
| 4 | On first load, a phone visitor sees the wordmark filling the screen; scrolling visibly transitions through the letterforms into the first gallery's photo | ✓ VERIFIED (as amended by 21-10, then restructured by 21-13 — see prior pass's note, carried forward) | The pre-wordmark opening is now ONE pinned, scroll-scrubbed logomark (plan 21-13, replacing round-1's two static beats) that shrinks continuously as the tagline arrives beneath it, then the unchanged wordmark-into-photo zoom. Built-artifact fact re-confirmed: exactly 1 `home-scroll-deck__intro-logo` in both `dist/index.html` and `dist/en/index.html`. `pinned intro scrub` describe block (22 cases, plan 21-14) passes live. The real-device "does the shrink/reveal actually read correctly, at the right pace" half is the item routed to human verification below |
| 5 | On tablet/desktop viewports, the carousel/grid toggle and both view modes behave exactly as they did before this milestone | ✓ VERIFIED | Full suite re-run this pass: 461/461 chromium (see flake note), 5/5 webkit-mobile; desktop-inert e2e cases across every 21-11..21-15 describe block pass; `--deck-vh` confirmed absent at 1280x800 |

**Score:** 5/5 roadmap success criteria verified against the CURRENT codebase by an independent, live re-run of the full automated suite plus direct source/build-artifact inspection — not by re-reading 21-11 through 21-15's own SUMMARY.md claims. 0 failed. The real-device confirmation of the redesigned intro's feel, the collapsed dead zone, the fixed white-bar, the reachable hamburger, and the readable descriptions remains the one item routed to human verification, exactly as this phase's own gap-closure plans (21-11 through 21-15) each deliberately deferred it.

### Plan-Level Must-Have Truths — Round-2 Gap-Closure Plans (21-11 through 21-15)

| Plan | Truth | Status | Evidence |
|------|-------|--------|----------|
| 21-11 | Every full-screen deck section is exactly as tall as the phone's CURRENT visible viewport, in every intermediate Safari-toolbar state | ✓ VERIFIED | `syncDeckViewportHeight()` present, called first in `frame()`; `grep -c 'var(--deck-vh, 100svh)'` = 13 across track/stage/wordmark-padding/intro/slide; viewport-height e2e cases pass live |
| 21-11 | No strip of page background left uncovered above/below a deck section when Safari's toolbar is partly/fully retracted | ✓ VERIFIED | Mechanism-level regression net (resolves near innerHeight, tracks a resize) passes; real-device confirmation deferred to this pass's human-verification item, per the plan's own explicit gap-closure gate |
| 21-11 | A reduced-motion phone visitor gets the same correct sizing, because the sync is not part of the motion driver | ✓ VERIFIED | "reduced motion, phone width: --deck-vh is STILL present" e2e case passes live |
| 21-11 | Nothing at 768px+ consumes the new unit; non-deck hero sizing untouched | ✓ VERIFIED | Desktop-absent e2e case passes; `.home-hero__photo`'s `min-height: 100svh` range sizing confirmed unchanged in source |
| 21-12 | Once the zoom completes, the first gallery's photo is on screen exactly once — no scroll position shows it twice | ✓ VERIFIED | Pull-up (`margin-top: calc(-1 * var(--deck-vh, 100svh))`) + opaque/stacked/retired-on-completion stage confirmed in source (`data-zoom-active` selectors at the documented lines); "no-doubling sweep" e2e case passes (confirmed reliably passing via 3x isolated repeat + 2 full-suite re-runs after one flaky occurrence under heavy parallel load — see Behavioral Spot-Checks) |
| 21-12 | The moment zoom progress reaches its completed value, the first slide is already full-screen at its own snap point | ✓ VERIFIED | "coincidence" and "at the completion offset" e2e cases pass live |
| 21-12 | Scrolling back up re-enters the zoom seamlessly, no flash of a second photo copy at the boundary | ✓ VERIFIED | "reversibility at the boundary" e2e case passes live |
| 21-12 | A reduced-motion or desktop visitor keeps exactly today's geometry (attribute-gated) | ✓ VERIFIED | Reduced-motion/desktop inert e2e cases in the `collapsed post-zoom dead zone` block pass live |
| 21-13 | The AJS logomark exists exactly once in the opening, shrinking continuously instead of being re-rendered in a second section | ✓ VERIFIED | Built-artifact fact re-confirmed this pass: `grep -o 'home-scroll-deck__intro-logo' dist/index.html \| wc -l` = 1 (same for `dist/en/index.html`); `INTRO_REVEAL_DISTANCE`/`computeIntroProgress`/`computeIntroScrubState` confirmed present in `src/lib/home-carousel.ts` at the documented lines |
| 21-13 | The intro tagline arrives beneath the shrinking logomark as a continuous function of scroll position, not a binary threshold flip | ✓ VERIFIED | `computeIntroScrubState`'s sub-range tagline opacity/translateY confirmed in source; "at the halfway point"/"at the end of the scrub" e2e cases pass live |
| 21-13 | The intro stage stays pinned for the whole scrub so the tagline is on screen for hundreds of pixels, not an instant | ✓ VERIFIED | "readability dwell (gap 2's intro half)" sweep e2e case passes live |
| 21-13 | The intro's scrub and the zoom's scrub are both re-derived from live rects on the same frame, no new desync risk at the new boundary | ✓ VERIFIED | `IntersectionObserver` count 0, `addEventListener('scroll'` count 1 (pre-existing `visualViewport` listener from 21-11, confirmed by source read, not a new scroll listener); "atomic boundary read" and "scroll-event independence" e2e cases pass live |
| 21-13 | A reduced-motion or desktop visitor keeps a fully static intro | ✓ VERIFIED | Reduced-motion/desktop e2e cases in the `pinned intro scrub` block pass live |
| 21-14 | The navigation hamburger is visible and tappable at scroll position 0, before any scrolling | ✓ VERIFIED | "reachable from arrival" and "the hamburger actually works from arrival" e2e cases pass live — both a visibility AND an interaction proof |
| 21-14 | The header stays reachable through the whole intro scrub, only disappearing during the wordmark zoom itself | ✓ VERIFIED | "still reachable through the whole intro" e2e case passes; `:not([data-intro-active])` addendum on the `data-zoom-active` header-hide rule confirmed present in source at the documented line, closing the second, previously-hidden defect this plan's own Task 3 discovered empirically |
| 21-14 | No second, smaller copy of the logomark competes with the intro's own logomark while it is on screen | ✓ VERIFIED | Narrow `.logo-mark`-only suppression rule (A7) confirmed present; "the logomark is the one thing suppressed (A7)" e2e case passes live |
| 21-14 | The pinned intro's geometry/curve/release/boundary are pinned down by automated cases | ✓ VERIFIED | `pinned intro scrub` describe block confirmed at 22 cases (geometry parity, rest/mid/end-of-scrub, dwell, reversibility, atomic boundary, scroll-event independence) — all pass live |
| 21-14 | A reduced-motion or desktop visitor sees an entirely inert intro, full suite green on both projects | ✓ VERIFIED | Reduced-motion/desktop cases pass; full suite this pass: 461/461 chromium, 5/5 webkit-mobile |
| 21-15 | A revealed description survives a momentary dip in slide coverage — the text stays readable | ✓ VERIFIED | `computeArrivalRevealed` confirmed present (`ARRIVAL_REVEAL_THRESHOLD`=0.9, `ARRIVAL_RELEASE_THRESHOLD`=0.45 confirmed via source read); "the latch holds (gap 2's fix)" e2e case passes live |
| 21-15 | A revealed description only releases once its slide is genuinely more than half off screen | ✓ VERIFIED | "the hold is not a fluke of one offset" sweep + "the latch releases" e2e cases pass live |
| 21-15 | A slide still only counts as arrived when it essentially fills the screen (D-14); D-09's one-gallery-at-a-time accent intact | ✓ VERIFIED | "the reveal side still demands arrival (D-14)" and "jitter does not restart the accent (D-09/HOME-16)" e2e cases pass live; `homepage-accent-random` (6 cases) re-confirmed green |
| 21-15 | The reveal is still a pure function of scroll position in both directions — no timer, no held state | ✓ VERIFIED | `computeArrivalRevealed`'s only state input is the DOM class list itself (confirmed by source read); reduced-motion e2e case passes; D-04 reversibility cases (pre-existing, unedited) pass |
| 21-15 | The developer gets ONE real-device pass covering all five round-2 UAT gaps, every open assumption named | ✓ VERIFIED (deliverable, not yet exercised) | The `<verify><human-check>` block in `21-15-PLAN.md` Task 3 confirmed present, unexecuted, and undeleted by direct read this pass — it is the single consolidated gate, harvested below as this pass's human-verification item |

**Score (round-2 gap-closure layer):** 23/23 plan-level truths verified. 0 failed.

### Plan-Level Must-Have Truths — Round-1 Build and Gap-Closure (21-01 through 21-10, plus round-1 review fixes), Regression-Checked

All 45 must-have truths verified by the prior (2026-08-08) verification pass were re-confirmed this pass by an independent live re-run of the full automated suite (typecheck clean, lint clean, build clean/29 pages, static artifact verified, unit 349/349, chromium e2e 461/461, webkit-mobile e2e 5/5) plus direct spot-checks of several structurally-significant artifacts that round-2 plans rewired (e.g. `applyArrival()`'s reveal comparison, the intro markup, the header-hide CSS, the deck viewport-height convention). No regression found in any of the 45 prior truths — every describe block round-2 plans did not explicitly rebase stayed green unedited (confirmed via each round-2 plan's own explicit "zero existing cases weakened" acceptance criteria, cross-checked against a live full-suite pass rather than trusted from the SUMMARY.md text). Full per-truth detail from the round-1 pass is preserved in git history (prior `21-VERIFICATION.md`, now superseded by this report) and is not repeated line-by-line here.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `--deck-vh` custom property + `syncDeckViewportHeight()` | live-synced viewport-height replacing static `100svh` | ✓ VERIFIED | Confirmed in source at the documented lines; `grep -c 'var(--deck-vh, 100svh)'` = 13 |
| Pull-up/opaque-stage/retire-on-completion trio | collapses the post-zoom dead zone | ✓ VERIFIED | `data-zoom-active`-keyed selectors confirmed present, `grep -c 'data-zoom-active'` = 15 |
| `INTRO_REVEAL_DISTANCE`, `computeIntroProgress()`, `computeIntroScrubState()` | pure, unit-tested intro-scrub exports | ✓ VERIFIED | All three confirmed present at the documented lines in `src/lib/home-carousel.ts` |
| One `.home-scroll-deck__intro-track`/`__intro-stage` pair, one logomark | replaces plan 21-10's two static sections | ✓ VERIFIED | Built-artifact fact: exactly 1 `home-scroll-deck__intro-logo` in each locale's built homepage |
| Narrow `.logo-mark`-only header suppression + `:not([data-intro-active])` addendum | hamburger reachable, only the small logo suppressed | ✓ VERIFIED | Both confirmed present in source at the documented lines |
| `ARRIVAL_REVEAL_THRESHOLD`, `ARRIVAL_RELEASE_THRESHOLD`, `computeArrivalRevealed()` | two-level reveal/release latch | ✓ VERIFIED | All three confirmed present in `src/lib/home-carousel.ts`; `applyArrival()`'s call site confirmed reading `wasRevealed` before computing `reached` |
| WR-01/WR-02/WR-03 review fixes | scoped round-2 code review's 3 warnings | ✓ VERIFIED | WR-01 (`lastDeckVh`-sourced `viewportHeight` in `applyArrival()`), WR-02 (`if (reached !== wasRevealed)` guard), WR-03 (`@media (scripting: none)` fallback, confirmed compiled into `dist/_astro/HomeCarousel.*.css`) all confirmed present by direct source AND build-artifact inspection, not by reading `21-REVIEW-FIX.md`'s claims |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `syncDeckViewportHeight()` (21-11) | every deck viewport-height CSS length | `var(--deck-vh, 100svh)` on `.home`, read via `getComputedStyle` in CSS `calc()` | ✓ WIRED | Confirmed: 13 occurrences of the expression; written before `applyProgress`/`applyArrival` inside `frame()` per source read |
| `applyArrival()`'s `viewportHeight` (21-15, WR-01 fix) | `lastDeckVh` cache (`syncDeckViewportHeight()`'s own module state) | direct read, not an independent `window.innerHeight` call | ✓ WIRED | Confirmed at the `applyArrival()` call site by direct read: `mobile.matches && lastDeckVh !== DECK_VH_SENTINEL ? lastDeckVh : window.innerHeight` |
| `data-zoom-active` pull-up/retirement pair (21-12) | `.home-scroll-deck__slides` margin-top / `.home-scroll-deck__stage` visibility | attribute-keyed CSS selectors, driver-written attribute | ✓ WIRED | Both selectors confirmed present; no-doubling sweep e2e case (reliably passing on isolated re-run) is the direct behavioral proof |
| `computeIntroProgress`/`computeIntroScrubState` (21-13) | intro logomark `transform`, tagline `opacity`/`transform`, cue `opacity` | `applyIntroScrub()` per-frame inline writes, matching removals in `clearInlineStyles()` | ✓ WIRED | Confirmed via source read at the documented call sites; `pinned intro scrub` e2e block (22 cases) is the behavioral proof |
| `data-intro-active` (21-14) | header `.logo-mark` suppression rule | direct CSS selector | ✓ WIRED | Confirmed present; e2e case "the logomark is the one thing suppressed (A7)" passes |
| `computeArrivalRevealed()` (21-15) | `applyArrival()`'s `classList.toggle('is-revealed', reached)` | direct function call, `wasRevealed` read from the live class list | ✓ WIRED | Confirmed at the call site by direct read; `gallery-description reveal latch` e2e block (8 cases) is the behavioral proof |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `.home-scroll-deck__intro-body` (pinned intro tagline) | `introBody` via `resolveHomepageIntro` | build-time Sanity fetch, same source as round-1's beat-2 tagline | Yes | ✓ FLOWING — re-confirmed this pass: `dist/index.html` contains the real French copy ("Découvrez un univers photographique sensible et singulier..."), not a placeholder or empty string; conditionally rendered only when non-empty |
| `.home-slide__img-placeholder`/`--sharp` pair | `gallery.blurSrc` | build-time Sanity image URL builder | Yes | ✓ FLOWING — re-confirmed: 5 placeholders, 5 sharp images in the built artifact, matching 5 galleries |
| `<meta name="theme-color">` | `COLOR_INK` literal | plain TS constant (site-config.ts), not CMS-sourced | Yes (static-by-design) | ✓ FLOWING — re-confirmed present with correct `media="(max-width: 767px)"` attribute in the built artifact |

### Behavioral Spot-Checks / Full Automated Verification Run (all independently re-executed live this pass)

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Typecheck | `npm run typecheck` | 0 errors, 0 warnings, 1 pre-existing unrelated hint | ✓ PASS |
| Lint | `npm run lint` | clean, no output | ✓ PASS |
| Build | `npm run build` | 29 pages built | ✓ PASS |
| Static artifact | `npm run test:artifact` | "Static artifact verified (29 HTML files, base /)" | ✓ PASS |
| Full unit suite + coverage | `npm run test:coverage` | 349/349 passed across 16 files; 95.31% stmt / 90.46% branch coverage (thresholds met) | ✓ PASS |
| Full e2e, chromium (1st run) | `npx playwright test --project=chromium` | 459 passed, 2 failed: `edition.spec.ts` masonry-ratio `NaN` (pre-existing, documented in `deferred-items.md` as unrelated to this phase, confirmed by reverting `HomeCarousel.astro` and reproducing identically) and `homepage-scroll-deck.spec.ts`'s "no-doubling sweep" case | ⚠️ 2 flakes investigated below |
| `homepage-scroll-deck` "no-doubling sweep", isolated, `--repeat-each=3` | `npx playwright test homepage-scroll-deck --project=chromium -g "no-doubling sweep" --repeat-each=3` | 3/3 passed | ✓ PASS |
| `homepage-scroll-deck` full file, isolated | `npx playwright test homepage-scroll-deck --project=chromium` | 95/95 passed | ✓ PASS |
| Full e2e, chromium (2nd full run) | `npx playwright test --project=chromium` | 461/461 passed | ✓ PASS |
| Full e2e, webkit-mobile | `npx playwright test --project=webkit-mobile` | 5/5 passed | ✓ PASS |

**Flake disposition:** both first-run failures are confirmed non-functional. `edition.spec.ts`'s failure is pre-existing, documented, and unrelated to any file this phase touches (`.planning/phases/21-homepage-scroll-experience/deferred-items.md`, confirmed independently by reverting `HomeCarousel.astro` in a prior plan's own investigation and reproducing the same failure with this phase's changes entirely absent). `homepage-scroll-deck.spec.ts`'s "no-doubling sweep" failure did not reproduce in 3 isolated repeats, in a full isolated run of its own file (95/95), or in a second full-suite run (461/461) — consistent with resource-contention timing sensitivity under a ~460-test parallel run rather than a functional regression in the gap-3 fix.

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` convention exists for this project and none is declared in any of the 15 plans or their summaries. Skipped.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| HOME-14 | 21-01 through 21-04, 21-06, 21-07, 21-08, 21-11, 21-12, 21-15 | Single scroll-driven view, no carousel/grid toggle; descriptions reveal on arrival | ✓ SATISFIED | Structural/reveal e2e coverage re-run live and green; round-2 fixes (deck-vh, collapsed dead zone, reveal latch) verified live in source and build artifact |
| HOME-15 | 21-01, 21-04, 21-05, 21-09, 21-10, 21-11, 21-13, 21-14 | Full-screen wordmark on load (now: after the redesigned pinned-scrub intro); scroll zooms through letterforms into first gallery's photo | ✓ SATISFIED (structural/automated half); real-device confirmation of the redesigned intro, the collapsed dead zone, and the fixed toolbar coverage still outstanding | Intro-scrub + zoom-driver e2e coverage re-run live and green; real-device pass is the one item routed to human verification |

`.planning/REQUIREMENTS.md` marks both `HOME-14` and `HOME-15` as `[x]` complete with status "Complete" — this pass confirms that marking is consistent with the codebase's automated evidence but, as with the prior pass, does not independently resolve the outstanding real-device gate (REQUIREMENTS.md tracks requirement-to-phase mapping, not the phase's own manual-verification sign-off in `21-VALIDATION.md`).

No orphaned requirements: `REQUIREMENTS.md` maps only `HOME-14` and `HOME-15` to Phase 21, and all 15 plans' frontmatter `requirements` fields collectively cover both (confirmed by grepping every `21-*-PLAN.md`'s `requirements:` field this pass).

### Anti-Patterns Found

None. `grep -nE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` against every file modified across plans 21-11 through 21-15 (`src/components/HomeCarousel.astro`, `src/lib/home-carousel.ts`, `tests/unit/home-carousel.test.ts`, `tests/e2e/homepage-scroll-deck.spec.ts`) returns no debt-marker matches (`PLACEHOLDER`-adjacent hits are legitimate `*-placeholder` class-name identifiers, not debt markers). No stub returns, no hardcoded-empty render paths, no unreferenced debt markers.

The scoped round-2 code review (`21-REVIEW.md`) independently found 0 critical, 3 warning, 1 info issues in this exact diff — all 3 warnings confirmed fixed by direct source and build-artifact inspection this pass (see Required Artifacts). The 1 info finding (`IN-01`, `.home-scroll-deck__stage` declared as two non-adjacent CSS rule blocks) is a pure discoverability nit with no behavioral effect, explicitly marked "not urgent" by the review and correctly left unfixed per the review-fix's own stated scope (critical + warning only).

### Human Verification Required

### 1. Real-device pass closing phase 21's ROUND-2 gap-closure set (required by `21-VALIDATION.md`, carried unexecuted in `21-15-PLAN.md`'s Task 3 human-check)

**Test:** Run `npm run build && npm run preview -- --host`, open the printed local-network URL on a REAL iPhone. Load fresh and look before scrolling (count logos, try to reach navigation); open and close the hamburger nav without scrolling; scroll slowly through the intro scrub, reading the tagline; continue slowly through the wordmark zoom and every gallery slide, trying to read each description as it settles; repeat at a brisker flicking pace; scroll back to the top; fling-scroll down then up; watch the status bar and Safari's toolbar area throughout; tap a photo; reach for the header mid-scroll; compare tablet/desktop against pre-milestone behaviour. Confirm or correct by name: A1/A2/A3/A5 (standing), A4 (superseded), A6/A7/A8/A9 (new, pending).

**Expected:** All 14 points listed in the frontmatter's `human_verification[0].expected` entry above — in short: exactly one logomark, hamburger reachable and functional from first paint, the shrink/reveal reads as one continuous motion, the tagline and every gallery description stay readable at both a slow and brisk pace, the cover photo never doubles, no white bar/gap at top or bottom at any point, scrolling fully reverses, and A1-A9 are each confirmed or corrected.

**Why human:** See the frontmatter's `human_verification[0].why_human` for full reasoning — in short, this phase has now shipped a fully green automated suite twice (round 1 and round 2's own gap-closure sets) and the developer found real, live-device-only defects both times despite dedicated automated coverage for every prior fix. This verification pass independently re-confirmed round 2's mechanism-level coverage is genuinely present, wired, and green (not merely claimed) — but the phase's own two-round history is direct evidence that this is necessary and not sufficient to predict the real-device result. Assumptions A6 (intro pace/shrink-in-place), A7 (header/logo composition), A8 (latch feel) and A9 (accepted snap-mode tradeoff) are explicitly the judgement calls no automated assertion can make, per `21-15-PLAN.md`'s own reasoning.

### Gaps Summary

No code gaps were found in this re-verification pass. Every truth, artifact, and key link this report checked was verified against the LIVE codebase — a full rebuild, a full re-run of the unit suite (349/349), the full e2e suite on both configured Playwright projects (461/461 chromium after investigating and clearing two flakes, 5/5 webkit-mobile), typecheck, lint, and static-artifact verification all pass cleanly. All three round-2 code-review warnings are confirmed fixed by direct source AND compiled-CSS inspection, not by reading `21-REVIEW-FIX.md`'s claims. All five round-2 `21-UAT.md` gaps have corresponding, verified, wired fixes with dedicated automated regression coverage, independently re-run and confirmed green this pass.

The single outstanding item — structurally identical in kind to both prior verification passes, now scoped to the round-2 gap-closure set plus the newly-redesigned intro, collapsed dead zone, live viewport-height sync, header fix, and reveal latch — is the phase's own mandatory real-device manual verification pass. This is not a code gap: it is the one check `21-VALIDATION.md`'s Manual-Only Verifications table and `21-15-PLAN.md`'s own Task 3 human-check both explicitly require and explicitly cannot be automated, confirmed present and unexecuted in the plan file by direct read this pass. Given that both of this phase's two prior "everything automated is green" states each independently preceded a real-device UAT round that found several live defects, this routes the phase to `human_needed` rather than `passed` a third time. Once a human runs the real-device pass and confirms/corrects assumptions A6 through A9 (re-confirming A1-A3/A5, noting A4 as superseded) — or the orchestrator captures a fresh `21-UAT.md` round-3 entry recording either a clean pass or new gaps — the phase can close.

---

_Verified: 2026-08-10T14:35:00Z_
_Verifier: Claude (gsd-verifier)_
