---
phase: 21-homepage-scroll-experience
plan: 04
type: execute
wave: 2
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - tests/e2e/homepage-scroll-deck.spec.ts
  - src/components/HomeCarousel.astro
autonomous: true
requirements: [HOME-14, HOME-15]
must_haves:
  truths:
    - "On a phone-width viewport the carousel/grid mode toggle is absent and non-interactive, and neither view mode renders."
    - "On a phone-width viewport the homepage opens on a wordmark that fills the screen."
    - "Scrolling a phone-width homepage moves through one full-screen slide per gallery, in gallery order, as a single sequence."
    - "Tapping a slide opens that gallery's detail page."
    - "Scrolling past the last slide reaches the site footer."
    - "A reduced-motion phone visitor sees every description already visible and no forced snapping."
    - "On tablet and desktop viewports the toggle and both view modes render and behave exactly as before."
  artifacts:
    - "tests/e2e/homepage-scroll-deck.spec.ts covering deck structure, toggle absence, full-screen wordmark, slide count/order, footer reachability, and the reduced-motion end state"
    - "A .home-scroll-deck markup block in src/components/HomeCarousel.astro, sibling to .home-hero and .home-grid"
    - "A phone-width CSS section in src/components/HomeCarousel.astro hiding .home-hero, .home-grid and the mode toggle, and showing the deck"
    - "A min-width:768px gate around the existing carousel-mode footer-hide rule"
  key_links:
    - "The deck is shown/hidden by a pure CSS media query at 767px/768px — no JS markup swap (21-RESEARCH.md Anti-Patterns)"
    - "The zoom photo custom property and slide 1's img resolve to the same first-gallery hero source, so the zoom lands on the photo it reveals"
    - "wordmarkPhotoFilter is imported from src/lib/home-carousel.ts and both in-file duplicates are deleted (20-REVIEW.md IN-01)"
---

<objective>
Build the phone-width scroll deck's structure: a failing structural e2e spec first, then the new parallel markup, then its CSS — retiring the carousel/grid/toggle below 767px while leaving everything at 768px and above byte-for-byte unchanged.

Purpose: this is the plan that makes phase success criteria 1, 2, 4 and 5 structurally true. `21-RESEARCH.md` is explicit that the mobile experience must be NEW, parallel markup gated by `@media (max-width: 767px)` rather than a JS mode swap or a conditional re-use of the carousel DOM (which carries progress dashes, an autoplay toggle, peek layers and a hover cursor that D-01/D-16 explicitly do not want). No motion is wired in this plan — the deck renders as a static wordmark plus a scrollable slide sequence, which is exactly the reduced-motion end state D-15 requires. Plans 21-05 and 21-06 layer the scroll driver and the arrival observer on top.

Output: `tests/e2e/homepage-scroll-deck.spec.ts` (new), plus markup and CSS in `src/components/HomeCarousel.astro`.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-homepage-scroll-experience/21-CONTEXT.md
@.planning/phases/21-homepage-scroll-experience/21-RESEARCH.md
@.planning/phases/21-homepage-scroll-experience/21-PATTERNS.md
@.planning/phases/21-homepage-scroll-experience/21-UI-SPEC.md
@.planning/phases/21-homepage-scroll-experience/21-VALIDATION.md
@.planning/phases/21-homepage-scroll-experience/21-01-SUMMARY.md
@.claude/skills/sketch-findings-ajs-website/references/homepage-motion.md
</context>

<artifacts_this_phase_produces>
Full phase-wide list of new symbols. Plans 21-05 and 21-06 consume these names verbatim — do not rename them.

| Symbol | Kind | Introduced by |
|--------|------|---------------|
| `tests/e2e/homepage-scroll-deck.spec.ts` | new file | 21-04 |
| `.home-scroll-deck` / `data-role="scroll-deck"` | deck root element | 21-04 |
| `.home-scroll-deck__track` / `data-role="zoom-track"` | tall scroll track (D-02) | 21-04 |
| `.home-scroll-deck__stage` / `data-role="zoom-stage"` | sticky pinned stage | 21-04 |
| `.home-scroll-deck__photo` / `data-role="zoom-photo"` | end-state crossfade layer | 21-04 |
| `.home-scroll-deck__wordmark` / `data-role="zoom-wordmark"` | full-screen `<h1>` (D-01) | 21-04 |
| `.home-scroll-deck__focus-letter` / `data-role="zoom-focus-letter"` | `<span>` around the "A" of Atelier | 21-04 |
| `.home-scroll-deck__slides` / `data-role="deck-slides"` | slide list wrapper | 21-04 |
| `.home-slide` / `data-role="deck-slide"` | one `<a>` per gallery (D-05/D-07/D-10) | 21-04 |
| `.home-slide__img` / `.home-slide__accent` / `.home-slide__title` / `.home-slide__description` | slide internals (D-06) | 21-04 |
| `--zoom-photo` / `--zoom-photo-filter` | CSS custom properties on `.home` | 21-04 |
| `--slide-accent` / `--slide-accent-text` | CSS custom properties per slide | 21-04 |
| `is-revealed` | arrival state class on `.home-slide` (D-13/D-14) | 21-04 declares the CSS, 21-06 toggles it |
| `data-zoom-active` | attribute on `.home` driving the header hide (D-12) | 21-04 declares the CSS, 21-05 writes it |
| `ZOOM_REVEAL_DISTANCE`, `computeZoomProgress`, `computeWordmarkZoomState`, `computeFocusOrigin`, `wordmarkPhotoFilter` | exports of `src/lib/home-carousel.ts` | 21-01 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the structural deck spec (RED)</name>
  <files>tests/e2e/homepage-scroll-deck.spec.ts</files>
  <read_first>
    - `tests/e2e/homepage-mobile-responsive.spec.ts` and `tests/e2e/homepage-chrome-nav.spec.ts` — the suite's viewport-setup, locator and `page.evaluate` conventions for homepage specs.
    - `tests/e2e/homepage-content-display.spec.ts` around line 150 — an existing `page.emulateMedia({ reducedMotion: 'reduce' })` call site to copy exactly.
    - `tests/e2e/homepage-accent-random.spec.ts` lines 1-35 — the helper that reads the build-time gallery list out of `ul[data-role="home-carousel-data"]`; reuse that read pattern to derive expected slide count/order/hrefs rather than hardcoding gallery names.
    - The artifacts table above — every selector this spec asserts against comes from there.
  </read_first>
  <behavior>
    All phone-width cases use a 393x852 viewport on `/` unless stated. All desktop cases use 1280x800.

    1. Toggle retirement (success criterion 1): at phone width, the mode-toggle control is not visible, and the carousel and grid subtrees are both not visible.
    2. Deck presence: at phone width the deck root is visible.
    3. Full-screen wordmark on first load (HOME-15, success criterion 4): at phone width, before any scrolling, the zoom wordmark is visible and its bounding box covers at least 60% of the viewport width and at least 40% of the viewport height.
    4. One slide per gallery, in order (HOME-14, success criterion 2): the number of slide elements equals the number of entries in the build-time gallery data list, and the Nth slide's href equals the Nth entry's href.
    5. Slide geometry: every slide's bounding box height is within 2px of the viewport height.
    6. Tap-to-open target (D-10): every slide is an anchor element with a non-empty href pointing at a gallery detail route.
    7. Intro-only wordmark (D-16): no slide contains a zoom-wordmark element, and the page exposes exactly one level-1 heading at phone width.
    8. Description default state (D-13): at phone width and before any arrival, each slide's description has computed opacity 0 while its title is visible.
    9. Reduced-motion end state (D-15): with reduced motion emulated at phone width, every slide description has computed opacity 1, and the document element's computed scroll-snap-type is `none`.
    10. Footer reachable (D-08): at phone width the footer's computed display is not `none`.
    11. No horizontal overflow at phone width: document scrollWidth is less than or equal to clientWidth (this repeats an existing guard deliberately, scoped to the new markup).
    12. Desktop unchanged (success criterion 5, UI-02): at 1280x800 the deck root is not visible, the mode-toggle is visible, and the carousel is visible.
  </behavior>
  <action>
    Create `tests/e2e/homepage-scroll-deck.spec.ts` covering every numbered case above, using only the selectors named in the artifacts table. Group the cases into describe blocks by theme (retirement / structure / reduced motion / desktop regression) and open the file with a comment stating that this spec is the phone-width replacement for the homepage coverage retired by plan 21-03, and naming HOME-14 and HOME-15.

    Derive expectations from the page's own build-time data node wherever possible (slide count, order, hrefs) so the spec does not hardcode content that Sanity owns.

    For the computed-opacity assertions, read `getComputedStyle(el).opacity` through `page.evaluate`/`locator.evaluate` rather than asserting visibility, because an opacity-0 element is still "visible" to Playwright.

    This task is expected to FAIL — the markup does not exist yet. Commit it as a failing test.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm run build && (npx playwright test homepage-scroll-deck --project=chromium; test $? -ne 0)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/homepage-scroll-deck.spec.ts` exists and its test run exits non-zero (RED).
    - The spec references every `data-role` value in the artifacts table that Task 2 will introduce.
    - `grep -c "393" tests/e2e/homepage-scroll-deck.spec.ts` is greater than 0 and `grep -c "1280" tests/e2e/homepage-scroll-deck.spec.ts` is greater than 0 (both phone and desktop coverage present).
    - No other spec file is modified.
    - `src/` is unchanged by this task.
  </acceptance_criteria>
  <done>The structural contract for the deck exists as executable, currently-failing assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Add the scroll-deck markup and consolidate the photo-cutout filter</name>
  <files>src/components/HomeCarousel.astro</files>
  <read_first>
    - `src/components/HomeCarousel.astro` lines 1-130 — the frontmatter: the `GalleryEntry` interface, `firstGallery`, the local `wordmarkPhotoFilter` const at lines 96-99, the `assetBase` pattern, and the `.home` root's inline custom-property style object at lines 116-121.
    - `src/components/HomeCarousel.astro` lines 260-350 — the accent panel, the grid block, and the hidden `ul[data-role="home-carousel-data"]` node. The new deck block goes between the grid block and that data node.
    - `src/components/HomeCarousel.astro` lines 352-402 — the client script's import statement and its duplicate `wordmarkPhotoFilter` const at lines 398-401.
    - `src/lib/home-carousel.ts` — the `wordmarkPhotoFilter` export added by plan 21-01.
    - `.claude/skills/sketch-findings-ajs-website/references/homepage-motion.md`, the "HTML Structures" section — the track / sticky stage / photo layer / focus-letter shape being reproduced.
    - `.planning/phases/21-homepage-scroll-experience/21-UI-SPEC.md`, "Layout & Structure Contract".
    Read only the ranges listed. Do not read the whole 3078-line file.
  </read_first>
  <action>
    Frontmatter changes:
    1. Import `wordmarkPhotoFilter` from `../lib/home-carousel` and delete the local const at lines 96-99. Every existing frontmatter call site keeps working unchanged.
    2. Extend the `.home` root's inline style object with two new custom properties: the zoom photo, set to a CSS `url(...)` built from `firstGallery.heroSrc`, and the zoom photo filter, set from `wordmarkPhotoFilter(firstGallery.heroTextColor)`. Leave the four existing properties exactly as they are.

    Client-script change:
    3. Add `wordmarkPhotoFilter` to the existing `import { ... } from '../lib/home-carousel'` statement at line 353 and delete the duplicate const at lines 398-401. Do not change any call site's arguments. Note the extracted signature accepts an optional argument while the deleted script-scope copy required one — that is a widening, so all existing calls stay valid.

    Markup: insert a new deck block as a sibling of `.home-hero` and `.home-grid`, placed after the grid block and before the hidden data list, structured exactly as follows (class names and `data-role` values are the contract plans 21-05/21-06 depend on):

    - A deck root `div` carrying `class="home-scroll-deck"` and `data-role="scroll-deck"`.
    - Inside it, a track `div` (`home-scroll-deck__track`, `data-role="zoom-track"`) containing a stage `div` (`home-scroll-deck__stage`, `data-role="zoom-stage"`).
    - Inside the stage, first a photo layer `div` (`home-scroll-deck__photo`, `data-role="zoom-photo"`, `aria-hidden="true"`, no children), then an `h1` (`home-scroll-deck__wordmark`, `data-role="zoom-wordmark"`) whose text is the three-line wordmark with the SAME line breaks the existing hero wordmark uses, and whose leading letter is wrapped in a `span` (`home-scroll-deck__focus-letter`, `data-role="zoom-focus-letter"`) so the remainder of that word follows the span as plain text.
    - After the track, a slides wrapper `div` (`home-scroll-deck__slides`, `data-role="deck-slides"`).
    - Inside the wrapper, one anchor per gallery, mapped over the same `galleries` prop the carousel and grid already map over. Each anchor carries `class="home-slide"`, `data-role="deck-slide"`, `data-index` set to the map index, `data-hero-color` and `data-hero-text-color` from the gallery, an `href` built with `getRelativeLocaleUrl(locale, \`galleries/${gallery.slug}\`)` exactly as the grid tiles already do, and an inline style object setting a per-slide accent background custom property and a per-slide accent text custom property from the gallery's own hero colors, each falling back to the same site tokens the carousel's own accent fallbacks use.
    - Each anchor contains: an `img` (`home-slide__img`) using the gallery's `heroSrc`/`heroSrcSet` with `sizes="100vw"` and the gallery's `alt`, eager+high-priority for the first gallery and lazy+async for the rest; then a panel `div` (`home-slide__accent`) containing a `span` (`home-slide__title`) with the gallery title and a `p` (`home-slide__description`) with the gallery statement.

    Use the first gallery's `heroSrc` for BOTH the zoom photo custom property and the first slide's image, so the zoom resolves onto the same photo the first slide shows.

    Add a leading comment on the deck block recording: HOME-14/HOME-15; that this is new parallel markup shown only below 767px by CSS with no JS markup swap; that D-16 is why no slide repeats the wordmark; that D-10 is satisfied by each slide being a real anchor rather than a synthesized tap handler, which also removes the tap-hijack class of bug CR-01 represents; and that D-07 is why there is exactly one photo per gallery.

    Do NOT touch `.home-hero`, `.home-grid`, the toggle button, the data list, the existing script's other logic, or any CSS in this task.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm run typecheck && npm run lint && npm run build && npm run test:artifact</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck`, `npm run lint`, `npm run build` and `npm run test:artifact` all pass.
    - `grep -c "data-role=\"deck-slide\"" src/components/HomeCarousel.astro` returns 1 (one templated occurrence inside the map).
    - `grep -c "wordmarkPhotoFilter" src/components/HomeCarousel.astro` returns exactly 3: the import specifier plus the two surviving call sites (frontmatter root style, and the client script's render path). No local function definition of it remains in the file.
    - `grep -c "from '../lib/home-carousel'" src/components/HomeCarousel.astro` returns 2 (frontmatter import and client-script import).
    - The built artifact contains one deck slide anchor per gallery: `grep -o 'data-role="deck-slide"' dist/index.html | wc -l` equals `grep -o 'data-role="home-carousel-data"' -A0 dist/index.html | wc -l` multiplied by the gallery count — practically, assert the count matches the number of `<li data-slug=` entries in `dist/index.html`.
    - `npx playwright test homepage-carousel-core homepage-content-display homepage-wordmark-peek --project=chromium` still passes (desktop/tablet behavior unchanged so far — the deck is unstyled and therefore still visible at every width until Task 3, so phone-width specs may still be red at this point; that is expected and closed by Task 3).
  </acceptance_criteria>
  <done>The deck exists in the DOM at every width, with a single shared photo-cutout filter helper.</done>
</task>

<task type="auto">
  <name>Task 3: Gate the deck to phone width, retire the carousel/grid/toggle below 767px, and style the deck</name>
  <files>src/components/HomeCarousel.astro</files>
  <read_first>
    - `src/components/HomeCarousel.astro` lines 2741-2805 — `.home-grid__tile-title` and `.home-grid__tile-description`, the exact reveal values D-13 locks (opacity 0 to 1, translateY 8px to 0, 180ms ease, the 2-line title clamp, the 3-line description clamp, the 52ch max-width, the min-height reservations).
    - `src/components/HomeCarousel.astro` lines 2806-2971 — the existing `@media (max-width: 767px)` and `@media (max-width: 359px)` blocks, so you place the new section after them and understand the wordmark clamp being reused.
    - `src/components/HomeCarousel.astro` lines 2855-2905 — the mobile `.home-hero__accent` band geometry and the mobile wordmark clamp, both of which the deck reuses conceptually.
    - `src/components/HomeCarousel.astro` lines 2975-2995 — the `is:global` block containing the carousel-mode footer-hide rule.
    - `src/components/SiteHeader.astro` lines 205-232 — the transparent header variant's `position: absolute` and `z-index`, which the phone-width deck must override.
    - `.planning/phases/21-homepage-scroll-experience/21-UI-SPEC.md`, "Motion & Interaction Contract" and "Layout & Structure Contract".
    - `.planning/phases/21-homepage-scroll-experience/21-RESEARCH.md` Pitfall 2, Pitfall 3 and Pitfall 6.
  </read_first>
  <action>
    Add ONE new clearly-commented CSS section at the very end of the component's scoped `<style>` block (after the existing 359px media block), so source order guarantees the overrides land. Then make one edit inside the `is:global` block.

    A. Base (all widths): the deck root is `display: none`.

    B. Inside a new `@media (max-width: 767px)` block:
    1. Hide `.home-hero`, `.home-grid`, and the mode-toggle button. Use `display: none` on each. The toggle is authored in this file (slotted into the header), so this file's scoped selector reaches it.
    2. Show the deck root as a block.
    3. Track: height `calc(100dvh + 900px)`, keeping the 900px in sync with the exported reveal-distance constant by referencing it in a comment (the CSS cannot import the TS constant; note in the comment that plan 21-05's driver reads the exported constant and that both must move together if the pace is tuned on a real device).
    4. Stage: `position: sticky`, `top: 0`, `height: 100dvh`, `overflow: hidden`, and a centering layout (flex, centered on both axes) for the wordmark.
    5. Photo layer: absolutely positioned to fill the stage, `background-image: var(--zoom-photo)`, `background-size: cover`, `background-position: center`, `background-repeat: no-repeat`, `opacity: 0` at rest. This is the end-state crossfade target plan 21-05 drives.
    6. Zoom wordmark: `font-family: var(--font-display)`, uppercase, `line-height: 0.92`, weight 900, centered text, and the SAME per-line clamp the existing mobile hero wordmark uses (`clamp(36px, 9.8vw, 50px)`) — reuse that value verbatim rather than inventing a size. Give it `will-change: transform, opacity` and `position: relative` so it paints above the photo layer. Set `background-image: var(--zoom-photo)`, `background-size: cover`, `background-position: center`, `background-repeat: no-repeat` directly on it — the full-screen wordmark's box IS the photo's box, so no alignment math is needed here (do NOT reach for the hero wordmark's alignment helper; per Pitfall 5 it solves a different geometry problem).
    7. Wrap the cutout activation in `@supports (background-clip: text) or (-webkit-background-clip: text)`, mirroring the existing grid-wordmark `@supports` block: set both the prefixed and unprefixed `background-clip: text`, transparent `color` and transparent `-webkit-text-fill-color`, and exactly ONE `filter` declaration reading `var(--zoom-photo-filter)`. Add a comment repeating the existing file's own Safari warning: extra paint layers (a second background, a box-shadow, a text stroke) combined with a text-clipped background produce raster seams on Safari, and the crossfade photo layer — not a flawless cutout at extreme scale — is the safety net. Outside the `@supports` block the wordmark keeps a solid readable colour as the unsupported-engine fallback.
    8. Focus-letter span: `display: inline-block` (so it has a measurable box for the origin math in plan 21-05) with no other visual effect — it must not change the rendered wordmark's appearance in any way.
    9. Slides wrapper: no snap type of its own (see item 14). Slides: `display: block`, `position: relative`, `height: 100dvh`, `scroll-snap-align: start`, `scroll-snap-stop: always`, `color` inherited from the per-slide accent text property, and `text-decoration: none`. Add a `:focus-visible` outline using the site's fixed brand accent token.
    10. Slide image: absolutely positioned to fill the slide, `object-fit: cover`, `width: 100%`, `height: 100%`.
    11. Slide accent panel: absolutely positioned as a full-width bottom-anchored band (`left: 0; right: 0; bottom: 0; width: 100%`) with `padding: var(--space-lg)`, background from the per-slide accent property, colour from the per-slide accent text property. This mirrors the mobile variant of the existing accent panel, not the desktop corner-card geometry (D-06).
    12. Slide title: reuse `.home-grid__tile-title`'s declarations verbatim (display, font-family, 32px, semibold weight token, line-height 1.2, the 2-line clamp and its min-height reservation). Always visible — the title is NOT part of the reveal.
    13. Slide description: reuse `.home-grid__tile-description`'s declarations verbatim (the 3-line clamp and its min-height reservation, the top margin, the 52ch max-width, 14px, line-height 1.4, `opacity: 0`, `transform: translateY(8px)`, and the 180ms ease transition on opacity and transform). Add the revealed-state rule keyed on the `is-revealed` class on the slide ancestor, setting opacity 1 and `transform: translateY(0)`. Do not retune any of these numbers (D-13).
    14. Header (D-12): using a selector specific enough to beat the transparent variant's own `position: absolute` (qualify with both the `.home` ancestor and both header classes inside a `:global()`), set `position: fixed` and a `transition` on opacity, and keep its stacking above the deck. Then add a rule keyed on `.home[data-zoom-active='true']` setting the header to `opacity: 0` and `visibility: hidden`, with the visibility change delayed until the opacity transition finishes so the fade reads as a fade. The attribute is absent by default, so the header is visible pre-JS, with JS disabled, and under reduced motion — that default is load-bearing for the reduced-motion route plan 21-03's tests rely on. Add a comment saying so.

    C. Inside the `is:global` block:
    15. Add a phone-width snap rule scoped to pages that actually render the deck: `html:has(.home-scroll-deck)` gets `scroll-snap-type: y proximity` inside `@media (max-width: 767px)`. Comment the choice explicitly: the page itself is the scroll container (D-08 requires scrolling past the last slide into the footer, so a nested scroller is out), and the zoom track is a deliberately non-snapping 900px scrub region that a strict snap mode would fight (Pitfall 6) — a proximity snap keeps D-04's free scrub intact while `scroll-snap-stop: always` on the slides still prevents a fling from skipping a gallery. Record that this is the "exact snap-scroll CSS technique" CONTEXT.md left to planning, not a change to any locked decision.
    16. Wrap the existing carousel-mode footer-hide rule in `@media (min-width: 768px)` so the footer is reachable at phone width (D-08). Do not otherwise alter that rule.

    D. A new `@media (max-width: 767px) and (prefers-reduced-motion: reduce)` block (D-15):
    17. `html:has(.home-scroll-deck)` gets `scroll-snap-type: none`.
    18. Slide descriptions get `opacity: 1` and `transform: none`, so they are permanently visible with no observer running.
    19. The zoom wordmark and photo layer keep their rest state (no rule needed beyond not setting anything) — add a comment stating the static full-screen wordmark IS the reduced-motion end state.

    Do not modify any existing rule other than the footer-hide media wrap in item 16. In particular, leave the existing phone-width `.home-hero__*` overrides in place — they now apply only to a hidden subtree and removing them would enlarge this phase's diff for no behavioural gain.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm run typecheck && npm run build && npx playwright test homepage-scroll-deck --project=chromium && npx playwright test --project=chromium && npx playwright test --project=webkit-mobile</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test homepage-scroll-deck --project=chromium` passes — the RED spec from Task 1 is now GREEN.
    - The FULL e2e suite passes on both the chromium and webkit-mobile projects.
    - `npm run typecheck`, `npm run lint`, `npm run build`, `npm run test:artifact` all pass.
    - `grep -c "home-scroll-deck" src/components/HomeCarousel.astro` is greater than 10 (markup plus the new CSS section).
    - `grep -n "180ms ease" src/components/HomeCarousel.astro` shows the description transition declared for both the grid tile and the new slide, with identical values.
    - `grep -c "100dvh" src/components/HomeCarousel.astro` is at least 3 (track calc, stage, slide).
    - The existing footer-hide rule is now inside a `min-width: 768px` media query, and `npx playwright test homepage-chrome-nav --project=chromium` (which asserts the desktop carousel-mode footer hide) still passes.
    - Manual desktop confirmation is deferred to the phase-level human check, but automated proof of no desktop regression is the full-suite pass above.
  </acceptance_criteria>
  <done>The phone-width homepage is the deck; the tablet/desktop homepage is byte-for-byte the carousel/grid it was.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time Sanity content -> rendered DOM | Gallery title, statement, alt text, hero image URL and hero colours are interpolated into the new deck markup. Identical trust level and identical rendering path to the carousel and grid blocks that already render the same fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04-A | Tampering / Information Disclosure | Gallery title and statement rendered into `.home-slide__title` / `.home-slide__description` | low | accept | Not a new risk. The values come from Sanity at build time and are emitted through Astro's auto-escaping template bindings, exactly as `.home-grid__tile-title` / `.home-grid__tile-description` already do. No `set:html`, no `innerHTML`, no new unescaped sink is introduced. |
| T-21-04-B | Tampering | Tap-to-open surface on `.home-slide` (D-10) | low | mitigate | This is the clickjacking / tap-hijack pattern `21-RESEARCH.md` names for this phase. Mitigated structurally rather than defensively: each slide is a real anchor with a build-time-rendered internal href, so navigation is the browser's own click semantics — there is no synthesized `openCurrent()` path, no coordinate heuristic, and therefore no exclusion guard to get wrong (which is precisely how CR-01 arose in the carousel handler, fixed separately in plan 21-02). |
| T-21-04-C | Tampering | `--zoom-photo` custom property built from `firstGallery.heroSrc` | low | accept | The value is a Sanity CDN URL produced by the same build-time image-URL builder the carousel already uses for its own `--wordmark-photo`; it is emitted via Astro's style-object binding, not string-concatenated into a raw style attribute. Same trust level, same sink, no new boundary. |

## ASVS Applicability (level 1)

Per `21-RESEARCH.md` Security Domain: V2 Authentication, V3 Session Management, V4 Access Control, V5 Input Validation and V6 Cryptography all remain **not applicable**. This plan renders already-public, already-validated build-time content into new markup and styles it. No new auth, session, access-control, input-parsing or cryptographic surface is created, and no new ASVS category becomes in-scope.

## Schema-Push Applicability

**Not applicable, explicitly.** This plan touches one Astro component and one Playwright spec. It modifies no Sanity schema file, no ORM migration and no database schema — the gallery fields it renders (`title`, `statement`, `heroSrc`, `heroColor`, `heroTextColor`) all already exist and are already fetched by the calling pages. No `[BLOCKING]` schema-push task is required.
</threat_model>

<verification>
- `npm run build && npx playwright test homepage-scroll-deck --project=chromium` — the new structural spec green.
- `npx playwright test --project=chromium` and `npx playwright test --project=webkit-mobile` — the FULL suite green. Plan 21-03 pre-reconciled the legacy specs precisely so this holds.
- `npm run typecheck`, `npm run lint`, `npm run test:artifact` — clean.
- Manual, cannot be automated (carried to the phase gate, per `21-VALIDATION.md`'s Manual-Only Verifications): serve the build over the local network and open the homepage on a REAL phone. Playwright's `webkit-mobile` project is the desktop WebKit build with an emulated iPhone viewport, not Mobile Safari, and has historically lagged on scroll APIs. Confirm on-device that the wordmark genuinely fills the screen, that slides snap one at a time, and that scrolling past the last slide reaches the footer.
</verification>

<success_criteria>
- Phase success criterion 1: the mode toggle does not appear at phone width.
- Phase success criterion 2 (structural half): one full-screen slide per gallery, in order, as a single scroll sequence.
- Phase success criterion 4 (static half): the wordmark fills the screen on first load at phone width.
- Phase success criterion 5: the full e2e suite, including every desktop/tablet carousel and grid assertion, is green.
- D-05, D-06, D-07, D-08, D-10, D-13 (values), D-15 (end state) and D-16 are all satisfied by structure and CSS alone, with no JS yet attached.
</success_criteria>

<output>
Create `.planning/phases/21-homepage-scroll-experience/21-04-SUMMARY.md` when done.
</output>
