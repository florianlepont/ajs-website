---
phase: quick-260728-el6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/schemas/editionsPage.ts
  - sanity/schemas/index.ts
  - sanity/schemas/structure.ts
  - src/lib/sanity.ts
  - src/lib/site-config.ts
  - src/pages/editions/index.astro
  - src/pages/en/editions/index.astro
  - src/components/EditionsOverviewBody.astro
  - tests/unit/site-config.test.ts
  - tests/e2e/edition.spec.ts
autonomous: true
requirements: [EDN-02, EDN-06, CMS-04]
must_haves:
  truths:
    - "Hovering a long real édition statement (e.g. live Silos copy) reveals its FULL text with no mid-sentence clip — the reveal animates to the statement's true intrinsic height regardless of length."
    - "The Éditions overview shows a Sanity-editable descriptive intro paragraph directly under the H1, in body typography (not the display font), on both /editions/ and /en/editions/."
    - "The redundant hairline directly under the H1 (the first-row top border) is gone; the shared page-top hairline in .editions-list__header is untouched."
    - "Both locales resolve the intro from editionsPage.intro when the singleton exists, and from DEFAULT_EDITIONS_INTRO otherwise — the page renders correct placeholder copy even before the document is seeded."
    - "EDN-06 stays clean (no commerce affordance/token); full unit + build + artifact + e2e suites pass."
  artifacts:
    - "sanity/schemas/editionsPage.ts (new editionsPage singleton, intro-only, no seo)"
    - "sanity/schemas/index.ts + structure.ts updated to register and pin the singleton"
    - "EditionsPage interface + EDITIONS_PAGE_QUERY + getEditionsPage() in src/lib/sanity.ts"
    - "DEFAULT_EDITIONS_INTRO + resolveEditionsIntro() in src/lib/site-config.ts"
    - "intro prop, .editions-list__intro paragraph, and grid-rows statement reveal in EditionsOverviewBody.astro"
    - "both editions/index.astro routes fetch getEditionsPage and pass intro"
    - "new resolveEditionsIntro unit tests + new anti-truncation e2e assertion"
  key_links:
    - "route → getEditionsPage() → resolveEditionsIntro(page, locale) → <EditionsOverviewBody intro> → rendered <p class='editions-list__intro'> in the header"
    - "structure.ts documentId('editionsPage') singleton pin must match EDITIONS_PAGE_QUERY's `_id == \"editionsPage\"` filter"
    - ".editions-index__statement-wrap grid-template-rows 0fr→1fr reveal is wired to the SAME hover + focus-visible triggers as today, and forced to 1fr on mobile (≤800px)"
---

<objective>
Fix two real issues Florian reported on the just-shipped B2-redesign Éditions overview (`/editions/`, `/en/editions/`, rendered by `EditionsOverviewBody.astro`) and add a new Sanity-editable intro paragraph:

1. **Statement truncation (real bug):** the hover-revealed `.editions-index__statement` clips real, longer Romane-authored statements because it animates to a fixed `max-height: 80px`. Replace with the `grid-template-rows: 0fr → 1fr` reveal so it animates to the content's true intrinsic height at any length.
2. **Redundant hairline + editable intro:** remove the hairline directly under the H1 (first-row top border) and add a body-typography descriptive intro paragraph under the H1 that Romane can edit in Sanity Studio — mirroring the existing `homePage`/`getHomePage()`/`resolveHomepageIntro()` pattern exactly.

Touched requirement areas: EDN-02 (overview route), EDN-06 (no-commerce guard, verified), CMS-04 (Sanity-editable editorial content, the pattern the new intro follows).

Purpose: unclip real statements so nothing reads as broken, and give the overview a distinct editorial voice under the title (the actual fix for "the title looks too similar to the rest") that Romane owns.
Output: new `editionsPage` singleton + data/config layer + shared-component fix + route wiring + unit & e2e coverage.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Sanity singleton pattern to mirror EXACTLY (intro-only, no seo):
@sanity/schemas/homePage.ts
@sanity/schemas/index.ts
@sanity/schemas/structure.ts

# Data + config layers to mirror (HomePage / getHomePage / resolveHomepageIntro):
@src/lib/sanity.ts
@src/lib/site-config.ts

# Component + routes + tests being changed:
@src/components/EditionsOverviewBody.astro
@src/pages/editions/index.astro
@src/pages/en/editions/index.astro
@tests/unit/site-config.test.ts
@tests/e2e/edition.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add editionsPage Sanity singleton (schema + registration + desk pin)</name>
  <files>sanity/schemas/editionsPage.ts, sanity/schemas/index.ts, sanity/schemas/structure.ts</files>
  <action>
Create `sanity/schemas/editionsPage.ts` mirroring `homePage.ts`'s shape EXACTLY, with two differences: (a) name/title/documentId are for editions, (b) NO seo field and NO seo group — Florian only asked for the intro text, and the route files keep hardcoding their own seoTitle/seoDescription (out of scope). Concretely: `defineType({ name: 'editionsPage', title: 'Page Éditions', type: 'document', ... })` with a single `groups: [{name: 'content', title: 'Contenu', default: true}]`, an `initialValue: {intro: defaultIntro}` where `defaultIntro` is the same fr/en placeholder object described below, and one required-both-locales `intro` object field using the identical locale-text-field pattern homePage.ts uses (an `object` field named `intro`, `group: 'content'`, `options: {columns: 2}`, containing two `text` sub-fields `fr` (title 'Français') and `en` (title 'Anglais'), `rows: 4`, each `validation: (rule) => rule.required()`). Give it a `preview.prepare()` returning a sensible title/subtitle like homePage's.

The `defaultIntro` placeholder copy — MUST be byte-identical to the `DEFAULT_EDITIONS_INTRO` values added in Task 2 so Studio and the code fallback read the same until Romane edits it:
- fr: "Les Éditions sont les objets imprimés — zines, livrets, tirages en petite série — qui prolongent le travail photographique de Romane Lepont sous une autre forme."
- en: "Éditions are the printed objects — zines, booklets, small-run prints — that extend Romane Lepont's photographic work into another form."
This copy is deliberately EDN-06-clean (it carries no commerce-affordance wording at all); do not alter it in a way that introduces commerce vocabulary.

Register it in `sanity/schemas/index.ts`: import `{editionsPage}` and add it to the `schemaTypes` array right after `homePage` (mirroring how homePage sits in the list).

Pin it as a singleton in `sanity/schemas/structure.ts`: copy the existing `homePage` `S.listItem()` block verbatim, changing only id/title/schemaType/documentId to `editionsPage` (title e.g. 'Page Éditions'), placing it right after the homePage entry; pick an existing icon already imported there (e.g. BookIcon is already imported and used for editions, or reuse a suitable one) — do not add a new icon import if an already-imported one fits. Also add `'editionsPage'` to the exclusion array in the trailing `S.documentTypeListItems().filter(...)` so it is not listed twice.

Do NOT run `sanity deploy`, and do NOT create/seed the live `editionsPage` document — that is handled outside this task (write-token step), exactly as siteSettings/homePage were seeded in earlier phases.
  </action>
  <verify>
    <automated>npm --prefix sanity run lint && npm --prefix sanity run build</automated>
  </verify>
  <done>Sanity Studio lints and builds clean; `editionsPage` is a registered document type, pinned as a fixed-documentId singleton in the desk structure, excluded from the generic type list, with an intro-only content group and no seo field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Data-fetch + config layer for the editions intro (mirror homePage exactly) + unit tests</name>
  <files>src/lib/sanity.ts, src/lib/site-config.ts, tests/unit/site-config.test.ts</files>
  <behavior>
    - resolveEditionsIntro(null, 'fr') returns DEFAULT_EDITIONS_INTRO.fr (the placeholder copy); resolveEditionsIntro(null, 'en') returns the en placeholder.
    - resolveEditionsIntro({intro: {fr: 'Texte éditable'}}, 'fr') returns 'Texte éditable' (populated Sanity value wins).
    - resolveEditionsIntro({intro: {fr: 'Texte éditable'}}, 'en') falls back to DEFAULT_EDITIONS_INTRO.en (missing locale falls back), matching resolveHomepageIntro's `|| default` shape.
  </behavior>
  <action>
In `src/lib/sanity.ts`, mirror the HomePage trio exactly: add `export interface EditionsPage { intro?: Partial<LocaleString> }` (no seo — editionsPage has no seo group), add `const EDITIONS_PAGE_QUERY = /* groq */ \`*[_id == "editionsPage"][0]{ intro }\`` (identical form to HOME_PAGE_QUERY, just the id and the single `intro` projection), and add `export async function getEditionsPage(): Promise<EditionsPage | null> { const result = await sanityClient.fetch<EditionsPage | null>(EDITIONS_PAGE_QUERY); return result ?? null }` (identical to getHomePage). Place them adjacent to the HomePage/getHomePage definitions for locality.

In `src/lib/site-config.ts`, mirror the homepage intro pair: import the `EditionsPage` type alongside `HomePage` from `./sanity`; add `const DEFAULT_EDITIONS_INTRO: Record<Locale, string>` with the two placeholder strings from Task 1 (byte-identical); add `export function resolveEditionsIntro(page: EditionsPage | null, locale: Locale) { return page?.intro?.[locale] || DEFAULT_EDITIONS_INTRO[locale] }` (identical shape to resolveHomepageIntro).

In `tests/unit/site-config.test.ts`, add `resolveEditionsIntro` to the import from `../../src/lib/site-config`, and add tests mirroring the existing `resolveHomepageIntro` cases: one asserting the fr fallback contains a stable substring of the placeholder (e.g. `'objets imprimés'`), one asserting the en fallback contains `'printed objects'`, and one asserting a populated `{intro: {fr: 'Texte éditable'}}` returns `'Texte éditable'` for 'fr' while 'en' still falls back to the default. Keep them in a small `describe('resolveEditionsIntro', ...)` (or extend the existing describe) — follow the file's existing style.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:unit</automated>
  </verify>
  <done>typecheck clean; new unit tests pass; getEditionsPage/EditionsPage/EDITIONS_PAGE_QUERY exist and mirror the HomePage trio; resolveEditionsIntro/DEFAULT_EDITIONS_INTRO mirror the homepage intro pair; populated value wins, missing locale falls back to default in both locales.</done>
</task>

<task type="auto">
  <name>Task 3: Component fix (grid-rows statement reveal + hairline removal + editable intro render) + route wiring + anti-truncation e2e</name>
  <files>src/components/EditionsOverviewBody.astro, src/pages/editions/index.astro, src/pages/en/editions/index.astro, tests/e2e/edition.spec.ts</files>
  <action>
**A. Fix statement truncation in `EditionsOverviewBody.astro` (grid-rows reveal, not a bigger magic number):**
- Markup: wrap the existing `<p class="editions-index__statement">{tile.statement}</p>` in a new `<div class="editions-index__statement-wrap">…</div>`.
- Add CSS: `.editions-index__statement-wrap { display: grid; grid-template-rows: 0fr; overflow: hidden; transition: grid-template-rows 0.3s ease; }`.
- On the existing reveal triggers — the `.editions-index__row:hover` block AND the `.editions-index__row:focus-visible` block — replace the current `.editions-index__statement { max-height: 80px; opacity: 1 }` rules so that the WRAPPER opens (`.editions-index__statement-wrap { grid-template-rows: 1fr; }`) while the statement keeps `opacity: 1`. Keep the same two trigger selectors; only change what they set.
- On `.editions-index__statement` itself: add `min-height: 0;` (required so the grid item can collapse below its intrinsic content height), remove the `max-height: 0` declaration, and reduce its `transition` to just `opacity 0.3s ease` (drop the max-height and margin-top transition parts — the wrapper now owns the height animation). Keep its `opacity: 0` default, `max-width: 46ch`, `line-height`, `margin`, and `color`.
- Mobile `@media (max-width: 800px)`: currently forces `.editions-index__statement { max-height: none; opacity: 1 }` — change to force the wrapper open (`.editions-index__statement-wrap { grid-template-rows: 1fr; }`) and keep `.editions-index__statement { opacity: 1; }`. The panel `display: none` in that media query stays.

**B. Remove the redundant hairline:** delete the entire `.editions-index__row:first-of-type { border-top: var(--border-hairline) solid var(--color-ink); }` rule. Do NOT touch `.editions-list__header`'s own `border-top` (shared chrome with About/Contact — that hairline stays).

**C. Add the editable intro paragraph:** add `intro: string` to the `Props` interface (the ONLY additive change to Props/EditionTile — leave everything else unchanged) and destructure it. Render `<p class="editions-list__intro">{intro}</p>` inside `.editions-list__header`, directly after `<h1>{heading}</h1>`. Style `.editions-list__intro` as body text deliberately distinct from the display-font H1 and titles: default body font (do NOT set `font-family: var(--font-display)`), `max-width: 60ch`, `margin: var(--space-md) 0 0`, `line-height: 1.5`, and a muted-but-legible color `#3a3a3a`. This distinct typographic voice is the actual fix for "the title looks too similar to the rest". Astro escapes `{intro}` as text by default — render it as text content, never as raw HTML.

**D. Wire both routes:** in `src/pages/editions/index.astro` and `src/pages/en/editions/index.astro`, import `getEditionsPage` from the sanity lib and `resolveEditionsIntro` from site-config (respecting each file's relative import depth — the fr route is one level shallower than the en route), fetch `const editionsPage = await getEditionsPage();`, compute `const intro = resolveEditionsIntro(editionsPage, locale);` (each file already defines `locale`), and pass `intro={intro}` to `<EditionsOverviewBody>`. Do not touch the existing seoTitle/seoDescription or any other prop.

**E. e2e anti-truncation assertion:** in `tests/e2e/edition.spec.ts`, in the `describe('editions overview layout')` hover test, keep the existing `await expect(firstRow.locator('.editions-index__statement')).toBeVisible()` assertion unchanged. After the existing `await secondRow.hover();` (inside the fr+en loop), add ONE assertion proving the second row's full statement is genuinely rendered at intrinsic height, not clipped to the old 80px box — use a retrying poll to accommodate the 0.3s reveal transition: `await expect.poll(async () => (await secondRow.locator('.editions-index__statement').boundingBox())?.height ?? 0).toBeGreaterThan(80);`. This would have FAILED under the old `max-height: 80px` clip (box capped at 80) and PASSES with the grid-rows reveal (box grows to the statement's true height, which for the real long statements exceeds 80px). Do not hardcode a slug or exact string; the boundingBox height check is content-length-agnostic beyond the >80 floor.

Keep the intro/placeholder copy EDN-06-clean; `npm run test:artifact` and the EDN-06 e2e checks run in verification and must stay green.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run test:artifact && npx playwright test tests/e2e/edition.spec.ts</automated>
  </verify>
  <done>Statement wrap uses grid-template-rows 0fr→1fr on the same hover+focus-visible triggers and is forced open on mobile; the first-row top-border hairline is gone (page-top header hairline intact); a body-typography `.editions-list__intro` renders under the H1 on both locales, sourced via getEditionsPage→resolveEditionsIntro with DEFAULT_EDITIONS_INTRO fallback; both routes pass the intro prop; the new e2e poll asserts the second row's statement exceeds 80px on hover; typecheck/build/artifact and the full edition e2e spec pass; EDN-06 guard clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Content Lake (build-time) → static HTML | Romane-authored `editionsPage.intro` text is fetched at build and rendered into the page. No request-time compute; no user input at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-el6-01 | Tampering/Injection | `.editions-list__intro` rendering Sanity `intro` text | low | mitigate | Rendered via Astro `{intro}` text interpolation, which HTML-escapes by default — no `set:html`/raw HTML path introduced. CMS content is authored only by the trusted site owner. |
| T-el6-02 | Information Disclosure | `EDITIONS_PAGE_QUERY` GROQ fetch | low | accept | Fixed singleton query with no user-controlled parameter (`_id == "editionsPage"` is a literal); build-time only, published perspective. No injection surface. |
| T-el6-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this task — no new dependencies added to root or `sanity/`; supply-chain surface unchanged. |
</threat_model>

<verification>
Full gate (matches the task brief):

1. `npm run typecheck` — Astro/TS clean.
2. `npm run test:coverage` — Vitest unit tests incl. new resolveEditionsIntro cases pass coverage thresholds.
3. `npm run build` — 27-page static build succeeds; both editions overview routes render the intro.
4. `npm run test:artifact` — EDN-06 static-artifact commerce-token guard stays clean.
5. `npx playwright test tests/e2e/edition.spec.ts` — targeted spec, incl. the new anti-truncation poll and the unchanged hover/EDN-06 assertions.
6. Full e2e suite — no regressions across other pages.
7. `npm --prefix sanity run lint && npm --prefix sanity run build` — Studio registers/pins the new singleton without error.
</verification>

<success_criteria>
- Hovering the real long "Silos" statement shows the complete text with no mid-sentence clip (reveal animates to intrinsic height).
- The Éditions overview renders a Sanity-editable body-typography intro paragraph under the H1 on both `/editions/` and `/en/editions/`, with correct fr/en placeholder copy even before the `editionsPage` document is seeded.
- The redundant first-row hairline under the H1 is removed; the shared page-top header hairline is untouched.
- `editionsPage` is a registered, desk-pinned Sanity singleton (intro-only, no seo).
- All verification gates pass; EDN-06 stays clean.
</success_criteria>

<output>
Create `.planning/quick/260728-el6-fix-ditions-overview-statement-text-trun/260728-el6-SUMMARY.md` when done.
</output>
