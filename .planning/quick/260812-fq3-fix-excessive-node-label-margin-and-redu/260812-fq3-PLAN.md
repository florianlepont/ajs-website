---
phase: quick-260812-fq3
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/EditorialDashboard.css
  - sanity/editorial/deployment.ts
  - tests/unit/editorial-dashboard-css.test.ts
  - tests/unit/deployment.test.ts
autonomous: true
requirements:
  - QUICK-260812-fq3
must_haves:
  truths:
    - "`.editorial-dashboard__pipeline-node-label` no longer reserves a second line of vertical space. The gap between each pipeline node's label and the small grey detail line under it is now the label's own `margin-top`/`line-height` box plus the detail's `margin-top: 4px` — nothing more."
    - "`text-wrap: balance` survives on that same rule as the remaining safety net if a label ever does wrap at some viewport."
    - "The `.editorial-dashboard__pipeline-node` width (152px base, 116px under `@media (max-width: 36em)`) is unchanged, and no other rule in `EditorialDashboard.css` is touched — the diff on that file is exactly one deleted line and zero added lines."
    - "In `resolvePromoteRow()`, the `segments.content !== 'done'` branch keeps its distinct title « Rien n’a encore été lancé. » — it still must not describe a wait, which is the entire reason that branch exists."
    - "That branch's `detail` is now the single sentence « L’étape 2 sera disponible une fois le site de test à jour. » It no longer re-instructs the maintainer to press the panel's own button, because that button and its label are already visible directly above this text."
    - "The neighbouring `segments.staging !== 'done'` branch (title « En attente du site de test… ») is byte-identical to before — it is the genuine in-flight wait and was not in scope."
    - "Every other branch of `resolvePromoteRow()` (requestError, production active/failed/done, staging failed, the final ready return) and the whole segment-kind computation (`deploymentSegmentKind`, `releasePipelineState`, `pipelineDisplaySegments`, `isProductionReleaseStale`) are byte-identical to before."
    - "The two guard tests that asserted the OLD behaviour — the CSS test demanding a `min-height` on the label, and the deployment test asserting the detail contains the button instruction — now assert the NEW behaviour instead of being deleted."
    - "`npm run test:unit`, `npm run typecheck`, `npm --prefix sanity run lint` and `npm --prefix sanity run build` all pass."
    - "`git diff --stat` proves `sanity/editorial/dashboardLogic.ts`, `sanity/editorial/EditorialDashboard.tsx` and `.github/workflows/` were never touched."
    - "The rebuilt Studio is deployed and the user has visually re-confirmed both fixes on the live Studio."
  artifacts:
    - sanity/editorial/EditorialDashboard.css
    - sanity/editorial/deployment.ts
    - tests/unit/editorial-dashboard-css.test.ts
    - tests/unit/deployment.test.ts
  key_links:
    - "The reserved two-line label height was added by quick task 260812-f22 *together with* the 104px→152px node widening, as belt-and-braces against a label/detail collision. The widening alone already solved it (confirmed on a live screenshot), so the reserved height is now pure surplus gap. This is why the width must NOT be reduced in the same change: the width is now the sole thing keeping « Contenu + site de test » on one line."
    - "Removing the reserved height means the two nodes' detail lines only stay on a shared baseline while BOTH labels occupy the same number of lines. At 152px both are one line. Under `@media (max-width: 36em)` the node is 116px, where a two-line wrap is conceivable — that is the one visual regression risk in this plan and is exactly what the narrow-width half of the human-check exists to catch."
    - "`extractRuleBlocks(source, '.editorial-dashboard__pipeline-node')` in the CSS test returns the base rule AND its narrow-viewport override, but does NOT match `-node-label` / `-node-detail`, because its generated regex requires optional whitespace then `{` directly after the selector. The label test must therefore query `'.editorial-dashboard__pipeline-node-label'` explicitly, and a file-wide `min-height` grep is NOT a valid gate — that property legitimately appears five other times in this stylesheet."
    - "After this change the two adjacent branches of `resolvePromoteRow()` return the same `detail` string and differ only in `title`. That is deliberate: the title carries the not-started-vs-waiting distinction. The explanatory comment above them must say so, or a later reader will re-merge the branches and reintroduce the exact bug 260812-f22 fixed."
    - "`tests/unit/deployment.test.ts` already asserts the neighbouring branches' exact titles (« En attente du site de test… », « Site de test à jour — prêt à publier ? »). Running that file is therefore itself the proof that those branches survived untouched — no extra grep needed."
---

<objective>
Two small, fully-decided follow-up fixes to the dashboard pipeline UI shipped by quick task 260812-f22, both found by the user on the live Studio:

1. The pipeline node label reserves a second line of height it no longer needs (the node was widened to 152px in the same task, so the label fits on one line), producing an oversized, unbalanced gap before the detail text under it.
2. The not-started message under the pipeline repeats the instruction to press « Mettre le site à jour » — redundant with the panel's own header and button already visible directly above it.

Purpose: 260812-f22 fixed a real collision and a real copy bug, but overshot on both — one with belt-and-braces CSS that is now visible surplus, one with copy that duplicates chrome the reader can already see. Both remedies are decided; there is no design question left open.

Output: one deleted CSS declaration, one shortened detail string, the two guard tests that locked in the old behaviour re-pointed at the new one, green gates, a redeployed Studio, and a human visual confirmation.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@sanity/editorial/EditorialDashboard.css
@sanity/editorial/deployment.ts
@tests/unit/editorial-dashboard-css.test.ts
@tests/unit/deployment.test.ts
@.planning/quick/260812-f22-fix-4-issues-in-the-new-dashboard-pipeli/260812-f22-SUMMARY.md
</context>

<scope_boundary>
Do NOT modify:

- `sanity/editorial/EditorialDashboard.tsx` — nothing in this plan is a markup change. Both fixes are one CSS declaration and one string.
- `sanity/editorial/dashboardLogic.ts` — including `publicationCardState()`, `buildProductionReleaseMarkerActions()`, `triggerProductionRelease()`, the publication controller.
- `.github/workflows/` — anything.
- In `deployment.ts`: `deploymentSegmentKind()`, `releasePipelineState()`, `pipelineDisplaySegments()`, `isProductionReleaseStale()`, `deploymentState()`, `deploymentSubtitle()`, and every branch of `resolvePromoteRow()` other than the single `segments.content !== 'done'` branch named in Task 2 — in particular the `segments.staging !== 'done'` branch immediately below it, which is the genuine in-flight wait and is NOT redundant.
- In `EditorialDashboard.css`: the `.editorial-dashboard__pipeline-node` width (base or narrow-viewport override), `.editorial-dashboard__pipeline-node-detail`, and every other rule. Exactly one declaration is deleted in this whole stylesheet.

Do NOT delete either of the two guard tests being updated. They were written by 260812-f22 to lock in behaviour that is now changing; re-point them at the new behaviour with an explanation of why it changed. A deleted guard is an untested surface.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Drop the reserved second line on the pipeline node label</name>
  <files>sanity/editorial/EditorialDashboard.css, tests/unit/editorial-dashboard-css.test.ts</files>
  <action>
In `sanity/editorial/EditorialDashboard.css`, the rule `.editorial-dashboard__pipeline-node-label` currently declares, in order: `font-size: 11px`, `font-weight: 700`, `line-height: 14px`, `margin-top: 8px`, a `min-height` of 28px (two lines at the declared line-height), and `text-wrap: balance`.

Delete the `min-height` declaration only. Every other declaration in that rule stays exactly as-is — `text-wrap: balance` in particular is kept, since it is the remaining safety net if the label ever does wrap at some viewport.

Rationale to carry into the commit message (not into the stylesheet): 260812-f22 widened `.editorial-dashboard__pipeline-node` from 104px to 152px AND reserved a two-line label box in the same change. The widening alone is what fixed the collision — at 152px both « Contenu + site de test » and « Site en ligne » fit on one line — so the reserved height now only adds an empty second line's worth of gap between every label and its detail line, which reads as an unbalanced hole. A live screenshot confirmed this.

Touch nothing else in this stylesheet. Do not adjust the node width, the label `margin-top`, or the detail `margin-top` to compensate — the resulting spacing is the intended target, and the human-check in Task 3 is what confirms it.

Then, in `tests/unit/editorial-dashboard-css.test.ts`, re-point the existing `it('reserves a two-line box on the pipeline node label …')` case (the last case in the describe block) rather than deleting it. Rename it to describe the new intent — that the label is left at its natural single-line height so the gap to the detail line stays tight — and replace its body with three assertions:

  1. `extractRuleBlocks(source, '.editorial-dashboard__pipeline-node-label')` returns exactly one block, matching the same ambiguity guard the `.editorial-dashboard__pipeline` case at the top of the file already uses. A second override block elsewhere in the file could otherwise reintroduce the reserved height without failing this test.
  2. That block declares NO `min-height` at all — assert `/min-height/.test(labelBlock)` is `false`, with a failure message explaining that the 152px node column already keeps both labels on one line, so reserving a second line's height only produces a visible empty gap before the detail text.
  3. That block still declares `text-wrap: balance`, with a failure message explaining it is the remaining safety net for a wrap at some narrower viewport.

Note for the executor: a file-wide `min-height` grep is NOT a valid check here — that property legitimately appears five other times in this stylesheet (page shell, buttons, inputs). The assertion must be scoped to the extracted label rule block, which is what `extractRuleBlocks` gives you.

Leave the two cases above it (the pipeline row width/flex case and the node-column width case) untouched — they still describe current, correct behaviour.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-css.test.ts</automated>
    <automated>git diff --numstat sanity/editorial/EditorialDashboard.css</automated>
  </verify>
  <done>The label rule keeps its type scale, its `margin-top` and `text-wrap: balance`, with the reserved-height declaration gone. `git diff --numstat` on the stylesheet reports exactly `0` added and `1` deleted line, proving no other rule moved. The re-pointed CSS guard test passes and now fails if the reserved height comes back, if a second label rule block appears, or if `text-wrap: balance` is dropped.</done>
</task>

<task type="auto">
  <name>Task 2: Shorten the not-started detail so it stops repeating the button above it</name>
  <files>sanity/editorial/deployment.ts, tests/unit/deployment.test.ts</files>
  <action>
In `sanity/editorial/deployment.ts`, `resolvePromoteRow()` has an `if (segments.content !== 'done')` branch (added by 260812-f22, sitting after the requestError / production-active / production-failed / production-done / staging-failed branches, immediately before the `if (segments.staging !== 'done')` branch). Change exactly one field in the object it returns:

- Keep `title: 'Rien n’a encore été lancé.'` verbatim. This branch exists precisely so the copy does NOT describe a wait — the title must never become « En attente… » or any waiting phrasing.
- Keep `buttonLabel: 'Publier sur le site en ligne'`, `buttonDisabled: true`, `dimmed: true` verbatim. The gate's lock behaviour is not in scope.
- Replace `detail` — currently a two-clause sentence telling the maintainer to start with the panel's own update button above and describing what that will do — with the single sentence:

      'L’étape 2 sera disponible une fois le site de test à jour.'

  (curly apostrophe U+2019, exactly as written; this is byte-identical to the `detail` the `segments.staging !== 'done'` branch below already uses.)

  The old text was redundant: the panel's header and its update button are rendered directly above this detail block, so the instruction to press it is already on screen. The new sentence instead states what step 2 is waiting on, matching the phrasing style used by the other branches of this same function.

Then amend the explanatory comment block directly above the `if (segments.content !== 'done')` branch. It currently explains why the two situations were split into two branches. Extend it to also record that the two branches now return the SAME `detail` and differ only in `title`, that this is deliberate (the title carries the not-started-vs-waiting distinction), and that they must not be re-merged on the grounds of looking near-identical — re-merging reintroduces the misleading-wait bug 260812-f22 fixed. Keep the existing sentences about why the not-started copy must not describe a wait; they are still the load-bearing rationale.

Do not touch the `segments.staging !== 'done'` branch below, the final ready `return`, any branch above, or anything outside `resolvePromoteRow()`.

Then, in `tests/unit/deployment.test.ts`, update the case named `'with unpublished drafts pending, tells the maintainer nothing has started and points at « Mettre le site à jour »'`:

  - Rename it so the name no longer claims the copy points at the panel button — say instead that it tells the maintainer nothing has started and names what step 2 is waiting on.
  - Keep the `expect(result.segments.content).toBe('pending')`, `dimmed`, and `buttonDisabled` assertions as they are.
  - Keep the exact-title assertion as it is — that title is the whole point of the branch.
  - Replace the `detail` assertion: instead of a `toContain` on the button label, assert the exact new sentence with `toBe`, and add a `not.toContain` assertion on the button label proving the instruction is no longer duplicated there. Add an inline comment on that negative assertion explaining that the panel's header and button render directly above this text, so repeating them is noise.

Leave every other case in that file untouched — in particular the two cases immediately below it, which already assert the neighbouring branches' exact titles and are therefore the proof those branches survived.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/deployment.test.ts</automated>
    <automated>git diff -U0 sanity/editorial/deployment.ts | grep '^@@'</automated>
  </verify>
  <done>The not-started branch keeps its distinct title and its dimmed/disabled gate state, with a one-sentence detail naming what step 2 waits on. The comment above it records why the two branches must stay split despite now sharing a detail. Every hunk header printed by the `git diff -U0` command falls inside the ~430–460 line range (the comment block plus that one branch) — no hunk anywhere else in the file. The deployment test file passes in full, which by itself proves the « En attente du site de test… » and « Site de test à jour — prêt à publier ? » branches are intact.</done>
</task>

<task type="auto">
  <name>Task 3: Run every gate, prove scope, redeploy the Studio, confirm visually</name>
  <files>(no source changes — verification, deploy and sign-off only)</files>
  <action>
Run the four blocking gates in this order, fixing anything they surface before moving on: `npm run test:unit` (full suite, not just the two files), `npm run typecheck`, `npm --prefix sanity run lint`, `npm --prefix sanity run build`.

Then prove the scope boundary held: `git diff --stat` across the working tree must list only `sanity/editorial/EditorialDashboard.css`, `sanity/editorial/deployment.ts`, `tests/unit/editorial-dashboard-css.test.ts`, `tests/unit/deployment.test.ts`, plus `.planning/` artefacts. `sanity/editorial/EditorialDashboard.tsx`, `sanity/editorial/dashboardLogic.ts` and `.github/workflows/` must be absent from it.

Then redeploy the live Studio with `npm run deploy --prefix sanity`. This is the established pattern for shipping a Studio change (same as 260812-f22 and 260812-dvq) and is expected and low-risk — do not stop and ask before running it. Wait for it to report success before presenting the human-check.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm --prefix sanity run build</automated>
    <automated>git diff --stat sanity/editorial/EditorialDashboard.tsx sanity/editorial/dashboardLogic.ts .github/workflows/</automated>
    <human-check>
The Studio has been redeployed. Open https://atelier-jacqueline-suzanne.sanity.studio/ and **hard-refresh** (Cmd+Shift+R) so the new bundle loads — this environment has no authenticated browser session, so only you can confirm the visual result.

In the « Mettre le site à jour » panel, check both fixes:

1. **Spacing** — under each pipeline node circle, the bold label now sits close to the small grey line beneath it; the oversized empty gap is gone. Confirm both labels (« Contenu + site de test » and « Site en ligne ») are still on ONE line each, and that the two nodes' grey detail lines therefore still sit at the same height as each other. Then narrow the window to phone width and look again: at that width the node column is 116px, and if a label wraps to two lines there, the two detail lines will no longer align — that is the one regression this change can cause, so please say so if you see it.

2. **Copy** — with at least one unpublished draft, the message under the pipeline should read « Rien n’a encore été lancé. » (unchanged) followed by a single short line, « L’étape 2 sera disponible une fois le site de test à jour. » It should no longer repeat the instruction to press « Mettre le site à jour », since that button is right above it. The button itself must still be visibly dimmed/disabled.

Reply « approuvé » if both look right, or describe what is off.
    </human-check>
  </verify>
  <done>All four gates pass, `git diff --stat` on the three forbidden paths returns empty, the Studio is redeployed, and the user's visual confirmation (or their correction list — especially any narrow-width label wrap) is recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npm run test:unit` — full suite green, including the re-pointed `editorial-dashboard-css.test.ts` label case and the updated `deployment.test.ts` not-started case.
- `npm run typecheck` — 0 errors (any pre-existing unrelated hint may remain).
- `npm --prefix sanity run lint` — clean.
- `npm --prefix sanity run build` — Studio bundles.
- `git diff --numstat sanity/editorial/EditorialDashboard.css` — exactly `0` added, `1` deleted.
- `git diff -U0 sanity/editorial/deployment.ts | grep '^@@'` — every hunk inside the not-started branch and its comment block.
- `git diff --stat sanity/editorial/EditorialDashboard.tsx sanity/editorial/dashboardLogic.ts .github/workflows/` — empty.
</verification>

<success_criteria>
- The pipeline node label no longer reserves a second line, so the gap to its detail line reads as balanced rather than as a hole — with the node width and `text-wrap: balance` untouched.
- The not-started message keeps its distinct, non-waiting title and gains a short detail that names what step 2 waits on instead of repeating a button already visible above it.
- The genuine in-flight-wait branch and all segment-kind computation are provably unchanged.
- Both guard tests were re-pointed at the new behaviour, not deleted.
- Every gate is green, scope is git-proven, the Studio is redeployed, and the user has signed off visually at both desktop and narrow width.
</success_criteria>

<output>
Create `.planning/quick/260812-fq3-fix-excessive-node-label-margin-and-redu/260812-fq3-SUMMARY.md` when done
</output>
