---
phase: quick-260801-d6m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/EditorialDashboard.tsx
  - sanity/editorial/EditorialDashboard.css
autonomous: true
requirements: [QUICK-260801-d6m]

must_haves:
  truths:
    - "The Sanity Studio dashboard no longer renders the three metric tiles (collections publiées / brouillons / contenus à vérifier)."
    - "The 'Mettre le site à jour' panel reads as a sober, refined surface with a thin indigo accent edge and a hairline divider, not a saturated solid indigo block."
    - "The publication panel behaves exactly as before: same button enable/disable/loading logic, same pairs list, same blocked/success/tracking-error/error states."
    - "'Mettre le site à jour' is the only solid primary call-to-action on the page; the header creation buttons are outlined/ghost."
    - "A 'Nouvelle édition' button sits next to 'Nouvelle collection' in the header and opens the Studio create intent for the edition document type."
    - "Sanity Studio lints and builds clean, with no dead code or dead CSS left behind."
  artifacts:
    - sanity/editorial/EditorialDashboard.tsx
    - sanity/editorial/EditorialDashboard.css
  key_links:
    - "IntentButton intent=create params.type=edition resolves in Studio without a registered template (unlike gallery, which passes template: 'gallery')."
    - "metricAccentStyles and TintChip must survive the metric-tile removal — they are still consumed by the publication panel, the blocking card, and toneChipStyles."
    - "Removing the tiles orphans the galleries/onlineGalleryCount locals and the isGalleryOnline import; sanity lint is the gate that catches them."
---

<objective>
Three visual refinements to the internal Sanity Studio editorial dashboard, requested by the user after reviewing live screenshots: drop the three useless metric tiles, modernize the heavy indigo "Mettre le site à jour" panel into something sober and refined, and fix the button hierarchy in the page header so the publish action is visibly the primary one.

Purpose: The dashboard is Romane's daily-use internal tool. The current visual weight is misleading — a draft-creation button looks exactly as important as publishing the whole public site — and three tiles of stats she never reads take up the top of the page.

Output: Modified `sanity/editorial/EditorialDashboard.tsx` and `sanity/editorial/EditorialDashboard.css`. No behavior change whatsoever, no schema change, no content mutation.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

Source files under change:
@sanity/editorial/EditorialDashboard.tsx
@sanity/editorial/EditorialDashboard.css

**Working agreement for this task:**
- Repo root: `/Users/florian/Projects/ajs-website`. Current branch: `codex/studio-publication-workflow` (PR #12 open, not merged). **Commit directly on this branch. Do NOT create a new branch.**
- A `sanity dev` server is already running on `localhost:3333` against the **real `production` dataset**. This is a presentation-only change: never publish, never mutate, never delete a document while verifying. Do not start a second Studio server on port 3333.
- Root `npm run lint` **ignores `sanity/`** (see `eslint.config.mjs` — `sanity/` is in `ignores`, it has its own eslint@9 toolchain). The meaningful lint gate for these two files is `npm --prefix sanity run lint`. Run the root gates too, as regression guards, but do not expect them to see these files.

**Verified facts about the current file (do not re-derive):**

| Symbol | Where used today | After this plan |
|---|---|---|
| `FolderIcon` | metric tile (line ~671) **and** `ShortcutRow` (line ~821) | KEEP the import — still used by ShortcutRow |
| `DocumentIcon` | metric tile only (line ~680) | import becomes dead |
| `WarningOutlineIcon` | metric tile only (line ~689) | import becomes dead |
| `AddIcon` | header "Nouvelle collection" (line ~442) | KEEP |
| `metricAccentStyles` | 9 lines — panel TintChip, `toneChipStyles`, MetricCard | KEEP (only the MetricCard consumer goes away) |
| `MetricAccent` type | type def + `Record<MetricAccent,…>` + MetricCard prop | KEEP type + Record; only the prop use goes away |
| `TintChip` | panel, blocking card, activity rows, MetricCard | KEEP |
| `galleries` (line 265) | only feeds `onlineGalleryCount` | becomes dead |
| `onlineGalleryCount` (line 266) | metric tile only | becomes dead |
| `isGalleryOnline` (import line 43) | only feeds `onlineGalleryCount` | becomes dead |
| `draftCount` (line 267) | also lines 277, 304, 327, 348-349 | KEEP |
| `listedAttention` | also lines 711-735 | KEEP |
| `pluralize` | many other call sites | KEEP |
| `.editorial-dashboard__surface` (CSS) | also tsx lines 736, 798, 807, 898 | KEEP the CSS rule |

**Test coverage:** `tests/unit/dashboard-logic.test.ts`, `editorial-checks.test.ts`, `deployment.test.ts`, `workflow-logic.test.ts` cover the *logic* modules (`dashboardLogic.ts`, `checks.ts`, `deployment.ts`) — none reference `EditorialDashboard.tsx` presentation, the metric CSS classes, or the button labels. Verified by grep. No tests to update, none to add.

**Baseline grep counts in `EditorialDashboard.tsx`** (used by the verify gates below):
`requestPublication` 3 · `refreshPublicationTracking` 2 · `publicationBusy` 10 · `publicationCard.` 9 · `IntentButton` 3 · `mode="ghost"` 0 · `BookIcon` 0
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the three metric tiles and everything they orphan</name>
  <files>sanity/editorial/EditorialDashboard.tsx, sanity/editorial/EditorialDashboard.css</files>
  <action>
Remove the `<div className="editorial-dashboard__metrics">` block (~lines 669-701) and its three child tiles from the `{!loading && !error && (<>…</>)}` fragment starting at line 667. The fragment must keep the `editorial-dashboard__columns` div that follows — only the metrics div goes. If removing the metrics div leaves the fragment wrapping a single child, collapsing the `<>…</>` is optional; prefer the minimal diff and leave the fragment in place.

Then delete every symbol that becomes unreachable:
  - the `MetricCard` function component (~lines 1005-1073) — grep-confirmed to have no other call site
  <!-- planner-discipline-allow: MetricCard -->
  - the `galleries` local (line 265) and the `onlineGalleryCount` local (line 266)
  <!-- planner-discipline-allow: onlineGalleryCount -->
  - the `isGalleryOnline` named import (line 43) from the `./dashboardLogic` import block
  - the `DocumentIcon` and `WarningOutlineIcon` named imports from the `@sanity/icons` block (lines 11 and 17)
  <!-- planner-discipline-allow: DocumentIcon -->
  <!-- planner-discipline-allow: WarningOutlineIcon -->

Do NOT touch: `FolderIcon` (still used by ShortcutRow), `draftCount`, `listedAttention`, `pluralize`, `TintChip`, `metricAccentStyles`, the `MetricAccent` type, or the `Record<MetricAccent, …>` declaration.

In `EditorialDashboard.css`, delete the five now-dead rules covering the metrics grid, the metric cell, the metric card surface, the interactive-cell variant, and its hover + focus-visible states (lines 48-84). Also delete the entire `@media (min-width: 40em)` block (lines 273-277) — the metrics grid rule is its only content, so the media query becomes empty. Keep `.editorial-dashboard__surface` (lines 9-11) and the `@media (max-width: 36em)` and `@media (min-width: 64em)` blocks untouched.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm --prefix sanity run lint</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -v '^\s*//' sanity/editorial/EditorialDashboard.tsx | grep -c 'editorial-dashboard__metric')" = "0" && test "$(grep -v '^\s*/\*' sanity/editorial/EditorialDashboard.css | grep -c 'editorial-dashboard__metric')" = "0" && echo METRIC_CSS_AND_MARKUP_GONE</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -v '^\s*//' sanity/editorial/EditorialDashboard.tsx | grep -c 'MetricCard')" = "0" && echo COMPONENT_GONE</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -c 'metricAccentStyles' sanity/editorial/EditorialDashboard.tsx)" -ge 8 && test "$(grep -c 'FolderIcon' sanity/editorial/EditorialDashboard.tsx)" -ge 2 && test "$(grep -c 'editorial-dashboard__surface' sanity/editorial/EditorialDashboard.css)" -ge 1 && echo KEEPERS_INTACT</automated>
    <automated>cd /Users/florian/Projects/ajs-website && npm --prefix sanity run build</automated>
  </verify>
  <done>The dashboard renders no metric tiles. `npm --prefix sanity run lint` reports zero errors — in particular zero unused-variable/unused-import errors, which is the gate proving every orphaned symbol was removed. `npm --prefix sanity run build` succeeds. The publication panel, blocking card, "À faire maintenant" list, activity list, and shortcuts all still render (TintChip and metricAccentStyles survived).</done>
</task>

<task type="auto">
  <name>Task 2: Restyle the "Mettre le site à jour" panel — sober surface, accent edge, hairline divider</name>
  <files>sanity/editorial/EditorialDashboard.tsx, sanity/editorial/EditorialDashboard.css</files>
  <action>
Target the `<Card radius={3} tone="primary" shadow={2} …>` opened at line ~482 inside `{!loading && !error && (…)}` — the publication panel. This is a **presentation-only** change: every conditional branch, handler, prop, and piece of copy inside it stays byte-identical in behavior.

**On the Card element itself:**
  - Drop the saturated fill: change the Sanity tone from primary to `transparent`.
  - Drop the inline `style={{border: '1px solid var(--card-border-color)'}}` — the border now comes from CSS.
  - Soften the elevation: `shadow={1}` instead of `shadow={2}` (the CSS rule supplies the real, softer shadow).
  - Give it more breathing room: `padding={[4, 4, 5]}` instead of `padding={[3, 4]}`.
  - Add `className="editorial-dashboard__publish-panel"`.

**In `EditorialDashboard.css`,** add a `.editorial-dashboard__publish-panel` rule in the same neighbourhood the deleted metric rules occupied. It must:
  - declare a local custom property for the accent colour set to the same indigo already hard-coded as the `primary` entry of `metricAccentStyles` in the TSX — `#556bfc` — so the panel edge and the PublishIcon chip read as one accent family;
  - paint the surface with `var(--dashboard-surface-background)` (the neutral card surface already defined on `.editorial-dashboard__page`), so the panel sits cleanly above the page background instead of tinting it;
  - draw a hairline border on all sides using `color-mix(in srgb, var(--card-fg-color) 9%, transparent)`;
  - override the left edge with a 3px solid rule in the accent custom property — this is the "thin accent-coloured rule" the user asked for;
  - apply a soft, low-opacity two-layer shadow (a 1px contact shadow plus a wide, heavily-negative-spread ambient shadow) rather than a hard drop shadow.

Also add a `.editorial-dashboard__publish-divider` rule: a 1px-tall, full-width block using `color-mix(in srgb, var(--card-fg-color) 10%, transparent)` as its background, with `flex: 0 0 auto` so the surrounding Stack cannot collapse it.

**Render the divider in the TSX,** inside the existing `<Stack space={4}>`, immediately after the closing tag of the header `<Flex>` (the one containing the TintChip + Heading + status Text + the publish Button) and before the `publicationCard.pairs.length > 0` block. Render it as a `<Box className="editorial-dashboard__publish-divider" />`.

The divider must appear only when there is content below it. Compute a single boolean local immediately above the returned JSX for that block (or inline in the JSX guard — your choice, but define it once), true when any of these hold: the pairs list is non-empty, the blocked-rows list is non-empty, the publication phase is `success` with a `publishedAt`, the phase is `tracking-error`, or the phase is `error`. Reuse the exact same expressions the existing branches already use — do not introduce a new source of truth for any of them.

**Keep exactly as-is:** the `TintChip` with `PublishIcon` and its `metricAccentStyles.primary` tint, all `Heading`/`Text` copy and sizes, the publish `Button` (`tone="primary"`, its `text` ternary, `disabled={publicationCard.buttonDisabled}`, `loading={publicationBusy}`, `onClick`, `minHeight: 44`), the pairs `IntentLink`s and their `Badge` tones, the blocked-rows critical Card, the success text, and the `tracking-error` / `error` Cards with their "Actualiser le suivi" / "Actualiser et réessayer" buttons. The publish Button stays solid primary — after Task 3 it is the only solid CTA on the page, which is the point.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm --prefix sanity run lint && npm --prefix sanity run build</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -c 'editorial-dashboard__publish-panel' sanity/editorial/EditorialDashboard.css)" -ge 1 && test "$(grep -c 'editorial-dashboard__publish-divider' sanity/editorial/EditorialDashboard.css)" -ge 1 && test "$(grep -c 'editorial-dashboard__publish-panel' sanity/editorial/EditorialDashboard.tsx)" = "1" && test "$(grep -c 'editorial-dashboard__publish-divider' sanity/editorial/EditorialDashboard.tsx)" = "1" && echo PANEL_CLASSES_WIRED</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -c '556bfc' sanity/editorial/EditorialDashboard.css)" -ge 1 && echo ACCENT_MATCHES_CHIP</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -c 'requestPublication' sanity/editorial/EditorialDashboard.tsx)" = "3" && test "$(grep -c 'refreshPublicationTracking' sanity/editorial/EditorialDashboard.tsx)" = "2" && test "$(grep -c 'publicationBusy' sanity/editorial/EditorialDashboard.tsx)" = "10" && test "$(grep -c 'publicationCard\.' sanity/editorial/EditorialDashboard.tsx)" -ge 9 && test "$(grep -c "publicationState.phase === " sanity/editorial/EditorialDashboard.tsx)" -ge 3 && echo BEHAVIOUR_UNCHANGED</automated>
    <human-check>In the already-running Studio at http://localhost:3333 (do NOT publish or mutate anything), confirm the panel now reads as a light neutral card with a thin indigo left edge and a soft shadow — not a saturated indigo block — that the PublishIcon chip still reads as part of the same accent family, that the hairline divider separates the header row from the pending-content list, and that the "Mettre le site à jour" button is unchanged in appearance and still correctly enabled/disabled for the current content state.</human-check>
  </verify>
  <done>The panel is a neutral surface with a 3px `#556bfc` left rule, hairline border, and soft two-layer shadow. A hairline divider appears between the header row and the body only when the body has content. All five behavioural grep invariants hold at their baseline counts, meaning no handler, guard, or state branch was disturbed. Lint and Studio build pass.</done>
</task>

<task type="auto">
  <name>Task 3: De-emphasize "Nouvelle collection" and add a "Nouvelle édition" sibling</name>
  <files>sanity/editorial/EditorialDashboard.tsx</files>
  <action>
In the header `<Flex className="editorial-dashboard__actions editorial-dashboard__header-side">` (~lines 419-450):

1. On the existing "Nouvelle collection" `IntentButton` (~line 439), change `mode="default"` to `mode="ghost"`. Keep `tone="primary"` (the accent colour is wanted, the solid fill is not), and keep `icon={AddIcon}`, `intent="create"`, `params={{type: 'gallery', template: 'gallery'}}`, `className`, `style={{height: 44}}`, and `paddingY={3}` exactly as they are.

2. Add a second `IntentButton` immediately after it, inside the same Flex, with the identical treatment: `className="editorial-dashboard__header-control"`, `style={{height: 44}}`, `intent="create"`, `tone="primary"`, `mode="ghost"`, `paddingY={3}`, `text="Nouvelle édition"`, and `icon={BookIcon}`.

3. Its params are `{type: 'edition'}` with **no `template` key**. This is deliberate and load-bearing: `sanity.config.ts` registers an initial-value template only for `gallery` (lines 52-60); passing a template id that is not registered makes the create intent fail to resolve. Do not mirror the gallery params here.

4. Add `BookIcon` to the existing `@sanity/icons` named-import block, keeping that block's alphabetical order (it belongs between `AddIcon` and `CheckmarkCircleIcon`). `BookIcon` is the icon already used for "Éditions" in `sanity/schemas/structure.ts`, so Studio navigation and dashboard stay visually consistent.

No CSS change is needed: `.editorial-dashboard__header-control` already pins the 44px height, and the existing `@media (max-width: 36em)` block already stretches these controls to full width — with two buttons they simply stack, each keeping its 44px tap target.

Leave the "Ouvrir le site" bleed link and the `<DeploymentStatus>` element untouched.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm --prefix sanity run lint && npm --prefix sanity run build</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -c 'BookIcon' sanity/editorial/EditorialDashboard.tsx)" = "2" && test "$(grep -c 'Nouvelle édition' sanity/editorial/EditorialDashboard.tsx)" = "1" && test "$(grep -c 'IntentButton' sanity/editorial/EditorialDashboard.tsx)" = "4" && echo EDITION_BUTTON_ADDED</automated>
    <automated>cd /Users/florian/Projects/ajs-website && test "$(grep -c 'mode="ghost"' sanity/editorial/EditorialDashboard.tsx)" = "2" && test "$(grep -c "template: 'edition'" sanity/editorial/EditorialDashboard.tsx)" = "0" && echo HIERARCHY_AND_PARAMS_CORRECT</automated>
    <human-check>In the running Studio at http://localhost:3333, confirm both header creation buttons render as outlined/accented (not solid), that "Mettre le site à jour" is now the only solid CTA on the page, that clicking "Nouvelle édition" opens a new empty `edition` document form without an error, and that at a narrow window width both buttons stack full-width and keep a comfortable tap target. Close the new document WITHOUT publishing.</human-check>
  </verify>
  <done>"Nouvelle collection" and "Nouvelle édition" both render as ghost/outlined primary buttons in the header; "Mettre le site à jour" is the sole solid primary CTA. "Nouvelle édition" opens the create intent for the `edition` type with no template param and no resolution error. Lint and Studio build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio user → Sanity Content Lake | Authenticated editor actions (create/publish intents) cross into the real `production` dataset |
| Developer verification session → live dataset | The `sanity dev` server used to eyeball this change is pointed at real, published content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260801-01 | Tampering | Verification against the live `production` dataset via `sanity dev` | medium | mitigate | Verification is read-only by construction: every `<human-check>` explicitly forbids publishing or mutating, and the "Nouvelle édition" check requires closing the new document without publishing. No task writes content. |
| T-260801-02 | Elevation of Privilege | `IntentButton intent="create" params={{type: 'edition'}}` | low | accept | Studio create intents are resolved and authorized server-side by Sanity against the caller's role; adding a client-side button grants no capability an editor did not already have via the "Éditions" structure pane. |
| T-260801-03 | Denial of Service | Publication panel restyle disturbing `buttonDisabled`/`loading` guards | medium | mitigate | Task 2 forbids touching any handler or guard and enforces it with five exact-count grep invariants (`requestPublication` 3, `refreshPublicationTracking` 2, `publicationBusy` 10, `publicationCard.` ≥9, `publicationState.phase ===` ≥3) plus a human check of the button's enabled/disabled state. |
| T-260801-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs zero packages. `BookIcon` comes from `@sanity/icons`, already a resolved dependency of the existing Studio. No `package.json` or lockfile in either project is modified; the lint/build gates would surface any drift. |
</threat_model>

<verification>
Run from the repo root, in order:

1. `npm --prefix sanity run lint` — zero errors. This is the real gate for these files; root `npm run lint` ignores `sanity/`.
2. `npm --prefix sanity run build` — Studio bundles clean.
3. `npm run lint` — root regression guard (expected to pass unchanged; it does not see `sanity/`).
4. `npm run typecheck` — root regression guard (`astro check`, unchanged).
5. `npm run test:unit` — the four editorial logic test files must stay green; none of them touch this component, so any failure means something outside the intended scope moved.

Human verification, against the already-running `sanity dev` on http://localhost:3333 — **read-only, never publish or mutate**:
- No metric tiles at the top of the dashboard; the page starts with the header, then the publication panel.
- The publication panel is a sober neutral card with a thin indigo left rule and soft shadow, with a hairline divider above the pending-content list.
- The publish button's enabled/disabled/loading state matches what it showed before the change for the same content state.
- Both header creation buttons are outlined; "Mettre le site à jour" is the only solid CTA.
- "Nouvelle édition" opens an empty `edition` form. Close it without publishing.
</verification>

<success_criteria>
- [ ] Three metric tiles removed from the dashboard render output
- [ ] `MetricCard`, `galleries`, `onlineGalleryCount`, the `isGalleryOnline` import, and the two orphaned icon imports are gone; `FolderIcon`, `draftCount`, `listedAttention`, `TintChip`, `metricAccentStyles`, and the `MetricAccent` type survive
- [ ] All five metric CSS rules and the now-empty `@media (min-width: 40em)` block removed; `.editorial-dashboard__surface` and the other two media queries untouched
- [ ] Publication panel restyled: neutral surface, 3px `#556bfc` left rule, hairline border, soft shadow, increased padding, hairline divider before the body — rendered only when the body has content
- [ ] Zero functional change to the publication panel, proven by the exact-count grep invariants and a human check of the button state
- [ ] "Nouvelle collection" is `mode="ghost"` with `tone="primary"`; a "Nouvelle édition" ghost sibling with `BookIcon` and `params={{type: 'edition'}}` (no `template`) sits beside it
- [ ] `npm --prefix sanity run lint`, `npm --prefix sanity run build`, `npm run lint`, `npm run typecheck`, `npm run test:unit` all pass
- [ ] Committed directly on `codex/studio-publication-workflow`; no new branch created
- [ ] No Sanity content was published, mutated, or deleted during verification
</success_criteria>

<output>
Create `.planning/quick/260801-d6m-dashboard-visual-tweaks/260801-d6m-SUMMARY.md` when done
</output>