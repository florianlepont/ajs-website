---
quick_id: 260826-r78
mode: quick
---

# Quick Task 260826-r78: Resolve mentions-légales address/phone placeholders

## Objective

`src/pages/mentions-legales.astro` and `src/pages/en/mentions-legales.astro` have shipped since
Phase 4 (2026-07-08) with explicit `.placeholder` address/phone fields ("à compléter"). Live in
production since v1.7 (2026-08-13), this is an unresolved LCEN Art. 1-1 disclosure gap. Resolved
via user decision (this conversation): declare the site's editorial activity non-professional
(LCEN Art. 1-1, II), which lets an individual substitute the identity already on file with their
host in place of publishing their own address/phone. Florian holds the real OVH hosting account
(not Romane), so his identity is what is genuinely on file — the page discloses that fact directly
rather than making an inaccurate claim about Romane's data being privately held by the host. No
phone number is published for either party. Exact FR/EN wording was drafted and approved by the
user in conversation.

## Task 1: Update FR page content and code comment

- files: src/pages/mentions-legales.astro
- action: Replace the `.placeholder` address/phone lines and the Statut placeholder with the
  approved non-professional-activity wording naming Florian Lepont as OVH account holder (7 rue
  Saint-Martin, 94290 Villeneuve-le-Roi, France). Update the D-07/D-09/D-10 frontmatter comment to
  reflect the new rationale and correct the stale "still actually live on GitHub Pages today"
  claim (Phase 5/v1.7 shipped — OVH is now the real production host).
- verify: `grep -c "placeholder" src/pages/mentions-legales.astro` returns 1 (the CSS class
  definition only, no more usages in the markup).
- done: page renders the approved wording with no pending/placeholder fields.

## Task 2: Update EN page content and code comment

- files: src/pages/en/mentions-legales.astro
- action: Mirror Task 1's changes in English (approved wording).
- verify: same placeholder-usage check as Task 1.
- done: EN page matches FR page's structure and disclosure, in English.

## Task 3: Fix the two now-stale e2e assertions

- files: tests/e2e/legal.spec.ts
- action: Update the two assertions that check for now-removed exact substrings —
  `'activité individuelle, non immatriculée'` → `'activité individuelle à titre non professionnel'`
  (FR), `'not registered'` → `'non-professional basis'` (EN).
- verify: `npx playwright test tests/e2e/legal.spec.ts --project=chromium` — all pass.
- done: e2e suite green, no assertions reference removed text.
