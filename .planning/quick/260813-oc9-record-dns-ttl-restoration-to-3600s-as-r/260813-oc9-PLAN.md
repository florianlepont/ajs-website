---
phase: quick-260813-oc9-record-dns-ttl-restoration-to-3600s-as-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-launch-domain-cutover/05-DNS-RUNBOOK.md
  - .planning/phases/05-launch-domain-cutover/05-CUTOVER-LOG.md
autonomous: true
requirements:
  - QUICK-260813-OC9

must_haves:
  truths:
    - "05-DNS-RUNBOOK.md Section 6's TTL-restoration checkbox and the Full Checklist's matching final line are both ticked [x], with notes recording the TTL-3600s restoration and dig verification evidence (both authoritative nameservers plus the public resolver, MX/SPF unchanged) in place of the prior not-yet-done caveat."
    - "05-CUTOVER-LOG.md's Follow-ups section records the TTL restoration as resolved with the same dig evidence, and the deploy-ovh.yml workaround-removal bullet in the same section reflects closure by quick task 260813-nyq."
  artifacts:
    - path: ".planning/phases/05-launch-domain-cutover/05-DNS-RUNBOOK.md"
      provides: "Section 6 and the Full Checklist's final line ticked complete, with TTL-restoration dig evidence recorded in place of the prior not-yet-done caveat."
    - path: ".planning/phases/05-launch-domain-cutover/05-CUTOVER-LOG.md"
      provides: "Follow-ups section recording both the TTL restoration and the deploy-ovh.yml workaround removal as resolved, consistent in tone."
  key_links:
    - from: "05-DNS-RUNBOOK.md Section 6 note"
      to: "05-CUTOVER-LOG.md Follow-ups bullet"
      via: "Cross-reference between the two documents' TTL-restoration record"
      pattern: "05-CUTOVER-LOG.md"
---

<objective>
Record, in both `05-DNS-RUNBOOK.md` and `05-CUTOVER-LOG.md`, that the apex and `www` `A` record TTLs for `atelierjacquelinesuzanne.fr` have been restored to their original value (3600s) in the OVH control panel — the last open item from Phase 5's cutover — and that this restoration has been independently verified via `dig` against both OVH authoritative nameservers and a public resolver, with MX/SPF confirmed unchanged at the same time.

Purpose: `05-DNS-RUNBOOK.md` Section 6 and its Full Checklist's final line, plus `05-CUTOVER-LOG.md`'s Follow-ups section, currently describe this as a deferred/not-yet-done item. The restoration has since actually happened and been verified; these documents are the project's evidence record and must reflect current, correct state rather than a stale caveat, so a future reader (or `/gsd-verify-work`) does not treat Phase 5 as having an open item it no longer has.

Output: both files updated in place — no new files, no code changes, no test changes.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-launch-domain-cutover/05-DNS-RUNBOOK.md
@.planning/phases/05-launch-domain-cutover/05-CUTOVER-LOG.md
@.planning/quick/260813-nyq-remove-the-one-time-sftp-workaround-step/260813-nyq-SUMMARY.md

<facts_verified_at_planning_time>
- The maintainer has restored the apex and `www` `A` record TTLs to 3600s in the OVH control panel.
- Independently verified via `dig +noall +answer A atelierjacquelinesuzanne.fr @ns16.ovh.net`, `dig +noall +answer A www.atelierjacquelinesuzanne.fr @ns16.ovh.net`, and the same two queries against `@dns16.ovh.net` (the zone's other authoritative nameserver, per `05-DNS-RUNBOOK.md` Section 0) — all four returned TTL 3600.
- Cross-checked via the public resolver `1.1.1.1` — same result (TTL 3600).
- At the same time, MX (`dig +short MX atelierjacquelinesuzanne.fr @ns16.ovh.net` → `mx1`/`mx2`/`mx3.mail.ovh.net`) and the SPF TXT record (`v=spf1 include:mx.ovh.com -all`) were independently re-confirmed unchanged — the hard invariant from Section 0 still holds.
- `05-DNS-RUNBOOK.md` Section 6 (line 118) and its Full Checklist's final line (line 143) are currently unticked, with prose framing the raise as not yet performed even though the surrounding text already says the site has been stable for the required waiting period.
- `05-CUTOVER-LOG.md`'s `## Follow-ups` section (lines 176-181) has two bullets: the TTL-restoration item (currently open) and the `deploy-ovh.yml` one-time-workaround-removal item. The second bullet was independently closed by quick task `260813-nyq` (commit `89af1fa`, see its `SUMMARY.md`) but the Follow-ups bullet text does not yet say so.
</facts_verified_at_planning_time>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record TTL restoration as resolved in both Phase 5 evidence documents</name>
  <files>.planning/phases/05-launch-domain-cutover/05-DNS-RUNBOOK.md, .planning/phases/05-launch-domain-cutover/05-CUTOVER-LOG.md</files>
  <action>
In `.planning/phases/05-launch-domain-cutover/05-DNS-RUNBOOK.md`:

(1) Section 6's TTL-restoration line (currently unticked, with a bolded callout framing the raise as not yet performed): tick the checkbox to `[x]`. Rewrite the callout to state plainly that the apex and `www` `A` records have been restored to 3600s in the OVH control panel, verified via `dig +noall +answer A atelierjacquelinesuzanne.fr @ns16.ovh.net`, `dig +noall +answer A www.atelierjacquelinesuzanne.fr @ns16.ovh.net`, and the same two queries against `@dns16.ovh.net` — all four returning TTL 3600 — cross-checked via the public resolver `1.1.1.1` with the same result, plus independent reconfirmation that MX (`mx1`/`mx2`/`mx3.mail.ovh.net`) and the SPF TXT (`v=spf1 include:mx.ovh.com -all`) remain unchanged. Keep the existing cross-reference to `05-CUTOVER-LOG.md`'s Follow-ups section and the "3600s" original-TTL figure.

(2) The Full Checklist's final line (the Section 6 TTL-restoration item, currently unticked with the same not-yet-done framing): tick it to `[x]` and shorten its note to reference the same restoration and the same dig evidence (both authoritative nameservers plus the public resolver, MX/SPF unchanged), keeping the cross-reference to `05-CUTOVER-LOG.md` and the "3600s" figure.

In `.planning/phases/05-launch-domain-cutover/05-CUTOVER-LOG.md`'s `## Follow-ups` section:

(3) Update the TTL-restoration bullet from an open item to a resolved one: state the restoration happened and cite the same dig evidence (both authoritative nameservers plus `1.1.1.1`, MX/SPF unchanged), and cross-reference `05-DNS-RUNBOOK.md` Section 6 and the Full Checklist now both being ticked.

(4) The second bullet (removal of the one-time `deploy-ovh.yml` workaround step) does not yet reference the quick task that closed it — update it to state it was resolved by quick task `260813-nyq` (commit `89af1fa`, which deleted the step and its explanatory comment, verified via `tests/unit/deploy-ovh-workflow.test.ts`'s 16 tests plus a full typecheck/vitest regression pass), matching the resolved tone of the TTL bullet in the same section.

Do not touch any other section of either file, and do not touch any file other than these two — this is a documentation-only correction of already-completed operational facts, not a re-plan of Phase 5.
  </action>
  <verify>
    <automated>RB=.planning/phases/05-launch-domain-cutover/05-DNS-RUNBOOK.md; CL=.planning/phases/05-launch-domain-cutover/05-CUTOVER-LOG.md; test "$(grep -ciF 'deliberately' "$RB")" -eq 0 && test "$(grep -ciF 'deliberately' "$CL")" -eq 0 && test "$(grep -cF '[x] Once the site has been stable for a day, raise the apex' "$RB")" -eq 1 && test "$(grep -cF '[x] Section 6: TTLs raised back to original values' "$RB")" -eq 1 && test "$(grep -ciF 'TTL 3600' "$RB")" -ge 1 && test "$(grep -cF '1.1.1.1' "$RB")" -ge 1 && test "$(grep -ciF 'restored' "$RB")" -ge 1 && test "$(grep -ciF 'TTL 3600' "$CL")" -ge 1 && test "$(grep -cF '1.1.1.1' "$CL")" -ge 1 && test "$(grep -ciF 'restored' "$CL")" -ge 1 && test "$(grep -cF '260813-nyq' "$CL")" -ge 1</automated>
  </verify>
  <done>Both files contain no remaining "not yet done"-style callout for the TTL restoration; both the Section 6 checkbox and the Full Checklist's matching line in 05-DNS-RUNBOOK.md are ticked `[x]` with dig-verification evidence (both authoritative nameservers, public resolver, MX/SPF unchanged) recorded in place of the old caveat; 05-CUTOVER-LOG.md's Follow-ups section shows both bullets as resolved, with the second one now citing quick task 260813-nyq.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | This plan only edits two Markdown planning documents to record an already-completed, independently-verified operational fact; no code, config, or secret-touching surface is modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-QUICK-260813-OC9-01 | Repudiation | 05-DNS-RUNBOOK.md / 05-CUTOVER-LOG.md historical record | low | accept | Both edits are additive corrections of an already-completed, independently `dig`-verified operational change (TTL restoration); no operational record is deleted, only corrected from "not yet done" to "done with evidence" — the underlying DNS change itself was already covered by Phase 5's own threat model. |
| T-QUICK-260813-OC9-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package install of any kind is part of this plan — pure Markdown edits to two existing files. |
</threat_model>

<verification>
- `05-DNS-RUNBOOK.md` Section 6's TTL line and the Full Checklist's matching final line are both ticked `[x]`.
- Neither file contains any remaining "deliberately [left unticked / deferred]" framing for the TTL item.
- `05-DNS-RUNBOOK.md` records dig evidence against both `ns16.ovh.net` and `dns16.ovh.net`, plus the public resolver, showing TTL 3600, and reconfirms MX/SPF unchanged.
- `05-CUTOVER-LOG.md`'s Follow-ups section shows both bullets (TTL restoration, deploy-ovh.yml workaround removal) as resolved, the second one citing quick task `260813-nyq`.
- No file other than these two changed.
</verification>

<success_criteria>
- Phase 5's last open item (TTL restoration) is recorded as resolved with verification evidence in both `05-DNS-RUNBOOK.md` and `05-CUTOVER-LOG.md`.
- The Follow-ups section in `05-CUTOVER-LOG.md` reads as fully resolved and internally consistent in tone.
- No code, test, or file outside the two named Markdown documents changed.
</success_criteria>

<source_audit>

| Source | ID | Feature / constraint | Task | Status | Notes |
|--------|-----|----------------------|------|--------|-------|
| GOAL | — | Record DNS TTL restoration to 3600s as resolved, with dig verification against authoritative OVH nameservers | 1 | COVERED | Task 1 ticks both checkboxes and records the full dig evidence in `05-DNS-RUNBOOK.md`. |
| REQ | QUICK-260813-OC9 | Update Section 6 checkbox + note, Full Checklist's final line, and 05-CUTOVER-LOG.md Follow-ups; check/update the sibling deploy-workaround bullet for tone consistency | 1 | COVERED | All four sub-edits (a, b in RUNBOOK; c, d in CUTOVER-LOG) are in task 1's action. |
| RESEARCH | n/a | Quick-task mode; no RESEARCH.md produced for this task | — | N/A | Not applicable — quick mode skips the research phase. |
| CONTEXT | (constraints passed directly in planning_context) | Only 05-DNS-RUNBOOK.md and 05-CUTOVER-LOG.md change as source content; no code/test changes; no new files | 1 | COVERED | `files_modified` lists only these two files; task action explicitly scopes the edit to them and forbids touching anything else. |

</source_audit>

<output>
Create `.planning/quick/260813-oc9-record-dns-ttl-restoration-to-3600s-as-r/SUMMARY.md` when done.
</output>
