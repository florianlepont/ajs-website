---
phase: quick-260826-pi1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "A recruiter skimming the first screen of README.md learns what the site is (a bilingual fr/en site for the working photographer 'Atelier Jacqueline Suzanne'), who it serves, and that it is a custom build replacing a paid SaaS site builder — before any setup/ops instruction appears."
    - "The intro names the notable technical choices AND the constraint that motivated each: near-zero hosting budget -> static Astro output with no SSR adapter; non-technical maintainer -> Sanity headless CMS with build-time content fetch; regression safety -> GitHub Actions with blocking test/lint/typecheck gates; existing low-cost host -> OVH static production target (per D-01)."
    - "README.md contains an Author section naming Florian Lepont with the LinkedIn URL https://www.linkedin.com/in/florianlepont/ — and no email address and no portfolio URL (per D-02)."
    - "Every factual item present in the pre-rewrite README still appears after the rewrite: the six-row environment-variable table, the two-target deploy table, the two production-deploy paths, the six one-time production setup steps, the four production run steps, the Vitest-split/typecheck-split explanation, the repo layout, prerequisites, setup commands, the scripts table, and the Sanity Studio steps (per D-04)."
    - "No claim is introduced that e-commerce, checkout, or Stripe is implemented — CLAUDE.md states none of it exists in the codebase; commerce may only be described as an upcoming milestone, or omitted entirely (per D-03 accuracy constraint)."
    - "README.md contains no secret VALUE — no token, password, key, or PAT — only secret NAMES, exactly as before."
    - "No level-2 heading appears twice, and the file has no section whose only content is a pointer back to another section of the same file."
    - "No disclaimer about forking, templating, or self-deploying is added anywhere in the file (per D-03)."
  artifacts:
    - README.md   # rewritten intro + Author section; all pre-existing operational documentation retained
  key_links:
    - "README.md intro <-> CLAUDE.md '## Technology Stack' table — every version number and stack claim in the new intro must trace to CLAUDE.md's implemented-stack table. CLAUDE.md explicitly marks the ORIGINAL research plan (Cloudflare Pages hosting, @astrojs/cloudflare adapter, Stripe-in-Workers checkout) as SUPERSEDED and not installed; sourcing the intro from that superseded plan is the single highest-risk failure of this task."
    - "README.md '## Environment variables' table <-> .env.example — the table is the human-facing mirror of that template. Rows must not be dropped, renamed, or have their Required?/Purpose cells reworded into something weaker."
    - "README.md '## Deployments' <-> .github/workflows/deploy.yml and .github/workflows/deploy-ovh.yml — the documented triggers, gates, environments, and webhook event names must keep matching the workflows byte-for-byte in meaning. The rewrite adds framing above this section; it does not restate or re-derive it."
---

<objective>
Rewrite README.md so it reads as a technical showcase to a recruiter skimming GitHub, without losing any of the operational documentation it currently carries.

Today README.md opens as an internal deploy/ops manual: a one-line tagline, a pointer to planning artifacts, then straight into a three-word "Stack" line, prerequisites, env vars, SFTP secrets, and CI gates. It never says what the project is, who it is for, or why any of its technical choices were made. The repo owner uses this repository partly as a hiring/recruitment showcase, so the top of the file has to do positioning work that it currently does not do.

This is an ADDITION and a REFRAMING, not a content removal (D-04). Everything below the intro stays technically accurate and stays aimed at someone building or deploying the site (D-03).

Purpose: make the first screen of the README carry the "pitch produit + vitrine technique" framing — factual and professional, not personal storytelling (D-01) — and attribute the work (D-02).
Output: one rewritten README.md at the repo root. No code changes, no test changes, no other file touched.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@README.md
@CLAUDE.md
@.planning/STATE.md
</context>

<user_decisions>
These were settled with the user before planning. They are NOT open questions — do not re-litigate, do not soften, do not "improve" past them.

- **D-01 — New intro near the top.** Tone: "pitch produit + vitrine technique". Factual/professional, NOT a personal storytelling narrative ("I built this because...", "my journey", etc. are wrong). It must state what the site is (bilingual photography site for a working photographer, "Atelier Jacqueline Suzanne"), what it does, then a short list of notable technical choices *and why* — e.g. static Astro output for near-zero hosting cost, Sanity as a headless CMS so a non-technical owner can edit content, GitHub Actions CI/CD with blocking test gates, OVH as a low-cost production host. It is meant to read as a technical showcase to someone evaluating the developer's skills.
- **D-02 — Author section.** Name "Florian Lepont" and the LinkedIn link https://www.linkedin.com/in/florianlepont/. Explicitly: NO email, NO portfolio link. The user chose "Name + LinkedIn only".
- **D-03 — No fork/template disclaimer.** Do NOT add any "this is not a template / don't fork or deploy this yourself" wording. Leave it implicit. Also: do not change the meaning or accuracy of the existing setup/deploy/testing/env-var documentation — it stays technically accurate and can remain addressed to "someone building or deploying this".
- **D-04 — Keep all existing content.** Stack, Repo layout, Prerequisites, Setup, Environment variables, Scripts, Testing, Deployments (including the OVH production setup), Sanity Studio all stay. Reorganize ONLY where it clearly improves flow — the user explicitly named one case: the intro may subsume or tighten the current one-line tagline plus the three-line "Stack" section rather than duplicating them. No factual information may be lost (env var table, CI gate list, deploy steps, secrets setup steps must all survive).
</user_decisions>

<pre_read_findings>
Discovery already performed during planning. These are settled — do not re-investigate.

**Current README.md is 133 lines. Its structure:**
- L1 title, L3 one-line tagline, L5 pointer to `.planning/PROJECT.md` + `CLAUDE.md`
- L7-9 `## Stack` — three-line summary carrying exactly four facts: Astro 7 static output with no SSR adapter; content from Sanity fetched at build time; bilingual fr/en; deployed to GitHub Pages (staging) and OVH (production).
- L11-15 `## Repo layout` — L17-19 `## Prerequisites` (Node 22) — L21-27 `## Setup`
- L29-42 `## Environment variables` — six-row table: `SANITY_PROJECT_ID`, `SANITY_DATASET`, `SANITY_API_READ_TOKEN`, `SITE_URL`, `ASTRO_BASE`, `PUBLIC_CONTACT_ENDPOINT`, plus the trailing note about the Studio's own `SANITY_STUDIO_PREVIEW_URL`.
- L44-52 `## Scripts` — five-row table.
- L54-62 `## Testing: two separate Vitest projects, deliberately coupled` — three dense bullets plus a closing "In short" paragraph. High-value, hard-won detail. Preserve wholesale; do not compress.
- L64-116 `## Deployments` — intro line, two-target table, the D-03 note about GitHub Pages staying alive, then three H3s: "the two paths", "one-time setup" (six numbered steps incl. `OVH_SFTP_PASSWORD`, `production-ovh`, `production-ovh-auto`, the `production-deploy-requested` Sanity webhook, `siteProductionRelease`, the fine-grained PAT warning, the `sanity-content-published` verification note), and "how to run one" (four numbered steps).
- L118-128 `## Sanity Studio`
- L130-132 `## Deployment` (singular) — a REDUNDANT trailing section. Its body restates the staging trigger already in the Deployments table, adds one fact not stated in the table ("runs Playwright + Vitest as a blocking gate"), and then points the reader back up to `## Deployments` above it. This is the one clear flow problem D-04 permits fixing.

**Source of truth for the new intro's technical claims: CLAUDE.md "## Technology Stack".** Relevant implemented facts:
- Astro 7.0.6, `output: 'static'`, NO server-rendering integration installed. Zero-JS-by-default, built-in i18n.
- `astro:i18n`: French at root, English under `/en/`, no Accept-Language auto-redirect.
- Sanity Content Lake + Studio (`@sanity/client` 7.23.0, `@sanity/image-url` 2.1.1, Studio `sanity` ^6.4.0). Content fetched at BUILD time, published perspective only. Studio is the separate `sanity/` subproject.
- `@fontsource/unbounded` — self-hosted brand font, no external font CDN.
- Vitest 4.1.9 and Playwright 1.61.1, both BLOCKING CI gates.
- GitHub Pages = staging (base path `/ajs-website/` via `ASTRO_BASE`). OVH Web Hosting = production, free tier, static files over SFTP, ZERO request-time compute — which is WHY the build is static-only.
- Cost table: GitHub Pages 0€/mo, Sanity 0€/mo, OVH + domain already owned. Target ~0-5€/month.

**ACCURACY TRAP — read this before writing a single word of the intro.** CLAUDE.md marks the original research plan as SUPERSEDED: Cloudflare Pages hosting, the `@astrojs/cloudflare` adapter, and Stripe-in-Workers checkout were all abandoned. CLAUDE.md's "Deferred to v1.x (not yet implemented)" section is explicit that there is NO `stripe` dependency, NO checkout or webhook code, NO stock fields, NO CGV page. CLAUDE.md's own opening "browse and buy / checkout" language describes the v1.x GOAL, not shipped behaviour. The intro must therefore NOT claim commerce exists. Describe it as a planned next milestone, or leave it out.

**Second accuracy note.** The current `## Stack` line ends "OVH (production, Phase 5)". "Phase 5" is an internal GSD planning reference, not a technical fact, and it is stale relative to the `## Deployments` section below it (which already documents the production domain and a working two-path production deploy). Dropping the bare "Phase 5" parenthetical is permitted and expected. Do NOT replace it with any new claim about cutover status in either direction — the `## Deployments` section already owns that ground and stays untouched.
</pre_read_findings>

<tasks>

<task type="auto">
  <name>Task 1: Replace the README opening with a positioning intro and a why-annotated technical highlights section</name>
  <files>README.md</files>
  <action>
Replace the current opening block (the H1, the one-line tagline, the planning-artifact pointer line, and the `## Stack` section — currently lines 1 through 9) with a new opening that does the positioning work D-01 asks for. Everything from `## Repo layout` downward is untouched by this task.

Keep the H1 as is. Then write, in English, matching the existing README's plain declarative register:

1. **A short positioning block (2-3 tight paragraphs, no heading, or under a single H2 such as "About the project").** It must convey: this is the custom-built bilingual (French/English) website for Atelier Jacqueline Suzanne, the practice of working photographer Romane Lepont; it presents her photographic work, her Éditions, an about/contact surface, and her agenda; content is edited by the photographer herself through a headless CMS; and it replaces a paid hosted SaaS portfolio builder with a custom build running at near-zero recurring cost. Factual and professional per D-01 — no first-person narrative arc, no "journey", no marketing superlatives ("beautiful", "blazing fast", "cutting-edge"). If commerce is mentioned at all, it is named as a planned next milestone and never as a shipped feature — see the ACCURACY TRAP in pre_read_findings.

2. **A technical highlights section (H2, e.g. "Technical highlights").** A short list — roughly five to seven bullets — where each bullet pairs a choice with the constraint that drove it. This is the part a recruiter reads to judge engineering judgement, so the *why* is the payload, not the *what*. Cover at minimum:
   - Astro 7, `output: 'static'`, with no SSR adapter installed — because the production host (OVH shared hosting, already owned) offers zero request-time compute, so the whole architecture is deliberately shaped to need none. Zero JS shipped by default.
   - Sanity as a headless CMS with content fetched at BUILD time — because the site's owner is non-technical and must self-serve galleries, agenda, and page copy without touching the repo; a publish in the Studio triggers a rebuild rather than a runtime query.
   - Bilingual fr/en on Astro's built-in i18n routing — French at the root, English under `/en/`.
   - GitHub Actions CI/CD with BLOCKING gates before any deploy — lint, typecheck, unit tests with coverage thresholds, and end-to-end browser tests, across both the site and the separate Studio subproject.
   - Two deploy targets with different roles — GitHub Pages as a permanently-live staging/preview environment, OVH as the production host serving the real domain, with production releases triggered by the photographer herself from the Studio rather than by a code push.
   - Near-zero recurring cost as an explicit design constraint, not an accident — free hosting tiers plus an already-owned domain and host.

   Draw every version number and claim from the CLAUDE.md facts listed in pre_read_findings. Do not invent numbers, do not describe anything from the superseded Cloudflare/Stripe research plan.

3. **Preserve the four `## Stack` facts.** D-04 permits the intro to subsume and tighten the tagline plus `## Stack` rather than duplicating them, so `## Stack` as a standalone section may be dropped — but only if all four of its facts (static Astro 7 with no SSR adapter; Sanity content fetched at build time; bilingual fr/en; GitHub Pages staging plus OVH production) now live in the highlights list. The bare "Phase 5" parenthetical is an internal planning reference and is dropped, with nothing asserted in its place.

4. **Preserve the planning-artifact pointer.** The line linking `.planning/PROJECT.md` and `CLAUDE.md` must survive with both links intact. Placement is your call anywhere between the end of the positioning block and the end of the highlights section — a short "For full project context, decisions, and constraints, see ..." line reads best after the highlights rather than interrupting them.

Do not add any fork/template/self-deploy disclaimer (D-03). Do not touch anything from `## Repo layout` down.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website; for f in SANITY_PROJECT_ID SANITY_DATASET SANITY_API_READ_TOKEN SITE_URL ASTRO_BASE PUBLIC_CONTACT_ENDPOINT OVH_SFTP_PASSWORD production-ovh-auto siteProductionRelease production-deploy-requested sanity-content-published deploy-ovh.yml; do grep -q -- "$f" README.md || { echo "LOST FACT: $f"; exit 1; }; done; echo "operational facts retained"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; grep -q 'Atelier Jacqueline Suzanne' README.md; grep -q 'Sanity' README.md; grep -q 'PROJECT.md' README.md; grep -q 'CLAUDE.md' README.md; test "$(sed -n '1,12p' README.md | grep -ci 'photograph')" -ge 1; echo "intro positioning present in first 12 lines"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; test "$(grep -ci 'cloudflare' README.md)" -eq 0; echo "no superseded-hosting-plan claim"</automated>
    <!-- planner-discipline-allow: Cloudflare -->
    <!-- The superseded adapter must never appear in README.md. It is named in the action above only as a prohibition; if that word reaches the file, this gate fails, which is the intended behaviour. Commerce-accuracy (no claim that checkout ships) is not grep-gated because naming it as a PLANNED milestone is explicitly permitted — that distinction is checked by the human-check in Task 3. -->
  </verify>
  <done>The first screen of README.md states what the site is and who it serves; a technical highlights section pairs each notable choice with the constraint that drove it; all four former Stack facts survive somewhere in the new opening; the PROJECT.md/CLAUDE.md pointer survives; no section from `## Repo layout` downward was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Author section and fold the redundant trailing deploy section into Deployments</name>
  <files>README.md</files>
  <action>
Two small, bounded edits at the bottom of README.md.

**(a) Author section (D-02).** Append a new H2 section titled `## Author` as the final section of the file, after `## Sanity Studio`. Its body is the name Florian Lepont on its own line in bold, and a markdown link labelled LinkedIn pointing at https://www.linkedin.com/in/florianlepont/ . Nothing else: no email address, no personal-portfolio URL, no availability or hire-me line, no photo, no badge row. The user explicitly chose "name plus LinkedIn only" and picking up anything beyond that is a scope violation, not a helpful addition.

**(b) Fold the redundant trailing deploy section.** The file currently ends with a second, singular-titled deploy section (the last three lines of the pre-rewrite file, immediately after `## Sanity Studio`) whose body restates the staging trigger and then tells the reader to look at the plural `## Deployments` section further up. It is the one clear flow problem D-04 permits fixing, and a duplicated near-identical heading reads badly to exactly the audience this rewrite targets.

Fold it, in this order:
1. It carries one fact the `## Deployments` table does NOT state: that the push-to-main pipeline runs Playwright and Vitest as a BLOCKING gate before deploying. Before deleting anything, confirm that fact is present in `## Deployments`. If it is not, add it there first — one sentence under the deploy table, e.g. noting that both deploy workflows run the full blocking gate set before publishing anything.
2. Only once that fact has a home, delete the redundant trailing section entirely — heading and body.
3. Its other two facts (push to `main` deploys to GitHub Pages; a Sanity publish fires a webhook that triggers a rebuild) are already in the Deployments table's Trigger row. Verify that before deleting; do not delete on assumption.

Do not otherwise touch `## Deployments`, `## Sanity Studio`, or any section above them. In particular the two-target table, the note about GitHub Pages staying alive permanently, the two production paths, the six one-time setup steps, and the four run steps are all out of scope for this task.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website; grep -q '^## Author$' README.md; grep -q 'Florian Lepont' README.md; grep -q 'linkedin.com/in/florianlepont' README.md; echo "author section present"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; test "$(grep -ciE 'mailto:|@yahoo|@gmail' README.md)" -eq 0; echo "no email address exposed"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; test -z "$(grep '^## ' README.md | sort | uniq -d)"; echo "no duplicated level-2 heading"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; test "$(grep -c '^## Deployment$' README.md)" -eq 0; test "$(grep -c '^## Deployments$' README.md)" -eq 1; echo "redundant trailing section folded; canonical section intact"</automated>
    <!-- planner-region-allow: ^## Deployments$ -->
    <!-- Not a ban: the plural heading is asserted to be present exactly once (-eq 1), which is precisely what Task 3 also requires. Only the singular-heading variant is gated to zero, and that literal never appears as a standalone line in any action body, so it cannot be tripped by echoed prose. -->

    <automated>cd /Users/florian/Projects/ajs-website; grep -qiE 'playwright' README.md; grep -qiE 'vitest' README.md; grep -qi 'blocking' README.md; echo "blocking-gate fact survives"</automated>
  </verify>
  <done>README.md ends with an Author section carrying only the name and the LinkedIn link; no level-2 heading appears twice; the blocking Playwright/Vitest gate fact still appears in the file; nothing else below `## Sanity Studio` was altered.</done>
</task>

<task type="auto">
  <name>Task 3: Whole-file coherence and no-loss pass</name>
  <files>README.md</files>
  <action>
Read the full rewritten README.md top to bottom in one pass and make only the small corrections that pass surfaces. This is the verification step the user asked for, done as a task rather than assumed.

Check and fix, in place:
- **Reading order.** The file should flow: title, positioning, technical highlights, then the operational documentation (repo layout, prerequisites, setup, env vars, scripts, testing, deployments, Studio), then Author. If a section landed out of that order, move it — move only, do not rewrite its body.
- **Seams.** The join between the new intro and `## Repo layout` should not restate something the highlights already said, and the highlights should not contradict the detail sections below (for example, the highlights' description of the CI gates must not describe a different gate set than `## Deployments` and the testing section do).
- **Register.** The intro must still read factual and professional per D-01. Strip any sentence that drifted into personal narrative or marketing superlative.
- **Nothing lost.** Confirm against the pre-rewrite structure listed in pre_read_findings that every section and every table row is still present. Specifically re-check: the six env var rows plus the Studio env note, the five script rows, all three dense testing bullets plus the "In short" paragraph, the two-target deploy table, the GitHub Pages-stays-alive note, both production paths, all six numbered one-time setup steps including the fine-grained PAT warning and the existing-webhook verification note, and all four numbered run steps.
- **No secret values.** The env var table and the OVH setup steps name secrets; confirm no real token, password, key, or project id value was pasted in while editing.
- **No disclaimer.** Confirm no fork/template/self-deploy warning was introduced anywhere (D-03).

If nothing needs correcting, change nothing and say so — an empty diff is a valid outcome for this task.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website; test "$(wc -l &lt; README.md)" -ge 140; echo "file grew: no content gutted"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; test "$(grep -cnE 'gh[pousr]_[A-Za-z0-9]{20,}|sk_(live|test)_[A-Za-z0-9]{16,}|AKIA[0-9A-Z]{12,}' README.md)" -eq 0; echo "no token-shaped literal in file"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; for h in 'Repo layout' 'Prerequisites' 'Setup' 'Environment variables' 'Scripts' 'Deployments' 'Sanity Studio' 'Author'; do grep -q "^## .*$h" README.md || { echo "MISSING SECTION: $h"; exit 1; }; done; grep -q '^## Testing' README.md; echo "all sections present"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; test "$(grep -c '^| \`' README.md)" -ge 11; echo "env var + scripts table rows intact"</automated>
    <automated>cd /Users/florian/Projects/ajs-website; git diff --name-only | grep -vq . || test "$(git diff --name-only)" = "README.md"; echo "only README.md modified"</automated>
    <human-check>Open README.md on GitHub (or in a markdown preview) and read it as a recruiter would — top to bottom, skimming. Confirm: (1) the first screen tells you what the project is and why its technical choices were made, without you having to scroll into setup instructions; (2) the tone is factual and professional, not a personal story; (3) the Author section shows the name and LinkedIn link and nothing else; (4) the setup, env var, testing and deploy documentation still reads accurately and completely to someone who would actually deploy this.</human-check>
  </verify>
  <done>README.md reads coherently end to end, contains the intro and Author section, retains every pre-existing factual item, exposes no secret values, adds no fork/template disclaimer, and is the only modified file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public GitHub | README.md is the most-read public artifact in the repository; anything written into it is published to anonymous readers and indexed. |
| author -> public GitHub | The Author section deliberately publishes real-world identity, so its contents are a scope decision, not an accident. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260826-pi1-01 | Information Disclosure | README.md `## Environment variables` table and the OVH production one-time setup steps | high | mitigate | The file documents secret NAMES only and must continue to. While editing near `OVH_SFTP_PASSWORD`, the Sanity read token row, and the fine-grained-PAT webhook step, no real value may be pasted in. Enforced by Task 3's token-shaped-literal grep gate (`gh*_`, `sk_live/test_`, `AKIA` prefixes) returning zero, and by the pre-existing "never commit real values" line above the env table, which stays. |
| T-260826-pi1-02 | Information Disclosure | README.md `## Author` section | low | accept | Publishing name plus a public LinkedIn profile is the explicit intent of D-02 — the repository is a hiring showcase, so discoverability is the goal. Bounded by excluding email address, phone, and portfolio URL; enforced by Task 2's `mailto:`/mail-domain grep gate returning zero. |
| T-260826-pi1-03 | Tampering | README.md documented deploy procedure | medium | mitigate | A reworded deploy/setup instruction could send a future operator down a wrong path (skipping the `production-ovh-auto` secret copy, or editing the existing `sanity-content-published` webhook instead of creating a new one) and break or mis-target a production deploy. Mitigated by scoping every task to leave `## Deployments` bodies untouched except for one additive sentence, plus the Task 1 fact-retention grep over the workflow/environment/webhook identifiers. |
| T-260826-pi1-04 | Spoofing | README.md intro claims vs. shipped code | medium | mitigate | Overstating capability (claiming shipped Stripe checkout, or the superseded Cloudflare hosting) misrepresents the work to the exact audience the rewrite targets, and is trivially falsifiable by anyone reading the repository. Mitigated by sourcing every claim from CLAUDE.md's implemented-stack table, the ACCURACY TRAP note in pre_read_findings, the `cloudflare` grep gate, and Task 3's human-check. |
| T-260826-pi1-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this plan. This is a documentation-only change touching one markdown file; no dependency is added, so the package legitimacy gate does not apply. |
</threat_model>

<verification>
- All automated gates in Tasks 1-3 pass.
- `git status --porcelain` shows README.md as the only modified tracked file.
- The Task 3 human-check has been performed and confirmed.
</verification>

<success_criteria>
- README.md opens with positioning that tells a skimming recruiter what the project is, who it serves, and why its notable technical choices were made (D-01).
- README.md ends with an Author section carrying the name Florian Lepont and the LinkedIn URL, with no email and no portfolio link (D-02).
- No fork/template/self-deploy disclaimer was added, and the setup, env var, testing, and deploy documentation is unchanged in meaning and accuracy (D-03).
- Every section and table row present before the rewrite is still present; the only structural removal is the redundant trailing deploy section, and only after its unique fact was rehomed (D-04).
- No secret value appears anywhere in the file.
- README.md is the only file changed.
</success_criteria>

<output>
Create `.planning/quick/260826-pi1-rewrite-readme-md-for-recruiting-portfol/260826-pi1-SUMMARY.md` when done.
</output>