---
phase: quick-260812-p85
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/releaseGate.ts
  - sanity/editorial/EditorialDashboard.tsx
  - tests/unit/release-gate.test.ts
  - tests/unit/editorial-dashboard-markup.test.ts
autonomous: true
requirements:
  - QUICK-260812-p85
must_haves:
  truths:
    - "In the `ready` gate state, clicking the round ▶ control opens the site de test in a new tab and publishes NOTHING. The production release trigger is not reachable from that click at all."
    - "That same click records that the current batch has been previewed, which is the only thing that unlocks the new « Publier sur le site en ligne » button rendered inside the pipeline-detail box below the pipeline."
    - "Before a preview, that new button is present but disabled; after a preview it is enabled, and clicking it is what actually starts the production release."
    - "The preview flag falls back to false the instant the gate leaves `ready` — release started, release failed, release finished, or new unpublished content pushing the pipeline backwards. A preview approves ONE batch and can never carry over to a newer, unreviewed one."
    - "The `failed` gate state is unchanged: clicking the red ✕ control still retries the production release directly, with no preview required, because the staging content it would republish has not changed."
    - "The `locked`, `active` and `done` gate states are unchanged in both behaviour and rendered content — the new publish button does not appear in them."
    - "The ▶ control's `title` and `aria-label` in the `ready` state name the action it now performs (opening the site de test); the « Publier sur le site en ligne » wording moves to the new button that actually performs it. The ▶ icon itself is unchanged."
    - "The routing rule, the disabled rule and the reset rule are three pure functions in their own module, each unit-tested across every gate variant — not regexes over source text."
    - "`npm run test:unit`, `npm run typecheck`, `npm --prefix sanity run lint` and `npm --prefix sanity run build` all pass."
    - "`git diff` proves `sanity/editorial/dashboardLogic.ts`, `sanity/editorial/deployment.ts` and `.github/workflows/` were never touched."
    - "The rebuilt Studio is deployed and the closing message asks the user to walk the real two-step flow end to end."
  artifacts:
    - sanity/editorial/releaseGate.ts
    - sanity/editorial/EditorialDashboard.tsx
    - tests/unit/release-gate.test.ts
    - tests/unit/editorial-dashboard-markup.test.ts
  key_links:
    - "WHY a new pure module rather than inline conditionals: this is the control that makes content public. The user just clicked the ▶ expecting a preview and triggered a real production release. `gateClickAction`, `productionPublishDisabled` and `nextPreviewedFlag` in `sanity/editorial/releaseGate.ts` are plain TypeScript with no `@sanity/ui` imports, so the root Vitest project can import and exercise them directly — real behavioural tests instead of the source-text regexes this repo is otherwise forced into. Root Vitest coverage already includes `sanity/editorial/**/*.ts` (vitest.config.ts line 19), so the new module is measured automatically."
    - "WHY the reset invariant is the dangerous part: `hasPreviewedStaging` is a plain boolean with no batch identity. Without a reset keyed on `gateVariant`, a preview of batch A would still be `true` when batch B later reaches `ready`, and the publish button would be live for content nobody looked at. Routing the reset through `nextPreviewedFlag(gateVariant, current)` inside a `useEffect` keyed on `gateVariant` makes the rule itself unit-testable, leaving only the wiring to a source-text guard."
    - "WHY `gateVariant === 'ready'` is exactly the final `resolvePromoteRow()` branch: `pipelineGateVariant()` (EditorialDashboard.tsx ~1270) returns `ready` only when nothing failed, `liveSite` is neither active nor done, AND `promote.buttonDisabled` is false. The only three branches with `buttonDisabled: false` are the requestError branch and the production-failed branch — both of which resolve to `failed` first — and the final fall-through (deployment.ts 477-483). So `ready` ⟺ that one branch, whose `buttonDisabled` is always false. The `pipeline.promote.buttonDisabled ||` term in the new button's disabled rule is therefore a defensive constant today; it is kept deliberately so a future branch change cannot silently enable the button."
    - "WHY the new button has a home to render into: the `ready` branch already returns a title (« Site de test à jour — prêt à publier ? ») and a detail (« Ouvrez le site de test pour vérifier avant de publier. »), so `promoteDetailBoxHasBody` is true and the `.editorial-dashboard__pipeline-detail` box renders. That existing copy already describes the two-step flow being built — no copy change is needed anywhere, which is what keeps `resolvePromoteRow()` untouched. Task 1 locks this coupling with a test so a future copy edit cannot make the button homeless."
    - "WHY clicking publish makes the button vanish rather than spin: `releasePipelineState({busy: true})` forces `segments.production` to `active` (deployment.ts 506-510), which flips `gateVariant` to `active` on the very next render — the button unmounts, the gate goes calm, and the live-site node carries the spinner. That is the existing designed feedback path. `loading={releaseBusy}` is still set on the button per convention and as a guard should that render window ever widen."
    - "There is no React testing library in this repo (unit tests run in a `node` environment and `sanity/` deps are not resolvable from the root Vitest project). Source-text assertions over `readFileSync(COMPONENT_PATH)` are the established guard pattern for JSX wiring — see the header comment of `tests/unit/editorial-dashboard-markup.test.ts`, which already holds five describe blocks in that style. Use it only for what the pure functions cannot cover."
    - "Sanity's Prettier config (`printWidth: 100`, `singleQuote`, no semicolons) applies inside `sanity/`; the root `tests/unit/` files use semicolons and single quotes. Match the file being edited — `npm --prefix sanity run lint` only checks the former."
---

<objective>
The round gate control between the two pipeline nodes publishes straight to production on a single click. In the `ready` state it shows a ▶ play glyph, which reads as "preview" — the user clicked it expecting to look at the site de test first and instead fired a real production release.

Purpose: split that one click into two. The ▶ becomes what its glyph promises — it opens the site de test — and a separate, explicitly-labelled button below the pipeline becomes the thing that publishes, unlocked only once the site de test has actually been opened.

Output: a new pure `releaseGate` module carrying the routing/disabled/reset rules with full unit coverage, the component rewired around it, source-text guards on the JSX wiring, green gates, and a redeployed Studio awaiting the user's end-to-end test of the real flow.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@sanity/editorial/EditorialDashboard.tsx
@sanity/editorial/deployment.ts
@tests/unit/editorial-dashboard-markup.test.ts
@tests/unit/deployment.test.ts
@.planning/quick/260812-o1d-remove-the-redundant-post-publish-succes/260812-o1d-PLAN.md
</context>

<scope_boundary>
Do NOT modify:

- `sanity/editorial/dashboardLogic.ts` — anything, including `buildProductionReleaseMarkerActions()`, `triggerProductionRelease()`, `publicationCardState()` and the publication controller.
- `sanity/editorial/deployment.ts` — anything. Not `releasePipelineState()`'s segment-kind computation, not `resolvePromoteRow()`'s copy or branching, not a single string. The two new files import from it read-only.
- `.github/workflows/` — anything. The production release still goes out through exactly the same `repository_dispatch` path; only the number of clicks in front of it changes.
- `sanity/editorial/EditorialDashboard.css` — no rule changes. The new button is a `@sanity/ui` `Button`, centred by an existing `Flex justify="center"` pattern; `.editorial-dashboard__pipeline-gate--ready` keeps its existing rule and its ▶ icon.
- In `EditorialDashboard.tsx`: `pipelineGateVariant()`'s branch order and logic (~1270-1282); `pipelineDisplaySegments` / `pipelineNodeDetail` / `pipelineCircleClassName` / `pipelineNodeIcon` usage; `promoteDetailBoxHasBody` and its three-term derivation; the two pipeline-node `<div>`s and their plain-`span` label/detail elements; the `actionUrl` link block inside the pipeline-detail box; `runPublication`, `refreshPublicationTracking`, the « Mettre le site à jour » button, and every card below the divider.

Do NOT change the behaviour of the `locked`, `active`, `done` or `failed` gate states — including by rendering the new publish button in them. The new button appears in `ready` only.

Do NOT weaken the two-step flow into a confirmation dialog, a "are you sure" toast, an auto-timer that unlocks after N seconds, or a checkbox. The unlock condition is: the site de test was actually opened from this control, for this batch.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the pure release-gate rules module with full behavioural coverage</name>
  <files>sanity/editorial/releaseGate.ts, tests/unit/release-gate.test.ts</files>
  <behavior>
    - `gateClickAction('ready')` returns the preview action, and specifically not the release action
    - `gateClickAction('failed')` returns the release action — a retry needs no fresh preview
    - `gateClickAction` returns the inert action for `locked`, `active` and `done`
    - `productionPublishDisabled` is true when the batch has not been previewed, false once it has
    - `productionPublishDisabled` stays true when the promote row itself is disabled, even after a preview
    - `nextPreviewedFlag` preserves the flag only while the variant is `ready`, and clears it for all four other variants
    - the `ready` promote row from `releasePipelineState()` carries copy, so the box hosting the new button actually renders
  </behavior>
  <action>
Create `sanity/editorial/releaseGate.ts` — a new, dependency-free module (no React, no `@sanity/ui`, no imports at all). Follow the Sanity subproject's Prettier style: single quotes, no semicolons, 100-column width. Open the file with a short head comment explaining WHY it exists: the round gate control is the step that makes content public, so the three rules governing it live as pure functions that can be exercised directly by the root Vitest project rather than inferred from source-text regexes.

Export four things:

  1. `PipelineGateVariant` — the string-union type with the five members currently declared privately in `EditorialDashboard.tsx` at line ~1262 (`locked`, `ready`, `active`, `done`, `failed`). Copy the member list exactly; Task 2 deletes the private declaration and imports this one, so any drift is a type error.
  2. `GateClickAction` — a string union of three members meaning, in order: open the site de test, start the production release, do nothing.
  3. `gateClickAction(variant: PipelineGateVariant): GateClickAction` — returns the preview member for `ready`, the release member for `failed`, and the inert member for everything else. Write it as an explicit `ready` check, then an explicit `failed` check, then a default return, so a future sixth variant defaults to inert rather than to publishing. Add a one-line comment on the `failed` branch recording WHY it bypasses the preview: a retry republishes the same staging content the user already had the chance to review, so demanding a second review would strand a failed release behind a pointless click.
  4. `nextPreviewedFlag(variant: PipelineGateVariant, current: boolean): boolean` — returns `current` when the variant is `ready`, and `false` otherwise. Comment WHY: the flag is a plain boolean with no batch identity, so anything other than "cleared the moment we leave `ready`" lets an approval of one batch unlock a later, unreviewed one.

Also export `productionPublishDisabled({promoteButtonDisabled, hasPreviewedStaging}: {promoteButtonDisabled: boolean; hasPreviewedStaging: boolean}): boolean`, returning `promoteButtonDisabled || !hasPreviewedStaging`. Use a named-object parameter, not two positionals, so the two booleans can never be swapped silently at the call site. Comment that the first term is a defensive constant today — the `ready` variant is only reachable when the promote row is enabled — kept so a future branch change cannot silently enable the button.

Then create `tests/unit/release-gate.test.ts` in the ROOT test style (semicolons, single quotes, `describe`/`it`, an explanatory second argument on `expect`). Import the three functions and the type from `'../../sanity/editorial/releaseGate'`.

Cover, at minimum:

  - `gateClickAction` for all five variants. For `ready`, assert both that it equals the preview member AND that it does not equal the release member, with a message recording that this is the exact regression being fixed: the control used to publish on click. Use `it.each` over a five-row table if you prefer, but the `ready` case needs its two-sided assertion.
  - `productionPublishDisabled` across the four combinations of its two inputs.
  - `nextPreviewedFlag` for all five variants with `current` true, plus `ready` with `current` false. Assert every non-`ready` variant clears it.
  - One coupling test: import `deploymentState` and `releasePipelineState` from `'../../sanity/editorial/deployment'` and build the `ready` situation — no pending drafts, a staging deployment whose `kind` is `current`, a production state with no release, an empty `productionReleaseAt`, `busy: false`. Follow the fixture pattern already established at `tests/unit/deployment.test.ts` lines ~440-476 (`deploymentState({runs: [run()], publishedAt, pendingCount: 0})` and a `noProductionRelease` state) rather than hand-building `DeploymentState` objects. Assert `promote.buttonDisabled` is false and that `promote.title` and `promote.detail` are both non-empty, with a message explaining that the new publish button renders inside the pipeline-detail box, and that box is skipped entirely when the promote row has no body — so emptying this copy would make the button unreachable. Read `deployment.ts` only; do not edit it.

Commit this task as exactly ONE commit — Task 4's scope gate counts back from HEAD.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/release-gate.test.ts</automated>
    <automated>test -f sanity/editorial/releaseGate.ts</automated>
    <automated>test "$(grep -c '^import ' sanity/editorial/releaseGate.ts)" = "0"</automated>
    <automated>node -e "const s=require('fs').readFileSync('sanity/editorial/releaseGate.ts','utf8');for(const n of ['gateClickAction','productionPublishDisabled','nextPreviewedFlag','PipelineGateVariant','GateClickAction'])if(!new RegExp('export (function|type|const) '+n).test(s))throw new Error('missing export: '+n);console.log('releaseGate exports OK')"</automated>
    <automated>npm --prefix sanity run lint</automated>
  </verify>
  <done>`sanity/editorial/releaseGate.ts` exists with zero imports and five exports; `tests/unit/release-gate.test.ts` passes and covers all five gate variants for both `gateClickAction` and `nextPreviewedFlag`, all four input combinations of `productionPublishDisabled`, and the `ready`-branch coupling assertion proving the promote row still carries the copy the new button's container depends on; Sanity lint is clean; the work is in one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the gate to preview-only and add the real publish button below the pipeline</name>
  <files>sanity/editorial/EditorialDashboard.tsx</files>
  <action>
Six edits to `sanity/editorial/EditorialDashboard.tsx`, and nothing else. Sanity Prettier style throughout (single quotes, no semicolons, 100 columns).

**Edit 1 — imports.** After the existing `./deployment` value and type imports (~lines 22-38), add a value import of `gateClickAction`, `nextPreviewedFlag` and `productionPublishDisabled` from `'./releaseGate'`, and a separate `import type {PipelineGateVariant} from './releaseGate'`, mirroring how `./deployment` already splits its value and type imports.

**Edit 2 — delete the private type.** Remove the `type PipelineGateVariant = ...` declaration at ~line 1262. The imported union replaces it; `pipelineGateVariant()` immediately below keeps its body and branch order untouched and simply returns the imported type. If `astro check` or Sanity lint reports an unused or duplicate identifier here, you deleted the wrong line — re-read.

**Edit 3 — new state.** Beside the other release state (~line 97, after `releaseError`), add a boolean state pair for whether the current batch has been previewed, initialised `false`.

**Edit 4 — derivations and the reset effect.** Immediately after `const gateVariant = pipelineGateVariant(...)` at ~line 432, derive the click action once via `gateClickAction(gateVariant)`, and derive the gate control's label as: the preview action gets a French label naming the opening of the site de test; every other action falls back to `pipeline.promote.buttonLabel` unchanged.

Then add a `useEffect` whose ONLY dependency is `gateVariant`, whose body calls the preview-flag setter with the updater form routed through `nextPreviewedFlag(gateVariant, current)`. Declaring it here rather than up with the other effects is required: `gateVariant` does not exist until line 432, and hook order stays unconditional either way. Write a comment above it stating WHY, in your own words: a preview approves one batch, and any state the pipeline moves into other than `ready` means the batch changed, so carrying the flag forward would let an old approval unlock content nobody reviewed. Do NOT add a second dependency, do NOT guard the effect behind an `if` outside it, and do NOT reset the flag from inside a click handler instead — the effect is the only place that sees every transition.

**Edit 5 — the gate control.** In the `<button className={...__pipeline-gate...}>` at ~lines 708-720, keep `type`, `className`, the `disabled` binding to `pipeline.promote.buttonDisabled`, and all four icon lines exactly as they are — the ▶ still renders for `ready`. Change:

  - `title` and `aria-label` to the derived gate label from Edit 4 (both to the same value, as today).
  - `onClick` to a new handler declared just after `triggerProductionReleaseClick` (~line 454, so the const it calls is already in scope). The handler branches on the derived click action: on the preview action it calls `window.open` with `SITE_PREVIEW_URL`, `'_blank'` and `'noopener'`, sets the preview flag true, and returns; on the release action it calls `triggerProductionReleaseClick()`; on the inert action it does nothing. The handler is synchronous — no `async`, and no `void` wrapper at the JSX call site.

After this edit the release trigger must be unreachable from the `ready` branch of that handler. The `failed` path through it stays a direct retry, unchanged in effect.

**Edit 6 — the new publish button.** Inside the `.editorial-dashboard__pipeline-detail` `Stack` (~lines 746-778), after the existing `actionUrl` block and still inside the `Stack`, add a block rendered only when `gateVariant === 'ready'`. It contains a `Flex justify="center"` wrapping a `@sanity/ui` `Button`, matching the « Mettre le site à jour » button's conventions at ~line 654: `tone="primary"`, a `text` prop, `loading={releaseBusy}`, `style={{minHeight: 44}}`. Its `text` is the French label for publishing to the live site — the same wording `pipeline.promote.buttonLabel` returns on this branch, written as a literal here rather than read from the promote row, because that row's label is now the gate's fallback and the two must be free to diverge. Its `disabled` is `productionPublishDisabled({...})` fed with `pipeline.promote.buttonDisabled` and the preview flag. Its `onClick` calls `triggerProductionReleaseClick()` wrapped in `void`, matching the existing async-handler call style in this file.

Restrict the block to `ready` — do not render it in `locked`, `active`, `done` or `failed`. Those four states are explicitly out of scope, and adding a new disabled button to them changes them.

Do NOT quote either of the two new French labels, or `window.open`, inside any comment you add. The gates below count their occurrences in this file and a quotation makes a passing count fail.

Commit this task as exactly ONE commit.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('sanity/editorial/EditorialDashboard.tsx','utf8');const i=s.indexOf('__pipeline-gate editorial');if(i<0)throw new Error('gate button not found');const j=s.indexOf('</button>',i);const slice=s.slice(i,j);if(slice.includes('triggerProductionRelease'))throw new Error('the gate control still calls the release trigger directly');if(!/onClick=\{[A-Za-z]/.test(slice))throw new Error('gate onClick is not a bare handler reference');console.log('gate click routing OK')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('sanity/editorial/EditorialDashboard.tsx','utf8');const m=s.match(/useEffect\(\(\)\s*=>\s*\{[^}]*nextPreviewedFlag\(gateVariant[\s\S]{0,200}?\},\s*\[gateVariant\]\)/);if(!m)throw new Error('no useEffect routing the preview flag through nextPreviewedFlag(gateVariant, ...) with [gateVariant] as its only dependency');console.log('preview-flag reset effect OK')"</automated>
    <automated>test "$(grep -vE '^\s*(//|\*|/\*)' sanity/editorial/EditorialDashboard.tsx | grep -cF 'triggerProductionReleaseClick')" = "3"</automated>
    <automated>test "$(grep -vE '^\s*(//|\*|/\*)' sanity/editorial/EditorialDashboard.tsx | grep -cF 'SITE_PREVIEW_URL')" = "3"</automated>
    <automated>test "$(grep -vE '^\s*(//|\*|/\*)' sanity/editorial/EditorialDashboard.tsx | grep -cF 'window.open')" = "1"</automated>
    <automated>test "$(grep -vE '^\s*(//|\*|/\*)' sanity/editorial/EditorialDashboard.tsx | grep -cF 'pipeline.promote.buttonLabel')" = "1"</automated>
    <automated>test "$(grep -vE '^\s*(//|\*|/\*)' sanity/editorial/EditorialDashboard.tsx | grep -cF 'pipeline.promote.buttonDisabled')" = "2"</automated>
    <automated>test "$(grep -cF 'Publier sur le site en ligne' sanity/editorial/EditorialDashboard.tsx)" = "1"</automated>
    <automated>test "$(grep -c 'promoteDetailBoxHasBody' sanity/editorial/EditorialDashboard.tsx)" = "2"</automated>
    <automated>test "$(grep -c 'publicationPanelHasBody' sanity/editorial/EditorialDashboard.tsx)" = "2"</automated>
    <automated>test "$(grep -c 'type PipelineGateVariant =' sanity/editorial/EditorialDashboard.tsx)" = "0"</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The gate control's own JSX no longer mentions the release trigger and routes through a bare handler reference; the release trigger is referenced exactly three times in executable lines (its definition, the handler's failed-retry branch, the new button); `SITE_PREVIEW_URL` is referenced three times (import, header link, the new preview open); `window.open` appears exactly once; `pipeline.promote.buttonLabel` survives once as the non-preview fallback and `pipeline.promote.buttonDisabled` twice (gate disabled binding plus the new button's rule); the publish label exists once, in the new button; the private gate-variant type is gone; `promoteDetailBoxHasBody` and `publicationPanelHasBody` are untouched at two references each; a `useEffect` keyed solely on `gateVariant` routes the preview flag through `nextPreviewedFlag`; Sanity lint and `astro check` are clean; the work is in one commit.</done>
</task>

<task type="auto">
  <name>Task 3: Guard the JSX wiring the pure functions cannot reach</name>
  <files>tests/unit/editorial-dashboard-markup.test.ts</files>
  <action>
Append a sixth `describe` block to `tests/unit/editorial-dashboard-markup.test.ts`, after the five existing ones (the last currently ends at ~line 237). Do not modify any existing block — each guards work from an earlier quick task.

Follow the file's established style exactly: `const source = readFileSync(COMPONENT_PATH, 'utf-8')` inside the describe, `expect(...).toBe(true/false)` or `.toHaveLength(n)` with an explanatory second argument on every `expect`, semicolons and single quotes.

Open the block with a short comment recording WHY these are source-text assertions rather than behavioural ones: the routing, disabled and reset RULES are unit-tested directly in `tests/unit/release-gate.test.ts`; what cannot be reached from `node` is the JSX and hook WIRING that connects those rules to the actual controls, and a mis-wire there reinstates the one-click-to-production bug the rules were written to prevent.

Assert five things:

  1. **The gate control does not call the release trigger.** Slice the source from the index of the pipeline-gate class marker to the next `</button>`, and assert that slice does not contain the release-trigger identifier. Message: this control's `ready` state must open the site de test, and a direct call here is exactly the regression being fixed. Scope the assertion to that slice deliberately — the identifier legitimately appears three times elsewhere in the file, so a whole-file assertion would be wrong.
  2. **The gate control routes through the shared rule.** Assert the source contains a call to `gateClickAction(gateVariant`. Message: re-deriving the branch inline at the call site would put the publish decision back outside test coverage.
  3. **The preview opens the staging URL and records the approval together.** Assert the source contains `window.open(SITE_PREVIEW_URL` and, within a slice of roughly 300 characters after that index, the preview-flag setter called with `true`. Message: opening the tab without recording the approval leaves the publish button permanently disabled; recording it without opening the tab unlocks publishing on a preview that never happened.
  4. **The reset is an effect keyed on the gate variant.** Assert a regex matching a `useEffect` whose body routes the preview-flag setter through `nextPreviewedFlag(gateVariant` and whose dependency array is exactly `[gateVariant]`. Message: this is the invariant that stops a stale approval of one batch from unlocking a newer, unreviewed one; widening the dependency array or moving the reset into a click handler silently breaks it.
  5. **The publish button is gated on `ready` and on the shared disabled rule.** Assert the source contains `productionPublishDisabled(`, and that the publish label appears exactly once. Message: the new button is the only control that starts a production release from this panel, so its disabled rule must come from the tested helper rather than an inline condition, and a second occurrence of the label means it leaked into another gate state or into a comment.

Commit this task as exactly ONE commit.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-markup.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('tests/unit/editorial-dashboard-markup.test.ts','utf8');const n=(s.match(/^describe\(/gm)||[]).length;if(n!==6)throw new Error('expected 6 describe blocks, found '+n);console.log('describe count OK')"</automated>
    <automated>npm run test:unit</automated>
  </verify>
  <done>The markup test file holds six top-level describe blocks, the five pre-existing ones byte-unchanged; the new block asserts the gate slice is free of the release trigger, the gate routes through `gateClickAction`, the preview opens the staging URL and records the approval in the same handler, the reset effect is keyed solely on `gateVariant` via `nextPreviewedFlag`, and the publish button uses the shared disabled rule with its label appearing exactly once; the full unit suite is green; the work is in one commit.</done>
</task>

<task type="auto">
  <name>Task 4: Run every gate, prove the scope held, redeploy the Studio and hand off for a real two-step test</name>
  <files>(no source changes — gates, deploy and sign-off only)</files>
  <action>
Run the four blocking gates in order, fixing anything they surface before continuing: `npm run test:unit` (full suite), `npm run typecheck`, `npm --prefix sanity run lint`, `npm --prefix sanity run build`.

Then prove the scope boundary held. `git diff --stat HEAD~3` must show exactly four paths — `sanity/editorial/releaseGate.ts`, `sanity/editorial/EditorialDashboard.tsx`, `tests/unit/release-gate.test.ts`, `tests/unit/editorial-dashboard-markup.test.ts` — and nothing else. `sanity/editorial/dashboardLogic.ts`, `sanity/editorial/deployment.ts` and anything under `.github/workflows/` must be absent. Record the observed output verbatim in the SUMMARY.

Then redeploy the live Studio with `npm run deploy --prefix sanity`. This is the established pattern for shipping a Studio change in this project (same as quick tasks dvq, f22, h3i, lvt, o1d) and is expected and low-risk — do not stop and ask before running it. Wait for success and note the deployed Studio URL from its output.

If the deploy fails, do not retry blindly: report the failure output and stop. The previous Studio bundle is still serving, so nothing is broken by waiting.

Finally, close your response by asking the user to hard-refresh the Studio (Cmd+Shift+R) and walk the REAL two-step flow — this environment has no authenticated browser session, so only they can see it, and only a live run proves the fix. Spell out the sequence and what each step must show:

  1. Get the pipeline into the ready state (content published, site de test up to date) — the round control shows ▶ and the box below reads « Site de test à jour — prêt à publier ? ».
  2. Confirm the new « Publier sur le site en ligne » button is visible in that box and DISABLED.
  3. Click the ▶. A new tab must open on the site de test, and NOTHING must publish — the live-site node must stay exactly as it was, with no release starting.
  4. Back in the Studio tab, the publish button must now be ENABLED.
  5. Click it. THIS is the click that publishes: the gate should go calm, the live-site node should pick up the spinner, and a production release should start.
  6. Once it finishes, note that the publish button is gone (the pipeline is in the done state) — and if new content is later published, confirm the button comes back DISABLED again, requiring a fresh preview.

Flag step 5 explicitly as the one that costs a real production deploy, so they choose their moment. This is a closing hand-off note, not a blocking checkpoint — the task is done once the deploy succeeds.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm --prefix sanity run build</automated>
    <automated>test -z "$(git diff --stat HEAD~3 -- sanity/editorial/dashboardLogic.ts sanity/editorial/deployment.ts .github/workflows/)"</automated>
    <automated>test "$(git diff --name-only HEAD~3 | sort | tr '\n' ' ')" = "sanity/editorial/EditorialDashboard.tsx sanity/editorial/releaseGate.ts tests/unit/editorial-dashboard-markup.test.ts tests/unit/release-gate.test.ts "</automated>
    <automated>npm run deploy --prefix sanity</automated>
  </verify>
  <done>All four gates pass; the three-commit diff touches exactly the four intended paths and none of the forbidden ones; the Studio deploy reports success with its URL recorded in the SUMMARY; and the response ends with the six-step live test request, with the publishing click called out as the one with a real production cost.</done>
</task>

</tasks>

<verification>
- `npm run test:unit` — full suite green, including the new `tests/unit/release-gate.test.ts` and the sixth describe block in `editorial-dashboard-markup.test.ts`.
- `npm run typecheck` — 0 errors (confirms the private gate-variant type was replaced by the imported one cleanly).
- `npm --prefix sanity run lint` — clean (confirms the `useEffect` dependency array satisfies react-hooks and that no identifier was orphaned).
- `npm --prefix sanity run build` — Studio bundles.
- Gate-slice check: the pipeline-gate `<button>` block contains no reference to the release trigger.
- Component greps: release trigger 3 on executable lines; `SITE_PREVIEW_URL` 3; `window.open` 1; `pipeline.promote.buttonLabel` 1; `pipeline.promote.buttonDisabled` 2; the publish label 1; `promoteDetailBoxHasBody` still 2; `publicationPanelHasBody` still 2; private `type PipelineGateVariant =` 0.
- `git diff --name-only HEAD~3` — exactly the four intended paths.
- `git diff --stat HEAD~3 -- sanity/editorial/dashboardLogic.ts sanity/editorial/deployment.ts .github/workflows/` — empty.
</verification>

<success_criteria>
- The ▶ control does what its glyph promises: it opens the site de test and publishes nothing.
- Publishing to production now requires a second, separately-labelled, explicitly-enabled click — and that click cannot be reached until the site de test has actually been opened for the batch in question.
- A preview approves exactly one batch: the flag clears on every transition out of `ready`, so a newer, unreviewed batch always starts locked.
- Retrying a failed release is unchanged — one click, no preview — and the `locked`, `active` and `done` states render and behave exactly as before.
- The three rules governing all of the above are pure, exported and unit-tested across every gate variant; the JSX and hook wiring around them is guarded by source-text assertions that fail on reintroduction.
- `deployment.ts`, `dashboardLogic.ts` and the workflows are provably untouched — the release path itself did not change, only the number of deliberate clicks in front of it.
- Every gate is green, scope is git-proven, the Studio is redeployed, and the user has been asked to walk the real flow end to end.
</success_criteria>

<output>
Create `.planning/quick/260812-p85-split-the-production-release-gate-into-a/260812-p85-SUMMARY.md` when done
</output>
