---
phase: quick-260813-g49
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/deployment.ts
  - sanity/editorial/pipelineView.ts
  - sanity/editorial/EditorialDashboard.tsx
  - sanity/README.md
  - tests/unit/deployment.test.ts
  - tests/unit/pipeline-view.test.ts
  - tests/unit/release-gate.test.ts
  - tests/unit/editorial-dashboard-markup.test.ts
autonomous: true
requirements:
  - QUICK-260813-g49
must_haves:
  truths:
    - "CHANGE 1 — every one of the eight branches of `resolvePromoteRow()` returns `title: ''` and `detail: ''`. Six branches still carry copy today; two (`segments.production === 'done'` and `segments.content !== 'done'`) were already emptied by quick task 260812-q0d. After this plan the promote box under the pipeline shows explanatory text in NO state whatsoever — that was the user's literal instruction: « je ne veux plus aucun texte dans cette zone, à aucune étape du process »."
    - "CHANGE 1 scope — `buttonLabel`, `buttonDisabled`, `dimmed`, `notStarted`, `actionLabel` and `actionUrl` are byte-identical in every branch. Those drive real button and link behaviour, not copy."
    - "CHANGE 2 (the regression this plan exists to prevent) — the « Publier sur le site en ligne » button renders whenever `gateVariant === 'ready'`, and the failure action link renders whenever `pipeline.promote.actionUrl` is set, NEITHER of them gated on title/detail any more. Emptying the copy without this restructure would silently delete the only control that triggers a real production release."
    - "CHANGE 3 — the panel header subtitle under « Mettre le site à jour » narrates the CURRENT stage of the whole release process across all eight states, in this priority order: requestError → production active → production failed → staging failed → content not done (existing count sentence + not-started addendum, UNCHANGED) → staging not done → ready to publish → everything done."
    - "CHANGE 3 constraint — the raw `requestError` technical string is never printed into that user-facing subtitle line, and the removed « étape 1 / étape 2 » step-numbering framing is not reintroduced anywhere."
    - "CHANGE 4 — the eight-branch decision is a pure exported function `releasePanelSubtitle()` in `sanity/editorial/pipelineView.ts` with no runtime imports, covered by real behavioural tests in `tests/unit/pipeline-view.test.ts` that assert each of the eight states AND the priority order between them explicitly (a higher-priority condition wins even when a lower-priority one is also true)."
    - "`PipelineSegmentKind` still has exactly four values. No state-machine change — this whole plan is presentational and copy."
    - "No test is deleted. Three existing guards assert behaviour this plan changes (`tests/unit/release-gate.test.ts` lines ~97-129, and two describe blocks in `tests/unit/editorial-dashboard-markup.test.ts`); each is re-pointed at the new contract with a comment saying why it changed."
    - "`buildProductionReleaseMarkerActions`, `triggerProductionRelease`, `dashboardLogic.ts`, `releaseGate.ts` and `.github/workflows/` are provably untouched by `git diff` against the recorded base SHA."
    - "`npm run test:unit`, `npm run typecheck`, `npm --prefix sanity run lint` and `npm --prefix sanity run build` all pass."
    - "The Studio is redeployed via `npm run deploy --prefix sanity`, and the SUMMARY ends with the three-part live confirmation request across several pipeline states."
  artifacts:
    - sanity/editorial/deployment.ts
    - sanity/editorial/pipelineView.ts
    - sanity/editorial/EditorialDashboard.tsx
    - sanity/README.md
    - tests/unit/deployment.test.ts
    - tests/unit/pipeline-view.test.ts
    - tests/unit/release-gate.test.ts
    - tests/unit/editorial-dashboard-markup.test.ts
  key_links:
    - "THE COUPLING BUG, in one sentence: `promoteDetailBoxHasBody` (EditorialDashboard.tsx ~line 454) is `Boolean(title || detail || actionUrl)`, and BOTH the « Publier… » button block (~lines 820-834) and the failure link (~lines 808-819) are nested INSIDE `{promoteDetailBoxHasBody && (…)}`. Empty every title/detail and that guard collapses to `Boolean(actionUrl)`, which is FALSE in the ready state — the production-release button disappears with no test failure to warn you unless Task 3 lands. Task 1 and Task 3 must both be complete before any deploy."
    - "`tests/unit/release-gate.test.ts` lines 97-129 is the tripwire for that same bug and it BREAKS the moment Task 1 lands: it asserts `result.promote.title.length > 0` and `result.promote.detail.length > 0`, and its own failure message spells out « an empty title here would make the button unreachable ». Do not delete it and do not paper over it — re-point it at the new contract (empty copy, reachability now guaranteed structurally by the gateVariant-keyed guard) in the SAME task that empties the copy."
    - "THE OLD RESTRICTION IS LIFTED. The immediately-preceding quick task (260812-q0d) forbade touching the panel header subtitle JSX and pinned an existing markup test to its exact contents. Changing that subtitle is the entire point of THIS task. Do not carry the old restriction forward by habit, and do not treat the existing markup assertion as a reason to keep the inline ternary."
    - "`tests/unit/editorial-dashboard-markup.test.ts` lines 62-85 currently REQUIRE `pipeline.promote.notStarted`, `publicationCard.total > 0` and the « Rien n’a été lancé… » literal to all sit inside the subtitle element. All three move into `pipelineView.ts` in this plan, so that describe block must be INVERTED (assert the wiring to the helper instead), not deleted."
    - "`tests/unit/editorial-dashboard-markup.test.ts` lines 93-151 require the box guard to reference title and detail. That is exactly the coupling being removed, so those assertions must be re-pointed at the new guard terms (`gateVariant === 'ready'` and `actionUrl`)."
    - "`tests/unit/editorial-dashboard-markup.test.ts` line ~292 asserts the literal « Publier sur le site en ligne » appears EXACTLY ONCE in `EditorialDashboard.tsx`. Do not quote that button label in any new code comment in that file — a comment mention breaks the count and that guard exists to catch the label leaking into a second gate state."
    - "ORDERING TRAP: `publicationCard` is declared around line 518, but `gateVariant`/`promoteDetailBoxHasBody` are around lines 435/454 — ~70 lines EARLIER. The new subtitle derivation needs `publicationCard.total`, so it must be declared next to `pipelineDetail` (~line 527), NOT next to `gateVariant`. Nothing else moves and no hook is reordered."
    - "`pipelineView.ts` must stay runtime-import-free (its own header comment states the rule, and the root Vitest project cannot resolve `sanity/`'s deps). Do NOT import `pluralize` from `dashboardLogic.ts` — that module imports `@sanity/icons`, `./checks` and `./workflowLogic`, which would drag the whole graph in. Inline the same one-line rule (`count > 1 ? plural : singular`) locally instead."
    - "The `requestError` state loses its box entirely (it sets no `actionUrl`, and `pipelineGateVariant()` returns `'failed'` there, not `'ready'`). Its retry stays reachable because `gateClickAction('failed')` routes the round gate button to `'release'`. This is intended: no box, no text, retry on the round gate, and the header subtitle says the release could not start."
    - "The raw technical `requestError` string stops being displayed anywhere in the UI. That is a deliberate accepted consequence of change 1, not an oversight — do not re-add it to the subtitle, the box, or a new element."
    - "Do not confuse the two subtitles. The PAGE header subtitle (`const subtitle`, ~line 508, fed by `deploymentSubtitle()` under the « Tableau de bord » heading) is NOT in scope. Only the PANEL header subtitle under the « Mettre le site à jour » heading (~lines 676-687) changes."
    - "Typography must match the existing files exactly or the string-equality tests fail: curly apostrophe U+2019 (’), single-character ellipsis U+2026 (…), em dash U+2014 (—), French guillemets « » with normal spaces, as already used throughout `deployment.ts` and `pipelineView.ts`."
---

<objective>
The user tested the deployed Studio dashboard live and reported that during a site update they still saw « En attente du site de test… / L’étape 2 sera disponible une fois le site de test à jour. » in the box under the release pipeline. Their instruction, verbatim: they want NO text in that zone at ANY stage of the process, and the panel's own header (« Mettre le site à jour » + its small subtitle) is what must report the current status of the release process instead.

Four coupled changes:

1. Empty `title` and `detail` in all six remaining branches of `resolvePromoteRow()` (two were already emptied by quick task 260812-q0d). Behaviour fields stay byte-identical.
2. Decouple the « Publier sur le site en ligne » button and the failure action link from that copy. Today both are nested inside a guard derived from title/detail/actionUrl; emptying the copy would silently delete the only control that starts a real production release. This is the single most important thing in this plan.
3. Rewrite the panel header subtitle so it narrates the current stage across all eight states — staging rebuild, staging failure, ready-to-publish, production in flight, production failure, request failure — instead of only counting modified contents.
4. Put that eight-branch decision in a pure, tested `releasePanelSubtitle()` helper in `pipelineView.ts`, following the extraction pattern established earlier the same day, rather than inlining an eight-branch ternary in JSX.

Purpose: the promote box was a third place saying what the two pipeline nodes and the round gate button already say, and its copy kept going stale as the pipeline evolved. One narrator, at the top of the panel, in the maintainer's own framing.

Output: six emptied branches in `deployment.ts`, a new tested helper in `pipelineView.ts`, a restructured promote box and a rewired subtitle in `EditorialDashboard.tsx`, three existing test guards re-pointed rather than deleted, a resynced README sentence, green gates, a redeployed Studio, and a live confirmation request covering three pipeline states.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@sanity/editorial/deployment.ts
@sanity/editorial/pipelineView.ts
@sanity/editorial/EditorialDashboard.tsx
@sanity/editorial/releaseGate.ts
@tests/unit/deployment.test.ts
@tests/unit/pipeline-view.test.ts
@tests/unit/release-gate.test.ts
@tests/unit/editorial-dashboard-markup.test.ts
</context>

<scope_boundary>
Base SHA for every scope proof in this plan: **`7d525d664002c519e9e995cbce1cb04d003698c8`**.

Do NOT modify:

- `sanity/editorial/dashboardLogic.ts` — including `buildProductionReleaseMarkerActions()`, `triggerProductionRelease()`, `publicationCardState()`, `preparePublicationBatch()`, `pluralize()`. This plan only READS `publicationCard.total`.
- `sanity/editorial/releaseGate.ts` — `gateClickAction()`, `nextPreviewedFlag()`, `productionPublishDisabled()` and the `PipelineGateVariant` type stay byte-identical. Type-only imports from it are allowed; edits are not.
- `.github/workflows/` — anything.
- `sanity/editorial/EditorialDashboard.css` — no style change is needed and no new colour may be introduced. The box keeps its existing `editorial-dashboard__pipeline-detail` / `--dimmed` classes.
- The `PipelineSegmentKind` union — still exactly four values. No fifth kind, no widening.
- In `deployment.ts`: `deploymentState()`, `deploymentSubtitle()`, `deploymentSegmentKind()`, `isProductionReleaseStale()`, `pipelineDisplaySegments()`, `deploymentTargetConfig`, and the whole of `releasePipelineState()`'s segment computation. Only `resolvePromoteRow()`'s eight returned objects change, and within them only `title` and `detail`.
- In `EditorialDashboard.tsx`: `pipelineGateVariant()` (~line 1289), `pipelineNodeIcon()`, `handleGateClick()`, `triggerProductionReleaseClick()`, the `nextPreviewedFlag` effect, the two pipeline node blocks and the round gate button block, and the PAGE header subtitle (`const subtitle` ~line 508, fed by `deploymentSubtitle()`).
- In `pipelineView.ts`: `pipelineCircleClassName()`, `pipelineNodeDetail()` and `pipelineGateCaption()` stay exactly as they are. This plan only ADDS a fourth function.

Do NOT delete any existing guard test. Three of them assert behaviour this plan deliberately changes; re-point each at the new behaviour with a comment recording why it moved. A deleted guard is an untested surface.

Do NOT reintroduce « étape 1 » / « étape 2 » step-numbering framing anywhere — that framing was deliberately removed earlier in this session.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: deployment.ts — empty the promote copy in every remaining branch, and re-point the two test guards that depended on it</name>
  <files>sanity/editorial/deployment.ts, tests/unit/deployment.test.ts, tests/unit/release-gate.test.ts</files>
  <action>
**Edit A — `resolvePromoteRow()` in `sanity/editorial/deployment.ts`.**

Set `title` and `detail` to the empty string in all six branches that still carry copy. In each branch change ONLY those two properties; `buttonLabel`, `buttonDisabled`, `dimmed`, `notStarted`, `actionLabel` and `actionUrl` stay byte-identical:

1. `if (requestError)` — the branch currently naming a failed start and passing the raw `requestError` string through as `detail`. Keep `buttonLabel: 'Réessayer'`, `buttonDisabled: false`, `dimmed: false`. The `requestError` parameter is still consumed by the function (it selects this branch), so no signature change and no unused-parameter lint.
2. `if (segments.production === 'active')` — keep `buttonLabel: 'Publication en cours…'`, `buttonDisabled: true`, `dimmed: false`.
3. `if (segments.production === 'failed')` — keep `buttonLabel: 'Réessayer'`, `buttonDisabled: false`, `dimmed: false`, and both `actionLabel`/`actionUrl` taken from `production`.
4. `if (segments.staging === 'failed')` — keep `buttonLabel: 'Publier sur le site en ligne'`, `buttonDisabled: true`, `dimmed: true`, and both `actionLabel`/`actionUrl` taken from `staging`.
5. `if (segments.staging !== 'done')` — the exact branch the user saw and flagged. Keep `buttonLabel: 'Publier sur le site en ligne'`, `buttonDisabled: true`, `dimmed: true`.
6. The final ready-to-publish fallback — keep `buttonLabel: 'Publier sur le site en ligne'`, `buttonDisabled: false`, `dimmed: false`.

The two branches already returning empty copy (`segments.production === 'done'` and `segments.content !== 'done'`) are unchanged.

Then rewrite the comment block that sits above `resolvePromoteRow()` (or add one if the existing per-branch comments are the only ones) so the new invariant is stated ONCE at function level rather than repeated eight times:

  - This function returns no explanatory copy in any branch. `title` and `detail` are always empty strings, in every state.
  - The panel header subtitle is now the single narrator of release status — one sentence at the top of the panel describing whichever stage is currently happening. It is computed by `releasePanelSubtitle()` in `pipelineView.ts` (added by Task 2 of this plan). Name that function so the next reader can find the copy.
  - The user's instruction was explicit: no text in the box under the pipeline at any stage of the process. Do not restore a title or a detail on any branch.
  - The two fields are deliberately KEPT on `ReleasePipelinePromote` rather than deleted, because the tests asserting they are empty in all eight states are what stop copy from being reintroduced here. Do not "clean up" the interface by removing them.
  - The raw `requestError` string is deliberately no longer surfaced in the UI. It is developer-facing detail, and the user-facing line only needs to say the release could not start.

Trim the two now-redundant per-branch comments only where they repeat what the function-level comment now says; keep the substantive reasoning already written on the `segments.content !== 'done'` branch (the two-situations split and the `notStarted` flag rationale), which is about branch STRUCTURE, not copy.

**Edit B — `tests/unit/deployment.test.ts`, inside `describe('release pipeline state')`.**

Re-point every assertion that expects copy. Change the assertion AND the test NAME wherever the name quotes removed copy:

  1. The case named `'with no production release ever recorded, keeps production pending and the row dimmed/disabled/titled "En attente du site de test…"'` (~line 561): rename it so it describes state rather than copy (production stays pending, the row stays dimmed and disabled, and it carries no copy). Replace the title assertion with `toBe('')`, add the matching `detail` assertion, keep `segments.production`, `dimmed`, `buttonDisabled` and the `notStarted` falsy assertion untouched.
  2. `'with content done, staging done, and no production release, enables the promote button'` (~line 584): replace the title assertion with `toBe('')`, add the `detail` one, keep `dimmed`, `buttonDisabled` and `buttonLabel`.
  3. `'a release in flight marks the production segment active and disables the button while it runs'` (~line 600): replace the title assertion with `toBe('')`, add `detail`, and add `expect(result.promote.buttonLabel).toBe('Publication en cours…')` so the in-flight label is proven to survive the copy removal.
  4. `'a production release older than the newest content publication falls back to pending and re-enables the button'` (~line 727): replace the title assertion with `toBe('')`, add `detail`.
  5. `'a failed staging run disables the button and names the site de test as the failure'` (~line 756): the name and the `title.toLowerCase()).toContain('site de test')` assertion both have to go. Rename it around what now carries the state — the row goes dimmed and disabled and keeps the staging run link — and assert `title` and `detail` are empty, `dimmed` is true, `buttonDisabled` is true, and `actionUrl` equals `staging.actionUrl`. That last assertion is load-bearing: the link is now the ONLY body this state's box has.
  6. `'a failed production run enables the button for retry and names the site en ligne as the failure'` (~line 776): same treatment — rename, assert empty `title`/`detail`, keep `segments.production`, `buttonDisabled` false, and `actionUrl === production.actionUrl`, and add `expect(result.promote.buttonLabel).toBe('Réessayer')`.
  7. `'a trigger request error wins over every other row state and is surfaced verbatim with an enabled retry button'` (~line 807): the "surfaced verbatim" premise is now false. Rename it around what still holds — the request error still wins over every other row state and still leaves an enabled retry — and replace the `detail` assertion with `toBe('')` plus `title` `toBe('')`. Add a comment on those two: the raw technical string is deliberately not shown to the maintainer any more; the header subtitle says the release could not start, and the retry lives on the round gate button.

Then add ONE new sweep case at the end of the describe, named for the contract it locks (no explanatory copy in any branch, all eight states). Build all eight states and, for each, assert BOTH that the state actually reached is the intended one AND that `title` and `detail` are empty — the discriminator assertion is what stops the sweep from silently degenerating into eight copies of the same state. Reuse the fixtures already present in the file (`run()`, `publishedAt`, `deploymentState`, `noProductionRelease`):

  - requestError set (any staging/production pair) — discriminator: `promote.buttonLabel` is `'Réessayer'` with `buttonDisabled` false.
  - production active via `busy: true` — discriminator: `segments.production === 'active'`.
  - production failed (the failed-production fixture) — discriminator: `segments.production === 'failed'`.
  - production done (the proven-current fixture) — discriminator: `segments.production === 'done'`.
  - staging failed (the failed-staging fixture) — discriminator: `segments.staging === 'failed'`.
  - content pending (`pendingCount: 1`) — discriminator: `promote.notStarted === true`.
  - staging active with content done (`in_progress` staging run, `pendingCount: 0`) — discriminator: `segments.staging === 'active'` and `segments.content === 'done'`.
  - ready (staging current, no production release) — discriminator: `promote.buttonDisabled === false` and `promote.dimmed === false`.

**Edit C — `tests/unit/release-gate.test.ts`, the describe at lines ~97-129.**

This block is the tripwire for the coupling bug and it fails the moment Edit A lands. Do not delete it; invert it.

Rename the describe from « the ready promote row carries copy, so the new button has a home to render into » to state the new contract: the ready promote row carries NO copy, and the publish button no longer depends on any. Keep the fixture and the `buttonDisabled` false assertion exactly as they are. Replace the two `.length > 0` assertions with `toBe('')` assertions on `title` and `detail`.

Rewrite both failure messages to record the change of mechanism, in enough detail that the next reader does not "restore" the old coupling: the publish button used to be nested inside a box whose existence was derived from title/detail/actionUrl, so emptying the copy would have made the button unreachable — Task 3 of this plan re-derives that box from `gateVariant === 'ready'` and `actionUrl` instead, and `tests/unit/editorial-dashboard-markup.test.ts` is now what guards the button's reachability. Point at that file by name.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/deployment.test.ts</automated>
    <automated>npm run test:unit -- tests/unit/release-gate.test.ts</automated>
    <automated>git diff -U0 7d525d664002c519e9e995cbce1cb04d003698c8 -- sanity/editorial/deployment.ts | grep '^@@'</automated>
    <automated>grep -cF "= 'pending' | 'active' | 'done' | 'failed'" sanity/editorial/deployment.ts</automated>
  </verify>
  <done>All eight branches of `resolvePromoteRow()` return empty `title` and `detail`, with every behaviour field unchanged. `tests/unit/deployment.test.ts` passes with seven re-pointed cases plus the new eight-state sweep, and `tests/unit/release-gate.test.ts` passes with its inverted, re-messaged guard. The `git diff -U0` hunk headers fall only inside `resolvePromoteRow()` and its comment block — no hunk touches `releasePipelineState()`, `deploymentState()` or `pipelineDisplaySegments()`. The final grep prints `1`, proving the segment-kind union was not widened.</done>
</task>

<task type="auto">
  <name>Task 2: pipelineView.ts — add the tested releasePanelSubtitle() helper that narrates all eight release states</name>
  <files>sanity/editorial/pipelineView.ts, tests/unit/pipeline-view.test.ts</files>
  <action>
Add a fourth exported function to `sanity/editorial/pipelineView.ts`, at the end of the file, leaving the three existing helpers untouched. It must obey the module's existing header rule: no React, no `@sanity/ui`, no `sanity`, type-only imports only. Extend the existing type-only import from `./deployment` to also bring in `ReleasePipelineSegments`.

Signature: `releasePanelSubtitle({segments, modifiedCount, notStarted = false, requestError}: {segments: ReleasePipelineSegments; modifiedCount: number; notStarted?: boolean; requestError?: string}): string`.

Eight branches, checked top to bottom, first match wins. The exact strings:

  1. `requestError` truthy → `'La publication sur le site en ligne n’a pas pu démarrer.'` — the raw `requestError` value is NOT interpolated. The parameter is read only as a condition.
  2. `segments.production === 'active'` → `'Publication sur le site en ligne en cours…'`
  3. `segments.production === 'failed'` → `'Échec de la publication sur le site en ligne — réessayez.'`
  4. `segments.staging === 'failed'` → `'Échec de la mise à jour du site de test.'`
  5. `segments.content !== 'done'` → the existing panel logic, moved verbatim: when `modifiedCount === 0`, `'Aucune modification publique en attente.'`; otherwise `` `${modifiedCount} ${modifiedCount > 1 ? 'contenus modifiés' : 'contenu modifié'} depuis la dernière mise en ligne.` ``. Then, and only when `notStarted` is true AND `modifiedCount > 0`, append the addendum with its leading space: `' Rien n’a été lancé pour l’instant : cliquez sur « Mettre le site à jour » pour démarrer.'`
  6. `segments.staging !== 'done'` → `'Mise à jour du site de test en cours…'`
  7. `segments.production !== 'done'` → `'Site de test à jour — prêt à publier sur le site en ligne.'`
  8. fallback → `'Aucune modification publique en attente.'`

Implementation notes, each of which must be reflected in comments on the function:

  - Branch order IS the contract. State that the order encodes what the maintainer most needs to know first: a release that could not start, then one in flight, then failures, then the ordinary content/staging progression. Note that once branches 1-4 decline, production is neither active nor failed, so branches 7 and 8 only ever see `'done'` or `'pending'` — which is why the eight branches are exhaustive without a default catch-all beyond branch 8.
  - Branch 5 is a verbatim move of the logic that lived in the panel header JSX, and is deliberately UNCHANGED behaviour: the count sentence, the zero-count sentence and the not-started addendum all keep their existing wording and their existing double guard. Record why the `modifiedCount > 0` guard exists alongside `notStarted`: the two values come from different sources (the publication inventory snapshot vs. the draft count driving the segments), and a rare disagreement between them must not print the addendum right after the "nothing pending" sentence.
  - Branch 6 replaces copy that used step numbering. Say plainly that the step-numbering framing was deliberately removed earlier in this session and must not come back.
  - Branch 1 deliberately does not interpolate the error. Say why: this line is user-facing and calm; the technical string is developer-facing detail and does not belong here.
  - The plural rule is inlined rather than imported from `dashboardLogic.ts`'s `pluralize()`. Say why: that module imports `@sanity/icons` and two sibling modules, and pulling it into this module's import graph would break the dependency-free rule this file's header states. The inlined rule (`> 1` takes the plural) mirrors `pluralize()` exactly — note that so a future reader knows the two must stay in step.

Then extend `tests/unit/pipeline-view.test.ts` with a new describe for `releasePanelSubtitle`, importing it alongside the three existing helpers. Define a small fixture helper in the describe that builds a `ReleasePipelineSegments` object with sensible defaults so each case only names what it changes.

Cases — all eight states, asserted against the exact strings:

  - requestError set → branch 1's string.
  - production `'active'` → branch 2's string.
  - production `'failed'` → branch 3's string.
  - staging `'failed'` (production `'pending'`) → branch 4's string.
  - content `'pending'` with `modifiedCount: 3` → the count sentence with the plural form and no addendum (`notStarted` omitted).
  - content `'pending'` with `modifiedCount: 1` → the count sentence with the SINGULAR form, proving the inlined plural rule.
  - content `'pending'` with `modifiedCount: 0` → exactly `'Aucune modification publique en attente.'`
  - content `'pending'`, `modifiedCount: 2`, `notStarted: true` → the count sentence followed by the addendum, asserted with `toContain` on the addendum and `toContain` on the count fragment.
  - content `'pending'`, `modifiedCount: 0`, `notStarted: true` → does NOT contain the addendum. Message: the two inputs come from different sources and a disagreement must not print a contradiction.
  - content `'done'`, staging `'active'` → branch 6's string. Comment this case as the exact state the user reported seeing the removed box copy in.
  - content `'done'`, staging `'done'`, production `'pending'` → branch 7's string.
  - all three `'done'` → branch 8's string.

Priority cases — assert the ORDER explicitly, not just each state in isolation. Each of these sets up two or more competing conditions and asserts the higher-priority one wins:

  - `requestError` set AND staging `'failed'` AND production `'failed'` → branch 1's string.
  - production `'active'` AND staging `'failed'` → branch 2's string.
  - production `'failed'` AND staging `'failed'` → branch 3's string.
  - staging `'failed'` AND content `'pending'` with `modifiedCount: 4` → branch 4's string (no count sentence).
  - content `'pending'` AND staging `'active'` → branch 5's count sentence, NOT branch 6's staging sentence.
  - content `'done'`, staging `'active'`, production `'done'` → branch 6's string, proving branch 6 sits above branches 7/8.

One more guard case: pass a realistic technical error such as `'GitHub a refusé la requête (403).'` as `requestError` and assert the result does not contain `'403'`. Message: the raw technical string is developer-facing and must never leak into this user-facing line.

Do not modify the three existing describes in the file.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/pipeline-view.test.ts</automated>
    <automated>git diff --name-only 7d525d664002c519e9e995cbce1cb04d003698c8 -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts</automated>
    <automated>grep -c "^import type" sanity/editorial/pipelineView.ts</automated>
  </verify>
  <done>`releasePanelSubtitle()` exists in `pipelineView.ts` with eight ordered branches and no runtime imports, and `tests/unit/pipeline-view.test.ts` passes with the eight state cases, the six explicit priority cases, the plural/addendum guards and the no-raw-error-leak guard, alongside the three untouched existing describes. The `git diff --name-only` command prints nothing. The final grep prints `2`, confirming the file still has only type-only import statements.</done>
</task>

<task type="auto">
  <name>Task 3: EditorialDashboard.tsx — narrate status in the panel header, and free the publish button and failure link from the removed copy</name>
  <files>sanity/editorial/EditorialDashboard.tsx, tests/unit/editorial-dashboard-markup.test.ts</files>
  <action>
Three edits in the component, then the markup-test updates. Note explicitly, in your commit message and in the SUMMARY: the previous quick task pinned this file's panel header subtitle as untouchable — that restriction is LIFTED for this task, because changing it is the point.

**Edit A — the box guard (~line 454).**

Rename `promoteDetailBoxHasBody` to `promoteActionsBoxHasBody` (the box no longer has a "detail"; it has actions) and redefine it as: `gateVariant === 'ready'` OR `Boolean(pipeline.promote.actionUrl)`. It stays declared in the same place, after `gateVariant`. Replace its comment with one that carries all four of these points:

  - The box carries its own padding and background, so it must not render at all when it has nothing inside — an unconditionally-rendered box paints a visible empty rectangle rather than disappearing. That was always the reason for this guard.
  - Its body is now exclusively actionable: the failure run link, and the production-release button in the ready state. It never carries explanatory copy — `resolvePromoteRow()` returns empty `title`/`detail` in every branch, and the panel header subtitle is the single narrator of release status.
  - The `ready` term is load-bearing and must not be dropped: it is what keeps the only control that starts a real production release mounted. Deriving this guard from title/detail again — as it was before — makes that button silently vanish, because the copy it used to depend on no longer exists.
  - The `actionUrl` term is equally load-bearing: the failed-staging and failed-production states have no other body, and losing it hides the run link that is their only actionable part.

**Edit B — the box JSX (~lines 789-836).**

Keep the outer `Stack` and its `editorial-dashboard__pipeline-detail` / `--dimmed` className expression exactly as they are, still keyed off `pipeline.promote.dimmed`, and keep it wrapped in the guard (now `promoteActionsBoxHasBody`). Inside it:

  - Delete both `<Text>` blocks that render `pipeline.promote.title` and `pipeline.promote.detail`, including their ternaries. After this edit the component must not reference those two properties anywhere — not in JSX, and not quoted in a comment (a markup assertion added below greps the whole file for them).
  - Keep the `pipeline.promote.actionUrl` link block byte-identical, still gated on `pipeline.promote.actionUrl`, still centred.
  - Keep the `gateVariant === 'ready'` button block byte-identical, including `productionPublishDisabled({promoteButtonDisabled: pipeline.promote.buttonDisabled, hasPreviewedStaging})`, `loading={releaseBusy}`, the click handler and `style={{minHeight: 44}}`. Do not restate its label text in any new comment — an existing test asserts that label appears exactly once in this file.

The visible result must be identical to today's minus the two text lines: same tinted rounded box, same dimming, same centred link, same centred button, same disabled rules.

**Edit C — the panel header subtitle (~lines 676-687).**

Import `releasePanelSubtitle` from `./pipelineView`, alongside the three helpers already imported from that module.

Derive it once, next to `pipelineDetail` (~line 527) — NOT next to `gateVariant`, which is declared ~70 lines before `publicationCard` exists: `const panelSubtitle = releasePanelSubtitle({segments: pipeline.segments, modifiedCount: publicationCard.total, notStarted: pipeline.promote.notStarted, requestError: releaseError})`.

Then replace the whole body of the subtitle `<Text size={1} muted>` under the « Mettre le site à jour » heading with `{panelSubtitle}` — the inline ternary, the template literal, the `pluralize` call and the not-started clause all leave the JSX. Keep the `<Text size={1} muted>` element itself, its props and its position unchanged.

Add a short comment above the derivation: this one line is now the only place the dashboard narrates which stage of the release is currently happening, because the box under the pipeline was emptied at the user's explicit request; the eight-branch decision lives in `releasePanelSubtitle()` so it has real behavioural tests instead of an eight-way ternary in JSX.

Check whether `pluralize` is still used elsewhere in the file after this edit (it is — several other call sites) and leave its import alone. `npm --prefix sanity run lint` is the gate on that.

**Markup test updates in `tests/unit/editorial-dashboard-markup.test.ts`.** Re-point, do not delete:

  1. The describe at lines ~62-85 (« panel header subtitle gates the not-started clause on the promote row flag ») must be inverted and renamed to describe the new wiring. Keep the same slicing technique (from the first « Mettre le site à jour » to the next `</Text>`). Assert that the slice renders `{panelSubtitle}`, and that it no longer contains `publicationCard.total` or `pipeline.promote.notStarted` — with a message recording that the decision moved into `releasePanelSubtitle()` in `pipelineView.ts` so it could get real behavioural tests, and that re-inlining any branch of it here would put the panel's status narration back outside test coverage. Then add a second case asserting the derivation exists exactly once and passes all four inputs (`pipeline.segments`, `publicationCard.total`, `pipeline.promote.notStarted`, `releaseError`) — each with its own message, since a dropped input silently disables whole branches of the subtitle.
  2. The describe at lines ~93-151 (« pipeline-detail box is skipped entirely when it has no body ») must be re-pointed at the new guard: rename it around the box now holding only actions; assert `promoteActionsBoxHasBody` is derived exactly once as a single expression; assert that derivation references `gateVariant === 'ready'` (message: this term is the only thing keeping the production-release button mounted now that the copy it used to depend on is gone) and `pipeline.promote.actionUrl` (message: the failed states have no other body); assert the box itself is still wrapped in the guard rather than its children; and assert the old identifier `promoteDetailBoxHasBody` appears nowhere in the file.
  3. Replace the case « makes each text line inside the box independently conditional » with the opposite guard: the component must not reference `pipeline.promote.title` or `pipeline.promote.detail` anywhere in its source — message: those are empty strings in every branch by contract, and rendering them again is how the copy the user asked to remove creeps back in.
  4. Keep the case « keeps the box and its action link intact rather than deleting them », and add to it that the ready-state button block survives — assert both `productionPublishDisabled(` and `gateVariant === 'ready'` are present, with a message naming this as the control that triggers a real production release.
  5. Leave every other describe in the file untouched, in particular the one counting the « Publier… » label exactly once and the `pipelineView` wiring block from the previous task.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-markup.test.ts</automated>
    <automated>npm run test:unit</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>git diff --name-only 7d525d664002c519e9e995cbce1cb04d003698c8 -- sanity/editorial/EditorialDashboard.css sanity/editorial/releaseGate.ts sanity/editorial/dashboardLogic.ts</automated>
  </verify>
  <done>The panel header subtitle renders a single derived string from the tested helper; the promote box renders only the failure link and the ready-state publish button, guarded by `gateVariant === 'ready' || actionUrl`; the component references neither `pipeline.promote.title` nor `pipeline.promote.detail` any more. The full unit suite and the Studio lint pass, and the `git diff --name-only` command prints nothing — the stylesheet and the two protected modules were not touched.</done>
</task>

<task type="auto">
  <name>Task 4: Resync the one README sentence this change makes false</name>
  <files>sanity/README.md</files>
  <action>
`sanity/README.md` documents the dashboard for Romane in French. Its pipeline paragraph (~lines 80-94) ends with a sentence stating that on failure, a message under the bar says which stage failed. That message was the promote box copy this plan removes, so the sentence is now false.

Rewrite that closing sentence only: the status of the release — including which stage failed — is now reported by the small line under the **Mettre le site à jour** heading at the top of the panel, and the zone under the progress bar carries only buttons and links, never explanatory text.

Leave everything else in the file untouched: the status-name list above (« Site de test à jour », « Site en ligne à jour » and friends are `deploymentState()` labels this plan does not change), the « Studio » / « Site en ligne » stage description, the blue modified-content state, the never-green-while-drafts-pending rule, the central button lifecycle and its caption, and everything from « ## Collections photo » onward.
  </action>
  <verify>
    <automated>git diff --stat 7d525d664002c519e9e995cbce1cb04d003698c8 -- sanity/README.md</automated>
    <automated>grep -c "Mettre le site à jour" sanity/README.md</automated>
  </verify>
  <done>The README's pipeline paragraph no longer promises a failure message under the progress bar and instead points at the panel header line, with the rest of the file untouched. `git diff --stat` shows one file with a sentence-sized hunk.</done>
</task>

<task type="auto">
  <name>Task 5: Run every gate, prove scope, redeploy the Studio, request the live confirmation</name>
  <files>(no source changes — verification, deploy and sign-off only)</files>
  <action>
Run the four blocking gates in this order, fixing anything they surface before moving on: `npm run test:unit` (full suite, not only the four touched test files), `npm run typecheck`, `npm --prefix sanity run lint`, `npm --prefix sanity run build`.

If a failure in the full suite is clearly unrelated to this plan's files, record it in the SUMMARY rather than chasing it — this repo runs concurrent sessions and the working tree can carry other agents' changes. Do not revert, commit or "clean up" anything outside this plan's eight files.

Then prove the scope boundary held, against base SHA `7d525d664002c519e9e995cbce1cb04d003698c8`:

  - `git diff --name-only 7d525d664002c519e9e995cbce1cb04d003698c8 -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts sanity/editorial/EditorialDashboard.css .github/workflows/` must print nothing.
  - `grep -cF "= 'pending' | 'active' | 'done' | 'failed'" sanity/editorial/deployment.ts` must print `1` — the segment-kind union was not widened.
  - `git diff --stat 7d525d664002c519e9e995cbce1cb04d003698c8 -- sanity/ tests/` must list only this plan's eight files. Call out any additional file explicitly in the SUMMARY so a concurrent session's work is never silently attributed to this plan.

Then run the single most important manual check before deploying, since it is the one thing tests can only approximate: read the rendered box JSX one last time and confirm by eye that the « Publier… » button block is reachable in the ready state without any dependency on title or detail. This is the control that triggers a real production release.

Then redeploy the live Studio with `npm run deploy --prefix sanity`. This is the established, pre-authorised pattern for shipping a Studio change in this session — do not stop and ask before running it. Wait for it to report success before presenting the human-check.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm --prefix sanity run build</automated>
    <automated>git diff --name-only 7d525d664002c519e9e995cbce1cb04d003698c8 -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts sanity/editorial/EditorialDashboard.css .github/workflows/</automated>
    <human-check>
Le Studio a été redéployé. Ouvre https://atelier-jacqueline-suzanne.sanity.studio/ et fais un **rechargement forcé** (Cmd+Shift+R) pour charger le nouveau bundle — cet environnement n'a pas de session navigateur authentifiée, donc toi seul peux confirmer le rendu.

Si possible, vérifie sur **trois moments différents** du process :

**A — au repos** (rien à publier, ou des brouillons en attente, avant tout clic) :
- (a) la zone sous la barre de progression n'affiche **aucun texte explicatif** — ni titre, ni phrase ;
- (c) la petite ligne sous « Mettre le site à jour » décrit bien l'état courant (« Aucune modification publique en attente. » au repos, ou le compte de contenus modifiés + « Rien n'a été lancé pour l'instant… » si des brouillons attendent).

**B — pendant la reconstruction du site de test** (juste après avoir cliqué « Mettre le site à jour ») :
- (a) toujours aucun texte sous la barre — c'est exactement la phrase « En attente du site de test… / L'étape 2… » que tu avais signalée, elle ne doit plus apparaître nulle part ;
- (c) la ligne sous le titre dit maintenant « Mise à jour du site de test en cours… ».

**C — une fois le site de test à jour (état prêt à publier)** :
- (b) **le point le plus important** : le bouton « Publier sur le site en ligne » est toujours là sous la barre, et il fonctionne toujours (il se débloque après avoir ouvert l'aperçu du site de test via le bouton rond, comme avant) ;
- (c) la ligne sous le titre dit « Site de test à jour — prêt à publier sur le site en ligne. ».

Réponds « approuvé » si tout est bon, ou décris ce qui cloche en précisant le moment (A/B/C) et le point (a/b/c).
    </human-check>
  </verify>
  <done>All four gates pass, the three scope-proof commands come back clean, the ready-state publish button was confirmed reachable by reading the rendered JSX, the Studio is redeployed, and the user's confirmation — or their correction list — is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Studio UI → GitHub Actions `repository_dispatch` | The maintainer-only control that starts a real production release crosses here. No untrusted input is introduced by this plan; the risk is availability of the control itself. |
| `resolvePromoteRow()` output → rendered DOM | Strings computed from GitHub API responses and local error objects are rendered as text. This plan REMOVES two of those render sites. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g49-01 | Denial of Service | The « Publier sur le site en ligne » button in `EditorialDashboard.tsx` | high | mitigate | Emptying the promote copy collapses the existing box guard to `Boolean(actionUrl)`, which is false in the ready state, removing the only production-release control. Task 3 re-derives the guard from `gateVariant === 'ready'`; Task 1 inverts the `release-gate.test.ts` tripwire; Task 3 adds markup assertions on both guard terms; Task 5 adds an explicit pre-deploy read of the rendered JSX. |
| T-g49-02 | Information Disclosure | `resolvePromoteRow()`'s `requestError` branch | low | mitigate | The raw GitHub API error string is no longer rendered into the maintainer-facing UI (Task 1), and `releasePanelSubtitle()` is tested to not interpolate it (Task 2's no-raw-error-leak case). Net reduction in surfaced internals. |
| T-g49-03 | Tampering | Package installs | low | accept | This plan installs no packages — no `npm`/`pip`/`cargo` install task exists in any task, so no package legitimacy gate applies. |
</threat_model>

<verification>
- `npm run test:unit` — full suite green, including the four updated/added test files.
- `npm run typecheck` — 0 errors.
- `npm --prefix sanity run lint` — clean (proves the subtitle rewrite left no unused import in the component).
- `npm --prefix sanity run build` — Studio bundles with the extended helper module.
- `git diff --name-only <base> -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts sanity/editorial/EditorialDashboard.css .github/workflows/` — empty.
- `grep -cF "= 'pending' | 'active' | 'done' | 'failed'" sanity/editorial/deployment.ts` — `1`.
- `npm run deploy --prefix sanity` — reports success.
</verification>

<success_criteria>
- CHANGE 1: all eight branches of `resolvePromoteRow()` return empty `title` and `detail`; every behaviour field unchanged; the eight-state sweep test locks it.
- CHANGE 2: the production-release button and the failure link survive with identical visible behaviour, now guarded by `gateVariant === 'ready' || actionUrl` instead of copy — proven by re-pointed guards in `release-gate.test.ts` and `editorial-dashboard-markup.test.ts`, and confirmed live in state C of the human check.
- CHANGE 3: the panel header subtitle narrates all eight release states in the specified priority order, keeps the existing not-started branch byte-identical, never prints the raw technical error, and never reintroduces step numbering.
- CHANGE 4: the decision lives in a pure `releasePanelSubtitle()` in `pipelineView.ts` with behavioural tests covering all eight states AND six explicit priority-order cases.
- No test deleted; three re-pointed with recorded reasons.
- Every gate green, scope git-proven, Studio redeployed, user asked to confirm across three pipeline states.
</success_criteria>

<output>
Create `.planning/quick/260813-g49-dashboard-move-all-promote-box-status-te/260813-g49-SUMMARY.md` when done.

End the SUMMARY with the human-verification request from Task 5 verbatim: ask the user to hard-refresh and confirm, across a few different pipeline states if possible (au repos, pendant la reconstruction du site de test, et une fois prêt à publier), that (a) the promote box under the pipeline never shows explanatory title/detail text at any stage, (b) the « Publier sur le site en ligne » button still appears and still works once ready, and (c) the header subtitle under « Mettre le site à jour » now narrates whichever stage is currently happening.

Also record in the SUMMARY:
- The coupling bug found during scoping and fixed here: emptying the promote copy would have silently deleted the production-release button, because the button was nested inside a box whose existence was derived from that copy. `tests/unit/release-gate.test.ts` had already documented the coupling in a failure message; it is now inverted to assert the opposite contract, with reachability guarded structurally in `tests/unit/editorial-dashboard-markup.test.ts`.
- That the previous quick task's "do not touch the header subtitle JSX" restriction was deliberately lifted for this task, so nobody re-imposes it from the older plan by habit.
- That the raw `requestError` string is now absent from the UI entirely — a deliberate consequence of the no-text-in-the-box instruction, not an oversight — and that the retry for that state stays reachable through the round gate button, which `gateClickAction('failed')` routes to a release retry.
- The final subtitle wording chosen for each of the eight states, so the maintainer can review the copy in one place without reading the code.
</output>
