---
phase: quick-260714-d6h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/EditorialDashboard.tsx
autonomous: true
requirements:
  - QUICK-260714-d6h
must_haves:
  truths:
    - "The 'À faire maintenant' section headers visually distinguish urgency tiers via Sanity @sanity/ui tone colors (critical / caution / primary / default), driven off group.tone."
    - "Each attention card shows a left accent bar colored by its section's urgency tier."
    - "The 'Manque' line is a single scannable, truncated line (max 2 items + '+N autre(s)') instead of a wall of text."
    - "The per-card completion count reads urgency by color (tonal badge), not just as muted text."
    - "No @sanity/ui components outside the existing import set and no new dependencies are introduced."
    - "Data/logic functions (getDocumentChecks, summarizeChecks, buildAttentionGroups, attentionPriority, editorialStatus, deploymentLabel) are unchanged — this is presentation-only."
    - "No tool-scoped router link (ToolLink) JSX is present; the media quick action still uses <Link href> with basePath."
  artifacts:
    - sanity/editorial/EditorialDashboard.tsx
  key_links:
    - "AttentionSection passes its group.tone down to each ContentRow (accent bar + tonal count badge); the tone values still originate from buildAttentionGroups, which stays untouched."
---

<objective>
Improve the Sanity Studio EditorialDashboard "À faire maintenant" panel so a non-technical editor can tell at a glance what is urgent vs. minor, and so the priority-section grouping reads by urgency.

Two presentation-only improvements, both inside `EditorialDashboard.tsx`:
1. Priority-section styling — make the section headers (À corriger / Modifications à publier / À finaliser / À améliorer) and their numbered count badges visually distinct by urgency using @sanity/ui `tone` conventions.
2. Card density/scannability — a left accent bar per urgency tier on each card, a truncated one-line "Manque" list, and a tonal completion-count badge.

Purpose: reduce the current repetitive, text-heavy read of the panel so urgency registers instantly, while staying native to Sanity Studio (no custom design system) and changing zero data/logic.

Output: an updated `sanity/editorial/EditorialDashboard.tsx` (presentational changes only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@sanity/editorial/EditorialDashboard.tsx
@sanity/editorial/checks.ts

Key facts for the executor:
- This is Sanity Studio chrome (the CMS admin app under `sanity/`), NOT the public Astro site. It uses @sanity/ui: `Badge, Box, Button, Card, Flex, Grid, Heading, Spinner, Stack, Text` and `tone` props (`'default' | 'primary' | 'positive' | 'caution' | 'critical'`). Stay within these — no new imports, no new dependencies.
- The panel is built by `AttentionSection` (a section card: tonal-less outer Card + header + a Stack of `ContentRow`s). `buildAttentionGroups` produces up to FOUR groups with tones: `blocking`→"À corriger"/critical, `publish`→"Modifications à publier"/caution, `finish`→"À finaliser"/primary, `recommended`→"À améliorer"/default. All styling MUST be generic across all four groups (drive off `group.tone`), not hardcoded to three.
- Presentation-only: do NOT modify `getDocumentChecks`, `summarizeChecks`, `buildAttentionGroups`, `attentionPriority`, `editorialStatus`, `deploymentLabel`, or the GROQ `query`. Keep all French UI copy exactly as-is.
- Router-crash guard (from commit `6b3de8d`): the "Photos et crédits" quick action must keep using `<Link href={...}>` with `useWorkspace().basePath` — do NOT introduce any tool-scoped router link element in this view.
- No live `sanity dev` server is available in the sandbox. Verify via TypeScript + ESLint + careful visual/logic reasoning about the JSX. The SUMMARY.md must note that live visual verification is pending a human check.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make priority-section headers urgency-distinct</name>
  <files>sanity/editorial/EditorialDashboard.tsx</files>
  <action>
In `AttentionSection`, restyle the header so each section reads by urgency using @sanity/ui tone conventions (no hardcoded colors):

- Replace the plain header `Box` with a `Card` carrying `tone={group.tone}`, so the header strip picks up its tier's tonal background (critical → red-tinted, caution → amber-tinted, primary → blue-tinted, default → neutral) from Sanity's own palette. Keep the outer section `Card` neutral with its existing `radius={3} shadow={1} overflow="hidden"`, and keep the existing bottom-divider under the header.
- Replace the pill count `Badge` with a fixed-size circular count element: a small `Card` (about 22–24px square) using the same `tone={group.tone}`, with `style` giving a fully-round `borderRadius` and centering its content (flex, centered), containing a `Text size={0} weight="semibold"` showing `group.rows.length`.
- Promote the section title `Text` to `weight="bold"` (keep `size={1}`); leave the description as `Text muted size={0}`.

Keep everything generic across all four groups from `buildAttentionGroups` — everything drives off `group.tone`; do not special-case a single tier. Do not touch `buildAttentionGroups`, `attentionPriority`, or any data function. Keep all French copy exactly as-is.
  </action>
  <verify>
    <automated>cd sanity && node_modules/.bin/tsc --noEmit && node_modules/.bin/eslint editorial/EditorialDashboard.tsx</automated>
  </verify>
  <done>Each of the (up to four) section headers renders with its tier's Sanity tone background and a circular tonal count badge; the title is bold; all four tier tones (critical/caution/primary/default) map correctly off group.tone; no data/logic function changed; tsc + eslint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten attention cards for at-a-glance scannability</name>
  <files>sanity/editorial/EditorialDashboard.tsx</files>
  <action>
Thread the section's urgency tone into each card and reduce visual density in `ContentRow`:

- Add an `accentTone: DashboardTone` prop to `ContentRow` and pass `accentTone={group.tone}` from `AttentionSection`'s row map (`<ContentRow ... accentTone={group.tone} />`).
- Left accent bar: restructure `ContentRow`'s inner `Card` so its top-level child is a `Flex` whose FIRST child is a thin tonal accent bar — a `Card tone={accentTone}` roughly 4px wide that stretches the full row height. Rely on the outer Flex's default cross-axis stretch (do NOT set `align` on that outer wrapper Flex); give the bar `style={{width: 4}}`. Follow the bar with a `Box` that holds the existing row content and carries the padding — move the current `padding={3}` off the outer card onto this inner `Box`. The existing inner content Flex keeps `align="center"`. For the lowest tier (`default` tone) the bar is intentionally neutral/near-invisible, signalling "minor". If a plain tonal Card background renders too faint to read as an accent bar, make it legible within @sanity/ui tokens only (e.g. a marginally wider bar) — never a hardcoded hex.
- Truncate the "Manque" line: slice missing checks to the first 2 (currently 3), keep the "· +N autre(s)" suffix computed from the real remaining count, and constrain the line to a single line with `textOverflow="ellipsis"` so long generated labels never wrap into a paragraph.
- Tonal completion count: render the right-side completion indicator so urgency reads by color — put the numeric count inside a `Badge mode="light" tone={accentTone}` and keep the French "à compléter" wording adjacent as muted text. Keep the existing "›" chevron and the `IntentLink` edit-intent wiring.

Do not change `editorialStatus`, `summarizeChecks`, `getDocumentChecks`, or the `IntentLink` edit intent. Keep the "Photos et crédits" quick action on its existing `<Link href>` + `basePath` pattern — do not add any tool-scoped router link element anywhere in this view. Keep all French copy exactly as-is.
  </action>
  <verify>
    <automated>cd sanity && node_modules/.bin/tsc --noEmit && node_modules/.bin/eslint editorial/EditorialDashboard.tsx && ! grep -q '<ToolLink' editorial/EditorialDashboard.tsx</automated>
  </verify>
  <done>Each card shows a left accent bar colored by its section's tier; the Manque line is a single truncated line (max 2 items + '+N autre(s)'); the completion count is a tonal badge whose color signals urgency; no data/logic function changed; no tool-scoped router link element present; tsc + eslint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-only change inside an authenticated Studio admin view; no new inputs, endpoints, or data mutations are introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q260714-01 | Tampering | Router state (ToolLink reintroduction) | low | mitigate | Keep the media quick action on `<Link href>`+`basePath`; automated gate `! grep -q '<ToolLink' ...` blocks reintroducing a tool-scoped link that previously crashed Studio's router (commit 6b3de8d). |
| T-q260714-SC | Tampering | npm/pip/cargo installs | low | accept | No packages are installed or added — change stays within the existing @sanity/ui import set; no supply-chain surface. |
</threat_model>

<verification>
- `cd sanity && node_modules/.bin/tsc --noEmit` passes (no new type errors).
- `cd sanity && node_modules/.bin/eslint editorial/EditorialDashboard.tsx` passes.
- `grep '<ToolLink' sanity/editorial/EditorialDashboard.tsx` finds no matches (no tool-scoped link element).
- Manual/logic review: no import list change; `getDocumentChecks`, `summarizeChecks`, `buildAttentionGroups`, `attentionPriority`, `editorialStatus`, `deploymentLabel`, and the GROQ `query` are byte-for-byte unchanged; all French copy unchanged.
- SUMMARY.md records that live visual verification (`sanity dev`) is pending a human check.
</verification>

<success_criteria>
- Section headers are urgency-distinct via @sanity/ui `tone` (all four groups), with a circular tonal count badge.
- Each card has a tier-colored left accent bar; the "Manque" line is one truncated line; the completion count is a tonal badge.
- Zero data/logic changes; zero new dependencies; French copy intact; no ToolLink reintroduced.
- tsc + eslint green.
</success_criteria>

<output>
Create `.planning/quick/260714-d6h-improve-the-sanity-studio-editorialdashb/260714-d6h-SUMMARY.md` when done, noting that live visual verification in `sanity dev` is pending a human check.
</output>
