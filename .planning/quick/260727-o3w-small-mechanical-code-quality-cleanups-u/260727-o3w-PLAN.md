---
phase: quick-260727-o3w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/editions/[slug].astro
  - src/pages/en/editions/[slug].astro
  - src/pages/galleries/[slug].astro
  - src/pages/en/galleries/[slug].astro
  - src/layouts/BaseLayout.astro
  - src/components/ContactForm.astro
autonomous: true
requirements:
  - AUDIT-CLEANUP-1   # remove/wire 4 unused Props interfaces
  - AUDIT-CLEANUP-2   # explicit is:inline on JSON-LD script
  - AUDIT-CLEANUP-3   # log swallowed ContactForm submission error

must_haves:
  truths:
    - "`npm run typecheck` (astro check) reports zero of the 4 original 'Props declared but never used' hints"
    - "`npm run typecheck` reports zero of the JSON-LD 'missing is:inline' hint"
    - "`npm run typecheck` reports no NEW errors or hints introduced by any of the 6 file edits"
    - "A ContactForm fetch/submit failure writes the actual caught error to the browser console before the generic error UI renders"
    - "`npm run build`, `npm run test:unit`, and the full Playwright e2e suite all pass"
  artifacts:
    - "src/pages/editions/[slug].astro (Props interface resolved)"
    - "src/pages/en/editions/[slug].astro (Props interface resolved)"
    - "src/pages/galleries/[slug].astro (Props interface resolved)"
    - "src/pages/en/galleries/[slug].astro (Props interface resolved)"
    - "src/layouts/BaseLayout.astro (is:inline on JSON-LD script)"
    - "src/components/ContactForm.astro (console.error in catch)"
  key_links:
    - "Each route file's `Astro.props` destructure stays correctly typed via getStaticPaths inference after the Props interface is removed"
---

<objective>
Apply three small, mechanical, pre-confirmed code-quality cleanups from a codebase audit. No re-audit — the findings were verified directly by reading the files and running `astro check`.

1. Resolve the 4 "interface Props declared but never used" `astro check` hints in the FR/EN édition and gallery detail route files.
2. Add explicit `is:inline` to the JSON-LD `<script>` in `BaseLayout.astro`.
3. Log the currently-silently-swallowed ContactForm submission error before showing the generic error UI.

Purpose: Clear the outstanding `astro check` hints and close a zero-visibility debugging gap in the contact form (which becomes functional at Phase 5's OVH cutover), with no behavior change beyond added console visibility.
Output: 6 files edited; typecheck/build/unit/e2e all green.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Route files (Fix 1) — all four share the SAME structure: a
# `getStaticPaths(...) satisfies GetStaticPaths` that returns `props`, an
# unreferenced `interface Props`, and a `const { edition|gallery } = Astro.props`
# destructure whose type already comes from getStaticPaths inference.
@src/pages/editions/[slug].astro
@src/pages/en/editions/[slug].astro
@src/pages/galleries/[slug].astro
@src/pages/en/galleries/[slug].astro

# Fix 2 target — JSON-LD script tag; sibling script on the next line already uses is:inline.
@src/layouts/BaseLayout.astro

# Fix 3 target — the client-side <script> submit handler's catch block.
@src/components/ContactForm.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the 4 unused Props interfaces in the détail route files</name>
  <files>src/pages/editions/[slug].astro, src/pages/en/editions/[slug].astro, src/pages/galleries/[slug].astro, src/pages/en/galleries/[slug].astro</files>
  <action>
    For EACH of the 4 files, independently confirm the situation before editing (drift-catching is a goal of this audit, so do not assume all 4 are identical — verify each):
    (a) `getStaticPaths` returns a `props` object (props.edition for éditions, props.gallery for galleries) and is exported with `satisfies GetStaticPaths`;
    (b) the `const { edition } = Astro.props` (éditions) or `const { gallery } = Astro.props` (galleries) destructure IS present and used downstream;
    (c) the local `interface Props { ... }` block is never referenced anywhere in the file.

    When (a)+(b)+(c) hold — which is the confirmed current state of all 4 files — the props are ALREADY type-safe via Astro's getStaticPaths inference (InferGetStaticPropsType), so the `interface Props` block is genuinely redundant dead code. Delete the entire `interface Props { ... }` block (the FR édition file's block spans its `interface Props`/`edition: Edition`/closing-brace lines; the EN édition, FR gallery, and EN gallery files each have the analogous 3-line block). Leave the `const { edition|gallery } = Astro.props` line and everything else untouched — its type continues to come from getStaticPaths inference.

    Only if verification on a given file UNEXPECTEDLY shows `Astro.props` is untyped (e.g. no getStaticPaths inference, resolving to `any`) should you instead annotate the destructure with `: Props` to close a real gap rather than delete — but this is NOT expected for any of the 4 files. If you hit that case on any file, note it in the SUMMARY.

    Do NOT touch the `import type { Edition }` / `import type { Gallery }` lines — those types are still used elsewhere (getStaticPaths, other annotations). Removing the Props interface must not leave those imports unused; if `astro check` flags a now-unused type import after the edit, that is a signal you removed too much — re-check.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -A1 -Ei "editions/\[slug\]|galleries/\[slug\]|declared but never used" || echo "NO Props hints remain in route files"</automated>
  </verify>
  <done>`npm run typecheck` no longer reports any "declared but never used" hint for the 4 route files, and reports no NEW error or unused-import hint introduced by the deletions. Each of the 4 files was verified independently before editing.</done>
</task>

<task type="auto">
  <name>Task 2: Add is:inline to the JSON-LD script and log the ContactForm submission error</name>
  <files>src/layouts/BaseLayout.astro, src/components/ContactForm.astro</files>
  <action>
    Fix 2 — BaseLayout.astro: On the JSON-LD structured-data script tag (`<script type="application/ld+json" set:html={serializedStructuredData}>`), add the `is:inline` directive so Astro's "do not process/bundle this script" behavior is explicit rather than incidental, silencing the astro check hint. This mirrors the sibling `<script is:inline ...>` directly below it. Add only the directive; do not change the tag's type, set:html expression, or position.

    Fix 3 — ContactForm.astro: In the client-side submit handler's `catch` block (currently a bare `catch { renderSubmissionError(); }`), bind the caught value (change to `catch (error)`) and add a `console.error(...)` call that logs a short human-readable prefix plus the actual caught `error`, placed BEFORE the existing `renderSubmissionError()` call. Keep `renderSubmissionError()` and the `finally` block exactly as they are. This is a minimal visibility-only addition — do NOT add retry logic, telemetry, user-facing detail, or any other behavior. `console` is already a declared global in eslint.config.mjs and there is no no-console rule, so this introduces no lint error; the caught value's `unknown` type is fine to pass to console.error (no cast needed).
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -Ei "is:inline|BaseLayout" || echo "NO is:inline hint remains"; grep -n "console.error" src/components/ContactForm.astro; grep -n 'application/ld+json' src/layouts/BaseLayout.astro | grep -q 'is:inline' && echo "is:inline PRESENT on JSON-LD"</automated>
  </verify>
  <done>The JSON-LD script tag carries `is:inline` and its astro check hint is gone; the ContactForm catch block binds the error and calls `console.error` with it before `renderSubmissionError()`; `npm run typecheck` reports no new errors/hints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries are introduced. All three fixes are internal, mechanical, and non-behavioral except for added browser-console logging.

| Boundary | Description |
|----------|-------------|
| visitor browser → Web3Forms API (pre-existing) | ContactForm already POSTs to api.web3forms.com; this change only adds client-side `console.error` visibility on failure, no new data flow, no new endpoint, no change to what is sent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-o3w-01 | Information Disclosure | `console.error(error)` in ContactForm catch | low | accept | Logs only the fetch/JS error object to the visitor's OWN browser console (not a server, not third parties). No secret is in scope: the access key is a client-side `PUBLIC_` var already shipped to the browser, and the error object is the caught fetch/network error, not request/response bodies. No user PII is added to the log. |
</threat_model>

<verification>
Run the full verification suite from the repo root, in order. All must pass:

1. `npm run typecheck` — confirms all 4 original Props hints AND the JSON-LD is:inline hint are gone, and that NO new type error/hint was introduced (critical for Fix 1: an incorrect Props removal or leftover unused type import would surface here).
2. `npm run test:unit` — Vitest unit suite green.
3. `npm run build` — static build succeeds (route files feed getStaticPaths at build time).
4. `npx playwright test` — full e2e suite green; `edition.spec.ts` / `gallery.spec.ts` specifically exercise the 4 route files Fix 1 touches, catching any regression in the détail pages.

Also spot-confirm the JSON-LD block still renders in built output (the `<script type="application/ld+json">` tag with serialized structured data should still be present on gallery detail pages) — `is:inline` changes processing, not rendering.
</verification>

<success_criteria>
- All 4 route files: no "Props declared but never used" hint; `Astro.props` destructure still correctly typed via getStaticPaths inference; each file verified independently.
- BaseLayout JSON-LD script has explicit `is:inline`; its astro check hint is gone.
- ContactForm catch block logs the actual caught error via `console.error` before `renderSubmissionError()`; no other behavior added.
- `npm run typecheck`, `npm run test:unit`, `npm run build`, and `npx playwright test` all pass with no new failures or hints.
- Exactly these 6 files changed; no other files touched.
</success_criteria>

<output>
Create `.planning/quick/260727-o3w-small-mechanical-code-quality-cleanups-u/260727-o3w-SUMMARY.md` when done. In it, record for EACH of the 4 route files whether the Props interface was deleted (expected) or annotated (unexpected — note why), and confirm the exact console.error wording added to ContactForm.
</output>
