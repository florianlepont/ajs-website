---
phase: quick-260728-ek0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ContactPageBody.astro
  - src/components/ContactForm.astro
  - tests/e2e/contact.spec.ts
  - tests/e2e/about.spec.ts
  - tests/e2e/visual.spec.ts-snapshots/contact-form.png
autonomous: false
requirements:
  - sketch-012-A3
user_setup: []

must_haves:
  truths:
    - "Visiting /contact/ and /en/contact/ on desktop shows a giant display 'Contact' title (larger than --editorial-page-title-size), the whole composition centered in one viewport with symmetric top/bottom margins and NO vertical scroll."
    - "The form panel has a hairline ink border and NO offset drop-shadow; its Nom/E-mail/Message fields render as tinted bordered boxes."
    - "The real <ContactForm> still submits (mocked success shows the localized thank-you), and its honeypot + per-field validation still work, in both locales."
    - "On a <=800px viewport the channels stack above the form, form controls are full-width and >=44px tall, and the page scrolls normally (no-scroll is desktop-only)."
    - "The Instagram channel (.contact-page__social) still links @ajs_romanelepont, opens in a new tab (target=_blank rel=noopener noreferrer), and appears before the form in DOM/reading order."
  artifacts:
    - src/components/ContactPageBody.astro
    - src/components/ContactForm.astro
    - tests/e2e/contact.spec.ts
    - tests/e2e/about.spec.ts
    - tests/e2e/visual.spec.ts-snapshots/contact-form.png
  key_links:
    - ".contact-page keeps `--color-accent: var(--color-ink)` so the entire subtree (including ContactForm's primitives) renders monochrome ink — this keeps the 'Contact uses neutral black accents' e2e green while implementing A3."
    - "ContactPageBody passes `locale` + `publicEmail` to <ContactForm> unchanged; the caller prop contract (contact.astro / en/contact.astro) is untouched."
    - "Desktop no-scroll = flex column + justify-content:center + min-height calc(100svh - chrome reserve) + a BOUNDED clamp() header gap — never justify-content:space-between (the sketch README documents that space-between pools a single void on short-wide viewports)."
---

<objective>
Implement the winning sketch-012 variant **A3 — "Grand titre, sans scroll"** in the real `src/components/ContactPageBody.astro` (markup + `<style>`), replacing the current two-column editorial grid whose form panel uses a hard `10px 10px 0` offset shadow — the exact motif Romane already rejected on the Éditions tiles (sketch 009).

Purpose: bring the Contact page up to the live "Bold & graphic" visual language with a giant typographic title, a one-viewport-tall desktop composition (symmetric margins, no scroll), and a hairline-bordered form panel with boxed/tinted fields — while keeping the real `<ContactForm>` island wired exactly as it is today and keeping the CI gate green.

Output: redesigned ContactPageBody + adapted ContactForm field/submit styling + the handful of locked e2e contracts the redesign intentionally changes, updated to match.

Source of truth for the design: `.planning/sketches/012-contact-page-composition/index.html` (the `.a3-*` rules, lines 256–385, and the `#variant-a3` markup, lines 656–716) and its `README.md` (why A3 looks the way it does — 3 rounds: bounded gap vs runaway space-between, boxed fields vs bare label+line, mobile tap-target/stacking fixes).
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/sketches/012-contact-page-composition/index.html
@.planning/sketches/012-contact-page-composition/README.md
@src/components/ContactPageBody.astro
@src/components/ContactForm.astro
@src/components/Input.astro
@src/components/Textarea.astro
@src/components/Button.astro
@src/pages/contact.astro
@src/pages/en/contact.astro
@src/layouts/BaseLayout.astro
@tests/e2e/contact.spec.ts
@tests/e2e/about.spec.ts
@tests/e2e/social-links.spec.ts
@tests/e2e/visual.spec.ts

# Real brand tokens live in BaseLayout.astro's `<style is:global>` :root block
# (lines ~243–325). The sketch's themes/default.css MIRRORS these but has
# DRIFTED: default.css defines `--weight-black: 900`, BaseLayout does NOT.
# Consequence: `var(--weight-black)` is undefined on the real site. Every
# --font-display consumer declares `--weight-semibold` (600); Unbounded loads
# ONLY its 900 weight file, so display text renders at 900 regardless. The
# real component MUST use `var(--weight-semibold)`, not `var(--weight-black)`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild ContactPageBody markup + style as A3, and adapt ContactForm field/submit styling</name>
  <files>src/components/ContactPageBody.astro, src/components/ContactForm.astro</files>
  <action>
  Replace ContactPageBody's markup and `<style>` block with the A3 composition. Keep the component's frontmatter (Props interface + destructuring) and the `<ContactForm locale={locale} publicEmail={publicEmail} />` mount call BYTE-IDENTICAL — only the surrounding markup/CSS changes. Keep the root `<div class="contact-page">` element and its `--color-accent: var(--color-ink)` custom-property override (this cascades to the whole subtree, keeps the page monochrome, and keeps the "Contact uses neutral black accents" e2e green). Because of that override, A3's pink accent (eyebrow color, field-focus border, channel hover-fill) renders as ink — this is intentional and correct; do NOT reintroduce pink on this page.

  MARKUP (mirror `#variant-a3`, index.html lines 656–716):
  - A header group `<div class="contact-page__header">` containing: `<p class="contact-page__eyebrow">Atelier Jacqueline Suzanne</p>`, `<h1 id="contact-title">Contact</h1>`, and `<p class="contact-page__lead">{intro}</p>`. Keep the `aria-labelledby="contact-title"` on the wrapping intro section/region and preserve `.contact-page__eyebrow`, `.contact-page h1`, `.contact-page__lead` class/selectors.
  - A `<div class="contact-page__grid">` (mirror `.a3-grid`, `1fr 1.3fr`): LEFT column holds the channels + meta; RIGHT column holds the form panel. Left column MUST precede the form panel in DOM order (social-links.spec asserts the social link precedes the `<form>`).
  - LEFT column: `<div class="contact-page__channels">` (mirror `.a3-channels`) with two channel anchors (mirror `.a3-channel`): (1) email `<a class="contact-page__detail" href={`mailto:${publicEmail}`}>` — inner row (`.contact-page__detail-inner`, flex space-between baseline) with a label span reading `{emailLabel}` and an arrow span reading the up-right arrow glyph, then a value span reading `{publicEmail}`; (2) Instagram — RENDER ONLY inside `{instagramLink && (...)}` — `<a class="contact-page__detail contact-page__social" href={instagramLink.url} target="_blank" rel="noopener noreferrer">` with label "Instagram", the arrow span, a value span reading `{instagramLink.label}`, and `<span class="sr-only">{newTabHint}</span>`. KEEP the `.contact-page__social` class and all three link attributes exactly (locked by social-links.spec). Then a `<div class="contact-page__meta">` (mirror `.a3-meta`) rendering `{location && <p>{location}</p>}` and `{availability && <p>{availability}</p>}` inside the existing `(location || availability)` guard, plus the existing `{otherLinks.length > 0 && (<ul class="contact-page__links">...)}` block preserved verbatim.
  - RIGHT column: `<section class="contact-page__form-panel" aria-labelledby="contact-form-title">` (mirror `.a3-form-panel`) containing `<h2 id="contact-form-title">{formHeading}</h2>`, `<p class="contact-page__form-sub">{formSubheading}</p>`, then the unchanged `<ContactForm .../>` mount.
  - REMOVE the old numbered editorial-sequence elements entirely: the `.contact-page__number` "02"/"03" spans on the channels and the `.contact-page__step` "01" on the form heading (A3 has no numbered sequence — the reachability test is updated in Task 2 to match). REMOVE the `.contact-page__form-heading` wrapper; the form panel now leads with the h2 + subheading directly.

  STYLE (translate the `.a3-*` rules, index.html lines 256–385, into `.contact-page__*` selectors), honoring the real tokens in BaseLayout.astro:
  - `.contact-page`: keep `--color-accent: var(--color-ink)`. Desktop layout — `display: flex; flex-direction: column; justify-content: center; max-width: var(--editorial-page-max); margin: 0 auto; box-sizing: border-box; padding: clamp(var(--space-md), 3.5vh, var(--space-2xl)) var(--editorial-page-padding-inline);` and `min-height: calc(100svh - var(--contact-chrome-reserve, 220px));`. The `--contact-chrome-reserve` accounts for the shared SiteHeader + footer that sit above/below `<main>` (BaseLayout renders both; contact does NOT pass hideFooter). Use `svh` (matches the Phase 06 mobile-hero decision to prefer svh over vh). `justify-content: center` produces the symmetric top/bottom margins when content is shorter than the available height. Do NOT use `justify-content: space-between`.
  - `.contact-page__header { margin-bottom: clamp(var(--space-xl), 6vh, 88px); }` — this BOUNDED clamp is the anti-runaway-gap fix (README Round 1); the header + grid move as one centered group.
  - `.contact-page h1` (the giant title, requirement 1 — explicitly NOT `--editorial-page-title-size`): `font-family: var(--font-display); font-weight: var(--weight-semibold); font-size: clamp(64px, min(15vw, 20vh), 220px); line-height: 0.82; letter-spacing: -0.03em; margin: 0 0 clamp(var(--space-sm), 1.2vh, var(--space-md));`. Use `--weight-semibold` NOT `--weight-black` (see context note on token drift). The `min(15vw, 20vh)` term self-scales the title down on short viewports so it never forces scroll.
  - `.contact-page__eyebrow`: 12px, `var(--weight-semibold)`, uppercase, `letter-spacing: 0.1em`, `color: var(--color-accent)` (= ink here).
  - `.contact-page__lead`: `max-width: 34ch; font-size: clamp(14px, 1.15vw, 17px); line-height: 1.3; margin: 0;` (A3 keeps the lead compact).
  - `.contact-page__grid`: `display: grid; grid-template-columns: 1fr 1.3fr; gap: clamp(18px, 2.6vw, 40px); align-items: start;`.
  - `.contact-page__channels { border-top: var(--border-focus) solid var(--color-ink); }` (2px). Channel rows, inner row, label, arrow, value, and the pink→ink hover-fill `::before` sweep: translate `.a3-channel*` verbatim into `.contact-page__detail*`, swapping the sketch's raw values for tokens where one exists (`var(--color-border)` for the row hairline, `var(--color-ink)` for the fill, `var(--gray-0)`/`var(--color-dominant)` for hover text). Keep `overflow: hidden` on the row for the sweep.
  - `.contact-page__meta`: mirror `.a3-meta` (small, muted).
  - `.contact-page__form-panel` (requirement 3): `padding: clamp(var(--space-md), 3vh, var(--space-xl)); border: var(--border-hairline) solid var(--color-ink);` and NO `box-shadow` at all (this removes the rejected `10px 10px 0` offset shadow). Style `.contact-page__form-panel h2` from `.a3-form-panel h2` (compact display heading) and `.contact-page__form-sub` from `.a3-form-panel p.sub`.
  - Mobile `@media (max-width: 800px)` (requirement 2/4 — the 800px breakpoint already used in this file): `.contact-page { min-height: auto; display: block; }` (release the no-scroll centering; normal flow + scroll), and `.contact-page__grid { grid-template-columns: 1fr; gap: var(--space-xl); }` (channels stack above the form). No-scroll is DESKTOP-ONLY.

  ContactForm.astro field + submit adaptation (requirement 3 + the "big submit styling" adaptation) — edit ONLY the `<style>` block; the template markup, `<script>`, data-* attributes, field names, honeypot, and submit LABEL text stay untouched:
  - Change the existing `.contact-form :global(.field__input), .contact-form :global(.field__textarea)` override from `background: transparent; border-color: var(--color-ink)` to A3's boxed/tinted treatment: `background: #F5F4F0; border: var(--border-hairline) solid var(--color-border);` (a tinted off-white box with a hairline border; `#F5F4F0` is a deliberate local value — no brand token for a warm tint exists, and A3 README Round 2 chose the tint so fields read as real boxes against the white page). Keep the primitives' existing `border-radius: var(--radius-sm)` and the current min-heights (input 52px, textarea 160px — both already >= the 44px `--tap-target-min`). Add a focus rule `.contact-form :global(.field__input:focus), .contact-form :global(.field__textarea:focus) { background: var(--color-dominant); }` to echo A3's focus (border still comes from the primitives' `outline: 2px solid var(--color-accent)`, which is ink here).
  - Enlarge/bold the submit to echo A3's bold submit while keeping the Button primitive and its localized label. Add `.contact-form__submit-wrap { border-top: var(--border-hairline) solid var(--color-border); padding-top: var(--space-md); }` (mirrors `.a3-submit-row`'s divider) and scope `.contact-form :global(.btn--primary)` to a larger, display-font treatment (e.g. `font-family: var(--font-display); font-size: clamp(16px, 1.6vw, 20px); padding: var(--space-md) var(--space-xl);`). Under the `.contact-page` accent override the primary Button renders ink-on-white text — a bold black submit, matching the monochrome direction. OPTIONAL: add a decorative up-right arrow via `.contact-form :global(.btn--primary)::after { content: "\2197"; }` (a CSS pseudo-element — it survives the script's `textContent` swaps, unlike a markup child span, so it will not disappear during the "Envoi en cours…" state). Do NOT change the button's text label (locked by e2e: /envoyer le message/i, /send message/i).
  - Bump ContactForm's own responsive breakpoint from `@media (max-width: 560px)` to `@media (max-width: 800px)` so the Nom|E-mail side-by-side row stacks to full width across the whole mobile range (requirement 4: stack side-by-side fields to full width at <=800px, one consistent breakpoint with ContactPageBody). Nothing else in that media block changes.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build 2>&1 | tail -5 && grep -c "box-shadow" src/components/ContactPageBody.astro</automated>
  </verify>
  <done>typecheck passes; build succeeds (all pages emitted). `grep -c "box-shadow" src/components/ContactPageBody.astro` returns 0 (offset shadow gone). ContactPageBody renders a giant `.contact-page h1`, a `.contact-page__grid`, a hairline-bordered `.contact-page__form-panel`, and still mounts `<ContactForm locale={locale} publicEmail={publicEmail} />` unchanged. `.contact-page__social` with target=_blank + rel noopener noreferrer is present and precedes the form. ContactForm's field boxes are tinted with a hairline border and the submit is enlarged; its script/markup/labels are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update the locked e2e contracts the redesign intentionally changes, regenerate the visual snapshot, and run the full gate</name>
  <files>tests/e2e/contact.spec.ts, tests/e2e/about.spec.ts, tests/e2e/visual.spec.ts-snapshots/contact-form.png</files>
  <action>
  The A3 redesign intentionally breaks three locked contracts. Update them to reflect the new design (do NOT weaken the surviving behavioral assertions):

  1. `tests/e2e/contact.spec.ts` — the "contact reachability" test currently asserts `page.locator('.contact-page__number')` has text `['02','03']`. A3 removed the numbered editorial-sequence motif. Replace that assertion (in both the `/contact/` and `/en/contact/` loop iterations) with reachability checks against the surviving channel markup: assert an email channel link exists whose href starts with `mailto:` AND the Instagram `.contact-page__social` link exists — i.e. both alternative contact channels are present on each locale. Add a short comment referencing sketch 012 / variant A3 as the reason the 02/03 numbering was dropped. Leave every OTHER test in this file unchanged — especially the "Contact uses neutral black accents" test (still passes because Task 1 keeps `--color-accent: var(--color-ink)`) and the header-nav reachability test.

  2. `tests/e2e/about.spec.ts` — the test titled "About and Contact share the same editorial type scale and page frame" now asserts a claim A3 deliberately makes false (Contact's giant title no longer matches About's `--editorial-page-title-size`, and Contact's page frame padding differs). Decouple Contact from this test: remove the `contactStyles` read and the `expect(aboutStyles).toEqual(contactStyles)` comparison so the test validates About's own editorial scale only (rename the test to reflect About-only scope, e.g. "About uses the shared editorial type scale and page frame"). Add a comment noting sketch 012 / A3 intentionally gives Contact its own big-title composition distinct from About. Do not touch the other About tests.

  3. `tests/e2e/visual.spec.ts` snapshot — the field-box + submit restyle changes the `#contact-form` screenshot (`contact-form.png`). Regenerate it: `npm run build` then `npm run test:e2e -- visual.spec.ts --update-snapshots`. The snapshot filename is platform-portable (playwright.config.ts's `snapshotPathTemplate` omits project/platform and `maxDiffPixelRatio: 0.03` absorbs macOS↔Ubuntu rasterization), so the regenerated `tests/e2e/visual.spec.ts-snapshots/contact-form.png` is CI-safe. Commit the regenerated PNG.

  Do NOT edit `tests/e2e/social-links.spec.ts` — Task 1 preserves `.contact-page__social`, the Instagram text/label/href/target/rel, and the social-precedes-form DOM order, so it must pass unchanged (this is a preserved contract, treat any failure here as a Task 1 markup regression to fix in the component, not the test).

  Then run the full local gate against the built site.
  </action>
  <verify>
    <automated>npm run build && npm run test:e2e && npm run test:unit</automated>
  </verify>
  <done>Full e2e suite passes (chromium + the webkit-mobile smoke subset), including: contact.spec (updated reachability + unchanged neutral-accents/form-submit/validation/honeypot tests), about.spec (About-only scale test), social-links.spec (unchanged, .contact-page__social intact), visual.spec (regenerated contact-form.png within 3% tolerance), and critical.smoke.spec's mocked contact submission under iPhone 15 Pro. Unit tests pass. The regenerated snapshot is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>ContactPageBody redesigned to sketch-012 variant A3: giant display "Contact" title, one-viewport-tall centered desktop composition with symmetric margins and no scroll, hairline-bordered form panel (offset shadow removed), tinted boxed fields, and a bold enlarged submit — with the real ContactForm island still wired unchanged. The automated gate (typecheck, build, unit, e2e incl. regenerated visual snapshot) is green.</what-built>
  <how-to-verify>
  1. Run `npm run dev` and open http://localhost:4321/contact/ in a desktop-sized window (try ~1280x720 and ~1440x900).
     - Expect: a giant "Contact" title; the eyebrow + title + lead + (channels | form) all fit in ONE screen height with NO vertical scrollbar, and roughly EQUAL empty space above and below the whole block (symmetric margins). The gap between the title block and the channels/form row should be bounded/tight, never a large pooled void.
     - Expect: the form panel is outlined by a thin (hairline) border with NO hard drop-shadow behind it; the Nom/E-mail/Message fields look like filled tinted boxes; the submit reads as a bold black button.
     - If a vertical scrollbar appears at 1280x720, the composition is slightly too tall — tighten the compact rhythm (paddings/gaps), not the title, and/or increase `--contact-chrome-reserve` on `.contact-page`. Re-check.
  2. Open http://localhost:4321/en/contact/ and confirm the same, with English copy ("Send a message", etc.).
  3. Resize the window narrow (or use devtools device mode at ~393px) — confirm: the channels stack ABOVE the form (single column), every field and the submit button are full-width and comfortably tall (>=44px, easy to tap), and the page now scrolls normally.
  4. Submit the form with valid Nom/E-mail/Message on /contact/ — confirm the inline "Merci, votre message a bien été envoyé…" status appears and the page does not navigate away (the real ContactForm still works).
  </how-to-verify>
  <resume-signal>Type "approved", or describe what looks off (title size, vertical centering/scroll, panel border, field boxes, mobile stacking, or the form submit) and it will be adjusted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Web3Forms API | The contact form POSTs visitor-supplied name/email/message to Web3Forms. This boundary is OWNED by ContactForm.astro and is NOT modified by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-qek0-01 | Tampering / DoS | contact form spam | low | accept | This is a pure markup/CSS redesign. The existing honeypot short-circuit, per-field validation, and duplicate-submit coalescing in ContactForm.astro's `<script>` are unchanged and remain covered by contact.spec's honeypot/validation/coalesce tests — no new attack surface is introduced. |
| T-qek0-02 | Info disclosure | new-tab external links | low | mitigate | The Instagram `.contact-page__social` link keeps `rel="noopener noreferrer"` + `target="_blank"` (asserted by social-links.spec); reverse-tabnabbing protection is preserved by contract. |

No package-manager installs occur in this plan → no Package Legitimacy Gate required.
</threat_model>

<verification>
- `npm run typecheck` (astro check) passes.
- `npm run build` succeeds and emits all pages.
- `npm run test:unit` passes (contact-form pure-logic unit tests unaffected).
- `npm run test:e2e` passes: contact.spec (updated reachability + unchanged submit/validation/honeypot/neutral-accents), about.spec (About-only scale), social-links.spec (unchanged), visual.spec (regenerated snapshot), critical.smoke.spec (mocked submit under mobile webkit).
- `grep -c "box-shadow" src/components/ContactPageBody.astro` == 0.
- Human visual checkpoint approved for both locales (desktop no-scroll + symmetric margins + big title + hairline panel + boxed fields; mobile stacking + 44px targets + normal scroll).
</verification>

<success_criteria>
- ContactPageBody renders the A3 composition (giant `--font-display` title, flex-column centered, bounded clamp() header gap, hairline-bordered panel, tinted boxed fields) with the offset shadow removed.
- The real `<ContactForm>` island is unchanged (markup/script/props/labels/field names/honeypot) and still submits successfully in both locales.
- The caller prop contract (contact.astro / en/contact.astro → ContactPageBody) is unchanged.
- Desktop /contact/ and /en/contact/ fit one viewport with no scroll and symmetric margins; <=800px stacks the grid, keeps 44px tap targets, and scrolls normally.
- The full CI gate is green, with the three intentionally-changed contracts (about parity, contact reachability, contact-form snapshot) updated to match the new design and all other contracts preserved.
</success_criteria>

<output>
Create `.planning/quick/260728-ek0-impl-menter-le-variant-gagnant-du-sketch/260728-ek0-SUMMARY.md` when done.
</output>
