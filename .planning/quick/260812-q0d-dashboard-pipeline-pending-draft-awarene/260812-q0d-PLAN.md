---
phase: quick-260812-q0d
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/deployment.ts
  - sanity/editorial/pipelineView.ts
  - sanity/editorial/EditorialDashboard.tsx
  - sanity/editorial/EditorialDashboard.css
  - sanity/README.md
  - tests/unit/deployment.test.ts
  - tests/unit/pipeline-view.test.ts
  - tests/unit/editorial-dashboard-markup.test.ts
  - tests/unit/editorial-dashboard-css.test.ts
autonomous: true
requirements:
  - QUICK-260812-q0d
must_haves:
  truths:
    - "ITEM 1 — `resolvePromoteRow()`'s `segments.production === 'done'` branch returns `title: ''` and `detail: ''`, so `promoteDetailBoxHasBody` is false in that state and the whole detail box disappears instead of rendering an empty tinted rectangle. Its `buttonLabel: 'Site en ligne à jour ✓'`, `buttonDisabled: true` and `dimmed: false` are byte-identical to before."
    - "ITEM 2 — `releasePipelineState()` computes the production segment as: `busy` → `'active'`; else a genuinely FAILED production run (`deploymentSegmentKind(production.kind) === 'failed'`) → `'failed'`; else `pendingCount > 0` → `'pending'`; else the existing stale/kind logic. Any unpublished Sanity draft therefore stops the « Site en ligne » node, the gate and the promote row from reading as done/green, exactly as the user asked."
    - "ITEM 2 correctness guard, deliberate and tested — a genuinely FAILED production run reports `'failed'` even while drafts are pending. Failure visibility (the retry button, the failure copy) must never be hidden behind an unrelated pending-draft state — the user asked for 'done' to stop reading as done, not for failures to go quiet. This precedence (failed checked BEFORE pendingCount) is locked in by a dedicated test."
    - "ITEM 3 — when node 1's display segment is `'pending'` AND `publicationCard.total > 0`, its circle carries `editorial-dashboard__pipeline-circle--modified` (blue, built from the existing `--dashboard-publish-accent` token, no animation, no new colour) and its detail line reads « Contenu modifié — prêt à être publié » instead of the generic « Aucune publication effectuée pour le moment »."
    - "ITEM 3 scope — `PipelineSegmentKind` still has exactly four values (`pending`/`active`/`done`/`failed`). The modified treatment is a presentational boolean, never a fifth segment kind."
    - "ITEM 4 — node 1's label reads « Studio ». The literal « Contenu + site de test » appears nowhere in `EditorialDashboard.tsx` (not in JSX, not in a comment) and no test asserts it any more."
    - "ITEM 5 — in the `ready` gate state only, a small muted caption « Aperçu du site de test » renders directly under the round gate button, in the same visual register as the two node detail lines. Every other gate state renders no caption. `releaseGate.ts`'s pure functions are untouched."
    - "Node 2's `pending` fallback detail no longer claims « Aucune publication effectuée pour le moment » when content has in fact been published before — with modified content pending it reads « Modifications pas encore mises en ligne ». This copy fix is required BY item 2: before item 2 that branch was unreachable after a successful production release, and item 2 makes it the common case."
    - "`pipelineCircleClassName()`, `pipelineNodeDetail()` and the new `pipelineGateCaption()` live in a new dependency-free `sanity/editorial/pipelineView.ts` and are covered by REAL behavioural unit tests in `tests/unit/pipeline-view.test.ts` — not only source-text regexes — following the precedent `releaseGate.ts` set for exactly this reason."
    - "`buildProductionReleaseMarkerActions`, `triggerProductionRelease`, `dashboardLogic.ts`, `releaseGate.ts` and `.github/workflows/` are provably untouched by `git diff` against the plan's recorded base SHA."
    - "`npm run test:unit`, `npm run typecheck`, `npm --prefix sanity run lint` and `npm --prefix sanity run build` all pass."
    - "The rebuilt Studio is deployed via `npm run deploy --prefix sanity` and the user has visually confirmed the four changes on the live Studio."
  artifacts:
    - sanity/editorial/deployment.ts
    - sanity/editorial/pipelineView.ts
    - sanity/editorial/EditorialDashboard.tsx
    - sanity/editorial/EditorialDashboard.css
    - tests/unit/deployment.test.ts
    - tests/unit/pipeline-view.test.ts
    - tests/unit/editorial-dashboard-markup.test.ts
    - tests/unit/editorial-dashboard-css.test.ts
    - sanity/README.md
  key_links:
    - "ORDERING TRAP: `publicationCard` is declared around line 517 of `EditorialDashboard.tsx`, but `pipelineDetail` is derived around line 434 — nearly 100 lines EARLIER. `hasModifiedContent = publicationCard.total > 0` therefore cannot be read where `pipelineNodeDetail()` is called today. The `pipelineDetail` derivation must move DOWN to just after `publicationCard`; nothing else moves, and no hook is reordered."
    - "`tests/unit/editorial-dashboard-markup.test.ts` slices the subtitle between the first `Mettre le site à jour` and the next `</Text>` and requires the literal `publicationCard.total > 0` INSIDE that slice. Substituting the new `hasModifiedContent` constant into the subtitle JSX would fail that existing test. The subtitle JSX stays byte-identical; the new constant is used only by the two pipeline helpers."
    - "The same markup test locates the gate button by `source.indexOf('__pipeline-gate editorial')` — i.e. the template literal `` `editorial-dashboard__pipeline-gate editorial-dashboard__pipeline-gate--${gateVariant}` ``. That className expression must keep its exact shape. The new caption class `editorial-dashboard__pipeline-gate-caption` does not collide with it (no space before `editorial`)."
    - "Two other existing markup tests count EXACTLY two `<span className=\"editorial-dashboard__pipeline-node-label\"` and EXACTLY two `<span className=\"editorial-dashboard__pipeline-node-detail\"` elements. The new gate caption must therefore use its own class, never either of those two."
    - "`extractRuleBlocks()` in the CSS test builds the regex `escaped-selector\\s*\\{`, so `.editorial-dashboard__pipeline-connector-track {` does NOT match a query for `.editorial-dashboard__pipeline-connector` (a `-` follows, not `{`). Adding the track rule is safe for the existing `flex: 1 1 auto` assertion. For the same reason the new `--modified` rule must be its OWN block, not grouped with `--active` behind a comma."
    - "`extractRuleBlocks()` captures only what is BETWEEN the braces. The new CSS test asserts the `--modified` block declares no `animation` — so the comment explaining why `--modified` and `--active` share their declarations must sit ABOVE the rule, never inside its braces."
    - "With a pending draft the gate variant is `locked`, so the ITEM 5 caption is NOT on screen — it only appears once drafts are published and the site de test is current (gate `ready`). The human-check must sequence the four confirmations accordingly, or the user will correctly report the caption as missing."
    - "Item 1 empties only the promote ROW copy. The three other « Site en ligne à jour » strings — `deploymentTargetConfig.production.currentLabel`, the promote `buttonLabel: 'Site en ligne à jour ✓'`, and node 2's `done` detail — all stay. `tests/unit/deployment.test.ts:307` asserts the first of them."
    - "Only ONE existing assertion breaks on item 1 (`deployment.test.ts` ~line 641, `expect(result.promote.title).toBe('Site en ligne à jour')`). Item 2 breaks nothing: every existing `releasePipelineState` case that passes `pendingCount > 0` already had a stale/absent production release, so it already expected `'pending'`."
    - "The working tree already carries unrelated uncommitted changes from a concurrent session (`src/client/`, `sanity/package.json`, several tests). Do NOT revert, commit or 'clean up' any of them. Scope proof must be path-scoped against the base SHA, not a demand for a clean tree."
---

<objective>
Five user-confirmed follow-up changes to the Sanity Studio dashboard's release pipeline widget, all tested live by the user this session and dictated in French across several messages. They are bundled here at the user's explicit request:

1. Remove the « Site en ligne à jour » box under the pipeline (promote row copy → empty strings, box self-hides).
2. « Site en ligne » must never read as done while ANY unpublished draft exists — `pendingCount > 0` forces the production segment to `'pending'` unless a release is genuinely in flight.
3. Node 1 gets a distinct blue « contenu modifié, prêt à être publié » look when a fresh draft is waiting — presentational only, no new segment kind.
4. Node 1's label « Contenu + site de test » becomes « Studio ».
5. The round gate button gets a short caption in the same visual format as the panel's two other buttons, naming the site de test it opens.

Purpose: the pipeline currently tells the maintainer the live site is up to date while unpublished edits sit in Sanity, and its first node is named after an implementation detail rather than the thing Romane actually uses. The user has accepted the explicit tradeoff that an unrelated minor edit will now immediately turn the « Site en ligne » indicator non-green even though the currently-live site is technically fine.

Output: two changed branches in `deployment.ts`, a new dependency-free `pipelineView.ts` helper module with real unit tests, the pipeline JSX rewired and relabelled, two new CSS rules plus a connector restructure, a README paragraph resynced, green gates, a redeployed Studio, and a human visual confirmation.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@sanity/editorial/deployment.ts
@sanity/editorial/EditorialDashboard.tsx
@sanity/editorial/EditorialDashboard.css
@sanity/editorial/releaseGate.ts
@tests/unit/deployment.test.ts
@tests/unit/editorial-dashboard-markup.test.ts
@tests/unit/editorial-dashboard-css.test.ts
@tests/unit/release-gate.test.ts
</context>

<scope_boundary>
Base SHA for every scope proof in this plan: **`7def0b606402b9ddf0c0821a8e4d25d30b23ad7c`**.

Do NOT modify:

- `sanity/editorial/dashboardLogic.ts` — including `publicationCardState()`, `buildProductionReleaseMarkerActions()`, `triggerProductionRelease()`, `preparePublicationBatch()`, the publication controller. This plan only READS `publicationCard.total`.
- `sanity/editorial/releaseGate.ts` — `gateClickAction()`, `nextPreviewedFlag()`, `productionPublishDisabled()` and the `PipelineGateVariant` type all stay byte-identical. Importing the type from it is allowed; changing it is not.
- `.github/workflows/` — anything.
- `PipelineSegmentKind` in `deployment.ts` — still exactly `'pending' | 'active' | 'done' | 'failed'`. No fifth value, no widening.
- In `deployment.ts`: `deploymentState()`, `deploymentSubtitle()`, `deploymentSegmentKind()`, `isProductionReleaseStale()`, `pipelineDisplaySegments()`, `deploymentTargetConfig` (its `currentLabel: 'Site en ligne à jour'` in particular), and every branch of `resolvePromoteRow()` other than the single `segments.production === 'done'` branch named in Task 1.
- In `EditorialDashboard.tsx`: the panel header subtitle JSX (the existing markup test asserts its exact contents), `pipelineGateVariant()`, `pipelineNodeIcon()`, `handleGateClick()`, `triggerProductionReleaseClick()`, the `nextPreviewedFlag` effect, `promoteDetailBoxHasBody`, and the « Publier sur le site en ligne » button block.
- No new colour values anywhere. The only accents available are the existing `--dashboard-publish-accent`, `--dashboard-pipeline-done`, `--dashboard-pipeline-failed` custom properties.

Do NOT delete any existing guard test. Where one asserts behaviour this plan changes, re-point it at the new behaviour with a comment saying why it changed. A deleted guard is an untested surface.

Do NOT "clean up" the unrelated uncommitted working-tree changes left by a concurrent session.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: deployment.ts — empty the done-state promote copy, and make any pending draft stop production reading as done</name>
  <files>sanity/editorial/deployment.ts, tests/unit/deployment.test.ts</files>
  <action>
Two edits in `sanity/editorial/deployment.ts`, then the test updates.

**Edit A (ITEM 1) — `resolvePromoteRow()`, the `if (segments.production === 'done')` branch.**
Set `title` to the empty string and `detail` to the empty string, matching the exact pattern the `if (segments.content !== 'done')` branch further down already uses for its no-copy state. Leave `buttonLabel: 'Site en ligne à jour ✓'`, `buttonDisabled: true` and `dimmed: false` exactly as they are. Do not add a `notStarted` flag here — that flag belongs to the other branch alone.

The box disappears rather than rendering empty because `EditorialDashboard.tsx` already guards it with `promoteDetailBoxHasBody` (title || detail || actionUrl). Do not touch that guard.

Add a short comment above the returned object recording that emptying the copy is the intended contract, not an oversight: node 2's own detail line already says « Site en ligne à jour » directly above this box, and the gate button already shows a checkmark, so a third copy of that status was pure noise. Say plainly that the copy must not be restored.

**Edit B (ITEM 2) — `releasePipelineState()`, the `productionSegment` computation.**
Today it is `busy ? 'active' : stale ? 'pending' : deploymentSegmentKind(production.kind)`. Insert TWO checks between the `busy` check and the `stale` check: first a genuinely failed production run, THEN `pendingCount > 0`. Final precedence, exactly:

  1. `busy` → `'active'` (something genuinely in flight always wins)
  2. `deploymentSegmentKind(production.kind) === 'failed'` → `'failed'` (a real failure must never go quiet)
  3. `pendingCount > 0` → `'pending'`
  4. `stale` → `'pending'`
  5. otherwise `deploymentSegmentKind(production.kind)`

Compute `deploymentSegmentKind(production.kind)` once into a local (e.g. `const productionKind = deploymentSegmentKind(production.kind)`) and reuse it for both the failed check and the final fallback, rather than calling the function twice.

Leave `content`, `stagingSegment`, `stale` and the `resolvePromoteRow()` call untouched.

Add a comment above the computation carrying three points, in the maintainer's own framing:
  - Clicking « Mettre le site à jour » publishes Sanity drafts and rebuilds the site de test only. While any draft is unpublished, neither the site de test nor the site en ligne includes the newest saved edits, so neither may be painted as up to date.
  - The accepted tradeoff, confirmed explicitly by the maintainer: an unrelated minor edit anywhere in Sanity immediately turns the « Site en ligne » indicator non-green even though the currently-live production site is technically fine. This is deliberate, not a bug — simplicity of understanding was chosen over technical precision.
  - Failure visibility is carved out on purpose: a genuinely FAILED production run reports `'failed'` even while drafts are pending, checked BEFORE the pendingCount rule. The pending-draft rule exists to stop a stale "done" from reading as current — it must never also swallow a real failure and its retry button.

**Test updates in `tests/unit/deployment.test.ts`** (all inside `describe('release pipeline state')`):

1. Re-point the existing case `'a proven-current production run newer than the newest publication marks production done and locks the button'`. Keep `expect(result.segments.production).toBe('done')` and `expect(result.promote.buttonDisabled).toBe(true)`. Replace the `expect(result.promote.title).toBe('Site en ligne à jour')` assertion with `toBe('')`, add the matching `expect(result.promote.detail).toBe('')`, and add `expect(result.promote.buttonLabel).toBe('Site en ligne à jour ✓')` proving only the copy was emptied and the locked gate's accessible name survived. Comment the empty assertions: this state renders no detail box at all because node 2 and the gate checkmark already carry the status.

2. New case: a proven-current, NON-stale production release plus `pendingCount: 1` reports `segments.production === 'pending'`. Build it by copying the fixture from the case in (1) — same `productionReleaseAt = '2026-07-29T09:05:00Z'`, same production run — and passing `pendingCount: 1`. Assert `production.kind` is still `'current'` and `isProductionReleaseStale` is not the cause (the release is newer than `publishedAt`), so the ONLY thing forcing `'pending'` is the unpublished draft. Name the case after the user's requirement: an unpublished draft anywhere stops the live site reading as up to date.

3. New case: `busy: true` with `pendingCount: 1` still reports `segments.production === 'active'`. Comment it: something genuinely in flight always outranks the pending-draft rule, otherwise a running release would visibly regress to a waiting state the moment anyone saved a draft.

4. New case: a FAILED production run plus `pendingCount: 1` still reports `'failed'`, not `'pending'`. Reuse the fixture from the existing `'a failed production run enables the button for retry…'` case with `pendingCount: 1` added. Assert `segments.production` is `'failed'` and `promote.buttonLabel` is still `'Réessayer'` (the retry affordance survives). Comment it: a real failure must stay visible and actionable regardless of unrelated pending drafts — the pending-draft rule governs the done↔pending boundary only, never the failed state.

Leave every other case in the file untouched — in particular `deployment state disambiguated by target` (line ~307), which asserts `deploymentTargetConfig.production.currentLabel` is still « Site en ligne à jour ».
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/deployment.test.ts</automated>
    <automated>npm run test:unit -- tests/unit/release-gate.test.ts</automated>
    <automated>git diff -U0 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/editorial/deployment.ts | grep '^@@'</automated>
  </verify>
  <done>The done-state promote row returns empty title and detail while keeping its button label, disabled flag and dimmed flag. The production segment resolves busy → active, then failed → failed, then pending-draft → pending, then the previous stale/kind logic. Both test files pass, including the four new/updated release-pipeline cases. The `git diff -U0` hunk headers fall only inside `resolvePromoteRow()`'s done branch and `releasePipelineState()`'s segment computation — no hunk anywhere else in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the pipeline's presentational helpers into a testable pipelineView.ts, with the modified-content state and gate caption</name>
  <files>sanity/editorial/pipelineView.ts, tests/unit/pipeline-view.test.ts</files>
  <action>
Create `sanity/editorial/pipelineView.ts` — a NEW module with no runtime imports at all (type-only imports only), so the root Vitest project can import and exercise it directly. This follows the precedent and rationale already written at the top of `sanity/editorial/releaseGate.ts`: rules that can only be guarded by source-text regexes while they live inside the component get pulled out so they can have real behavioural tests.

Open the file with a short header comment saying exactly that, and stating that nothing here may import React, `@sanity/ui` or `sanity` — those would make it unimportable from the root Vitest project.

Type-only imports: `PipelineSegmentKind` and `ReleasePipelineDisplaySegments` from `./deployment`; `PipelineGateVariant` from `./releaseGate`.

Export three pure functions:

1. `pipelineCircleClassName(kind: PipelineSegmentKind, hasModifiedContent = false): string`
   Move the body verbatim from `EditorialDashboard.tsx`, then extend the `'pending'` case only: when `hasModifiedContent` is true it returns the base class plus `editorial-dashboard__pipeline-circle--modified`; when false it returns the bare base class as today. `'done'`, `'active'` and `'failed'` keep returning `` `…-circle …-circle--${kind}` `` and must ignore the flag entirely.
   Comment it: this is a presentational modifier, deliberately NOT a fifth `PipelineSegmentKind` — the state machine in `deployment.ts` stays four-valued, and only node 1 ever passes `true`.

2. `pipelineNodeDetail(display: ReleasePipelineDisplaySegments, hasModifiedContent = false): {node1: string; node2: string}`
   Move the body verbatim from `EditorialDashboard.tsx`, then make exactly two changes, both in the final fallback of each chain:
   - node 1's `'pending'` fallback (currently « Aucune publication effectuée pour le moment ») becomes, when `hasModifiedContent` is true: `'Contenu modifié — prêt à être publié'` (em dash U+2014, curly-free). When false it keeps the existing string. The `active` / `done` / `failed` node-1 strings are untouched.
   - node 2's innermost fallback (also « Aucune publication effectuée pour le moment », reached when `liveSite` is pending and `testSite` is pending) becomes, when `hasModifiedContent` is true: `'Modifications pas encore mises en ligne'`. When false it keeps the existing string. Every other node-2 branch — including `done` → « Site en ligne à jour » — is untouched.
   Comment the node-2 change with WHY it is required by Task 1's item 2 and is not scope creep: before that change, a successful production release stayed `'done'` when a new draft appeared, so this fallback was unreachable after any real publication. Item 2 makes it the common case, and « Aucune publication effectuée pour le moment » would then be an actively false statement under the « Site en ligne » label.

3. `pipelineGateCaption(variant: PipelineGateVariant): string`
   Returns `'Aperçu du site de test'` for `'ready'` and `''` for every other variant.
   Comment it: in the `ready` variant the round gate button's click opens `SITE_PREVIEW_URL`, so the caption names what the click does, matching the small explanatory line the panel's two other buttons already carry. The other variants get no caption because there is nothing to preview yet (`locked`), nothing to do (`active`, `done`) or a different action entirely (`failed`) — and an empty string is what lets the component skip the element rather than render a blank line.

Then create `tests/unit/pipeline-view.test.ts` importing all three from `../../sanity/editorial/pipelineView`, with real behavioural cases (mirror the file-header style of `tests/unit/release-gate.test.ts`):

- `pipelineCircleClassName`: `('pending', true)` contains `--modified`; `('pending', false)` and `('pending')` return the bare base class with no `--` modifier at all; each of `'done' | 'active' | 'failed'` returns its own modifier and NEVER `--modified`, with the flag both true and false (an `it.each` over the three kinds). Add an explicit assertion that `('pending', true)` does not contain `--active`, since the two share their CSS declarations and only the icon distinguishes them.
- `pipelineNodeDetail`: with `{testSite: 'pending', liveSite: 'pending'}` and the flag true, `node1` is exactly `'Contenu modifié — prêt à être publié'` and `node2` is exactly `'Modifications pas encore mises en ligne'`; with the flag false, both are exactly `'Aucune publication effectuée pour le moment'`. Also assert the flag changes NOTHING for `{testSite: 'done', liveSite: 'done'}` (« Site de test à jour » / « Site en ligne à jour »), for an `active` testSite, and for a `failed` testSite — the modified state is only ever the pending fallback.
- `pipelineGateCaption`: `'ready'` returns a non-empty string containing `'site de test'`; each of `'locked' | 'active' | 'done' | 'failed'` returns `''` (an `it.each`). Comment the empty cases: the component renders the caption element only when this returns a truthy string, so `''` is the contract that keeps the connector column from growing a blank line.

Do not import from or modify `releaseGate.ts` beyond the type-only import.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/pipeline-view.test.ts</automated>
    <automated>git diff --name-only 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/editorial/releaseGate.ts sanity/editorial/dashboardLogic.ts</automated>
  </verify>
  <done>`sanity/editorial/pipelineView.ts` exists with three exported pure functions and no runtime imports. `tests/unit/pipeline-view.test.ts` passes and covers the modified-content branches of both helpers plus all five gate-caption variants. The `git diff --name-only` command prints nothing, proving `releaseGate.ts` and `dashboardLogic.ts` were not touched.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire EditorialDashboard.tsx — Studio label, modified-content node 1, gate caption</name>
  <files>sanity/editorial/EditorialDashboard.tsx, tests/unit/editorial-dashboard-markup.test.ts</files>
  <action>
All edits in `sanity/editorial/EditorialDashboard.tsx`:

**A. Import the extracted helpers.** Add `import {pipelineCircleClassName, pipelineGateCaption, pipelineNodeDetail} from './pipelineView'` next to the existing `./releaseGate` import, and DELETE the local `function pipelineCircleClassName(...)` and `function pipelineNodeDetail(...)` declarations near the bottom of the file. Keep `pipelineNodeIcon()` and `pipelineGateVariant()` local — they are unchanged and out of scope. Keep the `PipelineSegmentKind` and `ReleasePipelineDisplaySegments` type imports; both are still used by the two helpers that stay.

**B. Derive the modified-content boolean and relocate one derivation.** Immediately after `const publicationCard = publicationCardState(...)` (around line 517) add:

    const hasModifiedContent = publicationCard.total > 0

then MOVE the existing `const pipelineDetail = pipelineNodeDetail(displaySegments)` line down to sit directly beneath it, passing the new boolean as a second argument. It cannot stay at its current position (~line 434) because `publicationCard` does not exist yet there. Move nothing else: `displaySegments`, `gateVariant`, `gateAction`, `gateLabel`, the `nextPreviewedFlag` effect and `promoteDetailBoxHasBody` all stay exactly where they are, and no hook call is reordered — only `const` declarations move.

Comment the new constant: it reads the same publication-inventory signal that drives the panel's « X contenu(s) modifié(s) » subtitle, so the node and the sentence above it can never disagree about whether there is something waiting to publish.

**C. Do NOT touch the panel header subtitle JSX.** It must keep the literal `publicationCard.total > 0` inline — an existing markup test slices that subtitle and asserts that exact text. Do not substitute `hasModifiedContent` there.

**D. Node 1 (ITEM 3 + ITEM 4).** In the first `.editorial-dashboard__pipeline-node` div:
   - circle className becomes `pipelineCircleClassName(displaySegments.testSite, hasModifiedContent)`;
   - the label span's text becomes exactly `Studio` (replacing the two-word-plus-site label it carries today). Keep the surrounding `<span className="editorial-dashboard__pipeline-node-label">` element and its neighbouring explanatory comment about plain spans vs Sanity UI `<Text>` — only the text node changes;
   - the detail span keeps rendering `{pipelineDetail.node1}` and needs no JSX change.
   Put the rename rationale in the COMMIT MESSAGE, not in a code comment: the old label named an implementation pairing rather than the tool Romane opens, and the detail line under the node still names the site de test, so nothing is lost. Do not leave the old label text anywhere in this file, including in a comment — a markup test asserts it is gone.
<!-- planner-discipline-allow: Contenu + site de test -->

**E. Node 2.** Leave its circle call as `pipelineCircleClassName(displaySegments.liveSite)` — one argument, no modified treatment (item 3 is node 1 only). Its label and detail JSX are unchanged.

**F. Gate caption (ITEM 5).** Next to `gateLabel`, add `const gateCaption = pipelineGateCaption(gateVariant)`.
   Restructure the connector so the caption can sit beneath the button without disturbing the existing row:
   - keep the outer `<div className="editorial-dashboard__pipeline-connector">`;
   - wrap its three current children (link span, gate `<button>`, link span) — verbatim, no changes to any of them — in a new `<div className="editorial-dashboard__pipeline-connector-track">`;
   - after that track div, render `{gateCaption && (<span className="editorial-dashboard__pipeline-gate-caption">{gateCaption}</span>)}`.
   The gate `<button>`'s className template literal must keep its exact current shape (`` `editorial-dashboard__pipeline-gate editorial-dashboard__pipeline-gate--${gateVariant}` ``); an existing test locates the button by the substring `__pipeline-gate editorial`. Do not add `aria-hidden` to the caption, and do not change the button's `title`/`aria-label` (`gateLabel`) — the caption is additive, visible text.

**Test updates in `tests/unit/editorial-dashboard-markup.test.ts`.** Leave every existing case intact (the two span-count cases, the subtitle case, the detail-box cases, the release-gate wiring cases) and add ONE new describe block for this task's wiring, with these cases:

  1. Node 1's label span renders exactly `Studio`, and the old label literal appears nowhere in the component source. Assert both directions: a regex matching `<span className="editorial-dashboard__pipeline-node-label">` followed by whitespace and `Studio`, and `expect(source.includes('<old label>')).toBe(false)` with a message explaining the rename came from the maintainer finding the old name unclear.
  2. The component derives `hasModifiedContent` from `publicationCard.total > 0` exactly once (`const hasModifiedContent = publicationCard.total > 0`).
  3. Node 1's circle passes that boolean: the source contains `pipelineCircleClassName(displaySegments.testSite, hasModifiedContent)`, and node 2's call does NOT pass it (`pipelineCircleClassName(displaySegments.liveSite)`), with a message saying the modified treatment is node 1 only.
  4. `pipelineNodeDetail(displaySegments, hasModifiedContent)` is called with the boolean.
  5. Both helpers come from the extracted module — the source imports from `'./pipelineView'` and no longer declares `function pipelineCircleClassName` or `function pipelineNodeDetail` locally, with a message explaining that re-inlining them removes their behavioural test coverage.
  6. The gate caption is derived from the shared helper (`pipelineGateCaption(gateVariant)`) and rendered in its own span, gated on the helper's truthiness — assert the source contains `editorial-dashboard__pipeline-gate-caption` and `{gateCaption &&`, and that the caption literal itself does NOT appear inline in the JSX (it belongs to the tested helper, so a state that should render no caption cannot be hard-coded back in).
  7. The connector still wraps the two links and the gate in `editorial-dashboard__pipeline-connector-track`, and the gate button className template is intact — assert both substrings are present.

Do not weaken or delete the existing case asserting `Publier sur le site en ligne` appears exactly once.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-markup.test.ts</automated>
    <automated>npm --prefix sanity run lint</automated>
  </verify>
  <done>Node 1 reads « Studio », turns blue with « Contenu modifié — prêt à être publié » when `publicationCard.total > 0` and its segment is pending, and the round gate carries a caption span in the `ready` state only. The two helpers now come from `./pipelineView`. Every existing markup test still passes alongside the seven new wiring cases, and `sanity` lint is clean (no unused imports left by the extraction).</done>
</task>

<task type="auto">
  <name>Task 4: EditorialDashboard.css — modified circle face and gate caption, without new colours</name>
  <files>sanity/editorial/EditorialDashboard.css, tests/unit/editorial-dashboard-css.test.ts</files>
  <action>
Three additions and one restructure in `sanity/editorial/EditorialDashboard.css`. Reuse existing custom properties only — no new colour literals anywhere.

**A. `.editorial-dashboard__pipeline-circle--modified`.** Place it directly after `.editorial-dashboard__pipeline-circle--active`. Its three declarations are the same construction as `--active`: `background-color: color-mix(in srgb, var(--dashboard-publish-accent) 14%, transparent)`, `border-color: var(--dashboard-publish-accent)`, `color: var(--dashboard-publish-accent)`. It declares NO animation and NO other property.

Put an explanatory comment ABOVE the rule — never inside its braces, because the new test extracts what is between the braces and asserts no animation is declared there. The comment must say: this face intentionally shares its declarations with the in-progress face, because the difference between « something is running » and « something is waiting for you » is carried by the ICON (a spinning spinner vs a static publish glyph), not by hue; and that this must stay a separate rule block rather than being merged into a comma-grouped selector, because the CSS test's `extractRuleBlocks()` matches a selector only when `{` follows it directly.

**B. `.editorial-dashboard__pipeline-connector` restructure.** Keep `flex: 1 1 auto` and `min-width: 0` (an existing test asserts the flex value). Add `flex-direction: column` and `align-items: center`, and REMOVE `height: 36px` from this rule — the height moves to the new track rule below, so the connector can grow by the caption's height without stretching the links.

**C. `.editorial-dashboard__pipeline-connector-track`**, new rule immediately after it: `display: flex`, `align-items: center`, `width: 100%`, `height: 36px`. This is the row that used to be the connector itself, so the two link spans and the round gate keep their exact current geometry.

**D. `.editorial-dashboard__pipeline-gate-caption`**, new rule after the gate's `:focus-visible` rule: `margin-top: 8px`, `max-width: 132px`, `font-size: 10px`, `line-height: 13px`, `text-align: center`, `text-wrap: balance`, `color: var(--card-muted-fg-color)`. The font-size/line-height/colour deliberately match `.editorial-dashboard__pipeline-node-detail` so the caption reads as part of the same row of small explanatory text; the `margin-top` matches the node label's so it aligns with the two labels either side. The `max-width` keeps a long caption from stretching the connector at narrow viewports.

Do not touch the `@media (max-width: 36em)` block (the gate's `margin: 0 4px` and the link `min-width: 6px` still apply through the track), the `prefers-reduced-motion` block, the node widths, or any other rule.

**Test updates in `tests/unit/editorial-dashboard-css.test.ts`** — add cases to the existing describe block, and fix one stale message:

  1. `.editorial-dashboard__pipeline-circle--modified` is declared exactly once; its block references `var(--dashboard-publish-accent)`; its block declares no `animation` (message: this is the static « waiting for your action » face — a pulse or spin here would make it indistinguishable from the in-progress face, whose only differentiator is the icon); and its block contains no `#` colour literal (message: the pipeline's accents are tokens, and a hard-coded hue here would drift from the panel's left border).
  2. `.editorial-dashboard__pipeline-connector-track` is declared exactly once and declares `height: 36px` and `display: flex` (message: the caption moved the height off the connector, and losing it here collapses the link/gate row).
  3. `.editorial-dashboard__pipeline-connector` still declares `flex: 1 1 auto` (existing case — leave it) and now also declares `flex-direction: column` (new assertion, message: the connector became a two-row column so the caption can sit under the gate without stretching the connecting lines).
  4. `.editorial-dashboard__pipeline-gate-caption` is declared exactly once and declares a `font-size` and a `color`, with a message noting the caption is a plain span with no Sanity UI wrapper, so this rule is the only thing giving it type scale and colour — the same reason the node label rule carries its own colour.
  5. Fix the stale assertion MESSAGE in the existing node-column-width case, which currently justifies the 140px floor by naming node 1's old label. Keep the `toBeGreaterThanOrEqual(140)` threshold and the narrow-override comparison unchanged; only rewrite the message so it justifies the width by what the column must now hold — a two-line detail such as « Contenu modifié — prêt à être publié » sitting under a short label, on both nodes, without colliding.

Do not touch the existing `removedFragments` case; none of the new class names contain any of its fragments.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-css.test.ts</automated>
    <automated>git diff -U0 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/editorial/EditorialDashboard.css | grep '^@@'</automated>
  </verify>
  <done>The stylesheet gains a blue static `--modified` circle face, a connector track row and a gate caption rule, all built from existing tokens. Every existing CSS guard passes alongside the four new cases, and the diff hunks touch only the pipeline circle, connector and gate regions.</done>
</task>

<task type="auto">
  <name>Task 5: Resync the maintainer-facing README paragraph</name>
  <files>sanity/README.md</files>
  <action>
`sanity/README.md` documents the dashboard for Romane in French. The paragraph beginning « Sous le bouton **Mettre le site à jour**, une barre de progression montre deux étapes » (around line 80) still names node 1 by its old label and describes a state machine that this plan changes. Update that paragraph only:

  - name the two stages « Studio » and « Site en ligne »;
  - describe the new blue state on the Studio stage: when modifications are saved but not yet published, that stage turns blue and reads « Contenu modifié — prêt à être publié »;
  - state the new rule plainly: as long as unpublished modifications exist, the « Site en ligne » stage is never shown as up to date, even if the live site itself has not changed — this is intentional, so the indicator always describes the latest saved content;
  - keep the existing description of the central button's lifecycle (locked → clickable → quiet during publication → checkmark) and add that when it is clickable it carries a small caption naming the site de test it opens;
  - keep the closing sentence about a failure message naming which stage failed.

Leave the rest of the file — the status-name list above it (« Site de test à jour », « Site en ligne à jour », etc., which are `deploymentState()` labels this plan does not change), and everything from « ## Collections photo » onward — untouched.
  </action>
  <verify>
    <automated>git diff --stat 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/README.md</automated>
    <automated>grep -c 'Studio' sanity/README.md</automated>
  </verify>
  <done>The README's pipeline paragraph names « Studio » / « Site en ligne », documents the blue modified state, the never-green-while-drafts-pending rule and the gate caption, with the surrounding status list and collection instructions untouched. `git diff --stat` shows a single changed file with a paragraph-sized hunk.</done>
</task>

<task type="auto">
  <name>Task 6: Run every gate, prove scope, redeploy the Studio, confirm visually</name>
  <files>(no source changes — verification, deploy and sign-off only)</files>
  <action>
Run the four blocking gates in this order, fixing anything they surface before moving on: `npm run test:unit` (full suite, not just the four touched files), `npm run typecheck`, `npm --prefix sanity run lint`, `npm --prefix sanity run build`.

Note: the working tree already carried unrelated uncommitted changes from a concurrent session before this task started (`src/client/`, `sanity/package.json`, `tests/unit/edition-query.test.ts` and others). Do not revert, commit or "fix" them. If a pre-existing failure in the full suite is unrelated to this plan's files, record it in the SUMMARY rather than chasing it.

Then prove the scope boundary held, against the base SHA `7def0b606402b9ddf0c0821a8e4d25d30b23ad7c`:

  - `git diff --name-only 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts .github/workflows/` must print nothing.
  - `git diff 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/editorial/deployment.ts | grep -c 'PipelineSegmentKind ='` must be 0 — the segment-kind union was not widened.
  - `git diff --stat 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/ tests/` must list only this plan's nine files (plus the concurrent session's pre-existing files, which were already dirty at the base SHA — call them out explicitly in the SUMMARY so the distinction is on record).

Then redeploy the live Studio with `npm run deploy --prefix sanity`. This is the established, pre-authorised pattern for shipping a Studio change this session — do not stop and ask before running it. Wait for it to report success before presenting the human-check.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm --prefix sanity run build</automated>
    <automated>git diff --name-only 7def0b606402b9ddf0c0821a8e4d25d30b23ad7c -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts .github/workflows/</automated>
    <human-check>
Le Studio a été redéployé. Ouvre https://atelier-jacqueline-suzanne.sanity.studio/ et fais un **rechargement forcé** (Cmd+Shift+R) pour charger le nouveau bundle — cet environnement n'a pas de session navigateur authentifiée, donc toi seul peux confirmer le rendu.

**Étape 1 — avec au moins un brouillon non publié** (modifie n'importe quel contenu et laisse-le en brouillon), regarde le panneau « Mettre le site à jour » :

(a) Le texte « Site en ligne à jour » / « Le site en ligne reflète la dernière mise à jour. » sous le pipeline a disparu — plus de boîte grise vide à cet endroit.

(b) L'étape « Site en ligne » n'est plus verte / plus cochée, alors même que le site en ligne n'a pas changé. C'est le compromis que tu as accepté explicitement : dès qu'un brouillon existe, l'indicateur cesse d'annoncer que le site est à jour.

(c) La première étape s'appelle maintenant « Studio » (et non plus l'ancien nom), son rond est bleu (sans animation, sans clignotement) et la petite ligne dessous lit « Contenu modifié — prêt à être publié ».

À cette étape le bouton rond central est **verrouillé (cadenas) et n'a volontairement aucune légende** — il n'y a encore rien à prévisualiser. C'est normal, ce n'est pas le point (d).

**Étape 2 — pour vérifier le point (d)** : clique « Mettre le site à jour », attends que l'étape « Studio » redevienne verte (reconstruction du site de test terminée). Le bouton rond central devient alors bleu et cliquable :

(d) Une petite légende « Aperçu du site de test » apparaît juste sous ce bouton rond, dans le même registre visuel que les petites phrases explicatives des deux autres boutons du panneau.

**Point (d) — attention** : il a été implémenté à partir d'une description dictée vocalement et partiellement transcrite, sans capture d'écran de référence. Un écart de format (taille, position, formulation) est attendu et normal ; dis simplement ce qui ne va pas et on l'ajuste en un tour. Ce n'est pas un échec du reste.

Réponds « approuvé » si tout est bon, ou décris ce qui cloche (en précisant lequel des points a/b/c/d).
    </human-check>
  </verify>
  <done>All four gates pass, the three scope-proof commands come back clean, the Studio is redeployed, and the user's confirmation — or their correction list, most likely on point (d) — is recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npm run test:unit` — full suite green, including the four updated/added test files.
- `npm run typecheck` — 0 errors (pre-existing unrelated hints may remain).
- `npm --prefix sanity run lint` — clean (proves the helper extraction left no unused imports in the component).
- `npm --prefix sanity run build` — Studio bundles with the new module.
- `git diff --name-only <base> -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts .github/workflows/` — empty.
- `git diff <base> -- sanity/editorial/deployment.ts | grep -c 'PipelineSegmentKind ='` — 0.
- `npm run deploy --prefix sanity` — reports success.
</verification>

<success_criteria>
- ITEM 1: the done-state promote row carries no copy, and the detail box self-hides rather than painting an empty rectangle.
- ITEM 2: any unpublished draft forces the production segment to `'pending'`; an in-flight release still wins with `'active'`; a genuinely failed release still wins with `'failed'` (retry affordance stays visible) — both carve-outs documented in a comment and locked by tests.
- ITEM 3: node 1 shows a static blue circle and « Contenu modifié — prêt à être publié » when a draft is waiting, driven by a presentational boolean, with `PipelineSegmentKind` still four-valued.
- ITEM 4: node 1 is labelled « Studio » and the old label survives nowhere in source or tests.
- ITEM 5: the round gate carries « Aperçu du site de test » in its `ready` state only, from a tested pure helper, with `releaseGate.ts` untouched.
- The three presentational helpers have real behavioural unit tests instead of source-text regexes alone.
- Every gate green, scope git-proven, Studio redeployed, user signed off visually.
</success_criteria>

<output>
Create `.planning/quick/260812-q0d-dashboard-pipeline-pending-draft-awarene/260812-q0d-SUMMARY.md` when done.

End the SUMMARY with the human-verification note from Task 6 verbatim: ask the user to hard-refresh and confirm (a) the « Site en ligne à jour » box is gone, (b) « Site en ligne » no longer reads as done while a draft is pending, (c) node 1 shows the « Studio » label with the blue modified treatment and its new text, and (d) the round gate button shows the « Aperçu du site de test » caption once the gate is in its ready state — flagging that (d) was built from a garbled voice-dictated description and may need one more adjustment round, which is expected rather than a failure.

Also record in the SUMMARY:
- The correctness guard added to Task 1's precedence: a genuinely failed production run still reports `'failed'` (retry button and failure copy stay visible) even while drafts are pending — the pending-draft rule only ever downgrades a `'done'`/stale read, never a real failure. This was corrected during planning review, before execution, from an earlier draft of this plan that would have masked failures behind pending drafts.
- The reachability note the plan was asked to settle: `pendingCount > 0` is NOT the only way node 1's segment reaches `'pending'` — `deploymentState()` also returns the `unknown` kind (which maps to `'pending'`) when the GitHub API read fails or no publication reference exists. So the new `--modified` face distinguishes the common draft case from that rarer degraded-read case, rather than being an unreachable sub-branch.
</output>
