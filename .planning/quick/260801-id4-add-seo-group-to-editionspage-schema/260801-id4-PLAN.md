---
phase: quick-260801-id4
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [260801-id4]
files_modified:
  - sanity/schemas/editionsPage.ts
  - src/lib/sanity.ts
  - sanity/editorial/checks.ts
  - tests/unit/editorial-checks.test.ts
must_haves:
  truths:
    - "La fiche « Page Éditions » du Studio affiche un onglet SEO à côté de l'onglet Contenu, avec les mêmes champs partagés (titre, description, image de partage, masquer dans Google) que la page d'accueil."
    - "La checklist « Préparation du contenu » de editionsPage liste de nouveau les trois éléments SEO, et chacun est désormais atteignable et satisfaisable dans l'onglet SEO."
    - "Les trois éléments SEO restent recommandés (jamais bloquants) : une introduction bilingue seule suffit toujours pour que requiredComplete soit vrai."
    - "Le texte d'introduction par défaut reste strictement identique à DEFAULT_EDITIONS_INTRO dans src/lib/site-config.ts."
    - "Le site public est inchangé : /editions et /en/editions continuent d'utiliser leurs chaînes SEO codées en dur, aucune route Astro n'est modifiée."
  artifacts:
    - path: "sanity/schemas/editionsPage.ts"
      provides: "Groupe SEO + champ seo (type partagé 'seo') sur le singleton Page Éditions"
    - path: "sanity/editorial/checks.ts"
      provides: "Branche editionsPage de getDocumentChecks incluant de nouveau ...seoChecks(value.seo)"
    - path: "tests/unit/editorial-checks.test.ts"
      provides: "Test de régression inversé : les éléments SEO SONT listés et restent non bloquants"
  key_links:
    - "editionsPage.ts champ `seo` (type: 'seo') → type objet partagé défini dans sanity/schemas/seo.ts"
    - "checks.ts branche editionsPage lisant `value.seo` → nom de champ `seo` dans editionsPage.ts (même chemin que homePage/aboutPage/contactPage)"
    - "seoChecks() marque ses items `recommended: true` → summarizeChecks().requiredComplete inchangé → la porte de publication du tableau de bord reste non bloquée"
---

<objective>
Rendre le SEO de la page Éditions éditable dans le Studio et restaurer sa couverture dans la checklist éditoriale.

Purpose: L'utilisateur a testé en live le singleton « Page Éditions » et signalé « il manque la page SEO » — il attend le même onglet SEO que sur les autres pages publiques. Une session de debug antérieure aujourd'hui (`.planning/debug/resolved/editionspage-no-title-seo.md`) avait résolu l'incohérence dans l'autre sens (retirer les items de checklist inatteignables) parce que le champ n'existait pas ; la décision de scope a maintenant changé : le champ est ajouté, donc la checklist doit le refléter.
Output: Champ + groupe SEO sur `editionsPage`, checklist restaurée, test de régression inversé, commentaires devenus faux corrigés.

Branche : travailler et committer directement sur `codex/studio-publication-workflow` (PR #12 déjà ouverte). NE PAS créer de nouvelle branche.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/resolved/editionspage-no-title-seo.md

# Motif de référence à copier littéralement (lire en entier avant d'éditer) :
@sanity/schemas/homePage.ts
@sanity/schemas/seo.ts

# Fichiers modifiés :
@sanity/schemas/editionsPage.ts
@sanity/editorial/checks.ts
@tests/unit/editorial-checks.test.ts
</context>

<scope_boundaries>
HORS SCOPE — ne pas faire, même si ça semble « compléter » le travail :

1. **Aucune route Astro.** `src/pages/editions/index.astro` et `src/pages/en/editions/index.astro` gardent leurs `seoTitle`/`seoDescription` codés en dur. Brancher le `<title>`/les meta du site réel sur ce nouveau champ Sanity est une décision de scope distincte, non demandée ici.
2. **Aucun changement de requête ni de type de données.** `EDITIONS_PAGE_QUERY` dans `src/lib/sanity.ts` reste `{ intro }` et l'interface `EditionsPage` reste sans propriété `seo`. La seule modification autorisée dans ce fichier est la correction d'un commentaire (voir Task 1) — zéro changement de comportement.
3. **Aucune écriture dans Sanity.** Ne créer, ne sauvegarder, ne publier aucun document live. Le singleton `editionsPage` n'a jamais été sauvegardé côté serveur (vérifié en lecture seule) — c'est à l'humain de l'éditer et de le sauvegarder lui-même dans le Studio.
4. **Ne pas toucher** `defaultIntro`, `initialValue`, ni le champ `intro` de `editionsPage.ts`.
</scope_boundaries>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter le groupe et le champ SEO au schéma editionsPage</name>
  <files>sanity/schemas/editionsPage.ts, src/lib/sanity.ts</files>
  <action>
Dans `sanity/schemas/editionsPage.ts`, appliquer exactement le motif déjà en place dans `homePage.ts` (le lire en entier d'abord) :

1. `groups` : passer de la liste à un seul élément à deux éléments — garder `{name: 'content', title: 'Contenu', default: true}` tel quel, puis ajouter `{name: 'seo', title: 'SEO'}`. Reproduire le formatage multi-lignes de `homePage.ts`.

2. Ajouter un second `defineField` APRÈS le champ `intro` existant (dernier élément du tableau `fields`), avec : `name: 'seo'`, `title` valant `SEO de la page Éditions`, `type: 'seo'` (le type objet partagé de `sanity/schemas/seo.ts` — ne rien redéfinir en local), `group: 'seo'`, et la chaîne `description`. IMPORTANT : copier la chaîne `description` caractère par caractère depuis `homePage.ts` (elle contient de la ponctuation française pouvant inclure une espace insécable — ne pas la retaper à la main, la copier).

3. `preview.prepare()` : remplacer le `subtitle` `'Introduction'` par `'Introduction et référencement'`, comme `homePage.ts`. Le `title` `'Page Éditions'` ne change pas.

4. Commentaire d'en-tête : il affirme aujourd'hui que le schéma reproduit `homePage.ts` en excluant délibérément le champ/groupe SEO, et que c'était hors périmètre. Cette affirmation est maintenant fausse et doit disparaître. Réécrire cette première phrase pour dire que le schéma reproduit `homePage.ts` (même motif de champ texte localisé, même champ `seo` partagé) et noter que les routes Astro `/editions` continuent, elles, de coder en dur leur propre `seoTitle`/`seoDescription` — ce champ n'est donc pas encore consommé par le site public. CONSERVER INTÉGRALEMENT le second paragraphe du commentaire (le contrat « byte-identical » avec `DEFAULT_EDITIONS_INTRO` dans `src/lib/site-config.ts` et la mention EDN-06-clean) : il reste vrai et load-bearing.

5. Ne toucher à RIEN d'autre : `defaultIntro` (les deux chaînes fr/en), `initialValue`, et tout le `defineField` de `intro` restent strictement inchangés.

Dans `src/lib/sanity.ts`, correction de commentaire UNIQUEMENT : le commentaire JSDoc au-dessus de l'interface `EditionsPage` justifie aujourd'hui la forme intro-seule en affirmant que le schéma n'a pas de champ/groupe SEO — c'est désormais faux. Le reformuler pour dire que le schéma Studio expose bien un champ `seo`, mais que le site public ne le lit pas encore (les routes `/editions` codent leurs chaînes en dur), d'où la projection volontairement limitée à `intro`. NE PAS modifier l'interface `EditionsPage`, NE PAS modifier `EDITIONS_PAGE_QUERY`.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website &amp;&amp; grep -qF "{name: 'seo', title: 'SEO'}" sanity/schemas/editionsPage.ts &amp;&amp; test "$(grep -c "group: 'seo'" sanity/schemas/editionsPage.ts)" = 1 &amp;&amp; test "$(grep -c "type: 'seo'" sanity/schemas/editionsPage.ts)" = 1 &amp;&amp; grep -qF "Introduction et référencement" sanity/schemas/editionsPage.ts &amp;&amp; test "$(grep -cF 'minus the seo field/group' sanity/schemas/editionsPage.ts)" = 0 &amp;&amp; grep -qF "byte-identical to DEFAULT_EDITIONS_INTRO" sanity/schemas/editionsPage.ts &amp;&amp; diff <(grep -o "Facultatif.*vides\." sanity/schemas/homePage.ts) <(grep -o "Facultatif.*vides\." sanity/schemas/editionsPage.ts) &amp;&amp; diff <(grep -o "Les Éditions sont les objets.*" sanity/schemas/editionsPage.ts) <(grep -o "Les Éditions sont les objets.*" src/lib/site-config.ts) &amp;&amp; test "$(sed -n '/export interface EditionsPage/,/^}/p' src/lib/sanity.ts | grep -c seo)" = 0 &amp;&amp; grep -qF '*[_id == "editionsPage"][0]{ intro }' src/lib/sanity.ts &amp;&amp; npm --prefix sanity run lint &amp;&amp; npm --prefix sanity run build</automated>
  </verify>
  <!-- Les deux greps ci-dessus sont des portes POSITIVES (exactement 1 occurrence attendue) : ces littéraux sont précisément ce que la tâche ajoute au fichier. -->
  <!-- planner-discipline-allow: group: 'seo' -->
  <!-- planner-discipline-allow: type: 'seo' -->
  <done>
`editionsPage` déclare deux groupes (`content` par défaut, `SEO`) et un champ `seo` de type partagé `seo` rattaché au groupe `seo`, avec la même chaîne `description` que `homePage.ts` au caractère près ; le sous-titre de preview mentionne les deux préoccupations ; le contrat byte-identical de `defaultIntro` est intact et les deux chaînes d'intro n'ont subi aucune suppression/modification ; `src/lib/sanity.ts` ne contient que des changements de lignes commentées ; lint + build du Studio passent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restaurer les items SEO dans la checklist editionsPage et inverser le test de régression</name>
  <files>tests/unit/editorial-checks.test.ts, sanity/editorial/checks.ts</files>
  <behavior>
    - `getDocumentChecks('editionsPage', {intro: {fr, en}})` renvoie 4 items : 1 obligatoire (« Introduction française et anglaise ») + les 3 items SEO recommandés, exactement comme `getDocumentChecks('homePage', {intro: localized})` (déjà asserté à 4 dans le test « covers singleton, settings, exhibition, and unknown document types »).
    - Les 3 items SEO portent `recommended: true` : avec une intro bilingue et un `seo` vide, `summarizeChecks(...).requiredComplete` reste `true` et `recommendedComplete` est `false`.
    - Avec un `seo` complet (`title` fr+en, `description` fr+en, `image.asset`), `recommendedComplete` devient `true`.
    - Le test existant « requires the Éditions navigation label and the bilingual Éditions introduction » continue de passer sans modification (intro fr seule ⇒ `requiredComplete` false).
  </behavior>
  <action>
Procéder en RED d'abord.

RED — dans `tests/unit/editorial-checks.test.ts`, remplacer le test de régression actuel (celui qui affirme que la checklist `editionsPage` ne liste jamais d'items SEO, ~ligne 129-140, ajouté par la session de debug d'aujourd'hui quand le champ n'existait pas encore) par un test qui assure l'inverse, maintenant que le champ existe légitimement. Renommer le `it(...)` en conséquence (par ex. « lists the SEO checklist items for editionsPage now that the schema exposes an seo field ») et remplacer son commentaire explicatif : indiquer que `editionsPage.ts` expose désormais le groupe + champ `seo` partagé, donc les items sont atteignables dans l'onglet SEO du Studio, et que la session de debug `editionspage-no-title-seo` est explicitement supersédée par ce changement de scope. Assertions attendues, en reprenant les motifs déjà présents dans le fichier : longueur 4 via `toHaveLength(4)` (même forme que l'assertion `homePage` du test « covers singleton... ») ; exactement 3 items `recommended` ; `requiredComplete` vrai avec intro bilingue et `seo` absent ; `recommendedComplete` faux avec `seo` absent puis vrai avec un `seo` complet (`{title: localized, description: localized, image: {asset: {_ref: 'image-asset'}}}`, en réutilisant la fixture `localized` du haut de fichier). Lancer le test seul et CONSTATER L'ÉCHEC (checks.ts renvoie encore 1 item).

GREEN — dans `sanity/editorial/checks.ts`, branche `if (schemaType === 'editionsPage')` (~ligne 210) : supprimer entièrement le bloc de commentaire qui justifiait l'absence des vérifications SEO (il est devenu faux) et ajouter `...seoChecks(value.seo)` après l'item `Introduction française et anglaise`, en respectant exactement la forme utilisée par les branches voisines `homePage`, `aboutPage` et `contactPage` (spread en dernière position du tableau retourné, tableau multi-lignes). Ne modifier aucune autre branche, ni `seoChecks`, ni `CHECKLIST_TYPES`, ni `summarizeChecks`. Relancer le test : GREEN.

Puis lancer la suite unitaire complète pour confirmer qu'aucun autre test (dashboard, workflow) ne dépend de l'ancien compte de 1 item.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website &amp;&amp; test "$(sed -n '/schemaType === .editionsPage./,/^  }/p' sanity/editorial/checks.ts | grep -c 'seoChecks(value.seo)')" = 1 &amp;&amp; test "$(sed -n '/schemaType === .editionsPage./,/^  }/p' sanity/editorial/checks.ts | grep -cF 'No ...seoChecks')" = 0 &amp;&amp; npm run test:unit -- tests/unit/editorial-checks.test.ts &amp;&amp; npm run test:unit</automated>
  </verify>
  <!-- Porte POSITIVE (exactement 1 occurrence attendue dans la branche editionsPage) : ce littéral est précisément ce que la tâche restaure. -->
  <!-- planner-discipline-allow: seoChecks(value.seo) -->
  <done>
La branche `editionsPage` de `getDocumentChecks` renvoie 4 items dont 3 recommandés, le commentaire justifiant l'omission a disparu, le test de régression assure désormais la présence des items SEO et leur caractère non bloquant, et l'intégralité de `npm run test:unit` passe (aucune régression sur dashboard-logic / workflow-logic).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Éditeur Studio → Content Lake | L'éditeur saisit désormais des chaînes SEO libres sur un nouveau champ du singleton `editionsPage`. |
| Content Lake → build Astro | Le champ n'est PAS projeté par `EDITIONS_PAGE_QUERY` : aucune de ces chaînes n'atteint le site public dans ce scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260801-id4-01 | Tampering | Chaînes SEO éditeur rendues en HTML | low | accept | Le champ n'est consommé par aucune route (`EDITIONS_PAGE_QUERY` reste `{ intro }`, contrainte du scope). Si une phase ultérieure branche le champ, l'échappement par défaut d'Astro s'appliquera comme pour `homePage.seo` aujourd'hui. |
| T-260801-id4-02 | Denial of Service | Porte de publication du tableau de bord | medium | mitigate | Les 3 nouveaux items sont `recommended: true` via `seoChecks()` ; si l'un devenait obligatoire, un `editionsPage` sans SEO bloquerait tout le lot de publication. Vérifié par l'assertion explicite `requiredComplete === true` avec `seo` vide dans le test de Task 2. |
| T-260801-id4-03 | Information Disclosure | Dataset Sanity live | low | mitigate | Aucune écriture ni requête authentifiée dans ce plan ; l'inspection Studio éventuelle est en lecture seule sur `localhost:3333`. Interdiction explicite dans `<scope_boundaries>` point 3. |
| T-260801-id4-SC | Tampering | Chaîne d'approvisionnement | n/a | accept | Aucune installation npm/pip/cargo dans ce plan : le champ réutilise le type objet `seo` déjà présent dans le dépôt. Aucun audit de légitimité requis. |
</threat_model>

<verification>
Suite complète de portes, toutes doivent être vertes avant commit :

1. `npm run test:unit` — 0 échec (couvre `checks.ts` et le test de régression inversé).
2. `npm run lint` — propre.
3. `npm run typecheck` — 0 erreur (les indices préexistants non liés sont tolérés).
4. `npm --prefix sanity run lint` — propre.
5. `npm --prefix sanity run build` — succès (seule vraie compilation du schéma).
6. `git diff --stat` ne liste QUE : `sanity/schemas/editionsPage.ts`, `src/lib/sanity.ts`, `sanity/editorial/checks.ts`, `tests/unit/editorial-checks.test.ts`. Toute apparition de `src/pages/editions/*` est une violation de scope à annuler.

Confirmation visuelle facultative (non bloquante) : un serveur `sanity dev` tourne sur localhost:3333 — ouvrir « Contenu du site » → « Page Éditions » et vérifier qu'un onglet SEO apparaît à côté de Contenu et que la checklist affiche de nouveau les trois items SEO. Inspection en lecture seule : ne rien saisir, ne rien sauvegarder.
</verification>

<success_criteria>
- [ ] `editionsPage.ts` déclare le groupe `seo` et un champ `seo` de type partagé `seo`, copie conforme du motif `homePage.ts` (titre adapté à la page Éditions, `description` identique au caractère près).
- [ ] `preview.prepare()` renvoie le sous-titre `'Introduction et référencement'`.
- [ ] Le commentaire d'en-tête de `editionsPage.ts` ne contient plus l'affirmation devenue fausse d'une exclusion délibérée du SEO, et conserve intégralement l'avertissement byte-identical sur `defaultIntro`.
- [ ] `defaultIntro`, `initialValue` et le champ `intro` sont strictement inchangés.
- [ ] `checks.ts` branche `editionsPage` : `...seoChecks(value.seo)` restauré, commentaire obsolète supprimé.
- [ ] `tests/unit/editorial-checks.test.ts` assure la présence des 3 items SEO ET leur caractère recommandé/non bloquant.
- [ ] Les 5 portes automatisées de `<verification>` passent.
- [ ] Aucune route Astro modifiée, aucun document Sanity live créé ou publié.
- [ ] Commits sur `codex/studio-publication-workflow` (pas de nouvelle branche), messages scopés `quick-260801-id4-01`.
</success_criteria>

<output>
Créer `.planning/quick/260801-id4-add-seo-group-to-editionspage-schema/260801-id4-SUMMARY.md` en fin d'exécution.
</output>
