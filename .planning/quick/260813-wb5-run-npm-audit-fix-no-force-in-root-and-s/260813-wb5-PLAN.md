---
phase: quick-260813-wb5-run-npm-audit-fix-no-force-in-root-and-s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package-lock.json
  - sanity/package-lock.json
autonomous: true
requirements: [QUICK-260813-WB5]
---

## Objective

Patch non-breaking npm audit vulnerabilities in both the root project and `sanity/`, without using `--force` anywhere (which would pull a different `sanity` package version than the one deliberately pinned to exactly 6.6.0 in `260811-kog-06`).

<task>
<name>Run npm audit fix in both projects and verify nothing broke</name>
<files>package-lock.json, sanity/package-lock.json</files>
<action>
1. `npm audit fix` in the root project (4 high: brace-expansion, fast-uri, js-yaml, nanoid — all resolvable without --force).
2. `npm audit fix --prefix sanity` (brace-expansion, dompurify, nanoid resolvable without --force; js-yaml and undici remain, both nested inside `@sanity/cli`'s own dependency chain via `@vercel/frameworks`/`@module-federation/dts-plugin`, both only fixable via `--force` which would install `sanity@5.14.1` — a downgrade from the pinned 6.6.0. Do NOT force-fix these; leave them as a recorded follow-up.)
3. Run the full verification chain: `npm run typecheck`, `npx vitest run`, `npm --prefix sanity run lint`, `npm --prefix sanity run build`, `npm --prefix sanity run test:coverage`.
4. Confirm `git diff --stat` shows only the two lockfiles changed, no `package.json` version bumps.
</action>
<verify>npm audit; npm audit --prefix sanity; npm run typecheck; npx vitest run; npm --prefix sanity run lint; npm --prefix sanity run build; npm --prefix sanity run test:coverage</verify>
<done>Root shows 0 vulnerabilities; sanity/ shows only the 2 known --force-only advisories (js-yaml, undici), both nested in the sanity CLI's own dependency tree; full verification chain passes identically to before the fix.</done>
</task>

No new packages added deliberately — only transitive dependency versions bumped by npm's own audit-fix resolution. Package Legitimacy Gate not applicable.
