---
phase: quick-260728-g76
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EditionsOverviewBody.astro
autonomous: true
requirements:
  - "FOLLOW-UP UI CORRECTION: fixes a targeting mistake in the merged quick task 260728-el6 (Éditions overview intro + statement-clip fix, commits 51f8f46/3893379). Active REQUIREMENTS.md was reset after v1.3 closed and carries no EDN-02 tag, so this has no user-facing requirement of its own — it is Florian's live spacing correction on the deployed /editions/ page."
must_haves:
  truths:
    - "On both /editions/ and /en/editions/, there is NO hairline rule (`border-top`) above the `ATELIER JACQUELINE SUZANNE` eyebrow at the top of the `.editions-list__header` block — the line Florian pointed to in his screenshot is gone."
    - "The `Éditions` title block (eyebrow + h1 + intro) sits visibly higher on the page than before, because the header's `padding-top` that only existed to clear the removed border is gone — satisfying `remonter le titre plus haut` as a direct consequence, with no separate margin hack."
    - "The DIFFERENT hairline removed by 260728-el6 (`.editions-index__row:first-of-type` border-top, the line BELOW `Édition`) stays removed — this task does not reintroduce it."
    - "`margin-bottom: var(--space-2xl)` on `.editions-list__header` and all eyebrow / h1 / intro margins are unchanged; the only spacing shift is the removed header padding-top."
  artifacts:
    - "src/components/EditionsOverviewBody.astro (`.editions-list__header` CSS rule: two property lines removed, `margin-bottom` retained)"
  key_links:
    - "Both /editions/ and /en/editions/ render through this one shared `EditionsOverviewBody.astro` component → the single CSS edit applies to both locales with no per-locale route work."
    - "The `padding-top: var(--space-md)` (16px) removed here existed ONLY to create breathing room below the now-removed `border-top` → removing it is what raises the title block; the two removals are one coupled change, not two independent tweaks."
---

<objective>
Correct a targeting mistake from the just-shipped quick task 260728-el6. Florian gave live feedback on a screenshot of the deployed `/editions/` page: 260728-el6 removed the hairline BELOW the `Édition` header, but the line he actually meant is the DIFFERENT one still present ABOVE the `ATELIER JACQUELINE SUZANNE` eyebrow at the very top of the page. He also asked to bring the `Éditions` title up a bit higher (`remonter... plus haut`).

Root cause (already confirmed — do not re-investigate): the offending hairline is the `border-top` in the `.editions-list__header` rule of `src/components/EditionsOverviewBody.astro`. Its paired `padding-top: var(--space-md)` (16px) exists only to hold the title clear of that border. Removing the border AND that padding-top deletes the unwanted line and raises the whole header block — satisfying both asks with a single coupled change. The identical `padding-top` + `border-top` header pattern also appears independently in `AboutPageBody.astro` and `ContactPageBody.astro`, but Florian's feedback is Éditions-specific — those files are OUT of scope.

Purpose: match the deployed Éditions header to Florian's live correction.
Output: two property lines removed from one CSS rule in one shared component.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# The one file being touched — only the `.editions-list__header` CSS rule (currently ~lines 77-81).
@src/components/EditionsOverviewBody.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the header hairline + its paired padding-top from EditionsOverviewBody</name>
  <files>src/components/EditionsOverviewBody.astro</files>
  <action>
In `src/components/EditionsOverviewBody.astro`, find the `.editions-list__header` CSS rule inside the `<style>` block. It currently reads:

  .editions-list__header {
    padding-top: var(--space-md);
    border-top: var(--border-hairline) solid var(--color-ink);
    margin-bottom: var(--space-2xl);
  }

Delete TWO property lines from this rule:
1. `border-top: var(--border-hairline) solid var(--color-ink);` — the unwanted hairline above the eyebrow (the one Florian pointed to; NOT the `.editions-index__row:first-of-type` line 260728-el6 already removed).
2. `padding-top: var(--space-md);` — this padding existed only to clear the now-removed border; removing it entirely (not reducing to a smaller nonzero value) raises the eyebrow/title/intro block, which is the more literal reading of `remonter... plus haut`.

After the edit the rule must be exactly:

  .editions-list__header {
    margin-bottom: var(--space-2xl);
  }

Scope guardrails (do NOT deviate):
- Touch ONLY this file, ONLY the `.editions-list__header` rule. Leave `margin-bottom: var(--space-2xl)` on that rule untouched.
- Do NOT touch `.editions-list__eyebrow`, `.editions-list h1`, or `.editions-list__intro` — their margins stay as-is; the header padding-top removal alone is the fix, no compensating margin change.
- Do NOT reintroduce any `.editions-index__row:first-of-type` border (260728-el6 correctly removed that separate line — it stays removed).
- Do NOT touch `AboutPageBody.astro` or `ContactPageBody.astro` even though they carry the same header pattern — Florian's correction is Éditions-specific.
- Do NOT touch any route file (`src/pages/editions/index.astro`, `src/pages/en/editions/index.astro`), `src/lib/site-config.ts`, `src/lib/sanity.ts`, or any Sanity schema.
- No markup change — CSS-only, one rule, two property removals. No new test assertions (subjective spacing tweak, not an invariant to lock in).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && npm run test:artifact && npx playwright test tests/e2e/edition.spec.ts && npx playwright test tests/e2e/site-header.spec.ts</automated>
  </verify>
  <done>The `.editions-list__header` rule contains only `margin-bottom: var(--space-2xl)` — no `border-top`, no `padding-top`. Typecheck, build, and artifact verification pass; `edition.spec.ts` and `site-header.spec.ts` still pass (confirmed pre-edit: neither spec asserts on `.editions-list__header`'s box model or border). The diff is two removed lines in one file; both /editions/ and /en/editions/ show no hairline above the eyebrow and a higher-sitting title.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → static artifact | CSS-only change to one shared Astro component; touches no runtime/server surface, no dependency, no data path, no user input. Purely presentational. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g76-01 | Tampering | The `.editions-list__header` visual layout | low | mitigate | Change is scoped to two property removals in one CSS rule; `margin-bottom` and all sibling element margins are explicitly retained. Existing `edition.spec.ts` + `site-header.spec.ts` (verified pre-edit to not assert on this rule) must still pass, plus build + artifact checks. |
| T-g76-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; no dependency changes to root or `sanity/`. Supply-chain surface unchanged. |
</threat_model>

<verification>
1. `npm run typecheck` — Astro/TS clean.
2. `npm run build` — static build succeeds with the edited component.
3. `npm run test:artifact` — built artifact verification passes.
4. `npx playwright test tests/e2e/edition.spec.ts` — Éditions e2e still green (no assertion on `.editions-list__header`).
5. `npx playwright test tests/e2e/site-header.spec.ts` — site-header e2e still green.
</verification>

<success_criteria>
- `.editions-list__header` rule = only `margin-bottom: var(--space-2xl)`; `border-top` and `padding-top` gone.
- No hairline above the `ATELIER JACQUELINE SUZANNE` eyebrow on /editions/ or /en/editions/; title block sits higher.
- 260728-el6's `.editions-index__row:first-of-type` border removal not reintroduced; no other selector, file, route, or Sanity surface touched.
- Typecheck + build + artifact + both e2e specs pass; diff is two removed lines in one file.
</success_criteria>

<output>
Create `.planning/quick/260728-g76-fix-editions-header-hairline-and-title-p/260728-g76-SUMMARY.md` when done.
</output>
