---
id: 260811-9f5
status: complete
subsystem: e2e-tests
tags: [playwright, flaky-test, image-loading]
---

# Quick Task 260811-9f5 Summary

Corrigé l'instabilité E2E identifiée pendant l'audit : deux tests calculaient `naturalWidth / naturalHeight` sur une image Sanity avant qu'elle ait fini de décoder, produisant occasionnellement `NaN`. Les deux attendent maintenant explicitement `img.complete && img.naturalWidth > 0` via `expect.poll`, en reprenant un pattern déjà établi ailleurs dans `edition.spec.ts` (lignes ~299-312) pour le même problème sur un groupe de tuiles.

## Changes

- `tests/e2e/edition.spec.ts` (test « galleries unaffected: the gallery masonry path renders identically now that éditions share it ») : ajout d'un `await expect.poll(...).toBe(true)` sur `firstTileImg` juste avant le calcul du ratio.
- `tests/e2e/gallery.spec.ts` (test « gallery detail (masonry): every tile has 0px borders, keeps the ink loading background, and never letterboxes ») : même ajout, adapté à ce fichier qui n'avait pas encore ce pattern.

Aucun composant, style ou fichier `src/` modifié.

## Verification — résultats exacts

- `npx playwright test edition.spec.ts -g "galleries unaffected" --project=chromium --repeat-each=5` → **5 passed (1.5s)**, 0 échec, plus aucun NaN.
- `npx playwright test gallery.spec.ts -g "every tile has 0px borders" --project=chromium --repeat-each=5` → **10 passed (2.2s)** (le motif matche aussi le test édition voisin, lui aussi vert), 0 échec.
- `npm run typecheck` → **0 errors, 0 warnings, 1 hint** (hint pré-existant, sans rapport, `homepage-wordmark-peek.spec.ts:1031`, `webkitBackgroundClip` déprécié).
- `npm run lint` → **clean**, aucune sortie.
- `npm run test:unit` → **349/349 passed** (16 fichiers), aucune régression.
- `npm run test:e2e` (suite complète, chromium + webkit-mobile) → **334/334 passed (35.1s)**, aucun échec, aucun flake observé sur cette exécution.

## Files

- `tests/e2e/edition.spec.ts`
- `tests/e2e/gallery.spec.ts`

## Self-Check: PASSED

Les deux fichiers ciblés sont modifiés, rien d'autre. `git diff --stat` confirme +11/+11 lignes, aucun fichier `src/` touché. Les 4 commandes demandées ont toutes été exécutées avec les résultats ci-dessus.

**Aucun commit effectué** — conformément à la demande, en attente de validation.
