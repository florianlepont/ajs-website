---
phase: 260727-p0m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/homepage-carousel-core.spec.ts
  - tests/e2e/homepage-wordmark-peek.spec.ts
  - tests/e2e/homepage-content-display.spec.ts
  - tests/e2e/homepage-chrome-nav.spec.ts
  - tests/e2e/homepage-mobile-responsive.spec.ts
  - tests/e2e/homepage-loading-progress.spec.ts
  - tests/e2e/homepage.spec.ts
  - tests/e2e/site-header.spec.ts
  - tests/e2e/i18n.spec.ts
  - tests/e2e/gallery.spec.ts
autonomous: true
requirements: [AUDIT-homepage-spec-split]

must_haves:
  truths:
    - "Running the 6 homepage-*.spec.ts files executes exactly 97 tests with the identical passed/skipped outcomes as the pre-split baseline run of homepage.spec.ts"
    - "tests/e2e/homepage.spec.ts no longer exists (no stub, no re-export)"
    - "Each of the 6 new files contains exactly the describe blocks assigned to it, moved verbatim (assertions/selectors/timeouts/comments byte-for-byte unchanged)"
    - "The full suite (typecheck, coverage, build, playwright) passes with the same result as before the split (252 tests unaffected)"
  artifacts:
    - tests/e2e/homepage-carousel-core.spec.ts
    - tests/e2e/homepage-wordmark-peek.spec.ts
    - tests/e2e/homepage-content-display.spec.ts
    - tests/e2e/homepage-chrome-nav.spec.ts
    - tests/e2e/homepage-mobile-responsive.spec.ts
    - tests/e2e/homepage-loading-progress.spec.ts
  key_links:
    - "Every new file's first line is: import { test, expect, devices } from '@playwright/test';"
    - "The RED/Wave-0 top comment block moves to homepage-carousel-core.spec.ts"
    - "Sibling comment references (site-header, i18n, gallery) point to the correct new filenames"
---

<objective>
Split the 2,302-line tests/e2e/homepage.spec.ts (40 top-level describe blocks, 97 test/test.skip calls) into 6 feature-scoped spec files, then delete the original. This is a byte-preserving mechanical reorganization from a codebase-audit finding — ZERO behavior, assertion, selector, timeout, or comment changes inside any describe block.

Purpose: Reduce a single unwieldy 2.3k-line test file into navigable feature-scoped files (the wordmark-peek group alone absorbed a whole chain of bug-fix sessions). Playwright auto-discovers `*.spec.ts`, so no registry/index needs updating.

Output: 6 new `tests/e2e/homepage-*.spec.ts` files, the original deleted, and 4 stale sibling-comment cross-references repointed.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@tests/e2e/homepage.spec.ts

# The grouping below is authoritative and already verified — do NOT re-audit or re-derive.
# Every describe block is self-contained: local helpers (photoBox, sampleSeamDuringSlide,
# peekVisibleFraction, collectSamples, peekTranslateXPercent, transformTranslateXPx,
# scaleXFromTransform, etc.) are declared INSIDE their own describe block, so each block
# (with its inner helpers/nested describes) moves wholesale with zero cross-file wiring.
# The ONLY module-level import is line 1: import { test, expect, devices } from '@playwright/test';
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline, then create the 6 feature-scoped spec files by verbatim describe-block moves</name>
  <files>tests/e2e/homepage-carousel-core.spec.ts, tests/e2e/homepage-wordmark-peek.spec.ts, tests/e2e/homepage-content-display.spec.ts, tests/e2e/homepage-chrome-nav.spec.ts, tests/e2e/homepage-mobile-responsive.spec.ts, tests/e2e/homepage-loading-progress.spec.ts</files>
  <action>
FIRST, capture the pre-split baseline WHILE the original still exists (do not delete it in this task — deletion is Task 2). Run `npx playwright test tests/e2e/homepage.spec.ts` and record the exact summary line (the "N passed / M skipped" totals) — save it to the scratchpad file `/private/tmp/claude-501/-Users-florian-Projects-ajs-website/f54e06d4-24a6-43bd-bef0-38779dbdd202/scratchpad/homepage-baseline.txt` so Task 3 can compare. Also record the baseline test count: `grep -rhE "^[[:space:]]*test(\.skip)?\(" tests/e2e/homepage.spec.ts | wc -l` must print 97.

THEN create the 6 new files. Read tests/e2e/homepage.spec.ts once, in full. Each file's FIRST line MUST be exactly: import { test, expect, devices } from '@playwright/test'; — then, below it, the assigned describe blocks moved VERBATIM (full contents including local helpers and any nested describes), in the SAME relative order they appear in the original (the lists below are already in ascending original-file order). Copy byte-for-byte: do NOT rename, refactor, reformat, re-indent, or alter any assertion, selector, timeout, string, or comment.

homepage-carousel-core.spec.ts (11 blocks — the RED/Wave-0 top comment block at original lines 3-8 moves here, immediately below the import, because the "homepage carousel" block it describes now lives here):
  - homepage carousel
  - only galleries with photos appear (D-12)
  - carousel/grid display mode toggle (D-08)
  - auto-advance + pause (D-09)
  - i18n non-regression guard
  - single unified mode toggle (HOME-01, D-01/D-02)
  - grid hero-as-first-tile (HOME-02, D-04/D-06)
  - view-transition toggle — reduced-motion still swaps modes
  - view-transition accent-panel sequencing
  - square mode-toggle box (HOME-05)
  - mode-toggle icon color regression (HOME-10-REGRESSION)

homepage-wordmark-peek.spec.ts (12 blocks):
  - carousel hover cursor (sketch 008 Variant C)
  - carousel peek transform is un-eased while tracking (Bug 1)
  - carousel is-tracking re-armed after edge-click commit (quick-260727-fc2)
  - carousel wordmark mirrored-peek commit (quick-260727-iao)
  - carousel wordmark stays synced to the peek (Bug A)
  - carousel wordmark current layer does not freeze during an edge approach (quick-260727-kq8)
  - carousel edge-peek preview (sketch 008 Variant C)
  - carousel hover-click navigation (sketch 008 Variant C)
  - carousel accent panel narrowing, no wordmark clip (Item 4)
  - carousel accent panel crossfades on navigation (Bug 3)
  - carousel wordmark cutout (HOME-03, D-08)
  - grid hero wordmark cutout — mobile (HOME-03, D-05 reversal)

homepage-content-display.spec.ts (8 blocks):
  - collection statements on the homepage
  - carousel title has no underline (quick-260726-obg)
  - carousel intro paragraph resize + reposition (Item 5)
  - grid hero tile text color tracks accent (260718-r2o)
  - grid-tile title alignment (260718-rhv)
  - grid-tile hover polish (260718-rhv)
  - homepage hero photo matches the gallery detail hero (landscape-preference consistency)
  - cross-document morph — click-time source name assignment (sketch 006)

homepage-chrome-nav.spec.ts (3 blocks):
  - Instagram nav link (HOME-04)
  - homepage semantic heading (quick-260720-nm3)
  - footer visibility by display mode (quick-260726-obg)

homepage-mobile-responsive.spec.ts (4 blocks):
  - mobile hero visibility (D-08)
  - narrow-phone header regression
  - mobile full-bleed hero regression (HOME-06)
  - tall-desktop full-bleed hero regression

homepage-loading-progress.spec.ts (2 blocks):
  - progressive image loading (HOME-09)
  - carousel progress fill (quick-260725-dcg)

Do NOT delete or modify the original tests/e2e/homepage.spec.ts in this task. Both the original and the 6 new files coexist at the end of Task 1 (Playwright will temporarily see duplicate test names — that is expected and resolved by Task 2's deletion; do not run the full suite yet in this task).
  </action>
  <verify>
    <automated>test -f tests/e2e/homepage-carousel-core.spec.ts && test -f tests/e2e/homepage-wordmark-peek.spec.ts && test -f tests/e2e/homepage-content-display.spec.ts && test -f tests/e2e/homepage-chrome-nav.spec.ts && test -f tests/e2e/homepage-mobile-responsive.spec.ts && test -f tests/e2e/homepage-loading-progress.spec.ts && echo "all 6 exist"; echo "describe counts:"; for f in carousel-core wordmark-peek content-display chrome-nav mobile-responsive loading-progress; do printf "%s " "$f"; grep -cE "^test\.describe\(" tests/e2e/homepage-$f.spec.ts; done; echo "combined test count:"; grep -rhE "^[[:space:]]*test(\.skip)?\(" tests/e2e/homepage-*.spec.ts | wc -l; echo "each file imports devices:"; grep -lE "^import \{ test, expect, devices \} from '@playwright/test';" tests/e2e/homepage-*.spec.ts | wc -l</automated>
  </verify>
  <done>All 6 files exist; per-file top-level describe counts are exactly 11 / 12 / 8 / 3 / 4 / 2 (carousel-core / wordmark-peek / content-display / chrome-nav / mobile-responsive / loading-progress = 40 total); combined test/test.skip count across the 6 files is exactly 97; all 6 files begin with the devices import (count = 6); baseline pass/skip summary saved to scratchpad. Original homepage.spec.ts still present and untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the original file and repoint the 4 stale sibling-comment cross-references</name>
  <files>tests/e2e/homepage.spec.ts, tests/e2e/site-header.spec.ts, tests/e2e/i18n.spec.ts, tests/e2e/gallery.spec.ts</files>
  <action>
Delete tests/e2e/homepage.spec.ts entirely (`git rm tests/e2e/homepage.spec.ts` or `rm`). Do NOT leave an empty stub or a re-exporting shim — Playwright auto-discovers spec files, nothing imports this one.

Then update the 4 sibling comment references that name the old file by exact path, so they point to the new file that now contains the mirrored block. These are navigational comments only — change ONLY the filename inside the comment, leave surrounding prose and code untouched:
  1. tests/e2e/site-header.spec.ts (~line 5): the comment 'mirrors tests/e2e/homepage.spec.ts's "Instagram nav link (HOME-04)" describe' — the "Instagram nav link (HOME-04)" block moved to homepage-chrome-nav.spec.ts. Change the path to tests/e2e/homepage-chrome-nav.spec.ts.
  2. tests/e2e/i18n.spec.ts (~line 20): the comment 'See "footer visibility by display mode (quick-260726-obg)" in homepage.spec.ts' — that block moved to homepage-chrome-nav.spec.ts. Change "homepage.spec.ts" to "homepage-chrome-nav.spec.ts".
  3. tests/e2e/gallery.spec.ts (~line 7): 'mirroring tests/e2e/homepage.spec.ts's own grid-discovery pattern' — the grid-discovery pattern lives in "only galleries with photos appear (D-12)", which moved to homepage-carousel-core.spec.ts. Change the path to tests/e2e/homepage-carousel-core.spec.ts.
  4. tests/e2e/gallery.spec.ts (~line 686): 'Mirrors homepage.spec.ts's own synthetic Touch/TouchEvent construction' — that synthetic Touch/TouchEvent code is inside "carousel hover-click navigation (sketch 008 Variant C)", which moved to homepage-wordmark-peek.spec.ts. Change "homepage.spec.ts" to "homepage-wordmark-peek.spec.ts".

Do NOT touch .planning/STATE.md or CLAUDE.md — their mentions of homepage.spec.ts are historical narrative, not live references. Do NOT touch any comment that discusses "the homepage tests" conceptually without naming the file by path.
  </action>
  <verify>
    <automated>test ! -f tests/e2e/homepage.spec.ts && echo "original deleted"; echo "stale refs remaining in tests/e2e (must be 0):"; grep -rc "homepage\.spec\.ts" tests/e2e/ | grep -v ':0$' || echo "0 stale refs in tests/e2e"; echo "new-file refs present:"; grep -roE "homepage-(chrome-nav|carousel-core|wordmark-peek)\.spec\.ts" tests/e2e/site-header.spec.ts tests/e2e/i18n.spec.ts tests/e2e/gallery.spec.ts</automated>
  </verify>
  <done>tests/e2e/homepage.spec.ts no longer exists; zero remaining `homepage.spec.ts` path references inside tests/e2e/; the 3 sibling files now name the correct new files (site-header + i18n → homepage-chrome-nav.spec.ts, gallery line ~7 → homepage-carousel-core.spec.ts, gallery line ~686 → homepage-wordmark-peek.spec.ts). No changes to surrounding prose or code.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the split matches the baseline and the full suite is unaffected</name>
  <files>tests/e2e/homepage-carousel-core.spec.ts, tests/e2e/homepage-wordmark-peek.spec.ts, tests/e2e/homepage-content-display.spec.ts, tests/e2e/homepage-chrome-nav.spec.ts, tests/e2e/homepage-mobile-responsive.spec.ts, tests/e2e/homepage-loading-progress.spec.ts</files>
  <action>
Run the 6 new files together: `npx playwright test tests/e2e/homepage-*.spec.ts`. The run MUST report exactly 97 tests, and the passed/skipped breakdown MUST match the baseline captured in Task 1 (scratchpad/homepage-baseline.txt) EXACTLY — same number passed, same number skipped, zero failures. Because the moves are byte-preserving, any deviation means a block was dropped, duplicated, altered, or landed in the wrong file — investigate and fix (re-diff the offending block against the original in git history: `git show HEAD:tests/e2e/homepage.spec.ts`) rather than adjusting assertions.

Then run the full verification suite to confirm the rest of the repo is unaffected: `npm run typecheck && npm run test:coverage && npm run build && npx playwright test`. The full Playwright run should show the same total as the last audit baseline (252 tests) with the same pass/skip outcomes; typecheck, coverage, and build must all pass.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/homepage-*.spec.ts 2>&1 | tail -20; echo "=== baseline for comparison ==="; cat /private/tmp/claude-501/-Users-florian-Projects-ajs-website/f54e06d4-24a6-43bd-bef0-38779dbdd202/scratchpad/homepage-baseline.txt 2>/dev/null || echo "baseline file missing"</automated>
    <automated>npm run typecheck && npm run test:coverage && npm run build && npx playwright test 2>&1 | tail -25</automated>
  </verify>
  <done>`npx playwright test tests/e2e/homepage-*.spec.ts` reports exactly 97 tests with the identical passed/skipped counts as the Task 1 baseline and zero failures. The full suite (typecheck + coverage + build + full playwright, 252 tests) passes with the same outcomes as before the split.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Pure test-file reorganization: no new runtime code, no user input, no network surface, no new dependencies installed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260727p0m-01 | Tampering | e2e test coverage integrity | medium | mitigate | Byte-preserving move verified by exact 97-count assertion + baseline pass/skip comparison — a dropped/altered/duplicated test fails the Task 3 gate before merge. |
| T-260727p0m-02 | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed; no install task exists in this plan. |
</threat_model>

<verification>
- All 6 new files exist under tests/e2e/ with the exact describe-block grouping (11/12/8/3/4/2 top-level blocks).
- Combined test/test.skip count across the 6 files = 97 (identical to the original's baseline count).
- Original tests/e2e/homepage.spec.ts deleted; no `homepage.spec.ts` path reference remains inside tests/e2e/.
- 4 sibling comment cross-references repointed to their correct new files.
- `npx playwright test tests/e2e/homepage-*.spec.ts` = 97 tests, same pass/skip as baseline, zero failures.
- Full suite (typecheck + coverage + build + full playwright / 252 tests) unaffected.
</verification>

<success_criteria>
The homepage e2e suite is split into 6 feature-scoped files with byte-for-byte identical test content, the monolithic original is removed, sibling cross-references are accurate, and both the split-file run (97 tests) and the full suite (252 tests) pass with outcomes identical to pre-split.
</success_criteria>

<output>
Create `.planning/quick/260727-p0m-split-homepage-spec-ts-into-6-feature-sc/260727-p0m-SUMMARY.md` when done.
</output>