---
phase: quick-260724-uf5
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DetailHero.astro
  - src/pages/galleries/[slug].astro
  - src/pages/en/galleries/[slug].astro
  - src/layouts/BaseLayout.astro
  - src/components/SiteHeader.astro
  - src/components/HomeCarousel.astro
  - tests/e2e/gallery.spec.ts
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [QUICK-260724-uf5]

must_haves:
  truths:
    - "The gallery detail hero renders object-fit: cover (cropped edge-to-edge, no letterboxing), exactly like the homepage carousel/grid — the `objectFit` prop and the `detail-hero__img--contain` modifier are GONE from DetailHero.astro, and neither gallery twin passes `objectFit` anymore."
    - "Both édition AND gallery detail heroes show a scroll-down chevron hint (bottom-center of the pin) on desktop-with-motion that bounces and fades out over roughly the first 150px of scroll; the bounce is disabled under prefers-reduced-motion, and the hint is hidden on mobile and under reduced-motion (where the reveal is already shown settled)."
    - "On DESKTOP (>=768px), clicking a homepage gallery title (carousel mode) or grid tile (grid mode) morphs that photo into the gallery detail page's full-screen hero via a native cross-document View Transition, with the site header persisting (not crossfading); browsers without support just navigate normally (progressive enhancement)."
    - "The gallery hero image (.detail-hero__img) carries a cross-document view-transition-name of `hero-photo` ONLY at desktop widths — its computed view-transition-name is `none` at mobile widths (<768px) — confining the name to the existing min-width:768px desktop gate as a direct precaution against the HOME-06/D-10/D-12 mobile-regression bug class on this 100svh-sized element."
    - "The site header carries view-transition-name: `ajs-header` site-wide — the EXACT name the homepage already assigns to the same element (HomeCarousel.astro) — reused, not reinvented; a mismatched name would make the header crossfade instead of persist."
    - "Éditions are untouched by the crop revert (they were already on the cover default) and by the cross-document morph (no homepage entry point into éditions exists); éditions DO receive the shared scroll-hint since DetailHero is shared."
    - "Preserved without regression: pickHeroIndex landscape preference + heroIndex prop, the uncropped masonry thumbnail grid, the Lightbox open/index contract, all reduced-motion/mobile CSS fallbacks, and the existing e2e coverage for both édition and gallery detail pages."
  artifacts:
    - src/components/DetailHero.astro
    - src/pages/galleries/[slug].astro
    - src/pages/en/galleries/[slug].astro
    - src/layouts/BaseLayout.astro
    - src/components/SiteHeader.astro
    - src/components/HomeCarousel.astro
    - tests/e2e/gallery.spec.ts
    - tests/e2e/homepage.spec.ts
  key_links:
    - "BaseLayout.astro `@view-transition { navigation: auto }` (site-wide opt-in in the existing is:global block) -> every same-origin navigation becomes a cross-document transition; without it, no morph fires anywhere and no CSS change matters."
    - "HomeCarousel click listeners assign `hero-photo` to the clicked SOURCE photo (heroImg for the carousel title; the tile's own sharp img for a grid tile) <-> DetailHero `.detail-hero__img { view-transition-name: hero-photo }` inside `@media (min-width: 768px)` on the DESTINATION -> the two matched ends of the morph. The names must be identical and only ONE source element may carry the name at a time."
    - "SiteHeader `.site-header { view-transition-name: ajs-header }` (identical value to HomeCarousel's existing `.home :global(.site-header)` rule) -> header persists across the navigation on every page, not just the homepage."
    - "DetailHero desktop-gated `hero-photo` (present at load on desktop, absent entirely on mobile) -> mobile-safety against the HOME-06/D-10/D-12 100svh always-on-name regression class."
---

<objective>
Three related changes to the shared detail-page hero (`DetailHero.astro`), resulting from live validation of sketch 006 (cross-page-hero-transition) with the user, plus one reversal of an earlier decision:

1. CHANGE 1 — Revert the GALLERY hero to `object-fit: cover` (undo quick-260724-mjp's no-crop `contain` choice for the hero specifically, per an explicit user reversal after seeing it live: "je veux pas de flou, je veux que la photo aie le meme format que sur la homepage"). The gallery hero must crop-to-fill edge-to-edge exactly like the homepage carousel/grid. Since galleries were the ONLY consumer of the `objectFit` prop and they are reverting, the prop becomes genuinely dead code — remove it entirely. The masonry thumbnail grid below (quick-260724-oep) and `pickHeroIndex` are NOT touched.
2. CHANGE 2 — Add a bouncing scroll-down chevron hint to the shared DetailHero (validated live as sketch 006 variant D), inviting first-time visitors to scroll and discover the reveal. Applies to BOTH éditions and galleries (shared component). Fades out over the first ~150px of scroll, respects prefers-reduced-motion, and is hidden on mobile.
3. CHANGE 3 — Implement the native cross-document View Transition from the homepage (carousel title AND grid tile) into the gallery detail hero, header persisting, validated live in sketch 006 (real-browser `pagereveal`/`event.viewTransition` proof, not just visual). Desktop-only for the photo morph, by design and for mobile safety.

Purpose: match the photographer's explicit "same format as the homepage" requirement for the hero, give first-time visitors an affordance to discover the scroll reveal, and land the smooth homepage->gallery morph that was sketched and technically verified live.
Output: `DetailHero.astro` with the objectFit prop removed + a scroll-hint + a desktop-gated cross-document view-transition-name; both gallery twins de-passing `objectFit`; the site-wide `@view-transition` opt-in in BaseLayout; the reused `ajs-header` name in SiteHeader; the homepage click-time name assignment in HomeCarousel; and e2e coverage for the crop revert, the desktop/mobile name gating, and the click-time wiring.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# The validated reference design + live technical-verification notes (read in full first):
@.planning/sketches/006-cross-page-hero-transition/README.md

# The shared component to modify (remove objectFit prop, add scroll-hint, add desktop-gated view-transition-name):
@src/components/DetailHero.astro

# The two gallery twins that revert to cover (remove objectFit="contain"):
@src/pages/galleries/[slug].astro
@src/pages/en/galleries/[slug].astro

# The site-wide layout (add @view-transition to the existing is:global block ~line 220):
@src/layouts/BaseLayout.astro

# The universal header (add the reused ajs-header name to its is:global block ~line 97):
@src/components/SiteHeader.astro

# The homepage entry point — read lines ~350-378 (titleEl/heroImg/gridTileImgs), ~695-732 (the
# established dynamic-naming click pattern to mirror), ~806-842 (existing ajs-header rationale),
# ~1105-1124 (HOME-06 mobile-regression history that motivates the desktop gate):
@src/components/HomeCarousel.astro

# Test files to extend (the grid objectFit assertion is grid-scoped and stays valid; no existing
# hero object-fit assertion exists, so Change 1 breaks nothing — add positive guards instead):
@tests/e2e/gallery.spec.ts
@tests/e2e/homepage.spec.ts

# The immediately-preceding quick task this one partially reverses (context on the objectFit prop it added):
@.planning/quick/260724-mjp-extend-the-sketch-005-synthesis-scroll-r/260724-mjp-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert the gallery hero to object-fit: cover — remove the now-dead objectFit prop end-to-end</name>
  <files>src/components/DetailHero.astro, src/pages/galleries/[slug].astro, src/pages/en/galleries/[slug].astro, tests/e2e/gallery.spec.ts</files>
  <action>
Undo quick-260724-mjp's no-crop escape hatch for the HERO only. Galleries were the sole consumer and are reverting, so the prop is genuinely dead — remove every trace, do not keep it as speculative flexibility.

In src/components/DetailHero.astro:
1. Delete the `objectFit?: 'cover' | 'contain';` line from the `Props` interface.
2. Remove `objectFit` (and its `= 'cover'` default) from the `const { ... } = Astro.props;` destructure so it is no longer destructured at all.
3. On the hero `<img>`, replace the conditional class expression (currently `class={`detail-hero__img${objectFit === 'contain' ? ' detail-hero__img--contain' : ''}`}`) with the plain static `class="detail-hero__img"`.
4. In the `<style>` block, delete the entire `.detail-hero__img--contain { object-fit: contain; }` rule. KEEP the base `.detail-hero__img { ... object-fit: cover; ... }` rule (that is now the only, correct hero behavior) and every other CSS rule untouched.
5. Update the component's file-header comment block so it no longer describes a gallery `objectFit="contain"` no-crop mode — the shared hero now always uses `object-fit: cover` for both éditions and galleries. Preserve the rest of the header comment (threat-model boundary note, statement/noscript notes, the `total`/`heroIndex` prop notes). Do NOT leave any comment sentence that still claims galleries render `contain` — a stale comment describing the removed behavior is a defect.

In src/pages/galleries/[slug].astro AND src/pages/en/galleries/[slug].astro (identical change on both twins):
6. Remove the `objectFit="contain"` attribute from the `<DetailHero ... />` element. Every other prop stays exactly as-is (leadPhotoSrc, leadPhotoSrcSet, leadPhotoAlt, title, total, heroAriaLabel, heroIndex, statement). With no `objectFit` passed, the hero now defaults to the component's plain cover behavior, matching éditions' existing unchanged behavior.
7. The `pickHeroIndex(gallery.images)` call and the `heroIndex`/`heroImage` locals stay byte-identical — landscape preference is still valuable under cover (a landscape source photo crops less aggressively than a portrait one in a wide cover box). However, the frontmatter comment above `pickHeroIndex` currently justifies it with "the hero renders object-fit: contain — a portrait images[0] looks small/heavily letterboxed" — that rationale is now false. Update that comment on BOTH twins to reflect cover (landscape crops less aggressively than portrait in a wide cover box). Do not change the logic, only the now-inaccurate comment.

Do NOT touch: the masonry `gridItems` construction, the `<GalleryGrid layout="masonry">`, the full-array `<Lightbox images={gallery.images}>`, SEO/structuredData, or `headerVariant="transparent"`. Éditions pages are already not passing `objectFit`, so they are unaffected by steps 1-4.

In tests/e2e/gallery.spec.ts:
8. Add a positive regression guard proving the hero is now cover. In the desktop `gallery hero reduced-motion (sketch 005)` describe block (or a small new desktop-viewport describe), after navigating to a gallery detail page, assert `page.locator('.detail-hero__img').first()` computes `object-fit: cover` via `getComputedStyle(el).objectFit`. Discover the gallery URL via the existing homepage-grid pattern used elsewhere in this file (goto "/", click the "Grille" button, read the first `a.home-grid__tile` href) — never hardcode a slug. Do NOT modify the existing grid-scoped assertion (`.gallery-grid [data-gallery-thumb] img` computed objectFit `not.toBe('cover')`) — the masonry grid stays uncropped and that assertion remains valid and must keep passing.
  </action>
  <!-- planner-discipline-allow: objectFit -->
  <!-- planner-discipline-allow: detail-hero__img--contain -->
  <verify>
    <automated>npm run typecheck</automated>
    Also confirm the prop and modifier are fully removed and cover is intact:
    `grep -rc "objectFit" src/components/DetailHero.astro "src/pages/galleries/[slug].astro" "src/pages/en/galleries/[slug].astro"` returns 0 for all three files;
    `grep -c "detail-hero__img--contain" src/components/DetailHero.astro` is 0 (modifier class + its CSS rule gone);
    `grep -c "object-fit: cover" src/components/DetailHero.astro` is >= 1 (base hero rule preserved);
    when the Sanity `.env` is available: `npm run test:e2e -- tests/e2e/gallery.spec.ts` passes (the new cover guard AND the unchanged grid-uncropped assertion both green).
  </verify>
  <done>`DetailHero.astro` no longer references `objectFit` anywhere (Props, destructure, class expression, CSS rule, header comment) and its hero always renders `object-fit: cover`; both gallery twins render `<DetailHero>` with no `objectFit` attribute and an updated (cover-accurate) pickHeroIndex comment; the masonry grid, Lightbox, SEO, and heroIndex logic are unchanged; éditions are untouched; `npm run typecheck` passes; gallery.spec.ts gains a hero-cover guard while the grid-uncropped assertion still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add a bouncing scroll-down hint to the shared DetailHero (éditions + galleries), reduced-motion + mobile safe</name>
  <files>src/components/DetailHero.astro, tests/e2e/gallery.spec.ts</files>
  <action>
Add a small scroll-down chevron affordance to the shared hero, validated live as sketch 006 variant D. Because DetailHero is shared, this appears on BOTH édition and gallery detail pages automatically — this is generic, not crop-related.

Markup (in DetailHero.astro):
1. Add a decorative hint element as the LAST child of `.detail-hero__pin` (after `.detail-hero__reveal`): a `<div class="detail-hero__scroll-hint" aria-hidden="true">` containing a downward chevron SVG. Use a 28x28 viewBox="0 0 24 24" SVG, `fill="none"`, `stroke="white"`, `stroke-width="2"`, with a single `<polyline points="6 9 12 15 18 9">` (the same chevron shape validated in the sketch). It is purely decorative and non-interactive, so `aria-hidden="true"` and no focus/role.

CSS (in the `<style>` block):
2. Base rule (this is the desktop-with-motion state, matching how the component's other base rules are the desktop-active state): `.detail-hero__scroll-hint` is `position: absolute; left: 50%; bottom: var(--space-lg); transform: translateX(-50%); z-index: 5; opacity: 0.85; pointer-events: none;` and runs a bounce animation `sketch-bounce 1.8s ease-in-out infinite`. Define `@keyframes sketch-bounce` translating between `translate(-50%, 0)` and `translate(-50%, 8px)` at 0/100% and 50% respectively (the transform must keep the `-50%` X-centering so the bounce is purely vertical).
3. Reduced-motion (HARD REQUIREMENT — must respect the convention already in this component): add a bare `@media (prefers-reduced-motion: reduce) { .detail-hero__scroll-hint { animation: none; } }` so the bounce never runs under reduced motion. Additionally, inside the EXISTING `@media (prefers-reduced-motion: reduce) and (min-width: 768px)` settled-end-state block, set `.detail-hero__scroll-hint { display: none; }` — under reduced-motion desktop the reveal is already shown fully settled, so there is nothing to hint at.
4. Mobile: inside the EXISTING `@media (max-width: 767px)` block, set `.detail-hero__scroll-hint { display: none; }` — on mobile the reveal panel is already visible (no scroll-jacking/pin), so the hint has no purpose. Net effect: the hint is visible + bouncing ONLY on desktop-with-motion, exactly where the scroll reveal it points at actually happens.

Script (in the `<script>` block):
5. Add a lightweight passive scroll listener that fades the hint out over the first ~150px of page scroll, mirroring the sketch's fade. Query `const scrollHint = document.querySelector<HTMLElement>('.detail-hero__scroll-hint');` near the other element lookups, and if it exists, `window.addEventListener('scroll', () => { scrollHint.style.opacity = String(Math.max(0, 1 - window.scrollY / 150) * 0.85); }, { passive: true });`. Place this OUTSIDE the existing `if (track && photo && scrim && overlayTitle && reveal) { ... }` guard so it does not depend on the reveal driver being active. Writing opacity on a `display: none` element (mobile / reduced-motion desktop) is a harmless no-op, so no extra branching is needed. Do NOT wire the hint's fade into `onProgress`/`clearInlineStyles` — keep it a self-contained block so it cannot perturb the proven reveal math.

In tests/e2e/gallery.spec.ts:
6. Add a small desktop-viewport assertion (reuse a `test.use({ viewport: { width: 1280, height: 900 } })` describe, or the existing desktop hero describe) that, on a gallery detail page discovered via the homepage-grid pattern, the `.detail-hero__scroll-hint` element exists and is visible at rest without scrolling. Then set `page.emulateMedia({ reducedMotion: 'reduce' })`, reload, and assert the hint's computed `animation-name` is `none` (bounce disabled) OR its computed `display` is `none` — either satisfies the reduced-motion requirement per the CSS above. Keep this assertion tolerant of both acceptable reduced-motion outcomes.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    Confirm the hint exists with the required safeguards:
    `grep -c "detail-hero__scroll-hint" src/components/DetailHero.astro` is >= 4 (markup + base rule + reduced-motion rule + one of the display:none fallbacks);
    `grep -c "sketch-bounce" src/components/DetailHero.astro` is >= 2 (animation reference + @keyframes);
    `grep -c "prefers-reduced-motion" src/components/DetailHero.astro` is >= 2 (the pre-existing settled-state block + the new animation:none block);
    when the Sanity `.env` is available: `npm run test:e2e -- tests/e2e/gallery.spec.ts` passes.
  </verify>
  <done>Both édition and gallery detail heroes render a bottom-center `.detail-hero__scroll-hint` chevron that bounces and is visible at rest on desktop-with-motion, fades to opacity 0 across the first ~150px of scroll, has its bounce disabled under prefers-reduced-motion, and is `display: none` on mobile and under reduced-motion desktop; the reveal math and all pre-existing reduced-motion/mobile fallbacks are unchanged; `npm run typecheck` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-document View Transition CSS — site-wide opt-in, reused ajs-header name, and the DESKTOP-GATED hero-photo name (mobile safety)</name>
  <files>src/layouts/BaseLayout.astro, src/components/SiteHeader.astro, src/components/DetailHero.astro, tests/e2e/gallery.spec.ts</files>
  <action>
Add the three CSS pieces that let the browser capture and morph the shared photo + persist the header across a real navigation. Progressive enhancement: unsupported browsers simply navigate normally, and no JS feature-detection is needed for the CSS opt-in itself.

In src/layouts/BaseLayout.astro:
1. Add the site-wide opt-in at the TOP of the existing `<style is:global>` block (the one starting ~line 220 that declares `:root { ... }`), before the `:root` rule: `@view-transition { navigation: auto; }`. Precede it with a brief comment noting this is the single site-wide cross-document View Transitions opt-in (sketch 006 / quick-260724-uf5), that EVERY page including the homepage goes through this layout, and that it is progressive enhancement (unsupported browsers navigate normally). This is the correct single opt-in point; do not add `@view-transition` anywhere else.

In src/components/SiteHeader.astro:
2. In the component's existing `<style is:global>` block (starting ~line 97), add a NEW rule applying the header's persistent transition name site-wide: `.site-header { view-transition-name: ajs-header; }`. Place it right after the existing `.site-header { display: flex; ... }` layout rule. Precede it with a comment stating: this deliberately REUSES the exact same name the homepage already assigns to this element via HomeCarousel.astro's `.home :global(.site-header) { view-transition-name: ajs-header }` rule (that more-specific homepage rule wins on the homepage but sets the IDENTICAL value, so there is no conflict); everywhere else (gallery/edition/about/contact) this newly lets the header participate as a persistent, named element in cross-document transitions; and that a second/different name must NOT be invented because a mismatch between source and destination documents would make the header crossfade instead of persist. Do NOT modify HomeCarousel.astro's existing `.home :global(.site-header)` rule or its `ajs-header` value.

In src/components/DetailHero.astro:
3. Give `.detail-hero__img` its cross-document `view-transition-name: hero-photo` ONLY inside a desktop-gated `@media (min-width: 768px)` block. This component has NO existing plain `@media (min-width: 768px)` CSS block (its media queries are `@media (prefers-reduced-motion: reduce) and (min-width: 768px)` and `@media (max-width: 767px)`), so ADD a new `@media (min-width: 768px) { .detail-hero__img { view-transition-name: hero-photo; } }` block. This breakpoint MUST match the script's existing `matchMedia('(min-width: 768px)')` desktop gate exactly. Precede it with a comment explaining: this is DESKTOP ONLY and NEVER unconditional / NEVER on mobile, as a direct precaution against the HOME-06/D-10/D-12 bug class (a permanent view-transition-name on a 100svh-sized element was the suspected mobile regression, and `.detail-hero__img`/`.detail-hero__pin` are likewise 100svh-sized); and that unlike the homepage's own dynamically-named in-page morph, a cross-document DESTINATION element must carry the name statically at load time because the incoming document's transition is captured as the page loads (there is no "assign it right before" option), so confining it to desktop is the safe equivalent — which also makes the photo morph desktop-only by design, consistent with the scroll-reveal already being desktop-only. Do NOT put this name in the base `.detail-hero__img` rule and do NOT add it to the `@media (max-width: 767px)` block.

In tests/e2e/gallery.spec.ts:
4. Add a describe block proving the name gating (this is the CRITICAL mobile-safety verification). Discover a gallery detail URL via the existing homepage-grid pattern. Test A — desktop (`test.use({ viewport: { width: 1280, height: 900 } })`): assert `getComputedStyle(document.querySelector('.detail-hero__img')).viewTransitionName` resolves to `hero-photo`, and that the header (`header[data-role="site-header"]`) resolves to `ajs-header`. Test B — mobile (`test.use({ viewport: { width: 390, height: 844 } })`) in its own describe: assert `.detail-hero__img` computed `viewTransitionName` is `none` (the name MUST be absent on mobile — this is the HOME-06 precaution). Read the computed value with `el.evaluate((n) => getComputedStyle(n).viewTransitionName)`.
  </action>
  <verify>
    <automated>npm run build</automated>
    (`npm run build` is the always-runnable gate; it fails on any Astro/CSS compile error across all three edited files.)
    Confirm the three pieces are present and correctly placed:
    `grep -c "@view-transition" src/layouts/BaseLayout.astro` is 1 (site-wide opt-in, single point);
    `grep -c "view-transition-name: ajs-header" src/components/SiteHeader.astro` is 1 (reused name added to the universal header);
    `grep -c "view-transition-name: hero-photo" src/components/DetailHero.astro` is 1 and `grep -c "@media (min-width: 768px)" src/components/DetailHero.astro` is >= 1 (name lives inside a desktop-only gate);
    when the Sanity `.env` is available: `npm run test:e2e -- tests/e2e/gallery.spec.ts` passes, including the desktop `hero-photo`/`ajs-header` assertions and the mobile `none` assertion.
  </verify>
  <done>BaseLayout emits a single site-wide `@view-transition { navigation: auto }`; SiteHeader gives `.site-header` the reused `ajs-header` name; DetailHero gives `.detail-hero__img` the `hero-photo` name ONLY inside a new `@media (min-width: 768px)` block (computed `none` on mobile, `hero-photo` on desktop — verified by e2e); HomeCarousel's existing `ajs-header` rule is untouched; `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Homepage click-time name assignment — morph the clicked carousel title's / grid tile's photo</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <action>
Wire the SOURCE side of the morph on the homepage, mirroring the file's EXISTING dynamic-naming pattern (the in-page mode-toggle morph at lines ~700-732, which assigns `viewTransitionName` on click and leaves it in place). Only the click-time name assignment touches JS; it is harmless on browsers without cross-document support (a plain style write, no preventDefault, no manual navigation — every homepage->gallery link is a real `<a href>`).

Reuse the EXISTING element references already queried in this script (do not re-query): `heroImg` (line ~354, the `[data-role="hero-image"]` which IS `.home-hero__img--sharp`, the current carousel slide's sharp photo — the carousel re-srcs this single element, so it always represents the active gallery), `titleEl` (line ~357, the `[data-role="gallery-title"]` carousel title `<a>`), and `gridTileImgs` (line ~368, the array of `.home-grid__tile-img--sharp` grid-tile sharp images — this array excludes the non-link hero tile automatically, since `.home-grid__tile--hero` has no sharp img).

Add this wiring right after the existing `gridTileImgs` load-listener `forEach` (after line ~378), in the same scope where `heroImg`, `titleEl`, and `gridTileImgs` are all visible:
1. Declare a single cross-document morph tracker, mirroring the existing `namedMorphTile` pattern this file uses for its in-page morph: `let namedCrossDocPhoto: HTMLElement | null = null;` and a helper `function setCrossDocPhoto(img: HTMLElement) { if (namedCrossDocPhoto && namedCrossDocPhoto !== img) namedCrossDocPhoto.style.viewTransitionName = ''; img.style.viewTransitionName = 'hero-photo'; namedCrossDocPhoto = img; }`. This guarantees only ONE element ever carries `hero-photo` at a time — clearing any previously-named element first (important because inline names survive back/forward bfcache restores, and a user could otherwise leave a grid tile named, go back, switch to carousel or another tile, and end up with two elements sharing `hero-photo`, which is invalid and would silently skip the transition).
2. Carousel title: `if (titleEl && heroImg) titleEl.addEventListener('click', () => setCrossDocPhoto(heroImg));`. This names the current slide's sharp photo immediately before the browser's default link navigation proceeds. Do NOT call preventDefault and do NOT navigate manually.
3. Grid tiles: `gridTileImgs.forEach((img) => { const tile = img.closest('.home-grid__tile'); if (tile) tile.addEventListener('click', () => setCrossDocPhoto(img)); });`. Attaching to each tile's parent `.home-grid__tile` link and naming THAT tile's own sharp img means a click anywhere on the tile names exactly the right photo. Because `setCrossDocPhoto` clears the prior name first, only the just-clicked tile carries `hero-photo`.

This uses the SAME `hero-photo` name the destination `.detail-hero__img` carries (Task 3), and a DIFFERENT name/element from the file's existing in-page morph (which names the tile `<a>`/`.home-hero__photo` with `ajs-hero-morph`, and the accent panel with `ajs-accent-panel`) — so there is no collision with the mode-toggle transition. Do NOT touch the existing in-page morph wiring, `namedMorphTile`, `gridGalleryTiles`, or the `.home :global(.site-header)` / hero-photo-sizing CSS.

In tests/e2e/homepage.spec.ts:
4. Add a describe block proving the click-time assignment WITHOUT navigating away. To read the assignment before the real `<a>` navigation runs, add a capture-phase click listener in the test that calls `preventDefault()` (this cancels only the default navigation, not the component's own bubble-phase listener, so the name still gets set), then click and read the inline style. Test A (carousel): goto "/", inside `page.evaluate` add a capture-phase `click` listener on `.home-hero__title` that calls `e.preventDefault()`, click `.home-hero__title`, then assert `.home-hero__photo .home-hero__img--sharp` has inline `style.viewTransitionName === 'hero-photo'`. Test B (grid): goto "/", click the "Grille" toggle button, add the same capture-phase preventDefault listener to the first `a.home-grid__tile:not(.home-grid__tile--hero)`, click it, and assert THAT tile's `.home-grid__tile-img--sharp` has inline `style.viewTransitionName === 'hero-photo'`. Read the inline value with `el.evaluate((n) => (n as HTMLElement).style.viewTransitionName)` (assert the INLINE style set by the click, not the computed value).
  </action>
  <verify>
    <automated>npm run build</automated>
    Confirm the source-side wiring exists and reuses the shared name:
    `grep -c "hero-photo" src/components/HomeCarousel.astro` is >= 1 (the assigned cross-document name);
    `grep -c "namedCrossDocPhoto" src/components/HomeCarousel.astro` is >= 3 (tracker declaration + clear + reassign, proving single-at-a-time handling);
    `grep -c "ajs-hero-morph" src/components/HomeCarousel.astro` is unchanged from before (the existing in-page morph name is not disturbed);
    when the Sanity `.env` is available: `npm run test:e2e -- tests/e2e/homepage.spec.ts` passes, including the carousel-title and grid-tile click-time name assertions.
  </verify>
  <done>Clicking the carousel title assigns `hero-photo` to the current slide's `.home-hero__img--sharp`, and clicking a grid tile assigns `hero-photo` to that tile's `.home-grid__tile-img--sharp`, with a single tracker guaranteeing only one element carries the name at a time; the existing in-page mode-toggle morph (`ajs-hero-morph`/`ajs-accent-panel`/`namedMorphTile`) is untouched; `npm run build` succeeds and homepage.spec.ts proves both click-time assignments without navigating away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time Sanity fetch -> static HTML | Content is fetched at build time (published perspective) and baked into static files; nothing reaches the browser at request time. |
| static HTML -> browser (client JS islands) | `DetailHero.astro` and `HomeCarousel.astro` run dependency-free vanilla-JS islands in the browser (scroll fade, view-transition-name assignment). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-uf5-01 | Tampering (supply chain) | npm dependencies | low | accept | This plan installs NO packages — it edits existing .astro/.ts/.spec files only. No new dependency enters the tree, so the package-legitimacy gate does not apply. |
| T-uf5-02 | Denial of Service | client scroll/click listeners | low | mitigate | The scroll-hint fade uses a single `{ passive: true }` scroll listener writing one opacity value; the click listeners do a single style write and no navigation control. The cross-document photo name is desktop-gated (min-width:768px) so mobile — the environment where the HOME-06 100svh always-on-name regression was suspected — never carries the name, directly bounding the known risky code path. |
| T-uf5-03 | Information Disclosure | client islands | low | mitigate | Both islands receive only pre-computed, already-localized primitives / read already-rendered static markup; neither imports src/lib/sanity or src/lib/image, so no Sanity read token is bundled into the browser payload (existing boundary, preserved). |
</threat_model>

<verification>
- `npm run typecheck` (astro check) passes after Tasks 1-2 and `npm run build` passes after Tasks 3-4 — the always-runnable gates that need no Sanity credentials.
- When the Sanity `.env` is available (orchestrator/CI): `npm run test:e2e -- tests/e2e/gallery.spec.ts tests/e2e/homepage.spec.ts` passes, covering the hero-cover guard, the grid-still-uncropped assertion, the desktop `hero-photo`/`ajs-header` names, the mobile `none` name (mobile safety), and the click-time source-name assignments.
- Recommended non-blocking human spot-check (the cross-document morph is best judged live, per sketch 006): in Chrome/Edge 126+ on desktop, from the homepage carousel click a gallery title and confirm the photo morphs continuously into the gallery hero while the header stays put; toggle "Grille" and click a tile for the same result; confirm the gallery hero is now cropped edge-to-edge (cover), matching the homepage; confirm the bouncing scroll-down chevron appears and fades as you scroll; toggle OS "Reduce motion" and confirm no bounce and a settled hero; check a mobile width (no morph, no hint, no regressions) and confirm an édition detail page still works (hint present, no crop change, no homepage morph entry point).
</verification>

<success_criteria>
- CHANGE 1: the gallery hero renders `object-fit: cover` (edge-to-edge crop, no letterboxing) exactly like the homepage; the `objectFit` prop and `detail-hero__img--contain` modifier are fully removed from DetailHero and neither gallery twin passes `objectFit`; masonry grid, Lightbox, pickHeroIndex/heroIndex, SEO, and éditions are unchanged.
- CHANGE 2: both édition and gallery heroes show a bottom-center bouncing scroll-down chevron on desktop-with-motion that fades over the first ~150px, with the bounce disabled under prefers-reduced-motion and the hint hidden on mobile and reduced-motion desktop.
- CHANGE 3: clicking a homepage carousel title or grid tile morphs that photo into the desktop gallery hero via a native cross-document View Transition with the header persisting; the `hero-photo` name is present on the destination only at desktop widths (computed `none` on mobile) as the HOME-06 mobile-safety precaution; the header reuses the existing `ajs-header` name (not a new one); éditions have no morph entry point and are untouched by Change 3.
- All edited files compile (`npm run build`), and e2e coverage (gallery + homepage specs) passes where Sanity credentials are available.
</success_criteria>

<output>
Create `.planning/quick/260724-uf5-three-related-changes-to-the-gallery-det/260724-uf5-SUMMARY.md` when done.
</output>
