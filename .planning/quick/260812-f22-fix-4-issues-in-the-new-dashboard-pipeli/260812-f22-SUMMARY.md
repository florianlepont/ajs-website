---
phase: quick-260812-f22
plan: 1
subsystem: ui
tags: [sanity-studio, react, css, editorial-dashboard, deployment-pipeline]

requires:
  - phase: quick-260812-dvq
    provides: the two-node approval-gate pipeline (releasePipelineState, pipelineDisplaySegments, resolvePromoteRow) this task patches
provides:
  - Widened pipeline node column with a reserved two-line label height, fixing the label/detail-line collision
  - Split not-started vs genuine-in-flight-wait copy in resolvePromoteRow()
  - Leaner "Mettre le site à jour" panel with the redundant per-document list removed
  - Dashboard header with only its title, subtitle, and three action controls (status badge removed)
affects: [editorial-dashboard, sanity-studio-ui]

tech-stack:
  added: []
  patterns:
    - "CSS guard tests assert on rule blocks extracted from stylesheet source text (no CSS import in root Vitest project)"

key-files:
  created: []
  modified:
    - sanity/editorial/EditorialDashboard.css
    - sanity/editorial/EditorialDashboard.tsx
    - sanity/editorial/deployment.ts
    - tests/unit/editorial-dashboard-css.test.ts
    - tests/unit/deployment.test.ts

key-decisions:
  - "Widened .editorial-dashboard__pipeline-node from 104px to 152px (116px under 36em) rather than shrinking the label font, keeping the one-shared-class balance guarantee between both nodes"
  - "Split resolvePromoteRow()'s single content-or-staging-not-done branch into two ifs in the same chain position, rather than restructuring the branch order, to keep the diff minimal and provably scoped to one place"
  - "Removed the header DeploymentStatus badge and its exclusive deploymentDotColors/DeploymentState/deployment-date CSS entirely rather than hiding it, since the panel pipeline now fully supersedes what it communicated"

requirements-completed: [QUICK-260812-f22]

coverage:
  - id: D1
    description: "Pipeline node label no longer collides with its detail line at desktop and narrow widths, with both nodes still balanced"
    requirement: QUICK-260812-f22
    verification:
      - kind: unit
        ref: "tests/unit/editorial-dashboard-css.test.ts#widens the pipeline node column and its narrow-viewport override so the label has room to breathe"
        status: pass
      - kind: unit
        ref: "tests/unit/editorial-dashboard-css.test.ts#reserves a two-line box on the pipeline node label so both nodes keep their detail line on the same baseline"
        status: pass
    human_judgment: true
    rationale: "Visual wrapping/collision behavior at multiple viewport widths on the real deployed Studio can only be confirmed by a human looking at the rendered page; the unit tests lock the underlying CSS values but not the rendered visual outcome."
  - id: D2
    description: "Pipeline message distinguishes not-started from waiting-on-site-de-test, never claiming the latter when it means the former"
    requirement: QUICK-260812-f22
    verification:
      - kind: unit
        ref: "tests/unit/deployment.test.ts#with unpublished drafts pending, tells the maintainer nothing has started and points at « Mettre le site à jour »"
        status: pass
      - kind: unit
        ref: "tests/unit/deployment.test.ts#with no production release ever recorded, keeps production pending and the row dimmed/disabled/titled \"En attente du site de test…\""
        status: pass
    human_judgment: false
  - id: D3
    description: "Publish panel no longer renders the per-document list; count summary, confirmation dialog, and blocked/success/error blocks all still work"
    requirement: QUICK-260812-f22
    verification:
      - kind: other
        ref: "grep -c publicationCategoryLabels sanity/editorial/EditorialDashboard.tsx (0); grep -c publicationCard.pairs sanity/editorial/EditorialDashboard.tsx (0)"
        status: pass
    human_judgment: true
    rationale: "The confirmation dialog's click-through behavior after removal needs a human to click 'Mettre le site à jour' on the live Studio and confirm the dialog still opens/reads correctly, per the plan's human-check."
  - id: D4
    description: "Dashboard header carries only its title, subtitle and three action controls; no orphan code/CSS survives the badge removal"
    requirement: QUICK-260812-f22
    verification:
      - kind: other
        ref: "grep -rc DeploymentStatus sanity/editorial/EditorialDashboard.tsx (0); grep -rIl deploymentDotColors ... (no matches); grep -c editorial-dashboard__deployment-status ...tsx (1)"
        status: pass
      - kind: unit
        ref: "tests/unit/editorial-dashboard-css.test.ts (removedFragments includes 'deployment-date')"
        status: pass
    human_judgment: true
    rationale: "Final visual confirmation that the header reads correctly on the live re-deployed Studio requires a human (no authenticated browser session in this environment)."

duration: 20min
completed: 2026-08-12
status: complete
---

# Quick Task 260812-f22: Fix 4 issues in the new dashboard pipeline UX Summary

**Widened the pipeline node column with a reserved two-line label height, split the misleading "waiting" copy into a genuine not-started vs. in-flight-wait branch, removed the redundant per-document list from the publish panel, and removed the now-superseded header deployment status badge.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-08-12T10:57Z (first task commit)
- **Completed:** 2026-08-12T11:04Z (Studio redeployed)
- **Tasks:** 5 (4 code tasks + 1 gate/deploy task)
- **Files modified:** 5

## Accomplishments
- `.editorial-dashboard__pipeline-node` widened from 104px to 152px (116px under `@media (max-width: 36em)`), and `.editorial-dashboard__pipeline-node-label` now reserves a 28px (two-line) `min-height` with `text-wrap: balance`, so "Contenu + site de test" no longer collides with the detail line beneath it at any width, and both nodes stay balanced through the one shared class.
- `resolvePromoteRow()`'s single `segments.content !== 'done' || segments.staging !== 'done'` branch is now two ordered `if`s: a not-started branch ("Rien n'a encore été lancé." pointing at "Mettre le site à jour") and the unchanged genuine-wait branch ("En attente du site de test…").
- The publish panel no longer renders the persistent per-document list (title + Modifié/Nouveau badge); the count summary, confirmation dialog, and blocked/success/error blocks are unchanged. `publicationCategoryLabels` and the unused `PublicationCategory` type import are gone; `publicationPanelHasBody` no longer counts the deleted list's presence.
- The header's `DeploymentStatus` dot+label component, its `deploymentDotColors` map, the `DeploymentState` type import, and the `.editorial-dashboard__deployment-date` CSS rules (base + narrow override) are all removed. `.editorial-dashboard__deployment-status` (used by the pipeline's GitHub-logs link) and `deploymentState()`/`deploymentSubtitle()`/`releasePipelineState()` are untouched and still power the header subtitle and panel pipeline.
- All four blocking gates pass: `npm run typecheck` (0 errors, 1 pre-existing unrelated hint), `npm --prefix sanity run lint` (clean), `npm --prefix sanity run build` (Studio bundles), `npm run test:unit` (435/435 passing across 19 files).
- Scope proven: `git diff --stat sanity/editorial/dashboardLogic.ts .github/workflows/` is empty, and the only hunk in `sanity/editorial/deployment.ts` is inside `resolvePromoteRow()`.
- Studio redeployed successfully to https://atelier-jacqueline-suzanne.sanity.studio/.

## Task Commits

Each task was committed atomically:

1. **Task 1: Widen the pipeline node and give the label room to breathe** - `147cf5b` (fix)
2. **Task 2: Split the misleading initial-state copy in resolvePromoteRow()** - `c30e657` (fix)
3. **Task 3: Remove the redundant per-document list from the publish panel** - `e4e67c1` (fix)
4. **Task 4: Remove the header deployment status badge and everything that fed only it** - `6a29af3` (fix)
5. **Task 5: Run every gate, prove scope, redeploy the Studio, confirm visually** - no code changes; verification + `npm run deploy --prefix sanity` (see below)

**Plan metadata:** committed separately by the orchestrator (docs commit not made by this executor per constraints).

## Files Created/Modified
- `sanity/editorial/EditorialDashboard.css` - widened pipeline node width (base + narrow override), reserved label min-height, removed deployment-date rules, rewrote the pipeline-accent comment
- `sanity/editorial/EditorialDashboard.tsx` - split resolvePromoteRow copy is in deployment.ts (not this file); removed the per-document list block, `publicationCategoryLabels`, `PublicationCategory` import, `DeploymentStatus` component, `deploymentDotColors`, `DeploymentState` import, and the header's `<DeploymentStatus>` element
- `sanity/editorial/deployment.ts` - split the single not-done branch in `resolvePromoteRow()` into a not-started branch and the unchanged waiting-on-staging branch
- `tests/unit/editorial-dashboard-css.test.ts` - added two tests locking in the widened node widths and the reserved label min-height; added `'deployment-date'` to the removed-fragments guard
- `tests/unit/deployment.test.ts` - added one test covering the not-started case (`pendingCount: 1`)

## Decisions Made
- Kept the widened base/narrow pipeline-node widths within the plan's specified numeric bounds (152px / 116px) and verified against the plan's own narrow-viewport budget arithmetic (317px inner width comfortably fits 232+32+8+12=284px).
- Kept the two `resolvePromoteRow()` branches in the exact chain position specified (after requestError/production/staging-failed, before the final ready return), preserving the "hoisting it earlier would hide a real production failure" invariant.
- Left `sanity/README.md` untouched per the plan's explicit instruction, noting for the user (not fixing here) that its "lien du statut" sentence about reaching GitHub Actions from the header badge now only holds in the pipeline states that surface an action link (staging failure, production failure, unknown) — a possible follow-up doc pass.

## Deviations from Plan

None - plan executed exactly as written. `npm ci --prefix sanity` was run before the first Studio lint attempt because `sanity/node_modules` was missing in this fresh worktree — the plan explicitly anticipated and permitted this ("If `node_modules` is missing in either project, run `npm ci` / `npm ci --prefix sanity` first — that is lockfile installation, not a new dependency"), so this is not tracked as a deviation.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Human Verification Pending

The plan's Task 5 includes a `<human-check>` block that only the user can complete (no authenticated browser session in this environment). The Studio has been redeployed to https://atelier-jacqueline-suzanne.sanity.studio/ with all four fixes live. The user should hard-refresh and confirm:

1. **Wrapping** — the left node's label "Contenu + site de test" no longer collides with the grey detail line beneath it, both nodes' detail lines share a baseline, and the layout stays symmetrical at narrow widths too.
2. **Copy** — with at least one unpublished draft, the pipeline message reads "Rien n'a encore été lancé." and points at "Mettre le site à jour"; after publishing, while GitHub rebuilds, it switches to "En attente du site de test…".
3. **List** — the panel no longer shows the standing per-document list; the one-line count summary remains, and clicking "Mettre le site à jour" still opens the confirmation dialog correctly.
4. **Header** — the status dot/label are gone from the top-right; "Ouvrir le site", "Nouvelle collection", "Nouvelle édition" remain, and the subtitle under "Tableau de bord" still mentions site status.

This is recorded as pending per the executor's operating constraints (the deploy step was run; the visual sign-off itself requires the user).

## Next Phase Readiness
- All four reported UX issues are fixed, tested, and deployed to the live Studio.
- No blockers. Pending item is purely the human visual sign-off described above — no further code work is anticipated unless the user reports something off.

---
*Phase: quick-260812-f22*
*Completed: 2026-08-12*

## Self-Check: PASSED

All five key files (`EditorialDashboard.css`, `EditorialDashboard.tsx`, `deployment.ts`, `editorial-dashboard-css.test.ts`, `deployment.test.ts`) and this SUMMARY.md exist on disk. All four task commits (`147cf5b`, `c30e657`, `e4e67c1`, `6a29af3`) are present in `git log`.
