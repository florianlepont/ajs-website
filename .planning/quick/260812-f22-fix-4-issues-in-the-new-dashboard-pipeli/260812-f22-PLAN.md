---
phase: quick-260812-f22
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/EditorialDashboard.css
  - sanity/editorial/EditorialDashboard.tsx
  - sanity/editorial/deployment.ts
  - tests/unit/editorial-dashboard-css.test.ts
  - tests/unit/deployment.test.ts
autonomous: true
requirements:
  - QUICK-260812-f22
must_haves:
  truths:
    - "The « Contenu + site de test » node label no longer collides with the detail line under it: the node column is wide enough that the label fits comfortably, and the label reserves a fixed two-line height so both nodes' detail lines sit on the same baseline whether the label wraps or not."
    - "Both pipeline nodes stay visually balanced — they are still styled by one shared `.editorial-dashboard__pipeline-node` rule, so widening one widens the other by construction."
    - "When there are unpublished drafts (`segments.content !== 'done'`), the message under the pipeline says nothing has started yet and points at the « Mettre le site à jour » button above. It no longer claims something is being waited on."
    - "When content IS published but the site de test is not yet green (`segments.content === 'done' && segments.staging !== 'done'`), the message is still « En attente du site de test… » with its existing detail — that branch is a genuine in-flight wait and is unchanged."
    - "`resolvePromoteRow()`'s other branches (requestError, production active/failed/done, staging failed, the final ready return) and the whole segment-kind computation (`deploymentSegmentKind`, `releasePipelineState`, `pipelineDisplaySegments`, `isProductionReleaseStale`) are byte-identical to before."
    - "The « Mettre le site à jour » panel no longer renders the persistent per-document list (title + « Modifié » / « Nouveau » badge) in its resting state — that information already lives in « À faire maintenant » and « Activité récente » lower on the page. The single-line count summary (« N contenus modifiés depuis la dernière mise en ligne. ») stays."
    - "The confirmation dialog shown after clicking « Mettre le site à jour » is untouched, and `dashboardLogic.ts` (including `publicationCardState`'s `pairs`) is untouched."
    - "The dashboard header no longer renders the standalone deployment status dot + label next to « Ouvrir le site » / « Nouvelle collection » / « Nouvelle édition ». Those three controls and the header subtitle line remain."
    - "Everything that fed only that header badge is gone with it — no orphan component, no orphan colour map, no orphan type import, no orphan CSS rule. `deploymentState()` and `releasePipelineState()` in deployment.ts still exist and still power the panel pipeline and the header subtitle."
    - "`npm run test:unit`, `npm run typecheck`, `npm --prefix sanity run lint` and `npm --prefix sanity run build` all pass."
    - "`git diff --stat` proves `sanity/editorial/dashboardLogic.ts` and `.github/workflows/` were never touched."
    - "The rebuilt Studio is deployed and the user has visually re-confirmed the four fixes on the live Studio."
  artifacts:
    - sanity/editorial/EditorialDashboard.css
    - sanity/editorial/EditorialDashboard.tsx
    - sanity/editorial/deployment.ts
    - tests/unit/editorial-dashboard-css.test.ts
    - tests/unit/deployment.test.ts
  key_links:
    - "`.editorial-dashboard__pipeline-node` is ONE shared rule consumed by both node columns in `EditorialDashboard.tsx`. Changing its `width` changes both nodes at once — that is exactly why the two nodes cannot drift out of balance, and why the narrow-viewport override inside `@media (max-width: 36em)` must be widened in the same proportion rather than left at its old value."
    - "`resolvePromoteRow()`'s branch order is load-bearing: the new `segments.content !== 'done'` sub-case must sit INSIDE the existing `content !== 'done' || staging !== 'done'` position in the chain (after the requestError / production / staging-failed branches, before the final ready return). Hoisting it earlier would hide a real production failure behind a « rien n'a démarré » message."
    - "`pipeline.promote.buttonDisabled` and `pipeline.promote.dimmed` drive the gate `<button disabled>` and the dimmed detail block. Both new sub-cases keep `buttonDisabled: true` / `dimmed: true` — only `title` and `detail` differ, so the gate's lock behaviour is provably unchanged."
    - "`publicationCard.pairs.length > 0` currently appears in TWO places in `EditorialDashboard.tsx`: the list block being deleted, AND the `publicationPanelHasBody` expression that decides whether the `.editorial-dashboard__publish-divider` renders. Deleting the list without dropping that term from `publicationPanelHasBody` leaves a divider line with nothing under it."
    - "The CSS class `editorial-dashboard__deployment-status` is used by BOTH the header badge (being removed) AND the GitHub-logs link under the pipeline detail (staying). Its rules must survive. Only `editorial-dashboard__deployment-date`, which the removed component alone used, becomes dead."
---

<objective>
Fix the four issues the user found on the live Studio after quick task 260812-dvq shipped the new two-node approval-gate pipeline: (1) the « Contenu + site de test » label wraps and collides with the line under it, (2) the message under the pipeline says the site is being waited on even when nothing has been launched yet, (3) the publish panel repeats a per-document list that already exists twice lower on the page, (4) the header still carries a separate deployment status badge that the pipeline now supersedes.

Purpose: all four are the same class of defect — the redesign communicates its state well, and the surrounding chrome now either fights it (badge, list) or contradicts it (copy) or crowds it (wrapping). These are decided fixes with no design question left open.

Output: a wider, better-spaced pipeline node; a split copy branch in `resolvePromoteRow()`; a leaner publish panel; a header with only its three action controls; updated guard tests; green gates; a redeployed Studio; a human visual confirmation.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@sanity/editorial/EditorialDashboard.css
@sanity/editorial/EditorialDashboard.tsx
@sanity/editorial/deployment.ts
@tests/unit/editorial-dashboard-css.test.ts
</context>

<scope_boundary>
Do NOT modify:

- `sanity/editorial/dashboardLogic.ts` — in particular `publicationCardState()` (its `pairs` field stays and is still asserted on in `tests/unit/dashboard-logic.test.ts`), `buildProductionReleaseMarkerActions()`, `triggerProductionRelease()`, the publication controller.
- `.github/workflows/` — anything.
- In `deployment.ts`: `deploymentSegmentKind()`, `releasePipelineState()`, `pipelineDisplaySegments()`, `isProductionReleaseStale()`, `deploymentState()`, `deploymentSubtitle()`, and every branch of `resolvePromoteRow()` other than the single one named in Task 2. The pipeline's pending/active/done/failed computation is not being touched.
- The confirmation dialog block in `EditorialDashboard.tsx` (the `publicationCard.dialogOpen ? <Card tone="caution"> … : <Button …>` ternary). It stays exactly as-is.
- `sanity/README.md` — the status labels it documents all still exist (via the header subtitle and the pipeline detail block), so it needs no edit in this task.

Two removals in this plan delete UI. Neither may take a state machine with it: `deploymentState()` and `releasePipelineState()` in `deployment.ts` stay, because the header subtitle (`deploymentSubtitle(currentDeploymentState)`) and the whole panel pipeline still read from them. Only presentational pieces and their exclusive call sites go.
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Widen the pipeline node and give the label room to breathe</name>
  <files>sanity/editorial/EditorialDashboard.css, tests/unit/editorial-dashboard-css.test.ts</files>
  <action>
In `sanity/editorial/EditorialDashboard.css`:

1. `.editorial-dashboard__pipeline-node` (the base rule, currently `width: 104px`): raise the width to `152px`. Both node columns render with this one class, so this widens node 1 and node 2 together — that is the balance guarantee, do not add a per-node width.

2. `.editorial-dashboard__pipeline-node-label` (currently `font-size: 11px; font-weight: 700; line-height: 14px; margin-top: 6px`): keep the type scale, and add
   - `min-height: 28px` — two lines at the declared 14px line-height. This reserves the same vertical box on both nodes, so « Site en ligne » (one line) and a wrapped « Contenu + site de test » both leave their detail line on the same baseline. This is what stops the collision, independently of whether the wider column happens to fit the label on one line in a given font.
   - `text-wrap: balance` — if it still wraps at some viewport, it wraps evenly instead of orphaning one word.
   - raise `margin-top` from `6px` to `8px`.

3. `.editorial-dashboard__pipeline-node-detail`: raise `margin-top` from `2px` to `4px`.

4. Inside `@media (max-width: 36em)`, the override `.editorial-dashboard__pipeline-node { width: 84px; }` must be widened in the same proportion, to `116px`. Sanity check the narrow budget before committing: at 375px viewport the panel's inner width is roughly 317px, and 2 nodes (232) + gate (32) + gate margins (8) + two links at their 6px minimum (12) = 284px, which fits. Do not exceed 116px here.

Then, in `tests/unit/editorial-dashboard-css.test.ts`, add one `it(...)` to the existing describe block locking this in. Note that `extractRuleBlocks(source, '.editorial-dashboard__pipeline-node')` returns TWO blocks (the base rule, then the narrow-viewport override) and does NOT match `-node-label` or `-node-detail`, because its generated regex requires optional whitespace then `{` directly after the selector. So destructure both, and assert:
  - the first (base) block declares a `width` in px whose numeric value is at least 140 — parse it with a capture group rather than string-matching one exact value, so a later tweak of a few px does not fail the guard;
  - the second (narrow) block exists and declares a smaller px width than the base — proving the mobile override was not left behind at its old value;
  - the `.editorial-dashboard__pipeline-node-label` block declares a `min-height`, with a message explaining that the reserved two-line box is what keeps the two nodes' detail lines aligned and off the label.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-css.test.ts</automated>
    <automated>grep -c 'width: 104px' sanity/editorial/EditorialDashboard.css   # expect 0</automated>
    <automated>grep -c 'width: 84px' sanity/editorial/EditorialDashboard.css   # expect 0</automated>
  </verify>
  <done>The pipeline node column is 152px (116px under 36em), the label reserves a two-line box with more space above and below it, and the CSS guard test asserts both the widened base width and the widened narrow override.</done>
</task>

<task type="auto">
  <name>Task 2: Split the misleading initial-state copy in resolvePromoteRow()</name>
  <files>sanity/editorial/deployment.ts, tests/unit/deployment.test.ts</files>
  <action>
In `sanity/editorial/deployment.ts`, `resolvePromoteRow()` currently ends its chain with one branch covering two very different situations:

    if (segments.content !== 'done' || segments.staging !== 'done') { … 'En attente du site de test…' … }

Replace that single `if` with two consecutive `if`s, in this order and in this exact position in the chain (still after the requestError / production-active / production-failed / production-done / staging-failed branches, still before the final ready `return`):

1. `if (segments.content !== 'done')` — there are unpublished drafts and the maintainer has not pressed the panel's own button yet. Nothing is running, so the copy must not describe a wait. Return:
   - `title: 'Rien n’a encore été lancé.'`
   - `detail: 'Commencez par « Mettre le site à jour » ci-dessus : vos modifications seront publiées et le site de test reconstruit.'`
   - `buttonLabel: 'Publier sur le site en ligne'`, `buttonDisabled: true`, `dimmed: true` — identical to the branch being split, so the gate's lock behaviour is provably unchanged.

2. `if (segments.staging !== 'done')` — content is published and the site de test really is being rebuilt or has not been proven fresh yet. This IS a genuine in-flight wait, so keep the existing object verbatim: `title: 'En attente du site de test…'`, `detail: 'L’étape 2 sera disponible une fois le site de test à jour.'`, same button label, `buttonDisabled: true`, `dimmed: true`. (Because branch 1 already returned, reaching here implies `segments.content === 'done'`; do not restate that in the condition.)

Use the file's existing typographic characters (’ « » …) — the strings above are already written with them.

Add a short comment above the pair explaining WHY they are two branches: the first is a not-started state whose only remedy is the button above; the second is a real wait on a GitHub run. Change nothing else in this file — no segment-kind logic, no other branch, no new export.

Then in `tests/unit/deployment.test.ts`, in the same describe block that holds the existing `'with no production release ever recorded, keeps production pending and the row dimmed/disabled/titled "En attente du site de test…"'` test, add ONE new test for the not-started case: build a `releasePipelineState({...})` call with `pendingCount: 1` (which is what makes `segments.content` land on `pending`), a `staging` from `deploymentState({runs: [], publishedAt, pendingCount: 1})`, `production: noProductionRelease`, `productionReleaseAt: ''`, `busy: false`. Assert `result.segments.content` is `'pending'`, `result.promote.title` is the new not-started title, `result.promote.detail` contains `Mettre le site à jour`, and that `dimmed` and `buttonDisabled` are both still `true`. Leave the existing waiting-case test untouched — its `pendingCount: 0` + in-progress staging run already exercises the second branch and must keep passing unchanged, which is the proof the split did not move the genuine-wait copy.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/deployment.test.ts</automated>
    <automated>grep -c 'Rien n’a encore été lancé' sanity/editorial/deployment.ts   # expect 1</automated>
    <automated>grep -c 'En attente du site de test' sanity/editorial/deployment.ts   # expect 1</automated>
  </verify>
  <done>`resolvePromoteRow()` returns not-started copy naming the « Mettre le site à jour » button when drafts are pending, and the unchanged « En attente du site de test… » copy only when content is published but the site de test is not yet green. Both existing and new unit tests pass; no segment-kind code changed.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the redundant per-document list from the publish panel</name>
  <files>sanity/editorial/EditorialDashboard.tsx</files>
  <action>
<!-- planner-discipline-allow: publicationCategoryLabels -->

In `sanity/editorial/EditorialDashboard.tsx`, delete the persistent modified-documents list rendered inside the « Mettre le site à jour » panel: the whole `{publicationCard.pairs.length > 0 && ( <Stack space={2}> … pairs.map(… IntentLink … Badge …) … </Stack> )}` block that sits between the publish divider and the `publicationCard.blockedRows` critical card. It duplicates « À faire maintenant » and « Activité récente » further down the page.

Keep, untouched:
- the single-line count summary in the panel head (« Aucune modification publique en attente. » / « N contenus modifiés depuis la dernière mise en ligne. ») — that is the panel's summary, not the list;
- the whole `publicationCard.dialogOpen ? … : …` confirmation ternary above it;
- the `publicationCard.blockedRows` critical card below it;
- the success / tracking-error / error blocks below that.

Two consequences to handle in the same edit, or the panel breaks:

1. `publicationPanelHasBody` (the expression gating `.editorial-dashboard__publish-divider`) still counts `publicationCard.pairs.length > 0` as body content. Drop that term, so the divider only renders when something is actually below it: blocked rows, or the success line, or the tracking-error card, or the error card.

2. The record mapping publication categories to their French badge labels, declared near the top of the file, was consumed by the deleted list alone. Remove it, and remove the now-unused `PublicationCategory` entry from the `dashboardLogic` type import block. Do NOT remove `Badge` or `IntentLink` from the imports — both are still used by `AttentionSection` / `ContentRow` / `RecentRow`. Do NOT touch `dashboardLogic.ts` itself: `publicationCardState()` keeps returning `pairs`, and `tests/unit/dashboard-logic.test.ts` keeps asserting on it.
  </action>
  <verify>
    <automated>grep -c 'publicationCategoryLabels' sanity/editorial/EditorialDashboard.tsx   # expect 0</automated>
    <automated>grep -c 'publicationCard.pairs' sanity/editorial/EditorialDashboard.tsx   # expect 0</automated>
    <automated>git diff --stat sanity/editorial/dashboardLogic.ts   # expect empty</automated>
    <automated>npm --prefix sanity run lint</automated>
  </verify>
  <done>The publish panel renders its count summary, the button-or-confirmation ternary, the pipeline, and (only when relevant) the blocked/success/error blocks — no per-document list, no orphan divider, no unused label map or type import. Studio lint passes and dashboardLogic.ts is untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Remove the header deployment status badge and everything that fed only it</name>
  <files>sanity/editorial/EditorialDashboard.tsx, sanity/editorial/EditorialDashboard.css, tests/unit/editorial-dashboard-css.test.ts</files>
  <action>
<!-- planner-discipline-allow: DeploymentStatus -->
<!-- planner-discipline-allow: deploymentDotColors -->
<!-- planner-discipline-allow: deployment-date -->

In `sanity/editorial/EditorialDashboard.tsx`:

1. Delete the `<DeploymentStatus state={currentDeploymentState} />` element from the header actions `<Flex>`. The three controls beside it (« Ouvrir le site », « Nouvelle collection », « Nouvelle édition ») stay, as does the header's `Heading` + subtitle `<Text>` stack above.

2. Delete the `DeploymentStatus` function component itself (the one rendering the coloured dot, the label, the relative date, and wrapping in an `<a>` when `state.actionLabel` is set).

3. Delete the `deploymentDotColors` record — grep first to confirm it has no other reader; it is used only inside that component (dot background, and the critical-tone text colour).

4. Remove `DeploymentState` from the `./deployment` type import block. Confirm by grep that no other annotation in the file uses it. Do NOT remove `formatActivityDate` from the `dashboardLogic` import — `RecentRow` still calls it. Do NOT remove `deploymentState` / `deploymentSubtitle` / `releasePipelineState` value imports, and do NOT delete the `currentDeploymentState` binding: it still feeds `deploymentSubtitle(currentDeploymentState)` for the header subtitle AND `releasePipelineState({staging: currentDeploymentState, …})` for the panel pipeline.

In `sanity/editorial/EditorialDashboard.css`:

5. Delete both `.editorial-dashboard__deployment-date` rules — the base one and the `@media (max-width: 36em)` override. That class had exactly one consumer, the component just deleted.

6. Do NOT delete the `.editorial-dashboard__deployment-status` rules (base, `a…:hover`, `:focus-visible`, and the narrow-viewport `align-self` override). That class is still applied to the GitHub-logs link under the pipeline detail block, which stays.

7. Rewrite the comment above `--dashboard-pipeline-done` / `--dashboard-pipeline-failed` on `.editorial-dashboard__publish-panel`: it currently justifies those two hex values by pointing at the colour map deleted in step 3 and at the header dot that no longer exists. Keep both hex values exactly as they are and restate the reason in terms of what survives — they are the pipeline's own success/failure accents, matching the emerald/red tints the dashboard's positive and critical chips already use.

In `tests/unit/editorial-dashboard-css.test.ts`:

8. Add `'deployment-date'` to the existing `removedFragments` array in the last `it(...)`, so the stylesheet is guarded against the dead rules creeping back. Do not add `'deployment-status'` there — that fragment must still be present.
  </action>
  <verify>
    <automated>grep -rc 'DeploymentStatus' sanity/editorial/EditorialDashboard.tsx   # expect 0</automated>
    <automated>grep -rIl 'deploymentDotColors' sanity/editorial/EditorialDashboard.tsx sanity/editorial/EditorialDashboard.css   # expect no matches</automated>
    <automated>grep -c 'editorial-dashboard__deployment-status' sanity/editorial/EditorialDashboard.tsx   # expect 1</automated>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-css.test.ts</automated>
    <automated>npm --prefix sanity run lint</automated>
  </verify>
  <done>The dashboard header shows only its title, subtitle and three action controls. The status component, its colour map, its type import and its exclusive CSS class are all gone, while `deploymentState()`/`releasePipelineState()` and the pipeline's own GitHub-logs link still work.</done>
</task>

<task type="auto">
  <name>Task 5: Run every gate, prove scope, redeploy the Studio, confirm visually</name>
  <files>(no file changes — verification, deploy and sign-off)</files>
  <action>
Run all four blocking gates from the repo root and record their output in the SUMMARY:

    npm run typecheck
    npm --prefix sanity run lint
    npm --prefix sanity run build
    npm run test:unit

If `node_modules` is missing in either project, run `npm ci` / `npm ci --prefix sanity` first — that is lockfile installation, not a new dependency.

Then prove the scope boundary held, and paste both results into the SUMMARY:

    git diff --stat sanity/editorial/dashboardLogic.ts .github/workflows/
    git log --name-only --format=%H --grep='260812-f22' -- sanity/editorial/dashboardLogic.ts .github/workflows/

Both must be empty.

Then redeploy the hosted Studio:

    npm run deploy --prefix sanity

This is expected and low-risk — it is the same deploy this session already ran for quick task 260812-dvq. Do not treat it as a checkpoint; run it, capture the success line and the Studio URL.

Do not edit `sanity/README.md`. One observation to record in the SUMMARY (not to fix here): with the header badge gone, the README sentence about opening « le lien du statut » to reach GitHub Actions now only holds in the pipeline states that surface an action link (staging failure, production failure, unknown). The user can decide whether that warrants a follow-up doc pass.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm --prefix sanity run build</automated>
    <automated>npm run test:unit</automated>
    <automated>git diff --stat sanity/editorial/dashboardLogic.ts .github/workflows/   # expect empty</automated>
    <human-check>
The Studio has been redeployed. Open https://atelier-jacqueline-suzanne.sanity.studio/ and hard-refresh so the new bundle loads. This environment has no authenticated browser session, so only you can confirm the visual result. Check the four fixes:

1. **Wrapping** — in the « Mettre le site à jour » panel, the left node's label « Contenu + site de test » sits comfortably in its column and no longer runs into the small grey line beneath it. Both nodes' grey detail lines are on the same baseline, and the two node columns still look symmetrical around the central gate button. Also check it at a narrow window width.
2. **Copy** — with at least one unpublished draft, the message under the pipeline reads « Rien n’a encore été lancé. » and points you at the « Mettre le site à jour » button above, instead of claiming the site de test is being waited on. After you publish, while GitHub rebuilds, it should switch back to « En attente du site de test… ».
3. **List** — the panel no longer shows the standing list of individual documents (« Page d'accueil · Modifié » etc.). The one-line count (« 1 contenu modifié depuis la dernière mise en ligne. ») is still there. Click « Mettre le site à jour » once to confirm the confirmation dialog still appears and reads correctly, then cancel.
4. **Header** — the status dot and its label (« Modifications en attente » / « Site de test à jour ») are gone from the top-right. « Ouvrir le site », « Nouvelle collection » and « Nouvelle édition » are still there, and the grey subtitle under « Tableau de bord » still mentions the site status.

Reply « approuvé » if all four look right, or describe what is off.
    </human-check>
  </verify>
  <done>All four gates pass, git proves dashboardLogic.ts and .github/workflows/ were never touched, the Studio is redeployed, and the user's visual confirmation (or their correction list) is recorded.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` — 0 errors (the one pre-existing unrelated hint may remain).
- `npm --prefix sanity run lint` — clean, including no-unused-vars on the deleted component, colour map, label record and type imports.
- `npm --prefix sanity run build` — Studio bundles.
- `npm run test:unit` — full suite green, including the updated `editorial-dashboard-css.test.ts` and the new `deployment.test.ts` case.
- `git diff --stat sanity/editorial/dashboardLogic.ts .github/workflows/` — empty.
- `git diff sanity/editorial/deployment.ts` — the only hunk is inside `resolvePromoteRow()`, at the branch named in Task 2.
</verification>

<success_criteria>
- The pipeline's left node label no longer collides with its detail line, at desktop and narrow widths, with both nodes still balanced.
- The pipeline message distinguishes "nothing started yet" from "waiting on the site de test", and never says the latter when it means the former.
- The publish panel shows a count, an action, the pipeline, and problem states only — no duplicated document list, and its confirmation dialog still works.
- The dashboard header carries only its title, subtitle and three action controls.
- No orphan code or CSS survives either removal, and no state machine was removed with them.
- Every gate is green, scope is git-proven, the Studio is redeployed, and the user has signed off visually.
</success_criteria>

<output>
Create `.planning/quick/260812-f22-fix-4-issues-in-the-new-dashboard-pipeli/260812-f22-SUMMARY.md` when done
</output>
