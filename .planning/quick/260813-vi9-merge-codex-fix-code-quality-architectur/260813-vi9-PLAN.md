---
phase: quick-260813-vi9-merge-codex-fix-code-quality-architectur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - README.md
  - .github/workflows/deploy.yml
  - sanity/editorial/EditorialDashboard.tsx
  - src/components/ContactForm.astro
  - tests/e2e/contact.spec.ts
  - tests/e2e/critical.smoke.spec.ts
  - tests/scripts/verify-static-artifact.mjs
  - tests/unit/deployment.test.ts
autonomous: true
requirements: [QUICK-260813-VI9]
---

## Objective

Merge `codex/fix-code-quality-architecture` (27 commits, the 260811-kog code-quality remediation, forked from `main` at `7332061` on 2026-08-11) into `main`, which has since diverged 142 commits (all of Phase 5's launch/domain-cutover work plus the v1.7 milestone close). A `git merge-tree` dry run found exactly 9 files with overlapping changes; everything else in codex's 27 commits merges automatically.

The user explicitly chose "merge, resolving conflicts" over cherry-pick or abandon, after being shown the dry-run conflict scope and the one real semantic conflict (Web3Forms).

<task>
<name>Merge codex/fix-code-quality-architecture into main, resolving 9 conflicts</name>
<files>.planning/STATE.md, README.md, .github/workflows/deploy.yml, sanity/editorial/EditorialDashboard.tsx, src/components/ContactForm.astro, tests/e2e/contact.spec.ts, tests/e2e/critical.smoke.spec.ts, tests/scripts/verify-static-artifact.mjs, tests/unit/deployment.test.ts</files>
<action>
1. `git merge --no-commit --no-ff codex/fix-code-quality-architecture` on `main`.
2. Resolve each conflicting file by reading BOTH sides in full first (`git show main:<path>`, `git show codex/fix-code-quality-architecture:<path>`), then:
   - `.planning/STATE.md` — take main's version entirely (living tracking file, main's copy is authoritative).
   - `README.md` — additive merge: keep both sides' documentation additions.
   - `.github/workflows/deploy.yml` — main's version as base; layer in only codex's still-relevant CI additions (e.g. root lint / Studio coverage gates) if not already superseded by main's own later CI changes.
   - `sanity/editorial/EditorialDashboard.tsx` — main's version as base; apply codex's `@sanity/icons/AlertCircle` → `ErrorOutlineIcon` fix ONLY if main's current code still has the broken import (check first).
   - `src/components/ContactForm.astro` — take main's version entirely. Main's Phase 5 work replaced Web3Forms with a working `public/contact.php` backend; codex's "unconfigured Web3Forms key fails honestly" fix addresses a problem that no longer exists in main's codebase.
   - `tests/e2e/contact.spec.ts`, `tests/e2e/critical.smoke.spec.ts` — same disposition: take main's version entirely (already tests the contact.php flow).
   - `tests/scripts/verify-static-artifact.mjs` — additive merge if both sides' assertions are independent; drop codex's hunk only if it is also Web3Forms-specific and moot.
   - `tests/unit/deployment.test.ts` — keep codex's `DIAGNOSTIC-05/06` describe block (Sanity version pin + CI gate ordering assertions — still valid), adapted to whatever final `deploy.yml` shape results from the deploy.yml resolution above; preserve any of main's own independent changes to this file.
3. Stage all resolved files.
4. Run the full verification chain before committing: `npm run typecheck`, `npx vitest run`, `npm --prefix sanity run lint`, `npm --prefix sanity run build`, and Sanity Studio's own test suite if one exists (check `sanity/package.json` scripts — codex's kog-05 added a whole React test harness for the Studio that main has never run).
5. Complete the merge commit with a message describing what was kept vs. dropped, specifically noting the Web3Forms-fix supersession.
6. Do NOT push — stop after a verified local merge commit.
</action>
<verify>npm run typecheck && npx vitest run && npm --prefix sanity run lint && npm --prefix sanity run build</verify>
<done>main contains all of codex's still-valid work (route dedup, icon fix if needed, Sanity validation boundary, Studio test harness, version pin, CI gate additions, coverage threshold raise) merged cleanly, the superseded Web3Forms fix is dropped, and every verification command passes.</done>
</task>

## Threat Model

| Threat | Disposition | Mitigation |
|---|---|---|
| Silently reverting main's working `contact.php` implementation by blindly taking codex's ContactForm.astro/test changes | Mitigate | Explicit instruction to read main's current implementation first and confirm supersession before resolving — not a blind pick-one-side |
| Losing codex's still-valid quality work (dedup, tests, coverage, version pin) by taking "main wins" on every conflict | Mitigate | Per-file disposition table above, most files are additive-merge or take-main-plus-layer-in, not blanket overwrite |
| Pushing a broken merge to origin | Mitigate | Full verification chain required before commit; explicit instruction not to push |

No packages added by this plan — Package Legitimacy Gate not applicable.
