---
phase: quick-260724-l5i
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EditionHero.astro
  - src/pages/editions/[slug].astro
  - src/pages/en/editions/[slug].astro
  - tests/e2e/edition.spec.ts
autonomous: true
requirements:
  - QUICK-260724-l5i
must_haves:
  truths:
    - "On desktop with motion allowed, scrolling an édition detail page pins the lead photo and shrinks it from full-bleed to ~55% width settling left; the title fades in large on the right, then the pink-underlined format line crystallizes right after."
    - "With prefers-reduced-motion: reduce, the hero shows the settled end-state immediately — no sticky pin, no scroll-linked motion at all."
    - "Clicking the hero photo still opens the Lightbox at image 1 of N (data-gallery-thumb + data-index=0 preserved), and the click-to-expand View Transitions morph still runs."
    - "The detail page still exposes exactly one <h1> (the édition title), the statement, the canonical .edition-detail__format line, the back-link, the EDN-08 related-link logic, and the gallery grid below the hero."
    - "No price / availability / purchase affordance is introduced (EDN-06 preserved)."
    - "Both /editions/{slug}/ and /en/editions/{slug}/ receive identical treatment with locale-correct copy."
  artifacts:
    - src/components/EditionHero.astro
    - src/pages/editions/[slug].astro
    - src/pages/en/editions/[slug].astro
    - tests/e2e/edition.spec.ts
  key_links:
    - "EditionHero's button[data-gallery-thumb][data-index=0] ↔ Lightbox.astro's document-wide [data-gallery-thumb] click binding + view-transition morph"
    - "prefers-reduced-motion CSS end-state + JS matchMedia guard (JS sets NO inline styles when reduced-motion or mobile, so CSS wins)"
    - "scroll-track height calc(100svh + revealDistance) ↔ sticky pin releasing exactly when progress reaches 1"
---

<objective>
Replace the current static 70vh full-bleed hero on the édition detail page with sketch 005's approved winning variant, "Synthesis — Bold + Facts": on page scroll the lead photo pins (position: sticky) and shrinks from full-bleed to ~55% width settling left as a framed portrait; the space that opens on the right fills with the édition title in large Unbounded display type, then the pink-underlined format-details line crystallizes right after the title — all before the normal content flow (back-link, statement, gallery grid) continues below.

Purpose: Give the édition detail page a distinctive, on-brand entrance using a mechanism already proven in the sketch, without regressing the shipped lightbox/expand/SEO/content behavior, and with a genuine prefers-reduced-motion fallback.

Output: A shared `EditionHero.astro` component (markup + scoped CSS + scroll script) rendered by both the FR and EN detail pages, plus updated e2e coverage.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Proven mechanism to port — read the Synthesis onProgress function VERBATIM (lines ~377-403) and its CSS (#stageSynth, lines ~104-108) and the reduced-motion path in setupStage() (lines ~271-296):
@.planning/sketches/005-edition-hero-scroll-reveal/index.html
@.planning/sketches/005-edition-hero-scroll-reveal/README.md

# Files to modify — read the CURRENT hero markup + CSS you are replacing, and note every attribute that must be preserved:
@src/pages/editions/[slug].astro
@src/pages/en/editions/[slug].astro

# Established motion / reduced-motion conventions to mirror (matchMedia guard, resize debounce, VTDocument typing, dependency-free vanilla-JS island pattern):
@src/components/HomeCarousel.astro
@src/components/Lightbox.astro

# The Lightbox hook contract (binds document-wide to [data-gallery-thumb]) and the click-to-expand morph your new markup must keep satisfying:
# (already read via Lightbox.astro above)

# Existing e2e coverage you must keep green + extend:
@tests/e2e/edition.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared EditionHero.astro scroll-reveal component and wire it into both detail-page twins</name>
  <files>src/components/EditionHero.astro, src/pages/editions/[slug].astro, src/pages/en/editions/[slug].astro</files>
  <action>
Create `src/components/EditionHero.astro` — a dependency-free vanilla-JS island (no client:* directive), following the exact conventions of Lightbox.astro / HomeCarousel.astro (typed frontmatter Props, scoped `<style>`, a plain typed module `<script>`). It receives already-localized, already-computed primitives as props so all locale/image logic stays in the page frontmatter (do NOT import src/lib/image or src/lib/sanity inside the component). Props: `leadPhotoSrc: string`, `leadPhotoSrcSet: string`, `leadPhotoAlt: string`, `title: string`, `formatText: string`, `total: number`, `heroAriaLabel: string`, `caption: string`.

MARKUP — reproduce the Synthesis composition, preserving every existing behavior. Root `<div class="edition-detail__hero">` is the SCROLL-TRACK. Inside it a single `<div class="edition-detail__hero-pin">` (the sticky pin). Inside the pin, two siblings:
  1. `<div class="edition-detail__hero-photo">` (the shrinking wrapper) containing, IN THIS ORDER: the existing clickable `<button type="button" class="edition-detail__hero-trigger" data-gallery-thumb data-index="0" aria-label={heroAriaLabel}>` wrapping the `<img class="edition-detail__hero-img" src={leadPhotoSrc} srcset={leadPhotoSrcSet} sizes="100vw" alt={leadPhotoAlt} loading="eager" decoding="async" />` (keep the eager/decoding/hover-scale behavior byte-for-byte); the `<div class="edition-detail__hero-scrim" aria-hidden="true">`; the transient overlay title `<p class="edition-detail__hero-overlay-title" aria-hidden="true">{title}</p>` (sibling of the button — never a child, because a `<button>` may not contain flow content like `<p>`); and the existing expand-icon `<svg class="edition-detail__hero-expand-icon" ...>` with the four polyline corners, verbatim from the current file.
  2. `<div class="edition-detail__hero-reveal" aria-hidden-parts>` — the reveal panel, `pointer-events: none`. It contains, in order: `<p class="edition-detail__hero-caption" aria-hidden="true">{caption}</p>` (decorative eyebrow); the REAL heading `<h1 class="edition-detail__hero-reveal-title">{title}</h1>`; and `<span class="edition-detail__hero-format" aria-hidden="true">{formatText}</span>` (the format ECHO).

CRITICAL naming rule: the format echo class is `edition-detail__hero-format`, deliberately DIFFERENT from `edition-detail__format`. The canonical `.edition-detail__format` line MUST remain only in `.edition-detail__content` on the page (the e2e suite locates `.edition-detail__format` in strict mode and would throw on two matches — a class token `edition-detail__hero-format` does not satisfy the `.edition-detail__format` selector, so this keeps that locator resolving to exactly one element).

EXACTLY ONE `<h1>`: the reveal title (`.edition-detail__hero-reveal-title`) is the page's sole heading and lives in the DOM at all times (only opacity/transform animate — opacity:0 keeps it in the accessibility tree, so SEO/a11y are unaffected while it is visually hidden at scroll progress 0). The overlay title is a decorative `aria-hidden` `<p>`, NOT a heading. Do not emit a second `<h1>`.

SCRIPT — port the Synthesis `onProgress(cfg, t)` math VERBATIM (sketch index.html ~lines 387-403) into a single `onProgress(t)` over the component's own refs. Keep the proven constants exactly: `revealDistance = 900`; `width = lerp(100, 55, t)` → photo wrapper `left:'0%'`, `right:(100-width)+'%'`, `top:'0%'`, `bottom:'0%'`; scrim opacity `clamp01(1 - t*3.3)`; overlay-title opacity `clamp01(1 - t*4)`; `titleT = clamp01((t-0.3)/0.3)`; `formatT = clamp01((t-0.6)/0.4)`; reveal-panel opacity `Math.max(titleT, formatT)` with transform `translateY(-50%) translateX(${lerp(24,0,titleT)}px)`; format-echo opacity `formatT` with transform `translateY(${lerp(8,0,formatT)}px)`. Define local `clamp01` and `lerp` helpers identical to the sketch.

ADAPT the driver from the sketch's bounded-div `viewport.scrollTop` to REAL PAGE SCROLL: compute progress from the track's viewport position — `progress = clamp01(-track.getBoundingClientRect().top / revealDistance)` — which is robust regardless of anything above the hero. Throttle the scroll handler with requestAnimationFrame and register it `{ passive: true }`. Call the driver once on setup with the current scroll position (handles a page that loads already scrolled, e.g. back-forward navigation).

REDUCED-MOTION + MOBILE GUARD (mirror HomeCarousel's matchMedia convention): create `const reduceMotion = matchMedia('(prefers-reduced-motion: reduce)')` and `const desktop = matchMedia('(min-width: 768px)')`. In a `setup()` function: if `reduceMotion.matches || !desktop.matches`, remove the scroll listener AND clear every inline style the driver may have set (so the CSS end-state / mobile state fully controls the layout — inline styles would otherwise beat media-query CSS); otherwise attach the rAF scroll listener and prime it once. Re-run `setup()` on `reduceMotion` 'change' and on a debounced `resize` / `desktop` 'change' (same debounce shape as HomeCarousel's resize handler), so toggling OS reduce-motion or crossing the 768px breakpoint live re-evaluates correctly. The JS must set NO inline styles at all in the reduced-motion or mobile branch.

SCOPED CSS — express the three states so first paint and no-JS are sensible and the reduced-motion fallback is genuine:
  - BASE (all widths, motion default = scroll progress 0): `.edition-detail__hero` height `calc(100svh + 900px)` (track = pin height + revealDistance, so the sticky pin releases exactly when progress hits 1), background `var(--color-ink)`, position relative — do NOT set overflow:hidden on the track (it would break sticky). `.edition-detail__hero-pin` `position: sticky; top: 0; height: 100svh; overflow: hidden; background: var(--color-ink)`. `.edition-detail__hero-photo` `position: absolute; inset: 0; overflow: hidden`. The img fills it (absolute inset:0, width/height 100%, object-fit: cover) and keeps the existing hover/focus `scale(1.02)` transition. Scrim: absolute inset:0, the existing top-to-bottom gradient, opacity 1. Overlay title: absolute bottom-left over the photo, `var(--font-display)`, `var(--text-display-size)`, `var(--weight-semibold)`, white, opacity 1, pointer-events none. Expand icon: absolute bottom-right of the photo (as today). Reveal panel: absolute, `top:50%; right: var(--space-2xl); transform: translateY(-50%); max-width: 420px; text-align:left; opacity: 0; pointer-events:none`. Caption: `var(--text-label-size)`, uppercase, `letter-spacing:.06em`, `rgba(255,255,255,0.7)`. Reveal title: `var(--font-display)`, `font-size: clamp(40px, 6vw, 72px)`, `line-height:1.02`, `var(--weight-semibold)`, white. Format echo: inline-block, `var(--text-label-size)`, `var(--weight-semibold)`, uppercase, white, `border-bottom: 2px solid var(--color-accent)`, `padding-bottom: var(--space-sm)`, opacity 0.
  - REDUCED-MOTION SETTLED END-STATE — `@media (prefers-reduced-motion: reduce) and (min-width: 768px)`: `.edition-detail__hero { height: 100svh; }`; pin `position: relative; height: 100svh;`; photo `right: 45%;` (→ 55% width, left-settled); scrim opacity 0; overlay-title opacity 0; reveal panel `opacity: 1; transform: translateY(-50%);`; format echo `opacity: 1`. This mirrors onProgress(1) purely in CSS, so the settled composition shows immediately with zero scroll-linked motion and no dependence on the driver ever running.
  - MOBILE — `@media (max-width: 767px)` (applies regardless of motion preference; no scroll-jacking on phones and a 55% photo is too small there): `.edition-detail__hero { height: auto; }`; pin `position: relative; height: 70svh; min-height: 420px;`; photo full-bleed `inset: 0; right: 0;`; scrim opacity 1; overlay title `display: none`; reveal panel repositioned static-over-photo — `top: auto; bottom: var(--space-xl); left: var(--space-xl); right: var(--space-xl); transform: none; opacity: 1;`; caption `display: none`; reveal title `font-size: var(--text-display-size)` (matches the current mobile hero title scale); format echo `display: none` (the canonical `.edition-detail__format` below is the single visible format line on mobile). Net effect: mobile looks like today's hero (full-bleed photo + big title bottom-left) but the visible title is the real `<h1>`.

WIRE BOTH PAGES: in `src/pages/editions/[slug].astro` and `src/pages/en/editions/[slug].astro`, import EditionHero and REPLACE the entire existing `<div class="edition-detail__hero"> ... </div>` block with a single `<EditionHero ... />` call, passing the already-computed frontmatter values: `leadPhotoSrc={leadPhotoSrc}`, `leadPhotoSrcSet={responsiveImageSrcSet(edition.leadPhoto)}`, `leadPhotoAlt={leadPhotoAlt}`, `title={edition.title}`, `formatText={formatText}`, `total={total}`, and the locale-correct `heroAriaLabel` (FR `` `Voir en taille réelle, image 1 sur ${total}` ``, EN `` `View full size, image 1 of ${total}` ``) and `caption` (FR `Édition imprimée`, EN `Printed edition`). Delete the now-migrated hero-specific CSS rules from each page's `<style>` block (`.edition-detail__hero*`), but KEEP everything in `.edition-detail__content` and its CSS untouched (back-link, statement, `.edition-detail__format`, related, gallery grid) and KEEP `headerVariant="transparent"` on BaseLayout. Leave the `lightboxImages`/`gridItems`/`<Lightbox ... />` wiring exactly as-is — the Lightbox still binds document-wide to the hero button's `data-gallery-thumb`/`data-index="0"`.

Note the caption ("Édition imprimée"/"Printed edition") is decorative and aria-hidden; if any real édition would be mischaracterized as printed, the executor may drop the caption prop entirely — it is not load-bearing for the reveal.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>Both détail routes build statically with the new EditionHero; `astro check` is clean. The hero button still carries data-gallery-thumb/data-index="0"/aria-label and wraps the srcset img; the page has exactly one h1; `.edition-detail__format` still exists once in `.edition-detail__content`; the format echo uses class `edition-detail__hero-format`.</done>
</task>

<task type="auto">
  <name>Task 2: Adapt and extend the édition e2e suite for the new hero markup and reduced-motion fallback</name>
  <files>tests/e2e/edition.spec.ts</files>
  <action>
Update `tests/e2e/edition.spec.ts` so the existing hero-related assertions pass against the new markup, and add reduced-motion coverage. Discover the détail URL dynamically from the overview's first `.tile` href exactly as the existing tests do — never hardcode a slug.

1. Confirm the existing "editions lightbox" test still holds: the hero trigger `[data-gallery-thumb][data-index="0"]` is visible at load, clicking it opens `dialog[open]` with counter `1 / N`, Escape returns focus to the trigger, and its `img` has a `\d+w` srcset. Adjust only if a selector moved; the trigger contract is unchanged, so it should pass as-is — run it to confirm, do not rewrite it speculatively.

2. Confirm the "editions detail" test still resolves `.edition-detail__format` to a single element (visible, matches `/\d/`, `/Tirage/i` FR & `/Print run/i` EN, `/cm|in/i`). Because the hero echo uses `edition-detail__hero-format` (a different class token), this locator must still match exactly one node — if strict-mode fails here, the echo class was named wrong; fix the component, not the test.

3. Confirm both "no commerce affordances" tests (overview + detail) still pass — `main` innerText must contain none of the forbidden tokens. The new caption/title/format-echo text introduces no price/availability/purchase wording, so these should stay green.

4. ADD a new test (a new `test.describe('editions hero reduced-motion (sketch 005)')` block): call `await page.emulateMedia({ reducedMotion: 'reduce' })` BEFORE navigating, discover and open a détail URL, then assert the genuine fallback: the pin element (`.edition-detail__hero-pin`) has computed `position` that is NOT `'sticky'` (evaluate `getComputedStyle(el).position`); the reveal title `h1.edition-detail__hero-reveal-title` is visible immediately without any scrolling; the transient overlay title is not shown in the settled state (its computed opacity is `'0'` or it is `display:none`); and the hero trigger still opens the lightbox at `1 / N` (reduced-motion must not break the click-to-open behavior). Add a companion assertion that WITHOUT reduced-motion (default) on a desktop viewport the pin's computed `position` IS `'sticky'`, proving the scroll-linked path is active by default. Use a desktop viewport (`test.use({ viewport: { width: 1280, height: 900 } })`) for the pin-position assertions so the `min-width: 768px` branch applies.
  </action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/edition.spec.ts && npm run test:unit</automated>
  </verify>
  <done>The full édition e2e file passes, including the unchanged lightbox/format/commerce assertions against the new markup and the new reduced-motion settled-state test; unit tests remain green. No `.edition-detail__format` strict-mode violation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time Sanity → static HTML | Unchanged — the component receives already-fetched, already-computed props from the page frontmatter and never imports src/lib/sanity or src/lib/image, so no read token reaches the browser (mirrors Lightbox/HomeCarousel). |
| user scroll/click → client JS | The scroll driver only reads `getBoundingClientRect`/scroll position and writes inline styles on its own elements; no user-controlled data crosses into the DOM as markup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-l5i-01 | Information Disclosure | EditionHero client script | low | accept | Component takes only presentational primitives as props; never imports the Sanity client/token path — no new secret exposure surface vs the existing hero. |
| T-l5i-02 | Tampering | npm/pip/cargo installs | n/a | accept | No dependencies added — pure Astro component + vanilla JS reusing existing tokens/APIs. No package-legitimacy gate required. |
</threat_model>

<verification>
- `npm run typecheck` clean; `npm run build` produces both `/editions/{slug}/` and `/en/editions/{slug}/`.
- `npm run test:e2e -- tests/e2e/edition.spec.ts` green (lightbox 1/N, format single-locator, commerce-token guard, new reduced-motion settled-state test).
- `npm run test:unit` green.
- Optional non-blocking human check (recommended before merge): `npm run dev`, open an édition détail page on a desktop width and scroll — confirm the photo pins and shrinks left, the big title fades in on the right, then the pink-underlined format line appears; click the photo to confirm the lightbox still opens at 1/N; then enable OS "Reduce motion" (or DevTools "Emulate CSS prefers-reduced-motion: reduce") and reload — confirm the settled composition shows immediately with no pin and no scroll-linked motion. Repeat spot-check on the EN twin and at a mobile width (full-bleed photo, title legible, single format line below).
</verification>

<success_criteria>
- The Synthesis scroll reveal (proven revealDistance=900 and interpolation constants) drives the desktop détail hero on real page scroll for both locales.
- prefers-reduced-motion: reduce yields the settled end-state with no sticky pin and no scroll-linked motion (CSS-driven, JS sets no inline styles in that branch).
- Lightbox open-at-index-0, expand icon, SEO title/description, and all `.edition-detail__content` items (back-link, statement, canonical `.edition-detail__format`, related-link, gallery grid) are unchanged.
- Exactly one `<h1>`; no new commerce affordance; EN and FR treated identically with localized copy.
- e2e + unit suites pass, including the new reduced-motion test; `.edition-detail__format` resolves to exactly one element.
</success_criteria>

<output>
Create `.planning/quick/260724-l5i-build-sketch-005-s-winning-variant-synth/260724-l5i-SUMMARY.md` when done.
</output>
