---
phase: quick-260811-w8d
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/schemas/siteProductionRelease.ts
  - sanity/schemas/index.ts
  - sanity/schemas/structure.ts
  - sanity/editorial/workflowLogic.ts
  - sanity/editorial/dashboardLogic.ts
  - sanity/editorial/deployment.ts
  - sanity/editorial/EditorialDashboard.tsx
  - sanity/editorial/EditorialDashboard.css
  - .github/workflows/deploy-ovh.yml
  - tests/unit/workflow-logic.test.ts
  - tests/unit/dashboard-logic.test.ts
  - tests/unit/deployment.test.ts
  - tests/unit/deploy-ovh-workflow.test.ts
  - README.md
  - sanity/README.md
  - .planning/phases/05-launch-domain-cutover/05-CONTEXT.md
autonomous: true
requirements:
  - QUICK-260811-w8d
user_setup:
  - service: sanity
    why: "The Studio button writes a `siteProductionRelease` marker document, but NOTHING in this repository can turn that mutation into a GitHub deploy. A Sanity Project Webhook — configured in Sanity's own dashboard, which this environment has no access to — is the bridge. Until it exists, the new button will publish the marker successfully and then nothing will happen: no production run will ever start."
    dashboard_config:
      - task: "Create a second Project Webhook named e.g. `production-deploy-requested`. Trigger on Create + Update, dataset `production`, filter `_type == \"siteProductionRelease\"`, HTTP method POST, URL `https://api.github.com/repos/florianlepont/ajs-website/dispatches`, body (projection) producing `{\"event_type\": \"production-deploy-requested\"}`. Headers: `Accept: application/vnd.github+json`, `Authorization: Bearer <PAT>`, `Content-Type: application/json`."
        location: "https://www.sanity.io/manage → project `ajs-website` → API → Webhooks → Create webhook"
      - task: "Inspect the EXISTING webhook that fires `sanity-content-published` (it almost certainly filters on `_type == \"siteDeployment\"`) and confirm the new one mirrors its shape exactly except for the filter type and the `event_type` value. Do NOT edit the existing webhook — staging must keep listening to `sanity-content-published`."
        location: "Same Webhooks page — open the existing webhook and read its filter/projection/headers"
    env_vars:
      - name: GITHUB_DISPATCH_PAT
        source: "GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens. Scope it to the single repo `florianlepont/ajs-website` with `Contents: Read and write` (required for the repository_dispatch API) and an expiry. Paste it ONLY into the Sanity webhook's Authorization header field. It must never be written into this repository, into any workflow file, or into any commit."
must_haves:
  truths:
    - "Publishing content in Sanity Studio updates GitHub Pages staging automatically and NEVER starts an OVH production deploy on its own."
    - "The dashboard shows a 3-segment progress bar labelled Contenu / Staging / Production that reflects real state: content drafts pending, the live `deploy.yml` run, and the live `deploy-ovh.yml` run."
    - "Once staging is confirmed up to date, a `Mettre en production` button becomes enabled; clicking it publishes a `siteProductionRelease` marker and nothing else — no content document is republished by that click."
    - "While a production deploy is running the button is disabled and reads as in-progress; when it succeeds the row reads `Production à jour`; when either stage fails the corresponding segment turns red and the row states WHICH stage failed."
    - "The dashboard's existing status label reads `Staging à jour` (never the ambiguous `Site à jour`), and the production stage reads `Production à jour` — the two are never confusable."
    - "`.github/workflows/deploy-ovh.yml` listens only to `production-deploy-requested`; `.github/workflows/deploy.yml` still listens to `sanity-content-published`, untouched."
    - "`siteProductionRelease` never appears in Studio's create-new-document menu, omnisearch templates, or the desk content tree."
    - "No GitHub token or secret value appears in any committed file."
    - "D-01's original text and its 2026-08-11 supersession sub-bullet in 05-CONTEXT.md are both preserved verbatim, with the refinement recorded as a further dated note beneath them."
  artifacts:
    - sanity/schemas/siteProductionRelease.ts
    - sanity/editorial/deployment.ts
    - sanity/editorial/dashboardLogic.ts
    - sanity/editorial/EditorialDashboard.tsx
    - sanity/editorial/EditorialDashboard.css
    - .github/workflows/deploy-ovh.yml
    - tests/unit/deployment.test.ts
    - tests/unit/dashboard-logic.test.ts
    - tests/unit/deploy-ovh-workflow.test.ts
    - README.md
  key_links:
    - "`Mettre en production` click → `triggerProductionRelease()` → Sanity Actions API publishes `siteProductionRelease` → (human-configured) Sanity webhook → `repository_dispatch: production-deploy-requested` → deploy-ovh.yml. Every hop except the webhook lives in this repo; the webhook hop is `user_setup` and is the single point of failure."
    - "`buildProductionReleaseMarkerActions` takes NO content-publish actions parameter — the type signature is what structurally guarantees a production release can never be bundled into the content-publish batch."
    - "`siteProductionRelease` registered in THREE places or it leaks into the UI: `schemas/index.ts` (schema exists), `workflowLogic.ts` INTERNAL_SYSTEM_DOCUMENT_TYPES (hidden from create-new + document actions stripped), `structure.ts` exclusion list (hidden from the desk tree)."
    - "`siteProductionRelease.lastTriggeredAt` (persisted) ↔ the dashboard's production polling reference timestamp — this is what makes the pipeline state survive a page reload or a second browser tab, rather than living only in React state."
    - "The staleness comparison `publishedAt > productionReleaseAt` ↔ the button re-enabling after each NEW content publish; without it the row would stay stuck on `Production à jour` forever."
---

<objective>
Replace the raw auto-deploy-on-every-publish mechanism with a human-checkpoint-gated one, driven by a `Mettre en production` button in Sanity Studio's editorial dashboard, implementing sketch 016 Variant C.

Quick task 260811-v3t made a Sanity content publish deploy straight to atelierjacquelinesuzanne.fr with no approval, on the reasoning that Romane has no GitHub access and therefore no way to approve anything. That solved the access problem by deleting the checkpoint entirely — every publish, including an accidental one, immediately reaches the real domain. This task puts the checkpoint back where she can actually reach it: in the Studio she already uses. She publishes → staging rebuilds automatically → she looks at staging → she clicks `Mettre en production`. Her click IS the approval, so no GitHub reviewer gate is needed on that path.

Purpose: give the non-technical maintainer a real, self-serve verify-then-ship loop instead of a hair-trigger, without ever asking her to open GitHub.

Output: a new internal `siteProductionRelease` marker document + the Actions-API function that touches it; a workflow-parameterized `deployment.ts` that can track staging or production runs and stops saying the ambiguous `Site à jour`; a pure `releasePipelineState` state machine; the sketch-016 Variant C UI (3-segment bar + one dynamic action row) in the dashboard; `deploy-ovh.yml` re-pointed at a new `production-deploy-requested` event; and corrected docs including the human-only Sanity webhook setup.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@.planning/sketches/016-dashboard-deploy-stepper/README.md
@.planning/sketches/016-dashboard-deploy-stepper/index.html

@sanity/schemas/siteDeployment.ts
@sanity/schemas/index.ts
@sanity/schemas/structure.ts
@sanity/editorial/workflowLogic.ts
@sanity/editorial/dashboardLogic.ts
@sanity/editorial/deployment.ts
@sanity/editorial/EditorialDashboard.tsx
@sanity/editorial/EditorialDashboard.css
@sanity/sanity.config.ts

@.github/workflows/deploy-ovh.yml
@.github/workflows/deploy.yml
@tests/unit/deploy-ovh-workflow.test.ts
@tests/unit/deployment.test.ts
@tests/unit/dashboard-logic.test.ts
@tests/unit/workflow-logic.test.ts

@README.md
@sanity/README.md
@.planning/phases/05-launch-domain-cutover/05-CONTEXT.md
</context>

<interface_context>
**Existing shapes to mirror, verified in the source before this plan was written:**

`sanity/schemas/siteDeployment.ts` — a `defineType` document with `name`/`title`/`type: 'document'` and exactly two `defineField`s: `buildSequence` (number, `readOnly: true`, `rule.required().integer().positive()`) and `lastTriggeredAt` (datetime, `readOnly: true`, `rule.required()`), preceded by a two-line comment explaining it is an internal trigger document. Registered in `schemas/index.ts` (import + array entry), listed in `structure.ts`'s `S.documentTypeListItems().filter(...)` exclusion array (line ~90), and present in `INTERNAL_SYSTEM_DOCUMENT_TYPES` in `workflowLogic.ts` (line 24) — which `sanity.config.ts` lines 87-89 use to strip it from the global create-new-document menu, and `filterDocumentActions` uses to strip publish/unpublish/delete/duplicate/discardChanges/restore.

`dashboardLogic.ts` marker plumbing, lines 75-115 and 435-485: three action interfaces (`CreateDeploymentMarkerAction`, `EditDeploymentMarkerAction`, `PublishDeploymentMarkerAction`) whose `publishedId`/`draftId` are currently hardcoded string literal types; the `DocumentAction` union; the `DeploymentMarker` interface; `DEPLOYMENT_MARKER_QUERY` (line 172); and `buildDeploymentMarkerActions(publicActions, marker, lastTriggeredAt)` which returns create+publish when the marker is absent, and edit+publish with `ifPublishedRevisionId` when it exists, throwing `'Marqueur technique de déploiement invalide. Actualisez et réessayez.'` on a malformed marker.

`PublicationClient` (line 153) is the narrow structural interface the controller depends on: `fetch<T>(query, params?, options?)` and `action(actions, {tag})`. `EditorialDashboard.tsx` line 96 casts Sanity's `useClient` result to it. `publicationError(reason)` (line 487) is currently module-private and normalizes unknown throwables to a French string — it must be exported for the new handler to reuse it.

`deployment.ts` currently hardcodes `deploy.yml` into two module constants (`GITHUB_WORKFLOW_URL`, `GITHUB_RUNS_URL`), reads them from `getRecentDeployments`, and `deploymentState` returns one of six `DeploymentStateKind`s: `pending-content`, `waiting-run`, `deploying`, `current`, `failed`, `unknown`. Both GitHub reads are unauthenticated public API calls from the browser — no token is involved, which is exactly why the same code can poll a second workflow file with no new credential.

`EditorialDashboard.tsx` line 282-325 is the staging polling effect: an `AbortController` + self-rescheduling `setTimeout` whose delay comes from `nextDeploymentPollDelay`, keyed on `[draftCount, publishedReference]`, returning a cleanup that cancels, aborts and clears. The production polling effect must be a structural copy of it — same abort/cancel/clear discipline, different workflow file and different reference timestamp.

**Sanity Actions API constraint that shapes the design:** `client.action([...])` commits its array as ONE transaction. That is why `buildDeploymentMarkerActions` takes the content-publish actions as its first argument (bundling them). The production release must NOT be bundleable, so its builder deliberately has no such parameter — the absence of the parameter is the guarantee, not a comment.

**Sanity webhook mechanics (why step 5 is `user_setup`, not code):** a Sanity Project Webhook is configured in Sanity's hosted dashboard and stored in Sanity's own infrastructure. There is no file in this repository that defines it and no CLI in this environment authenticated to create it. The webhook is what converts a `siteProductionRelease` mutation into a GitHub `repository_dispatch`. Everything this plan builds is inert until a human creates it.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the siteProductionRelease internal marker schema and register it in all three places</name>
  <files>sanity/schemas/siteProductionRelease.ts, sanity/schemas/index.ts, sanity/schemas/structure.ts, sanity/editorial/workflowLogic.ts, tests/unit/workflow-logic.test.ts</files>
  <behavior>
    - `isInternalSystemDocumentType('siteProductionRelease')` returns true.
    - `INTERNAL_SYSTEM_DOCUMENT_TYPES` contains both marker types and nothing else.
    - `filterDocumentActions(actions, 'siteProductionRelease')` strips publish, unpublish, delete, duplicate, discardChanges and restore — identically to how it treats the existing marker type.
    - `isPublicSiteDocumentType('siteProductionRelease')` stays false, so the new type never enters the publication inventory.
  </behavior>
  <action>
Create `sanity/schemas/siteProductionRelease.ts` as a field-for-field mirror of `sanity/schemas/siteDeployment.ts`. Same import line, same `defineType` shape, same two `defineField`s (`buildSequence`: number, readOnly, `rule.required().integer().positive()`; `lastTriggeredAt`: datetime, readOnly, `rule.required()`), same field titles in French. Only three things differ: the export/`name` is `siteProductionRelease`, the `title` is `'Marqueur technique de mise en production'`, and the leading doc comment says this fixed-ID document is the internal PRODUCTION release trigger, created and published only by the Editorial Dashboard's dedicated production-release action — never by the content-publish batch — and that a Sanity Project Webhook filtering on this type is what starts the OVH deploy workflow.

Register it in `sanity/schemas/index.ts`: add the import next to the existing marker import, and add `siteProductionRelease` to the `schemaTypes` array immediately after the existing marker entry.

Register it in `sanity/editorial/workflowLogic.ts`: extend `INTERNAL_SYSTEM_DOCUMENT_TYPES` (line 24) to a two-element `as const` tuple. Change nothing else — `InternalSystemDocumentType`, `internalSystemTypeSet`, `isInternalSystemDocumentType` and `filterDocumentActions` all derive from that constant and pick the new type up for free. Extend the existing comment above it by one sentence noting that both entries are deploy-trigger markers written only by the dashboard.

Register it in `sanity/schemas/structure.ts`: add `'siteProductionRelease'` to the string array inside `S.documentTypeListItems().filter(...)` (line ~90), directly after the existing marker entry, so it is excluded from the generic document-type list at the bottom of the desk.

Then extend `tests/unit/workflow-logic.test.ts` with the four assertions in the behavior block above. Follow the file's existing describe/it style; do not restructure existing tests.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/workflow-logic.test.ts && npm --prefix sanity run lint && grep -c "siteProductionRelease" sanity/schemas/index.ts sanity/schemas/structure.ts sanity/editorial/workflowLogic.ts</automated>
  </verify>
  <done>The new schema file exists and mirrors the existing marker; the type is present in `schemaTypes`, in `INTERNAL_SYSTEM_DOCUMENT_TYPES`, and in the desk exclusion array (grep reports at least 1 hit in each of the three files); the four new workflow-logic assertions pass and every pre-existing assertion in that file still passes; Studio lint is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize the marker actions and add a standalone production-release trigger</name>
  <files>sanity/editorial/dashboardLogic.ts, tests/unit/dashboard-logic.test.ts</files>
  <behavior>
    - `buildProductionReleaseMarkerActions(null, iso)` returns exactly two actions: a create of `siteProductionRelease` with `buildSequence: 1` and `ifExists: 'fail'`, then a publish of `drafts.siteProductionRelease` → `siteProductionRelease`. No content-publish action is present, and the function accepts no parameter through which one could be supplied.
    - `buildProductionReleaseMarkerActions({_id, _rev, buildSequence: 4}, iso)` returns an edit action setting `buildSequence: 5` and the given timestamp, then a publish carrying `ifPublishedRevisionId` equal to the marker's `_rev`.
    - A marker with a non-integer, zero/negative `buildSequence`, or a missing `_rev`, throws a French "actualisez et réessayez" style error — same failure mode as the existing deployment-marker builder.
    - `buildDeploymentMarkerActions` keeps its exact current signature and output for both the absent-marker and present-marker cases (its existing tests must pass byte-identically).
    - `triggerProductionRelease(client)` fetches the marker with the published perspective, calls `client.action` exactly once with the built actions and a tag distinct from `editorial.publish-all`, and resolves to the ISO timestamp it wrote.
    - `triggerProductionRelease` propagates a rejected `client.action` rather than resolving — the caller must be able to surface the failure.
    - `PRODUCTION_RELEASE_MARKER_QUERY` targets `_id`/`_type` `siteProductionRelease` and projects `lastTriggeredAt` in addition to `_id`, `_rev`, `buildSequence`.
  </behavior>
  <action>
Refactor `sanity/editorial/dashboardLogic.ts` so the marker plumbing is type-parameterized by marker id rather than duplicated.

1. Above the action interfaces, add two exported id constants and a union type: the existing marker id, the new production release marker id, and `export type MarkerDocumentId = typeof <existing> | typeof <new>`. Widen the three marker action interfaces so `publishedId` is `MarkerDocumentId`, `draftId` is the template-literal type `drafts.${MarkerDocumentId}`, and the create action's `attributes._type` is `MarkerDocumentId`. Rename nothing that is imported elsewhere — verify with a repo-wide grep for each interface name before renaming anything, and if in doubt keep the existing names and only widen the field types. The `DocumentAction` union is unchanged in shape.

2. Widen `DeploymentMarker` the same way: `_id: MarkerDocumentId`, keep `_rev` and `buildSequence`, add an optional `lastTriggeredAt?: string`. Add `export type ProductionReleaseMarker = DeploymentMarker & {_id: <production id>}` only if it reads more clearly than reusing `DeploymentMarker` directly; a single widened interface is acceptable and preferred if the extra alias earns nothing.

3. Add a private `markerQuery(id: MarkerDocumentId, extraFields?: string[])` helper that builds the GROQ string. Keep `DEPLOYMENT_MARKER_QUERY` exported and byte-equivalent to its current value (an existing test asserts on its contents). Add `PRODUCTION_RELEASE_MARKER_QUERY`, built the same way but also projecting `lastTriggeredAt`.

4. Extract the create-or-edit-then-publish body of `buildDeploymentMarkerActions` into a private `markerActions(markerId, marker, lastTriggeredAt): DocumentAction[]` that returns ONLY the two marker actions and performs the existing validity check (safe integer, at least 1, `_rev` present) with an error message naming the marker in French. Then: `buildDeploymentMarkerActions(publicActions, marker, lastTriggeredAt)` becomes `[...publicActions, ...markerActions(<existing id>, marker, lastTriggeredAt)]` — signature and behaviour unchanged. And add `export function buildProductionReleaseMarkerActions(marker, lastTriggeredAt): DocumentAction[]` returning `markerActions(<production id>, marker, lastTriggeredAt)` directly. Add a comment on the new function stating that the deliberate absence of a `publicActions` parameter is what structurally prevents a production release from riding along with a content publish — the Actions API commits its array as one transaction, so the only safe guarantee is that content actions cannot be handed to this builder at all.

5. Export the currently-private `publicationError` (add the `export` keyword only; do not change its body).

6. Add `export async function triggerProductionRelease(client: PublicationClient, now: Date = new Date()): Promise<string>`: fetch `PRODUCTION_RELEASE_MARKER_QUERY` with `{perspective: 'published'}`, compute `now.toISOString()`, build the actions, `await client.action(actions, {tag: 'editorial.production-release'})`, and return the timestamp. No try/catch — the caller owns error presentation, exactly as `createPublicationController` leaves `publicationError` formatting to its own state machine.

Then extend `tests/unit/dashboard-logic.test.ts`: add a `describe('production release marker actions', ...)` block next to the existing `describe('deployment marker actions', ...)` covering every bullet in the behavior block. Use the same fake-client shape the existing publication-controller tests use (an object with `fetch` and `action` vi.fn()s). Do not modify any existing assertion in that file.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/dashboard-logic.test.ts && npm --prefix sanity run lint</automated>
  </verify>
  <done>Both marker builders share one implementation; `buildDeploymentMarkerActions`'s existing tests pass untouched; the new production-release describe block passes; `triggerProductionRelease` calls `client.action` once with a dedicated tag and returns the written timestamp; `publicationError` is exported; Studio lint is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Parameterize deployment tracking by workflow file and target, and disambiguate the status label</name>
  <files>sanity/editorial/deployment.ts, tests/unit/deployment.test.ts</files>
  <behavior>
    - `getRecentDeployments(signal, 'deploy-ovh.yml')` fetches a runs URL containing `deploy-ovh.yml` and `per_page=10`; called with no second argument it fetches the staging URL exactly as today.
    - `deploymentState({...})` with no `target` behaves as staging: a proven post-publication successful run yields label `Staging à jour` and `actionUrl` equal to the staging preview URL.
    - `deploymentState({..., target: 'production'})` for the same inputs yields label `Production à jour` and `actionUrl` equal to the production domain URL.
    - Non-terminal and failure states of a production-target call link to the production workflow's Actions page, never the staging one.
    - `deploymentSubtitle` of a proven-current staging state is `Tous les contenus sont publiés · Staging à jour.`
    - Every other state kind, tone, terminal flag and poll cadence is unchanged for both targets.
  </behavior>
  <action>
Refactor `sanity/editorial/deployment.ts`.

1. Add exported constants for the two workflow filenames (staging is `deploy.yml`, production is `deploy-ovh.yml`) and for the production site URL `https://atelierjacquelinesuzanne.fr`. Add two exported pure builders — one returning the human Actions page URL for a workflow file, one returning the public unauthenticated runs API URL for a workflow file (preserving the existing `?per_page=10`). Redefine the existing `GITHUB_WORKFLOW_URL` and `GITHUB_RUNS_URL` constants in terms of those builders applied to the staging filename, so their values are byte-identical to today and every existing import keeps working.

2. Give `getRecentDeployments` an optional SECOND parameter for the workflow filename, defaulting to staging. Do not change the first parameter or its position — `EditorialDashboard.tsx` and the existing tests both call it positionally with just the abort signal. `getLatestDeployment` forwards the same optional parameter.

3. Add `export type DeploymentTarget = 'staging' | 'production'` and a module-level record keyed by target holding, per target: the label used for the proven-current state (`Staging à jour` / `Production à jour`), the site URL to open, and the workflow Actions page URL. Add an optional `target` field to `deploymentState`'s parameter object, defaulting to staging.

4. Inside `deploymentState`, replace every hardcoded reference to the module-level workflow URL and preview URL with a lookup from that record, and replace the proven-current state's hardcoded label with the record's label. The `unknownDeployment` helper must also take the resolved workflow URL rather than closing over the staging constant. Also make the two prose details that say "le site" target-aware in the production case so a failure reads as a production failure rather than a generic one — keep the staging wording byte-identical to today so no staging-facing copy silently drifts. Do not change any `kind`, `tone` or `terminal` value.

5. Update `tests/unit/deployment.test.ts`: in the `deployment-aware dashboard subtitle` describe block, update the one test that asserts the proven-current label and the one that asserts the full subtitle string so both expect the staging-disambiguated label. Then add a new describe block covering the production-target assertions and the workflow-file-parameterized fetch from the behavior list. Leave the existing negative assertion (that a non-current subtitle does not contain the old generic label) exactly as written — it must keep passing unchanged, which it does because the new label is a different string.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/deployment.test.ts && grep -c "Staging à jour" sanity/editorial/deployment.ts && grep -c "Production à jour" sanity/editorial/deployment.ts</automated>
  </verify>
  <done>`deployment.ts` tracks either workflow via a filename parameter with staging as the default; the proven-current label is target-specific and both new label strings are present in the source; `deploymentSubtitle` reports the staging-disambiguated label; the full deployment test file passes including every pre-existing assertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add the releasePipelineState state machine driving sketch 016 Variant C</name>
  <files>sanity/editorial/deployment.ts, tests/unit/deployment.test.ts</files>
  <behavior>
    - Content segment is `done` when no drafts are pending and `pending` otherwise.
    - Staging segment maps the staging deployment kind: proven-current → `done`; deploying or waiting-for-run → `active`; failed → `failed`; pending-content or unknown → `pending`.
    - With no production release ever recorded, the production segment is `pending` and the row is dimmed, disabled, titled `En attente du staging…`.
    - Content done + staging done + no production release → row is NOT dimmed, the button is ENABLED and reads `Mettre en production`, titled `Staging vérifié — prêt pour la production ?`.
    - A release in flight (busy) → production segment `active`, button disabled, title `Mise en production en cours…`.
    - A proven-current production run newer than the newest publication → production segment `done`, button disabled, title `Production à jour`.
    - A production release older than the newest content publication (stale) → production segment falls back to `pending` and the button re-enables, so a second publish can be promoted.
    - A failed staging run → staging segment `failed`, button disabled, and the row title names the STAGING stage as the failure.
    - A failed production run → production segment `failed`, button ENABLED for retry, and the row title names the PRODUCTION stage as the failure.
    - A trigger request error string, when present, wins over every other row state and is surfaced verbatim as the row detail with an enabled retry button.
  </behavior>
  <action>
Add to `sanity/editorial/deployment.ts` a pure, side-effect-free state machine that the dashboard renders directly — no state derivation may live in the component.

Export `type PipelineSegmentKind = 'pending' | 'active' | 'done' | 'failed'` and an interface for the returned shape: a `segments` object with `content`, `staging` and `production` keys of that type, plus a `promote` object with `title`, `detail`, `buttonLabel`, `buttonDisabled`, `dimmed`, and optional `actionLabel`/`actionUrl` used only by the two failure rows.

Export `releasePipelineState({pendingCount, staging, production, publishedAt, productionReleaseAt, busy, requestError})` where `staging` and `production` are `DeploymentState` values produced by `deploymentState` for their respective targets, `publishedAt` is the newest content publication reference already computed by the dashboard, `productionReleaseAt` is the marker's last trigger timestamp (empty string when never released), `busy` is true while a trigger request is in flight, and `requestError` is an optional message from a failed trigger.

Derive, in this order:
- Map deployment kinds to segment kinds per the behavior list, via one small private mapper reused for both stages.
- Compute staleness: stale when there is no production release timestamp at all, or when the publication reference parses to a strictly later instant than the production release timestamp. Reuse the module's existing timestamp-parsing helper; treat an unparseable value the same way the rest of the module does rather than inventing new failure semantics.
- Production segment: `active` when busy; else `pending` when stale; else the mapped production kind.

Then resolve the single promote row by first match, in exactly this precedence: (1) a `requestError` is present — title says the production release could not be started, detail is the error verbatim, button enabled and labelled as a retry; (2) production segment is `active` — title `Mise en production en cours…`, detail `GitHub construit et pousse le site vers OVH.`, button disabled labelled `Déploiement en cours…`; (3) production segment is `failed` — title names the production stage as failed, detail explains staging is fine but the push to the real domain failed and to retry or warn the maintainer, button ENABLED labelled as a retry, and expose the production run link as `actionLabel`/`actionUrl`; (4) production segment is `done` — title `Production à jour`, detail `Le site public reflète la dernière publication.`, button disabled labelled `Production à jour ✓`; (5) staging segment is `failed` — title names the STAGING stage as failed, detail says the production step is blocked until staging succeeds, button disabled, row dimmed, expose the staging run link; (6) content or staging is not yet `done` — title `En attente du staging…`, detail `La production sera disponible une fois le staging confirmé.`, button disabled labelled `Mettre en production`, row dimmed; (7) otherwise — title `Staging vérifié — prêt pour la production ?`, detail `Ouvrez le site de préproduction pour vérifier avant de continuer.`, button ENABLED labelled `Mettre en production`, row not dimmed.

`dimmed` is true only in cases 5 and 6 (mirroring the sketch's locked row opacity, which it applies whenever the promote affordance is not yet reachable). Copy strings must match the sketch's `STATES`/`applyStateC` values where the sketch defines them; the failure-row copy the sketch never modelled is written fresh here in the same register.

Then add a `describe('release pipeline state', ...)` block to `tests/unit/deployment.test.ts` with one `it` per bullet in the behavior list, building inputs with the file's existing `run()` factory and `deploymentState` rather than hand-writing `DeploymentState` literals, so the tests break if the state machine's inputs drift.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/deployment.test.ts && npm run test:coverage</automated>
  </verify>
  <done>`releasePipelineState` is exported, pure, and covers all eleven behaviors; every listed precedence case has a passing test; the full unit suite passes and coverage thresholds (70/65/70/70 over `src/lib` and `sanity/editorial`) still hold.</done>
</task>

<task type="auto">
  <name>Task 5: Render the Variant C pipeline bar and promote row in the editorial dashboard</name>
  <files>sanity/editorial/EditorialDashboard.tsx, sanity/editorial/EditorialDashboard.css</files>
  <action>
Extend the existing `Mettre le site à jour` Card (starts line 496) — do not replace it, do not move the existing publish button, and do not alter the existing confirmation dialog, pair list, blocked-rows card, or the three publication-state cards below it.

**State and data (EditorialDashboard.tsx).**

Add four pieces of component state next to the existing `publishedAt` state: the production runs array, the production deployment error, the production release timestamp string, and a busy flag plus an optional trigger-error string for the click handler.

Add an effect keyed on `[client, refreshKey]` that fetches `PRODUCTION_RELEASE_MARKER_QUERY` with `{perspective: 'published'}` and seeds the production release timestamp from the marker's `lastTriggeredAt`. This is what makes the pipeline survive a reload or a second tab. Guard it with a cancelled flag in the cleanup, matching the file's existing effect style. A fetch failure here must NOT break the dashboard — treat a missing/failed marker read as "never released" and continue.

Add a production polling effect that is a structural copy of the staging effect at lines 282-325: same `AbortController`, same self-rescheduling `setTimeout` fed by `nextDeploymentPollDelay`, same cleanup that sets cancelled, aborts and clears the timer, same `AbortError` swallow. Differences only: it passes the production workflow filename as `getRecentDeployments`' second argument, it uses the production release timestamp as its reference, it computes its next delay from a `deploymentState` call with the production target, and it returns early (clearing runs, no polling) when there is no production release timestamp — never poll a workflow for a release that was never requested.

Derive the production deployment state with `deploymentState({runs, publishedAt: productionReleaseAt, pendingCount: 0, error, target: 'production'})` — `pendingCount` is deliberately zero here because pending drafts are the content segment's concern, not production's. Then call `releasePipelineState` with the draft count, both deployment states, the existing `publishedReference`, the production release timestamp, the busy flag and the trigger error. Render straight from its result; put no branching copy in JSX.

Add the click handler: set busy, clear the trigger error, `await triggerProductionRelease(client as unknown as PublicationClient)`, fold the returned timestamp into the production release timestamp with `latestValidTimestamp` (same pattern as `confirmPublication` uses for `publishedAt`), and on failure set the trigger error from the now-exported `publicationError`. Always clear busy in a `finally`. The handler must call ONLY `triggerProductionRelease` — it must not call `publicationController.publish()`, must not preflight content, and must not be reachable from the existing publish button.

**Markup (EditorialDashboard.tsx).**

Immediately after the existing header `Flex` of the publish Card (the one closing at line 580) and BEFORE the existing `publicationPanelHasBody` divider, insert the Variant C block, rendered only when not loading and not in error:

- A `Box` with the pipeline bar class containing three sibling segment `<span>`s, each carrying the base segment class plus a modifier class derived from its `PipelineSegmentKind`. Mark the bar `aria-hidden="true"` — it is decoration over text that already states the same thing.
- A `Flex` of three `Text` labels reading `Contenu`, `Staging`, `Production`, each getting a strong modifier class when its segment is `done` or `active`, matching the sketch's label emphasis rule.
- One promote row `Flex` carrying the row class plus a locked modifier when `dimmed`: a left `Stack` with the row title as a semibold `Text size={1}` and the detail as a muted `Text size={1}`, and on the right the `Mettre en production` `Button` with `tone="primary"`, `text` bound to `buttonLabel`, `disabled` bound to `buttonDisabled`, `loading` bound to the busy flag, `style={{minHeight: 44}}` to match the existing publish button, and `onClick` calling the new handler. When the row state carries an `actionUrl`, render its `actionLabel` as an anchor between the text and the button, reusing the existing `editorial-dashboard__deployment-status` class and the same `target="_blank" rel="noreferrer"` treatment the header status link uses.

Give the whole block an accessible name: the bar plus labels plus row sit inside a `<section aria-label="Progression de la mise en ligne">` (or an equivalent `role`/`aria-label` on the wrapping `Stack`), so a screen-reader user meets it as a named region rather than three loose paragraphs.

Also add a production polling error surface consistent with the existing tracking-error card ONLY if the state machine does not already cover it — if `releasePipelineState` maps an unknown production state into a row, do not add a second competing message.

**Styles (EditorialDashboard.css).**

Add the new rules next to the existing `.editorial-dashboard__publish-panel` block. On that panel selector, declare two additional custom properties for the done and failed segment fills, set to the exact hex values already hardcoded in `deploymentDotColors` in `EditorialDashboard.tsx` (its positive and critical entries), with a comment stating they mirror that map so the two never drift. Use the existing `--dashboard-publish-accent` for the active fill. Use a `color-mix` over `--card-bg-color`/`--card-fg-color` for the empty track, following the same idiom as the existing `--dashboard-group-header-background` — introduce NO new hue anywhere.

Port the sketch's Variant C geometry: a flex bar, 6px tall, 4px gap, 4px radius, `overflow: hidden`, each segment `flex: 1`; a labels row justified space-between with each label `flex: 1` and centered, 11px, muted by default and full-strength semibold when done/active; and the promote row as a flex row, space-between, 14px/16px padding, 6px radius, on a slightly raised surface, with the locked modifier reducing opacity. Add the pulse keyframes for the active segment and wrap that animation in a `@media (prefers-reduced-motion: reduce)` block that disables it — this file currently has no reduced-motion handling and an infinitely pulsing bar is exactly the case that needs it.
  </action>
  <verify>
    <automated>npm --prefix sanity run lint && npm --prefix sanity run build</automated>
  </verify>
  <done>The publish panel shows a 3-segment bar labelled Contenu/Staging/Production plus one dynamic action row driven entirely by `releasePipelineState`; the button calls only `triggerProductionRelease`; production runs are polled from the production workflow only after a release exists; Studio lint and Studio build both succeed; no new color literal was introduced beyond the two mirrored from `deploymentDotColors`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Re-point deploy-ovh.yml at its own production-deploy-requested event</name>
  <files>tests/unit/deploy-ovh-workflow.test.ts, .github/workflows/deploy-ovh.yml</files>
  <behavior>
    - The production workflow's trigger surface is `workflow_dispatch` plus a `repository_dispatch` whose types list is exactly the new production-deploy-requested event.
    - Stripped of comment lines, the production workflow no longer mentions the content-publish event name at all — the header comment may still explain the history, and the assertion must tolerate that by filtering comment lines before searching.
    - The production workflow still has no branch-commit trigger.
    - The trigger-conditional `environment.name` expression is unchanged, and both environment names still appear.
    - Every other assertion in the file passes byte-identically, and the staging describe block is untouched.
  </behavior>
  <action>
First update `tests/unit/deploy-ovh-workflow.test.ts` so it fails against the current workflow (RED), then update the workflow to make it pass (GREEN). Commit the two steps separately.

In the test file, modify ONLY the `it` currently named for triggering on the Sanity content webhook. Rename it to describe triggering on manual dispatch and on the dedicated production-release event, never on a code commit. Keep its `workflow_dispatch` assertion, keep its `repository_dispatch:` assertion, and keep its existing negative branch-trigger assertion verbatim including its argument string. Replace its types assertion with one expecting the new event name in the same `types: [...]` form. Then ADD one assertion that builds a comment-stripped view of the workflow — split on newlines, drop every line whose trimmed start is a hash, rejoin — and asserts that view does not contain the content-publish event name. Add a short inline comment explaining that the comment-stripped form is deliberate: the header comment legitimately narrates the old mechanism, and a naive whole-file search would make that documentation self-invalidating.

Touch no other assertion, and do not touch the `.github/workflows/deploy.yml` describe block at all — staging keeps its trigger, and the assertion that it still has both a push and a repository_dispatch trigger must keep passing.

Then in `.github/workflows/deploy-ovh.yml`, change exactly two things:

1. The `types:` list under `repository_dispatch:` (line 36) becomes the single new event name, formatted identically to today (same bracket-list style, same indentation).

2. The header comment block (lines 3-31). Rewrite the first bullet so it describes the corrected mechanism: this workflow now starts only from a deliberate `Mettre en production` click in Sanity Studio's editorial dashboard, which publishes an internal release-marker document; a Sanity Project Webhook filtering on that document type fires this workflow's dedicated event. Explain that this is why the unprotected environment carries no GitHub reviewer gate — the editor's click IS the human checkpoint, and it happens after she has checked staging herself, so a second approval in a tool she has no access to would add nothing. State plainly that a content publish alone no longer reaches production; it only rebuilds GitHub Pages staging via the other workflow. Keep the existing `main`-HEAD caveat paragraph and the concurrency rationale as they are. Reference the quick task directory `.planning/quick/260811-w8d-build-the-real-mettre-en-production-feat/`.

Leave the workflow name, the whole `build` job, the `concurrency` block, the conditional `environment.name` expression, the credentials guard step, and both SFTP steps completely untouched. Also keep the existing hard constraint from the prior task: the header prose must not contain the branch-trigger YAML key (the four letters p-u-s-h immediately followed by a colon), because an assertion still requires that substring to be absent from this file.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/deploy-ovh-workflow.test.ts && grep -c "production-deploy-requested" .github/workflows/deploy-ovh.yml && grep -c "sanity-content-published" .github/workflows/deploy.yml</automated>
  </verify>
  <done>The production workflow triggers on manual dispatch plus the new dedicated event and nothing else; its comment-stripped body no longer names the content-publish event; the staging workflow still names it; all assertions in `deploy-ovh-workflow.test.ts` pass; the build job diffs clean against its previous content.</done>
</task>

<task type="auto">
  <name>Task 7: Correct the deploy documentation, add the webhook setup step, and record the D-01 refinement</name>
  <files>README.md, sanity/README.md, .planning/phases/05-launch-domain-cutover/05-CONTEXT.md</files>
  <action>
**README.md — `## Deployments` (starts line 54).**

Fix the production Trigger cell in the two-target table (line 60): it currently claims production is automatic on the content-publish webhook with no approval. Replace with wording covering the real mechanism — automatic on the dedicated production-release event fired by the editor's `Mettre en production` click in Sanity Studio; manual dispatch otherwise (Required-reviewer approval); a code commit to `main` still never deploys here. Leave the staging column and every other row untouched.

Rewrite `### Production deploy: the two paths` (line 67) to describe the corrected loop, keeping the same plain register:
- **Content path (editor-gated).** Romane publishes in Studio → the content webhook fires and rebuilds GitHub Pages staging only → the dashboard's pipeline bar shows staging going green → she opens staging and checks it herself → she clicks `Mettre en production` → that publishes an internal release-marker document → a second Sanity webhook fires the production-release event → deploy-ovh.yml runs every blocking gate and deploys to the real domain with no GitHub approval pause, because her click already was the human checkpoint and she has no GitHub access to give a second one.
- **Code path (manual, unchanged).** A commit landing on `main` deploys only to GitHub Pages staging. Shipping code to production is still an explicit `gh workflow run deploy-ovh.yml` that pauses on the `production-ovh` Required reviewer.
- **The caveat, restated:** because the release event always builds the default branch, a production release also ships whatever code is currently on `main`. Keep `main` production-ready. Note that this is now materially safer than before, because the release is a deliberate, separately-timed act rather than a side effect of every content publish.

Extend `### Production deploy: one-time setup` with a SIXTH numbered item: the Sanity Project Webhook. It must state that this is configured in Sanity's own dashboard (`https://www.sanity.io/manage` → project → API → Webhooks), NOT in this repository, and that until it exists the Studio button will publish its marker and nothing will happen. Give the exact configuration: trigger on create and update, dataset `production`, filter on the release-marker document type, POST to `https://api.github.com/repos/florianlepont/ajs-website/dispatches`, a body producing the `event_type` this repo's workflow now listens for, and the three headers (GitHub Accept, JSON content type, and a bearer Authorization). State that the token is a fine-grained GitHub PAT scoped to this single repository with Contents read/write and an expiry, that it lives ONLY in that webhook's header configuration in Sanity's dashboard, and that it must never be committed to this repository. Add the verification note that the EXISTING webhook driving the staging event almost certainly filters on the OTHER marker document type — open it, confirm the shape, and mirror it rather than editing it, because staging must keep its own trigger.

Finally, sanity-check the trailing `## Deployment` (singular) section near the end of the file and make sure its pointer sentence still describes reality; adjust only if it now misleads.

**sanity/README.md — Romane's French guide.**

In `## État de la mise à jour du site` (line 55), rename the bullet for the proven-current state to the disambiguated staging label, and add a short new paragraph after the bullet list describing the new 3-segment bar (Contenu / Staging / Production) and the `Mettre en production` button: publishing updates the preview site automatically, she checks it, then clicks the button to send it to the real address; the button stays greyed out until staging is confirmed, shows a spinner while production deploys, and turns into a done state afterwards; if a segment turns red the row tells her which stage failed.

In the verification checklist further down (around line 140), update the expected status name to the disambiguated staging label, and add a line for confirming the production step separately.

**.planning/phases/05-launch-domain-cutover/05-CONTEXT.md.**

Do NOT modify D-01's original line 17 or the existing 2026-08-11 supersession sub-bullet on line 18 — both must survive byte-identical. APPEND a further indented sub-bullet directly beneath the existing one, beginning `  - **D-01 refined again (2026-08-11, same day):**`, recording: the blanket automatic production deploy introduced earlier that day is withdrawn; production now deploys only from a deliberate `Mettre en production` click in Sanity Studio's editorial dashboard, which publishes a `siteProductionRelease` marker that a dedicated Sanity webhook turns into a `production-deploy-requested` repository_dispatch. Note that this restores D-01's checkpoint intent — an explicit human trigger before the real domain changes — while keeping it reachable by a maintainer with no GitHub access, which is why the unprotected environment path is retained for this event. Reference this quick task's directory. Do not modify D-02 through D-09 or any other line.
  </action>
  <verify>
    <automated>grep -q 'Mettre en production' README.md && grep -q 'sanity.io/manage' README.md && grep -q 'D-01 refined again' .planning/phases/05-launch-domain-cutover/05-CONTEXT.md && grep -qF 'D-01:** Deploys to the real domain are always an explicit, manual trigger' .planning/phases/05-launch-domain-cutover/05-CONTEXT.md && grep -qF 'D-01 superseded in part (2026-08-11, post-launch)' .planning/phases/05-launch-domain-cutover/05-CONTEXT.md && grep -q 'Staging à jour' sanity/README.md && npm run test:unit && npm run typecheck</automated>
  </verify>
  <done>README documents the editor-gated production path and the sixth one-time setup item (the Sanity webhook, flagged as human-only, with no token value written down); sanity/README.md uses the disambiguated staging label and explains the new bar and button in French; 05-CONTEXT.md holds D-01, its first supersession, and the new refinement note, all three intact; the full unit suite and typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio editor → production domain | Any authenticated Studio user can now start a real production deploy from a UI button. |
| Sanity webhook → GitHub `repository_dispatch` | An external HTTP caller holding a repo-scoped PAT starts a deploy that no GitHub reviewer approves. |
| GitHub PAT at rest in Sanity's dashboard | A repo-write credential lives outside this repository, in third-party webhook config, with no rotation automation. |
| Browser → public GitHub Actions API | The Studio polls two workflows' run status unauthenticated, from the editor's browser. |
| `main` branch HEAD → production | The release event builds the default branch, so a content release also ships whatever code is on `main`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w8d-01 | Elevation of Privilege | `Mettre en production` button in the Studio dashboard | medium | accept | This is the feature's entire point: Studio access is content authority, and the dataset already has exactly one non-technical editor plus the maintainer. Accepting is strictly safer than the status quo it replaces, where EVERY publish auto-deployed with no click at all. |
| T-w8d-02 | Tampering | GitHub PAT stored in Sanity's webhook header config | high | mitigate | Require a FINE-GRAINED token scoped to the single repo `florianlepont/ajs-website` with `Contents: Read and write` and an expiry date — never a classic `repo`-scope token, which would reach every repo the owner can write. The token is declared in `user_setup`, entered only in Sanity's dashboard, and never written to any file in this repository. Task 7's docs state the never-commit rule explicitly. |
| T-w8d-03 | Tampering | `main` HEAD shipped by a content release | medium | mitigate | Unchanged risk, materially reduced exposure: the release is now a deliberate, separately-timed act rather than a side effect of every publish, so the window in which unreleased `main` code can be shipped by accident shrinks from "every publish" to "each explicit promotion". Documented in both the workflow header comment and the README caveat. |
| T-w8d-04 | Denial of Service | repeated clicks / burst of marker mutations | low | mitigate | Three independent brakes: the button is disabled whenever the pipeline state is `active` or the request is in flight; `buildProductionReleaseMarkerActions` uses `ifPublishedRevisionId`, so a stale concurrent click fails rather than double-writing; and the workflow's existing `ovh-production` concurrency group with `cancel-in-progress: false` serialises runs and keeps only the most recent pending one. |
| T-w8d-05 | Spoofing | anyone who learns the dispatch event name | low | accept | Firing `repository_dispatch` requires a token with repo write access regardless of whether the event name is known; the name is not a secret and is already visible in the committed workflow. |
| T-w8d-06 | Information Disclosure | `siteProductionRelease` readable in the public dataset | low | accept | The document holds only a monotonic counter and a timestamp — no credential, no content, no PII. It is strictly less sensitive than the published content already served on the public site. |
| T-w8d-07 | Tampering | unpinned/new third-party dependencies | high | mitigate | This plan introduces NO npm/pip/cargo install of any kind — every file it touches uses libraries already in the two lockfiles. If execution discovers a genuine need for a new package, STOP and re-plan with a package-legitimacy gate rather than installing ad hoc. `npm ci --prefix sanity` (installing already-declared lockfile deps) is the only permitted install command. |
</threat_model>

<verification>
1. `npm ci --prefix sanity` if `sanity/node_modules` is absent — known environment gap recorded in 260811-v3t's SUMMARY; it installs only already-declared lockfile dependencies and adds nothing new.
2. `npm run test:unit` — full Vitest suite green, including the new workflow-logic, dashboard-logic, deployment and deploy-ovh assertions.
3. `npm run test:coverage` — coverage thresholds still met over `src/lib/**` and `sanity/editorial/**` (the new logic lands inside a measured path, so it must be genuinely tested, not just written).
4. `npm run typecheck` — clean (note: this excludes `sanity/`, which is why step 5 is separate and non-optional).
5. `npm --prefix sanity run lint && npm --prefix sanity run build` — the ONLY gate that type-checks and bundles `sanity/editorial/*.tsx`.
6. `git diff .planning/phases/05-launch-domain-cutover/05-CONTEXT.md` shows an addition only — D-01's original line and its first supersession sub-bullet must be untouched.
7. `git status --short .planning/phases/05-launch-domain-cutover/` lists ONLY `05-CONTEXT.md` — a separate phase-05 verification effort is live in this worktree and no other `05-*` file may be created, renamed or modified.
8. `git diff` review for secrets: no token, bearer value, or PAT literal anywhere in the diff.
9. Manual read of the `deploy-ovh.yml` diff: only the `types:` line and the header comment block changed; the `build` job is byte-identical.
</verification>

<success_criteria>
- `sanity/schemas/siteProductionRelease.ts` exists, mirrors the existing marker schema, and is registered in `schemas/index.ts`, `INTERNAL_SYSTEM_DOCUMENT_TYPES`, and the `structure.ts` exclusion array.
- `buildProductionReleaseMarkerActions` exists, shares one implementation with `buildDeploymentMarkerActions`, and takes no parameter capable of carrying content-publish actions.
- `triggerProductionRelease` publishes the marker in a single tagged Actions-API call and is the ONLY thing the new button invokes.
- `deployment.ts` tracks either `deploy.yml` or `deploy-ovh.yml` via a filename parameter, and reports `Staging à jour` / `Production à jour` instead of the ambiguous label — including for the pre-existing header status.
- `releasePipelineState` is pure, exported, and fully covers the five sketch states plus both failure stages and the trigger-error case.
- The dashboard renders the sketch-016 Variant C 3-segment bar and one dynamic action row, using only colors already present in the codebase, with the pulse animation disabled under `prefers-reduced-motion`.
- `deploy-ovh.yml` listens to `production-deploy-requested` only; `deploy.yml` still listens to `sanity-content-published`.
- `npm run test:unit`, `npm run test:coverage`, `npm run typecheck`, `npm --prefix sanity run lint` and `npm --prefix sanity run build` all pass.
- README, `sanity/README.md` and 05-CONTEXT.md describe the real mechanism; the Sanity webhook is documented as human-only setup; no secret is committed.
</success_criteria>

<output>
Create `.planning/quick/260811-w8d-build-the-real-mettre-en-production-feat/260811-w8d-SUMMARY.md` when done.

Flag prominently in the summary that the Sanity Project Webhook is a HUMAN prerequisite (see `user_setup`) and that this feature is INERT until it exists: the button will publish its marker and report success, and no production deploy will start. Also record whether `npm ci --prefix sanity` had to be run, and confirm that no file under `.planning/phases/05-launch-domain-cutover/` other than `05-CONTEXT.md` was touched.
</output>
