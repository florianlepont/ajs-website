---
phase: quick-260727-kq8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/home-carousel.ts
  - tests/unit/home-carousel.test.ts
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [QUICK-260727-kq8]

must_haves:
  truths:
    - "During a right-edge hover approach from proximity 0.80 to 0.995, --wordmark-bg-position on .home-hero__wordmark keeps changing — it is never frozen at a fixed value (e.g. -804px) partway through."
    - "computeWordmarkBackgroundPosition() returns the RAW (unclamped) position when the new clamp opt-out is passed; its DEFAULT behavior (clamp on) is byte-unchanged for every existing caller."
    - "Both mirrored-peek call sites (current-layer in syncWordmarkAlignment(), peek-layer in syncWordmarkLayers()) opt out of the clamp because the seam-driven clip-path already gates them to in-bounds content."
    - "All existing clamped-path unit tests and all existing mirrored-peek e2e tests (commit + hover seam-growth, FR/EN) still pass unchanged."
  artifacts:
    - src/lib/home-carousel.ts
    - tests/unit/home-carousel.test.ts
    - src/components/HomeCarousel.astro
    - tests/e2e/homepage.spec.ts
  key_links:
    - "The --wm-seam-driven clip-path on .home-hero__wordmark-stack (CSS zone selectors) is what guarantees only the in-bounds [0, seam] slice ever paints — this invariant is WHY the clamp is redundant at both call sites and WHY opting out is safe."
---

<objective>
Fix the confirmed live bug where the wordmark's CURRENT layer (`.home-hero__wordmark`, the accessible `<h1>`) freezes its `--wordmark-bg-position` partway through a hover-peek/commit while the peek layer and the carousel photo keep sliding. Root cause is already precisely diagnosed (do NOT re-diagnose): `computeWordmarkBackgroundPosition()` still carries quick-260727-drq's per-axis clamp, which the quick-260727-iao mirrored-peek redesign made redundant (the `--wm-seam` clip-path now guarantees only in-bounds `[0, seam]` content ever paints) but never removed — so at high proximity the full-box clamp pins the returned position at a fixed boundary value, freezing the whole visible slice.

Purpose: restore continuous 1:1 tracking of the current layer with the sliding photo, matching the peek layer, at every proximity up to the edge.
Output: a clamp opt-out on the pure function (default behavior preserved), both mirrored-peek call sites opting out, and unit + FR/EN e2e coverage proving the unclamped path and the no-freeze behavior.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/lib/home-carousel.ts
@src/components/HomeCarousel.astro
@tests/unit/home-carousel.test.ts
@tests/e2e/homepage.spec.ts
@.planning/quick/260727-iao-replace-wordmark-clamp-solid-ink-fallbac/260727-iao-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a clamp opt-out to computeWordmarkBackgroundPosition() + unit-test the unclamped path</name>
  <files>src/lib/home-carousel.ts, tests/unit/home-carousel.test.ts</files>
  <behavior>
    - Existing clamped case (default, no new arg): the current x-clamp test — naturalW/H 1000x1000, heroRect {width:500,height:500,left:-400,top:0}, wordmarkRect {width:100,height:50,left:100,top:100}, objectPosition 0.5/0.5 — STILL returns `{ size: '500px 500px', position: '-400px -100px' }`. Unchanged.
    - New unclamped case (same geometry, clamp opt-out passed): returns the RAW position `{ size: '500px 500px', position: '-500px -100px' }` (the pre-clamp value the drq test comment documents), proving the clamp is skipped.
    - Every other existing test in the file (in-bounds cases, y-clamp case, null guards) passes unchanged, since none passes the new argument.
  </behavior>
  <action>
    In `src/lib/home-carousel.ts`, add a new trailing parameter to `computeWordmarkBackgroundPosition()` named `clampToPhoto: boolean = true` (defaulting true so every existing caller and unit test is byte-for-byte unaffected). When `clampToPhoto` is true, keep the current per-axis clamp exactly as-is (`Math.min(0, Math.max(-minValidX, rawX))` / same for Y). When it is false, return `rawX`/`rawY` directly (skip the clamp) in the `position` string; `size` is unchanged in both branches. Do NOT remove the existing clamp logic — it stays as the default path (still-correct defensive behavior for any hypothetical unclipped caller). Update the drq clamp comment block to note the new opt-out and WHY it exists: callers already gated by the seam-driven clip-path (`--wm-seam`) only ever paint the in-bounds `[0, seam]` slice, so clamping against the full theoretical box is redundant there and its only observable effect was freezing the returned position once the full box exceeded photo bounds. Keep the function pure/DOM-free (module convention). Then in `tests/unit/home-carousel.test.ts`, add ONE new `it(...)` inside the existing `describe('computeWordmarkBackgroundPosition', ...)` block that reuses the exact geometry of the existing x-clamp test but passes the opt-out argument as false, asserting the returned `position` is the raw `-500px -100px` instead of the clamped `-400px -100px`. Leave all existing tests untouched.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/home-carousel.test.ts</automated>
  </verify>
  <done>The new signature accepts an optional trailing boolean defaulting true; the new unit test asserting the raw unclamped position passes; all pre-existing computeWordmarkBackgroundPosition tests (clamped x, clamped y, in-bounds, null guards) pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Opt both mirrored-peek call sites out of the clamp in HomeCarousel.astro</name>
  <files>src/components/HomeCarousel.astro</files>
  <action>
    In `src/components/HomeCarousel.astro`, update BOTH `computeWordmarkBackgroundPosition(...)` invocations to pass the clamp opt-out as false: (1) the current-layer computation inside `syncWordmarkAlignment()` (the call that sets `--wordmark-bg-size`/`--wordmark-bg-position` on `wordmarkEl`), and (2) the peek-layer computation inside `syncWordmarkLayers()` (the call producing `peekResult` for the active peek layer). Confirm exact function/line locations via grep first, since line numbers may have shifted since planning. At each call site add a short inline comment explaining that the clamp is skipped because the seam-driven clip-path (`--wm-seam` on `.home-hero__wordmark-stack`, CSS zone selectors) already guarantees only the in-bounds visible slice is ever painted — so the drq clamp is redundant here and its only effect was the current-layer freeze bug this change fixes. Both layers are equally clip-gated now, so both opt out uniformly. Change nothing else (no CSS, no markup, no other logic).
  </action>
  <verify>
    <automated>npx astro check</automated>
  </verify>
  <done>Both computeWordmarkBackgroundPosition() calls (syncWordmarkAlignment current-layer and syncWordmarkLayers peek-layer) pass the opt-out argument as false with an explanatory comment; `npx astro check` reports zero errors (the new positional boolean is type-checked against Task 1's updated signature).</done>
</task>

<task type="auto">
  <name>Task 3: Add FR+EN e2e proving the current layer's bg-position does not freeze across an edge approach</name>
  <files>tests/e2e/homepage.spec.ts</files>
  <action>
    In `tests/e2e/homepage.spec.ts`, add a new `test.describe` (e.g. "carousel wordmark current layer does not freeze during an edge approach (quick-260727-kq8)") with an FR (`/`) and an EN (`/en/`) test. Reuse the setup pattern from the existing "carousel wordmark stays synced to the peek (Bug A)" block: `page.goto`, click `[data-role="autoplay-toggle"]`, await `.home` has class `has-wordmark-photo`, and use its `photoBox(page)` helper (or an equivalent local `.home-hero__photo` boundingBox read). Grep the current `EDGE_ZONE_FRACTION` constant value in HomeCarousel.astro (it is 0.22) and derive each sample's cursor x-fraction from proximity using the right-zone mapping `xFraction = (1 - EDGE_ZONE_FRACTION) + EDGE_ZONE_FRACTION * proximity`. Walk the proximity sequence [0.80, 0.85, 0.90, 0.93, 0.95, 0.97, 0.985, 0.995] (mirroring the live measurement), and at each step: `page.mouse.move(box.x + box.width * xFraction, box.y + box.height * 0.3, { steps: 10 })`, let the rAF pump settle (~150-200ms via waitForTimeout), then read `--wordmark-bg-position` on `.home-hero__wordmark` via getComputedStyle. Collect the samples taken at proximity 0.85 onward (the range where the pre-fix code freezes at a fixed value). Assert the count of DISTINCT sample strings in that tail is at least 3 — on the frozen (pre-fix) code every sample in that range is identical (exactly one distinct value, e.g. `-804px -575.5px`), so this fails RED before Task 2 and passes GREEN after. Keep the y component fixed (vertical fraction 0.3) so only x varies and full-string comparison is meaningful. Do NOT weaken or modify the existing mirrored-peek commit or hover seam-growth tests.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/homepage.spec.ts -g "does not freeze"</automated>
  </verify>
  <done>The new FR and EN tests pass against the fixed code; both would fail against the pre-fix (frozen) code because the current-layer bg-position samples across proximity 0.85–0.995 collapse to a single value. Existing mirrored-peek commit and hover seam-growth tests in the same file remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced. This is a client-side, build-time-static UI geometry fix: no network calls, no user input parsing, no new data flow, no persistence.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-kq8-01 | Tampering | package installs (npm) | low | accept | No packages are installed or upgraded; no dependency surface changes. Nothing to verify. |
| T-kq8-02 | Denial of Service | wordmark rAF sync loop | low | accept | Change is a pure per-frame arithmetic branch (skip a clamp); no new loop, no added per-frame cost, no unbounded work introduced. |
</threat_model>

<verification>
- `npx vitest run tests/unit/home-carousel.test.ts` — new unclamped-path test passes, all existing computeWordmarkBackgroundPosition + seam-fraction + swipe + hover-zone tests pass unchanged.
- `npx astro check` — zero type errors (new signature + both opt-out call sites type-check).
- `npx playwright test tests/e2e/homepage.spec.ts` — new FR/EN no-freeze tests pass; existing mirrored-peek commit (FR/EN right, FR left) and hover seam-growth (FR/EN) tests stay green.
- The orchestrator independently re-verifies: diff review, typecheck, unit, build, isolated-port e2e, and live Playwright re-measurement of proximity 0.80→0.995 confirming the current layer's bg-position now changes continuously instead of freezing at -804px, plus a mid-peek screenshot confirming both layers track fluidly.
</verification>

<success_criteria>
- `computeWordmarkBackgroundPosition()` gains an optional trailing `clampToPhoto` boolean defaulting true; default (clamped) behavior and all existing unit tests are byte-for-byte preserved.
- A new unit test proves the unclamped path returns the raw out-of-bounds position.
- Both mirrored-peek call sites in HomeCarousel.astro opt out of the clamp with an explanatory comment.
- New FR+EN e2e coverage proves `--wordmark-bg-position` on `.home-hero__wordmark` keeps changing across the 0.80→0.995 edge approach (fails on frozen pre-fix code, passes on fixed code).
- Full unit + e2e suites (minus known pre-existing unrelated failures) remain green.
</success_criteria>

<output>
Create `.planning/quick/260727-kq8-fix-wordmark-current-layer-freezing-duri/260727-kq8-SUMMARY.md` when done.
</output>
