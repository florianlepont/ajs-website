---
phase: quick-260726-u97
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/home-carousel.ts
  - tests/unit/home-carousel.test.ts
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [QUICK-260726-u97]

must_haves:
  truths:
    - "The scroll-to-open gesture is GONE: no wheel/scroll/window-touch listener navigates the homepage; scrolling the carousel does nothing except normal browser scroll. No overscroll accumulator, threshold, decay watchdog, pull-scale/darken feedback, or .home-hero__pull-overlay remains anywhere in HomeCarousel.astro (markup, CSS, or script)."
    - "On a real mouse desktop (hover:hover + pointer:fine), a custom cursor follows the pointer inside .home-hero__photo (native cursor hidden): a translucent hairline ring ALWAYS labelled OUVRIR/OPEN in the center zone, morphing into a pill tinted with the current gallery's --current-accent with a directional arrow in the left/right edge zone (edge zone = 22% of width per side)."
    - "As the pointer approaches an edge, the current photo peels away from that edge (parallax push, up to ~16% of its width at max proximity) revealing the ADJACENT (prev/next) gallery's REAL photo underneath — sourced from the already-present data list, never a placeholder color; at rest / in the center zone no peek is visible."
    - "Clicking the center zone opens the CURRENT gallery (identical destination + cross-document hero-photo morph as clicking the title link); clicking the left edge goes to the previous gallery and the right edge to the next, via the existing goToPrev()/goToNext()."
    - "On touch / coarse-pointer devices the cursor + edge-hover + peek system is inert; tapping the photo (a touch that is NOT a swipe) opens the current gallery, and the existing horizontal swipe still navigates prev/next unchanged."
    - "The .home-hero__img overhang (top:-4%;left:-4%;width/height:108%) is reverted to simple full coverage (inset:0; width/height:100%; object-fit:cover), removing the ~8% inherent rest-zoom; the black-frame regression it guarded against no longer exists because the pull-scale-down is gone."
    - "The carousel-mode footer-hide (body:has(.home[data-display-mode='carousel']) footer.chrome-band { display:none }) is UNCHANGED and is now risk-free — nothing reads scroll position to navigate anymore, so the vacuous-atBottom() concern that made hiding it risky is irrelevant."
    - "Existing keyboard arrow-key nav, progress-dash click nav, auto-advance/pause, the title's own click-through + morph naming, the accent panel / wordmark / caption / progress-dash visuals, and the horizontal touch-swipe are all unchanged. Both FR and EN work throughout."
  artifacts:
    - "src/lib/home-carousel.ts — new pure computeHoverZone(xFraction, edgeZoneFraction) returning { zone, proximity }, alongside the existing computeWordmarkBackgroundPosition/detectSwipeDirection."
    - "tests/unit/home-carousel.test.ts — computeHoverZone cases: center, left/right, exact zone boundaries, and proximity values at several points, mirroring detectSwipeDirection's style."
    - "src/components/HomeCarousel.astro — overscroll machinery removed; img overhang + down-chevron cursor removed; new custom cursor element + CSS, hover-zone/peek/cursor script gated behind (hover:hover) and (pointer:fine), prev/next peek layers, center-open / edge-nav click, mobile tap-to-open fallback."
    - "tests/e2e/homepage.spec.ts — obsolete overscroll/cursor blocks removed; new coverage for cursor states, peek (real adjacent photo, proportional, not-at-rest), center/edge clicks, and mobile tap-to-open + unchanged-swipe."
  key_links:
    - "mousemove on .home-hero__photo -> computeHoverZone(x/rect.width, 0.22) -> currentZone/currentProximity -> updateCursor() (position + zone state) AND updatePeek() (parallax) — one source of truth for zone state consumed by cursor, peek, and click."
    - "photo click -> currentZone: center -> openCurrent() (== titleEl.click(): reuses href + setCrossDocPhoto morph); left -> goToPrev(); right -> goToNext(). Clicks inside .home-hero__caption are ignored so the title/dashes/autoplay keep their own handlers."
    - "touchend on .home-hero__photo -> detectSwipeDirection(): a swipe -> goToPrev/goToNext (unchanged); NOT a swipe AND negligible movement (tap) -> openCurrent() — the coarse-pointer fallback, composed so a vertical scroll-drag is neither a swipe nor a tap and does nothing."
    - "render() -> assigns prev/next peek <img> src/srcset from galleries[(index±1+N)%N] and resets the peek — the peek always tracks the current neighbours across auto-advance/manual swaps."
---

<objective>
Remove the homepage carousel's scroll-to-open gesture and ALL of its supporting machinery entirely, then replace it with sketch 008's approved Variant C: a custom hover cursor (permanent OUVRIR ring in the center, accent-tinted directional arrow pill at the 22% edge zones) plus edge-zone click navigation and a parallax-push peek preview of the adjacent gallery. On touch devices the whole hover system is inert and a plain tap opens the current gallery.

Purpose: A step-back UX + code-quality review found the scroll-to-open gesture inherently risky (a realistic 2-3 tick scroll crosses the navigation threshold — confirmed live) and most of its accumulated complexity (idle-decay watchdog, neutral-reset-before-navigate, mobile/desktop threshold asymmetry, the just-fixed black-frame overhang) to be compensation for that risk, not the underlying idea. The user decided to drop the gesture entirely for an explicit, unambiguous hover+click interaction. Removing the gesture also makes the footer-hide risk-free (nothing reads scroll position anymore) and lets the ~8% image overhang be reverted for sharper framing.

Output: An updated HomeCarousel.astro (gesture removed, hover-cursor + edge-zone + peek built), a new pure computeHoverZone() with unit tests, and a reworked homepage e2e suite proving the new interaction FR + EN, desktop hover + mobile tap.
</objective>

<safety_and_scope>
Read and honor this section — it is load-bearing, not commentary. The orchestrator will independently re-verify all of it (diff review, typecheck, unit, build, isolated-port e2e, and a LIVE Playwright MCP browser check with ACTUAL SCREENSHOTS of the cursor states and peek animation — computed-style/DOM-state checks alone are the exact gap that let the black-frame bug ship in quick-260726-qem).

**FOOTER-HIDE — deliberately KEPT, now risk-free.** Leave the `is:global` rule `body:has(.home[data-display-mode='carousel']) footer.chrome-band { display: none; }` EXACTLY as-is. Removing the scroll gesture entirely means `atBottom()`'s vacuous-true-at-scrollY-0 condition (the ENTIRE reason hiding the footer was ever risky, per quick-260725-sj4) is now irrelevant: nothing reads scroll position to trigger navigation anymore, so the footer can stay hidden in carousel mode with zero risk and zero compensating machinery. This is a genuine, deliberate simplification — state it plainly in the SUMMARY. KEEP the existing `footer visibility by display mode (quick-260726-obg)` e2e tests unchanged (pure CSS-scoping tests, unrelated to gesture safety). The two full-bleed tests that assert `footer.toBeHidden()` (`mobile full-bleed hero regression (HOME-06)`, `tall-desktop full-bleed hero regression`) also stay valid unchanged.

**DELETED (script, in the `<script>` overscroll block):** constants `OPEN_OVERSCROLL_THRESHOLD`, `BOTTOM_EPSILON`, `RESET_IDLE_MS`, `OVERSCROLL_DECAY_MS`; state `overscrollAccum`, `lastOverscrollTs`, `decayRaf`, the old `navigating` guard; functions `atBottom()`, `setPullFeedback()`, `decayOverscroll()`, `registerDownwardIntent()`, `navigateToCurrent()`, the proactive idle-decay `setInterval` watchdog; the window-level `scroll`, `wheel`, and the OVERSCROLL `touchstart`/`touchmove` listeners (the ones using `lastMoveY`/`overscrollTouchStartX`/`overscrollTouchStartY`); the overscroll reset lines inside `render()` and `showGrid()`.

**DELETED (markup + CSS):** the `.home-hero__pull-overlay` element and every CSS rule referencing it; the `--pull-scale`/`--pull-darken` custom properties and every rule using them (the `transform: scale(var(--pull-scale,1))` on `.home-hero__img`, the pull references in `.home-hero__img--sharp`'s transition and the reduced-motion block); the `.home-hero__photo.is-opening .home-hero__img, ...pull-overlay { transition: none }` hook (repurposed — see below); the `background-color: #000` on `.home-hero__photo` (its documented job was to keep the pulled-back edge dark; with the pull gone and the img reverting to full cover, it's dead, and the peek layers now cover any gap during the push); the custom down-chevron data-URI `cursor` on `.home-hero__photo` (fully superseded by the JS cursor).

**REVERTED:** `.home-hero__img` from `top:-4%; left:-4%; width:108%; height:108%` back to the ORIGINAL pre-qem rule `position:absolute; inset:0; width:100%; height:100%; object-fit:cover;` (plus the NEW `transform: translateX(var(--peek-shift, 0))` for the parallax push — see Task 4). Do NOT repeat qem's overhang-sizing investigation: there is no overhang needed at all now (the overhang existed only to give the pull-scale-down room; that feature is gone). Also update `syncWordmarkAlignment()`'s comment, which currently explains the 108% box — it now reads the plain full-cover box.

**KEPT UNCHANGED:** the horizontal-swipe `touchstart`/`touchend` handlers on `.home-hero__photo` (`SWIPE_MIN_DISTANCE`/`SWIPE_DIRECTION_RATIO`/`detectSwipeDirection` — a separate, working mechanism) EXCEPT the touchend handler gains a tap-to-open branch (Task 5); keyboard arrow nav; progress-dash click nav; auto-advance/pause + the progress-dash fill (quick-260725-dcg); the title's own click listener (`setCrossDocPhoto`) + its href + the mode-toggle view-transition morph; the accent panel / wordmark / caption visuals; the blur-up crossfade (HOME-09); `tests/e2e/i18n.spec.ts` and `tests/e2e/legal.spec.ts` (do NOT touch — their footer grid-mode-detour fixes remain correct).

**MOBILE COMPOSITION (no conflict with swipe).** The sketch is hover-only; the real site must work on touch. Gate the ENTIRE new cursor + edge-hover + peek system behind `(hover: hover) and (pointer: fine)` — the CSS `cursor: none` and the cursor element visibility via a `@media` query, and the mousemove/peek/cursor JS via `window.matchMedia('(hover: hover) and (pointer: fine)')`. On touch/coarse-pointer there is no hover to approach an edge with, so do NOT replicate zones/peek. Fallback: a tap anywhere on the photo opens the current gallery. It composes with the existing swipe detection in ONE place — the existing `touchend` handler: compute `detectSwipeDirection(...)`; if it returns `next`/`prev`, navigate (unchanged); if it returns `null` AND the total finger movement on both axes is negligible (a genuine tap), call `openCurrent()`. A vertical scroll-drag over the photo has a large `deltaY`, so it is neither a swipe nor a tap and correctly does nothing. This is why a tap "does not fire if the touch was actually part of a swipe" and does not hijack a scroll.

**RE-ENTRANCY GUARD.** The old `navigating` guard is deleted with the gesture. `openCurrent()` gets a fresh minimal `opening` boolean guard — justified because on a hybrid touchscreen-laptop a tap can fire BOTH `touchend` (tap-to-open) and a synthesized `click` (desktop center-open), and native navigation should not be triggered twice. That is the only guard the new feature needs.
</safety_and_scope>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# Approved design spec (winner: "C") — port Variant C's mechanism directly; tuning is confirmed
@.planning/sketches/008-carousel-hover-navigation/README.md
@.planning/sketches/008-carousel-hover-navigation/index.html

# Source of truth — re-derive EVERYTHING against the CURRENT file; do NOT trust any summary's line numbers
@src/components/HomeCarousel.astro

# The established pure-logic extraction pattern to follow for computeHoverZone
@src/lib/home-carousel.ts
@tests/unit/home-carousel.test.ts

# What is being removed (full context) and the black-frame overhang being reverted
@.planning/quick/260726-obg-implement-sketch-007-variant-c-overscrol/260726-obg-SUMMARY.md
@.planning/quick/260726-qem-fix-black-frame-regression-in-the-homepa/260726-qem-SUMMARY.md
</context>

<interfaces>
Current HomeCarousel.astro anchors (grep to locate — line numbers WILL have drifted, this file has been edited across 8+ quick tasks this session):

- **Overscroll block** (in the `<script>`, currently ~lines 762-983): the constants/state/functions/listeners enumerated in `<safety_and_scope>` DELETED. It sits between the swipe handlers above it and the `modeToggleBtn` lookup below it.
- **Horizontal swipe** (KEEP): `const SWIPE_MIN_DISTANCE = 50; const SWIPE_DIRECTION_RATIO = 1.5;`, `touchStartX/Y`, `const heroPhoto = hero.querySelector<HTMLElement>('.home-hero__photo')`, and the `heroPhoto?.addEventListener('touchstart'|'touchend', ...)` pair. `heroPhoto` (nullable) is reused for the new cursor/peek/click listeners.
- `heroImg` = `[data-role="hero-image"]`, `heroPlaceholderImg` = `[data-role="hero-image-placeholder"]`, `titleEl` = `[data-role="gallery-title"]` (an `<a>` with an existing click listener calling `setCrossDocPhoto(heroImg)` and a real `href`). `render()`, `showGrid()`, `showCarousel()`, `goToPrev()`, `goToNext()`, `goToIndex()`, `carouselIndex`, `galleries[]` (each has `slug,title,heroSrc,heroSrcSet,blurSrc,alt,statement,href,heroColor,heroTextColor`), `reduceMotionQuery` (`window.matchMedia('(prefers-reduced-motion: reduce)')`, in scope for the whole script).
- **Markup**: `.home-hero__photo` contains, in order: `.home-hero__img.home-hero__img-placeholder` (blur), `.home-hero__img.home-hero__img--sharp` (current sharp), `.home-hero__scrim`, `.home-hero__pull-overlay` (DELETE), `.home-hero__caption` (holds `.home-hero__carousel-controls` with the progress dashes + autoplay toggle, `.home-hero__index`, and the `.home-hero__title` `<a>`; `z-index:3`). `.home-hero__accent` is a SIBLING of `.home-hero__photo`. The build-time data list `ul[data-role="home-carousel-data"]` carries `data-hero-src`/`data-hero-srcset` per gallery (already parsed into `galleries[]`).
- **CSS anchors**: `.home-hero__photo` (custom cursor + `background-color:#000` — both DELETE; add `cursor:none` gated), `.home-hero__img` (base: REVERT overhang, add `translateX(var(--peek-shift,0))`), `.home-hero__img--sharp` (`.is-loaded` opacity crossfade — KEEP; retune its transition to `opacity 260ms ease, transform 420ms ...`), `.home-hero__scrim` (permanent legibility gradient — DO NOT touch), `.home-hero__caption`/`.home-hero__title` (KEEP), the reduced-motion `@media` block, and the `is:global` footer-hide (KEEP).
- **--current-accent / --current-accent-text**: set by `render()` on the `.home` root; the cursor element (inside `.home-hero__photo`) inherits both. The edge pill uses `var(--current-accent)` for background and `var(--current-accent-text)` for the arrow/label color.
</interfaces>

<sketch_reference>
Variant C peek math (from sketch 008 index.html, the CONFIRMED tuning — port directly, do NOT use the abandoned 15% experiment):

- `EDGE_ZONE_FRACTION = 0.22` (22% per side).
- Zone detection given `xFrac = x / rect.width`: `xFrac < 0.22` -> left, `proximity = 1 - xFrac/0.22`; `xFrac > 0.78` -> right, `proximity = (xFrac - 0.78)/0.22`; else center, `proximity = 0`.
- Variant C peek (maxPushFrac = 0.16): prev rests at `translateX(-100%)`, next at `translateX(100%)`. In the LEFT zone: `current -> translateX(p*16%)`, `prev -> translateX(-100% + p*100%)`. In the RIGHT zone: `current -> translateX(-p*16%)`, `next -> translateX(100% - p*100%)`. Center: current back to `translateX(0)`, prev/next off-screen. (Because prev/next slide at a 100% rate while current pushes at 16%, the adjacent layer always covers the vacated gap — no background shows.)
- Cursor CSS to port: ring at rest is `~68px` circle, `1.5px` white hairline border, `rgba(255,255,255,.14)` fill, white text label; edge pill is `~92x52px`, `border-radius:999px`, background+border `var(--current-accent)`, text/arrow `var(--current-accent-text)`; positioned by `transform: translate(x, y)` with `margin` offsets centering it on the hotspot; transitions `opacity 160ms`, `transform 160ms` (position trail) and `width/height/margin/border-radius/background 200ms` (the ring->pill morph). The arrow glyph is shown only in edge zones; the left arrow is `scaleX(-1)`.

REAL-SITE ADAPTATIONS (the sketch is a throwaway mock — adapt, don't copy verbatim):
- The label is bilingual: `OUVRIR` (FR) / `OPEN` (EN) — pass it as a frontmatter-derived value like the other labels (`toggleCarouselLabel` etc.), do NOT hardcode "OUVRIR". The label is PERMANENT in the center zone (confirmed feedback — there is no unlabeled ring state).
- The sketch uses `<div>` background-image slides; the real hero uses `<img>`. The peek layers are `<img>` (matching the `.home-hero__img` object-fit:cover pattern), sourced from the adjacent gallery's real `heroSrc`/`heroSrcSet`.
- The sketch's `goTo()`/`openCurrent()` are demo mocks. The real ones are `goToPrev()`/`goToNext()` (existing) and `openCurrent()` = `titleEl.click()` (reuses the real href + cross-document morph).
- Ignore the sketch's toolbar, variant tabs, `preventDefault`, `touch-action`, and the fake "Ouverture…" flash payoff.
</sketch_reference>

<tasks>

<task type="auto">
  <name>Task 1: Remove the scroll-to-open gesture and ALL its machinery; revert the img overhang and down-chevron cursor; drop the obsolete e2e blocks</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <action>
Grep the CURRENT file first (do not trust any prior summary's line numbers). This task is pure deletion + revert — no new behavior. After it, the carousel keeps working via the title link, keyboard, dashes, swipe, auto-advance, and footer-hide, just with no scroll gesture, no pull feedback, and the native default cursor over the photo.

In `src/components/HomeCarousel.astro` `<script>`, DELETE the entire overscroll block: constants `OPEN_OVERSCROLL_THRESHOLD`, `BOTTOM_EPSILON`, `RESET_IDLE_MS`, `OVERSCROLL_DECAY_MS`; state `overscrollAccum`, `lastOverscrollTs`, `decayRaf`, and the old `navigating` guard; functions `atBottom()`, `setPullFeedback()`, `decayOverscroll()`, `registerDownwardIntent()`, `navigateToCurrent()`; the proactive idle-decay `setInterval` watchdog; the window-level `scroll` listener, the window-level `wheel` listener, and the OVERSCROLL `touchstart`/`touchmove` listeners (the pair using `lastMoveY`/`overscrollTouchStartX`/`overscrollTouchStartY`). Do NOT delete the horizontal-swipe `touchstart`/`touchend` handlers on `.home-hero__photo` (they use `touchStartX/Y` + `detectSwipeDirection`) — those stay.

In `render()`: delete the pull/decay reset block at its top (`if (decayRaf !== null) { cancelAnimationFrame(...); decayRaf = null; } overscrollAccum = 0; setPullFeedback(0);`). Leave the rest of `render()` intact. In `showGrid()`: delete the `overscrollAccum = 0;` and `setPullFeedback(0);` lines (and their comments). Leave the display-mode/hidden/stopAutoAdvance logic intact.

In the scoped `<style>`: DELETE the `.home-hero__pull-overlay` rule; on `.home-hero__img` delete `transform: scale(var(--pull-scale, 1)); transform-origin: center; transition: transform 90ms ease-out;` and REVERT the box to the original pre-qem full cover — `position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover;` (remove `top:-4%; left:-4%; width:108%; height:108%` and rewrite the stale comment to describe a plain full-cover box, per qem's own note that `<img>` full-cover via `inset:0` + `100%/100%` is fine); on `.home-hero__img--sharp` revert its `transition` to `opacity 260ms ease` only (drop the `, transform 90ms ease-out`); DELETE the `.home-hero__photo.is-opening .home-hero__img, .home-hero__photo.is-opening .home-hero__pull-overlay { transition: none; }` rule; on `.home-hero__photo` delete the multi-line data-URI `cursor: url(...) 14 14, pointer;` declaration and the `background-color: #000;` declaration (both were pull/gesture machinery). In the `@media (prefers-reduced-motion: reduce)` block, delete the pull-specific rules: the `.home-hero__img, .home-hero__img--sharp { transition-property: opacity; }` override and the `.home-hero__pull-overlay { transition: none; }` rule (keep the progress-dash reduced-motion rules untouched). Update `syncWordmarkAlignment()`'s inline comment that references the `top:-4%...108%` box so it describes the reverted full-cover box.

DELETE the markup line `<div class="home-hero__pull-overlay" aria-hidden="true"></div>` (and its comment) inside `.home-hero__photo`.

In `tests/e2e/homepage.spec.ts`, DELETE three now-obsolete describe blocks in full: `carousel scroll-to-open (quick-260725-cfm, simplified quick-260725-pit)`, `carousel overscroll pull feedback (quick-260726-obg, sketch 007 Variant C)` (including its nested `rendered-pixel edge regression (quick-260726-qem)` sub-suite), and `carousel custom cursor affordance (Item 3)` (it asserts the down-chevron data-URI cursor that no longer exists). Do NOT touch the `footer visibility by display mode (quick-260726-obg)` block, the `carousel title has no underline (quick-260726-obg)` block, the two `full-bleed ... footer.toBeHidden()` tests, or the `carousel progress fill (quick-260725-dcg)` block — all remain valid. Do NOT touch `tests/e2e/i18n.spec.ts` or `tests/e2e/legal.spec.ts`.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5 && npm run build >/dev/null 2>&1 && echo BUILD_OK && grep -c "pull-overlay\|OVERSCROLL\|setPullFeedback\|registerDownwardIntent\|navigateToCurrent\|home-hero__img[^-].*108%\|108%" src/components/HomeCarousel.astro</automated>
  </verify>
  <done>astro check passes (0 errors); build succeeds; the grep count for removed tokens is 0; `.home-hero__img` uses `inset:0; width:100%; height:100%`; the three obsolete e2e describe blocks are gone; the footer-hide `is:global` rule and the swipe/keyboard/dash/auto-advance/title logic are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extract computeHoverZone() as a pure function with unit tests (mirroring the established pattern)</name>
  <files>src/lib/home-carousel.ts, tests/unit/home-carousel.test.ts</files>
  <action>
Follow the deliberate quick-260720-dzs pattern: the zone-detection math lives as a pure, DOM-free function in `src/lib/home-carousel.ts` next to `computeWordmarkBackgroundPosition`/`detectSwipeDirection`, unit-tested outside Playwright. The just-removed overscroll accumulator was the cautionary counter-example (never extracted, never unit-tested) — do not repeat that with the new logic.

In `src/lib/home-carousel.ts`, add:
- An exported interface `HoverZone { zone: 'center' | 'left' | 'right'; proximity: number; }`.
- An exported pure function `computeHoverZone(xFraction: number, edgeZoneFraction: number): HoverZone`. Faithful transcription of the sketch's zone math: if `xFraction < edgeZoneFraction` return `{ zone: 'left', proximity: 1 - xFraction / edgeZoneFraction }`; if `xFraction > 1 - edgeZoneFraction` return `{ zone: 'right', proximity: (xFraction - (1 - edgeZoneFraction)) / edgeZoneFraction }`; otherwise `{ zone: 'center', proximity: 0 }`. Add a short doc comment noting the caller passes `x / rect.width` (already in [0,1]) and `edgeZoneFraction` = 0.22 (confirmed sketch tuning). Do NOT add clamping or "improve" the formula — mirror the sketch exactly so the live behavior matches the approved design.

In `tests/unit/home-carousel.test.ts`, add a `describe('computeHoverZone', ...)` block in the same style as `detectSwipeDirection`, using `edgeZoneFraction = 0.22`, covering at least: center (e.g. `0.5` -> center/0), left interior (`0.11` -> left, proximity `0.5`), right interior (`0.89` -> right, proximity `0.5`), the exact left boundary (`0.22` -> center/0, proving `<` not `<=`), the exact right boundary (`0.78` -> center/0), the extreme left edge (`0` -> left, proximity `1`), and the extreme right edge (`1` -> right, proximity `1`). Use `toBeCloseTo` for the fractional proximities to avoid float-equality flakiness.
  </action>
  <verify>
    <automated>npm run test:unit -- home-carousel 2>&1 | tail -20</automated>
  </verify>
  <done>All `computeHoverZone` unit tests pass; the existing `computeWordmarkBackgroundPosition`/`detectSwipeDirection` tests still pass; `computeHoverZone` is exported and imported by nothing yet (wired in Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Build the custom hover cursor + zone detection + visual states (center OUVRIR ring / edge accent arrow pill), desktop-gated</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <action>
Build the cursor system from sketch 008 Variant C, gated to real mouse desktop only. No click/peek yet (Tasks 4-5) — this task delivers the visible affordance.

Frontmatter: add a bilingual label `const openLabel = isEn ? 'OPEN' : 'OUVRIR';` alongside the other label consts.

Markup: inside `.home-hero__photo`, after `.home-hero__caption`, add the cursor element (aria-hidden, non-interactive):
`<div class="home-hero__cursor" data-role="hero-cursor" data-zone="center" aria-hidden="true"><span class="home-hero__cursor-label">{openLabel}</span><span class="home-hero__cursor-arrow" aria-hidden="true">&#8594;</span></div>`.

CSS (scoped `<style>`, gated so it NEVER activates on touch): wrap the cursor visuals and `cursor: none` in `@media (hover: hover) and (pointer: fine) { ... }`. Inside that media block:
- `.home-hero__photo { cursor: none; }` (hide the native cursor on real-mouse desktop only — coarse-pointer keeps the default cursor).
- `.home-hero__cursor`: `position:absolute; top:0; left:0; z-index:4;` (above the scrim, at/above the caption's z-index:3 but `pointer-events:none` so it never blocks clicks), `pointer-events:none;`, flex-center its label/arrow, ring geometry (~68px square, `margin:-34px 0 0 -34px` to center on the hotspot), `border:1.5px solid rgba(255,255,255,.9); background:rgba(255,255,255,.14); color:#fff; border-radius:999px;`, small semibold letter-spaced label font, `opacity:0` at rest, and the sketch's transitions: `opacity 160ms ease, transform 160ms cubic-bezier(.2,.8,.2,1), width/height/margin/border-radius/background/border-color 200ms ...`. A `.home-hero__photo:hover .home-hero__cursor` (or a JS-toggled `.is-active` class on the photo) sets `opacity:1`.
- Edge state via the `[data-zone="left"]`/`[data-zone="right"]` attribute: pill geometry (~92x52px, `margin:-26px 0 0 -46px`), `background:var(--current-accent); border-color:var(--current-accent); color:var(--current-accent-text);`. Show `.home-hero__cursor-arrow` only in edge zones; left zone flips it with `transform:scaleX(-1)`. The center label stays visible in the center zone (permanent — no unlabeled state); hide the label in edge zones (arrow only). Add a `@media (prefers-reduced-motion: reduce)` override inside the hover block dropping the cursor's own transitions to instant.

Script (reuse the already-declared `heroPhoto`; guard everything behind a single `const hoverCapable = window.matchMedia('(hover: hover) and (pointer: fine)').matches;`): only when `hoverCapable && heroPhoto` wire the cursor. Look up `const cursorEl = heroPhoto.querySelector('[data-role="hero-cursor"]')`. Declare module-scope `let currentZone: 'center'|'left'|'right' = 'center'; let currentProximity = 0;` (these are the single source of truth Tasks 4-5 also read). Import `computeHoverZone` from `../lib/home-carousel`. On `heroPhoto` `mousemove`: compute `rect = heroPhoto.getBoundingClientRect()`, `x = e.clientX - rect.left`, `y = e.clientY - rect.top`; set `cursorEl.style.transform = translate(${x}px, ${y}px)`; call `computeHoverZone(x / rect.width, 0.22)`, store `currentZone`/`currentProximity`, set `cursorEl.dataset.zone = currentZone`. On `mouseenter` add an `.is-cursor-active` class to `heroPhoto` (or toggle cursor opacity); on `mouseleave` remove it and reset `currentZone='center'; currentProximity=0; cursorEl.dataset.zone='center'` (so the next enter starts clean). Use `EDGE_ZONE_FRACTION = 0.22` as a named const in the script.

e2e (`tests/e2e/homepage.spec.ts`), new describe `carousel hover cursor (sketch 008 Variant C)` — default desktop chromium project reports hover:hover + pointer:fine, so the cursor activates:
- Center: `page.goto('/')`, `mouse.move` to the photo's horizontal center, assert `[data-role="hero-cursor"]` has `data-zone="center"`, its label reads `OUVRIR`, and it is visible (opacity 1 after entering). Repeat on `/en/` asserting label `OPEN`.
- Left edge: move to `~5%` of the photo width, assert `data-zone="left"`, the arrow span is visible, and the cursor background computed color equals the current `--current-accent` (read the resolved value off `.home`).
- Right edge: move to `~95%`, assert `data-zone="right"` and arrow visible.
- Assert the native cursor is hidden: `getComputedStyle(.home-hero__photo).cursor === 'none'`.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5 && npm run build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <done>Build + astro check pass; on desktop the cursor follows the pointer, shows OUVRIR/OPEN in the center and an accent arrow pill at both edges, and the native cursor is `none`; the new cursor e2e block passes; nothing renders/activates on touch (gated by the media query + matchMedia).</done>
</task>

<task type="auto">
  <name>Task 4: Parallax-push peek preview of the adjacent gallery (real photos), render() integration, reduced-motion + snapshot-reset hook</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <action>
Add the Variant C parallax peek: as the pointer nears an edge, the current photo peels away and the adjacent gallery's REAL photo is revealed underneath. Desktop-gated (same `hoverCapable` guard).

Markup: inside `.home-hero__photo`, BEFORE the current photo layers (`.home-hero__img-placeholder`/`--sharp`) so they sit UNDERNEATH, add two peek layers:
`<img data-role="peek-prev" alt="" aria-hidden="true" class="home-hero__peek home-hero__peek--prev" />` and `<img data-role="peek-next" alt="" aria-hidden="true" class="home-hero__peek home-hero__peek--next" />`.

CSS: `.home-hero__peek { position:absolute; inset:0; width:100%; height:100%; object-fit:cover; z-index:0; transition: transform 420ms cubic-bezier(.2,.8,.2,1); will-change: transform; }` with `.home-hero__peek--prev { transform: translateX(-100%); }` and `.home-hero__peek--next { transform: translateX(100%); }` as the resting off-screen positions. Ensure the current `.home-hero__img` layers sit above the peek (z-index >= 1) and give `.home-hero__img` the parallax transform + easing: `transform: translateX(var(--peek-shift, 0)); transition: transform 420ms cubic-bezier(.2,.8,.2,1);` (and retune `.home-hero__img--sharp`'s transition to `opacity 260ms ease, transform 420ms cubic-bezier(.2,.8,.2,1)` so the crossfade AND push both animate). Re-add a repurposed commit-reset hook: `.home-hero__photo.is-opening .home-hero__img, .home-hero__photo.is-opening .home-hero__peek { transition: none; }` (used by Task 5's openCurrent to snapshot-clean the cross-document morph). Under `@media (prefers-reduced-motion: reduce)` disable the peek push transitions (`.home-hero__peek, .home-hero__img { transition: none; }`, keeping `.home-hero__img--sharp { transition-property: opacity; }` for the crossfade) — and see the JS reduced-motion branch below.

Script (inside the `hoverCapable && heroPhoto` block): look up `const peekPrev = heroPhoto.querySelector('[data-role="peek-prev"]')` and `peekNext`. Add helpers:
- `resetPeek()`: `heroPhoto.style.setProperty('--peek-shift', '0'); peekPrev.style.transform = 'translateX(-100%)'; peekNext.style.transform = 'translateX(100%)';`
- `updatePeek()`: if `reduceMotionQuery.matches`, call `resetPeek()` and return (no parallax under reduced motion — it is decorative; the cursor arrow still signals direction). Otherwise port the sketch's Variant C math with `maxPushFrac = 0.16`: LEFT zone -> `--peek-shift` = `${currentProximity * 16}%`, `peekPrev.transform = translateX(${-100 + currentProximity * 100}%)`, `peekNext` rest; RIGHT zone -> `--peek-shift` = `${-currentProximity * 16}%`, `peekNext.transform = translateX(${100 - currentProximity * 100}%)`, `peekPrev` rest; center -> `resetPeek()`.
Call `updatePeek()` at the end of the `mousemove` handler (right after storing `currentZone`/`currentProximity`) and call `resetPeek()` in the `mouseleave` handler. 

Populate the peek image sources from real data in `render()`: after the current-gallery image swap, set `peekPrev.src`/`peekPrev.srcset`/`peekPrev.sizes` from `galleries[(carouselIndex - 1 + galleries.length) % galleries.length]` and `peekNext` from `galleries[(carouselIndex + 1) % galleries.length]` (use `heroSrc`/`heroSrcSet`/`'100vw'`), then `resetPeek()`. Guard the whole peek-src assignment behind `hoverCapable` (skip the extra image loads on touch). Because `render()` runs on init and every swap, the peek always tracks the current neighbours. Also call `resetPeek()` inside `showGrid()` (guarded by `hoverCapable`) so a lingering peek can't survive a mode switch. With N galleries the modulo wraps correctly (with N=2, prev and next resolve to the same gallery — fine).

e2e (`tests/e2e/homepage.spec.ts`), new describe `carousel edge-peek preview (sketch 008 Variant C)`:
- At rest / center: assert `[data-role="peek-prev"]` and `[data-role="peek-next"]` are at their off-screen resting transform (translateX ±100%) — not visibly peeking. Move to center, assert `--peek-shift` on `.home-hero__photo` resolves to `0`.
- Approaching the LEFT edge: read the gallery order from `ul[data-role="home-carousel-data"] li` (the prev of index 0 = last entry). Pin the slide (click autoplay-toggle). `mouse.move` to `~3%` of the photo width; assert `peek-prev`'s `src` equals the expected adjacent gallery's `data-hero-src` (a `cdn.sanity.io` URL — a REAL photo, not a placeholder color), and assert the current `.home-hero__img--sharp` has a non-zero positive `translateX` (the push) and/or `--peek-shift` is a positive percentage. Move progressively closer (`~1%`) and assert the push magnitude increases (proportional to proximity).
- Approaching the RIGHT edge: mirror the above for `peek-next` and a negative push.
- Assert this whole block is desktop-only in spirit by keeping it in the default chromium (hover) project; add a mobile check in Task 5 that the peek layers never move on touch.
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5 && npm run build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <done>Build + astro check pass; on desktop hover the current photo peels away proportionally and the adjacent gallery's real photo (cdn.sanity.io src, matching the prev/next data entry) is revealed; at rest/center no peek shows; the peek tracks slide changes via render(); reduced motion disables the parallax; the peek e2e block passes.</done>
</task>

<task type="auto">
  <name>Task 5: Click behavior (center opens current, edges navigate prev/next) + mobile tap-to-open fallback + full e2e coverage</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <action>
Wire the interaction and the touch fallback; this completes the feature.

Script — `openCurrent()` (module-scope, works on both desktop and touch): add `let opening = false;`. `function openCurrent() { if (opening) return; opening = true; if (heroPhoto) { heroPhoto.classList.add('is-opening'); heroPhoto.style.setProperty('--peek-shift', '0'); void heroPhoto.offsetWidth; } if (titleEl) { titleEl.click(); } else { const href = galleries[carouselIndex]?.href; if (href) window.location.href = href; } }`. This reuses the title link's existing click listener (which calls `setCrossDocPhoto(heroImg)`) AND its real href — do NOT duplicate the routing or the morph naming. The synchronous `--peek-shift: 0` + `.is-opening` (transition disabled) + forced reflow guarantee the outgoing cross-document `hero-photo` View Transition snapshot captures the photo at rest even if an edge-peek push was still easing back.

Desktop click (inside the `hoverCapable && heroPhoto` block): `heroPhoto.addEventListener('click', (event) => { const target = event.target as HTMLElement; if (target.closest('.home-hero__caption')) return; if (currentZone === 'left') { goToPrev(); } else if (currentZone === 'right') { goToNext(); } else { openCurrent(); } });`. The `.home-hero__caption` early-return lets the title link, progress dashes, and autoplay toggle keep their own handlers (they must NOT be hijacked by zone navigation).

Mobile tap-to-open — extend the EXISTING horizontal-swipe `touchend` handler (do NOT add a second touch listener): after computing `direction = detectSwipeDirection(deltaX, deltaY, SWIPE_MIN_DISTANCE, SWIPE_DIRECTION_RATIO)`, keep `if (direction === 'next') { goToNext(); return; }` / `if (direction === 'prev') { goToPrev(); return; }`, then add: `const TAP_MAX_MOVEMENT = 10; if (Math.abs(deltaX) <= TAP_MAX_MOVEMENT && Math.abs(deltaY) <= TAP_MAX_MOVEMENT) { openCurrent(); }`. A genuine tap (negligible movement) opens; a real horizontal swipe navigates (unchanged); a vertical scroll-drag (large deltaY) is neither and does nothing — so tap-to-open never fires on a scroll and never conflicts with the swipe.

e2e (`tests/e2e/homepage.spec.ts`), new describe `carousel hover-click navigation (sketch 008 Variant C)`:
- Center click opens the current gallery: pin the slide, read the title's `href`, `mouse.move` to the photo center, `mouse.down()/up()` (or `page.locator('.home-hero__photo').click({ position: center })`) and assert the URL becomes that gallery detail href (same destination as clicking the title). Do it on FR and EN.
- Edge click navigates: at 1600px width, pin the slide, capture the index label, `mouse.move` to `~3%` then click — assert the index/title changed to the previous gallery; repeat at `~97%` for next. (Assert against the `[data-role="index-label"]` / `[data-role="gallery-title"]` text, not navigation — edge clicks are in-page carousel moves.)
- Clicking the title/dash/autoplay is NOT hijacked: click the autoplay-toggle in the center region and assert it toggles pause (URL unchanged, no navigation).
- Keyboard / dash / auto-advance spot-check (not full re-coverage): ArrowRight advances the index; clicking dash n selects gallery n; the mocked-clock auto-advance still ticks — a light assertion each, proving these untouched paths still work.
- Mobile (use the `devices['iPhone 14 Pro']` descriptor minus `defaultBrowserType`, matching the existing `mobile full-bleed hero regression` block, so pointer:coarse/hover:none): (a) a tap on the photo (dispatch touchstart+touchend at the same point, or `page.tap('.home-hero__photo')` with negligible movement) opens the current gallery; (b) a real horizontal swipe still navigates prev/next unchanged (mirror the deleted swipe test's synthetic Touch dispatch, or use `page.mouse`/touch drag); (c) assert the peek layers never leave their resting transform on touch (no hover system).
  </action>
  <verify>
    <automated>npx astro check 2>&1 | tail -5 && npm run build >/dev/null 2>&1 && echo BUILD_OK</automated>
  </verify>
  <done>Build + astro check pass; center-click opens the current gallery (same destination + morph as the title), edge-clicks navigate prev/next, caption controls are not hijacked, keyboard/dash/auto-advance still work, mobile tap opens + swipe navigates unchanged + peek stays inert on touch; all new e2e blocks pass FR + EN.</done>
</task>

</tasks>

<verification>
Run the full gate the orchestrator will re-run independently:
- `npx astro check` — 0 errors (pre-existing unrelated warnings about `webkitBackgroundClip`/unused `Props` in untouched files are acceptable).
- `npm run test:unit` — all pass, including the new `computeHoverZone` cases. (Note: a pre-existing, unrelated `tests/unit/dashboard-logic.test.ts` load failure from a missing `@sanity/icons` in the `sanity/` subproject may appear — it is NOT caused by this task; do not fix it here, log it to deferred-items.md if it surfaces.)
- `npm run build` — succeeds against real Sanity content.
- `npx playwright test` on an isolated port (temporary local port override, reverted, not committed) — both `chromium` and `webkit-mobile` projects green, including all new cursor / peek / click / mobile-tap blocks and the untouched footer-visibility, title-underline, progress-fill, full-bleed, i18n, and legal suites.
- Confirm by grep that no `--pull-scale`, `--pull-darken`, `.home-hero__pull-overlay`, `OVERSCROLL`, `atBottom`, `registerDownwardIntent`, or `navigateToCurrent` token remains in `HomeCarousel.astro`, and that `.home-hero__img` no longer carries a `108%` / `-4%` overhang.

The orchestrator will additionally do a LIVE Playwright MCP browser check with REAL SCREENSHOTS of: the center OUVRIR ring, the left/right accent arrow pills, and the peek parallax mid-animation (both directions) — the computed-style/DOM-state gap that let the black-frame bug ship in quick-260726-qem is explicitly being closed by real-pixel human verification, so ensure the visuals are genuinely correct, not merely test-passing.
</verification>

<success_criteria>
- The scroll-to-open gesture and every piece of its machinery are gone; scrolling the homepage never navigates.
- The sketch 008 Variant C hover interaction works on desktop: permanent OUVRIR/OPEN center ring, 22% edge zones with an accent-tinted directional arrow pill, and a real-photo parallax peek of the adjacent gallery proportional to proximity.
- Center-click opens the current gallery (same destination + cross-document morph as the title); edge-clicks navigate prev/next.
- Touch devices: the hover system is inert; a tap opens the current gallery; the existing swipe still navigates prev/next.
- `.home-hero__img` overhang reverted to full cover (sharper framing, no rest-zoom); footer-hide kept and now risk-free.
- All untouched paths (keyboard, dashes, auto-advance, title morph, accent/wordmark visuals, i18n/legal footer tests) still pass, FR + EN.
- `computeHoverZone` is a pure, unit-tested function in `src/lib/home-carousel.ts`.
</success_criteria>

<output>
Create `.planning/quick/260726-u97-remove-the-scroll-to-open-gesture-entire/260726-u97-SUMMARY.md` when done. In it, explicitly document (per the task's requirement): the footer-hide simplification reasoning (kept, now risk-free), exactly what CSS/JS was deleted vs. kept vs. reverted, and how the mobile tap-to-open fallback composes with the existing swipe detection without conflict.
</output>
