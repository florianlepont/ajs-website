---
phase: quick-260813-gw2
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - sanity/editorial/pipelineView.ts
  - sanity/editorial/deployment.ts
  - sanity/editorial/EditorialDashboard.tsx
  - sanity/README.md
  - tests/unit/pipeline-view.test.ts
  - tests/unit/deployment.test.ts
  - tests/unit/editorial-dashboard-markup.test.ts
autonomous: true
requirements:
  - QUICK-260813-gw2
must_haves:
  truths:
    - "CHANGE 1 — the panel has exactly ONE action button. The top button's label, tone, disabled, loading and click target all evolve with the workflow stage, computed by a new pure `releaseActionButtonState()` in `pipelineView.ts`. The user's words: « je m'attendais à ce que le bouton grisé 'mettre à jour le site' devienne 'publier sur le site', qu'il évolue dynamiquement en fonction du workflow »."
    - "CHANGE 1 priority order IS the contract, top wins: (1) publishing drafts → 'Vérification…'/'Publication…' ; (2) production release in flight → 'Publication…' ; (3) staging ready → 'Publier sur le site en ligne' ; (4) drafts pending → 'Mettre le site à jour' enabled ; (5) everything else → 'Mettre le site à jour' disabled."
    - "CHANGE 1 introduces ZERO new user-facing strings. All four labels already exist verbatim in the codebase today. No new copy is written anywhere."
    - "THE SAFETY GATE SURVIVES INTACT — in the ready state the merged button shows « Publier sur le site en ligne » but stays DISABLED until `hasPreviewedStaging` is true, i.e. until the round gate has been clicked to open the site de test. `productionPublishDisabled()` is reused unchanged. The gate is enforced TWICE in the helper's output (`disabled: true` AND `action: 'inert'`), so a single mis-wire in the component cannot publish to production without a preview."
    - "CHANGE 2 — the second « Publier sur le site en ligne » `<Button>` inside the promote box is REMOVED, and `promoteActionsBoxHasBody` simplifies back to `Boolean(pipeline.promote.actionUrl)` only. The box itself and its failure/staging run link survive: that link is still the ONLY way a maintainer reaches the GitHub Actions run for a broken deploy."
    - "CHANGE 2 scope — `runPublication()` and `triggerProductionReleaseClick()` are reused verbatim, not rewritten. The round gate button, its caption, `handleGateClick()`, `pipelineGateVariant()`, `gateClickAction()`, `nextPreviewedFlag()`, `productionPublishDisabled()`, the whole preview/approval mechanism and the panel header subtitle (`releasePanelSubtitle()`) are all UNCHANGED."
    - "CHANGE 3 — in `deploymentState()`'s no-qualified-run branch, `kind` becomes `'failed'` instead of `'waiting-run'` once `timedOut` is true (>= 3 minutes with still no run). ONLY the `kind` field changes; `label`, `detail`, `tone`, `terminal`, `actionLabel` and `actionUrl` are byte-identical. This makes `deploymentSegmentKind()` paint the stuck stage RED instead of leaving it identical to a genuinely-running deploy (blue, spinning) forever."
    - "CHANGE 3 — `terminal` stays `false` on that branch on purpose. Polling continues at 15s, so if a run appears late the state self-heals back to 'deploying'/'current'. Flipping `terminal` to `true` would freeze the poll at 5 minutes and strand a recoverable state as permanently red. A test asserts `terminal === false`."
    - "CHANGE 3 does NOT touch the `qualifiedRun.status !== 'completed'` branch (kind `'deploying'`). A run that WAS found and is genuinely mid-flight — like the real 5.5-minute production run observed today — stays blue and spinning. The 3-minute timeout is about a run never APPEARING, never about a run taking long to FINISH."
    - "Failure states are explicitly OUT OF SCOPE for the merge: with `gateVariant === 'failed'` the merged button keeps its ordinary drafts-pending/disabled 'Mettre le site à jour' behaviour. Retry stays exclusively on the round gate (`gateClickAction('failed') → 'release'`). No second retry affordance is added, and the helper never returns a red/critical tone."
    - "`PipelineSegmentKind` still has exactly four values and `PipelineGateVariant` exactly five. Change 3 reassigns an EXISTING `DeploymentStateKind` value in one branch; no union is widened."
    - "No test is deleted. Four existing guards assert behaviour this plan changes (two cases in `tests/unit/deployment.test.ts`, two describes in `tests/unit/editorial-dashboard-markup.test.ts`); each is re-pointed at the new contract with a comment recording why it changed."
    - "`dashboardLogic.ts`, `releaseGate.ts`, `EditorialDashboard.css` and `.github/workflows/` are provably untouched by `git diff` against the recorded base SHA."
    - "`npm run test:unit`, `npm run typecheck`, `npm --prefix sanity run lint` and `npm --prefix sanity run build` all pass."
    - "The Studio is redeployed via `npm run deploy --prefix sanity`, and the SUMMARY ends with the three-part live confirmation request (a/b/c), including the instruction to leave the tab open rather than refreshing while watching a long production release."
  artifacts:
    - sanity/editorial/pipelineView.ts
    - sanity/editorial/deployment.ts
    - sanity/editorial/EditorialDashboard.tsx
    - sanity/README.md
    - tests/unit/pipeline-view.test.ts
    - tests/unit/deployment.test.ts
    - tests/unit/editorial-dashboard-markup.test.ts
  key_links:
    - "THE ONE THING THAT MUST NOT BREAK: after Task 3 deletes the promote-box button, the merged top button is the ONLY control in the entire Studio that starts a real production release. If its `action: 'release'` routing is mis-wired, there is no second way to publish the site. Task 1's helper tests, Task 3's markup assertions on `triggerProductionReleaseClick()` call-site count, and Task 5's pre-deploy JSX read all exist for this single failure mode."
    - "ORDERING TRAP (same one g49 hit): `publicationCard` is declared ~line 536, but `gateVariant` / `promoteActionsBoxHasBody` are ~lines 440/473 — about 70 lines EARLIER. `releaseActionButtonState()` needs `publicationCard.total` AND `publicationCard.buttonDisabled`, so its derivation must sit next to `panelSubtitle` (~line 551), NOT next to `gateVariant`. Nothing else moves and no hook is reordered."
    - "TDZ TRAP: `triggerProductionReleaseClick` is declared ~line 476 but `runPublication` ~line 568. The new `handleReleaseActionClick` closes over both, so declare it AFTER `refreshPublicationTracking` (~line 590), immediately before the `return (`. It is only invoked at click time, so ordering is about readability and lint, not correctness — but put it there anyway."
    - "COMMENT-TEXT TRAP: `tests/unit/editorial-dashboard-markup.test.ts` currently asserts the literal « Publier sur le site en ligne » appears EXACTLY ONCE in `EditorialDashboard.tsx`. After this plan it must appear ZERO times there — the literal lives in the tested helper. Do NOT quote that label in any new comment in `EditorialDashboard.tsx`; a comment mention breaks the re-pointed assertion. Same trap for « Vérification… », which the new markup test greps for absence."
    - "TEST-FIXTURE TRAP: two existing cases in `tests/unit/deployment.test.ts` call `deploymentState()` WITHOUT a `now` argument while their fixture publication timestamp (2026-07-29) is months in the past, so `timedOut` is silently true today. The case at ~line 96 ('uses the newest valid local or remote publication timestamp across tabs') currently passes only because a timed-out state still returned `'waiting-run'`; Change 3 flips it to `'failed'` and it breaks. Fix it by passing an explicit `now`, not by changing its expectation — that test is about timestamp SELECTION, not about the timeout."
    - "ACCEPTED CONSEQUENCE of Change 3, must be recorded in the SUMMARY, not 'fixed': a timed-out STAGING deploy now produces `segments.staging === 'failed'` → `displaySegments.testSite === 'failed'` → `pipelineGateVariant()` returns `'failed'` → the round gate flips from locked to a red retry that routes to `triggerProductionReleaseClick()`. This is the EXACT behaviour a genuinely-failed staging run already has today; the kind reassignment simply routes the stuck case down the same existing path. Changing it would require editing `releaseGate.ts`, which is out of scope."
    - "WELCOME CONSEQUENCE of Change 3, worth stating in the SUMMARY: with `segments.staging === 'failed'`, `resolvePromoteRow()` takes its staging-failed branch and sets `actionUrl`/`actionLabel` from the staging state — so the stuck case now renders a « Prévenir le mainteneur » link to the workflow page in the promote box, where previously it rendered nothing at all."
    - "`pipelineView.ts` must stay runtime-import-free (its own header states the rule, and the root Vitest project cannot resolve `sanity/`'s deps). The new helper takes `productionPublishBlocked` as a plain BOOLEAN input — the component calls `productionPublishDisabled()` and passes the result in. Do NOT add a value import of `./releaseGate` to `pipelineView.ts`."
    - "Tone deviation, deliberate and recorded: the return type's tone union is `'primary' | 'positive'`, NOT `'primary' | 'positive' | 'critical'`. A `critical` tone would only ever mean a retry, and this task states that retry stays exclusively on the round gate — an unreachable union member invites exactly the second retry control the constraints forbid. A matrix test asserts no input combination ever yields anything but the two allowed tones. `positive` is an established Sanity UI theme token already used across `sanity/editorial/` (`DeploymentTone`, `DashboardTone`, several `Badge tone=` sites), so no new colour is introduced."
    - "Typography must match the existing files exactly or the string-equality tests fail: curly apostrophe U+2019 (’), single-character ellipsis U+2026 (…), em dash U+2014 (—), French guillemets « » with normal spaces — as already used throughout `deployment.ts`, `pipelineView.ts` and `EditorialDashboard.tsx`."
---

<objective>
The user tested the deployed Studio dashboard live and reported that they expected the greyed « Mettre le site à jour » button to BECOME « Publier sur le site en ligne » as the workflow advances — one control that evolves — rather than a second, separate button appearing in a box below the pipeline once the site de test is ready.

Three coupled changes:

1. Add a pure, tested `releaseActionButtonState()` helper to `pipelineView.ts` that computes the merged button's label, tone, disabled, loading and click routing from the current workflow stage, in a strict priority order. Following the presentational-helper extraction pattern established earlier today (`pipelineCircleClassName`, `pipelineNodeDetail`, `pipelineGateCaption`, `releasePanelSubtitle`), with the same rigour of explicit priority-order tests.
2. Rewire `EditorialDashboard.tsx` so the existing top button is the SOLE button, driven by that helper and routed to the correct existing handler; delete the second button from the promote box and simplify the box guard back to the action link alone.
3. Fix a real, separately-confirmed state-tracking gap found while investigating the report: after the 3-minute no-run timeout, `deploymentState()` returns `kind: 'waiting-run'` with `tone: 'critical'`, but `deploymentSegmentKind()` branches only on `kind` — so a deploy that never started renders identically to one still running (blue, spinning) forever. Reassigning that one branch's `kind` to `'failed'` makes the stuck case red, with no other code change.

Purpose: the two-button design forced the maintainer to hunt for a control that appeared somewhere else on the page; and the pipeline could not tell « still deploying normally » apart from « something is stuck and needs attention ».

Explicitly NOT in scope: the two underlying actions stay technically distinct (publishing Sanity drafts and triggering a production release are different Sanity Actions API calls with different consequences) — this plan merges the UI only. The preview-before-publish safety gate stays fully intact. Failure retry stays exclusively on the round gate button.

Output: a new tested helper in `pipelineView.ts`, a one-field fix plus test coverage in `deployment.ts`, a single evolving button in `EditorialDashboard.tsx`, four re-pointed test guards, a resynced README, green gates, a redeployed Studio, and a live confirmation request covering three workflow moments.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@sanity/editorial/pipelineView.ts
@sanity/editorial/deployment.ts
@sanity/editorial/releaseGate.ts
@sanity/editorial/EditorialDashboard.tsx
@sanity/README.md
@tests/unit/pipeline-view.test.ts
@tests/unit/deployment.test.ts
@tests/unit/release-gate.test.ts
@tests/unit/editorial-dashboard-markup.test.ts
</context>

<scope_boundary>
Base SHA for every scope proof in this plan: **`2bad450287ebdd7a1dc7ca3ac515625686123b05`**.

Do NOT modify:

- `sanity/editorial/dashboardLogic.ts` — including `buildProductionReleaseMarkerActions()`, `triggerProductionRelease()`, `publicationCardState()`, `createPublicationController()`, `publishAfterPreflight()`. This plan only READS `publicationCard.total` and `publicationCard.buttonDisabled`.
- `sanity/editorial/releaseGate.ts` — `gateClickAction()`, `nextPreviewedFlag()`, `productionPublishDisabled()` and the `PipelineGateVariant` type stay byte-identical. Type-only imports from it are allowed; edits are not.
- `sanity/editorial/EditorialDashboard.css` — no style change is needed and no new colour may be introduced. The merged button keeps `style={{minHeight: 44}}` and a Sanity UI `tone` token.
- `.github/workflows/` — anything.
- The `PipelineSegmentKind` union (four values) and the `PipelineGateVariant` union (five values). No widening.
- In `deployment.ts`: everything except the single `kind:` property on the no-qualified-run return object. `resolvePromoteRow()`, `releasePipelineState()`, `deploymentSegmentKind()`, `pipelineDisplaySegments()`, `isProductionReleaseStale()`, `deploymentTargetConfig`, `nextDeploymentPollDelay()` and the `qualifiedRun.status !== 'completed'` branch are all untouched.
- In `pipelineView.ts`: `pipelineCircleClassName()`, `pipelineNodeDetail()`, `pipelineGateCaption()` and `releasePanelSubtitle()` stay exactly as they are. This plan only ADDS a fifth function.
- In `EditorialDashboard.tsx`: `pipelineGateVariant()` (~line 1299), `pipelineNodeIcon()`, `handleGateClick()`, `triggerProductionReleaseClick()`, `runPublication()`, `refreshPublicationTracking()`, the `nextPreviewedFlag` effect, the two GitHub polling effects, the two pipeline node blocks, the round gate button block and its caption, the panel `<Heading>` text, the `panelSubtitle` derivation and its `<Text>`, and the PAGE header subtitle (`const subtitle`, ~line 526).
- `tests/unit/release-gate.test.ts` — this plan changes nothing it asserts. Leave the file alone; it is in `<context>` for understanding only.

Do NOT delete any existing guard test. Four of them assert behaviour this plan deliberately changes; re-point each at the new behaviour with a comment recording why it moved. A deleted guard is an untested surface.

Do NOT shortcut, weaken or bypass `hasPreviewedStaging`. It was built earlier this session at explicit request so a maintainer cannot publish to production without first opening the site de test.

Do NOT add a retry affordance to the merged button in any failure state.
</scope_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipelineView.ts — add the tested releaseActionButtonState() helper that drives the single evolving button</name>
  <files>sanity/editorial/pipelineView.ts, tests/unit/pipeline-view.test.ts</files>
  <behavior>
    - Publishing drafts wins over everything: the label reads the preflight text while preflighting and the publish text otherwise, and the button is disabled + loading.
    - A production release in flight (either the trigger request or an observed in-flight run) shows the publish text, disabled + loading, in the go-live tone.
    - Staging ready shows the go-live label; it is clickable ONLY when the preview approval is recorded, and its routing is inert otherwise.
    - Drafts pending shows the update label, enabled unless the publication card blocks it, and routes to the draft-publish action.
    - Nothing pending shows the update label, disabled, routing inert — in every remaining variant including `failed`.
    - No input combination ever yields a red/critical tone, and no input combination ever yields the release routing without a recorded preview approval.
  </behavior>
  <action>
Add a fifth exported function to `sanity/editorial/pipelineView.ts`, at the end of the file, leaving the four existing helpers untouched. It must obey the module's existing header rule: no React, no `@sanity/ui`, no `sanity`, type-only imports only. It needs `PipelineGateVariant`, which the file already type-imports from `./releaseGate` — do NOT add a value import of that module, and do NOT change either existing `import type` statement.

Export a small result interface, then the function:

  - `ReleaseActionButtonTone` = `'primary' | 'positive'`.
  - `ReleaseActionButtonAction` = `'publish' | 'release' | 'inert'`. Use the word `inert` deliberately, matching the vocabulary `GateClickAction` in `releaseGate.ts` already uses for "this click does nothing".
  - `ReleaseActionButtonState` = `{label: string; disabled: boolean; loading: boolean; tone: ReleaseActionButtonTone; action: ReleaseActionButtonAction}`.
  - `releaseActionButtonState({gateVariant, publicationBusy, preflighting, releaseBusy, modifiedCount, publishButtonDisabled, productionPublishBlocked})` with those seven inputs, all required, typed `PipelineGateVariant` / `boolean` / `number` as appropriate. Name `modifiedCount` to match `releasePanelSubtitle`'s existing parameter, since both are fed by the same `publicationCard.total` signal.

Five branches, checked top to bottom, first match wins:

  1. `publicationBusy` → label `preflighting ? 'Vérification…' : 'Publication…'`, `disabled: true`, `loading: true`, `tone: 'primary'`, `action: 'inert'`.
  2. `releaseBusy || gateVariant === 'active'` → label `'Publication…'`, `disabled: true`, `loading: true`, `tone: 'primary'`, `action: 'inert'`.
  3. `gateVariant === 'ready'` → label `'Publier sur le site en ligne'`, `disabled: productionPublishBlocked`, `loading: false`, `tone: 'positive'`, `action: productionPublishBlocked ? 'inert' : 'release'`.
  4. `modifiedCount > 0` → label `'Mettre le site à jour'`, `disabled: publishButtonDisabled`, `loading: false`, `tone: 'primary'`, `action: publishButtonDisabled ? 'inert' : 'publish'`.
  5. fallback → label `'Mettre le site à jour'`, `disabled: true`, `loading: false`, `tone: 'primary'`, `action: 'inert'`.

All four labels already exist verbatim in the codebase — three in the button ternary Task 3 removes, one in `resolvePromoteRow()`. This helper writes no new user-facing copy.

Implementation notes, each of which must be reflected in comments on the function:

  - Branch order IS the contract, and it encodes what the maintainer is currently doing rather than what the pipeline currently shows. Record that this is the whole point of the merge: the user expected one control that evolves through the workflow instead of a second button appearing elsewhere on the page.
  - Branch 1 hardcodes `disabled: true` rather than forwarding `publishButtonDisabled`. Note that this is provably identical to today's behaviour, not a change: `publicationCardState()` already sets `buttonDisabled` to `busy || trackingFailed || total === 0 || blockedRows.length > 0`, so it is always true while `publicationBusy` holds. Hardcoding states the intent (a running publish is never clickable) instead of relying on a term of an expression owned by another module.
  - Branch 2 is the NEW behaviour the top button gains: until now only the removed bottom button and the round gate reflected the production-release phase. `releaseBusy` covers the few hundred milliseconds of the trigger request; `gateVariant === 'active'` covers the minutes-long observed run that follows. Both must be here — the real run observed today took about 5.5 minutes, and `releaseBusy` alone would leave the button idle for nearly all of it.
  - Branch 2's tone is `'primary'`, deliberately NOT `'positive'`, even though branch 3 right below it is `'positive'`. The outcome of an in-flight release is still unknown — `'positive'` already has an established meaning of "succeeded" in this codebase (`deploymentState()` sets `tone: 'positive'` only once `qualifiedRun.conclusion === 'success'`), so using it here would claim success before it happened. It would also visually contradict node 2's own circle, which stays the blue `--active` face for the entire duration of this same branch — two elements describing the identical in-flight state must not disagree in colour.
  - Branch 3 double-guards the preview approval: it sets BOTH `disabled: true` and `action: 'inert'` when `productionPublishBlocked` is true. Say why plainly: this is the deliberate preview-before-publish safety gate built earlier in this session at the maintainer's explicit request, and it is the only thing standing between a single click and a real production release, so one mis-wire in the component must not be enough to defeat it. Note that the button still shows its TARGET label while disabled — that is the point of the merge, telling the maintainer what the next step will be.
  - Branch 3 sitting above branch 4 is deliberate and has a visible consequence worth recording: `gateVariant` derives from the dashboard's `draftCount`, while `modifiedCount` derives from the publication inventory snapshot. The two refresh independently, so a brief disagreement is possible where drafts appear as pending while the gate still reads ready. In that window the button offers the production release of the staging build the maintainer already previewed, which is safe; and it self-corrects within one refresh, because a non-zero draft count moves `segments.content` to pending, which locks the gate and returns the button to branch 4.
  - Branch 5 covers `locked`, `done` and `failed` alike. Say explicitly that `failed` is deliberately NOT a branch of its own: retry lives exclusively on the round gate, which `gateClickAction('failed')` routes to a release retry. A second retry affordance here was explicitly ruled out, and the tone union has no `critical` member precisely so one cannot be added by accident.
  - Note the module rule being respected: `productionPublishBlocked` arrives as a plain boolean because calling `productionPublishDisabled()` here would mean importing `./releaseGate` for a value, and this file's header restricts it to type-only imports.

Then extend `tests/unit/pipeline-view.test.ts` with a new describe for `releaseActionButtonState`, importing it alongside the four existing helpers. Define a fixture helper in the describe that builds the seven-input object with idle defaults (`gateVariant: 'locked'`, all booleans false, `modifiedCount: 0`) so each case only names what it changes. Do not modify the four existing describes.

State cases, asserted on every returned field unless noted:

  - `publicationBusy` + `preflighting` → preflight label, disabled, loading, `tone: 'primary'`, `action: 'inert'`.
  - `publicationBusy` without `preflighting` → publish label, disabled, loading, `tone: 'primary'`, `action: 'inert'`.
  - `releaseBusy` alone → publish label, disabled, loading, `tone: 'primary'`, `action: 'inert'`. Message: the tone stays `'primary'`, not `'positive'`, because the release has not yet succeeded — `'positive'` is reserved for the ready-to-publish CTA and would misreport an in-flight, uncertain release as already successful.
  - `gateVariant: 'active'` with `releaseBusy: false` → same as above, `tone: 'primary'`. Message: this is the minutes-long window of a real production run, and `releaseBusy` alone would leave the button idle throughout it; the tone must match branch 1's and node 2's circle (also `--active`, also blue) for the whole duration.
  - `gateVariant: 'ready'`, `productionPublishBlocked: false` → go-live label, `disabled: false`, `loading: false`, `tone: 'positive'`, `action: 'release'`.
  - `gateVariant: 'ready'`, `productionPublishBlocked: true` → SAME label, `disabled: true`, `action: 'inert'`. This is the single most important case in the file: message it as the deliberate preview-before-publish gate, and assert both the label (the maintainer must see the target) and both guards.
  - `gateVariant: 'locked'`, `modifiedCount: 3`, `publishButtonDisabled: false` → update label, `disabled: false`, `action: 'publish'`, `tone: 'primary'`.
  - `gateVariant: 'locked'`, `modifiedCount: 3`, `publishButtonDisabled: true` → update label, `disabled: true`, `action: 'inert'`. Message: blocked rows and tracking errors still disable the publish exactly as they do today.
  - `gateVariant: 'locked'`, `modifiedCount: 0` → update label, `disabled: true`, `action: 'inert'`, `loading: false`. This is the narrow transitional window the user was NOT complaining about.
  - `gateVariant: 'done'`, `modifiedCount: 0` → update label, disabled, inert. The idle default once everything has shipped.
  - `gateVariant: 'failed'`, `modifiedCount: 0` → update label, disabled, inert, `tone: 'primary'`. Message: retry belongs to the round gate alone; this button must not grow a second retry.
  - `gateVariant: 'failed'`, `modifiedCount: 2`, `publishButtonDisabled: false` → update label, enabled, `action: 'publish'`. Message: a failure does not suspend the ordinary drafts-pending behaviour.

Priority cases — assert the ORDER explicitly, each setting up two or more competing conditions:

  - `publicationBusy` AND `preflighting: true` AND `releaseBusy` AND `gateVariant: 'ready'` → branch 1 wins. Assert the label is the PREFLIGHT text (`'Vérification…'`), not `'Publication…'`. Set `preflighting: true` deliberately: since branches 1 and 2 now share the same `tone: 'primary'`, the label is the only field that still distinguishes "branch 1 fired" from "branch 2 fired" when both conditions hold simultaneously — without it the two branches would be indistinguishable in this scenario.
  - `releaseBusy` AND `gateVariant: 'ready'` → branch 2 wins: publish label, not the go-live label.
  - `gateVariant: 'active'` AND `modifiedCount: 5` → branch 2 wins over branch 4.
  - `gateVariant: 'ready'` AND `modifiedCount: 4` AND `publishButtonDisabled: false` → branch 3 wins: the go-live label, not the update label. Comment this as the transient disagreement window between the draft count and the inventory snapshot, and note that it self-corrects.
  - `gateVariant: 'failed'` AND `modifiedCount: 0` → falls through to branch 5 rather than producing any retry label or tone.

Invariant cases — build a small matrix over all five `gateVariant` values crossed with the relevant boolean combinations, and assert:

  - the returned `tone` is `'positive'` if and only if `gateVariant === 'ready'` (branch 3); every other reachable combination returns `'primary'`. Message: `'positive'` is this codebase's established "succeeded" signal, so it may only ever describe the safe, deliberate call-to-action of a release not yet started — never an in-flight or uncertain one, and never (per the tone union itself) a failure or retry.
  - `loading` is never true while `disabled` is false. Message: a spinning button that still looks clickable invites a double-trigger.
  - `action: 'release'` is returned ONLY when `gateVariant === 'ready'` and `productionPublishBlocked` is false. Message this as the machine-checked form of the preview-before-publish rule — the strongest guard in the file.
  - `action: 'publish'` is never returned when `modifiedCount === 0`. Message: publishing an empty batch is a dead click.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/pipeline-view.test.ts</automated>
    <automated>grep -c "^import type" sanity/editorial/pipelineView.ts</automated>
    <automated>git diff --name-only 2bad450287ebdd7a1dc7ca3ac515625686123b05 -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts</automated>
  </verify>
  <done>`releaseActionButtonState()` exists in `pipelineView.ts` with five ordered branches and no runtime imports, and `tests/unit/pipeline-view.test.ts` passes with the twelve state cases, the five explicit priority cases and the four matrix invariants, alongside the four untouched existing describes. The grep prints `2`, confirming the file still has only its two original type-only import statements. The `git diff --name-only` command prints nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: deployment.ts — make a never-started deploy read as failed instead of forever-in-progress</name>
  <files>sanity/editorial/deployment.ts, tests/unit/deployment.test.ts</files>
  <behavior>
    - Before three minutes with no qualified run, the state keeps its waiting kind and the pipeline segment stays active.
    - At and after three minutes with still no qualified run, the state's kind becomes the failed kind and the pipeline segment turns failed — for both the staging and the production target.
    - Every other field of that state, and the terminal flag in particular, is unchanged.
  </behavior>
  <action>
**Edit A — `deploymentState()` in `sanity/editorial/deployment.ts`, the `if (!qualifiedRun)` branch's return object (~lines 239-252).**

Change exactly one property: `kind: 'waiting-run'` becomes `kind: timedOut ? 'failed' : 'waiting-run'`. Leave `label`, `detail`, `tone`, `terminal`, `actionLabel` and `actionUrl` byte-identical, including their existing `timedOut` ternaries and the `target === 'production'` ternary inside `detail`. Do not touch the `timedOut` computation itself.

Add a comment directly above the return object carrying all five of these points:

  - `deploymentSegmentKind()` maps `'waiting-run'` to the `'active'` segment and `'failed'` to the `'failed'` segment. Before this change, a deploy that never started and a deploy still legitimately running rendered identically — blue and spinning, forever — because that mapping branches on `kind` only and never on `tone`, even though this branch had already been reporting `tone: 'critical'` for three minutes. A maintainer had no visual way to tell « still deploying normally » from « something is wrong and needs attention ».
  - `terminal` deliberately stays `false`. Polling continues at 15-second intervals, so if the run finally appears the state self-heals to `'deploying'` and then `'current'`. Flipping `terminal` to `true` here would slow the poll to five minutes and strand a recoverable state as permanently red.
  - This timeout is about a run never APPEARING, never about a run taking long to FINISH. The branch below, `qualifiedRun.status !== 'completed'` → `kind: 'deploying'`, is what covers a run that was found and is genuinely mid-flight, and it is untouched. A real production release observed on 2026-08-13 took about 5.5 minutes end to end and must stay blue for all of it.
  - Reassigning this branch's kind also flips the round gate for a timed-out STAGING deploy from locked to a red retry, because `pipelineGateVariant()` keys on the merged display segment. That is the same path a genuinely-failed staging run already takes today, not a new mechanism.
  - The `DeploymentStateKind` union is unchanged: this reassigns an existing value in one branch and adds nothing.

**Edit B — `tests/unit/deployment.test.ts`.**

Re-point the two existing cases this breaks, then add the new coverage:

  1. `'uses the newest valid local or remote publication timestamp across tabs'` (~line 96). It calls `deploymentState()` with no `now`, so the real clock makes its 2026-07-29 fixture months stale and `timedOut` silently true — it passes today only because a timed-out state still returned the waiting kind. Add `now: new Date('2026-07-29T09:02:30Z')` to that call so 30 seconds have elapsed and the assertion stays meaningful. Keep the expectation as the waiting kind. Add a short comment: this test is about timestamp SELECTION, so it pins an explicit `now` rather than inheriting the three-minute timeout's verdict from the wall clock.
  2. `'surfaces the not-started timeout after three minutes'` (~line 253). Change the kind expectation to the failed kind, and keep the existing `label` and `actionLabel` assertions untouched. Add three assertions with their own messages: `detail` is unchanged (the not-started sentence), `tone` is still `'critical'`, and `terminal` is still `false` — the last one messaged as the reason the state can still self-heal if the run appears late. Rename the test so it names the new verdict rather than just the timeout.

  Then add a new describe covering the boundary and the segment mapping, which is the user-visible half of this change. `deploymentSegmentKind()` is not exported, so prove the mapping through `releasePipelineState()`:

  - Build two staging states from `runs: []` with the shared `publishedAt` fixture: one at `now: new Date('2026-07-29T09:02:59Z')` (one second short) and one at `now: new Date('2026-07-29T09:03:00Z')` (exactly three minutes — the `>=` boundary). Assert the first has the waiting kind and the second the failed kind, then feed each into `releasePipelineState({pendingCount: 0, staging, production: <the file's existing noProductionRelease fixture>, publishedAt, productionReleaseAt: '', busy: false})` and assert `segments.staging` is `'active'` and `'failed'` respectively. Message the pair as the whole point: blue while it may still be starting, red once it demonstrably never did.
  - Repeat for the production target: build the same two states with `target: 'production'`, feed each as the `production` argument with `productionReleaseAt` set equal to `publishedAt` (so `isProductionReleaseStale()` does not override the result with `'pending'`), and assert `segments.production` is `'active'` then `'failed'`.
  - Add one case proving the untouched branch: a run that WAS found and is `status: 'in_progress'`, evaluated with a `now` well past three minutes, still reports the deploying kind and maps to the `'active'` segment. Message it with the real observation: production releases can legitimately run five-plus minutes, and this test is what stops a future reader from folding the two branches together.

  Do not add a row to the existing `it.each` sweep that maps staging kinds to segments — a timed-out row would collide with its `'failed'` row's generated test name. The dedicated describe above covers it.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/deployment.test.ts</automated>
    <automated>npm run test:unit -- tests/unit/release-gate.test.ts</automated>
    <automated>git diff -U0 2bad450287ebdd7a1dc7ca3ac515625686123b05 -- sanity/editorial/deployment.ts | grep '^@@'</automated>
    <automated>grep -cF "= 'pending' | 'active' | 'done' | 'failed'" sanity/editorial/deployment.ts</automated>
  </verify>
  <done>A no-qualified-run state reports the waiting kind before three minutes and the failed kind at or after it, with every other field unchanged, and `releasePipelineState()` turns the corresponding pipeline segment red for both targets. `tests/unit/deployment.test.ts` passes with two re-pointed cases and the new boundary/segment/untouched-branch describe, and `tests/unit/release-gate.test.ts` still passes untouched. The `git diff -U0` hunk headers fall only inside `deploymentState()`'s no-qualified-run branch and its comment — no hunk touches `resolvePromoteRow()`, `releasePipelineState()` or `pipelineDisplaySegments()`. The final grep prints `1`, proving the segment-kind union was not widened.</done>
</task>

<task type="auto">
  <name>Task 3: EditorialDashboard.tsx — one evolving button instead of two, and simplify the promote box back to its link</name>
  <files>sanity/editorial/EditorialDashboard.tsx, tests/unit/editorial-dashboard-markup.test.ts</files>
  <action>
Four edits in the component, then the markup-test updates.

**Edit A — the derivations.**

Import `releaseActionButtonState` from `./pipelineView`, alongside the four helpers already imported from that module. Keep the existing `productionPublishDisabled` import from `./releaseGate`.

Next to the existing `publicationBusy` derivation (~line 532), add `const publicationPreflighting = publicationState.phase === 'preflighting'`, so the phase comparison stops living inside JSX.

Next to the `panelSubtitle` derivation (~line 551) — NOT next to `gateVariant`, which is declared about seventy lines before `publicationCard` exists — derive the button state once:

`const releaseAction = releaseActionButtonState({gateVariant, publicationBusy, preflighting: publicationPreflighting, releaseBusy, modifiedCount: publicationCard.total, publishButtonDisabled: publicationCard.buttonDisabled, productionPublishBlocked: productionPublishDisabled({promoteButtonDisabled: pipeline.promote.buttonDisabled, hasPreviewedStaging})})`

Add a comment above it: this panel now has a single action button whose label, tone, disabled state and click target all evolve with the workflow, because the maintainer expected the greyed update button to become the go-live button rather than a second button appearing below the pipeline. The five-branch decision lives in `releaseActionButtonState()` so it has real behavioural tests instead of a stack of ternaries in JSX. Note that the two underlying actions remain technically distinct — publishing drafts and triggering a production release are different Sanity Actions API calls with different consequences — and that only the UI is merged.

**Edit B — the click router.**

After `refreshPublicationTracking()` (~line 590) and before the `return (`, add a handler shaped like the existing `handleGateClick`:

`const handleReleaseActionClick = () => { if (releaseAction.action === 'publish') { void runPublication(); return } if (releaseAction.action === 'release') { void triggerProductionReleaseClick() } }`

Comment it: the merged button routes to one of two existing, unchanged handlers depending on which stage it currently represents, and the `inert` action is a real outcome rather than a fallthrough — combined with the button's own `disabled` flag it is the second of two independent guards on the preview-before-publish rule. Do not add an `else` that calls anything.

**Edit C — the top button (~lines 710-727).**

Replace its four evolving props with the derived state: `tone={releaseAction.tone}`, `text={releaseAction.label}`, `disabled={releaseAction.disabled}`, `loading={releaseAction.loading}`, `onClick={handleReleaseActionClick}`. Keep `style={{minHeight: 44}}` and the element's position inside the header `Flex` exactly as they are. The three-way label ternary and the direct `runPublication()` call leave the JSX entirely.

Replace its existing comment with one that says the button now spans the whole workflow — the draft-publish half AND the production-release half — and that its previous comment described only the first half. Do NOT quote « Publier sur le site en ligne » or « Vérification… » in that comment or anywhere else in this file: a markup assertion greps the whole file for their absence, because those literals now belong exclusively to the tested helper.

Leave the panel `<Heading as="h2" size={2}>Mettre le site à jour</Heading>` and the `<Text size={1} muted>{panelSubtitle}</Text>` beside it untouched. The heading stays static while the button evolves, and it is also the anchor an existing markup test slices from.

**Edit D — the promote box (~lines 453-474 and 809-846).**

Simplify `promoteActionsBoxHasBody` to `Boolean(pipeline.promote.actionUrl)`. Rewrite its comment to describe the box's now-single responsibility, keeping the parts that are still true and dropping the parts that are not:

  - The box carries its own padding and background, so it must not render at all when it has nothing inside — an unconditionally-rendered box paints a visible empty rectangle rather than disappearing. That was always the reason for this guard.
  - Its body is now exactly one thing: the run link for a failed staging or production deploy. It carries no explanatory copy (`resolvePromoteRow()` returns empty title/detail in every branch) and no button any more.
  - The `actionUrl` term is load-bearing and is the only term left: the failed states have no other body, and losing it hides the run link that is their only actionable part — the sole route a maintainer has to the GitHub Actions page for a broken deploy.
  - The ready-state term that used to sit here is gone on purpose: the control it kept mounted moved to the top of the panel and is now the panel's single evolving button.

Then delete the `{gateVariant === 'ready' && ( … )}` block and the `<Button>` inside it, including its wrapping `<Flex justify="center">`. Keep the outer `Stack`, its `editorial-dashboard__pipeline-detail` / `--dimmed` className expression keyed off `pipeline.promote.dimmed`, and the `pipeline.promote.actionUrl` link block byte-identical.

After this edit `gateVariant` must still be used by the className template, the icon conditionals, `gateAction`, the `nextPreviewedFlag` effect and the `releaseAction` derivation; `hasPreviewedStaging` and `productionPublishDisabled` must still be used by the `releaseAction` derivation. `npm --prefix sanity run lint` is the gate on all of that.

**Markup test updates in `tests/unit/editorial-dashboard-markup.test.ts`.** Re-point, do not delete:

  1. In the describe about the release gate wiring, the case `'gates the publish button on ready and on the shared disabled rule, with the label appearing exactly once'` must be re-pointed and renamed. Keep the `productionPublishDisabled(` presence assertion. Replace the exactly-once count on the go-live label with an exactly-ZERO count, messaged as: the label now belongs to the tested helper, exactly as the gate caption literal already does, so a state that should not offer the production release cannot silently grow the label back inline. Leave every other case in that describe untouched — in particular the one asserting the round gate never calls the release trigger from inside its own button, which must still pass unchanged.
  2. In the describe about the pipeline-detail box, re-point the case that asserts the guard references the ready variant: assert the `promoteActionsBoxHasBody` declaration slice still includes `pipeline.promote.actionUrl` (message: the failed states have no other body) and NO LONGER includes the ready-variant comparison (message: the button that term kept mounted moved to the top of the panel, so leaving the term would mount an empty box in the ready state). Keep the exactly-one-derivation case, the wraps-the-Stack-itself case, the never-references-title-or-detail case and the old-identifier case unchanged.
  3. In the same describe, re-point the case `'keeps the ready-state publish button block, the control that triggers a real production release'`: rename it around the control having MOVED rather than existing here, and assert that the box's own body no longer contains a button. Slice the source from `{promoteActionsBoxHasBody && (` to the next `</Stack>` and assert the slice contains no `<Button`, with a message recording that the merged top button is now the only control that starts a production release and that a second one here would resurrect the two-button design the user rejected.
  4. Add a new describe for the merged button wiring, following the shape of the existing `panelSubtitle` describe:
     - `const releaseAction = releaseActionButtonState(` appears exactly once.
     - That derivation passes all seven inputs, each asserted with its own message naming what silently breaks if it is dropped — with the `productionPublishDisabled({promoteButtonDisabled: pipeline.promote.buttonDisabled, hasPreviewedStaging})` argument messaged as the preview-before-publish gate itself.
     - The top button renders `tone={releaseAction.tone}`, `text={releaseAction.label}`, `disabled={releaseAction.disabled}`, `loading={releaseAction.loading}` and `onClick={handleReleaseActionClick}`.
     - The preflight label literal appears nowhere in the component source, proving the label ternary really moved into the helper rather than being duplicated.
     - `void triggerProductionReleaseClick()` appears exactly twice: once in `handleGateClick` (the failed-variant retry, unchanged) and once in `handleReleaseActionClick`. Message it as the guard that the bottom button is gone AND that the gate retry survived — a count of one means one of the two was lost.
     - `void runPublication()` appears exactly twice: once in `handleReleaseActionClick` and once in the error-card retry button, which keeps its own direct call.
     - The panel heading still renders the static update label inside its `<Heading as="h2" size={2}>`, messaged as both the deliberate choice to keep the panel title fixed while the button evolves and the anchor the subtitle test slices from.
  5. Leave every other describe in the file untouched, in particular the pipeline-node span describes, the `panelSubtitle` wiring describe, the one-click-gesture describe and the `pipelineView` helper wiring describe.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/editorial-dashboard-markup.test.ts</automated>
    <automated>npm run test:unit</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>git diff --name-only 2bad450287ebdd7a1dc7ca3ac515625686123b05 -- sanity/editorial/EditorialDashboard.css sanity/editorial/releaseGate.ts sanity/editorial/dashboardLogic.ts .github/workflows/</automated>
  </verify>
  <done>The panel renders exactly one action button, driven end to end by `releaseActionButtonState()` and routed through `handleReleaseActionClick` to the two unchanged handlers; the promote box holds only its run link and is guarded on `actionUrl` alone; the round gate, its caption, the preview effect and the panel subtitle are byte-identical. The full unit suite and the Studio lint pass, and the `git diff --name-only` command prints nothing.</done>
</task>

<task type="auto">
  <name>Task 4: Resync the README passages this change makes false</name>
  <files>sanity/README.md</files>
  <action>
`sanity/README.md` is Romane's French guide to the Studio. Three passages are now inaccurate — one of them was already inaccurate before this plan. Rewrite only these, leaving everything else in the file untouched:

  1. Step 6 of « Le parcours quotidien » (~lines 15-18). It currently says that once the site de test is confirmed up to date, the round button on the line between the two stages unlocks and clicking it sends the site to its real address. That has been wrong since the preview gate was introduced: the round button opens the site de test and records that it was reviewed, and the real publication is the panel's own button. Rewrite it to describe the current two-step flow — click the round button to review the site de test, then click the panel's button, which now reads « Publier sur le site en ligne », to send it live — and keep the existing closing point that the two actions are deliberately separate, adapting it to say the button shows its next step but stays greyed until the site de test has been reviewed.
  2. The bullet « **Mise à jour non démarrée** : aucune exécution n'est apparue après trois minutes » (~line 70). Add that the corresponding stage now turns red in this case, so a blocked update is distinguishable from one still in progress, and that this is the moment to open the link and warn the maintainer.
  3. The paragraph opening « Sous le bouton **Mettre le site à jour** » (~line 80) and the closing paragraph about the central button (~lines 91-96). Rewrite the minimum needed so that: the panel's single button is described as changing label according to the stage rather than being named only « Mettre le site à jour »; and the closing sentence about the zone under the progress bar says it carries only a link — never explanatory text and no longer a button — since the publication button now lives at the top of the panel.

Do not touch: the status-name list above (those are `deploymentState()` labels this plan does not change), the « Studio » / « Site en ligne » stage description, the blue modified-content state, the never-green-while-drafts-pending rule, the checklist and publication-batch sections, and everything from « ## Collections photo » onward.
  </action>
  <verify>
    <automated>git diff --stat 2bad450287ebdd7a1dc7ca3ac515625686123b05 -- sanity/README.md</automated>
    <automated>grep -c "Publier sur le site en ligne" sanity/README.md</automated>
  </verify>
  <done>The README describes one panel button whose label follows the stage, a red stage for an update that never started, a zone under the progress bar carrying only a link, and a step 6 that matches the actual preview-then-publish flow. `git diff --stat` shows one file with paragraph-sized hunks, and the grep prints at least `1`.</done>
</task>

<task type="auto">
  <name>Task 5: Run every gate, prove scope, redeploy the Studio, request the live confirmation</name>
  <files>(no source changes — verification, deploy and sign-off only)</files>
  <action>
Run the four blocking gates in this order, fixing anything they surface before moving on: `npm run test:unit` (the full suite, not only the three touched test files), `npm run typecheck`, `npm --prefix sanity run lint`, `npm --prefix sanity run build`.

If a failure in the full suite is clearly unrelated to this plan's files, record it in the SUMMARY rather than chasing it — this repo runs concurrent sessions and the working tree can carry other agents' changes. Do not revert, commit or "clean up" anything outside this plan's seven files.

Then prove the scope boundary held, against base SHA `2bad450287ebdd7a1dc7ca3ac515625686123b05`:

  - `git diff --name-only 2bad450287ebdd7a1dc7ca3ac515625686123b05 -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts sanity/editorial/EditorialDashboard.css tests/unit/release-gate.test.ts .github/workflows/` must print nothing.
  - `grep -cF "= 'pending' | 'active' | 'done' | 'failed'" sanity/editorial/deployment.ts` must print `1`, and `grep -cF "= 'locked' | 'ready' | 'active' | 'done' | 'failed'" sanity/editorial/releaseGate.ts` must print `1` — neither union was widened.
  - `git diff --stat 2bad450287ebdd7a1dc7ca3ac515625686123b05 -- sanity/ tests/` must list only this plan's seven files. Call out any additional file explicitly in the SUMMARY so a concurrent session's work is never silently attributed to this plan.

Then run the two manual checks tests can only approximate, both about the single control that starts a real production release:

  - Read the rendered top-button JSX one last time and confirm by eye that its `disabled` and `onClick` both come from the derived `releaseAction`, and that nothing else in the panel can reach `triggerProductionReleaseClick()` except `handleGateClick`'s failed-variant retry.
  - Confirm the promote box body now contains only the run link, so no state can render an empty tinted rectangle and no state can render a second publish button.

Then redeploy the live Studio with `npm run deploy --prefix sanity`. This is the established, pre-authorised pattern for shipping a Studio change in this session — do not stop and ask before running it. Wait for it to report success before presenting the human-check.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm run typecheck</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm --prefix sanity run build</automated>
    <automated>git diff --name-only 2bad450287ebdd7a1dc7ca3ac515625686123b05 -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts sanity/editorial/EditorialDashboard.css tests/unit/release-gate.test.ts .github/workflows/</automated>
    <human-check>
Le Studio a été redéployé. Ouvre https://atelier-jacqueline-suzanne.sanity.studio/ et fais un **rechargement forcé** (Cmd+Shift+R) pour charger le nouveau bundle — cet environnement n'a pas de session navigateur authentifiée, donc toi seul peux confirmer le rendu.

Il n'y a désormais qu'**un seul bouton** dans le bloc de publication, en haut à droite : c'est lui qui change de texte, de couleur et d'action selon l'étape. Le bouton qui apparaissait sous la barre de progression a disparu.

Si possible, vérifie sur plusieurs moments :

**(a) Une fois le site de test à jour** — le bouton du haut doit maintenant afficher **« Publier sur le site en ligne »**, en vert, mais **grisé** tant que tu n'as pas cliqué le bouton rond au centre de la barre pour ouvrir l'aperçu du site de test. Après cet aperçu, il devient cliquable. Ce garde-fou est volontaire et n'a pas changé : impossible de publier en ligne sans avoir ouvert le site de test avant.

**(b) Une fois la mise en ligne terminée et plus rien en attente** — le bouton doit revenir à **« Mettre le site à jour »** (grisé s'il n'y a rien à publier, bleu et cliquable dès qu'un brouillon attend).

**(c) Pendant une vraie mise en ligne** — le bouton affiche « Publication… » avec sa roue, et l'étape « Site en ligne » reste bleue **tant que le déploiement tourne réellement**, puis passe au vert une fois terminé. Important : la dernière mise en ligne réelle a pris **environ 5 minutes 30**, donc c'est normal que ce soit long. **Laisse l'onglet ouvert sans le recharger** pendant que tu observes : un rechargement réinitialise le suivi en cours dans le navigateur et affichera un état différent — ce n'est pas un bug.

Et si un déploiement ne démarre jamais (aucune exécution GitHub après trois minutes), l'étape concernée devient maintenant **rouge** au lieu de rester bleue indéfiniment, avec un lien pour prévenir le mainteneur.

Réponds « approuvé » si tout est bon, ou décris ce qui cloche en précisant le point (a/b/c).
    </human-check>
  </verify>
  <done>All four gates pass, the scope-proof commands come back clean, the two manual reachability checks were performed by reading the rendered JSX, the Studio is redeployed, and the user's confirmation — or their correction list — is recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Studio UI → GitHub Actions `repository_dispatch` | The maintainer-only control that starts a real production release crosses here. This plan changes WHICH button crosses it and adds a second machine-checked guard on the crossing. No untrusted input is introduced. |
| GitHub Actions API response → pipeline segment rendering | Run timestamps and statuses from an external API decide what colour the maintainer sees. Change 3 alters one verdict in that mapping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gw2-01 | Elevation of Privilege | The merged button's ready state in `releaseActionButtonState()` + its wiring in `EditorialDashboard.tsx` | high | mitigate | A dropped `productionPublishBlocked` input would let one click publish to production without the required site-de-test review. The helper enforces the gate twice (`disabled: true` AND `action: 'inert'`), a matrix invariant test asserts `action: 'release'` is unreachable without a recorded preview, and a markup assertion pins the `productionPublishDisabled({...hasPreviewedStaging})` argument in the derivation. |
| T-gw2-02 | Denial of Service | The production-release control, after Task 3 deletes the promote-box button | high | mitigate | Once the second button is gone the merged top button is the only control that can start a production release; a mis-wire leaves the site unpublishable. Task 1 tests the `'release'` routing directly, Task 3 asserts `triggerProductionReleaseClick()` has exactly two call sites (merged button + gate retry), and Task 5 re-reads the rendered JSX before deploying. |
| T-gw2-03 | Repudiation | `deploymentState()`'s no-qualified-run branch | medium | mitigate | A stuck deploy was indistinguishable from a running one, so a failure to publish left no visible trace and the maintainer could believe the site was updating. Change 3 reassigns the kind so the stage turns red, with boundary and segment-mapping tests for both targets, and `terminal: false` retained so a late run still self-heals the state. |
| T-gw2-04 | Elevation of Privilege | The round gate after a timed-out STAGING deploy | medium | accept | The timed-out staging state now flips the gate from locked to a red retry, which `gateClickAction('failed')` routes to a production release of a staging build that never rebuilt. This is the identical path a genuinely-failed staging run already takes today; the gate is maintainer-only and behind the same preview mechanism; changing it would require editing `releaseGate.ts`, which is out of scope. Recorded in the SUMMARY for a future task. |
| T-gw2-05 | Tampering | Package installs | low | accept | This plan installs no packages — no `npm`/`pip`/`cargo` install task exists in any task, so no package legitimacy gate applies. |
</threat_model>

<verification>
- `npm run test:unit` — full suite green, including the three updated test files.
- `npm run typecheck` — 0 errors.
- `npm --prefix sanity run lint` — clean (proves the button rewire left no unused import, variable or handler in the component).
- `npm --prefix sanity run build` — Studio bundles with the extended helper module.
- `git diff --name-only <base> -- sanity/editorial/dashboardLogic.ts sanity/editorial/releaseGate.ts sanity/editorial/EditorialDashboard.css tests/unit/release-gate.test.ts .github/workflows/` — empty.
- `grep -cF "= 'pending' | 'active' | 'done' | 'failed'" sanity/editorial/deployment.ts` — `1`.
- `grep -cF "= 'locked' | 'ready' | 'active' | 'done' | 'failed'" sanity/editorial/releaseGate.ts` — `1`.
- `npm run deploy --prefix sanity` — reports success.
</verification>

<success_criteria>
- CHANGE 1: one button in the panel, whose label, tone, disabled, loading and click routing come from `releaseActionButtonState()`, with five ordered branches, zero new user-facing strings, and behavioural tests covering every state, five explicit priority pairs and four matrix invariants.
- SAFETY GATE: in the ready state the button shows « Publier sur le site en ligne » and stays disabled until the site de test has been previewed, guarded twice in the helper's output and pinned by a matrix invariant plus a markup assertion. `releaseGate.ts` is byte-identical.
- CHANGE 2: the promote-box button is gone, `promoteActionsBoxHasBody` is `Boolean(pipeline.promote.actionUrl)`, the failure run link survives, and the round gate, its caption, the preview effect and the panel subtitle are unchanged.
- CHANGE 3: a never-started deploy reads as failed at and after three minutes and turns the pipeline stage red for both targets, with `terminal: false` retained; a run that was found and is genuinely mid-flight still reads as deploying and stays blue.
- FAILURE STATES: the merged button keeps its ordinary drafts-pending behaviour under `gateVariant === 'failed'`, never returns a critical tone, and gains no retry affordance.
- No test deleted; four re-pointed with recorded reasons.
- Every gate green, scope git-proven, Studio redeployed, user asked to confirm across the three named moments.
</success_criteria>

<output>
Create `.planning/quick/260813-gw2-dashboard-merge-mettre-a-jour-publier-bu/260813-gw2-SUMMARY.md` when done.

End the SUMMARY with the human-verification request from Task 5 verbatim: ask the user to hard-refresh and confirm, across a few states if possible, that (a) the top button now reads « Publier sur le site en ligne » — greyed until the round gate/preview has been clicked — once the site de test is ready, instead of a separate button appearing below; (b) the button goes back to reflecting « Mettre le site à jour » once production is done and nothing new is pending; and (c) if a production release genuinely runs long (the last one took about 5.5 minutes), the button and pipeline stay blue while it is genuinely still running and turn green once it actually finishes — with the explicit instruction to leave the tab open without refreshing while watching, since a page refresh resets in-browser release tracking and is expected to show a different, non-stuck state rather than a bug.

Also record in the SUMMARY:
- The exact five-branch priority order of the merged button and the label/tone/action each branch produces, so the maintainer can review the whole behaviour in one place without reading the code.
- That the preview-before-publish gate is untouched and is now enforced twice (disabled flag AND inert routing), and that `releaseGate.ts` is byte-identical.
- That failure-state retry deliberately stays exclusively on the round gate, and that the tone union has no critical member specifically so a second retry control cannot be added by accident.
- The Change 3 investigation trail: the live production run started 2026-08-13T09:38:55Z finished at 09:44:18Z, about 5.5 minutes — so the user's original observation was most likely a legitimately long run rather than a bug, and the distinct gap fixed here is that a deploy which never STARTS was rendering identically to one still running.
- The accepted consequence of Change 3 (a timed-out staging deploy flips the round gate from locked to a red retry that routes to a production release, matching the existing failed-staging path) and the welcome one (that state now renders a « Prévenir le mainteneur » link in the promote box where it previously rendered nothing).
- Confirmation that `dashboardLogic.ts`, `releaseGate.ts`, `EditorialDashboard.css`, `tests/unit/release-gate.test.ts` and `.github/workflows/` were not touched, and that neither the segment-kind nor the gate-variant union was widened.
</output>
