---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: MVP
current_phase: 05
current_phase_name: launch-domain-cutover
status: executing
stopped_at: Phase 5 planned — 6 plans, 4 waves, verification passed
last_updated: "2026-08-13T14:43:45.389Z"
last_activity: 2026-08-13
last_activity_desc: Phase 05 execution started
progress:
  total_phases: 1
  completed_phases: 0
  total_plans: 6
  completed_plans: 5
  percent: 0
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-08-04)

**Core value:** Visitors can browse Romane's photographic work and buy a piece through a real, working checkout — everything else supports that. (v1 milestone delivers the portfolio/about/contact foundation; v1.3 adds a non-transactional Éditions showcase; checkout still follows in the future v1.x shop milestone.)
**Current focus:** Phase 05 — launch-domain-cutover

## Current Position

Phase: 05 (launch-domain-cutover) — EXECUTING
Plan: 1 of 6
Status: Executing Phase 05
Last activity: 2026-08-13 — Phase 05 execution started

## Performance Metrics

**Velocity:**

- Total plans completed: 47
- Average duration: - min
- Total execution time: 0 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 02 | 4 | - | - |
| 03 | 3 | - | - |
| 04 | 3 | - | - |
| 04.3 | 3 | - | - |
| 07 | 2 | - | - |
| 09 | 2 | - | - |
| 10 | 4 | - | - |
| 11 | 2 | - | - |
| 12 | 3 | - | - |
| 13 | 2 | - | - |
| 14 | 4 | - | - |
| 15 | 4 | - | - |
| 17 | 1 | - | - |
| 18 | 2 | - | - |
| 19 | 2 | - | - |
| 20 | 6 | - | - |

**Recent Trend:**

- Last 5 plans: -
- Trend: -

*Updated after each plan completion*
| Phase 01 P01 | 11min | 3 tasks | 12 files |
| Phase 01 P02 | 15min | 2 tasks | 1 files |
| Phase 01 P03 | 35min | 2 tasks | 9 files |
| Phase 01 P04 | 14min | 3 tasks | 8 files |
| Phase 01 P05 | 35min | 3 tasks | 3 files |
| Phase 02 P01 | 50 | 3 tasks | 13 files |
| Phase 02 P02 | 25 | 2 tasks | 5 files |
| Phase 02 P03 | ~2h (incl. content migration + 2 real bugs found/fixed) | 3 tasks | 8 files |
| Phase 02 P04 | 90 | 3 tasks | 3 files |
| Phase 06 P01 | ~21h elapsed (two sessions, long overnight gap) | 3 planned tasks + live post-checkpoint follow-on | 3 files |
| Phase 07 P01 | 15min | 2 tasks | 2 files |
| Phase 07 P02 | ~20min | 2 tasks | 2 files |
| Phase 08 | unknown (implemented directly on main, outside plan/execute tracking) | - | 2 files (HomeCarousel.astro, homepage.spec.ts) + unrelated adjacent Sanity schema work |
| Phase 05 P01 | 25min | 3 tasks | 5 files |

## Accumulated Context

### Roadmap Evolution

- Phase 04.1: Design System & Homepage Refresh (INSERTED after Phase 4, URGENT) — import + adopt the Claude-Design-generated monochrome/pop-pink identity and rebuild the homepage per the imported prototype. See `.planning/design-import/README.md`.
- Phase 04.2: Social Media Links (INSERTED after Phase 4, URGENT) — Instagram link in the footer and on the About/Contact page.
- Phase 04.3 inserted after Phase 4: Homepage Refinements: logo hover crossfade, remove standalone Galleries page (grid view on homepage is now the sole browse entry point), icon-based carousel/grid toggle, fix mobile hero visibility bug, three-line wordmark, clickable gallery names in carousel + grid modes (URGENT)
- Phase 04.3 planned: 3 plans, 2 waves — plan-checker VERIFICATION PASSED, all 11 CONTEXT.md decisions covered
- v1.1 milestone (Homepage Refinements) roadmapped 2026-07-12: single new Phase 6 (integer, continuing numbering from v1.0's last phase, Phase 5) covers all three v1.1 requirements (HOME-01, HOME-02, HOME-03) — tightly-scoped, single-component (HomeCarousel.astro) refinement, so kept as one phase rather than split. Per PROJECT.md, Phase 6 is intended to execute before Phase 5's domain cutover despite the higher phase number (numbering reflects milestone-arrival order, not execution sequence).
- Phase 6 completed 2026-07-13: HOME-01/02/03 delivered per plan (commits `ad27437`..`dbebd6f`), plus a substantial live post-checkpoint follow-on (commit `44cc10d`) reworking the mobile hero (full-bleed `100svh`, panel overlaid on the photo) and replacing the arrow-button navigation with a clickable dashed progress indicator plus keyboard/swipe support — none of this follow-on was in the original plan; it was discovered through direct use after Task 3's checkpoint was already approved, and reconciled into tracked history via `06-01-SUMMARY.md` per explicit user request. v1.1 milestone is now complete; Phase 5 (Launch & Domain Cutover) is the only remaining v1 phase.
- v1.2 milestone (Homepage Polish, Pre-Launch) roadmapped 2026-07-13: four new integer phases (7–10, continuing numbering from Phase 6) cover all eight v1.2 requirements (HOME-04..HOME-10, I18N-04), sequenced by blast radius rather than dumped into one phase — Phase 7 (HOME-04, HOME-05, HOME-06): small, contained, homepage-only header/CSS tweaks and a mobile bug fix, done first with no shared-component or content-model risk. Phase 8 (HOME-07, HOME-08): grouped together because both surface the same new per-gallery description content (Sanity schema addition), just in two display forms (carousel byline, grid hover) — building the schema once and wiring both displays in the same phase avoids doing the content-model work twice. Phase 9 (HOME-09): kept standalone — progressive image loading has its own distinct testing surface (loading states/priority/transition timing) and potentially touches more than just the homepage's own images, so it wasn't folded into Phase 7's contained fixes. Phase 10 (HOME-10, I18N-04): the highest-risk item (structural header-consolidation refactor spanning homepage + About/Contact) done last, in its own phase, depending on Phase 7 — so the header nav/toggle groundwork already fixed in Phase 7 gets carried into the unified component once, rather than rebuilt twice; I18N-04 (language switcher globe-icon change) is bundled in because its presentation lives inside the same header being unified.
- v1.3 milestone "Éditions" roadmapped 2026-07-22: four new integer phases (11–14, continuing numbering from Phase 10; Phase 5 stays untouched/separately tracked and out of this milestone) cover all eight v1.3 requirements (EDN-01..EDN-07, CMS-04), following research/SUMMARY.md's proposed 4-phase structure near-verbatim (schema before data-fetch, routes before nav since nav touches every-page shared chrome, verification as an explicit final pass for omission-class risks). Phase 11 (Schema & Content Model — CMS-04, EDN-05): new `edition` Sanity document type mirroring `gallery.ts`'s richer editorial workflow, seeded with real content; format-detail fields grouped and typed now so a future shop `commerce` field group is additive, not a restructuring; also the phase where the "Rebut" gallery/édition naming-overlap question gets raised with Romane. Phase 12 (Data-Fetch Layer & Routes — EDN-02, EDN-03, EDN-04, EDN-06, EDN-07): build-time GROQ layer + bilingual overview/detail routes, verifiable in isolation before anything shared is touched; detail page mirrors `galleries/[slug].astro` directly, overview page is genuinely new (no gallery equivalent). Phase 13 (Nav Integration — EDN-01): touches the one piece of shared, every-page chrome in this feature (`SiteHeader`, threaded through two independent call sites — `BaseLayout.astro` and `HomeCarousel.astro`); sequenced last of the build phases so nav never points at an unready route. Phase 14 (Verification & UAT — no primary requirement, cross-cutting): a dedicated pass because this milestone's dominant risk class is omission bugs (missed locale, missed sitemap entry, missed nav call site) that don't fail loudly on a single happy-path check.
- v1.4 milestone "Editorial Design Consistency" roadmapped 2026-07-29: two new integer phases (15–16, continuing numbering from Phase 14; Phase 5 stays untouched/separately tracked and out of this milestone) cover all three v1.4 requirements (ABOUT-03, ABOUT-04, ERR-01). Phase 15 (About Page Editorial Redesign — ABOUT-03, ABOUT-04): both requirements touch the same file (`AboutPageBody.astro`) and are sequential steps toward one end state, so kept as a single phase rather than split — the mechanical `PageTitleHeader` title swap (ABOUT-03) done first, then the broader layout rework (ABOUT-04) via a sketch-explored design process mirroring the Contact page redesign (multiple layout proposals sketched, a winner picked, then implemented). Phase 16 (404 Page Editorial Redesign — ERR-01): kept separate from Phase 15 despite reusing the identical `PageTitleHeader` component, because it targets a different page/user surface and needs no design exploration (the treatment is already fully specified by Contact/Éditions/About) — a small, independent, visual-only phase.
- Phase 16 edited: Goal/Success Criteria/Depends-on rewritten during /gsd-discuss-phase 16 (2026-07-29): user rejected the PageTitleHeader-reuse approach for a fully custom, interactive concept (full-bleed photo backdrop popping at pointer/touch-proximity-driven rate, AJS logo + 404 marker + bilingual phrase over a dimming scrim). See 16-CONTEXT.md.
- v1.5 milestone "Global Improvements & Bug Fixes" roadmapped 2026-08-02: three new integer phases (17–19, continuing numbering from Phase 16; Phase 5 stays untouched/separately tracked and out of this milestone) cover all eight v1.5 requirements (HOME-11, HOME-12, PORT-04, PORT-05, PORT-06, EDN-09, UI-01, CONT-03) — all small, independent, single-or-few-component CSS/Astro/vanilla-JS bug fixes with no shared data model, so grouped by area/blast-radius into 3 phases rather than one giant phase or 8 thin ones. Phase 17 (Homepage Carousel & Intro Fixes — HOME-11, HOME-12): both bugs live in `HomeCarousel.astro` alone, no shared-component risk, done first. Phase 18 (Gallery & Éditions Display Fixes — PORT-04, PORT-05, PORT-06): `DetailHero.astro`, the shared `GalleryGrid.astro` thumbnail component, and the two gallery detail-page route files — contained to the Portfolio/Éditions display surface, done second. Phase 19 (Site-Wide Visual Polish — EDN-09, UI-01, CONT-03): `EditionsOverviewBody.astro`, the shared `PageTitleHeader.astro` consumed by Contact/About/Éditions, and `ContactPageBody.astro` — sequenced last because UI-01 touches the one component shared by three pages and must not reintroduce the horizontal-scroll bug Phase 16 fixed.
- v1.6 milestone "Mobile Experience Redesign" roadmapped 2026-08-03: four new integer phases (20–23, continuing numbering from Phase 19; Phase 5 stays untouched/separately tracked and out of this milestone) cover all nine v1.6 requirements (HOME-13, HOME-14, HOME-15, HOME-16, PORT-07, EDN-10, EDN-11, ABOUT-05, UI-02). Sequenced by blast radius/dependency rather than one phase per requirement or per page: Phase 20 (Mobile Navigation & Accent Color — HOME-13, HOME-16): the homepage's mechanical, lower-risk pieces (mobile nav menu, per-visit random accent color reusing the existing `heroColor` field), done first with no sketch exploration needed, and the accent-color mechanism the next phase's scroll view will consume. Phase 21 (Homepage Scroll Experience — HOME-14, HOME-15): the milestone's biggest, riskiest, sketch-explored centerpiece — the scroll-driven single view replacing the carousel/grid toggle, plus the full-screen wordmark-to-letterform-zoom entry transition into it (HOME-15 is literally the entry transition into HOME-14's view, so kept in the same phase) — depends on Phase 20. Phase 22 (Gallery & Édition Scroll Navigation — PORT-07, EDN-10, EDN-11): PORT-07 and EDN-10 grouped because Gallery and Édition detail pages already share the `GalleryGrid`/`Lightbox` component surface (same Lightbox-retirement pattern applied to both); EDN-11 (sketch-explored intro-text legibility + primary-photo placement) folded into the same phase rather than split into a thin single-requirement phase, since it's a dependent next step on the same Édition detail page once EDN-10's scroll layout exists (the primary photo can only sit "among the others" once the others are shown in that new layout). Phase 23 (About Portrait Placement & Milestone Regression Close — ABOUT-05, UI-02): the isolated, single-component, sketch-explored About fix, kept standalone since it shares no surface with the other three phases (matching the Phase 15/16 precedent of keeping unrelated single-page fixes separate rather than force-merging them); UI-02 (the desktop/tablet-unchanged regression guard) is formally mapped here for traceability as the milestone's closing combined regression sweep, but per explicit user framing is also checked as a running local success criterion within Phases 20, 21, and 22 individually — not deferred to Phase 23 alone, since each earlier phase touches shared components also rendered on desktop.

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- [Roadmap]: v1 milestone scoped to portfolio + about + contact + baseline legal + DNS cutover only; shop/checkout/exhibitions deferred to v1.x (separate future roadmap).
- [Roadmap]: Bilingual routing + CMS scaffolding front-loaded into Phase 1 since I18N cross-cuts every later content phase.
- [Roadmap]: Legal & Compliance (Phase 4) depends on Phase 3 (Contact) since the privacy notice must describe contact-form data handling.
- [Roadmap]: v1.1 milestone (Homepage Refinements) mapped to a single new Phase 6, continuing integer numbering from v1.0's Phase 5 — all three requirements (HOME-01/02/03) touch the same component (HomeCarousel.astro) and are cosmetic/interaction refinements with no new backend/data model, so a single phase was judged more coherent than splitting.
- [Roadmap]: v1.2 milestone (Homepage Polish, Pre-Launch) mapped to four new phases (7–10), continuing integer numbering from Phase 6 — split by blast radius/risk (contained visual fixes → shared content-model addition → self-contained perf change → shared-component structural refactor done last) rather than one large phase, since HOME-10/I18N-04 carry materially higher risk (multi-page shared component) than the other six requirements.
- [Roadmap]: v1.3 milestone "Éditions" mapped to four new phases (11–14), continuing integer numbering from Phase 10 — Phase 5 (Launch & Domain Cutover) is untouched and remains separately tracked, not part of this milestone. Phase order follows dependency + blast-radius reasoning from research/SUMMARY.md: schema/content model must exist and be seeded before the data-fetch layer can be verified against anything real; routes come before nav wiring since nav is the only part touching shared chrome used by every existing page; verification is its own explicit final phase because this milestone's pitfalls are dominated by omission-class bugs.
- [Roadmap]: v1.4 milestone "Editorial Design Consistency" mapped to two new phases (15–16), continuing integer numbering from Phase 14 — Phase 5 (Launch & Domain Cutover) remains untouched and separately tracked. ABOUT-03 and ABOUT-04 grouped into one phase (15) rather than split, since both touch the same file and are sequential steps of the same About-page deliverable (mechanical title swap first, then a sketch-explored layout rework) — splitting would have created a thin, single-requirement phase for ABOUT-03 alone. ERR-01 kept as its own phase (16), independent of Phase 15, since it's a different page/user surface needing no design exploration.
- [Roadmap]: v1.5 milestone "Global Improvements & Bug Fixes" mapped to three new phases (17–19), continuing integer numbering from Phase 16 — Phase 5 (Launch & Domain Cutover) remains untouched and separately tracked, not part of this milestone. Grouped by blast radius rather than one phase per bug: Phase 17 (HOME-11, HOME-12, both in `HomeCarousel.astro`), Phase 18 (PORT-04/05/06, Portfolio/Éditions display surface), Phase 19 (EDN-09, UI-01, CONT-03, sequenced last since UI-01 touches the `PageTitleHeader.astro` component shared by Contact/About/Éditions and carries the milestone's highest regression risk).
- [Roadmap]: v1.6 milestone "Mobile Experience Redesign" mapped to four new phases (20–23), continuing integer numbering from Phase 19 — Phase 5 (Launch & Domain Cutover) remains untouched and separately tracked, not part of this milestone. PORT-07 and EDN-10 grouped into Phase 22 (shared `GalleryGrid`/`Lightbox` component surface); EDN-11 folded into the same phase rather than kept as its own thin single-requirement phase, since it's a dependent next step on EDN-10's layout. UI-02 mapped to the closing Phase 23 for formal traceability, but is also carried as a local success criterion in every earlier phase (20–22) per explicit user direction — a cross-cutting regression guard, not a standalone final-phase-only check.
- [Phase 01]: No SSR adapter installed for Astro (output: 'static' framework default) per OVH static-hosting constraint — OVH Web Hosting is a zero-compute Apache file server; @astrojs/cloudflare/wrangler are explicitly excluded
- [Phase 01]: Phase 1 staging deploys to GitHub Pages (repo now public) instead of an OVH subdomain; OVH SFTP facts recorded for Phase 5 — OVH Free hosting tier cannot attach any subdomain (multisite requires paid tier); GitHub Pages reuses existing repo with zero new signup and automatic HTTPS
- [Phase 01]: Sanity read-only + one-time write tokens created and revoked entirely via CLI (sanity tokens add/delete), no dashboard visit needed — sanity tokens add/list/delete supports full non-interactive token lifecycle management
- [Phase 01]: siteSettings singleton bilingual placeholder copy published via a temporary editor-role token created and deleted in the same session — Viewer (read-only) token cannot write; a scoped, immediately-revoked editor token avoided a manual Studio-publish step for Florian
- [Phase 01]: [Phase 01 Plan 04]: Wired the previously-unused Sanity navLabels.home field into BaseLayout as a localized Home/Accueil nav link, since the seeded siteTitle is an intentionally untranslated brand name shared across locales
- [Phase 01]: [Phase 01 Plan 04]: vitest.config.ts switched from vitest/config defineConfig to astro/config getViteConfig so the astro:i18n virtual module resolves under Vitest
- [Phase 01]: [Phase 01 Plan 04]: getSwitcherHref normalizes trailing slash independent of the project's trailingSlash/build.format config (homepage always trailing-slash, other pages never) to match the locked shared-slug unit test contract
- [Phase 01]: [Phase 01 Plan 05]: CI builds the site twice (root base for the Playwright+Vitest hard gate, GitHub Pages base for the deployed artifact) to avoid coupling the locked e2e test suite to the deploy target's base path
- [Phase 01]: [Phase 01 Plan 05]: Fixed getSwitcherHref and the locale-cookie redirect script to strip the configured base path before computing slugs/paths — both broke under GitHub Pages' non-root base, only discovered once ASTRO_BASE was actually exercised in this plan
- [Phase 01]: [Phase 01 Plan 05]: Phase 1 staging site is live at https://florianlepont.github.io/ajs-website/, verified end-to-end (push-triggered deploy, repository_dispatch-triggered rebuild, live switcher/cookie/404 checks via a headless-browser script against the production URL) — Phase 1 is complete
- [Phase 01]: Post-completion code review (01-REVIEW.md) found 1 Critical + 6 Warnings; fixed CR-01 (404 page's hardcoded links weren't base-aware — real live bug, confirmed via curl against the deployed site) plus WR-01 (Sanity singleton create/duplicate guard), WR-02 (locale cookie scoped to base path, not domain-wide), WR-03 (null-safety for partially-populated Sanity docs), WR-04 (extracted stripBasePath + unit tests + a CI grep guard against un-prefixed hrefs), WR-06 (404 page switcher no longer computes a nonsensical /404 slug). WR-05 and remaining Info items deferred by explicit choice. Re-verified live post-fix: 404 page links all correctly base-prefixed, HTTP 404 served correctly.
- [Phase 02]: @sanity/orderable-document-list verified legitimate via blocking human checkpoint (npmjs.com, sanity-io org, v2.0.8, matching peer deps) before install — repo now lives in the sanity-io/plugins monorepo, a normal consolidation, not a legitimacy concern
- [Phase 02]: Shipped visual rebrand on the system-font fallback; Delight variable font sourcing/licensing deferred as a follow-up, not a Phase 2 blocker — user chose fallback-only at the checkpoint rather than block the phase on sourcing/licensing a Behance-hosted font with no CDN/npm distribution
- [Phase 02]: Fixed gallery images schema to attach alt-text fields directly on an image-type array member (not a wrapping object type), restoring Sanity Studio's native multi-file drag-and-drop upload — Romane's Lightroom-exported folders need batch upload; the original galleryImage object wrapper broke Studio's per-file-drop-to-array-item heuristic, discovered when she reported drag-and-drop of multiple files wasn't working
- [Phase 02]: Restricted sanityClient to perspective: 'published' so build-time queries never pull in unpublished drafts — an in-progress draft gallery (title set, no images) reached getGalleries() and crashed the static build with a null images array — published-only perspective ensures Romane's mid-edit documents can never break a production build
- [Phase 02]: Pushed to origin/main mid-phase to verify the CMS-01/lightbox checkpoint against real deployed GitHub Pages staging content, including a genuine mobile-device touch test — user chose live verification over local-only preview since real touch-swipe feel and Romane's unassisted Studio workflow can't be confidently confirmed from a local dev server
- [Phase 02]: Overrode D-13 (all-known-projects-migrated) to close Phase 2 with 2/8 projects published (Silos, Brume) — the content-migration pipeline is proven end-to-end with real content; remaining 6 projects (Rebut, Adults, The Victorian Tea Room, Paysages, Accumulation, MADO) are tracked as follow-up content work, not a code blocker — user explicitly accepted this override
- [Phase 06]: Mobile hero restructured into two independently-sized boxes (.home-hero__photo / .home-hero__accent) after live feedback surfaced overlap bugs — the accent panel's own in-flow height was stretching the absolutely-positioned photo/caption/arrows that shared its containing block; hardcoded pixel offsets kept breaking as content changed, so the structural fix removed the need for them
- [Phase 06]: Mobile hero uses 100svh, not 100vh — 100vh resolves to the large (chrome-hidden) viewport on real mobile Safari, letting the site footer peek in before the user scrolls; 100svh always reflects the guaranteed-visible area
- [Phase 06]: Prev/next arrow buttons replaced with a compact (~20-30% viewport width) clickable dashed progress indicator plus keyboard ArrowLeft/ArrowRight and touch-swipe navigation, per direct user request — arrows are gone from the component entirely
- [Phase 06]: Mobile-only wordmark background-position bypass (added when the panel briefly sat below the photo) was removed once the panel returned to overlaying the photo — mobile now shares desktop's live pixel-alignment computation, not an approximation
- [Phase 07]: [Phase 07 Plan 01]: Instagram nav link and square mode-toggle box committed together (not split per-task) since HOME-05's 44px tap-target fix directly changed HOME-04's mobile pixel-budget math
- [Phase 07]: [Phase 07 Plan 01]: .home-header's mobile inter-item gap trimmed a second time (8px to 4px) beyond the plan's nav-only trims, to absorb the wider 44px toggle button and keep all 5 header items on one row at 393px
- [Phase 07]: [Phase 07 Plan 02]: HOME-06's D-10 view-transition hypothesis could not be directly confirmed/reproduced under Chromium mobile emulation (test passed GREEN before and after a disable/re-enable experiment) — applied the fix anyway per D-12 since it corrects a real, identified asymmetry (.home-hero__photo/.home-hero__accent's unconditional CSS view-transition-name vs the grid tile's always-dynamic naming)
- [Phase 07]: [Phase 07 Plan 02]: dynamic view-transition-name assignment for the hero photo/accent panel is set once per toggle click and never cleared afterward (not via transition.finished) — clearing raced a rapid second click and broke the pre-existing accent-panel fade-timing test
- [Phase 08]: HOME-07 and HOME-08 (gallery description on homepage byline + grid-tile hover) were implemented and shipped directly to `main` by Florian (commits `38457dd`, `3360f16`, `78f3c61`, `04b10a1`, `a68ee00`, `602d24b`, 2026-07-13/14) ahead of the formal `/gsd-discuss-phase` → `/gsd-plan-phase` → `/gsd-execute-phase` cycle. Both surfaces reuse the existing `gallery.statement` field from Phase 2 — no new Sanity schema field was needed, confirming the roadmap's rationale for grouping HOME-07/08 into one phase. `/gsd-discuss-phase 8` (2026-07-14) found the implementation already complete, verified it (13/13 unit tests, 23/23 e2e tests passing, including the dedicated "collection statements on the homepage" describe block), and retroactively closed the phase — see `.planning/phases/08-gallery-descriptions/08-CONTEXT.md` and `08-SUMMARY.md`.
- [Phase 16]: Pointer Events (`pointermove`) used as the single input source for both mouse and touch in the pop-rate engine, rather than separate `mousemove`/`touchmove` listeners — satisfies D-09's behavioral intent (touch position drives the same speed curve) without duplicated/desyncable logic, per RESEARCH Open Question 1.
- [Phase 16]: D-10 cap override (2026-07-29, live at plan 16-03's human-verify checkpoint): after testing the original WCAG-adjacent-capped build (`MIN_INTERVAL_MS = 350`, ≈2.86/sec), the user explicitly asked for a faster rate and knowingly accepted the WCAG 2.3.1 flash-rate tradeoff ("tant pis pour les flash effect"). Presented with the risk and three concrete options, they chose to raise the cap significantly while keeping a finite ceiling: `MIN_INTERVAL_MS` moved from 350ms to 150ms (≈6.7/sec at dead-center); `MAX_INTERVAL_MS`/`DRIFT_INTERVAL_MS` unchanged. This is a knowing, user-confirmed departure from WCAG general-flash guidance for this one page, not an oversight — do not revert without the user raising the cap again. Reconciled back into plan 16-01's already-merged `16-01-PLAN.md`/`16-01-SUMMARY.md` via a dated post-completion addendum (mirroring the Phase 6 `06-01-SUMMARY.md` precedent for live post-checkpoint follow-ons), rather than left to silently drift out of sync with the shipped code. See `16-CONTEXT.md` D-10 override and `16-03-SUMMARY.md`.
- [Phase 16]: Post-completion code review (`16-REVIEW.md`) found 0 Critical + 3 Warnings + 2 Info; fixed WR-01 (bilingual `<span>`/`<a>` elements on the 404 page lacked `lang="fr"`/`lang="en"`, a real WCAG 3.1.2 gap invisible to the existing axe-core suite) and WR-02 (stale `MIN_INTERVAL_MS = 350ms` comment in `not-found.spec.ts`, left over from the D-10 override above). WR-03 (the D-10 cap itself, ≈6.7/sec) was flagged by design, not fixed — it's the same accepted tradeoff, not a new issue. IN-01/IN-02 (missing default-engine e2e coverage; all pool photos `loading="eager"`) deferred as Info-level, matching this project's established review-triage convention (see Phase 01's review note above).
- [Phase 16]: Post-merge/regression testing surfaced a pre-existing, out-of-scope regression from Phase 15 (`tests/e2e/site-header.spec.ts`, 4 failures on `/about/` at 320/360/767px, horizontal `scrollWidth` overflow) — deferred at the time as out of scope for ERR-01. **RESOLVED 2026-07-29, post-merge to `main`**: GitHub Actions caught the same failure blocking `main`'s deploy (run `30467173706`) after the v1.4 PR merged. Root-caused to a **shared `PageTitleHeader.astro` bug affecting About, Contact, AND Éditions** (not About-specific as first suspected): (1) the giant title's `white-space: nowrap` + a 64px font-size floor overflowed on any heading longer than ~"Contact" at narrow widths, and (2) the halftone texture's intentional `-700px` bleed was never actually contained horizontally on any page (the component's own comment claiming Contact had a scoped fix was stale/false — no such rule existed). Fixed by removing the `nowrap` (titles now wrap to 2 lines instead of overflowing) and adding a site-wide `overflow-x: hidden` to `html, body` in `BaseLayout.astro` (contains any bleed element's horizontal overflow site-wide, without touching Éditions' intentional *vertical* bleed). Verified zero overflow across all 3 consumer pages + homepage + 404, both locales, at 320/360/767px. See `.planning/phases/16-404-page-editorial-redesign/deferred-items.md` for full root-cause writeup.
- [Phase 17]: Homepage carousel hover-pause removed for mouse only (`mouseenter`/`mouseleave` deleted); keyboard-focus pause (`focusin`/`focusout`) explicitly kept — a deliberate mouse-vs-keyboard behavioral split, per user decision during discuss-phase, to avoid regressing keyboard accessibility while satisfying the literal "stop pausing on hover" request. Do not remove the focus-pause without the user asking again.
- [Phase 17]: Grid-mode intro paragraph's `-webkit-line-clamp: 2` removed entirely with no substitute cap — user chose simplicity/fidelity to real content over a defensive higher line limit.
- [Phase 18]: PORT-04's overflow defence deliberately moved from a CSS clamp to a Sanity Studio field-level max-length validation (not a higher CSS line-clamp) — user's explicit choice, since `DetailHero` is a fixed-height sticky panel (unlike Phase 17's freely-growing tile) and content-authoring-time prevention is more robust than display-time truncation. N=700, empirically derived against the real component (8 candidate lengths × 3 viewports) and floor-checked against the longest published statement (453 chars) — see `18-02-SUMMARY.md`'s calibration table.
- [Phase 18]: Planner corrected two factual errors in `18-CONTEXT.md` before planning: (1) masonry grid layout IS live on gallery detail pages via `GalleryDetailBody.astro` (CONTEXT.md wrongly claimed no page used it), and (2) the CONTEXT.md's "250-320 char" starting estimate for PORT-04's N would have invalidated 4 of 8 published statements — both caught via live GROQ queries against the actual dataset before locking the plan, not discovered post-ship.
- [Phase 18]: Live UAT (human verification pass, post-merge) caught a real regression automated Playwright screenshots missed: a ~3.5px gap below every gallery-detail thumbnail image (masonry mode), from `.gallery-grid--masonry .tile img` being left at its default `display: inline` — the `.tile`'s intentional `background: var(--color-ink)` loading fallback (D-05) bled through the gap as a visible dark strip, only noticeable once the border removal (PORT-05) stopped visually dominating the tile edge. Fixed with `display: block;` on the img (verified 3.5px → 0px empirically); a new regression assertion (img-vs-tile bounding-box comparison) was added to the existing PORT-05 masonry e2e test, since a border-width check cannot catch a non-border layout gap. Commit `e4bdf16`.
- [Phase 20]: Delivered HOME-13 (mobile nav menu + switcher) and HOME-16 (per-visit random accent color) across 6 plans, 5 waves. Live on-phone UAT (20-UAT.md Test 2) reversed D-04's switcher clause post-ship: the language switcher moved from a fourth equal-weight primary link to a small secondary-tier line above the Instagram link (same size/color), and the Instagram link gained the header's own SVG glyph — closed by gap-closure plan 20-06, independently re-verified (54/54 mobile-nav e2e including 7 new tests, own screenshots, live coverage re-run matching baseline exactly) before phase close. Security audit (retroactive, first SECURITY.md for this phase) closed all 19 registered threats, 4 of them high-severity (transitionend-gated close funnel DoS, shared-header-edit blast radius, halftone-containment DoS, base-path bypass) — see `20-SECURITY.md`. Code review flagged 1 unrelated Critical (pre-existing `HomeCarousel.astro` touch-handler can misfire on progress-dash/autoplay controls on real touchscreens) — not fixed in this phase, tracked in `20-REVIEW.md`.
- [Phase 21]: Round-2 on-phone UAT (21-UAT.md) found 5 more live-device gaps against the HomeCarousel scroll-deck after round 1's gap-closure had shipped green (duplicated intro logo, unreadable text-reveal timing, a doubled cover photo, a persistent white bar top/bottom, an unreachable nav hamburger) — closed by plans 21-11 through 21-15 (5 waves, worktree-isolated), plus a scoped deep code review (`21-REVIEW.md`, 0 critical/3 warning/1 info, all three warnings fixed and independently reconfirmed — `21-REVIEW-FIX.md`, commits `c163d13`/`3991e4a`/`db1af15`). `gsd-verifier` then routed to `human_needed` a third time, pending a consolidated real-device UAT round 3 (persisted as `21-UAT.md`, commit `d8c01ae`) — the phase never reached that round.
  Before round 3 could run, the phone-width mobile homepage was rebuilt from scratch outside this plan/execute cycle: `src/components/MobileHomePrototype.astro` (a new component, different mechanism — wordmark scale/fade → description reveal → contemplation beat → horizontal photo pan through the series, not the letterform-zoom `HomeCarousel` built) was introduced and wired into `src/pages/index.astro`/`src/pages/en/index.astro` at ≤767px by undocumented work with no GSD artifact, then refined by a Codex-run `/gsd:quick` task (`260810-qmm`, `.planning/quick/260810-qmm-.../`, uncommitted by its own executor — "shared worktree contains user-owned uncommitted changes"), and everything was squashed into one manually-authored commit, `6c51695` ("feat: refine mobile gallery journey", 2026-08-10 22:14, no phase/plan reference). `HomeCarousel`'s scroll-deck (all 15 phase-21 plans' work, three UAT rounds, the code review) remains in the codebase, correct and passing its own tests, but is now CSS-hidden (`display: none !important` under `@media (max-width: 767px)`) and unreachable at the one viewport it targeted — `MobileHomePrototype` owns phone-width HOME-14/HOME-15 instead.
  Per explicit user decision (2026-08-10, mechanical-sync path chosen over a retroactive `/gsd-discuss-phase 21` re-verification — see the Phase 8 precedent above for the alternative pattern): Phase 21 is closed on `6c51695` as the authoritative final state, without re-checking `MobileHomePrototype` against `21-CONTEXT.md`'s locked decisions (D-01–D-16) or HOME-15's literal "letterform" wording. The pending round-3 UAT is superseded/cancelled — it tested a mechanism no longer live on phone. `21-VERIFICATION.md`'s frontmatter `status` was changed from `human_needed` to `superseded` (with a `superseded_note` explaining why) so `gsd-tools progress` stops reporting the phase as "Needs Review" — it now reads "Executed", the honest label for a phase whose plans all ran but whose own verification never reached a terminal `passed`/`gaps_found` state. `21-VERIFICATION.md`'s findings and `21-REVIEW.md` remain accurate as historical snapshots of the (now-superseded) `HomeCarousel` mechanism, not of what's shipped today.

- [Phase 22]: CANCELLED 2026-08-10 by explicit user decision ("Cancel phase 22"). Never planned or implemented — no `.planning/phases/22-*/` directory was ever created, no discuss-phase or plan-phase ran. Requirements PORT-07, EDN-10, EDN-11 (Gallery/Édition Lightbox retirement + Édition intro-text/photo treatment) are dropped from active v1.6 scope; see `REQUIREMENTS.md`'s `[ ]` entries marked CANCELLED and the Traceability table. Revisit only via a future milestone if this work is wanted again — nothing about the cancellation implies the underlying user need was solved elsewhere.
- [Phase 23]: COMPLETE 2026-08-10 by explicit user decision ("Phase 23 is done by previous commit"), delivered directly in commit `6c51695` alongside Phase 21's supersession and the `260810-qmm` quick task — no `.planning/phases/23-*/` directory, no formal plans, no discuss-phase ever ran. Verified before accepting the claim: `git show 6c51695 -- src/components/AboutPageBody.astro` confirms a real ABOUT-05-shaped change — `.about-page__bio-row` moved from `flex-direction: column-reverse` to `display: block` with the portrait `float: right` (64px) and text wrapping around it at ≤767px (was a stacked column before), desktop keeps the flex row with an explicit `order` swap (portrait now precedes the lead paragraph in DOM order too). UI-02's combined desktop/tablet regression sweep was accepted on the user's word, not independently re-run by this session. Requirements ABOUT-05 and UI-02 marked `[x]` in `REQUIREMENTS.md`.
- [Milestone v1.6 audit]: Ran `/gsd-audit-milestone 1.6` 2026-08-11 before archiving (user chose the thorough path over proceeding directly). Phase 20/21 VERIFICATION.md read directly; `gsd-integration-checker` spawned for cross-phase wiring and live E2E re-run. Found one real regression the prior reconciliation missed because it worked at the tracking-artifact level, not the runtime level: **HOME-16 (per-visit random accent color) was silently broken by commit `6c51695`.** `pickRandomGalleryIndex()` in `HomeCarousel.astro` still runs and writes `--current-accent` onto `.home`, but `.home`'s non-header children are `display:none` at phone width since `MobileHomePrototype.astro` took over that viewport; the actual phone-visible background is a separate, hardcoded mechanism (`phoneCanvasColor={galleries[0]?.heroColor}` in `index.astro:70`, applied via `BaseLayout.astro:188`) that never reads the random pick. Live-verified by the integration checker (three page loads at 390×844 with `Math.random` forced to 0/0.5/0.999, identical `document.body` background every time) and independently re-confirmed by the orchestrator via direct source read — the safety classifier was unavailable for that subagent run, so its claim was not accepted on its word alone. `homepage-accent-random.spec.ts` never catches this: it forces a 1280×800 desktop viewport in its own `beforeEach`. Audit written to `.planning/v1.6-MILESTONE-AUDIT.md`, `status: gaps_found`.
  Presented to the user with the standard gap-closure options (insert a closure phase, or proceed and accept as tech debt). User chose neither — explicit instruction "annule le HOME-16" (cancel HOME-16), the same disposition already applied to Phase 22's three requirements. `REQUIREMENTS.md` HOME-16 changed from `[x]` Complete to cancelled (unchecked, with the full history inline); `ROADMAP.md`'s Phase 20 line and success-criterion 3 annotated; the audit file's own `status` updated to `tech_debt` via an explicit `resolution` block in its frontmatter — the original `gaps_found` finding and evidence were preserved, not rewritten, matching how `21-VERIFICATION.md`'s `superseded_note` was handled during the earlier reconciliation. v1.6 is now clear to archive via `/gsd-complete-milestone 1.6` with 5/9 requirements delivered and 4/9 cancelled (Phase 22's three plus HOME-16).

- [Phase 05]: Contact-form recipient confirmed by the maintainer as contact@atelierjacquelinesuzanne.fr (D-07) — the existing publicEmail default, explicitly confirmed rather than assumed.
- [Phase 05]: PHP mail() endpoint validation tested via source-invariant text assertions (no PHP runtime), per 05-VALIDATION.md Wave 0's recommendation for a single ~110-line script.

### Pending Todos

None yet.

### Blockers/Concerns

- ⚠️ [Phase 20] `HomeCarousel.astro`'s touchstart/touchend handler (~lines 970-1002) doesn't exclude `.home-hero__caption` the way the equivalent desktop click handler does — a tap on the progress-dash buttons or the autoplay toggle on a real touchscreen can satisfy the tap-detection threshold and trigger `openCurrent()` (navigation) before the button's own handler runs, effectively breaking those controls on touch devices. Pre-existing, unrelated to Phase 20's own changes; found by `20-REVIEW.md`'s code review, not yet fixed. Fix via `/gsd-code-review 20 --fix` or a dedicated quick-task.

Both prior research-carryover items were resolved during Phase 1 execution:

- Domain email service: confirmed active (MX Plan + Zimbra mailbox) via the OVH panel — Phase 5's DNS cutover must preserve these records, not wipe the zone.
- OVH deployment method: confirmed via the OVH panel — "Free hosting" tier, SFTP enabled on port 22, host `ftp.cluster129.hosting.ovh.net`, user `atelihu`, home dir `/home/atelihu` (see 01-02-SUMMARY.md). Note: this same Free tier cannot attach any subdomain (multisite requires a paid tier) — Phase 1 staging used GitHub Pages instead; Phase 5's production cutover plan should account for the single-domain limitation.

(The v1.3 Rebut/Éditions naming-overlap question carried here previously was resolved during Phase 11 — see PROJECT.md Key Decisions; removed as a blocker.)

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260713-hcj | Make the grid-mode hero tile's Atelier Jacqueline Suzanne wordmark bigger on mobile and give it the same transparent photo-cutout effect as the carousel wordmark | 2026-07-13 | 1b1b9e1 | | [260713-hcj-make-the-grid-mode-hero-tile-s-atelier-j](./quick/260713-hcj-make-the-grid-mode-hero-tile-s-atelier-j/) |
| 260713-jfz | Add an animated transition between carousel and grid view modes using the View Transitions API, with shared-element morphing and graceful fallback | 2026-07-13 | 57733a8 | | [260713-jfz-add-an-animated-transition-between-carou](./quick/260713-jfz-add-an-animated-transition-between-carou/) |
| 260811-v3t | Make Sanity-content-triggered production deploys automatic, while keeping code-push-triggered production deploys manual | 2026-08-11 | 3af94aa | | [260811-v3t-make-sanity-content-triggered-production](./quick/260811-v3t-make-sanity-content-triggered-production/) |
| 260811-w8d | Build the real "Mettre en production" feature per sketch 016 (Variant C) — replaces 260811-v3t's raw auto-deploy with a human-checkpoint-gated Sanity Studio dashboard button | 2026-08-11 | d5b41ae | Verified | [260811-w8d-build-the-real-mettre-en-production-feat](./quick/260811-w8d-build-the-real-mettre-en-production-feat/) |
| 260812-bj1 | Rework the dashboard's vocabulary and layout (Étape 1/Étape 2 card, "site de test"/"site en ligne", 2-segment bar) so the publish→promote flow reads clearly for Romane | 2026-08-12 | 79b7891 | | [260812-bj1-retravailler-le-vocabulaire-et-la-mise-e](./quick/260812-bj1-retravailler-le-vocabulaire-et-la-mise-e/) |
| 260812-ca1 | Fix the invisible pipeline progress bar (.editorial-dashboard__pipeline-bar collapsed to zero width inside a non-stretching Sanity UI Stack) — add width:100%, redeploy Studio | 2026-08-12 | 88d5015 | | [260812-ca1-fix-the-invisible-pipeline-progress-bar-](./quick/260812-ca1-fix-the-invisible-pipeline-progress-bar-/) |
| 260812-dvq | Replace the sketch-016 pipeline UI with sketch 017 Variant B (manual approval gate, CI/CD motif) — two connected nodes + central gate button, redeploy Studio | 2026-08-12 | 9eb7a4c | | [260812-dvq-replace-the-sketch-016-pipeline-ui-in-th](./quick/260812-dvq-replace-the-sketch-016-pipeline-ui-in-th/) |
| 260812-f22 | Fix 4 dashboard pipeline UX issues found in user testing: label wrapping, misleading initial-state copy, redundant document list, redundant header badge | 2026-08-12 | 6a29af3 | | [260812-f22-fix-4-issues-in-the-new-dashboard-pipeli](./quick/260812-f22-fix-4-issues-in-the-new-dashboard-pipeli/) |
| 260812-fq3 | Remove excessive node-label margin (leftover reserved 2-line height) and redundant not-started copy in the dashboard pipeline UI | 2026-08-12 | 48fdf01 | | [260812-fq3-fix-excessive-node-label-margin-and-redu](./quick/260812-fq3-fix-excessive-node-label-margin-and-redu/) |
| 260812-h3i | Fix a verified @sanity/ui Text box-height bug causing pipeline node label/detail text to visually touch (swap to plain spans); move not-started framing into the header subtitle | 2026-08-12 | 9f99716 | | [260812-h3i-fix-sanity-ui-text-box-height-bug-in-pip](./quick/260812-h3i-fix-sanity-ui-text-box-height-bug-in-pip/) |
| 260812-lvt | Remove the dangling "Étape 2" title/detail from the not-started pipeline state (inconsistent with the current numbering-free UI); skip rendering an empty detail box | 2026-08-12 | 3f8bdb0 | | [260812-lvt-remove-the-inconsistent-tape-2-title-det](./quick/260812-lvt-remove-the-inconsistent-tape-2-title-det/) |
| 260812-mev | Remove the outdated "publish to the public site" confirmation dialog (premise false post staging/prod split); merge into single-click publish, preserving the preflight checklist gate | 2026-08-12 | 3070b3b | | [260812-mev-remove-the-outdated-public-confirmation-](./quick/260812-mev-remove-the-outdated-public-confirmation-/) |
| 260812-ncd | Fix a live, user-blocking bug: marker document create action was missing attributes._id (Sanity Actions API requirement), causing real publish to fail | 2026-08-12 | 5cd60ad | | [260812-ncd-fix-missing-attributes-id-on-marker-docu](./quick/260812-ncd-fix-missing-attributes-id-on-marker-docu/) |
| 260812-nqg | Second fix for the same live publish blocker: marker create action's attributes._id needed a "drafts." prefix per the Sanity Actions API | 2026-08-12 | bcc1a28 | | [260812-nqg-prefix-marker-create-action-s-attributes](./quick/260812-nqg-prefix-marker-create-action-s-attributes/) |
| 260812-o1d | Remove the redundant post-publish success text (duplicated the live pipeline visualization) and its empty-divider condition | 2026-08-12 | 4a25575 | | [260812-o1d-remove-the-redundant-post-publish-succes](./quick/260812-o1d-remove-the-redundant-post-publish-succes/) |
| 260812-p85 | Split the production release gate into a two-step preview-then-publish flow (play opens site de test; separate publish button requires preview first) | 2026-08-12 | 38e3a60 | | [260812-p85-split-the-production-release-gate-into-a](./quick/260812-p85-split-the-production-release-gate-into-a/) |
| 260812-q0d | Remove stale "Site en ligne à jour" text, make production segment pending-draft-aware (with a failure-precedence carve-out so real failures stay visible), relabel node 1 "Studio" with a blue modified state, add a gate-button site-de-test caption | 2026-08-13 | 0bcfd9e | | [260812-q0d-dashboard-pipeline-pending-draft-awarene](./quick/260812-q0d-dashboard-pipeline-pending-draft-awarene/) |
| 260813-g49 | Empty all promote-box status text across every pipeline stage (not just "done"), move status narration into the panel header subtitle via a new tested releasePanelSubtitle() helper, and decouple the "Publier sur le site en ligne" button/failure link from that removed copy so the production-release control can't silently disappear | 2026-08-13 | 8694e2a | | [260813-g49-dashboard-move-all-promote-box-status-te](./quick/260813-g49-dashboard-move-all-promote-box-status-te/) |
| 260813-gw2 | Merge the two dashboard publish buttons ("Mettre le site à jour" / "Publier sur le site en ligne") into one evolving control via a new tested releaseActionButtonState() helper, with the preview-before-publish safety gate double-enforced; fix a real bug where a deploy that never started rendered identically (blue, forever) to one genuinely in progress | 2026-08-13 | 2b669ef | | [260813-gw2-dashboard-merge-mettre-a-jour-publier-bu](./quick/260813-gw2-dashboard-merge-mettre-a-jour-publier-bu/) |
| 260713-kit | Fix the pink accent panel's view-transition fade — corrected to a sequential handoff (photo morph finishes, then panel fades in cleanly), not overlapping or popping | 2026-07-13 | 2a943ef | | [260713-kit-the-pink-accent-panel-now-appears-only-a](./quick/260713-kit-the-pink-accent-panel-now-appears-only-a/) |
| 260714-d6h | Improve the Sanity Studio EditorialDashboard "À faire maintenant" panel UI — urgency-distinct priority-section headers (tonal Card + circular tonal count badge) and denser, more scannable attention cards (tonal left accent bar, truncated Manque line, tonal completion badge) | 2026-07-14 | 1a19972 | | [260714-d6h-improve-the-sanity-studio-editorialdashb](./quick/260714-d6h-improve-the-sanity-studio-editorialdashb/) |
| 260718-qdz | Fix mode-toggle icon color regression on the homepage — restored the carousel-mode white / grid-mode ink color split for the display-mode toggle, silently dropped when Phase 10's SiteHeader refactor replaced the old `.home-header` container-level color rule with narrower per-element overrides | 2026-07-18 | 292488a | | [260718-qdz-fix-mode-toggle-icon-color-regression-on](./quick/260718-qdz-fix-mode-toggle-icon-color-regression-on/) |
| 260720-nm3 | Fix 2 UI-audit findings: darken the site-wide link/accent pink token (`--pink-600`) from `#FF3B94` to `#D6327C` to clear WCAG AA text contrast (4.56:1 vs white), and promote the homepage's two wordmark elements from `<p>` to semantic `<h1>` (homepage previously had zero heading elements). A 3rd audit finding — a colliding Sanity gallery slug (`/galleries/adults/` mistakenly used by the "Paysage" gallery, colliding with the real "Adults" gallery's `/galleries/adult/`) — was fixed directly via the Sanity CLI, renamed to `/galleries/paysage/`, outside this code plan | 2026-07-20 | 1e28b74 | | [260720-nm3-fix-3-ui-audit-findings-1-site-wide-pink](./quick/260720-nm3-fix-3-ui-audit-findings-1-site-wide-pink/) |
| 260721-jm0 | Fix 6 Sanity Studio EditorialDashboard audit findings: dark-mode-aware backgrounds (was hardcoded light-only hex), inconsistent gray row background, human error copy (raw error moved to a collapsed `<details>`), exclude intentional "en préparation" drafts from the urgent to-do bucket, level-3 heading semantics for priority-group titles, dead CSS class + deployment tone dead-code cleanup | 2026-07-21 | 05faefe | Verified | [260721-jm0-fix-sanity-studio-editorialdashboard-iss](./quick/260721-jm0-fix-sanity-studio-editorialdashboard-iss/) |
| 260718-r2o | Fix grid-mode hero tile text legibility on dark per-gallery accents (Violet/Plum) — wired a `--current-accent-text` companion CSS variable, mirroring the existing `--current-accent` mechanism, so the grid tile's text color follows the same WCAG-contrast value the carousel accent panel already used correctly | 2026-07-18 | 7900284 | [260718-r2o-fix-the-homepage-per-gallery-accent-colo](./quick/260718-r2o-fix-the-homepage-per-gallery-accent-colo/) |
| 260718-rhv | Fix grid-tile title misalignment (always-reserve description height + single-line title clamp for long gallery names) and add a per-tile accent-color hover tint + subtle title lift, human-verified live for alignment and on-brand legibility | 2026-07-18 | 0cd2741 | [260718-rhv-fix-grid-mode-tile-title-misalignment-an](./quick/260718-rhv-fix-grid-mode-tile-title-misalignment-an/) |
| 260720-dzi | Add CI/tooling quality gates from the quality-auditor findings: blocking `astro check` type-check step in CI, root flat ESLint config + wired lint script, Sanity Studio lint script, and Vitest v8 coverage reporting — fixed 3 genuine pre-existing type errors and several real lint violations surfaced by the new gates | 2026-07-20 | 725871e | [260720-dzi-add-ci-tooling-quality-gates-astro-check](./quick/260720-dzi-add-ci-tooling-quality-gates-astro-check/) |
| 260720-dzo | Fix stale CLAUDE.md/AGENTS.md/research/STACK.md "Technology Stack" docs (retired Cloudflare/Stripe-in-Workers plan replaced with the real static-Astro + GitHub Pages/OVH + Sanity architecture, e-commerce marked deferred to v1.x) and add a root README.md developer quick-reference | 2026-07-20 | b1b0520 | [260720-dzo-fix-the-stale-claude-md-agents-md-techno](./quick/260720-dzo-fix-the-stale-claude-md-agents-md-techno/) |
| 260720-dzs | Extract HomeCarousel.astro's pure computational logic (wordmark crop/scale math, swipe-direction detection) into a testable `src/lib/home-carousel.ts` module with unit tests, mirroring the i18n-paths.ts/site-config.ts pattern — pure refactor, zero behavior change, full e2e suite green | 2026-07-20 | 1ecfc21 | [260720-dzs-extract-homecarousel-astro-s-pure-comput](./quick/260720-dzs-extract-homecarousel-astro-s-pure-comput/) |
| 260720-sanity-dashboard-density | Simplify the Sanity dashboard density: retain two useful quick actions, remove redundant validation details from attention rows, and increase vertical rhythm throughout the main sections | 2026-07-20 | 7cee97a | [260720-sanity-dashboard-density](./quick/260720-sanity-dashboard-density/) |
| 260720-sanity-dashboard-activity-authors | Turn the Sanity dashboard's recent activity into a real editorial log using transaction history: identify the author, classify the action, summarize changed fields, and retain a safe fallback for purged history | 2026-07-20 | 3bff5d7 | [260720-sanity-dashboard-activity-authors](./quick/260720-sanity-dashboard-activity-authors/) |
| 260720-sanity-dashboard-compact-layout | Recompose the Sanity dashboard as a compact responsive editorial cockpit: header toolbar, horizontal metrics, single-line task rows, and a desktop 2/3 + 1/3 tasks/activity layout | 2026-07-20 | e64b856 | [260720-sanity-dashboard-compact-layout](./quick/260720-sanity-dashboard-compact-layout/) |
| 260720-sanity-dashboard-three-pass-polish | Complete three consecutive UI audit/fix loops on the compact Sanity dashboard: fix hierarchy and repetition, consolidate responsive metrics, strengthen affordances, and remove a duplicate styled-components runtime caught during the final audit | 2026-07-20 | 46d57fe | [260720-sanity-dashboard-three-pass-polish](./quick/260720-sanity-dashboard-three-pass-polish/) |
| 260720-remove-dashboard-home-action | Remove the redundant « Modifier l'accueil » action from the Sanity dashboard header, leaving only collection creation and site preview | 2026-07-20 | a12a615 | | [260720-remove-dashboard-home-action](./quick/260720-remove-dashboard-home-action/) |
| 260720-apply-personal-dashboard-ui-audit | Apply the personal dashboard UI audit: primary collection action, semantic iconography, vertical KPI structure, separate deployment status, stable task metadata, comfortable targets, fixed activity dates, and native Sanity card depth | 2026-07-20 | ce8bbb6 | | [260720-apply-personal-dashboard-ui-audit](./quick/260720-apply-personal-dashboard-ui-audit/) |
| 260720-polish-dashboard-pixel-perfect | Complete the dashboard pixel-polish pass: tinted canvas and white surfaces, explicit low-emphasis site status, per-task icons, compact targets, resilient activity dates, responsive header/KPI behavior, and hover/focus/accessibility states | 2026-07-20 | cfa3d84 | | [260720-polish-dashboard-pixel-perfect](./quick/260720-polish-dashboard-pixel-perfect/) |
| 260720-fix-dashboard-spacing-and-task-details | Standardize dashboard control heights and icon spacing, rebuild priority rows on a stable grid, and explicitly display the remaining work for each content item | 2026-07-20 | ae2591c | | [260720-fix-dashboard-spacing-and-task-details](./quick/260720-fix-dashboard-spacing-and-task-details/) |
| 260720-rebuild-dashboard-optical-grid | Rebuild the regressed dashboard header, priority rows, activity rows and KPI rhythm around shared optical text/icon axes, with two buttons and a separate compact site status | 2026-07-20 | 69b6f7d | | [260720-rebuild-dashboard-optical-grid](./quick/260720-rebuild-dashboard-optical-grid/) |
| 260722-afi | Corriger le contraste visuel entre l'en-tête et les items du tableau de bord éditorial (section Informations manquantes) — recessed header band, hairline divider, uppercase section-label title, human-verified via mockup replicating the exact CSS in light + dark mode | 2026-07-22 | 9762000 | Verified | [260722-afi-corriger-le-contraste-visuel-entre-l-en-](./quick/260722-afi-corriger-le-contraste-visuel-entre-l-en-/) |
| 260722-tcv | Close the complete test-coverage audit: blocking thresholds, Sanity pure-logic coverage/build gate, contact failure states, iPhone/WebKit smoke, deterministic CMS/static-route cases, 404/base-path artifact verification, axe accessibility checks, and portable visual baselines | 2026-07-22 | — | Verified | [260722-tcv-close-test-coverage-audit-gaps](./quick/260722-tcv-close-test-coverage-audit-gaps/) |
| 260723-qiz | Fix Éditions overview page header coherence: dropped the mismatched `headerVariant="transparent"` override (built for full-bleed hero pages) so `/editions/` and `/en/editions/` fall back to the shared `solid` header, matching About/Contact; added a Playwright regression guard | 2026-07-23 | 1ff46a2, d2c7d6f | | [260723-qiz-improve-ui-of-the-editions-page-notammen](./quick/260723-qiz-improve-ui-of-the-editions-page-notammen/) |
| 260723-r1e | Fix Éditions overview reversed-row layout: added `grid-row: 1` (+ mobile `grid-row: auto` reset) so CSS Grid auto-placement stops splitting alternating rows' photo/text into two stacked implicit rows; added a Playwright regression guard; fix independently re-verified via direct DOM inspection after a port-collision false test failure | 2026-07-23 | fb13388, 0d3cf8c | | [260723-r1e-fix-editions-overview-reversed-row-layou](./quick/260723-r1e-fix-editions-overview-reversed-row-layou/) |
| 260723-txi | Rebuild Éditions overview as the sketch-approved asymmetric Poster Grid (grouped-by-3 hero+small tiles, alternating side, opt-in bold EmptyState variant) and restyle the détail page's format-details line (bold/uppercase/pink underline); repaired the full edition e2e suite (166/166 passing, independently re-verified) | 2026-07-23 | d6a7cf8, 62bab00, fbb90c0 | | [260723-txi-rebuild-the-editions-overview-page-as-th](./quick/260723-txi-rebuild-the-editions-overview-page-as-th/) |
| 260724-dlc | Apply sketch 004 winner (A2) to the shared GalleryGrid.astro thumbnail grid: props-based asymmetric bento (generalizes across real counts 2-11), staggered scroll-reveal, and a click-to-expand View Transitions morph wired into Lightbox's open/close/Escape/backdrop lifecycle across all 4 gallery/édition detail pages (FR/EN); 168/168 e2e passing, independently re-verified | 2026-07-24 | 3032126, 23cdcd9, d299593, f6b3d97 | | [260724-dlc-apply-the-a2-asymmetric-bento-scroll-rev](./quick/260724-dlc-apply-the-a2-asymmetric-bento-scroll-rev/) |
| 260724-dw6 | Add EDN-08, an optional unidirectional Éditions↔Portfolio cross-link: new `relatedGallery` Sanity reference field on `edition`, dereferenced in both édition GROQ queries, rendered via a pure locale-aware `getRelatedGalleryLink` helper as a quiet "see also" link on both détail-page twins (renders nothing until a Studio editor populates the field); 169/169 e2e passing, independently re-verified | 2026-07-24 | 51d4816, d961ae5, 81c2af0, 32e0798 | Verified | [260724-dw6-add-edn-08-an-optional-cross-link-on-the](./quick/260724-dw6-add-edn-08-an-optional-cross-link-on-the/) |
| 260724-l5i | Build sketch 005's winning variant (Synthesis — Bold + Facts) on the édition detail hero: new shared `EditionHero.astro` scroll-reveal island, ported verbatim from the proven sketch — sticky-pinned lead photo shrinks 100%→55% width on real page scroll, title fades in large-scale, then the pink-underlined format line crystallizes; genuine `prefers-reduced-motion` + mobile CSS fallbacks (JS sets no inline styles in either branch); Lightbox/expand-icon/exactly-one-h1/single-`.edition-detail__format` contracts preserved on both FR/EN twins; 171/171 e2e passing, independently re-verified | 2026-07-24 | 7ee9093, 8e0216b, bd39482 | Verified | [260724-l5i-build-sketch-005-s-winning-variant-synth](./quick/260724-l5i-build-sketch-005-s-winning-variant-synth/) |
| 260724-mjp | Extend the sketch-005 Synthesis scroll-reveal hero to Portfolio gallery detail pages: generalized `EditionHero.astro` into a shared `DetailHero.astro` (optional `formatText`/`caption`/`total`, new `objectFit` prop) and wired both gallery twins with `objectFit="contain"` so gallery hero photos are never cropped (unlike the édition hero's unchanged `cover` default) — the cover photo also becomes clickable for the first time (opens Lightbox at index 0); `gallery.images.slice(1)` grid skip, full-array Lightbox, and SEO preserved; 175/175 e2e passing incl. 4 new gallery hero tests, independently re-verified | 2026-07-24 | fe86708, 3c10f40, 5cc6a7e, 86a7993 | Verified | [260724-mjp-extend-the-sketch-005-synthesis-scroll-r](./quick/260724-mjp-extend-the-sketch-005-synthesis-scroll-r/) |
| 260724-oep | Two related gallery fixes driven by new real Sanity image-dimension metadata: (1) hero now prefers the first landscape photo via a pure `pickHeroIndex` helper instead of hardcoded `images[0]`, with careful Lightbox index remapping (hero trigger/grid/aria-label/counter all agree on the real position); (2) gallery thumbnail grid switched from cropped bento to an uncropped CSS multi-column masonry (`layout="masonry"` opt-in on the shared `GalleryGrid.astro`, ported from sketch 004 Variant B) — editions keep bento + index-0 hero unchanged. 176/176 e2e passing incl. a definitive index-remapping correctness proof, independently re-verified on real content | 2026-07-24 | 9c64cf8, 80a04de, 65eae11, e74813f, fd89cb6, 80fc77e, 1d6735b | Verified | [260724-oep-two-related-fixes-to-the-just-shipped-po](./quick/260724-oep-two-related-fixes-to-the-just-shipped-po/) |
| 260724-rhq | Moved the description/statement paragraph on all 4 édition/gallery detail-page twins out of the below-hero content block and into the shared `DetailHero.astro` reveal panel (order: caption → title → statement → format), as a clean move (not duplicated) — added a real non-`aria-hidden` `.detail-hero__statement`, a `<noscript>` fallback so it's reachable with JS disabled, and a 4-line clamp so the panel can't overflow the fixed-height hero at any breakpoint. 177/177 e2e passing incl. a new no-JS reachability test; independently re-verified with an actual `javaScriptEnabled:false` browser context | 2026-07-24 | c8a4cc6, ed01afa, e46499d, 3b45797 | Verified | [260724-rhq-move-the-description-text-the-statement-](./quick/260724-rhq-move-the-description-text-the-statement-/) |
| 260724-uf5 | Three related changes from sketch 006 validation: (1) reverted the gallery hero to `object-fit: cover` (removed the now-dead `objectFit` prop entirely) per explicit user reversal after seeing the no-crop version live; (2) added a shared bouncing scroll-down chevron to `DetailHero` (éditions + galleries), reduced-motion/mobile safe; (3) implemented a native cross-document View Transition morphing the homepage carousel/grid photo into the gallery hero, header persisting via the reused `ajs-header` name, with the `hero-photo` destination name desktop-gated only (`min-width: 768px`) as a direct precaution against this codebase's own documented HOME-06/D-10/D-12 mobile-regression history. 183/183 e2e passing (full suite, incl. the pre-existing HOME-06 test staying green); independently re-verified end-to-end in a real browser (`pagereveal`/`event.viewTransition`, desktop `hero-photo`/mobile `none` name gating) | 2026-07-24 | fd3157b, ec8b832, 9bfc8bc, aa714cd, 2d5cc6f | Verified | [260724-uf5-three-related-changes-to-the-gallery-det](./quick/260724-uf5-three-related-changes-to-the-gallery-det/) |
| 260724-wdr | Two fixes from live testing of the 260724-uf5 cross-page hero morph: (1) homepage cover selection now reuses `pickHeroIndex` (both fr/en twins) instead of hardcoding `images[0]`, so the homepage always shows the same photo as the gallery detail hero — fixes the visible "shift" on portrait-first galleries (Paysage); (2) `DetailHero`'s scroll-down hint gained a required, locale-aware text label ("Faire défiler" / "Scroll") above a larger chevron, wired through all four detail-page twins as a genuinely required prop so a missed twin fails `astro check`. 77/77 targeted e2e passing (incl. a new homepage-tile/detail-hero photo-pathname-identity proof and both label assertions); independently re-verified via real `npm run build` + isolated-port Playwright run | 2026-07-25 | b118c69, 13b6a6d | Verified | [260724-wdr-two-fixes-from-live-user-testing-of-the-](./quick/260724-wdr-two-fixes-from-live-user-testing-of-the-/) |
| 260725-cfm | In carousel mode on the homepage, scrolling down now opens the currently-shown collection directly: a "keep scrolling to open" bouncing chevron + locale-aware label (mirroring `DetailHero`'s visual language) appears once the visitor starts scrolling, and continuing to overscroll past the bottom of the page (deliberate threshold, 600px) navigates via the existing title link's href/cross-document morph — the accumulator only arms once already at the bottom, so the footer is always reachable by normal scrolling first; grid mode is unaffected; reduced motion disables the bounce only. Independent verification (real Sanity credentials, isolated-port Playwright) caught and fixed a real bug the executor's own run couldn't reach: the overscroll accumulator stayed stuck after returning to the top via a non-wheel/non-touch scroll (`scrollTo`, keyboard, scrollbar drag), leaving the hint visibly stuck and weakening the "deliberate threshold" guarantee — fixed by resetting the accumulator on any move off the bottom, regardless of input. 187/187 e2e passing full-suite after the fix | 2026-07-25 | 26f33cd, fafa362, 867b47c, 3a899bf | Verified | [260725-cfm-in-carousel-mode-on-the-homepage-homecar](./quick/260725-cfm-in-carousel-mode-on-the-homepage-homecar/) |
| 260725-dcg | Three follow-up fixes from live testing of 260725-cfm's scroll-to-open: (1) the site footer is now hidden entirely while the homepage is in carousel mode (still reachable via grid mode) via a `body:has(.home[data-display-mode='carousel']) footer.chrome-band` rule, crossing the HomeCarousel/BaseLayout component-scope boundary since a `<main>` wrapper prevents a plain sibling selector; (2) the "keep scrolling to open" hint's inverted visibility was fixed (now visible at rest, fading over the first ~150px of scroll, mirroring `DetailHero`'s exact formula, instead of starting hidden); (3) the existing per-gallery progress dash gained a dynamic Instagram-Stories-style accent fill synced to the real 6000ms auto-advance cycle, freezing on pause and restarting on every gallery change. Independent verification (real Sanity credentials, live browser spot-check) caught two real issues beyond the plan's own scope: (a) making the hint visible at rest exposed a pre-existing layout collision with `.home-hero__accent` (desktop) and `.home-hero__caption` (mobile) — fixed by repositioning the hint near the top of the hero, clear of both on every breakpoint, with a permanent overlap regression test; (b) hiding the footer broke two pre-existing, compliance-relevant tests outside this task's own spec file (`i18n.spec.ts`, `legal.spec.ts`'s mentions-légales/confidentialité footer-link reachability) — fixed by updating them to the new footer-hidden-in-carousel contract rather than weakening the compliance guarantee. 196/196 e2e passing full-suite after both fixes | 2026-07-25 | 9cff087, 102d51c, b8f3e17, abc35ef, cd0a122 | Verified | [260725-dcg-three-follow-up-fixes-from-live-testing-](./quick/260725-dcg-three-follow-up-fixes-from-live-testing-/) |
| 260725-pit | Simplified the carousel scroll-to-open gesture per direct live-testing feedback ("the long scrolling is quite strange"; the hint was disliked, seen floating mid-photo in a live screenshot): removed the "keep scrolling to open" hint entirely (markup, CSS, keyframes, locale const, all script wiring) — the gesture is now silent/implicit — and lowered `OPEN_OVERSCROLL_THRESHOLD` from 600 to 150 (kept the accumulator rather than firing on a single delta, since trackpad scrolling emits many tiny per-event deltas; 150 sits just above one mouse-wheel notch to avoid stray-tick misfires while feeling near-immediate; documented as a tunable). The at-bottom gate, no-`preventDefault` guarantee, and the scroll-listener's stale-accumulator-reset fix (from cfm's own verification) were all preserved; footer-hide and the progress-dash fill (dcg) were untouched. 194/194 e2e passing full-suite; independently re-verified live in a real browser (`wheel` events dispatched directly: 200px triggers real navigation, 60px correctly does not) | 2026-07-25 | c0c7145, e334b3a | Verified | [260725-pit-simplify-the-carousel-scroll-to-open-ges](./quick/260725-pit-simplify-the-carousel-scroll-to-open-ges/) |
| 260725-sj4 | Fixed a serious accidental-navigation bug found via a requested live audit of the carousel scroll-to-open feature: because 260725-dcg hid the footer entirely in carousel mode, the homepage had zero scrollable content below the hero, making the `atBottom()` gate vacuously TRUE from the very first paint at scrollY 0 — confirmed live, two ordinary 80px mouse-wheel nudges (desktop) or one natural ~180px touch swipe (mobile) silently navigated a first-time visitor to a random gallery page with zero warning, on a totally normal "let me look around" scroll. Fix: removed the footer-hide CSS rule, restoring the footer to visible in carousel mode — this reinstates genuine scroll distance below the hero so `atBottom()` is correctly false at load, giving the light 150px overscroll threshold (unchanged, from 260725-pit) a real gate to sit on top of again; also better satisfies the original pre-dcg footer feedback ("put it at the end of the page, on the end of the scroll"). Reverted the 3 spec files' footer-hidden test assumptions (homepage.spec.ts, i18n.spec.ts, legal.spec.ts's mentions-légales/confidentialité reachability) and added new fresh-load regression coverage using synthetic WheelEvent/TouchEvent dispatch — the missing test class that let the bug ship undetected. 196/196 e2e passing full-suite; independently re-verified by replaying the exact live-browser repro that found the bug (confirmed it now stays on the homepage) and the positive path (genuine scroll-to-bottom still opens the collection) | 2026-07-25 | 956fb61, 016f214, 94fe950 | Verified | [260725-sj4-fix-a-confirmed-accidental-navigation-bu](./quick/260725-sj4-fix-a-confirmed-accidental-navigation-bu/) |
| 260725-tqs | Six related changes polishing the carousel<->collection transition, from direct live-testing feedback: (1) the gallery detail page's visible overlay title now matches the homepage carousel title's end-state (18px/left 16px/uppercase), crossfading in place via the existing site-wide `@view-transition{navigation:auto}` instead of jumping — a shared cross-document view-transition-name was deliberately deferred (same HOME-06/D-10/D-12 always-on-name risk class); (2) removed the carousel's per-slide byline/description; (3) added a silent custom down-chevron cursor over the hero photo (no visible hint, per 260725-pit); (4) narrowed the accent panel `min(800px,60%)`->`min(700px,52%)` with a retuned wordmark clamp (verified via a standalone geometry harness: zero clip 768-1920px) and a matching caption offset; (5) resized/repositioned the sitewide intro paragraph (16px->15px, confined to ~left half on desktop); (6) NEW FEATURE — scrolling up while genuinely at the top of a gallery detail page returns to the homepage carousel on the SAME gallery, the mirror image of the scroll-to-open gesture, explicitly designed against the quick-260725-sj4 failure mode via a `hasEngaged` gate (armed only after a genuine 300px+ scroll down) so a fresh scrollY-0 load can never misfire — opt-in via `carouselReturnHref` (gallery pages only, édition pages inert), landing via a safelymatched `?carousel=<slug>` findIndex read (no selector injection). 212/212 e2e passing full-suite; independently re-verified live end-to-end (title match, cursor, panel geometry, and the full scroll-up-to-return gesture including the `?carousel=` init and édition-exclusion) | 2026-07-26 | eb3dff2, 135b831, 3967f47, 2906ea7, a767ac6 | Verified | [260725-tqs-improve-the-carousel-to-collection-trans](./quick/260725-tqs-improve-the-carousel-to-collection-trans/) |
| 260726-ltr | Three follow-up fixes from live-testing feedback on the carousel-collection transition: (1) hid the site footer on gallery detail pages only (not éditions) via a new opt-in `hideFooter` BaseLayout prop, mirroring the gallery-only `carouselReturnHref` scoping — carried a real prior-incident safety burden (quick-260725-sj4's homepage footer-hide misfire) that was explicitly investigated and cleared: the sj4-affected `atBottom()` gate doesn't exist on gallery pages, and the tqs return-gesture's `hasEngaged` gate fails safe under reduced scroll height (harder to arm, never vacuous) — DetailHero's own `calc(100svh + 900px)` desktop scroll track provides 900px independent of the footer/grid, comfortably above the 300px `ENGAGE_DISTANCE` requirement even for a single-image gallery with no grid; (2) widened the homepage intro paragraph from `max-width: 50%` to `66.67%` (~2/3) of the accent panel on desktop; (3) fully removed the now-useless "Faire défiler"/"Scroll" hint (`.detail-hero__scroll-hint`) from the shared gallery/édition detail hero — markup, CSS, keyframes, both media overrides, the fade listener, and the `scrollHintLabel` prop across all 4 detail-page twins. 212/212 e2e passing full-suite (an initial 5-worker run produced 12 spurious timeouts from preview-server resource contention under parallel load — confirmed as a test-environment artifact, not a regression, by re-running with reduced parallelism); independently re-verified against the specific safety proofs (footer absent on galleries/present on éditions, >=300px scroll track maintained, the pre-existing tqs return-gesture guards still passing with the footer hidden) | 2026-07-26 | f36c724, a245e26, fbe7a83, 1c99b94 | Verified | [260726-ltr-three-follow-up-fixes-hide-the-site-foot](./quick/260726-ltr-three-follow-up-fixes-hide-the-site-foot/) |
| 260726-obg | Reintroduced the homepage carousel's footer-hide (quick-260725-sj4's precondition — `atBottom()` vacuously true at scrollY 0 on desktop — is now a deliberate, documented tradeoff) safely, by porting sketch 007's user-approved Variant C as a real overscroll safety signal: the hero photo itself scales down (~0.94 min) and a new `.home-hero__pull-overlay` radially darkens it (~0.85 max opacity), both proportional to overscroll accumulation, well before the existing 150px threshold — replacing the footer's old accidental scroll-distance buffer with an early, visible warning. Added a proactive idle-decay watchdog (a stopped gesture now visibly recedes to neutral on its own, rather than sitting stale until the next scroll event) and a synchronous, un-eased neutral-reset immediately before the real navigation click fires, so the outgoing cross-document `hero-photo` View Transition snapshot never captures a mid-pull frame. Reduced motion keeps the full feedback values (only the easing drops) since it's now the sole safety signal. Reworked the falsified sj4 regression tests to the new early-warning contract (a single 80px tick already shows clear feedback, before any navigation — crossing 150-160px and navigating is now correct, not a bug) and re-applied the grid-mode-detour fix to `i18n.spec.ts`/`legal.spec.ts`. Also removed the persistent carousel-title underline per direct feedback (BaseLayout's global `a:hover` rule required an explicit override on hover/focus too). 223/223 e2e passing full-suite. Independent live-browser verification (beyond the automated suite) traced an initial false alarm to a testing-methodology gap, not a real bug: reading the img/overlay's actual `transform`/`opacity` synchronously right after a dispatched event catches the pre-transition value (CSS transitions need a rendered frame to engage) — confirmed correct with a short wait, then verified every safety property live: early-warning feedback, decay-on-release, the instantaneous pre-navigation reset (captured with zero wait, proving it lands before any transition could run), footer scoping both directions, reduced motion, and mobile touch. Executor was interrupted twice mid-run by a monthly spend-limit error (unrelated to the code) and resumed both times from its own uncommitted worktree state with no work lost | 2026-07-26 | 8ed91f9, d1042c3, aec6048 | Verified | [260726-obg-implement-sketch-007-variant-c-overscrol](./quick/260726-obg-implement-sketch-007-variant-c-overscrol/) |
| 260726-qem | Fixed a visual regression in quick-260726-obg's overscroll pull feedback, reported live by direct user feedback ("zoom out effect and a black frame"): `.home-hero__img` sat at `inset: 0` with zero overhang, so the pull-scale-down (to 0.94 at the threshold) shrank the photo away from every edge and exposed `.home-hero__photo`'s `background-color: #000` as a solid black border — a regression against the approved sketch 007 spec, which used a `-3%` overhang specifically to avoid this. Fixed by giving `.home-hero__img` a centered 4%-per-side overhang reserve, consumed as bleed during the pull instead of exposing black — but the plan's literal `inset: -4%` (width/height removed) shorthand turned out not to work at all for `<img>` (a CSS replaced element resolves auto width/height from intrinsic size, not inset offsets, even with all four insets specified — a real, non-obvious CSS gotcha caught via live rect measurement, not assumed), so the executor switched to explicit `top: -4%; left: -4%; width: 108%; height: 108%`, verified via the same live measurement to actually inflate the box correctly. Added a genuine rendered-pixel regression test (screenshots thin edge bands at the forced 0.94 floor, decodes them in-browser via createImageBitmap/OffscreenCanvas, asserts real photo content rather than solid black — no new npm dependency) that provably fails on a temporary revert, closing the exact verification gap (computed-value-only checks) that let the original bug ship. 226/226 e2e passing full-suite. Independently re-verified live via the real wheel-gesture path (not forced CSS vars): confirmed geometrically that the image box now exceeds the container on all four sides at the pull floor, and visually via fresh screenshots (rest framing, full pull, edge crops) that zero black band remains and the wordmark photo-cutout still aligns correctly | 2026-07-26 | b23352e, 78494fd, eefc0e5 | Verified | [260726-qem-fix-black-frame-regression-in-the-homepa](./quick/260726-qem-fix-black-frame-regression-in-the-homepa/) |
| 260726-u97 | Following a step-back UX + code-quality review (a realistic 2-3 tick scroll was confirmed live to cross the navigation threshold, and a code audit found most of the accumulated complexity was compensation for that risk, not the underlying idea), removed the homepage carousel's scroll-to-open gesture entirely — every constant/state/function/listener from quick-260725-cfm/dcg/pit/sj4 and quick-260726-obg/qem, plus the `.home-hero__img` overhang qem added and the down-chevron cursor — and replaced it with sketch 008's user-approved Variant C: a custom hover cursor (permanent "OUVRIR"/"OPEN" ring in the center, morphing into an accent-tinted directional pill within the confirmed 22% edge zones), a parallax-push peek preview of the adjacent gallery's real photo, and click-to-navigate (center opens, edges go prev/next), with a plain-tap mobile fallback composed into the existing swipe-detection handler (no second listener). The carousel-mode footer-hide is kept unchanged and is now risk-free — nothing reads scroll position to navigate anymore, so the vacuous-`atBottom()` concern from quick-260725-sj4 cannot recur. New pure `computeHoverZone()` extracted to `src/lib/home-carousel.ts` with unit tests, following the established quick-260720-dzs pattern (unlike the deleted accumulator, which was never extracted or unit-tested). 227/227 e2e passing full-suite. Independent live-browser verification with real screenshots (not just computed-style checks, closing the exact gap that let qem's black-frame bug ship) confirmed the desktop cursor and peek animation are visually correct, but caught a genuine bug the automated suite missed: the cursor's base invisible/out-of-flow CSS was nested entirely inside the desktop-only `(hover:hover) and (pointer:fine)` media query, so on touch devices it fell back to browser defaults and rendered as a visible "OUVRIR→" text block — invisible in one screenshot only by the coincidence of the site header's higher stacking context painting over it. Fixed by moving the base `position:absolute`/`opacity:0`/`pointer-events:none` to an ungated rule, with a new regression test proven to fail on revert, then re-verified live on both desktop and an emulated mobile context | 2026-07-26 | d104d1d, 2230403, 840abe2, e24d5d2, 98bcf66, f59bf73 | Verified | [260726-u97-remove-the-scroll-to-open-gesture-entire](./quick/260726-u97-remove-the-scroll-to-open-gesture-entire/) |
| 260727-bsm | Fixed three bugs in sketch 008's hover-navigation carousel (quick-260726-u97), reported live after shipping and independently root-caused via live Playwright measurement before dispatch (not re-diagnosed by the executor): (1) the wordmark photo-cutout froze relative to the hero photo during a peek push, because `syncWordmarkAlignment()` only ran on load/resize, never while the photo was transform-shifting — fixed with a small self-scheduling `requestAnimationFrame` pump (`keepWordmarkSynced`/`pumpWordmarkSync`) that keeps re-syncing for ~500ms past the last peek-transform change, wired into `updatePeek()`/`resetPeek()`; (2) the custom hover cursor jittered in Safari (not Chrome) because one `transform` value carried both per-mousemove position tracking and an eased scale/geometry morph under one shared transition, so every mousemove retargeted an in-flight transition (and silently dropped `scale()`) — fixed by splitting the cursor into an untransitioned outer position anchor and a new inner `.home-hero__cursor-ring` owning all eased morph transitions; (3) an edge-zone click caused an abrupt content "pop" because `goToNext()`/`goToPrev()` swapped `heroImg.src` synchronously while the photo was still at its peek-pushed offset, then eased back in the wrong direction relative to the peek just shown — fixed with a new `commitEdge()` that continues the in-progress peek to a full slide first, then swaps content synchronously under the existing `.is-opening` transition-disabled class once the slide's `transitionend` fires (480ms fallback timer), behind a `committing` re-entrancy guard; keyboard/dash/swipe/auto-advance untouched. No new pure functions added to `src/lib/home-carousel.ts` (all three fixes are timing/structure, not math). 81/81 homepage e2e, 232/232 full e2e suite (chromium + webkit-mobile), 154/154 unit, `astro check` clean. Independently re-verified live via Playwright MCP with frame-by-frame timing and real screenshots: confirmed the wordmark bg-position now tracks the photo's live transform pixel-for-pixel through a full push, confirmed the cursor's outer position element carries no `transform` transition while the ring's morph still eases (visual parity screenshots, center/edge states), and confirmed the edge-click sequence is a single continuous slide from the pre-click offset to full commit followed by one pixel-coincident content swap at a neutral transform — no pop, no direction reversal, no third-image flash. Bug B's Safari-specific jitter cannot be verified in this Chromium-only environment; flagged for the user to confirm live in Safari after deploy | 2026-07-27 | b167a56, 5aa4c68, 4425a54 | Verified | [260727-bsm-fix-wordmark-peek-desync-safari-cursor-j](./quick/260727-bsm-fix-wordmark-peek-desync-safari-cursor-j/) |
| 260727-drq | Follow-up to 260727-bsm, reported live after that deploy — this time diagnosed using REAL desktop WebKit (`playwright-core`'s bundled `webkit` launcher, not just Chromium) for direct engine comparison, closing the "can't verify Safari" gap from the prior task. Four fixes: (1) the peek/photo transform still jittered in Safari — `updatePeek()` retargeted an in-flight 420ms eased `transform` transition on every mousemove (same anti-pattern as bsm's cursor fix, just not yet applied here); measured WebKit's peek-layer motion jerk at ~18x Chromium's for an identical continuous-approach gesture before the fix, and confirmed the two engines converge to near-parity (~1.07x) after — fixed with a new `.is-tracking` class that disables the transform transition for the whole continuous hover, re-enabled only for the two discrete moments (mouseleave's `resetPeek()`, `commitEdge()`'s full-slide commit) that should still ease; (2) the wordmark cutout went fully BLANK (not just desynced) for some letters at high peek proximity, confirmed live in plain Chromium too (a pure math bug, not an engine quirk) — `computeWordmarkBackgroundPosition()`'s raw position could sample past the rendered photo's actual pixel bounds, and with `background-repeat: no-repeat` that renders transparent, letting the solid accent color show through glyph cutouts — fixed by clamping both axes to `[-(rendered − box), 0]` with a degenerate-bound safeguard, TDD (failing tests first); (3) the accent panel's background-color had no transition at all, so every carousel navigation hard-cut the color — added `transition: background-color 300ms ease`; (4) restored the "Faire défiler"/"Scroll" hint on the gallery/édition detail hero (byte-exact revert of quick-260726-ltr's premature removal, `git show a245e26^` as reference), unconditionally on both page types, plus the e2e coverage `1c99b94` had removed, additively alongside the footer-scoping tests it added. 156/156 unit (incl. 2 new clamp cases), 237/237 full e2e (chromium + webkit-mobile), `astro check` clean. Executor was interrupted once mid-run by a monthly spend-limit error after Task 2 (with Task 1's edits already made but uncommitted) and resumed cleanly with no work lost. Independently re-verified live: re-ran the same real-WebKit jerk measurement against the fixed code (engine divergence gone), screenshotted the wordmark at the exact previously-broken peek position (now fully rendered, no blank glyphs), sampled the accent color through a real navigation (smooth 12-step crossfade over ~300ms), and confirmed the scroll hint renders with the correct locale label on all 4 routes (FR/EN × gallery/édition) and fades correctly on scroll | 2026-07-27 | 4f89249, 9cb6db0, 28dace4, 01b83d3, 5a1d729 | Verified | [260727-drq-fix-safari-peek-transition-jitter-wordma](./quick/260727-drq-fix-safari-peek-transition-jitter-wordma/) |
| 260727-fc2 | Regression fix for 260727-drq's own Bug 1 fix, reported live and root-caused via live instrumented DOM checks before dispatch: `.is-tracking` (which disables the peek transform's CSS transition during active mousemove tracking) is armed exactly once per hover session on `mouseenter`, and correctly removed by `commitEdge()` before its full-slide commit — but `finish()`'s cleanup never re-added it afterward. Since a user peeking/clicking through several galleries in a row typically never lifts the mouse off the photo (no fresh `mouseenter`), the FIRST edge-click commit in a session permanently disarmed the fix for every peek after it — reverting to the eased, continuously-retargeted transition and reintroducing the exact Safari jitter drq's Bug 1 fixed, with the wordmark-cutout sync dragged down along with it (tracking the now-jittery photo motion). Confirmed live via instrumented checks: after one commit (mouse never leaving the photo), `.is-tracking` was absent and `transform` was back in the transition list. Fixed with a one-line, hover-guarded re-arm in `finish()`'s existing cleanup block — reuses the already-present `.is-cursor-active` class (zero new state) so tracking never re-arms if the pointer actually left during the commit window. TDD (failing FR+EN e2e first). 239/239 full e2e, typecheck clean. Independently re-verified live across THREE consecutive edge-click commits in one continuous hover session (not just one) — `.is-tracking`/`transitionProperty` stayed correctly armed through all three, and the wordmark cutout was confirmed to re-sync correctly on the second peek too | 2026-07-27 | 87707fa, 45e7454 | Verified | [260727-fc2-fix-is-tracking-not-being-re-armed-after](./quick/260727-fc2-fix-is-tracking-not-being-re-armed-after/) |
| 260727-g04 | A fourth, distinct bug in the same carousel area, reported live and root-caused via frame-by-frame measurement before dispatch: during `commitEdge()`'s edge-click full-slide commit (~420-480ms), the hero photo visibly slides the entire viewport width, but the wordmark cutout stayed completely frozen at its clamp boundary (`--wordmark-bg-position: -804px`) for nearly the whole slide, only snapping to the correct new value once the swap landed. Root cause: quick-260727-drq's Bug 2 clamp (added to keep the wordmark's sample position within the rendered photo's real pixel bounds) was validated against the hover-peek's ~16%-max push range — but `commitEdge()`'s full-slide commit pushes `--peek-shift` to ±100%, a vastly larger offset whose raw (pre-clamp) position blows past the boundary almost immediately and stays there, so the clamp pins the output at the same value for nearly the entire slide — mathematically correct, but visually indistinguishable from "the picture inside the font does not follow." Fixed by making `commitEdge()`'s full-slide commit consistent with every other navigation path: it now reverts the wordmark to its solid-ink fallback (`has-wordmark-photo` removed, `--wordmark-photo: none`) at the START of the slide — mirroring what `render()` already does unconditionally for keyboard/dash/auto-advance/swipe navigation — and lets the existing `finish()` → `render()` → `revealWordmarkPhoto()` sequence re-reveal the cutout correctly realigned to the new photo once the swap lands; also removed the now-pointless `keepWordmarkSynced(600)` rAF pump from `commitEdge()` since nothing visible needs syncing while the cutout is hidden. TDD (failing FR+EN e2e first, using a deterministic 150ms mid-slide sample rather than an auto-retrying assertion, to avoid racing the sub-frame remove/re-add on the buggy path). 241/241 full e2e, typecheck clean. Independently re-verified live with the exact frame-by-frame measurement that found the bug: `has-wordmark-photo` now correctly reads `false` throughout the entire slide (matching the photo's transform moving from -389px to -1440px) and flips back to `true` exactly when the swap lands and the transform resets to 0 — no more freeze | 2026-07-27 | a378fe3, 08f08ae | Verified | [260727-g04-fix-wordmark-cutout-freezing-at-clamp-bo](./quick/260727-g04-fix-wordmark-cutout-freezing-at-clamp-bo/) |
| 260727-iao | Direct follow-up to g04's solid-ink fallback, after live video review + a structured design question ("full mirrored peek" vs "smooth crossfade" vs "something else" — user picked full mirrored peek explicitly): replaced the wordmark's clamp-and-hold/solid-ink mechanism with a TRUE mirrored peek — the wordmark cutout is now a three-layer stack (`.home-hero__wordmark-stack`: the accessible `<h1>` plus two `aria-hidden` `.home-hero__wordmark-peek--prev/--next` spans with identical text) that mirrors `.home-hero__photo`'s own current/peekPrev/peekNext structure, so the cutout visibly slides to reveal the incoming gallery's real texture in 1:1 sync with the photo's own motion, through both hover-peek and click-commit. Geometric model: at any x-coordinate, which photo is visible is fully determined by the current photo's (`heroImg`'s) live translated edge (its right edge for the right zone, left edge for the left zone) — a new pure `computeWordmarkSeamFraction(zone, heroLeft, heroRight, wordmarkLeft, wordmarkWidth)` (src/lib/home-carousel.ts, TDD, 8 new unit cases) converts that edge into a 0..1 seam fraction in wordmark-local coordinates; CSS `clip-path` rules keyed off a single `--wm-seam` custom property + `data-peek-zone` attribute do all the splitting (no clip-path strings built in JS). `syncWordmarkLayers()` extends the existing `syncWordmarkAlignment()` — feeds the active peek layer its own independent crop via the unchanged, still-clamped `computeWordmarkBackgroundPosition()`, and recomputes the seam every frame from `heroImg`'s live rect — driven through the SAME shared `keepWordmarkSynced()`/`pumpWordmarkSync()` rAF loop as before, through both `updatePeek()` (hover) and `commitEdge()` (commit slide, where g04's solid-ink removal is now fully retired since three independently-clamped layers mean there's always a correct photo to show at every proximity). Live verification found and documented a real, correct geometric consequence (not a bug): `.home-hero__accent`'s right-anchored position means a mere 16%-max hover push from the LEFT edge never reaches the panel, so left-hover legitimately shows no seam growth — only a full left-commit does; the e2e suite was written to prove this contract instead of a nonexistent one. Also auto-fixed a real mobile 320px overflow regression (peek layers needed the same mobile font-size clamp as the h1). 164/164 unit, 245/245 e2e (chromium + webkit-mobile, `--workers=1`), typecheck clean. Independently re-verified live: the computed `--wm-seam` value matched my own independently-computed geometric formula to full floating-point precision at every sampled proximity; a live screenshot during an active hover peek visually showed the rightmost letters of "JACQUELINE" rendering the incoming photo's texture while the rest still showed the current photo, split at a clean seam; frame-by-frame sampling through a full commit slide confirmed the seam moves continuously (never frozen) and `has-wordmark-photo` never drops (no solid-ink beat) | 2026-07-27 | 7813fc6, cfdec7c, a9e34a3, 6eac9c7, ea7293e | Verified | [260727-iao-replace-wordmark-clamp-solid-ink-fallbac](./quick/260727-iao-replace-wordmark-clamp-solid-ink-fallbac/) |
| 260727-kq8 | Follow-up to iao, reported live after that deploy ("the picture in the font still freeze") and independently root-caused via live frame-by-frame proximity measurement before dispatch (not re-diagnosed by the executor): iao's new seam-driven clip-path correctly gates both mirrored-peek layers to their in-bounds `[0, seam]` slice, but `computeWordmarkBackgroundPosition()` still carried drq's per-axis clamp — computed against the FULL theoretical (unclipped) wordmark box — so once the raw position exceeded the photo's rendered bounds the clamp pinned the returned position at a fixed boundary value, freezing the current layer's `--wordmark-bg-position` for the remainder of the approach even though the seam kept correctly shrinking the (now-static) visible slice around it. Confirmed live: at 1440x900, the current layer froze at exactly `-804px -575.5px` for every sample from proximity 0.85 through 0.995 while `--wm-seam` kept shrinking smoothly (0.960→0.722) and the peek layer's own position kept changing continuously over the same range. Fixed by adding a `clampToPhoto: boolean = true` opt-out parameter to `computeWordmarkBackgroundPosition()` (default unchanged, byte-identical for every existing caller/test) and passing `false` from both mirrored-peek call sites (current-layer in `syncWordmarkAlignment()`, peek-layer in `syncWordmarkLayers()`) — both are equally seam-clip-gated, so both are equally safe to leave unclamped; the clamp itself is kept as correct defensive behavior for any hypothetical unclipped caller. TDD (failing unit test for the unclamped path first). New FR+EN e2e walks the same right-edge proximity sequence used to diagnose the bug and asserts the current layer's bg-position is not frozen (≥3 distinct values across the 0.85–0.995 tail) — confirmed to genuinely fail on the pre-fix code via a controlled revert-and-rerun, then pass on the fix. 130/130 relevant unit (1 pre-existing unrelated `sanity/` subproject failure, unchanged), 93/93 e2e, `astro check` clean. Independently re-verified live with the exact diagnostic proximity sequence (0.80→0.995): all 8 samples now distinct (no freeze), seam continues shrinking in parallel exactly as before; a mid-peek screenshot confirmed both layers render their respective photo textures with a clean, fluid split (no frozen/solid patch) | 2026-07-27 | 0192ceb, c61a112, d736a77, 0da0e48 | Verified | [260727-kq8-fix-wordmark-current-layer-freezing-duri](./quick/260727-kq8-fix-wordmark-current-layer-freezing-duri/) |
| 260727-mvx | First of a batch of fixes from a user-requested full codebase audit (structure/dead-code, quality, test coverage, CI — see `quality-auditor` report; no blocking/critical findings, this task addresses the top-priority dependency/security items). Bumped root `astro` from the exact pin `7.0.6` to `^7.1.4`, clearing a moderate-severity reflected-XSS advisory (GHSA-4g3v-8h47-v7g6, View Transition animation properties) while staying on the 7.x line (this project deliberately avoids Astro 8.x). Ran non-force `npm audit fix` in root (4→0 vulnerabilities, clean) and in `sanity/` (dedupes `@oclif/core` to one version; residual `adm-zip`/`brace-expansion` advisories are chained through `@sanity/cli`'s own tree with no non-force fix available — the only resolution is a breaking `sanity@6.1.0` downgrade, correctly left out of scope). The `sanity` audit-fix's dedupe incidentally bumped transitive `@sanity/icons` 3.8.0→5.2.1 (a major version no file directly declares), whose v5 barrel dropped all per-icon named exports — broke the Studio build with 27 `MISSING_EXPORT` errors; the executor caught and fixed this by migrating all 6 affected files to `@sanity/icons`'s documented per-icon subpath imports (e.g. `@sanity/icons/Dashboard`), a mechanical, zero-behavior-change fix, confirmed against a fresh `npm ci` (the executor had itself been misled once by a stale `node_modules` left by the orchestrator's own investigation mid-stall, caught it, and re-verified from a clean tree rather than trusting the false-negative). 165/165 unit, 247/247 e2e (chromium + webkit-mobile), typecheck clean, `sanity` lint + build clean. Independently re-verified from a fully clean install (`rm -rf node_modules sanity/node_modules && npm ci` in both locations): root audit 0 vulnerabilities, sanity lint/build clean, typecheck/unit/build/full-e2e suite all green, matching the executor's own numbers exactly. Deployed successfully to GitHub Pages | 2026-07-27 | 35a4de1, e0af91f | Verified | [260727-mvx-dependency-security-remediation-bump-ast](./quick/260727-mvx-dependency-security-remediation-bump-ast/) |
| 260727-ntd | Second of the audit remediation batch (CI architecture item). Added `actions/cache@v4` for `~/.cache/ms-playwright` (keyed on OS + `package-lock.json` hash) before the Playwright browser-install step in `.github/workflows/deploy.yml`, correctly splitting the install into a cache-miss full `--with-deps` install and a cache-hit `install-deps`-only install (OS-level deps aren't stored in the browser cache, so they must still run even on a hit). Extended the existing `actions/setup-node@v4` `cache-dependency-path` to cover both `package-lock.json` and `sanity/package-lock.json`, so the shared npm-download cache also benefits the `sanity/` subproject's `npm ci --prefix sanity` install. No existing CI gate reordered, removed, or weakened. Separately, replaced CLAUDE.md's materially-incomplete one-line CI pipeline description with a full ordered-flow paragraph matching the real `deploy.yml` step-by-step — adding the four previously-undocumented facts the audit found (Sanity Studio lint+build as its own blocking gate, `astro check` typecheck as a gate before the first build, the static-artifact verifier running twice — root base and GitHub-Pages base with `EXPECTED_BASE` — and Playwright scoped to exactly `chromium`+`webkit`, no Firefox, matching the two configured Playwright projects). 165/165 unit, typecheck clean, build clean (247/247 e2e not re-run since this task touches only CI config + docs, no application code). Independently re-verified: valid YAML, every single clause of the new CLAUDE.md paragraph cross-checked line-by-line against the actual `deploy.yml` (all matched exactly), confirmed `playwright.config.ts` really does define only `chromium`+`webkit-mobile`, confirmed the diff touches exactly the two intended files (`deploy.yml` + `CLAUDE.md`, 20 insertions/1 deletion total). Deployed successfully to GitHub Pages, confirming the new caching's cache-miss path works end-to-end on real CI infra | 2026-07-27 | f836d9f, 841e0ff | Verified | [260727-ntd-ci-architecture-improvements-add-playwri](./quick/260727-ntd-ci-architecture-improvements-add-playwri/) |
| 260727-o3w | Third of the audit remediation batch (small mechanical code-quality items). Deleted 4 genuinely-dead `interface Props` blocks in the FR/EN édition and gallery detail route files (`src/pages/{editions,galleries}/[slug].astro` + their `en/` twins) — `Astro.props` was already correctly typed via `getStaticPaths(...) satisfies GetStaticPaths` inference, so the local interfaces were pure dead code, verified independently per file (all 4 matched the expected pattern, no annotate-instead-of-delete fallback needed). Caught and fixed a real second-order deviation: deleting the interfaces left the `import type { Edition }`/`{ Gallery }` lines genuinely unused in all 4 files (their only consumer was the deleted interface, contrary to the plan's initial assumption they were used elsewhere) — removed those too, since leaving them would have violated the plan's own "no new hints" success criterion. Added explicit `is:inline` to `BaseLayout.astro`'s JSON-LD structured-data script (mirroring its sibling inline script), silencing an `astro check` hint with zero effect on the actually-rendered output (confirmed the full serialized JSON-LD still renders correctly in built HTML — `is:inline` is a compile-time-only directive). Closed a zero-visibility debugging gap in `ContactForm.astro`: its submit handler's bare `catch { renderSubmissionError() }` now binds and `console.error`s the actual caught error first — currently invisible in practice since the form is deliberately non-functional pending Phase 5's OVH/Web3Forms cutover, but will matter the moment it goes live. `astro check` dropped from 7 hints to 2 (only the pre-existing, unrelated `webkitBackgroundClip` deprecation warnings in test files remain). 165/165 unit, build clean, 247/247 e2e (chromium + webkit-mobile). Independently re-verified from a clean state: typecheck confirms exactly the 5 targeted hints are gone with zero new ones, diff review of all 6 touched files, confirmed JSON-LD content genuinely present in built `dist/` output, re-ran the full unit + build + e2e suite myself matching the executor's numbers exactly. Deployed successfully to GitHub Pages | 2026-07-27 | 809c58f, a16d3b1 | Verified | [260727-o3w-small-mechanical-code-quality-cleanups-u](./quick/260727-o3w-small-mechanical-code-quality-cleanups-u/) |
| 260727-ohf | Fourth of the audit remediation batch (test coverage). Unit-tested every previously-untested export of `sanity/editorial/dashboardLogic.ts` (Studio-only, not customer-facing): `isGalleryOnline`, `mutationDocumentId`, `mutationFields`, `contentNoun`, `attentionPriority`, and — with real depth — `describeTransaction` (every mutation-shape branch: published, unpublished, created, modified with 1/2/>2 recognized fields, and the generic fallback) and `buildActivities` (empty-list edge case, author-name fallback chain displayName→email→generic-label, most-recent-wins dedup across two transactions for the same document, and a transaction whose document is missing from the documents list being skipped). `dashboardLogic.ts` coverage rose from 43.67%/43.1% (stmt/branch) to 94.25%/91.37% — independently confirmed via a fresh `test:coverage` run reading the file's own row in the report, not just trusting the executor's claim. Genuinely interesting correction chain during dispatch: the ORIGINAL audit-derived task brief correctly said `mutationFields` strips array-index bracket suffixes and filters `_`-prefixed fields; the orchestrator (pre-dispatch, trying to verify the plan) misread the source with a truncated `grep -A 20` that cut off before the function's final `.map().filter()` chain, and "corrected" the plan to the WRONG claim that the function returns raw paths verbatim; the executor, instructed to always treat the function body as ground truth, re-read the full source, caught the orchestrator's own error, and wrote tests against the actual (original, correct) behavior — documented explicitly in both the commit message and SUMMARY. Also expanded `tests/e2e/accessibility.spec.ts` from 5 to 10 routes: added `/editions/`, `/confidentialite/`, `/en/`, `/en/about/` to the static scan loop, plus a new dynamic test that discovers a real published édition's detail URL from the overview's first `.tile` href (mirroring `edition.spec.ts`'s established pattern, never hardcoding a slug) and axe-scans it — closing the audit's most significant gap (zero `/en/*` or `/editions/*` accessibility coverage). Same AxeBuilder config/assertions throughout, no new helper or approach. 206/206 unit, typecheck clean, build clean, 252/252 e2e (chromium + webkit-mobile). Independently re-verified from a fully clean install (`rm -rf node_modules sanity/node_modules`, fresh `npm ci` both locations): re-read the coverage table myself and confirmed the exact 94.25%/91.37% figures, diff-reviewed both test files line-by-line, re-ran the full typecheck/coverage/build/e2e suite matching the executor's numbers exactly. Deployed successfully to GitHub Pages | 2026-07-27 | 442dff8, 9a77458 | Verified | [260727-ohf-test-coverage-additions-unit-tests-for-d](./quick/260727-ohf-test-coverage-additions-unit-tests-for-d/) |
| 260727-p0m | Fifth of the audit remediation batch (test-file structure). Split the 2,302-line `tests/e2e/homepage.spec.ts` (40 top-level `describe` blocks, 97 `test`/`test.skip` call sites, 93 actually-registered tests) into 6 feature-scoped files by exact, pre-verified grouping: `homepage-carousel-core.spec.ts` (11 blocks — basic carousel mechanics/mode toggle/view-transitions), `homepage-wordmark-peek.spec.ts` (12 blocks — the wordmark/photo-cutout/hover-peek/gesture cluster that absorbed this whole session's bsm/drq/fc2/g04/iao/kq8 bug-fix chain), `homepage-content-display.spec.ts` (8), `homepage-chrome-nav.spec.ts` (3), `homepage-mobile-responsive.spec.ts` (4), `homepage-loading-progress.spec.ts` (2). Byte-preserving move confirmed via precise line-range diff against git history (zero content alteration). Original deleted; 4 stale sibling comment cross-references in `site-header.spec.ts`, `i18n.spec.ts`, and `gallery.spec.ts` (independently confirmed to be the only such references repo-wide) repointed to the correct new filenames. Orchestrator caught a real regression during independent re-verification: applying the same `import { test, expect, devices }` line to all 6 files left 4 unused-`devices` warnings (only wordmark-peek and mobile-responsive actually use `devices`) — sent back to the executor, fixed in a third atomic commit, typecheck hints restored from 6 back to the pre-split baseline of 2. 206/206 unit, typecheck clean (2 hints, unrelated pre-existing), build clean, 252/252 e2e — split-file run (93 tests) matched the pre-split baseline exactly. Independently re-verified from a fully clean install: precise line-range diff of the wordmark cluster's most complex block against git history confirmed byte-identical, cross-reference greps confirmed zero stale `homepage.spec.ts` references anywhere in the live repo, re-ran the full suite myself matching all numbers exactly. Cleanup-wave's automated merge tool correctly blocked on the intentional file deletion (a deliberate safety guard, not a bug) — merged manually from the main repo root using the identical `git merge --no-ff --no-edit` command the tool itself would have run, after the independent verification above had already satisfied what that guard exists to force | 2026-07-27 | 6a0ef47, ded5658, 7896c19 | Verified | [260727-p0m-split-homepage-spec-ts-into-6-feature-sc](./quick/260727-p0m-split-homepage-spec-ts-into-6-feature-sc/) |
| 260727-the | Sixth and final item of the audit remediation batch — the largest and highest-impact (~1,400 lines of near-byte-identical fr/en page code flagged by the audit). Extracted 5 shared body components under `src/components/` (`AboutPageBody`, `ContactPageBody`, `EditionDetailBody`, `GalleryDetailBody`, `EditionsOverviewBody`), each holding the full markup + the single copy of its pair's previously-duplicated `<style>` block. All 10 route files (about/contact/editions-detail/galleries-detail/editions-overview, both locales) thinned to their own Sanity fetch + `.fr`/`.en` field resolution + fallback strings, then a single `<BaseLayout><…Body {...resolvedProps} /></BaseLayout>` call — locale-specific visible UI strings passed as explicit props from each route (no new i18n dictionary introduced), `getStaticPaths` correctly kept in the 4 `[slug]` route files. Deliberately excluded (per direct analysis, not the audit's blanket suggestion): `mentions-legales`/`confidentialite` (genuinely hardcoded, legally-reviewed prose per their own header comments — extraction would reduce legal-text reviewability without meaningfully reducing duplication) and `index.astro` (already thin, already delegates to the existing shared `HomeCarousel` component). Used `--validate` mode given the size/risk — the plan-checker caught one real, load-bearing type error before dispatch (the plan had typed `lightboxImages` as `SanityImage[]`, but `Lightbox.astro` requires `GalleryImage[]`, which has a required `alt` field `SanityImage` lacks — would have failed `npm run typecheck`), fixed via one targeted revision round, re-checked clean. Also independently verified myself before dispatch: every subtle plan claim (ampersand-escaping via single-`&` JS-expression props reproducing byte-identical `&amp;` output; all three guard-equivalence proofs — édition grid `gridItems.length > 0` ⟺ `(edition.images?.length ?? 0) > 0`, gallery grid `gridItems.length > 0` ⟺ `gallery.images.length > 1` since `gridItems` excludes the hero by real index, éditions-overview `tiles.length === 0` ⟺ `editions.length === 0`) checked directly against source and confirmed exact. Executor caught and fixed one real deviation during execution (documented): `ContactPage.professionalLinks[].url` needed `?? ''` coalescing to satisfy TypeScript (the Sanity type is `string | undefined` and `.filter()` doesn't narrow it through `.map()`) — a type-level-only fix, zero behavior change since the route's own filter already guarantees truthiness at runtime. 206/206 unit, typecheck clean, build clean (27 pages, both locales), 252/252 e2e — full suite matched the pre-refactor baseline exactly, zero test files touched. Independently re-verified from a fully clean install: diff-reviewed all 5 components and all 10 route files in full against the plan's exact specification (every prop, every literal-vs-prop decision, every preserved rationale comment), confirmed the 6 excluded files byte-unchanged via `git status --short`, re-ran the complete clean-state suite matching the executor's numbers exactly, and did a live visual check via Playwright on the deployed-equivalent preview build — About (FR+EN, ampersand headings render as clean single `&`, not double-escaped), Contact (FR+EN, curly-apostrophe byte-verified against git history), the Éditions overview poster grid, an édition detail page including opening the Lightbox at the correct combined-array index (caught and corrected my own test-methodology mistake mid-verification — an imprecise selector had clicked the hero trigger instead of the intended grid tile, not a real bug), and a gallery detail page with `headerVariant="transparent"` intact | 2026-07-27 | 64fea0e, a8e8303, a69b580, 04ae2c2, 8ed5572 | Verified | [260727-the-extract-shared-fr-en-page-body-component](./quick/260727-the-extract-shared-fr-en-page-body-component/) |
| 260728-dbf | Redesigned the Éditions overview page (`EditionsOverviewBody.astro`) from the zigzag "Poster Grid" tile layout to sketch 010's "B2 — Cursor Preview" pattern, the Romane-confirmed winner of a multi-round design exploration (sketch 009 settled the tile hover treatment as a duotone-reveal spotlight; Romane disliked the pink accent, sketch 011 explored neutral/negative alternatives; sketch 010 then rechallenged the layout itself with several variants — B2's flat text-row index with a cursor-following photo panel won). Added `previewPanelUrl(img, width=680)` (3:4 portrait Sanity crop) to `src/lib/image.ts`, unit-tested, wired into both `/editions/` and `/en/editions/` route files replacing the old square-cropped `thumbnailUrl` — Props interface (`EditionTile`/`Props`) and both route files' `tiles.map()` shape otherwise untouched. Rewrote the component's non-empty branch to `.editions-index` (flat list of `.editions-index__row`: gray index label + `clamp(32px,5vw,64px)` title + hover-revealed statement) plus a fixed `.editions-preview` panel (340px, 3:4, cursor-follow via a client `<script>` reading `mousemove` + each row's `data-img`); non-hovered titles dim to 0.28 opacity; `:focus-visible` mirrors the hover reveal for keyboard parity (not in the sketch, added to avoid dropping a11y coverage); mobile (≤800px) hides the panel and forces statements always-visible. Header chrome (hairline + eyebrow + h1) and the zero-éditions `EmptyState` fallback left byte-unchanged. Rewrote `tests/e2e/edition.spec.ts`'s zigzag hero/small geometry test into a real hover-interaction test (panel starts inactive, activates + shows the correct row's `data-img` on hover, updates when hovering a different row); swapped every `.tile` nav-discovery selector (this file plus `gallery.spec.ts` ×2 and `accessibility.spec.ts` ×1) to `.editions-index__row`; EDN-06 commerce-token scan and `seo.spec.ts`/`site-header.spec.ts` left untouched. Executor auto-fixed one TS build error (untyped `mousemove` callback needed an explicit `MouseEvent` cast). 207/207 unit, typecheck clean, build clean (27 pages), EDN-06 `test:artifact` guard clean, 101/101 targeted e2e (edition/accessibility/gallery/seo/site-header), 252/252 full e2e. Independently re-verified: full diff review of all 3 task commits against the plan's exact CSS/markup/JS values, a from-scratch re-run of the entire verification gate in the worktree (not trusting the executor's numbers), and a live Playwright check of both locales against real production content (Rebut, Silos) — desktop hover correctly swaps the floating panel to each row's photo and dims other titles, mobile hides the panel and always shows statements. Worktree merge was initially blocked by the cleanup tool's fork-point safety check (`base_mismatch`, then `worktree_dirty` after fixing the first): root cause was local `main` sitting 3 unpushed commits ahead of `origin/main` (the sketch-009/010/011 decision docs accumulated across this whole design-exploration session) — pushed `main` first, then preserved the executor's uncommitted `SUMMARY.md` by copying it out of the worktree before cleanup removed it, then the merge succeeded cleanly | 2026-07-28 | 2403837, 6d9aaca, 527bfbe, a30e118 | Verified | [260728-dbf-redesign-ditions-overview-page-editionso](./quick/260728-dbf-redesign-ditions-overview-page-editionso/) |
| 260728-el6 | Fixed 2 real regressions from 260728-dbf's B2 redesign, reported live by Florian with screenshots: (1) the hover-revealed édition statement was clipped mid-sentence by a fixed `max-height: 80px` — real Romane-authored statements (e.g. Silos) are longer than the sketch's placeholder text. Replaced with a `.editions-index__statement-wrap` grid container animating `grid-template-rows: 0fr -> 1fr` on hover/focus-visible (and forced `1fr` on mobile), which grows to the statement's true intrinsic height regardless of length; `.editions-index__statement` itself keeps only its `opacity` fade plus `min-height: 0` (required for a grid item to collapse below intrinsic height). (2) The H1 "looked too similar to the rest of the page" — removed the redundant `.editions-index__row:first-of-type` top-border hairline sitting directly under it (the shared page-top `.editions-list__header` hairline was untouched), and added a genuinely Sanity-editable body-typography intro paragraph rendered under the H1 on both locales. Built a new `editionsPage` Sanity singleton (intro-only, no seo group) mirroring `homePage.ts` exactly — schema + `schemas/index.ts` registration + `structure.ts` desk-pin — plus the matching `getEditionsPage()`/`EditionsPage`/`EDITIONS_PAGE_QUERY` trio in `src/lib/sanity.ts` and `DEFAULT_EDITIONS_INTRO`/`resolveEditionsIntro()` fallback pair in `src/lib/site-config.ts`, mirroring `getHomePage()`/`resolveHomepageIntro()`; fr/en placeholder copy kept byte-identical between the Sanity schema's `initialValue` and the code-side default so Studio and the fallback never diverge until Romane edits the live document. Both route files wired to fetch/resolve/pass the new `intro` prop. Task 2 executed as genuine TDD (RED commit with 3 failing tests, then GREEN). Added a new e2e anti-truncation `expect.poll` assertion (second row's statement bounding-box height > 80px on hover) that would have caught the original bug; existing hover/EDN-06 assertions otherwise unchanged. Explicitly out of scope (deferred to the orchestrator): `sanity deploy` and seeding/publishing the live `editionsPage` document — the code-level default fallback means the site renders correct placeholder copy in the interim. 210/210 unit, typecheck clean, build clean (27 pages), EDN-06 `test:artifact` guard clean, 12/12 targeted e2e (including the new anti-truncation poll), 252/252 full e2e, `sanity` lint+build clean. Independently re-verified: full diff review of all 4 task commits against the plan's exact schema/CSS/markup values, a from-scratch re-run of typecheck/build/artifact/targeted-e2e in the worktree, and a live Playwright/DOM check of both locales confirming the Silos statement's hover box height (120px) exactly matches its `scrollHeight` (no clipping), the intro paragraph renders with the correct fr/en copy, and the redundant hairline rule is absent from the computed stylesheet. Worktree merge required pushing local `main` first (the pre-dispatch plan commit had never been pushed) and rescuing the executor's deliberately-uncommitted `SUMMARY.md` before cleanup, same pattern as 260728-dbf | 2026-07-28 | 0701430, fdcc6c1, f12a9b0, 51f8f46, 6123890 | Verified | [260728-el6-fix-ditions-overview-statement-text-trun](./quick/260728-el6-fix-ditions-overview-statement-text-trun/) |
| 260728-fjl | CI-only hotfix: `main`'s Playwright e2e gate went red (GitHub Actions run 30344677877, 3/3 attempts) immediately after 260728-el6 merged, on `tests/e2e/edition.spec.ts`'s `editions overview layout` hover test — `expect(preview).not.toHaveClass(/active/)` failed on the `/en/editions/` loop iteration because the panel already carried `active`. Root cause: the test loops over both locale URLs on one shared `page`; Playwright's virtual mouse position persists across `page.goto()` by design, so the cursor was left wherever the French page's `secondRow.hover()` placed it, and 260728-el6's new `.editions-list__intro` paragraph (rendered in the bare `--font-sans` system stack, `-apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif` — no self-hosted web font) wraps to a different line count on CI's Linux/Chromium than on macOS, shifting row Y-positions between locales just enough for the leftover cursor to land on a row on the English page and fire an implicit hover before the assertion ran. Confirmed locally that FR/EN row geometry is identical on macOS at 1280×900 (why it passed in prior local verification) and that the failure was deterministic across all 3 CI attempts (each fresh attempt reruns the same loop from scratch and hits the same layout-dependent outcome — not flakiness). Fixed with a single added `page.mouse.move(0, 0)` reset (plus a why-comment) immediately after each `page.goto(url)` in the loop, before the `not.toHaveClass(/active/)` assertion — test-only change, zero production code touched (deliberately did not "fix" the font stack, out of scope for a hotfix). Independently re-verified: diff review confirming the change is exactly one file/6 lines, a from-scratch build + typecheck in the worktree, 12/12 targeted `edition.spec.ts` pass (including the previously-failing hover test on both locales), and a full 252/252 Playwright suite pass (chromium + webkit-mobile) confirming no other test relied on the same cross-navigation mouse-bleed assumption | 2026-07-28 | 2d9b09a | Verified | [260728-fjl-fix-editions-e2e-hover-bleed-across-nav](./quick/260728-fjl-fix-editions-e2e-hover-bleed-across-nav/) |
| 260728-g76 | Follow-up correction to 260728-el6's hairline removal, per Florian's live feedback with a screenshot of the deployed page: "tu as supprimé la ligne en dessous Édition alors que je parlais de celle du dessus. Tu peux remonter un peu le titre 'Editions' plus haut" — I'd removed the WRONG hairline (`.editions-index__row:first-of-type`, below the header, correctly staying removed) instead of the one he actually meant, which was still visible directly above the "ATELIER JACQUELINE SUZANNE" eyebrow at the very top of the page. Removed `border-top: var(--border-hairline) solid var(--color-ink)` and its paired `padding-top: var(--space-md)` from `EditionsOverviewBody.astro`'s `.editions-list__header` rule — the padding existed only to clear that border, so removing both together raises the whole eyebrow/title/intro block, satisfying "remonter le titre plus haut" as a direct consequence rather than a separate margin hack. Confirmed the identical `padding-top`+`border-top` header pattern exists independently in `AboutPageBody.astro`/`ContactPageBody.astro` (repeated convention, not shared code) and left both untouched — Florian's correction was Éditions-specific. CSS-only, one file, two lines removed, `margin-bottom` and all sibling margins unchanged. Typecheck clean, build clean (27 pages), artifact guard clean, 12/12 edition e2e + 41/41 site-header e2e. Independently re-verified: diff review confirming exactly 2 lines removed, live DOM/computed-style check on both locales (`border-top-style: none`, `padding-top: 0px`) and a visual screenshot confirming the hairline is gone and the title sits higher, directly against the fresh worktree build | 2026-07-28 | 36ec9c2 | Verified | [260728-g76-fix-editions-header-hairline-and-title-p](./quick/260728-g76-fix-editions-header-hairline-and-title-p/) |
| 260728-hxv | Third and final hairline correction on the Éditions overview page this session, per Florian's live feedback: "but WHERE IS THE LINE UNDER the title + description part???" — 260728-el6 had wrongly removed `.editions-index__row:first-of-type`'s `border-top` (the separator between the intro paragraph and "Édition 01 / Rebut"), and 260728-g76's plan incorrectly assumed that removal should stay. Restored the exact rule 260728-el6 removed — `.editions-index__row:first-of-type { border-top: var(--border-hairline) solid var(--color-ink); }` — in `EditionsOverviewBody.astro`, immediately after the unchanged `.editions-index__row` base rule, while leaving `.editions-list__header`'s rule exactly as 260728-g76 correctly left it (`margin-bottom: var(--space-2xl)` only, no border-top/padding-top). CSS-only, one file, one rule added. Typecheck clean, build clean (27 pages), artifact guard clean, 12/12 edition e2e + 41/41 site-header e2e. Independently re-verified: diff review confirming exactly one rule added, live computed-style check on both locales confirming BOTH hairlines simultaneously in their correct final state (header border-top: none/padding-top: 0px; first row border-top: solid var(--color-ink)), and a visual screenshot showing the separator restored between the intro and "Édition 01" while the header stays clean | 2026-07-28 | a0e7e0b | Verified | [260728-hxv-restore-editions-list-separator-hairline](./quick/260728-hxv-restore-editions-list-separator-hairline/) |
| 260728-lbh | Implemented the approved sketch-012 winner (F1 — "Drifting Grey Halftone," Round 12 final state — Florian: "It's amazing! approved") into production: `EditionsOverviewBody.astro`'s header rebuilt as a two-column broken grid (title left, intro right beside a hairline, ≥760px) with the "Éditions" title enlarged to `clamp(64px,9vw,140px)`, a grey dot-halftone texture drifting continuously behind the title (mathematically-derived edge-less fade: 700px box overflow on all four sides safely exceeding the 640px mask radius, the exact invariant that took 12 sketch rounds to nail down), a pulsing pink eyebrow marker, and a staggered CSS-only entrance choreography (eyebrow → title → intro → halftone → each row, escalating per-row delay capped at 1.2s for future catalog growth) — zero JavaScript added. Sole file touched, both locales covered via the shared component. Executor found and fixed two real bugs during its own verification before I ever saw the diff: (1) the halftone was rendering completely invisible in production because `.editions-list__header` had no stacking context, letting `z-index:-1` escape behind `<main>`'s opaque background — fixed with one additive `isolation: isolate`; (2) a reproducible WCAG 1.4.3 contrast violation where axe-core caught the header text (`#1A1A1A` ink) mid-opacity-fade at as low as 1.35:1 — fixed by dropping the opacity component from the eyebrow/h1/intro entrance animations, keeping transform-only slide/settle. I independently computed the same risk existed for the row list's grey `#6b6b6b` text (5.33:1 at rest, 3.19:1/2.05:1/1.40:1 at 75%/50%/25% opacity — all failing AA) and sent the executor back to apply the identical fix there before merging, since the first run's silence on it was a timing accident (rows' 0.6s+ stagger delay meant axe's scan most likely landed while they were still at opacity:0, which axe skips, not genuinely safe). Verification: typecheck clean, build clean, artifact guard clean, accessibility.spec.ts 100/100 across repeat-runs (was 4/4 failing pre-fix), edition.spec.ts 120/120 across repeat-runs, full suite 252/252 clean on my own independent run. Independently re-verified beyond the executor's own check: full diff review of both commits: computed the halftone's actual box-to-anchor margins via `getBoundingClientRect()` at both 800px and 1440px (700px margin on every side, provably exceeding the 640px radius — not just eyeballed); used `document.getAnimations()` to confirm the entire entrance choreography's delays/durations/ordering are wired correctly end-to-end (eyebrow 0ms → h1 100ms → intro 500ms → halftone-fade-in 300ms → row0 600ms → row1 720ms, matching the per-row inline `animation-delay` styles exactly); confirmed hover genuinely swaps `animation-duration` from `10s` to `4s` and applies the contrast/brightness filter; used a throwaway Playwright script with `page.emulateMedia({reducedMotion:'reduce'})` to confirm every element resolves to static opacity:1/transform:none/no-active-animations; confirmed both locales and the sub-760px single-column no-halftone fallback. A pre-existing, unrelated flaky test in `homepage-wordmark-peek.spec.ts` (different component, not touched) surfaced during the executor's repeat-runs and was correctly logged to a deferred-items note rather than fixed — my own single full-suite run was clean regardless | 2026-07-28 | 10d57cd, 4059eb6, b483b5b | Verified | [260728-lbh-implement-editions-header-personality](./quick/260728-lbh-implement-editions-header-personality/) |
| 260728-nj7 | Fixed two live-reported bugs on the just-deployed sketch-012 header (260728-lbh), Florian: "le 'E' est dans Atelier Jacqueline Suzanne. Et tu peux un peu tout remonter ?" — (1) the "É" accent in the h1 overlapped 6.5px into the eyebrow line above it (measured directly on the deployed page via `Range.getBoundingClientRect()` on the glyph vs. the eyebrow's own rect); root cause was `line-height: 0.9` on the h1, a ratio below 1.0 that shrinks the line box below the font's natural metrics so an accented capital's diacritic paints outside it — tested candidate values live (0.95 still overlapping at -3px, 1.0 exactly touching at 0px with no safety margin, 1.05 giving a clean +3.5px buffer) before writing the plan, so the fix (`line-height: 0.9` → `1.05`) was live-verified, not guessed; being ratio-based it holds across the h1's fluid `clamp(64px,9vw,140px)` sizing at any viewport. (2) the header-to-row-list gap read as too big now that the title is much bigger — measured the actual 80px total (48px header `margin-bottom` + 32px the row's own top padding, left untouched since it's shared inter-row rhythm) and reduced `.editions-list__header`'s `margin-bottom` from `var(--space-2xl)` to `var(--space-lg)`, dropping the total to 56px. Exactly two property values changed, one file, both locales covered via the shared component. Typecheck/build/artifact clean, 63/63 targeted e2e (edition+site-header+accessibility), 252/252 full suite. Independently re-verified with the same live-measurement technique used to diagnose it: positive glyph/eyebrow clearance confirmed at both 800px (+9px) and 1440px (+3.5px) via a fresh preview-server build (not trusting the executor's numbers); `margin-bottom` confirmed exactly 24px via computed style; visual screenshot at 1440px matching the live pre-dispatch verification. The executor caught a real edge case I hadn't anticipated: the EN locale's heading is literally "Editions" (no accent), so there's no "É" character to target with a Range on `/en/editions/` — substituted the h1's own top edge for that locale's clearance check, correctly documented as a deviation | 2026-07-28 | 447eeae | Verified | [260728-nj7-fix-editions-header-accent-overlap-and-s](./quick/260728-nj7-fix-editions-header-accent-overlap-and-s/) |
| 260728-ok5 | Fixed a third, distinct live-reported gap on the sketch-012 header, Florian: "mais tu n'as pas résuit la marge entre le header et le titre" — the site-nav-to-title gap ABOVE the eyebrow/title block (not the header-to-row gap below it, already fixed in 260728-nj7). Measured on the deployed page: the site header ends at y=148.9px, `.editions-list` starts there with `padding-top: 96px` (computed), placing the eyebrow at y=244.9px — a 96px gap sourced from the SHARED token `--editorial-page-padding-block: clamp(48px, 7vw, 96px)`, also consumed by About/Contact (confirmed via `getComputedStyle` on `/about/`: identical values, so the token itself wasn't broken — Éditions' much bigger h1, `clamp(64px,9vw,140px)`, just makes the same whitespace read as excessive with nothing else occupying it, unlike About's portrait-filled hero). Fix: split `.editions-list`'s `padding` shorthand into three longhands, capping ONLY `padding-top` with an Éditions-scoped `clamp(48px, 7vw, 56px)` — same minimum/slope as the shared token, lower 56px ceiling — while `padding-bottom`/`padding-inline` keep consuming the untouched shared token. Live-tested before dispatch: at 1440px 96px→56px, at 800px a no-op (56px→56px, already at that value), at mobile a no-op (48px minimum unchanged). Exactly one declaration changed, one file, both locales via the shared component. Typecheck/build/artifact clean, 63/63 targeted e2e (edition+site-header+accessibility), 252/252 full suite. Independently re-verified via a fresh preview-server build (not trusting the executor's numbers): `padding-top` = 56px at 1440px and unchanged 56px at 800px on both `/editions/` and `/en/editions/`; `/about/` confirmed numerically unaffected (56px/96px, matching the untouched shared token exactly). This closes out the third and final Éditions-header gap Florian flagged this session | 2026-07-28 | edf8c19 | Verified | [260728-ok5-fix-editions-header-nav-gap](./quick/260728-ok5-fix-editions-header-nav-gap/) |
| 260728-ek0 | Implemented sketch-013's confirmed winner, variant A3 ("Grand titre, sans scroll"), in the real `ContactPageBody.astro` — the two-column layout's hard offset-shadow form panel (the exact motif Romane had already rejected on Éditions tiles, sketch 009) replaced with a giant self-scaling `--font-display` title (`min(15vw,20vh)`), a desktop composition centered as one flex group with a bounded `clamp()` header-to-grid gap, and a hairline-bordered panel with tinted/boxed `ContactForm` fields. Real `<ContactForm>` island (script/markup/validation/honeypot) left byte-unchanged — only its `<style>` block adapted. `tests/e2e/contact.spec.ts`'s "contact reachability" test and `tests/e2e/about.spec.ts`'s cross-page parity test updated for the intentionally-changed contracts. Independent orchestrator verification (this worktree had no `.env`, blocking the executor's own build) found the composition overflowed the real desktop viewport by 239px at 1440×900 once the actual `SiteHeader`+footer chrome were in play — invisible from the sketch, which had no equivalent chrome to test against. Root-caused to `Textarea`'s `rows=5` default (forcing ~138px regardless of the intended `min-height`), an always-margined empty status paragraph, and a wrong `--contact-chrome-reserve` guess (220px vs. real 258px) — fixed, cutting overflow to 49px without touching the title scale or header-gap, both already signed off on. Built both a footer-visible and footer-hidden variant and showed the user a live side-by-side comparison (the only lever big enough to close the rest of the gap without eroding the signed-off design); they picked footer-hidden, matching the existing gallery/edition-detail-page pattern — Mentions légales/Confidentialité stay reachable from every other page's footer, just not this one. Applying it caught a real gap mid-review: the fix had only been applied to the French route; the user was actually comparing via the English page (EN switcher) and still saw the old scrollable footer — fixed for both locales in the same commit, plus the chrome-reserve fallback corrected to 152px (header-only) and two more small vh-based padding trims. Final measured overflow (Playwright against the real preview server, both locales): 0px at 900–1080px height, 6px at 768px (imperceptible). Follow-up at the user's request: restyled the title/eyebrow to match the just-shipped Éditions title's new treatment (pulsing accent dot, ink-colored eyebrow, `-0.04em` h1 tracking) while deliberately keeping Contact's own `min(vw,vh)`/0.82-line-height size formula, not Éditions' pure-vw one, since that's what preserves the no-scroll guarantee. On merging back to `main`, found and resolved a genuine sketch-numbering collision: this branch's Contact sketch had independently claimed "012" while a concurrent session claimed the same number for the Éditions header work (now merged) — renumbered to 013, the free slot. Full gate green: typecheck 0 errors, 207/207 unit (once `sanity/node_modules` installed), 252/252 e2e (chromium + webkit-mobile), mobile tap targets confirmed still 44px (`--tap-target-min`) after all trims. `tests/e2e/visual.spec.ts-snapshots/contact-form.png` regenerated twice as the form's real dimensions shifted. Process note: an overly broad `pkill -f "astro preview"` while freeing port 4321 briefly risked killing a concurrent agent's dev server (a different worktree, `agent-a21b976ec4c4fcde6`, was found holding that port) — no confirmed damage, but subsequent servers were pinned to a dedicated port (4919) and killed only by exact PID from then on. Worked entirely in an isolated worktree (`sketch-012-contact-finalize`) per explicit user instruction, since another agent was concurrently active on `main` | 2026-07-28 | 993c67f, 273b275, a76dbe3, 00ed32a, e4d1c0e, 2b68ecb | Verified | [260728-ek0-impl-menter-le-variant-gagnant-du-sketch](./quick/260728-ek0-impl-menter-le-variant-gagnant-du-sketch/) |
| 260801-d6m | Dashboard visual tweaks: removed the 3 metric tiles, restyled the "Mettre le site à jour" panel to a sober accent-edged surface, de-emphasized "Nouvelle collection" to ghost mode and added a "Nouvelle édition" sibling. Live UAT of the just-merged studio publication workflow PR also surfaced and fixed 3 real bugs on the same branch as separate debug sessions: the dashboard spinner hanging forever on a `historyStore.getTransactions` stall, a dead disabled document action, and 3 unreachable SEO checklist items on `editionsPage` — plus one further live-UAT regression this task's own restyle exposed: React Strict Mode's phantom effect cleanup permanently invalidating the inventory generation guard before its first fetch could resolve (fixed via a symmetric `reactivate()`). All fixes verified live against a running `sanity dev` session and via the full automated gate suite | 2026-08-01 | 5613694, 1bd4c7c, fb9737c, ff921ff | | [260801-d6m-dashboard-visual-tweaks](./quick/260801-d6m-dashboard-visual-tweaks/) |
| 260801-fxe | Replaced the dashboard's modal `Dialog` publish confirmation with an inline two-state confirmation in the "Mettre le site à jour" panel itself, at the user's explicit request ("pas besoin d'une popup, ça peut être direct dans le pannel"). Also rewrote the confirmation message in plain French for the non-technical end user (fixed a singular/plural grammar bug, dropped "lot"/"échoue" batch jargon and the redundant per-category breakdown already shown by the panel's own item list). Zero business-logic change (`requestPublication`/`confirmPublication`/`publicationController`/`publicationCardState` untouched); `tests/unit/dashboard-logic.test.ts` stayed green and unmodified (259/259). Live-verified the normal (no residual modal) state on `localhost:3333`; the confirming sub-state itself could not be screenshotted live since nothing was pending publication at check time — remains to visually confirm next time a real publish is queued | 2026-08-01 | ec388de | | [260801-fxe-inline-publish-confirmation-instead-of-d](./quick/260801-fxe-inline-publish-confirmation-instead-of-d/) |
| 260801-id4 | Restored a real SEO tab (group + shared `seo` field) on the `editionsPage` Studio schema, mirroring homePage/aboutPage/contactPage, plus the corresponding 3 recommended checklist items — at the user's explicit request after live-testing showed the SEO tab missing. Reverses this same session's earlier `editionspage-no-title-seo` debug fix, made when the field genuinely didn't exist; the field remains Studio-only (not wired into `EDITIONS_PAGE_QUERY`/the public `/editions` Astro routes — a separate, unscoped decision). SEO items stay strictly recommended (never required), so an intro-only draft still satisfies the publish gate. TDD RED→GREEN for the checklist test; live-verified the SEO tab renders correctly on `localhost:3333` | 2026-08-01 | 2b29d77, d4e3c31, 5955e0f | | [260801-id4-add-seo-group-to-editionspage-schema](./quick/260801-id4-add-seo-group-to-editionspage-schema/) |
| 260801-kgh | Unified `edition`'s cover-photo management with `gallery`'s existing pattern at the user's explicit request — removed the dedicated `leadPhoto` field entirely; editions now pick their cover from the shared `images` array via the same landscape-preferring `pickHeroIndex` logic galleries already use. Orchestrator first live-migrated the 3 real published editions (Silos, Rebut, Entasse) on the production dataset — each existing `leadPhoto` asset inserted as `images[0]` with matching alt text (written after viewing each cover image) and copied rights, `leadPhoto` unset — preserving their exact live cover photo before any code changed. Code task then removed `leadPhoto` from the Studio schema, TS type, both GROQ queries (adding a shared dimensions projection so `pickHeroIndex` has real geometry), the editorial checklist, and all 4 édition Astro routes (FR/EN detail+list); `EditionDetailBody` now forwards a `heroIndex` prop it previously dropped. Bundled a separate, unrelated user request: renamed the gallery Studio tab "Accueil" to "Couleur". Original executor hit an API session-limit mid-final-verification after all 5 commits landed clean; orchestrator independently completed and confirmed every remaining gate: unit (271/271), lint, typecheck, Sanity Studio lint+build, root Astro build against the real dataset (29/29 pages incl. all 3 real édition pages), and the full edition+gallery e2e suites (45/45, chromium) | 2026-08-01 | 676d321, 88bae08, 56608ed, a0be71c, 8a51e75 | | [260801-kgh-unify-edition-cover-photo-with-gallery-p](./quick/260801-kgh-unify-edition-cover-photo-with-gallery-p/) |
| 260801-pn7 | Two follow-up Studio UX requests: (1) reordered `gallery.ts`'s tab groups so Photos is first (and default-selected on open), live-verified on a real Collection; (2) attempted a shared "Couverture" badge overlay on the first `images[]` item on both `gallery.ts` and `edition.ts` via a `components.item` override, correctly implemented against the installed `sanity@6.6.0` API — but live DOM inspection against a real edition confirmed the badge never rendered, root-caused to Sanity's `options: {layout: 'grid'}` array input not invoking `components.item` at all (a genuine Studio limitation, not a wiring bug). Presented the tradeoff (switch to list layout to make the badge work, vs. keep the grid and drop it) to the user rather than deciding unilaterally, since it changes Romane's day-to-day photo-editing ergonomics; user chose "Garder la grille, annuler le badge" — fully reverted (component deleted, both schema wirings removed), re-verified live that both grids render unchanged. Tab reorder unaffected and shipped | 2026-08-01 | 3fba363, bdc444c, c242ddc | | [260801-pn7-primary-photo-badge-and-reorder-gallery-](./quick/260801-pn7-primary-photo-badge-and-reorder-gallery-/) |
| 260803-bvu | Fixed 8 client-reported UI/UX defects across three page areas, from direct live-site review with screenshots (homepage hero carousel, éditions detail pages, Contact page footer), on an isolated branch/worktree (`fix/homepage-editions-contact-ux`) per explicit user instruction, since a concurrent Phase 18 UAT session was active on `main` in the primary checkout at the time — never touched `18-UAT.md`, never touched `main`. (1) Auto-advance's photo swap was never a genuine crossfade — reproduction via `getComputedStyle` sampling found that hovering an edge zone produced an instant fade-less pop (`.is-tracking`'s blanket `transition:none` also silenced the sharp layer's own opacity transition) and, independently of hover, the crossfade never played at all because `render()` removed then synchronously re-added `is-loaded` within the same script tick; fixed with a new `is-auto-snap` class (forces the transform half instant, keeps the opacity transition alive, cascade-ordered after `.is-tracking`) plus a genuine 60ms-deferred re-add via a new `forceCrossfade` `render()` option — confirmed empirically that neither a forced reflow nor a double-rAF wait was sufficient. (2) The accent cursor pill was painting over the pause/progress caption controls (which sit inside the left edge zone); hidden via a new `is-over-controls` class toggled from the `mousemove` handler. (3) `.home-grid__tile-title`'s single-line ellipsis clamp cut long titles ("The Victorian Tea room") mid-word; replaced with a 2-line `-webkit-line-clamp` + `min-height` reservation mirroring the sibling `.home-grid__tile-description` pattern, preserving the 260718-rhv same-top-offset invariant. (4) Édition-to-édition navigation shook because scrolling down an outgoing édition page shrinks its hero photo to its 55%-width settled state (measured 960px) at the exact moment the shared `hero-photo` View Transition snapshot is captured, while a freshly-loaded incoming page always starts full-bleed (measured 1280px) — fixed by giving éditions `view-transition-name: none` via a new opt-in `editionVariant` scoping prop; galleries keep their shared name, confirmed unchanged. (5) Removed the duplicated `.edition-detail__format` metadata paragraph from `EditionDetailBody`; `DetailHero`'s own reveal-panel copy is now the sole instance, `aria-hidden` removed and restored to visible on mobile. (6) Activated `DetailHero`'s existing scroll-up-to-return gesture for éditions (`carouselReturnHref={overviewHref}`, prop intentionally left unrenamed) and removed the now-redundant in-flow "Retour aux éditions" back-link, its dead CSS, and the unused `backLinkLabel` prop. (7) Reused the same `editionVariant` prop to switch the édition hero to `object-fit: contain` so the photo shows whole/uncropped, letterboxed on the pin's own ink background (confirmed via screenshots this reads as intentional, not broken, at both the desktop 55%-width scroll-reveal and the 70svh mobile pin); gallery detail heroes keep their existing crop completely untouched, per the plan's explicit scope guard referencing a prior user reversal on this exact point. (8) Traced Contact's entire form-panel-to-footer gap to a single declaration (`.contact-page`'s own `padding-bottom: var(--editorial-page-padding-block)`, measured 89.6px at 1280×900 / 48px at 390×844) rather than the grid/column-imbalance candidates the plan flagged as worth checking — neither of those contributed measurably; fixed with a flat `var(--space-xl)` override scoped to `.contact-page` (never the shared token) plus a separate, footer-specific (not header-shared) vertical-padding reduction at desktop. One deviation: rewrote one pre-existing `gallery.spec.ts` sub-test whose "gesture stays inert on édition heroes" assertion was made obsolete by Item 6's intended behavior change (éditions now correctly carry the return attribute, pointing at a different destination than galleries) — rewritten to assert the new correct contract, not weakened or deleted. Executor was interrupted once mid-Task-1 by a session usage limit and resumed cleanly from its own uncommitted worktree state with no work lost. 283/283 e2e (chromium + webkit-mobile), 276/276 unit, typecheck/lint/build/test:artifact all clean. Independently re-verified by the orchestrator from a second, separately node_modules/`.env`-provisioned worktree before merging: typecheck, lint, unit (276/276), build (29 pages), and the full e2e suite (283/283) all matched the executor's reported numbers exactly | 2026-08-03 | 2b8578b, 9cd9a9f, 0d2416b | Verified | [260803-bvu-fix-homepage-carousel-editions-detail-pa](./quick/260803-bvu-fix-homepage-carousel-editions-detail-pa/) |
| 260803-ira | Follow-up correction to 260803-bvu's Item 7, reported live by the user with a screenshot while reviewing that task's dev server: "pictures in editions are still cropped. Zoom on the primary one was ok, restaure it." 260803-bvu's Item 7 fix only applied to `DetailHero.astro`'s single hero photo; it missed the secondary photos rendered below the hero in a bento grid. Root cause, confirmed by direct code inspection: `EditionDetailBody.astro` calls `<GalleryGrid items={gridItems} />` with no `layout` prop, defaulting to `layout="bento"`, while `GalleryDetailBody.astro` explicitly passes `layout="masonry"` — `grep -rn "<GalleryGrid" src/` confirms these are the only two call sites, so bento is used exclusively by éditions and masonry exclusively by galleries. `GalleryGrid.astro`'s shared `.tile img` base rule (bento's fit) was `object-fit: cover` (cropped); `.gallery-grid--masonry .tile img` already overrode it with `object-fit: contain` ("the never-crop guarantee") for galleries — so galleries' grid photos were already correct and éditions' were the ones still cropped, the inverse of what most people would assume. Fixed by changing the shared base rule's `object-fit` directly from `cover` to `contain` (no new prop needed, since bento is already éditions-exclusive) — a computed-value no-op for galleries since their masonry override already redeclared the same fit, and a real fix for éditions. Preserved the existing `.tile:hover img`/`.tile:focus-visible img` `scale(1.03)` hover-zoom effect on both the bento group's larger `tile--hero` tile and the smaller `tile--small` tiles (this hover-zoom, not the page's separate `DetailHero` component, is what "Zoom on the primary one was ok" referred to) — proven still working post-fix via a polling transform assertion. Rewrote (never deleted/weakened) the one pre-existing `gallery.spec.ts` sub-test that asserted the old cropped-bento contract, plus its describe-block header comment; the masonry sub-test and every other assertion in that file are byte-unchanged. Bento layout/composition (asymmetric 1-large + 2-small grouping, gaps, cell sizes) is untouched — crop-only fix. Reviewed the one open risk the plan flagged (mobile bento cells are pinned to a 3:4 portrait aspect ratio below 800px, so landscape photos could letterbox heavily) via real screenshots at 1280×900 and 390×844 for all 3 published éditions — reads as a deliberate "framed on the wall" presentation at every viewport, consistent with the hero's already-approved treatment; flagged in the SUMMARY as not yet seen live by the site owner. 286/286 e2e (chromium + webkit-mobile), 276/276 unit, typecheck/lint/build/test:artifact clean, `git diff --stat` confirmed exactly the 3 expected files changed. Independently re-verified by the orchestrator: typecheck, the 60 targeted `edition.spec.ts`/`gallery.spec.ts` tests, and the full unit suite (276/276) all matched the executor's reported results | 2026-08-03 | f6cbfcc | Verified | [260803-ira-fix-dition-detail-pages-bento-grid-photo](./quick/260803-ira-fix-dition-detail-pages-bento-grid-photo/) |
| 260803-jby | Second follow-up correction, reported live right after 260803-ira: "hm, that's weird you put like a grey background to not crop the images. But you just had to display the picture entirely the same way as it's done in Galleries pages." 260803-ira's fix (object-fit contain inside éditions' fixed-size bento grid cells) stopped cropping but exposed the tile's `--color-ink` background wherever a photo's ratio didn't match its fixed cell, since bento cell size is independent of each photo's real aspect ratio. Galleries avoid this entirely by using `layout="masonry"` (CSS multi-column flow where each tile's height IS `aspect-ratio: var(--ar, 1)`, computed per-photo, so there is never leftover space to expose a background). Fixed by mirroring `src/pages/galleries/[slug].astro`'s exact grid-derivation pattern into both édition `[slug].astro` route files (fr/en): real per-photo `aspectRatio` from Sanity image `dimensions` (already fetched via the existing `IMAGES_WITH_DIMENSIONS_PROJECTION`, no data-layer change needed), and the uncropped `fullSizeUrl`/`responsiveImageSrcSet` helpers in place of the square-crop `thumbnailUrl`/`responsiveThumbnailSrcSet` ones; localized `alt` and locale-specific `ariaLabel` deliberately kept (NOT copying galleries' empty-alt pattern, since édition thumbnails are non-decorative). `EditionDetailBody.astro`'s `GalleryGrid` call now passes `layout="masonry"` explicitly, matching `GalleryDetailBody.astro`. Accepted consequence, per the user's own words, not treated as a misread: éditions lose the previously-approved asymmetric "1 large + 2 small" bento composition (sketch-004 variant A2) and become a flowing multi-column grid, mechanically identical to galleries. `GalleryGrid.astro`'s bento code path (grouping logic, `.gallery-grid__group`/`tile--hero`/`tile--small` CSS) is now unreferenced by any real caller but was deliberately left in place (comments-only edit there) rather than deleted inside a live-feedback fix — flagged as available for a future separate cleanup. Rewrote (never deleted/weakened) 260803-ira's now-superseded bento-specific `edition.spec.ts` block to assert the new masonry contract (per-tile img-flush-with-tile check within 0.5px, natural-ratio check, fr+en, hover-zoom preserved, vacuous-loop guard) and `gallery.spec.ts`'s PORT-05 édition sub-test similarly, plus corrected two stale header comments describing the old bento-vs-masonry split; every gallery-side assertion in that file stayed byte-unchanged. 287/287 e2e (chromium + webkit-mobile; one transient parallel-load flake in an unrelated, unmodified assertion confirmed pre-existing via isolated re-run), 276/276 unit, typecheck/lint/build/test:artifact clean, `git diff --stat` confirmed exactly the 6 expected files changed. Independently re-verified by the orchestrator: typecheck, the 61 targeted `edition.spec.ts`/`gallery.spec.ts` tests, and the full unit suite (276/276) all matched the executor's reported results; diff-reviewed `GalleryGrid.astro`'s change directly and confirmed it is comments-only as required | 2026-08-03 | f22c123 | Verified | [260803-jby-make-edition-detail-grid-use-masonry-lay](./quick/260803-jby-make-edition-detail-grid-use-masonry-lay/) |
| 260803-jwl | Third follow-up correction, reported live right after 260803-jby: the user confirmed the grid fix ("parfait !") but added "mais tu n'as pas remis la primary en zoomée comme avant" ("but you haven't put the primary back zoomed like before"). Re-reading the whole feedback arc, this clarified that "the primary" has always meant the page's actual hero photo (`DetailHero.astro`'s image, not anything in the grid) and "zoomée" means the CROPPED/filled presentation (`object-fit: cover`), not a CSS hover-scale effect — so the original "Zoom on the primary one was ok, restaure it" message (which 260803-ira had interpreted as being about the grid tiles' hover-zoom) actually meant: put the hero's crop back to how it was before 260803-bvu's Item 7 ever touched it. Fixed by deleting the `.detail-hero--edition .detail-hero__img { object-fit: contain; }` override in `DetailHero.astro`, so édition heroes fall back to the base `object-fit: cover` rule — identical to gallery heroes, which were never changed. Preserved, verbatim and unedited, the sibling `.detail-hero--edition .detail-hero__img { view-transition-name: none; }` rule (Item 4's fix for the édition-to-édition hero shake bug, a wholly independent concern sharing the same selector) — confirmed still working via a mandatory live empirical check (9 desktop édition-to-édition navigations after scrolling to the settled reveal panel, plus mobile, no shake). Corrected every stale comment left claiming the édition hero is uncropped: `DetailHero.astro`'s file header and its `editionVariant` Props doc (now documents the flag as scoping ONLY the view-transition suppression), one clause inside the Item 4 diagnosis comment, `edition.spec.ts`'s hero-crop test (rewritten, not deleted, to assert édition and gallery heroes now report the identical crop value) and its stale ira-attribution sentence above the masonry helpers, and `src/lib/image-orientation.ts`'s `pickHeroIndex` doc (which incorrectly claimed the hero renders uncropped). `git diff --name-only` confirmed exactly the 3 intended files changed, with `tests/e2e/gallery.spec.ts` and every grid/masonry file from 260803-ira/260803-jby completely absent from the diff. 276/276 unit, typecheck/lint/build/test:artifact clean; the executor's own full `test:e2e` run reported 287/287, though it separately had to work around a stray `astro dev` process squatting on Playwright's default port 4321 (from a concurrent, unrelated session on the main checkout) via a temporary local `playwright.config.ts` port override, reverted before committing. Independently re-verified by the orchestrator: typecheck, unit (276/276), and the diff scope all matched; the orchestrator's own first verification pass hit that SAME stray-port collision (9 apparent failures, actually against main's stale un-fixed code, not this branch) — resolved with the identical temporary-port-override technique, after which all 61 targeted `edition.spec.ts`/`gallery.spec.ts` tests passed, then confirmed the config override was fully reverted (`git status` clean on `playwright.config.ts`) before merging | 2026-08-03 | ff195ad | Verified | [260803-jwl-revert-edition-hero-photo-back-to-croppe](./quick/260803-jwl-revert-edition-hero-photo-back-to-croppe/) |
| 260810-qmm | Refined the phone-width mobile homepage (`MobileHomePrototype.astro`, which superseded `HomeCarousel`'s Phase 21 scroll-deck at ≤767px by undocumented prior work — see [Phase 21] above): extended the pinned arrival into four scroll-led beats (wordmark → readable description → description fade → photo alone before the series list), replaced vertical arrival/series translations with horizontal X-axis pans, fixed the "Défiler"/"Scroll" cue's contrast to ink-on-accent, and rewrote the E2E suite (`homepage-scroll-deck.spec.ts` and 5 other files) to test the shipped prototype and hamburger instead of the retired scroll-deck's structural assertions. Run via a Codex `/gsd:quick` session, not this Claude session. Its own executor made no commit ("shared worktree contains user-owned uncommitted changes" — self-check PASSED regardless); everything landed via the user's own later manual commit `6c51695`. Recorded here retroactively during the 2026-08-10 Phase 21 mechanical reconciliation, since its own commit step never ran | 2026-08-10 | 6c51695 (manual, not from this task's own executor) | Complete (self-check PASSED) | [260810-qmm-ajuster-l-accueil-mobile-apr-s-l-apparit](./quick/260810-qmm-ajuster-l-accueil-mobile-apr-s-l-apparit/) |
| 260811-9f5 | Fixed a flaky-E2E audit finding: `edition.spec.ts`'s "galleries unaffected" test and `gallery.spec.ts`'s "every tile has 0px borders" test both occasionally read a Sanity `<img>`'s `naturalWidth`/`naturalHeight` before it finished decoding, producing a random `NaN` ratio (both passed reliably on a targeted retry — a test-sync race, not a UI bug). Fixed by adding `await expect.poll(() => img.complete && img.naturalWidth > 0).toBe(true)` before each ratio read, mirroring the identical pattern already established in `edition.spec.ts`'s PORT-05 block for a multi-tile case. Test-only change, no `src/` file touched. Verified: both targeted tests green across 5 repeats each (5/5, 10/10), full suite green afterward — typecheck (0 errors), lint (clean), unit (349/349), e2e (334/334) | 2026-08-11 | c35905b | Complete | [260811-9f5-corrige-l-instabilite-e2e-des-assertions](./quick/260811-9f5-corrige-l-instabilite-e2e-des-assertions/) |

## Deferred Items

Items acknowledged and carried forward from previous milestone close:

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| v1.x scope | Exhibitions, shop, checkout, shipping, commerce-specific legal (EXHB-*, SHOP-*, CHK-*, SHIP-*, LEGAL-02, LEGAL-04, CMS-02/03, I18N-02b/03), plus the Éditions cross-link differentiator (EDN-08) | Tracked in REQUIREMENTS.md v2 section, not yet roadmapped | Roadmap creation 2026-07-05 (v2 baseline); EDN-08 added 2026-07-22 |

Items acknowledged and deferred at v1.0 milestone close on 2026-07-27 (retroactive `/gsd-complete-milestone`, closing v1.0/v1.1/v1.2 together — v1.1 and v1.2 had no open items requiring acknowledgment):

| Category | Item | Status |
|----------|------|--------|
| phase | Phase 5: Launch & Domain Cutover (LAUNCH-01) | Not started — deliberately deferred, not abandoned. Direct user decision: "cancel the initial plan to deliver and publish the website officially... we'll do it later." Remains open in ROADMAP.md for whenever launch is next prioritized. |

Items acknowledged and deferred at v1.3 milestone close on 2026-07-23 (`/gsd-complete-milestone` pre-close audit, 11 open items, user chose "Acknowledge all, proceed"):

| Category | Item | Status |
|----------|------|--------|
| debug | knowledge-base | unknown (in substance resolved — this is the persistent resolved-sessions index file itself, not a live investigation; all 10 entries in it are mirrored as resolved in `.planning/debug/resolved/`) |
| uat_gap | Phase 03: 03-HUMAN-UAT.md | partial (0 pending scenarios; Phase 03 shipped and marked complete in ROADMAP.md long before this audit tooling existed) |
| uat_gap | Phase 14: 14-ROMANE-UAT.md | unknown (0 pending scenarios; Romane's pass was completed and explicitly approved — see 14-04-SUMMARY.md — this file is a checklist doc with no completion frontmatter field for the auditor to read) |
| verification_gap | Phase 03: 03-VERIFICATION.md | human_needed (stale flag; Phase 03 shipped long ago) |
| verification_gap | Phase 04.1: 04.1-VERIFICATION.md | human_needed (stale flag; Phase 04.1 shipped long ago) |
| quick_task | 260722-compact-sanity-document-layout | missing (PLAN.md exists, no SUMMARY.md — planned, never executed; out of v1.3 Éditions scope) |
| quick_task | 260722-improve-contact-page | missing (same as above) |
| quick_task | 260722-normalize-sanity-form-spacing | missing (same as above) |
| quick_task | 260722-rebalance-contact-typography | missing (same as above) |
| quick_task | 260722-refine-contact-details | missing (same as above) |
| quick_task | 260722-remove-redundant-sanity-fieldsets | missing (same as above) |

Items acknowledged and deferred at v1.4 milestone close on 2026-08-01 (`/gsd-complete-milestone` pre-close audit, 7 open items re-surfaced from the v1.3 close — none resolved in the interim, none blocking; user chose "Acknowledge all, proceed"):

| Category | Item | Status |
|----------|------|--------|
| debug | knowledge-base | unknown (same persistent false positive as the v1.3 close note above — the resolved-sessions index file itself, not a live investigation; all 14 entries in it are mirrored as resolved in `.planning/debug/resolved/`) |
| quick_task | 260722-compact-sanity-document-layout | missing (still unexecuted; the Sanity Studio document layout it targeted has since been substantially reworked by the Phase 260801 studio-publication-workflow effort — superseded, not worth resurrecting) |
| quick_task | 260722-improve-contact-page | missing (still unexecuted; the Contact page it targeted was fully rebuilt in the 260728-ek0 sketch-013 redesign — superseded) |
| quick_task | 260722-normalize-sanity-form-spacing | missing (still unexecuted; overlaps the same Studio rework as compact-sanity-document-layout above — superseded) |
| quick_task | 260722-rebalance-contact-typography | missing (still unexecuted; Contact page typography was redone in the sketch-013 redesign — superseded) |
| quick_task | 260722-refine-contact-details | missing (still unexecuted; Contact page contact-details block was rebuilt in the sketch-013 redesign — superseded) |
| quick_task | 260722-remove-redundant-sanity-fieldsets | missing (still unexecuted; overlaps the same Studio rework — superseded) |

Items acknowledged and deferred at v1.5 milestone close on 2026-08-03 (`/gsd-complete-milestone` pre-close audit, the same 7 open items re-surfaced unchanged from the v1.4 close — none resolved in the interim, none blocking; user chose "Acquitter tout, proceed"):

| Category | Item | Status |
|----------|------|--------|
| debug | knowledge-base | unknown (same persistent false positive noted at both the v1.3 and v1.4 close — the resolved-sessions index file itself, not a live investigation) |
| quick_task | 260722-compact-sanity-document-layout | missing (still unexecuted, still superseded by the Studio rework — see v1.4 note above) |
| quick_task | 260722-improve-contact-page | missing (still unexecuted, still superseded by the sketch-013 Contact redesign — see v1.4 note above) |
| quick_task | 260722-normalize-sanity-form-spacing | missing (still unexecuted, still superseded by the Studio rework — see v1.4 note above) |
| quick_task | 260722-rebalance-contact-typography | missing (still unexecuted, still superseded by the sketch-013 Contact redesign — see v1.4 note above) |
| quick_task | 260722-refine-contact-details | missing (still unexecuted, still superseded by the sketch-013 Contact redesign — see v1.4 note above) |
| quick_task | 260722-remove-redundant-sanity-fieldsets | missing (still unexecuted, still superseded by the Studio rework — see v1.4 note above) |

Items acknowledged and deferred at v1.6 milestone close on 2026-08-11 (`/gsd-complete-milestone` pre-close audit, 17 open items; user chose "Acquitter comme abandonnées, procéder"):

| Category | Item | Status |
|----------|------|--------|
| debug | knowledge-base | unknown (same persistent false positive noted at every prior close since v1.3 — the resolved-sessions index file itself, not a live investigation) |
| debug | homepage-scroll-missing-intro-beat | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-10 (round 1) |
| debug | homepage-scroll-zoom-handoff-glitch | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-07 (round 1) |
| debug | homepage-scroll-deck-blur-placeholder-jank | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-09 (round 1) |
| debug | homepage-scroll-ios-status-bar-white-gap | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-09 (round 1) |
| debug | homepage-scroll-intro-logo-duplication | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-13 (round 2, gap 1) |
| debug | homepage-scroll-text-reveal-too-fast | diagnosed, hypothesis CONFIRMED — closed by gap-closure plans 21-13 (intro half) and 21-15 (gallery half) (round 2, gap 2) |
| debug | homepage-scroll-cover-photo-doubled | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-12 (round 2, gap 3) |
| debug | homepage-scroll-still-not-fullscreen | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-11 (round 2, gap 4) |
| debug | homepage-scroll-nav-unreachable-at-start | diagnosed, hypothesis CONFIRMED — closed by gap-closure plan 21-14 (round 2, gap 5) |
| uat_gap | Phase 21: 21-UAT.md | superseded (0 pending scenarios — round-3 real-device check never ran; the mechanism it would have tested was itself superseded by commit `6c51695` before that round could happen — see the [Phase 21] Accumulated Context entry above) |
| quick_task | 260722-compact-sanity-document-layout | missing (still unexecuted, still superseded — see v1.4/v1.5 notes above) |
| quick_task | 260722-improve-contact-page | missing (still unexecuted, still superseded — see v1.4/v1.5 notes above) |
| quick_task | 260722-normalize-sanity-form-spacing | missing (still unexecuted, still superseded — see v1.4/v1.5 notes above) |
| quick_task | 260722-rebalance-contact-typography | missing (still unexecuted, still superseded — see v1.4/v1.5 notes above) |
| quick_task | 260722-refine-contact-details | missing (still unexecuted, still superseded — see v1.4/v1.5 notes above) |
| quick_task | 260722-remove-redundant-sanity-fieldsets | missing (still unexecuted, still superseded — see v1.4/v1.5 notes above) |

Note on the 9 `homepage-scroll-*` debug sessions above: all confirmed root-caused and closed via specific plans within this same session. Never moved to `.planning/debug/resolved/` because that auto-move (execute-phase.md's `close_parent_artifacts` step) only fires for decimal phases (X.Y) — Phase 21 is an integer phase, so the move never triggered. A tooling gap, not neglect; recorded here rather than silently acknowledged.

## Session Continuity

Last session: 2026-08-11T14:15:22.081Z
Stopped at: Phase 5 context gathered
Resume file: .planning/phases/05-launch-domain-cutover/05-CONTEXT.md

**Next up:** Phase 21 (Homepage Scroll Experience — HOME-14, HOME-15) is ready to plan via `/gsd-plan-phase 21` — the milestone's biggest, riskiest, sketch-explored centerpiece, depending on Phase 20's now-shipped accent-color mechanism. Phase 5 (Launch & Domain Cutover) remains open and deliberately deferred whenever launch is next prioritized. One non-blocking carryover: `20-REVIEW.md`'s unfixed Critical finding on `HomeCarousel.astro`'s touch handler (see Blockers/Concerns).

## Operator Next Steps

- Start the next milestone with /gsd-new-milestone
