---
phase: 13-nav-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SiteHeader.astro
  - tests/e2e/site-header.spec.ts
autonomous: false
gap_closure: true
requirements: [EDN-01]

must_haves:
  truths:
    - "The header keeps the language switcher on the SAME row as the 4-link nav (Éditions/À propos/Contact/Instagram) — NO two-row wrap — across the entire 320px-767px viewport range, INCLUDING the previously-broken 360px-375px band, in BOTH the solid (/about/, /contact/) and transparent (/ homepage, /galleries/[slug]/) header variants. This directly repairs the FAILED Truth #5 of 13-VERIFICATION.md (D-02, SC #5, EDN-01)."
    - "At no width from 320px to 767px does the header produce horizontal page overflow (document.documentElement.scrollWidth <= window.innerWidth) — the header fits via the existing compensating padding/gap/font-size/logo trims, never via wrapping or horizontal scrolling (D-02)."
    - "The regression is caught by a NEW automated e2e assertion that samples INSIDE the 360-375px band (specifically 375px, the plan's own named 'iPhone SE ~375px' target) and fails on a two-row wrap — closing the 320px/393px test blind spot that let the bug ship undetected (13-VERIFICATION.md missing item #2)."
    - "A live human-check at 375px (and 360px/320px) on BOTH header variants is actually re-run and confirms single-row fit — replacing the 13-01-SUMMARY.md claim that 13-VERIFICATION.md found was contradicted by direct reproduction (missing item #3)."
  artifacts:
    - "src/components/SiteHeader.astro — the @media(max-width:767px) block's .site-header rule uses flex-wrap: nowrap (changed from wrap); the compensating-trim @media breakpoint is raised from max-width:359px to max-width:400px (same declarations, wider ceiling)"
    - "tests/e2e/site-header.spec.ts — a new describe block asserting the first .site-nav .nav-link and the .language-switcher share a row (vertical centers within 5px) AND scrollWidth <= innerWidth, sampled across widths {320,360,374,375,390,767} for both a solid page (/about/) and a transparent page (/)"
  key_links:
    - "@media(max-width:767px) flex-wrap: nowrap -> the language switcher physically cannot drop to a second row anywhere below 768px (root-cause fix for the vertical wrap)"
    - "@media(max-width:400px) compensating trims -> the nowrap header fits without horizontal overflow down to 320px across the 360-400px band that previously wrapped (the trims proven sufficient at 320px in 13-01 now cover the whole band)"
    - "new same-row e2e assertion (nav vs switcher center) sampled at 375px -> catches the vertical wrap the scrollWidth<=innerWidth check structurally cannot (VERIFICATION missing item #2)"
---

<objective>
Close the single verification gap from 13-VERIFICATION.md: the 4th nav link ("Éditions") shipped in plan 13-01 causes the shared `<SiteHeader>` to wrap to a SECOND row (the language switcher drops below the nav) at 360px-375px viewport widths, in BOTH the solid and transparent header variants. This is the failed Truth #5 (D-02 / SC #5). Fix the responsive CSS so the header stays a single row across the full 320px-767px range, add an e2e assertion that samples inside the broken band and fails on wrap, and actually re-run the live human-check that was previously claimed-but-not-verified.

This plan builds on the already-executed, already-committed 13-01 (do NOT re-execute 13-01). It is scoped to exactly two files — `src/components/SiteHeader.astro` (CSS breakpoints only) and `tests/e2e/site-header.spec.ts` (a new regression assertion). Every other Phase 13 artifact (siteSettings.ts, site-config.ts, sanity.ts, BaseLayout.astro, HomeCarousel.astro) passed verification cleanly (Truths #1-#4) and MUST NOT be touched.

Purpose: EDN-01's nav entry must be robust on the common ~360-375px phones (iPhone SE 375px, iPhone 12 mini / many Android 360px) — "discoverable on every page" implicitly includes those devices; a two-row header there is a real, reproducible visual regression, not a nitpick.

Output: a corrected mobile-fit CSS contract (nowrap across the sub-768px range + compensating trims raised to cover 360-400px), a regression e2e that lands inside the previously-untested band, and a genuine live confirmation in both variants.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-nav-integration/13-VERIFICATION.md
@.planning/phases/13-nav-integration/13-UI-SPEC.md
@.planning/phases/13-nav-integration/13-01-SUMMARY.md
@src/components/SiteHeader.astro
@tests/e2e/site-header.spec.ts
</context>

## Artifacts this phase produces

New/changed symbols introduced by this gap-closure plan (downstream verification & re-verification reference):

- **CSS change (root cause)**: in `SiteHeader.astro`'s `<style is:global>` block, the `@media (max-width: 767px)` `.site-header` rule changes `flex-wrap: wrap` → `flex-wrap: nowrap`. The header is now single-row at every width (the base ≥768px rule already defaults to nowrap).
- **CSS change (compensation)**: the last-resort trim media query condition is raised from `@media (max-width: 359px)` to `@media (max-width: 400px)`. All declarations inside it are unchanged; only the ceiling widens so the 12px-font / 2px-padding / 40px-logo / `gap: 0` trims now cover the 360-400px band.
- **New e2e describe block** in `tests/e2e/site-header.spec.ts`: "Shared SiteHeader — single-row fit across the mobile range (EDN-01, D-02, SC #5, gap-closure)" — asserts same-row (nav-link vs language-switcher vertical centers within 5px) AND no horizontal overflow, across widths {320, 360, 374, 375, 390, 767} × pages {/about/ (solid), / (transparent)}.
- **Explicitly NOT produced**: no D-03 Éditions-label abbreviation span/markup (the trim-breakpoint raise fits all four full labels — abbreviation stays untriggered, consistent with 13-01-SUMMARY.md); no new CSS class; no schema/resolver/type/call-site edits; no markup, prop, color, or logo-hover changes.

<tasks>

<task type="auto">
  <name>Task 1: RED — add a same-row regression assertion that samples inside the 360-375px wrap band</name>
  <files>tests/e2e/site-header.spec.ts</files>
  <read_first>
    - tests/e2e/site-header.spec.ts (the existing mobile-fit describe blocks: "mobile fit at 393px" lines 46-59 and "mobile fit at 320px with 4 nav links" lines 68-81 — both only assert scrollWidth <= innerWidth, which structurally cannot detect a vertical two-row wrap; and the header/nav/switcher selectors used elsewhere: `[data-role="site-header"]`, `.site-nav > a.nav-link`, `.language-switcher` / `.language-switcher a` at lines 98, 133, 206)
    - src/components/SiteHeader.astro (lines 256-343 — the `.site-nav`, `.nav-link`, and `.language-switcher` layout the test targets, and the two @media blocks whose gap causes the wrap)
    - .planning/phases/13-nav-integration/13-VERIFICATION.md (the reproduction detail: post-Phase-13 at 375px the switcher's boundingClientRect top drops from 44 to 112 while the nav stays at 44 — a ~68px vertical drop — on both /about/ and /; broken band ~360-375px)
  </read_first>
  <action>
    Add a new describe block to tests/e2e/site-header.spec.ts named "Shared SiteHeader — single-row fit across the mobile range (EDN-01, D-02, SC #5, gap-closure)". Do NOT modify or delete the existing 393px (lines 46-59) or 320px (lines 68-81) describe blocks — they still pass and stay as-is; this block ADDS the missing coverage inside the broken band.

    Parametrize the block over two pages — '/about/' (solid variant) and '/' (transparent/homepage variant) — and over the viewport widths 320, 360, 374, 375, 390, and 767 (height 800). For each (path, width): call page.setViewportSize, page.goto(path), then run a single page.evaluate that, scoped to `[data-role="site-header"]`, reads getBoundingClientRect for the FIRST `.site-nav a.nav-link` (the Éditions link) and for the `.language-switcher` container, computes each element's vertical center as rect.top + rect.height / 2, and returns both centers plus document.documentElement.scrollWidth and window.innerWidth.

    Assert two things per sample: (a) Math.abs(navCenterY - switcherCenterY) <= 5 — proving the switcher shares the nav's row; when the header wraps, the switcher's center drops by roughly the full nav-row height (~68px per the VERIFICATION measurement), which far exceeds the 5px tolerance, so this fails on wrap; (b) scrollWidth <= innerWidth — no horizontal page overflow at that width. Use a 5px tolerance (not larger) so a genuine wrap cannot slip through; do NOT loosen it later to force a pass.

    These assertions are expected RED now: against the current unmodified CSS the switcher wraps at 360/374/375px on both pages (the (a) same-row check fails there), while 320/390/767 pass. Confirm the failures land specifically inside the 360-375px band and are same-row-tolerance failures, not locator/syntax errors.
  </action>
  <verify>
    <automated>npm run build && npm run test:e2e -- site-header 2>&1 | tail -40  # NEW same-row assertions FAIL at 360/374/375px on both /about/ and / (switcher center ~68px below nav center) — expected RED; 320/390/767 pass</automated>
  </verify>
  <acceptance_criteria>
    - tests/e2e/site-header.spec.ts contains a new describe block that, for both '/about/' and '/', asserts Math.abs(navCenterY - switcherCenterY) <= 5 (first `.site-nav a.nav-link` vs `.language-switcher`) AND scrollWidth <= innerWidth, sampled at widths including 375px, 360px, and 374px.
    - Running the verify command shows the NEW same-row assertions failing at 360/374/375px on BOTH pages, and passing at 320/390/767 — failing for the wrap reason (center delta ~68px), not a compile/locator error.
    - The pre-existing "mobile fit at 393px" and "mobile fit at 320px with 4 nav links" describe blocks are still present and unmodified.
    - The 5px same-row tolerance is used verbatim (not widened).
  </acceptance_criteria>
  <done>A committed, currently-RED e2e assertion proves the 360-375px two-row wrap exists in both header variants, sampling inside the band the previous tests never touched.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — fix the breakpoint gap so the 4-link header stays single-row across 320-767px</name>
  <files>src/components/SiteHeader.astro</files>
  <read_first>
    - src/components/SiteHeader.astro (lines 256-343: the `.site-nav` base rule at 256-261, `.nav-link` at 263-272 with its `min-height: var(--tap-target-min)`, the `@media (max-width: 767px)` block at 285-300 containing `.site-header { flex-wrap: wrap; gap: var(--space-xs); }`, and the `@media (max-width: 359px)` last-resort block at 306-343 containing `flex-wrap: nowrap` plus the 12px-font / 2px-padding / 40px-logo / `gap: 0` trims and the `.chrome-band { padding: var(--space-sm); }` and switcher-link trims)
    - .planning/phases/13-nav-integration/13-UI-SPEC.md (Mobile fit section: D-02 "no two-line wrap at any width", `--tap-target-min` 44px floor must be preserved, D-03 abbreviation is a last resort only reached AFTER the padding/gap/font trims are proven insufficient)
    - .planning/phases/13-nav-integration/13-VERIFICATION.md (Key Link Verification row: the @media breakpoints are "NOT WIRED" — 360-374px falls under the 767px block's flex-wrap: wrap with no override)
    - tests/e2e/site-header.spec.ts (the Task 1 same-row assertions this task must turn GREEN, plus the existing site-header suite that must stay GREEN)
  </read_first>
  <action>
    Repair the root-cause breakpoint gap so the 4-link header stays one row with the switcher, with no horizontal overflow, across the full 320-767px range in BOTH variants. Make exactly two edits, both inside SiteHeader.astro's `<style is:global>` block, touching ONLY responsive header rules — do NOT alter markup, props, colors, the logo-hover mechanic, the desktop (>=768px) layout, or any `min-height`/tap-target value:

    1. In the `@media (max-width: 767px)` block, change the `.site-header` rule's flex-wrap value from wrap to nowrap. This is the root fix: setting the header to nowrap across the whole sub-768px range means the language switcher can no longer drop onto a second row at ANY width — the exact failure Truth #5 reported. The base (>=768px) `.site-header` rule has no flex-wrap declaration (so it already behaves as nowrap), making the header consistently single-row at every width after this change. Keep the block's `gap: var(--space-xs)` and the `.site-nav` margin/gap trims exactly as they are.

    2. Raise the compensating-trim breakpoint: change the media query condition from `@media (max-width: 359px)` to `@media (max-width: 400px)`. Leave EVERY declaration inside that block unchanged (flex-wrap: nowrap; `.site-header` gap: 0; `.chrome-band` padding var(--space-sm); `.logo-mark__img` width 40px; `.site-nav` margin-left var(--space-xs) + gap 0 + min-width 0; `.site-nav .nav-link` padding 2px left/right + font-size 12px + min-width 0; `.site-nav .nav-link svg` 18px; `.site-header .language-switcher .switcher-link` padding 2px + font-size 12px). Widening the ceiling to 400px makes the same trims that already fit all 4 links + logo + switcher on one row at 320px (proven in 13-01) now apply across the entire previously-broken 360-400px band, so the nowrap header fits there without horizontal overflow. Above 400px the existing 767px-block trims (14px font, 56px logo) already fit the 4 links on one row (13-VERIFICATION.md independently confirmed no wrap at 393px), so the roomier tier is preserved for wider phones.

    Do NOT add D-03's Éditions-label abbreviation: the trim-breakpoint raise fits all four full labels on one row, so no abbreviation markup/span and no `.nav-link--editions` class are introduced (consistent with 13-01-SUMMARY.md, which already found the trims sufficient at 320px). Every `.nav-link` keeps `min-height: var(--tap-target-min)` (44px) at every breakpoint — this fix changes horizontal fit only, never the vertical tap target.

    Concrete breakpoint target = 400px (guaranteed to cover the 360-375px band with margin; the value 13-VERIFICATION.md itself suggested). Only if Task 3's live check confirms the 380-400px widths ALSO stay single-row on the roomier (>=768px-style, 14px/56px) tier may the executor instead lower the raised condition to `@media (max-width: 380px)` to avoid over-compacting the common ~390px phones — and only if the Task 1 sweep stays fully GREEN at every sampled width.
  </action>
  <verify>
    <automated>npm run build && npm run test:e2e -- site-header 2>&1 | tail -40  # Task 1 same-row assertions PASS at 360/374/375px on both /about/ and /; all pre-existing site-header assertions stay GREEN</automated>
    <automated>npm run test:e2e -- homepage 2>&1 | tail -20  # homepage regression guard: existing homepage nav/order/overflow tests still PASS</automated>
    <automated>grep -n "max-width: 400px" src/components/SiteHeader.astro  # the compensating-trim media query ceiling was raised from 359px (or 380px if the Task 3-verified refinement was taken)</automated>
  </verify>
  <acceptance_criteria>
    - The new Task 1 same-row assertions (nav-link vs switcher center within 5px) PASS at every sampled width 320/360/374/375/390/767 for BOTH /about/ and /.
    - scrollWidth <= innerWidth holds at every sampled width (the nowrap change introduced no horizontal overflow).
    - `grep -n "max-width: 400px" src/components/SiteHeader.astro` returns the raised compensating-trim media query (or `max-width: 380px` only if Task 3's live check backed the tighter refinement).
    - The `@media (max-width: 767px)` `.site-header` rule sets flex-wrap: nowrap (behaviorally proven by the passing same-row e2e across the whole range).
    - git diff for src/components/SiteHeader.astro is confined to the two responsive @media rules — no new nav elements, no abbreviation span, no `.nav-link--editions` class, no changes to markup/props/colors/logo-hover/desktop layout.
    - Every `.nav-link` still declares `min-height: var(--tap-target-min)`; the full existing site-header e2e suite (nav order, editions-route, carousel-editions-free, contextual link colors) and the homepage suite stay GREEN.
  </acceptance_criteria>
  <done>The 4-link header sits on one row with the language switcher — no two-row wrap, no horizontal overflow — across 320-767px in both the solid and transparent variants (Truth #5 repaired); Task 1's regression assertions are GREEN and no other suite regresses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live human re-verification at 375px in both header variants (closes missing item #3)</name>
  <files>src/components/SiteHeader.astro (verified visually; not edited in this task)</files>
  <read_first>
    - .planning/phases/13-nav-integration/13-VERIFICATION.md (the specific claim being re-checked: the SUMMARY's assertion that a live screenshot at "320px and 375px" confirmed single-row fit in both variants was directly contradicted by the verification's own reproduction at 375px — this checkpoint exists to make the live check actually happen this time)
  </read_first>
  <what-built>
    A CSS breakpoint fix in SiteHeader.astro: `flex-wrap: nowrap` applied to `.site-header` across the whole `@media (max-width: 767px)` range (was `wrap`), plus the compensating padding/gap/font-size/logo trims raised from `@media (max-width: 359px)` to `@media (max-width: 400px)`. An automated same-row e2e sweep now passes at 360/374/375px (the previously-untested wrap band) on both a solid and a transparent page. This checkpoint confirms the fix visually on real/emulated devices — the live step 13-VERIFICATION.md found was falsely reported in 13-01.
  </what-built>
  <action>
    Automated verification is complete (Tasks 1-2). This is a blocking human-verify gate — do NOT auto-approve it (this exact live step was previously claimed but not actually performed, which is how the regression shipped). Present the how-to-verify steps to the human and wait for their explicit response before the phase can close. Steps for the human:
    1. Build and serve the site locally: `npm run build && npm run preview`; note the local preview URL.
    2. In a browser, open responsive/device mode and set the viewport to 375px wide (iPhone SE), ~700px tall.
    3. Load the homepage `/` (transparent header over the hero photo): confirm the four nav items (Éditions, À propos, Contact, Instagram icon) + the logo + the FR/EN language switcher all sit on ONE row — the switcher is NOT on a second line below the nav and does not appear as a wrapped row over the hero photo.
    4. Load `/about/` (solid header): confirm the same single-row layout — switcher on the nav's row, no wrap, no horizontal scrollbar.
    5. Repeat at 360px and at 320px on both pages: still a single row, nav text legible at the compact 12px size, no clipping, no horizontal scroll.
    6. Slowly sweep the width from 320px up to ~450px on both pages and confirm there is NO width where the switcher drops to a second row — pay closest attention to the 360-375px band.
  </action>
  <verify>
    <human-check>
      Human confirms a single-row header (switcher on the nav's row, no second row) at 375px, 360px, and 320px on BOTH `/` (transparent) and `/about/` (solid), and that no width in a 320-450px sweep produces a two-row wrap on either page. If any wrap remains, report the failing width(s)/page(s) so Task 2's trim breakpoint is widened (toward/at 400px) before closing — do not approve.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms a single-row header (switcher on the nav's row, no second row) at 375px, 360px, and 320px on BOTH `/` (transparent) and `/about/` (solid).
    - Human confirms no width in a 320-450px sweep produces a two-row wrap on either page.
    - If any wrap remains at any width/page, the checkpoint is NOT approved and the failing width(s) and page(s) are reported so Task 2's trim breakpoint can be widened (toward/at 400px) before closing.
  </acceptance_criteria>
  <resume-signal>Type "approved" if the header is single-row at 320/360/375px and across the 320-450px sweep on BOTH the homepage (transparent) and /about/ (solid); otherwise describe the exact width(s) and page(s) where the switcher still wraps.</resume-signal>
  <done>The live human-check is re-run and approved in both header variants — no two-row wrap at 320/360/375px or across the 320-450px sweep — replacing the previously contradicted 13-01-SUMMARY.md claim (missing item #3 closed).</done>
</task>

</tasks>

<threat_model>
## Assessment: no new attack surface introduced

This gap-closure plan changes two CSS media-query rules in one presentational Astro component and adds one Playwright e2e assertion. It introduces **no new request-time surface** (the site is static, zero-compute on OVH/GitHub Pages — all content is fetched at BUILD time, published perspective only), **no new user input**, **no new authentication/authorization**, **no new data flow or sensitive-data storage**, and **no package-manager installs** (npm/pip/cargo). No new markup, props, or dynamic content are added — only responsive layout thresholds and a test. A full STRIDE sweep would fabricate threats that do not apply here; the register below records the assessment and confirms no high/BLOCKER threat exists (ASVS L1, block-on-high).

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (No new boundary) | The Éditions link and its label already shipped in 13-01 across the pre-existing, already-trusted Sanity-content -> build-time GROQ -> static-HTML boundary. This plan touches only CSS breakpoints and an e2e test; it crosses no boundary and adds none. |
| (No request-time boundary) | The deployed site has no server/compute; the header is static HTML/CSS. No client-side data flow is added or changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02-01 | Tampering | `SiteHeader.astro` responsive CSS (`@media` breakpoints) | low | accept | CSS layout thresholds carry no security-relevant state; a wrong value degrades visual layout only (the bug being fixed), never data integrity/confidentiality. No mitigation code applies beyond the e2e regression guard added by this plan. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install occurs in this phase (no new dependency). No RESEARCH Package Legitimacy Gate applies. |

**Block-on-high check:** no threat is rated high or critical; nothing blocks. The block is present as required, with all threats dispositioned.
</threat_model>

<verification>
Phase-level gates (run after all tasks complete):

- `npm run build` succeeds with no TypeScript/Astro errors.
- `npm run test:e2e -- site-header` fully GREEN — including the new "single-row fit across the mobile range" describe block passing at 320/360/374/375/390/767 on both /about/ (solid) and / (transparent), AND every pre-existing site-header assertion (nav order, Éditions-route resolution, carousel-editions-free guard, contextual link colors, the 320px and 393px scrollWidth checks) still passing.
- `npm run test:e2e -- homepage` fully GREEN — no homepage regression from the header CSS change.
- Live human-check (Task 3) recorded and approved: single-row header confirmed at 375px/360px/320px on both variants, no wrap across a 320-450px sweep.
- SUMMARY records the final compensating-trim breakpoint used (400px, or 380px only if Task 3-verified) and confirms D-03 abbreviation was NOT triggered.
</verification>

<success_criteria>
Repairs the one failed must-have from 13-VERIFICATION.md (Truth #5 / SC #5), addressing all three of its "missing" items:

1. Missing item #1 (CSS fix): the header stays single-row with no wrap across the full 320-767px range — not just at 320px/393px — via `flex-wrap: nowrap` in the `@media (max-width: 767px)` block plus the compensating trims raised to `@media (max-width: 400px)`. Verified across a width sweep in both variants.
2. Missing item #2 (test blind spot): a new e2e assertion samples INSIDE the 360-375px band (375px, the "iPhone SE ~375px" target) and fails on a vertical wrap by comparing the nav-link and language-switcher bounding-rect centers — coverage the `scrollWidth <= innerWidth` check structurally could not provide.
3. Missing item #3 (live re-check): the live human-check at 375px in both the solid (/about/) and transparent (/) variants is actually re-run (blocking checkpoint), replacing the previously contradicted claim.

EDN-01 coverage is restored to fully satisfied: the nav entry is now robust on the common ~360-375px phones, so "discoverable on every page" holds on those devices too.
</success_criteria>

<output>
Create `.planning/phases/13-nav-integration/13-02-SUMMARY.md` when done, recording: the two CSS edits made (nowrap in the 767px block; the raised compensating-trim breakpoint value actually used), whether the executor kept 400px or took the 380px refinement (and why), the new e2e describe block and the widths it samples, confirmation that D-03 abbreviation was NOT needed, and the Task 3 live human-check result in both variants.
</output>
