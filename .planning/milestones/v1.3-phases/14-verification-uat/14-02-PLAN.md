---
phase: 14-verification-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/scripts/verify-static-artifact.mjs
autonomous: true
requirements: []   # Cross-cutting verification phase — owns no primary REQ. Implements locked decision D-05 (closes PITFALLS.md "No stray commerce UI" more thoroughly).

must_haves:
  truths:
    - "`npm run test:artifact` scans BOTH the built Éditions HTML (existing behaviour) AND the `sanity/schemas/edition.ts` source file (new, D-05) for commerce tokens, reusing the SAME shared token arrays and `containsWholeWord` helper already defined in the script, and exits 0 against the current tree."
    - "The extension is a regression GUARD, not a fix: the current `edition.ts` Studio copy contains none of the forbidden tokens, so the added scan passes immediately — but introducing any commerce token into the schema's field titles/descriptions/group titles would fail the build-blocking guard."
    - "No new tool, framework, dependency, or forked token list is introduced — the addition reuses the existing script's helper and arrays verbatim (D-05)."
  artifacts:
    - path: "tests/scripts/verify-static-artifact.mjs"
      provides: "A second scan target reading sanity/schemas/edition.ts through the existing commerce-token loops (D-05)"
      contains: "sanity/schemas/edition.ts"
  key_links:
    - from: "tests/scripts/verify-static-artifact.mjs (existing containsWholeWord + token arrays)"
      to: "sanity/schemas/edition.ts (Studio field copy)"
      via: "reused (not forked) token loops applied to the schema source string"
      pattern: "edition\\.ts"
---

<objective>
Implement locked decision **D-05**: extend the existing build-blocking commerce-language guard (`tests/scripts/verify-static-artifact.mjs`, run by `npm run test:artifact`) so it also scans the developer-authored Studio copy in `sanity/schemas/edition.ts` (field titles, descriptions, group titles) for stray commerce terms — not just the rendered visitor-facing HTML it already checks.

PITFALLS.md's "No stray commerce UI" checklist item explicitly says to grep the new templates AND the **schema**; the current guard covers only built HTML, so schema-level commerce copy is a blind spot. This closes it as a cheap regression guard, protecting the EDN-06 "no pricing/availability/purchase" boundary at the source-code layer where the future shop milestone is most likely to first leak commerce language.

Purpose: the omission-class risk this milestone is dominated by includes commerce language creeping into Studio labels; a source-level guard fails loudly at build time if it ever does.
Output: one extended script; `npm run test:artifact` still green (the schema is currently clean, verified live by the pattern-mapper).

MVP-mode note (judgment per orchestrator): closure/hardening pass, not a new end-to-end feature — no vertical UI→API→DB slice applies; the plan is a single surgical script extension.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-verification-uat/14-CONTEXT.md
@.planning/phases/14-verification-uat/14-PATTERNS.md
@tests/scripts/verify-static-artifact.mjs
@sanity/schemas/edition.ts
</context>

## Artifacts this phase produces

New/changed symbols introduced by this plan (downstream verification reference):

- **`tests/scripts/verify-static-artifact.mjs`** — a NEW scan block, added after the existing dist-HTML scan, that reads `sanity/schemas/edition.ts` (relative to the script via `import.meta.url`) as plain UTF-8 text and runs its lowercased content through the SAME three existing token loops (`symbolCommerceTokens`, `prefixCommerceTokens`, and `wholeWordCommerceTokens` via the existing `containsWholeWord` helper), pushing an EDN-06-labelled failure string for any match. No script/style stripping (it is source, not HTML); no dist/HTML-only path filter (it is a source file, not a build artifact).
- **Explicitly NOT produced**: no new token list (the existing arrays are reused, NOT forked or edited — the token set is contractually tied to 12-UI-SPEC.md's Copywriting Contract and must not change here); no new npm script, tool, or dependency; no rewrite of the existing dist-HTML scan; no change to `sanity/schemas/edition.ts` itself.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the artifact guard to scan the edition schema source (D-05)</name>
  <files>tests/scripts/verify-static-artifact.mjs</files>
  <read_first>
    - tests/scripts/verify-static-artifact.mjs (the existing structure: the three token arrays and the `containsWholeWord` word-boundary/accent-aware helper; the existing dist-HTML scan loop and how it accumulates into the `failures` array and exits non-zero when non-empty)
    - .planning/phases/14-verification-uat/14-PATTERNS.md (the "tests/scripts/verify-static-artifact.mjs (D-05 commerce-language grep extension)" section — the exact addition shape and the note that the schema is currently clean, so this passes immediately)
    - sanity/schemas/edition.ts (the scan target — confirm its Studio copy contains no forbidden token today, so the added scan is a green regression guard, not a fix)
  </read_first>
  <action>
    Add a second scan target to the script, AFTER the existing dist-HTML scan (a pure addition — do not modify or reorder the existing block). Read `sanity/schemas/edition.ts` as plain UTF-8 text via a URL resolved from `import.meta.url`, lowercase it, and run it through the SAME three token checks the HTML scan already uses: the symbol-token `includes` check, the prefix-token `includes` check, and the whole-word check via the existing `containsWholeWord` helper. On any match, push a clearly-labelled failure into the existing `failures` array (message naming the file and referencing EDN-06) so the script's existing non-zero-exit-on-failures behaviour blocks the build.
    Reuse the token arrays and `containsWholeWord` helper already defined in the file — do NOT redeclare, fork, edit, or extend the token lists (they are pinned to the 12-UI-SPEC.md Copywriting Contract). Since `edition.ts` is source (not built HTML), do not strip script/style and do not apply the dist path filter. Do NOT re-state the literal token values anywhere in a new comment; reference the existing arrays by name only.
  </action>
  <verify>
    <automated>npm run test:artifact && grep -c "edition.ts" tests/scripts/verify-static-artifact.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `tests/scripts/verify-static-artifact.mjs` reads `sanity/schemas/edition.ts` and runs it through the existing `symbolCommerceTokens`/`prefixCommerceTokens`/`wholeWordCommerceTokens` + `containsWholeWord` checks, pushing an EDN-06-labelled failure on any match.
    - The three token arrays and `containsWholeWord` are reused, not redeclared or forked; the existing dist-HTML scan block is unchanged.
    - `npm run test:artifact` exits 0 (the schema is currently clean), and the script now references `sanity/schemas/edition.ts` (grep count ≥ 1).
  </acceptance_criteria>
  <done>The build-blocking commerce guard now covers both built Éditions HTML and the édition schema source, reusing the existing helper/token set; it passes clean today and would fail loudly if commerce language were ever added to the schema's Studio copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer-authored Studio copy (`edition.ts`) → build pipeline | This plan adds a build-time verification gate over source code that Romane/Florian author. No request-time boundary (static site, zero compute). It strengthens an existing boundary check; it does not add a data flow or user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-I | Information Disclosure | Éditions Studio field copy / rendered pages | medium | mitigate | This plan's D-05 extension is itself the mitigation: it prevents commerce language (price/stock/availability/purchase terms) from silently entering the schema copy — the EDN-06 boundary — where it would leak a transactional affordance this milestone explicitly defers. Impact medium (scope/boundary breach, not data leak), likelihood low; closed by the new build-blocking scan. |
| T-14-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs; no new dependency. The script uses only Node built-ins (`fs`, `url`) already imported. No `[ASSUMED]`/`[SUS]` packages. |

No high-severity threat exists for this plan (ASVS L1, block-on-high): the single tracked threat is medium and is mitigated by the change itself.
</threat_model>

<verification>
- `npm run test:artifact` exits 0 with the new schema scan active.
- `grep -c "edition.ts" tests/scripts/verify-static-artifact.mjs` returns ≥ 1 (the scan target is wired).
- Diff review confirms the token arrays / `containsWholeWord` helper were reused, not forked, and the existing dist-HTML scan is untouched.
</verification>

<success_criteria>
The commerce-language guard scans both built Éditions HTML and `sanity/schemas/edition.ts` using one shared token set + helper, passes clean on the current tree, and would block a build that introduced commerce language into the schema copy (EDN-06 protected at the source layer).
</success_criteria>

<output>
Create `.planning/phases/14-verification-uat/14-02-SUMMARY.md` when done.
</output>
