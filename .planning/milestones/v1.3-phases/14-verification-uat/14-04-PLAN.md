---
phase: 14-verification-uat
plan: 04
type: execute
wave: 3
depends_on: [14-03]
files_modified:
  - .planning/phases/14-verification-uat/14-ROMANE-UAT.md
autonomous: false
requirements: []   # Cross-cutting verification phase — owns no primary REQ. Implements locked decisions D-03 + D-04 (ROADMAP SC #3).

must_haves:
  truths:
    - "A short, non-technical, FRENCH Studio checklist doc (`14-ROMANE-UAT.md`) instructs Romane to CREATE a brand-new édition (not just edit the seeded 'Rebut'), add at least one photo, fill statement + format details, PUBLISH it, then DRAG-REORDER the two éditions in the Studio list and confirm the new order persists across a refresh — matching the tone/register of the existing EditorialDashboard French copy (D-03, D-04)."
    - "The create-step requirement means create + edit + publish + drag-reorder are ALL genuinely exercised — closing the exact gap `11-UAT.md` left open when drag-reorder was waived for lack of a second document (D-04)."
    - "A BLOCKING human-verify checkpoint records Romane's completed pass via an explicit typed approval relayed by Florian — the executor CANNOT self-report it; the phase cannot be marked complete until it is approved, mirroring Phase 13's Task-3 gate that 13-VERIFICATION.md independently cross-checked (D-03, ROADMAP SC #3)."
  artifacts:
    - path: ".planning/phases/14-verification-uat/14-ROMANE-UAT.md"
      provides: "French, plain-imperative Studio checklist: create → add photos → statement/format → publish → drag-reorder → confirm persistence (→ optional cleanup)"
      min_lines: 20
  key_links:
    - from: ".planning/phases/14-verification-uat/14-ROMANE-UAT.md (create-a-second-édition step)"
      to: ".planning/phases/11-schema-content-model/11-UAT.md (the waived drag-reorder test)"
      via: "a genuine 2nd document makes drag-reorder meaningfully testable for the first time"
      pattern: "drag-reorder|réorganiser|glisser"
---

<objective>
Implement locked decisions **D-03** and **D-04** — the one ROADMAP success criterion (#3) that has never actually been done: a real, hands-on Sanity Studio content-editing pass by Romane herself, confirming she can self-serve éditions the same way she already does galleries.

Two parts: (1) produce a short, non-technical, French step-by-step checklist doc for Florian to hand to Romane, in the register of the existing EditorialDashboard Studio copy (plain imperative present tense, no jargon); (2) a BLOCKING human-verify checkpoint that the phase cannot close without — Romane's completed pass, relayed by Florian as an explicit typed approval, never self-reported by the executor. The checklist requires her to CREATE a new édition (D-04), not merely edit the seeded "Rebut", so create + edit + publish + drag-reorder are all genuinely exercised — closing the gap `11-UAT.md` left open when drag-reorder was waived because only one document existed.

Purpose: PROJECT.md's core maintainer constraint is that Romane self-serves content; SC #3 requires this confirmed in her own experience ("works the same way galleries already do") — never yet validated with her own hands for Éditions.
Output: `14-ROMANE-UAT.md` (French checklist) + a recorded blocking approval.

MVP-mode note (judgment per orchestrator): the only place MVP sequencing genuinely applies to this audit phase — the blocking human gate is placed LAST (Wave 3, after all automated fixes and the closure audit are green in Plans 14-01/02/03), so Romane's real-Studio time is not spent until everything else is confirmed closed.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-verification-uat/14-CONTEXT.md
@.planning/phases/14-verification-uat/14-PATTERNS.md
@.planning/phases/11-schema-content-model/11-UAT.md
@.planning/phases/13-nav-integration/13-02-PLAN.md
@sanity/editorial/EditorialDashboard.tsx
</context>

## Artifacts this phase produces

New symbols introduced by this plan (downstream verification reference):

- **`.planning/phases/14-verification-uat/14-ROMANE-UAT.md`** — a French, non-technical Studio checklist for Romane. Steps (plain imperative, EditorialDashboard tone): open the hosted Studio → in "Éditions", CREATE a new édition (real content if a second one such as "Sillo" is ready, otherwise a throwaway to delete afterward) → give it a title → add at least one photo with its alt/rights → write the FR + EN statement → fill the format details (pages, tirage, dimensions) → set it "En préparation" and confirm it does NOT appear on the live site → PUBLISH it → confirm it appears on `/editions/` and `/en/editions/` → DRAG-REORDER the two éditions in the Studio list and confirm the new order persists after a page refresh (identical to how "Collections photo" already behaves) → (optional) delete the throwaway édition. Ends with what to report back to Florian.
- **Explicitly NOT produced**: no code change; no automated test (SC #3 is a human action Claude cannot perform); the doc is Studio-editor-facing prose, not developer instructions.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write the French Romane Studio UAT checklist (D-03, D-04)</name>
  <files>.planning/phases/14-verification-uat/14-ROMANE-UAT.md</files>
  <read_first>
    - sanity/editorial/EditorialDashboard.tsx (the French Studio-copy tone to match: short declarative/imperative present-tense French, no jargon — e.g. "à vérifier avant publication", "en cours de rédaction", "Aucun contenu en attente")
    - .planning/phases/11-schema-content-model/11-UAT.md (the drag-reorder test that was WAIVED for lack of a 2nd document — the exact gap this checklist closes by requiring a create step)
    - .planning/phases/14-verification-uat/14-PATTERNS.md (the "Romane UAT checklist doc (D-03/D-04)" section — tone takeaways, the create-step requirement, and the mirror of Phase 13's blocking gate)
    - .planning/phases/14-verification-uat/14-CONTEXT.md (D-03/D-04 in full)
  </read_first>
  <action>
    Write `.planning/phases/14-verification-uat/14-ROMANE-UAT.md` as a short French, non-technical checklist Florian can hand to Romane. Use plain imperative present tense in the EditorialDashboard register ("Créez une nouvelle édition...", "Ajoutez au moins une photo...", "Cliquez sur Publier...", "Faites glisser pour réorganiser...") — no technical/developer terms. It MUST include, in order: create a NEW édition (real content if "Sillo" or another second édition is ready, otherwise a temporary one to delete afterward — D-04, do not just edit "Rebut"); give it a title; add at least one photo with its credits/rights; write the statement in French and English; fill the format details (nombre de pages, tirage, dimensions); set it "En préparation" and confirm it stays OFF the live site; publish it and confirm it appears on the Éditions pages in both languages; drag-reorder the two éditions in the Studio list and confirm the order persists after refreshing the page (same as "Collections photo"/galleries); optionally delete the throwaway. End with a short "à confirmer à Florian" note listing what she should report (did each step behave like galleries; anything confusing). Keep it concise and scannable. This is prose — do not include code, and do not re-state any commerce/price term.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-verification-uat/14-ROMANE-UAT.md && grep -ciE "cr[eé]er|nouvelle [eé]dition" .planning/phases/14-verification-uat/14-ROMANE-UAT.md && grep -ciE "glisser|r[eé]organiser|r[eé]ordonner" .planning/phases/14-verification-uat/14-ROMANE-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `14-ROMANE-UAT.md` exists, is written in non-technical French matching the EditorialDashboard register, and reads as steps for a non-developer.
    - It requires CREATING a new édition (not only editing "Rebut") and includes create + edit + publish + drag-reorder + confirm-persistence steps, plus the "En préparation" draft-does-not-go-live check and optional throwaway cleanup.
    - It ends with what Romane should report back to Florian.
    - No code and no commerce/price terminology appear in the doc.
  </acceptance_criteria>
  <done>A concise French Studio checklist exists that exercises create/edit/publish/drag-reorder end-to-end, closing the drag-reorder gap 11-UAT.md waived — ready for Florian to hand to Romane.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING — Romane's real Studio content-editing pass (D-03, D-04, ROADMAP SC #3)</name>
  <files>.planning/phases/14-verification-uat/14-ROMANE-UAT.md (used as the script; not edited in this task)</files>
  <read_first>
    - .planning/phases/14-verification-uat/14-ROMANE-UAT.md (the checklist just written — the steps Romane follows)
    - .planning/phases/13-nav-integration/13-02-PLAN.md (Task 3 — the blocking human-verify gate pattern to mirror: explicit typed approval, executor may NOT auto-approve, and 13-VERIFICATION.md independently cross-checked a real approval was recorded)
  </read_first>
  <what-built>
    The Éditions content model + routes + nav shipped in Phases 11-13, and (this phase) the null-safety hardening (14-01), commerce-guard extension (14-02), and closure audit (14-03) are all complete and green. What has NEVER been done is a real, hands-on Sanity Studio pass by Romane herself — every prior touch of Éditions was Florian's dev-side work, and `11-UAT.md` explicitly WAIVED the drag-reorder test because only one édition existed. This checkpoint is that pass: Romane creates a second édition, edits/publishes it, and drag-reorders — confirming she can self-serve éditions exactly like galleries (SC #3).
  </what-built>
  <action>
    Automated work for this phase is complete (Plans 14-01/02/03). This is a BLOCKING human-verify gate — do NOT auto-approve it and do NOT self-report it (SC #3 requires Romane's own hands, cross-checkable the same way 13-VERIFICATION.md verified Phase 13's gate). Present the checklist to Florian to relay to Romane, and WAIT for an explicit typed response before the phase can close. Steps for the human (Florian → Romane):
    1. Open the hosted Sanity Studio and hand Romane the checklist in `14-ROMANE-UAT.md`.
    2. Romane creates a NEW édition (a real second one like "Sillo" if ready, otherwise a temporary throwaway), adds at least one photo, writes the FR + EN statement, fills the format details, sets it "En préparation", and confirms it does NOT appear on the live `/editions/` pages.
    3. Romane publishes it and confirms it appears on both `/editions/` and `/en/editions/`.
    4. Romane drag-reorders the two éditions in the Studio "Éditions" list and confirms the new order persists after refreshing the page — the same behaviour as "Collections photo" (galleries).
    5. Romane confirms, in her own words, that editing éditions works the same way galleries already do; the throwaway édition (if used) is deleted.
    Florian relays her outcome as the approval/rejection below.
  </action>
  <verify>
    <human-check>
      Florian confirms (relaying Romane) that she successfully created, edited, published, and drag-reordered a second édition in the hosted Studio, that the "En préparation" draft did NOT go live, that the reordering persisted across a refresh, and that the experience matches galleries. If any step failed or confused her, that is reported instead of approved, with the specific step and what went wrong.
    </human-check>
  </verify>
  <acceptance_criteria>
    - An explicit typed approval from Florian (relaying Romane) is recorded — not inferred or self-reported by the executor.
    - The approval confirms create + edit + publish + drag-reorder-with-persistence all worked, and that "En préparation" stayed off the live site.
    - If not approved, the failing step(s) and observed behaviour are recorded so the gap can be addressed before the phase closes.
    - The phase is NOT marked complete until this checkpoint is approved (blocking gate, mirroring Phase 13 Task 3).
  </acceptance_criteria>
  <resume-signal>Type "approved" if Romane successfully created, edited, published, AND drag-reordered a second édition in the hosted Studio (order persisting across refresh, "En préparation" staying off the live site, experience matching galleries); otherwise describe the exact step where it failed or confused her.</resume-signal>
  <done>Romane's real end-to-end Studio pass (create/edit/publish/drag-reorder) is completed and recorded via an explicit human approval relayed by Florian — closing ROADMAP SC #3 and the drag-reorder gap 11-UAT.md waived, with the approval independently cross-checkable (not executor self-reported).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-technical editor (Romane) → hosted Sanity Studio → published content → build | This plan validates the EXISTING content-authoring boundary by exercising it manually; it adds no new boundary. The Studio and its publish-time validation already shipped in Phase 11. No code, no request-time surface (static site, zero compute), no new user input on the public site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04-T | Tampering / content integrity | A published (or throwaway) édition created during the UAT pass | low | mitigate | The checklist instructs setting "En préparation" first and confirming it stays off the live site before publishing, and deleting any throwaway afterward — so the UAT pass cannot leave stray/half-populated content live. Plan 14-01's null-safety guards additionally ensure a partially-populated UAT document cannot crash the build. |
| T-14-04-R | Repudiation / verification integrity | The SC #3 human-approval record | medium | mitigate | The blocking checkpoint requires an explicit typed approval relayed by Florian and forbids executor self-reporting — mirroring Phase 13's gate that 13-VERIFICATION.md independently cross-checked; this prevents a "claimed but not performed" UAT (the exact failure mode 13-VERIFICATION.md caught once before). |
| T-14-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs; documentation + human verification only. No `[ASSUMED]`/`[SUS]` packages. |

No high-severity threat exists for this plan (ASVS L1, block-on-high): the tracked items are low/medium and mitigated by the checklist's draft-first/cleanup discipline and the blocking, non-self-reportable approval gate.
</threat_model>

<verification>
- `14-ROMANE-UAT.md` exists, is non-technical French, and covers create/edit/publish/drag-reorder + the draft-stays-off-live check + optional cleanup.
- The blocking checkpoint is recorded as approved via an explicit typed response from Florian (relaying Romane), not self-reported — the phase does not close otherwise.
- The approval confirms the experience matches galleries and the reordering persisted across a refresh (closing the 11-UAT.md waiver).
</verification>

<success_criteria>
ROADMAP SC #3 is genuinely met: Romane has created, edited, published, and drag-reordered a second édition in the hosted Studio with her own hands and confirmed it works like galleries, recorded as an explicit blocking human approval that an executor could not fabricate.
</success_criteria>

<output>
Create `.planning/phases/14-verification-uat/14-04-SUMMARY.md` when done.
</output>
