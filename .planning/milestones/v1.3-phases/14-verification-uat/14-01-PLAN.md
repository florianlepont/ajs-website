---
phase: 14-verification-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/editions/[slug].astro
  - src/pages/en/editions/[slug].astro
  - src/pages/editions/index.astro
  - src/pages/en/editions/index.astro
autonomous: true
requirements: []   # Cross-cutting verification phase — owns no primary REQ (EDN-01..07/CMS-04 are owned by Phases 11-13). Implements locked decision D-02.

must_haves:
  truths:
    - "Both Éditions detail pages (FR `src/pages/editions/[slug].astro` + EN twin) access every format-detail and image-array field through `?.`/`?? ''` guards, so a published-but-partially-populated édition (missing dimensions / pageCount / printRun / images) renders its page instead of throwing during `astro build` (D-02)."
    - "Both Éditions overview pages (FR `src/pages/editions/index.astro` + EN twin) read `edition.statement[locale]` via `edition.statement?.[locale] ?? ''`, matching the detail page's own already-correct guard for that exact field (D-02)."
    - "`npm run build` and `npx astro check` both succeed and `npm run test:e2e -- edition` stays 6/6 green — the guards add resilience with ZERO behavioural regression against the well-formed seeded 'Rebut' édition."
  artifacts:
    - path: "src/pages/editions/[slug].astro"
      provides: "Guarded dimensions/pageCount/printRun/images access (D-02), mirroring WR-03"
      contains: "edition.images ?? []"
    - path: "src/pages/en/editions/[slug].astro"
      provides: "EN twin — identical guards at the same relative lines"
      contains: "edition.images ?? []"
    - path: "src/pages/editions/index.astro"
      provides: "Guarded statement access on the overview row"
      contains: "edition.statement?.[locale]"
    - path: "src/pages/en/editions/index.astro"
      provides: "EN twin — guarded statement access"
      contains: "edition.statement?.[locale]"
  key_links:
    - from: "src/pages/galleries/[slug].astro"
      to: "src/pages/editions/[slug].astro"
      via: "WR-03 null-guard idiom (`?.` / `?? ''`) applied to the equivalent édition field class"
      pattern: "\\?\\? ''|\\?\\."
---

<objective>
Close the non-blocking WARNING carried forward from `12-VERIFICATION.md` (code review CR-01/CR-02): the Éditions pages access nested/array fields WITHOUT the defensive `?.`/`?? ''` guards that the gallery detail page (WR-03) and the édition detail page's own `statement` line already use. Apply those guards to the four Éditions page files so a single malformed/partially-populated future édition document cannot crash the entire static build during `getStaticPaths` SSG.

This implements locked decision **D-02** exactly: inline guards mirroring the established WR-03 idiom (`src/pages/galleries/[slug].astro:33`), scoped to the specific unguarded accesses named in CONTEXT.md — no schema change, no refactor, no new capability.

Purpose: `astro build` renders every published édition in ONE pass; an unguarded field access on one bad document fails the WHOLE site build (every page, not just Éditions). This is the phase-goal's "the boundary holds across the whole feature" applied to the omission/resilience risk class.
Output: four hardened page files; build + typecheck + édition e2e all green.

MVP-mode note (judgment per orchestrator): this is a cross-cutting closure/hardening pass over already-shipped work, not a new end-to-end feature — there is no meaningful UI→API→DB vertical slice to build here, so a user-story slice structure is deliberately NOT forced. Tasks are grouped by the surgical edit surface instead.

Planner's-discretion note on D-02 test shape: NO new dedicated malformed-document unit/e2e test is added, and this is a deliberate choice, not an omission. (a) D-02 is locked as INLINE guards and the WR-03 precedent it mirrors is itself inline in `galleries/[slug].astro` with no template-layer malformed-doc test (its data-layer null case is already covered by `tests/unit/gallery-query.test.ts`); (b) an inline `.astro`-frontmatter guard cannot be unit-tested without either extracting a helper — which would exceed D-02's "add `?.`/`?? ''`" scope and contradict the pattern-map's fix shape — or introducing the Astro Container testing API, a technique used nowhere in this repo (new abstraction, against the project's "no coverage/abstraction beyond what's needed" convention); (c) the resilience property is guaranteed by `?.`/`?? ''` semantics and is confirmed PRESENT by the closure audit (Plan 14-03), which cites the guarded lines as evidence. Verification here is build + `astro check` + the existing édition e2e staying green (no regression against the well-formed doc).
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-verification-uat/14-CONTEXT.md
@.planning/phases/14-verification-uat/14-PATTERNS.md
@.planning/phases/12-data-fetch-layer-routes/12-VERIFICATION.md
@src/pages/galleries/[slug].astro
@src/pages/editions/[slug].astro
@src/pages/en/editions/[slug].astro
@src/pages/editions/index.astro
@src/pages/en/editions/index.astro
</context>

## Artifacts this phase produces

New/changed symbols introduced by this plan (downstream verification reference):

- **`src/pages/editions/[slug].astro` (FR detail)** — guards added on: the lightbox-images assembly (`[edition.leadPhoto, ...(edition.images ?? [])]`, was line 57); the format-detail string composition (`edition.dimensions?.width`/`.height`/`.unit`, `edition.pageCount`/`edition.printRun` with `?? ''` fallbacks, was lines 62-63); and the images grid render (`(edition.images?.length ?? 0) > 0` and `(edition.images ?? []).map`, was lines 111/114). The already-correct `edition.statement?.[locale] ?? ''` (line 46) and `edition.leadPhoto.alt?.[locale] ?? ''` (line 48) are LEFT UNTOUCHED.
- **`src/pages/en/editions/[slug].astro` (EN detail)** — the identical guards applied at the same relative lines (the file is a 1:1 mirror of the FR file per its own header comment, differing only in `locale = 'en'` and import depth).
- **`src/pages/editions/index.astro` (FR overview)** — `edition.statement[locale]` (was line 56) changed to `edition.statement?.[locale] ?? ''`. The already-guarded `edition.leadPhoto.alt?.[locale] ?? ''` in the same map callback is LEFT UNTOUCHED.
- **`src/pages/en/editions/index.astro` (EN overview)** — the same one-line guard (was line 52).
- **Explicitly NOT produced**: no schema change; no new `src/lib` helper / extraction; no new test file (see planner's-discretion note in the objective); no guard on `edition.leadPhoto` itself (out of D-02's stated scope — leadPhoto is the required lead image); no change to markup, styling, GROQ projections, or the `statement`/`leadPhoto.alt` accesses that are already correctly guarded.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Guard the two Éditions DETAIL pages (D-02)</name>
  <files>src/pages/editions/[slug].astro, src/pages/en/editions/[slug].astro</files>
  <read_first>
    - src/pages/galleries/[slug].astro (the WR-03 idiom being mirrored — the `?.`/`?? ''` guards on `statement`/`heroAlt`/`seo.*`; this is the canonical pattern to copy)
    - src/pages/editions/[slug].astro (the FR file — confirm the current unguarded accesses: the lightbox-images spread of `edition.images`; the `edition.dimensions.width`/`.height`/`.unit`, `edition.pageCount`, `edition.printRun` in the format-detail string; and the `edition.images.length`/`edition.images.map` in the images grid. The `edition.statement?.[locale] ?? ''` and `edition.leadPhoto.alt?.[locale] ?? ''` lines are ALREADY guarded — do not touch them)
    - src/pages/en/editions/[slug].astro (the EN twin — structurally identical minus `locale = 'en'` and import depth; the same accesses are unguarded at the same relative positions)
    - .planning/phases/14-verification-uat/14-PATTERNS.md (the "src/pages/editions/[slug].astro (D-02 null-safety)" section — exact current lines and the required fix shape)
    - .planning/phases/12-data-fetch-layer-routes/12-VERIFICATION.md (the "Non-blocking finding carried forward" note — the exact fields flagged and WHY the blast radius is whole-build)
  </read_first>
  <action>
    In BOTH detail files, apply the WR-03 `?.`/`?? ''` idiom to the currently-unguarded nested/array accesses, mirroring `src/pages/galleries/[slug].astro`:
    1. Lightbox-images assembly: spread `edition.images` through a nullish fallback so the array construction cannot throw when `images` is null/undefined — i.e. spread `(edition.images ?? [])` instead of `edition.images`.
    2. Format-detail string: guard the dimensions object member access with optional chaining (`edition.dimensions?.width`, `.height`, `.unit`) and give each interpolated value an empty-string fallback; give `edition.pageCount` and `edition.printRun` the same `?? ''` interpolation fallback. Follow WR-03's precedent of `?? ''` for interpolated fields — the binding requirement is that NO unguarded nested/array access remains, exact fallback token is your judgment.
    3. Images grid render: guard the length check as `(edition.images?.length ?? 0) > 0` and the map as `(edition.images ?? []).map(...)` so a missing `images` array renders nothing rather than throwing.
    Do NOT touch the already-correct `edition.statement?.[locale] ?? ''` line or the `edition.leadPhoto.alt?.[locale] ?? ''` line. Do NOT guard `edition.leadPhoto` itself (out of D-02 scope). Apply the FR and EN edits together so the two locale twins stay identical (Pitfall 3 — per-locale drift); the EN file must receive the exact same guards at the same relative lines.
    Do NOT add fenced code, JSDoc, or head-comment text that re-states any commerce token — this phase's build-blocking guard greps rendered HTML for those literals.
  </action>
  <verify>
    <automated>npm run build && npx astro check && npm run test:e2e -- edition 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - In both `src/pages/editions/[slug].astro` and `src/pages/en/editions/[slug].astro`: the lightbox-images assembly spreads `edition.images ?? []`; `edition.dimensions` is accessed via `?.`; `edition.pageCount`/`edition.printRun` have `?? ''` fallbacks; the images-grid length check and `.map` both go through `edition.images?` / `edition.images ?? []`.
    - The pre-existing `edition.statement?.[locale] ?? ''` and `edition.leadPhoto.alt?.[locale] ?? ''` lines are unchanged, and `edition.leadPhoto` is NOT newly guarded.
    - The FR and EN detail files differ ONLY in `locale`/import depth (guards are byte-for-byte equivalent otherwise).
    - `npm run build` succeeds, `npx astro check` reports 0 errors, and `npm run test:e2e -- edition` is 6/6 green (no regression against the well-formed 'Rebut' édition).
  </acceptance_criteria>
  <done>Neither édition detail page contains an unguarded nested/array access to `dimensions`/`pageCount`/`printRun`/`images`; a partially-populated édition would degrade gracefully instead of crashing the build; build, typecheck, and the édition e2e suite are all green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Guard the two Éditions OVERVIEW pages (D-02)</name>
  <files>src/pages/editions/index.astro, src/pages/en/editions/index.astro</files>
  <read_first>
    - src/pages/editions/index.astro (the FR overview — the `<p class="editions-list__statement">{edition.statement[locale]}</p>` access is unguarded, unlike the detail page's own `statement?.[locale] ?? ''`; the `edition.leadPhoto.alt?.[locale] ?? ''` earlier in the same map callback is already guarded)
    - src/pages/en/editions/index.astro (the EN twin — same unguarded `statement[locale]` access)
    - .planning/phases/14-verification-uat/14-PATTERNS.md (the "src/pages/editions/index.astro (D-02 null-safety)" section — one-line self-referential fix)
  </read_first>
  <action>
    In BOTH overview files, change the statement render `edition.statement[locale]` to `edition.statement?.[locale] ?? ''`, matching the detail page's own already-correct guard for the identical field. This is a one-line change per file. Do NOT touch the already-guarded `edition.leadPhoto.alt?.[locale] ?? ''` in the same callback, and do NOT alter any other markup, class, or logic. Apply the FR and EN edits together so the locale twins stay identical.
  </action>
  <verify>
    <automated>npm run build && npx astro check && npm run test:e2e -- edition 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/editions/index.astro` and `src/pages/en/editions/index.astro` both render the overview statement via `edition.statement?.[locale] ?? ''`.
    - No other line in either file changed; the two twins differ only in `locale`/import depth.
    - `npm run build` succeeds, `npx astro check` reports 0 errors, and `npm run test:e2e -- edition` stays 6/6 green.
  </acceptance_criteria>
  <done>Both overview pages guard the statement access identically to the detail page; a partially-populated édition renders an empty statement instead of throwing; build/typecheck/édition e2e green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Content Lake → build-time GROQ → static HTML | The existing, already-trusted build-time content boundary. This plan does NOT add a new boundary; it hardens how one already-crossed value (a partially-populated édition document) is consumed during SSG. No request-time surface exists (static site on OVH/GitHub Pages, zero compute, published perspective only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01-D | Denial of Service | `getStaticPaths` SSG render of `editions/[slug].astro` (+ EN twin) over ALL published éditions in one `astro build` | high | mitigate | This plan's D-02 `?.`/`?? ''` guards: a single malformed/partially-populated édition (created via Content Lake API / bulk import / a doc published before validation existed) can no longer throw during SSG and take down the entire site build (all pages). High impact (whole-deploy DoS) × low likelihood (Studio publish-time validation normally prevents it) — closed in-phase, so it does not block. |
| T-14-01-I | Information Disclosure | Overview/detail rendered HTML | low | accept | The guards render empty strings for missing fields — no draft/unpublished content is exposed (client is `published` perspective only, per existing `src/lib/sanity.ts`). No new data flow. |
| T-14-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan performs NO package-manager installs and adds no dependency — no supply-chain surface. No `[ASSUMED]`/`[SUS]` packages. |

No high-severity threat is left un-mitigated (ASVS L1, block-on-high): the one high-severity item (T-14-01-D) is the exact risk this plan mitigates.
</threat_model>

<verification>
- `npm run build` succeeds and emits all four édition route/locale HTML files.
- `npx astro check` reports 0 errors.
- `npm run test:e2e -- edition` is 6/6 green (overview ×3, detail, lightbox, no-commerce) — proving no regression against the well-formed seeded 'Rebut' édition.
- Manual diff review confirms every guard named in "Artifacts this phase produces" is present and no already-guarded line was touched.
</verification>

<success_criteria>
No unguarded nested/array access to `dimensions`/`pageCount`/`printRun`/`images` remains on either detail page, and both overview pages guard `statement[locale]`; the whole-build DoS risk (T-14-01-D) is mitigated; build, typecheck, and the édition e2e suite all pass with zero behavioural change against current content.
</success_criteria>

<output>
Create `.planning/phases/14-verification-uat/14-01-SUMMARY.md` when done.
</output>
