---
phase: 10-unified-header-simplified-language-switcher
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - src/components/LanguageSwitcher.astro
  - src/components/HomeCarousel.astro
  - tests/e2e/i18n.spec.ts
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [I18N-04]
must_haves:
  truths:
    - "On every page in both locales, the language switcher shows exactly ONE link (the other language) with a leading globe icon and no current-language link and no separator — D-07/D-08."
    - "Clicking the switcher navigates to the translated version of the current page and writes the ajs_locale cookie exactly as before — D-10."
    - "The switcher link's accessible name still contains 'EN' (on FR pages) / 'FR' (on EN pages), so getByRole link-name matching keeps passing in i18n.spec.ts and legal.spec.ts with no code change to legal.spec.ts — D-11/Pitfall 3/4."
  artifacts:
    - "src/components/LanguageSwitcher.astro simplified to one link + inline globe SVG"
  key_links:
    - "getSwitcherHref is called once for the non-current locale only (D-08); the inline cookie <script> is unchanged (D-10)."
    - "Only the globe <svg> is aria-hidden; the visible EN/FR text node stays in the accessible name (D-11)."
---

<objective>
Collapse `LanguageSwitcher.astro` from two links + a `|` separator to a single link — the OTHER language's code (EN on French pages, FR on English pages) preceded by a small dependency-free inline globe SVG (D-07/D-08/D-09) — while preserving the cookie-write + navigation behavior byte-for-byte (D-10) and keeping the visible EN/FR label inside the accessible name so existing role-based test locators keep resolving (D-11, Pitfall 3/4). This is I18N-04.

Purpose: Give the visitor one clear, terse language control instead of a two-link toggle where one link points at the page they are already on.
Output: A rewritten `LanguageSwitcher.astro`, a small dead-selector cleanup in `HomeCarousel.astro`, rewritten switcher assertions in `i18n.spec.ts` + `homepage.spec.ts`, and a verified-green `legal.spec.ts` regression.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-CONTEXT.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-RESEARCH.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-PATTERNS.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-UI-SPEC.md
@src/components/LanguageSwitcher.astro
@src/lib/i18n-paths.ts
</context>

<phase_goal>
**As a** bilingual visitor, **I want to** see one clear language control — the other language's code with a globe icon — instead of a two-link toggle that includes the language I'm already reading, **so that** switching languages is a single obvious action rather than visual clutter I have to parse.
</phase_goal>

<mvp_slice_note>
This plan's user-facing slice: after it ships, every page in both locales shows the simplified one-link + globe switcher, and clicking it still lands on the translated page and remembers the choice. It is a real, observable end-to-end change built on the now-shared header from Plans 01–02.
</mvp_slice_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — rewrite the switcher assertions to the one-link + globe contract (RED)</name>
  <files>tests/e2e/i18n.spec.ts, tests/e2e/homepage.spec.ts</files>
  <read_first>
    - tests/e2e/i18n.spec.ts lines 10-41 (the `locale content` block — lines 17 and 26 assert the current two-code separator text; line 38-39 compares header innerText across locales) and lines 43-85 (the `switcher` block — click-navigation + cookie assertions that must KEEP passing).
    - tests/e2e/homepage.spec.ts lines 90-104 (the `i18n non-regression guard` — line 96 asserts the current two-code separator text on `[data-role="site-header"]`).
    - tests/e2e/legal.spec.ts lines 118-156 — the four `getByRole('link', { name: 'EN' | 'FR' })` switcher-click tests that must stay GREEN with NO code change (Pitfall 4); read them so you understand the accessible-name substring dependency you must preserve.
    - 10-RESEARCH.md Pitfall 3 (only the globe SVG is aria-hidden; the visible code stays in the accessible name) and 10-UI-SPEC.md Copywriting Contract (final visible labels + sr-only hint copy).
  </read_first>
  <behavior>
    - On `/` (French), the switcher contains exactly one `.switcher-link`, its accessible name contains "EN", it renders an inline globe `<svg>`, and there is no `.switcher-separator` and no second language-code link.
    - On `/en/` (English), the single `.switcher-link` accessible name contains "FR".
    - The existing switcher click-navigation and cookie tests (i18n.spec.ts lines 43-85, legal.spec.ts lines 118-156) still pass unchanged — clicking the one remaining link navigates to the translated page and sets `ajs_locale`.
  </behavior>
  <action>
    Rewrite the two-language-code text assertions to the new one-link contract. In i18n.spec.ts, replace the two assertions that currently check the header contains both language codes joined by a pipe separator (lines 17 and 26) with assertions that the header's `.language-switcher` contains exactly one `.switcher-link`, no `.switcher-separator`, and that on the French page the single link's accessible name contains "EN" (and "FR" on the English page via the `/en/` case), plus that the link renders one inline `<svg>`. Keep the `locale content` block's cross-locale header-innerText-differs assertion (lines 38-39) — it stays valid. In homepage.spec.ts, replace the single two-code separator text assertion (line 96) with the same one-`.switcher-link`/no-separator/accessible-name-contains-"EN" check on `[data-role="site-header"]`. Do NOT modify i18n.spec.ts's `switcher` describe block (lines 43-85) or legal.spec.ts at all — those exercise click behavior via `getByRole('link', { name: 'EN' | 'FR' })` and are the regression guard that the accessible-name substring survives (Pitfall 3/4). These rewritten assertions are expected to FAIL RED now because the switcher still renders two links + a separator — do not weaken them. (When writing the new assertions, express the retired two-links-with-pipe format by asserting `.switcher-link` count === 1 and `.switcher-separator` count === 0, rather than by embedding the old literal separator string.)
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/i18n.spec.ts tests/e2e/homepage.spec.ts --project=chromium 2>&1 | grep -qE "failed|error" && echo "RED as expected" || (echo "ERROR: rewritten switcher assertions should be RED before implementation" && exit 1)</automated>
  </verify>
  <done>The rewritten one-link/no-separator switcher assertions are RED against the current two-link switcher; the i18n switcher click block and legal.spec.ts are untouched.</done>
  <acceptance_criteria>
    i18n.spec.ts and homepage.spec.ts assert the one-link + globe switcher (one `.switcher-link`, zero `.switcher-separator`, accessible name contains the other language's code); the i18n.spec.ts `switcher` click/cookie block and legal.spec.ts are untouched; the suite is RED against the current two-link switcher.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Simplify LanguageSwitcher to one link + globe; clean up the dead separator override</name>
  <files>src/components/LanguageSwitcher.astro, src/components/HomeCarousel.astro</files>
  <read_first>
    - src/components/LanguageSwitcher.astro (full) — current two-link markup (lines 12-24), the `.switcher-link`/`.is-current`/`.switcher-separator` CSS (26-64), and the inline cookie-write `<script>` (66-90) that MUST stay unchanged (D-10).
    - src/lib/i18n-paths.ts — `getSwitcherHref(pathname, targetLocale)`, reused unchanged, now called once for the non-current locale only (D-08).
    - src/components/HomeCarousel.astro — the switcher-link color override rule (renamed to `.site-header` in Plan 02) that still references `.switcher-separator`; that selector half is now dead and must be removed.
    - 10-RESEARCH.md Code Examples (the concrete one-link rewrite + globe SVG: a 16×16 `viewBox="0 0 24 24"` `stroke="currentColor"` glyph = a circle + one vertical-axis ellipse + one horizontal meridian line) and 10-UI-SPEC.md Copywriting Contract (sr-only hint phrased in the CURRENT page's language: FR page → "Passer en anglais"; EN page → "Switch to French").
  </read_first>
  <action>
    Rewrite LanguageSwitcher.astro to render a single link (D-07/D-08). In frontmatter, compute `currentLocale`, `targetLocale` (the opposite), `targetHref = getSwitcherHref(Astro.url.pathname, targetLocale)` (called once), `targetLabel` ("EN" when target is en, "FR" when target is fr), and `switchHint` phrased in the current page's language ("Passer en anglais" on FR pages, "Switch to French" on EN pages — mirroring the `instagramNewTabHint` current-language precedent, D-11/Pitfall 3). Render one `<nav class="language-switcher" aria-label="Language switcher">` containing one `<a class="switcher-link" data-locale={targetLocale} href={targetHref}>` whose children in order are: the inline globe `<svg aria-hidden="true">` (dependency-free, `currentColor`, per D-09 — a recognizable globe glyph as in 10-RESEARCH.md Code Examples), then the visible `{targetLabel}` text node, then a `<span class="sr-only">` carrying `{switchHint}`. CRITICAL (Pitfall 3): only the globe `<svg>` is `aria-hidden`; the visible EN/FR text node must NOT be hidden, so the computed accessible name still contains the code and `getByRole('link', { name: 'EN' | 'FR' })` keeps resolving in i18n.spec.ts and legal.spec.ts. Drop the `.is-current` and `.switcher-separator` CSS rules (no element uses them anymore) and add `gap: var(--space-xs)` to `.switcher-link` (now `display:inline-flex; align-items:center`) to space the globe from the label (10-UI-SPEC Spacing exception); keep the `min-height:44px`, padding, `color: var(--color-accent)`, and hover/focus-visible rules. Leave the `<script>` block byte-for-byte unchanged (D-10) — its `querySelectorAll('.language-switcher .switcher-link')` already works with a single link. Finally, in HomeCarousel.astro, remove the now-dead `.switcher-separator` selector from the `[data-display-mode] .site-header :global(...)` color-override rules (the separator element no longer exists), leaving the `.switcher-link` half intact.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/i18n.spec.ts tests/e2e/homepage.spec.ts tests/e2e/legal.spec.ts --project=chromium</automated>
    <automated>npm run test:e2e && npm run test:unit</automated>
    <human-check>View the switcher on at least one page in both locales and confirm the globe glyph is legible/recognizable at its rendered ~16px size next to the EN/FR label (10-VALIDATION.md Manual-Only Verifications); optionally spot-check with a screen reader that the FR-page link announces "EN Passer en anglais" and the EN-page link "FR Switch to French".</human-check>
  </verify>
  <done>The switcher is one link + globe on every page; i18n.spec.ts, homepage.spec.ts, and legal.spec.ts are GREEN; full suite passes; build succeeds.</done>
  <acceptance_criteria>
    LanguageSwitcher renders exactly one link (other language) with a leading aria-hidden globe SVG + visible code + sr-only current-language hint; the current-language link and separator are gone; the cookie/navigation `<script>` is unchanged (D-10); i18n.spec.ts, homepage.spec.ts, and legal.spec.ts are all GREEN (accessible-name substring preserved — Pitfall 3/4); the full `npm run test:e2e && npm run test:unit` suite passes; `npm run build` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser click → cookie write → navigation | The switcher's client `<script>` writes the `ajs_locale` cookie on click; this boundary is unchanged by this plan (D-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | `ajs_locale` cookie write (LanguageSwitcher `<script>`) | low | accept | The cookie-write logic is preserved byte-for-byte (D-10) — still `SameSite=Lax; Secure` and scoped to the deployed base path (WR-02); no new logic, no new value source. Only the rendered DOM (one link instead of two) changes. |
| T-10-06 | Information Disclosure | globe SVG + switcher href | low | accept | `targetHref` comes from the already unit-tested `getSwitcherHref` (no new URL builder — Don't Hand-Roll); the globe SVG is a static inline glyph with no external fetch. No secret or user input reaches the DOM. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (10-RESEARCH.md Package Legitimacy Audit: zero new dependencies; the globe icon is hand-authored inline SVG, D-09). |

No high-severity threats (security_block_on=high) — nothing blocks.
</threat_model>

<artifacts>
## Artifacts this plan produces
- **Modified:** `src/components/LanguageSwitcher.astro` — one `.switcher-link` (other language) with an inline aria-hidden globe `<svg>`, visible EN/FR label, and an sr-only current-language hint.
- **New consts in LanguageSwitcher.astro:** `targetLocale`, `targetHref`, `targetLabel`, `switchHint`.
- **Retired symbols:** the current-language `.switcher-link.is-current`, the `.switcher-separator` element + CSS, and the `.switcher-separator` half of HomeCarousel's color-override selector.
- **Unchanged (D-10):** the `.language-switcher .switcher-link` cookie-write `<script>` and `getSwitcherHref` (now called once).
- **Rewritten tests:** switcher-content assertions in `tests/e2e/i18n.spec.ts` + `tests/e2e/homepage.spec.ts` (click/cookie tests and `legal.spec.ts` left unchanged).
</artifacts>

<verification>
- `npx playwright test tests/e2e/i18n.spec.ts tests/e2e/homepage.spec.ts tests/e2e/legal.spec.ts --project=chromium` — GREEN (including the untouched legal.spec.ts switcher regression, Pitfall 4).
- `npm run test:e2e && npm run test:unit` — full suite GREEN.
- `npm run build` — succeeds.
- Live: globe legible at ~16px next to the EN/FR label in both locales.
</verification>

<success_criteria>
- The switcher shows only the other-language link + globe icon, no separator, on every page site-wide (I18N-04 success criterion #4).
- Clicking it navigates to the translated version of the current page and sets `ajs_locale` exactly as before (#5, D-10).
- The accessible name still contains EN/FR, so `getByRole` locators in i18n.spec.ts and legal.spec.ts pass with no change to legal.spec.ts (D-11, Pitfall 3/4).
</success_criteria>

<output>
Create `.planning/phases/10-unified-header-simplified-language-switcher/10-03-SUMMARY.md` when done.
</output>
