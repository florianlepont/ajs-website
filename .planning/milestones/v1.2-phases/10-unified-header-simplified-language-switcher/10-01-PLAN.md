---
phase: 10-unified-header-simplified-language-switcher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SiteHeader.astro
  - src/layouts/BaseLayout.astro
  - tests/e2e/site-header.spec.ts
autonomous: true
requirements: [HOME-10]
must_haves:
  truths:
    - "Visiting /about/, /contact/, and any /galleries/[slug]/ page shows an Instagram icon link in the header nav (previously homepage-only) — D-03."
    - "The About/Contact/gallery-detail header renders from the shared SiteHeader component, not BaseLayout's inline <header> markup — D-01."
    - "At a 393px viewport, the About and Contact headers fit on one row with no horizontal page overflow now that Instagram is present — Pitfall 1."
  artifacts:
    - src/components/SiteHeader.astro
    - tests/e2e/site-header.spec.ts
  key_links:
    - "BaseLayout renders <SiteHeader variant={headerVariant}> behind its existing headerVariant !== 'none' conditional (Open Question 2 — 2-value variant union)."
    - ".chrome-band header padding still reaches the SiteHeader-rendered <header> after the header leaves BaseLayout's component scope."
---

<objective>
Extract BaseLayout's inline `<header>` into a new standalone `<SiteHeader>` component (D-01), move the Instagram nav link into it so it renders on every non-homepage page (D-03), and port the homepage's live-tested mobile header CSS onto it so About/Contact/gallery-detail don't overflow at phone widths (Pitfall 1). This is HOME-10 part 1: the shared component exists and About/Contact/gallery-detail consume it. The homepage is wired in Plan 02.

Purpose: Establish the single canonical header implementation that both call sites (BaseLayout here, HomeCarousel in Plan 02) will render from — the whole point of HOME-10.
Output: `src/components/SiteHeader.astro` (new), a rewired `BaseLayout.astro`, and `tests/e2e/site-header.spec.ts` proving the shared-component contract for About/Contact/gallery-detail.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-CONTEXT.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-RESEARCH.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-PATTERNS.md
@.planning/phases/10-unified-header-simplified-language-switcher/10-UI-SPEC.md
@src/layouts/BaseLayout.astro
@src/components/HomeCarousel.astro
@src/components/LanguageSwitcher.astro
</context>

<phase_goal>
**As a** bilingual visitor, **I want to** find the same clean header — logo, nav, an Instagram link, and a single language control — in the same place on every page, **so that** the site feels coherent page to page instead of showing a different header on the homepage than on About/Contact.
</phase_goal>

<mvp_slice_note>
This plan's user-facing slice: after it ships, a visitor on About, Contact, or any gallery-detail page can reach Romane's Instagram directly from the header (they could not before — Instagram was homepage-only). That change is real and end-to-end (markup + CSS + passing e2e), not scaffolding. The homepage's own header is unified in Plan 02.
</mvp_slice_note>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing e2e contract for the shared header on About/Contact/gallery-detail</name>
  <files>tests/e2e/site-header.spec.ts</files>
  <read_first>
    - tests/e2e/homepage.spec.ts lines 394-448 — the existing homepage Instagram-in-header + 393px scrollWidth mobile-fit pattern to mirror for About/Contact.
    - tests/e2e/social-links.spec.ts — confirms the existing "Instagram absent from footer" contract stays true (Instagram goes into the header NAV, not the footer); the in-content About/Contact Instagram mentions are scoped to `main`/`.contact-page__social`, so a header Instagram link does not collide with them.
    - 10-VALIDATION.md "Wave 0 Requirements" and Per-Task Verification Map (task 10-03-01) — the mobile-fit and shared-component assertions this file must contain.
  </read_first>
  <behavior>
    - On /about/ and /en/about/ (and likewise /contact/): the page header (`[data-role="site-header"]`) contains exactly one Instagram link to https://www.instagram.com/ajs_romanelepont/ with target="_blank" and rel containing noopener + noreferrer.
    - That header Instagram link exposes an accessible name containing "Instagram" (role-based query scoped to the header), and renders an inline `<svg>` (not visible text).
    - At a 393px viewport, loading /about/ and /contact/ produces no horizontal page overflow: `document.documentElement.scrollWidth <= window.innerWidth`.
    - The header nav on /about/ exposes About + Contact + Instagram links (3 `.nav-link` children in the `.site-nav`), in that DOM order.
  </behavior>
  <action>
    Create tests/e2e/site-header.spec.ts as the Wave 0 RED contract for the shared header on non-homepage pages (HOME-10, D-01/D-03). Mirror the assertion style of homepage.spec.ts's "Instagram nav link (HOME-04)" describe block, but scope every locator to `[data-role="site-header"]` / `.site-nav` and drive it against /about/, /en/about/, /contact/, and /en/contact/. Include: (a) exactly-one-Instagram-link-in-header with correct href/target/rel; (b) header Instagram link has an accessible name containing "Instagram" and renders an `<svg>`; (c) a 393px-viewport scrollWidth-vs-innerWidth non-overflow check on /about/ and /contact/ (mirror homepage.spec.ts lines 434-448); (d) the `.site-nav` on /about/ contains About, Contact, and Instagram links in that order. Do NOT assert anything about the homepage here (the homepage header is not wired until Plan 02) and do NOT add a `/` vs `/about/` cross-page-identity assertion yet (that lands in Plan 02, where the homepage renders `.nav-link`). These assertions are expected to FAIL now because BaseLayout's current header has no Instagram link and no `data-role` attribute — do not weaken them to pass early.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/site-header.spec.ts --project=chromium 2>&1 | grep -qE "failed|error" && echo "RED as expected" || (echo "ERROR: Wave 0 test should be RED before implementation" && exit 1)</automated>
  </verify>
  <done>tests/e2e/site-header.spec.ts exists and fails RED against the current (un-refactored) BaseLayout.</done>
  <acceptance_criteria>
    tests/e2e/site-header.spec.ts exists, is syntactically valid, and fails RED against the current (un-refactored) BaseLayout because the About/Contact header has no Instagram link and no `[data-role="site-header"]` attribute.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create SiteHeader.astro and rewire BaseLayout to render it</name>
  <files>src/components/SiteHeader.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro lines 181-194 (inline header markup to extract), lines 334-495 (scoped `<style>` block holding `.chrome-band`, `.site-header*`, `.logo-mark*`, `.site-nav`, `.nav-link` CSS), lines 208-332 (`<style is:global>` block that already holds `:root` tokens + `.sr-only`), and lines 106-126 (homeHref/aboutLabel/aboutHref/contactLabel/contactHref/logoBlackSrc/logoWhiteSrc consts + siteCopy).
    - src/components/HomeCarousel.astro lines 65-72 (`instagramNewTabHint` const) and lines 112-139 (the exact Instagram nav-link markup + inline camera SVG to move in verbatim per D-03).
    - 10-PATTERNS.md "src/components/SiteHeader.astro (NEW component)" — the Props contract, the core markup pattern, the `is:global` scoping requirement, and the mobile `@media` block to port.
    - 10-RESEARCH.md Pattern 3 (`is:global` scoping) and Open Question 2 (variant prop is the 2-value union `'solid' | 'transparent'`, NOT 3-value; `'none'` suppression stays in BaseLayout).
  </read_first>
  <action>
    Create src/components/SiteHeader.astro as the single canonical header (D-01). Give it a `Props` interface with `variant: 'solid' | 'transparent'` (2-value union per Open Question 2 — `'none'` is NOT a SiteHeader concern) plus `siteTitle`, `homeHref`, `aboutLabel`, `aboutHref`, `contactLabel`, `contactHref`, `instagramUrl`, `instagramLabel`, `instagramNewTabHint`, `logoBlackSrc`, `logoWhiteSrc` — every href/label is passed in by the caller (both call sites already compute these identically today), so SiteHeader itself only imports `LanguageSwitcher` from './LanguageSwitcher.astro'. Render `<header class:list={['chrome-band','site-header',\`site-header--${variant}\`]} data-role="site-header">` containing, in order: the `.logo-mark` anchor (chip + default/hover `<img>` pair, copied verbatim from BaseLayout lines 183-187), a `.site-nav` with `.nav-link` About + Contact anchors, then the Instagram `.nav-link` anchor with its inline camera `<svg>` and `.sr-only` `instagramNewTabHint` span moved verbatim from HomeCarousel lines 112-139 (D-03 — this is why it now renders on every page), then `<slot name="extra" />` (D-04 — reserved for the homepage mode-toggle in Plan 02; empty everywhere else), then `<LanguageSwitcher />`. Move the header CSS into SiteHeader's own `<style is:global>` block (Pattern 3 — NOT Astro's default scoped `<style>`): copy the `.site-header`, `.site-header--solid`, `.site-header--transparent`, `.site-header--transparent .nav-link`, the full `.logo-mark*` hover-crossfade block (BaseLayout lines 399-476), `.site-nav`, and `.nav-link` rules verbatim from BaseLayout. Also PORT a mobile block — translate HomeCarousel's `@media (max-width: 767px)` header trims (lines 1547-1571: `flex-wrap: wrap` + `gap: var(--space-xs)` on the header, `margin-left: 0` + `gap: var(--space-xs)` on the nav, trimmed `.nav-link` horizontal padding) onto the unified class names, because BaseLayout's header has zero mobile CSS today and About/Contact now carry a 3rd nav item (Pitfall 1/2). SCOPING GOTCHA: the `<header>` now renders from SiteHeader, so it leaves BaseLayout's scoped-`<style>` scope — relocate the `.chrome-band` base padding rule AND its `@media (min-width: 768px)` variant (BaseLayout lines 337-345) into an `is:global` scope (put them in SiteHeader's `is:global` block, or BaseLayout's existing `is:global` block) so the header keeps its padding; the footer's own `footer.chrome-band` border-top rule stays scoped in BaseLayout. Then rewire BaseLayout.astro: replace the inline `<header>...</header>` (lines 181-194) with `{headerVariant !== 'none' && (<SiteHeader variant={headerVariant} .../>)}` passing the existing consts (add one new const `instagramNewTabHint = locale === 'en' ? ' (opens in new tab)' : ' (nouvelle fenêtre)'` matching HomeCarousel line 69, and pass `siteCopy.instagramUrl` / `siteCopy.instagramLabel`); import SiteHeader; delete the now-extracted header CSS rules from BaseLayout's scoped `<style>` (`.site-header*`, `.logo-mark*`, `.site-nav`, `.nav-link`), keeping `:root`, `body`/`a`/`.sr-only`, footer, and `main` CSS. Do NOT change `headerVariant`'s own 3-value prop type on BaseLayout — it keeps `'solid' | 'transparent' | 'none'` and its `!== 'none'` conditional.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/site-header.spec.ts tests/e2e/about.spec.ts tests/e2e/contact.spec.ts tests/e2e/gallery.spec.ts tests/e2e/social-links.spec.ts --project=chromium</automated>
    <human-check>At 373px, 393px, and 428px viewports, load /about/ and /contact/ in both locales and confirm the header (logo, About, Contact, Instagram, switcher) reads cleanly on one row without cramping or a wrapped second row — an automated scrollWidth check catches hard overflow but not awkward wrapping (10-VALIDATION.md Manual-Only Verifications).</human-check>
  </verify>
  <done>site-header.spec.ts is GREEN and About/Contact/gallery-detail render <SiteHeader> with the Instagram link and no 393px overflow; existing suite unaffected; build succeeds.</done>
  <acceptance_criteria>
    site-header.spec.ts is GREEN; about.spec.ts, contact.spec.ts, gallery.spec.ts, and social-links.spec.ts all stay GREEN (Instagram is in the header nav, still absent from the footer, and the in-content About/Contact Instagram mentions are unaffected). About/Contact/gallery-detail render `<SiteHeader>` with the Instagram link and no 393px overflow. `npm run build` succeeds.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time render → static HTML → browser | All header hrefs/labels are build-time values from Sanity `siteSettings`/`site-config.ts`; no runtime server, no user-controlled input crosses into the header. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | SiteHeader.astro (build-time render) | low | accept | SiteHeader is a pure static-render `.astro` component; it never imports `src/lib/sanity.ts` or any token, so no secret (e.g. `SANITY_API_READ_TOKEN`) reaches the client — same guarantee HomeCarousel already relies on (T-04.1-04-ID). Nav labels/hrefs are already build-time-computed and trusted. |
| T-10-02 | Tampering | Instagram nav link (D-03) | low | accept | The Instagram anchor keeps `rel="noopener noreferrer"` + `target="_blank"` verbatim from its Phase 7 origin; no reverse-tabnabbing surface is introduced. |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (10-RESEARCH.md Package Legitimacy Audit: zero new dependencies — pure internal component extraction). |

No high-severity threats (security_block_on=high) — nothing blocks.
</threat_model>

<artifacts>
## Artifacts this plan produces
- **New file:** `src/components/SiteHeader.astro`
- **New component:** `<SiteHeader>` with `Props { variant: 'solid' | 'transparent'; siteTitle; homeHref; aboutLabel; aboutHref; contactLabel; contactHref; instagramUrl; instagramLabel; instagramNewTabHint; logoBlackSrc; logoWhiteSrc }`
- **New named slot:** `slot="extra"` on `<SiteHeader>` (empty in this plan; consumed by the homepage toggle in Plan 02)
- **New attribute:** `[data-role="site-header"]` now on every non-homepage header
- **New const in BaseLayout.astro:** `instagramNewTabHint`
- **New file:** `tests/e2e/site-header.spec.ts`
- **Behavior change:** Instagram nav link now renders on /about/, /contact/, and /galleries/[slug]/ (all locales)
- **CSS relocation:** `.chrome-band` header padding moved to `is:global` scope so the SiteHeader-rendered header keeps its padding
</artifacts>

<verification>
- `npx playwright test tests/e2e/site-header.spec.ts tests/e2e/about.spec.ts tests/e2e/contact.spec.ts tests/e2e/gallery.spec.ts tests/e2e/social-links.spec.ts --project=chromium` — all GREEN.
- `npm run build` — succeeds (SiteHeader renders at build time under Astro static output).
- Live: About/Contact headers read cleanly on one row at 373–428px in both locales.
</verification>

<success_criteria>
- `src/components/SiteHeader.astro` exists and is the single header implementation rendered by BaseLayout for every non-homepage page.
- About/Contact/gallery-detail headers now include the Instagram link (D-03) and fit at 393px with no overflow (Pitfall 1).
- BaseLayout no longer contains inline header markup or duplicated header CSS; `headerVariant` prop and `!== 'none'` conditional unchanged.
- Full existing suite unaffected by the extraction (no regressions on about/contact/gallery/social-links).
</success_criteria>

<output>
Create `.planning/phases/10-unified-header-simplified-language-switcher/10-01-SUMMARY.md` when done.
</output>
