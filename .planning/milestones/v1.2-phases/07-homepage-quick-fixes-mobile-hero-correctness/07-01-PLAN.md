---
phase: 07-homepage-quick-fixes-mobile-hero-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [HOME-04, HOME-05]
must_haves:
  truths:
    - "Visitor sees an Instagram icon (not text) in the homepage header nav that opens https://www.instagram.com/ajs_romanelepont/ in a new tab (D-01/D-02/D-05, HOME-04)"
    - "The carousel/grid mode-toggle button's visible border traces a square (equal width and height) in BOTH carousel and grid modes (D-06/D-09, HOME-05)"
    - "The toggle's clickable/tappable area stays at or above 44px in both dimensions even though its visible square box is smaller (D-08)"
    - "At a 393px mobile viewport the header (logo, About, Contact, Instagram, toggle, FR|EN) fits without horizontal page overflow (D-03)"
  artifacts:
    - "src/components/HomeCarousel.astro: a new Instagram `<a class=\"home-nav-link\">` with an inline SVG glyph, placed after the Contact link inside `.home-nav`"
    - "src/components/HomeCarousel.astro: a new frontmatter const `instagramNewTabHint` (locale-conditional)"
    - "src/components/HomeCarousel.astro: a new `.home-toggle__box` element + CSS with explicit 28px square dimensions and the relocated 1.5px border"
    - "tests/e2e/homepage.spec.ts: assertions for the Instagram nav link and the square toggle box in both modes"
  key_links:
    - "Instagram `<a>` carries `target=\"_blank\"` + `rel=\"noopener noreferrer\"` (reverse-tabnabbing mitigation, D-05)"
    - "`.home-toggle` retains `min-width`/`min-height: var(--tap-target-min)` (44px hit box) while `.home-toggle__box` is the smaller visible square (D-08)"
    - "The hover-invert background moves to `.home-toggle__box`, NOT the invisible 44px hit box (D-06.3)"
---

<objective>
Deliver two small, contained homepage header fixes in `src/components/HomeCarousel.astro`:

- **HOME-04 (D-01–D-05):** add a recognizable Instagram icon link to the homepage header nav.
- **HOME-05 (D-06–D-09):** make the carousel/grid mode-toggle button's visible border trace a genuine square (currently its box is wider than tall because it has no explicit dimensions), while keeping the 44px tap-target floor.

Purpose: These are the low-risk, homepage-only header corrections that must land first (Phase 7's ordering rationale) so their groundwork carries cleanly into Phase 10's later header consolidation.
Output: Modified `HomeCarousel.astro` (markup + scoped CSS) and new e2e assertions in `tests/e2e/homepage.spec.ts`.

## Phase Goal (user story)

**As a** homepage visitor, **I want to** reach Romane's Instagram from a recognizable icon in the header and see a cleanly square mode-toggle button, **so that** the header controls read as polished and intentional and I can follow her work.

> Note (MVP-mode judgement): Phase 7 is a fix/polish phase, not a new end-to-end feature. Per the planning brief, each of its fixes is treated as its own complete vertical slice — markup + CSS + its own test together in one task — rather than forced into an artificial test/impl/data horizontal split. The user story above is synthesised from the ROADMAP success criteria for HOME-04/HOME-05 (both already visitor-framed); no capability was invented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-homepage-quick-fixes-mobile-hero-correctness/07-CONTEXT.md
@.planning/phases/07-homepage-quick-fixes-mobile-hero-correctness/07-PATTERNS.md
@.planning/phases/07-homepage-quick-fixes-mobile-hero-correctness/07-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instagram icon link in the homepage header nav (HOME-04, D-01–D-05)</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <read_first>
    - src/components/HomeCarousel.astro — frontmatter consts (lines ~33–69), the `.home-header`/`.home-nav` markup (lines ~80–109), the shared `.home-nav-link, .home-toggle` CSS rule (lines ~710–728), and the mobile header media query with its live pixel-budget comment (lines ~1232–1253). This is the file being modified.
    - src/layouts/BaseLayout.astro — reference only: the footer Instagram `<a>` (lines ~143–150) and the `instagramNewTabHint` const (line 76). Copy its `href`/`target`/`rel`/sr-only-hint semantics verbatim (D-05).
    - .planning/phases/04.2-social-media-links/04.2-01-SUMMARY.md — the original Instagram footer link implementation this reuses (D-05 source).
  </read_first>
  <behavior>
    - Test 1 (desktop, FR `/`): the homepage `header` contains exactly one link whose `href` is `https://www.instagram.com/ajs_romanelepont/`, with `target="_blank"` and `rel` containing both `noopener` and `noreferrer`.
    - Test 2: that Instagram link contains an inline `<svg>` element (icon, not visible text), and has an accessible name of "Instagram" (via `aria-label` or sr-only text).
    - Test 3 (DOM order): inside `.home-nav`, the Instagram link comes AFTER the Contact link (D-02 nav order: About — Contact — Instagram).
    - Test 4 (mobile, 393px viewport): the Instagram link is visible AND the document body has no horizontal overflow (`document.documentElement.scrollWidth <= window.innerWidth`), proving the 5th header item fits without pushing the page wider than the viewport (D-03).
    - Test 5 (EN `/en/`): the sr-only new-tab hint reads " (opens in new tab)" on EN and " (nouvelle fenêtre)" on FR (locale-conditional reuse, not re-derived).
  </behavior>
  <action>
    Add a locale-conditional `instagramNewTabHint` const to `HomeCarousel.astro`'s frontmatter (same ternary and same two exact strings as `BaseLayout.astro:76` — " (opens in new tab)" for EN, " (nouvelle fenêtre)" for FR); this component does not import from BaseLayout, so the const must be declared locally (D-05).

    Insert a new `<a class="home-nav-link">` inside `.home-nav`, positioned AFTER the existing Contact link and before `</nav>` (D-02: Logo — About — Contact — Instagram — [toggle] — FR|EN). Give it `href="https://www.instagram.com/ajs_romanelepont/"`, `target="_blank"`, `rel="noopener noreferrer"`, and `aria-label="Instagram"`. Its content is a hand-authored, dependency-free inline `<svg>` Instagram glyph (rounded-square camera outline) using `currentColor` for stroke/fill, sized to roughly a 20x20 footprint (Instagram's mark is conventionally square — do not stretch it), following the existing "no icon library" convention exemplified by the toggle's `.home-toggle__morph` cells (D-01). Append an sr-only hint node carrying `instagramNewTabHint` so screen readers announce the new-tab behaviour (D-05). Exact SVG path data is Claude's Discretion — any standard, recognisable Instagram rendering is acceptable; verify legibility at header icon size.

    Reuse the existing `.home-nav-link` class as-is for the link (`display:inline-flex; align-items:center; font-size:14px; padding: var(--space-xs) var(--space-sm); min-height: var(--tap-target-min); color:inherit; text-decoration:none`) — do NOT add a bordered box around the icon (D-04); the glyph is expected to read as clickable on its own. Ensure the sr-only hint uses the site's existing `.sr-only` utility (as the footer link does) so no visible text appears next to the icon.

    Mobile fit (D-03): adding a 5th header item is projected to overflow the 393px row by ~29px per the UI-SPEC. Re-measure live at 393px (same iterative methodology documented in the `HomeCarousel.astro:1232–1253` pixel-budget comment) and close the gap by trimming `.home-header`'s gap and/or `.home-nav`'s internal gap and/or the Instagram glyph's own footprint — leave the toggle and switcher spacing untouched, since they are unrelated to this addition. Update the pixel-budget comment to reflect the new 5-item measurement. The pre-existing `flex-wrap: wrap` on `.home-header` stays as a safety net but a single-row fit is the intended outcome; do not rely on wrapping as the primary solution.

    Add the Playwright assertions described in the behavior block to `tests/e2e/homepage.spec.ts` (new `test.describe('Instagram nav link (HOME-04)')`). Do not weaken or delete any existing homepage test.
  </action>
  <verify>
    <automated>npm run test:e2e -- homepage.spec.ts -g "Instagram"</automated>
  </verify>
  <acceptance_criteria>
    - `HomeCarousel.astro` renders an `<a href="https://www.instagram.com/ajs_romanelepont/" target="_blank" rel="noopener noreferrer">` inside `.home-nav`, after the Contact link.
    - The link's content is an inline `<svg>` (no visible text label) and its accessible name is "Instagram".
    - The sr-only new-tab hint is " (opens in new tab)" at `/en/` and " (nouvelle fenêtre)" at `/`.
    - At 393px viewport width, `document.documentElement.scrollWidth <= window.innerWidth` (no horizontal page overflow) and the Instagram link is visible.
    - `npm run test:e2e -- homepage.spec.ts` passes with the new tests and all pre-existing homepage tests still green.
  </acceptance_criteria>
  <done>The homepage header nav shows a recognizable Instagram icon (FR and EN) linking to Romane's profile in a new tab with reverse-tabnabbing protection, fitting the mobile header row without page overflow, and the new e2e assertions plus all prior homepage tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Square the mode-toggle button box while preserving the 44px tap target (HOME-05, D-06–D-09)</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <read_first>
    - src/components/HomeCarousel.astro — the `.home-toggle` button markup (lines ~90–107), the shared `.home-nav-link, .home-toggle` rule (lines ~710–728), the `.home-toggle` border/padding + hover rules (lines ~740–763), the `.home-toggle:not(.home-toggle--used)` attention-pulse keyframes (lines ~777–797), and the `.home-toggle__morph`/`.home-toggle__morph-cell` icon rules (lines ~810–841). This is the file being modified.
    - src/components/LanguageSwitcher.astro — reference only: the `.switcher-link` tap-target pattern (lines ~36–44), the exact template for the "smaller visible box, 44px minimum tappable area" split (D-08).
    - src/layouts/BaseLayout.astro — reference only: `--tap-target-min` (line 217) and `--radius-none` (line 212) tokens.
  </read_first>
  <behavior>
    - Test 1 (carousel mode, desktop): the toggle's inner `.home-toggle__box` has an equal computed width and height (a square, ~28px), i.e. `boundingBox.width === boundingBox.height` within a 1px tolerance.
    - Test 2 (carousel mode): the `.home-toggle` button's own bounding box measures at least 44px in BOTH width and height (tap-target floor preserved, D-08).
    - Test 3 (grid mode): after toggling to grid, `.home-toggle__box` is still a square with the same equal-dimension property (D-09 — must hold in both modes).
    - Test 4: the visible border lives on `.home-toggle__box` (computed `border-width` > 0 on the box), and the existing `[data-role="mode-toggle"]` count stays exactly 1 (no new button introduced) — existing HOME-01 single-toggle contract must not regress.
  </behavior>
  <action>
    Root cause (D-06): `.home-toggle` has no explicit `width`/`height`, so its bounding box is whatever the 20x14 morph icon plus `padding: var(--space-xs)` plus the 1.5px border produce — wider than tall. This is a box-dimension bug, not a corner-radius bug (no `border-radius` is set; corners already read square per `--radius-none`). Do NOT touch the morph icon's own `width:20px; height:14px` or its fixed `row-gap:2px` / animated `column-gap` — those proportions are intentional (D-07).

    Restructure into two nested boxes:
    1. Wrap the existing `<span class="home-toggle__morph">…</span>` in a new inner element `<span class="home-toggle__box" aria-hidden="true">` placed directly inside the `<button class="home-toggle">`. `.home-toggle__box` is the VISIBLE square: give it explicit `width: 28px; height: 28px`, `display:flex; align-items:center; justify-content:center`, and MOVE the border here as `border: 1.5px solid currentColor` (identical value, relocated from `.home-toggle`). 28px is the D-08 tight midpoint (a 4px-grid multiple, snug around the 20x14 icon); if live verification shows 28px reads too cramped against the header's 44px-tall items, step up to 32px (Claude's Discretion within D-08's 28–32px range).
    2. Make the outer `.home-toggle` `<button>` the INVISIBLE, tappable box: remove its `border` and its `padding: var(--space-xs)` shorthand, and add explicit `min-width: var(--tap-target-min)` alongside the `min-height: var(--tap-target-min)` it already inherits, keeping `display:inline-flex; align-items:center; justify-content:center` so the visible square centers within the 44px hit area (D-08).
    3. Move (do not duplicate) the hover/focus invert: the rule currently at `.home-toggle:hover, .home-toggle:focus-visible { background-color: currentColor; }` must retarget `.home-toggle:hover .home-toggle__box, .home-toggle:focus-visible .home-toggle__box` — painting the full invisible 44px hit box on hover would visually contradict the compact-square goal (D-06.3).
    4. Leave the morph-cell colour-invert rules (`.home-toggle:hover .home-toggle__morph-cell` and the grid-mode variant) as-is — the descendant combinator still resolves correctly through the new intermediate `.home-toggle__box` wrapper.
    5. Retarget the attention-pulse: the `.home-toggle:not(.home-toggle--used)` keyframe animation currently scales `.home-toggle` itself; move the `transform: scale(...)` animation to target `.home-toggle__box` so the visible square pulses rather than the invisible hit box (low-risk consistency cleanup per the UI-SPEC).

    Verify in both `data-display-mode="carousel"` and `data-display-mode="grid"` — each has its own `.home-toggle`/morph-cell colour treatment; the square border and hover-invert must render correctly in both (D-09).

    Add the Playwright assertions described in the behavior block to `tests/e2e/homepage.spec.ts` (new `test.describe('square mode-toggle box (HOME-05)')`), measuring `.home-toggle__box` and `.home-toggle` bounding boxes in both modes. Do not weaken or delete any existing homepage test — in particular keep the existing `.home-toggle__btn` count === 0 and `[data-role="mode-toggle"]` count === 1 assertions valid (the new class is `.home-toggle__box`, distinct from the superseded `.home-toggle__btn`).
  </action>
  <verify>
    <automated>npm run test:e2e -- homepage.spec.ts -g "square mode-toggle|single unified mode toggle"</automated>
  </verify>
  <acceptance_criteria>
    - `.home-toggle__box` exists inside the `.home-toggle` button and computes to an equal width and height (~28px, ±1px) in both carousel and grid modes.
    - `.home-toggle` (the button) measures ≥44px in both width and height (tap-target floor via `min-width`/`min-height`).
    - The 1.5px `currentColor` border is on `.home-toggle__box`; hover/focus paints `.home-toggle__box`'s background (not the 44px hit box).
    - `.home-toggle__morph`'s own dimensions (20x14, row-gap 2px) are unchanged.
    - `[data-role="mode-toggle"]` count is still exactly 1 and `.home-toggle__btn` count is still 0.
    - `npm run test:e2e -- homepage.spec.ts` passes including the new square-box tests and all pre-existing toggle tests.
  </acceptance_criteria>
  <done>The mode-toggle button shows a compact square visible border in both carousel and grid modes, with a ≥44px invisible tappable area, the morph icon proportions untouched, and the single-toggle contract intact — verified by new and existing e2e tests.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-01)

New symbols / markup / CSS introduced by this plan:

- **`instagramNewTabHint`** — new locale-conditional frontmatter const in `src/components/HomeCarousel.astro` (mirrors `BaseLayout.astro:76`).
- **Instagram nav `<a class="home-nav-link">`** — new markup inside `.home-nav`, after the Contact link, carrying `href="https://www.instagram.com/ajs_romanelepont/"`, `target="_blank"`, `rel="noopener noreferrer"`, `aria-label="Instagram"`, an inline `<svg>` glyph, and an sr-only hint span.
- **Inline Instagram `<svg>` glyph markup** — hand-authored, dependency-free, `currentColor`-based (no new CSS class strictly required; reuses `.home-nav-link`).
- **`.home-toggle__box`** — new CSS class + new `<span class="home-toggle__box" aria-hidden="true">` wrapper element inside the `.home-toggle` button; the visible 28px square carrying the relocated `border: 1.5px solid currentColor`.
- **Modified CSS rules** — `.home-toggle` (border/padding removed; `min-width`/`min-height` added), hover/focus invert retargeted to `.home-toggle__box`, attention-pulse `transform` retargeted to `.home-toggle__box`.
- **New e2e assertions** — `test.describe('Instagram nav link (HOME-04)')` and `test.describe('square mode-toggle box (HOME-05)')` in `tests/e2e/homepage.spec.ts`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → external site (instagram.com) | The new Instagram link opens `instagram.com` in a new browser tab via `target="_blank"`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | Instagram `target="_blank"` link in `.home-nav` | medium | mitigate | Reverse-tabnabbing: the opened tab could otherwise access `window.opener` and redirect the origin tab. Mitigated by `rel="noopener noreferrer"` on the link (D-05, reusing the already-shipped footer link semantics). Verified by the HOME-04 e2e assertion checking `rel` contains `noopener` and `noreferrer`. |
| T-07-02 | Information Disclosure | Homepage static markup/CSS | low | accept | This phase adds only static, prerendered markup + scoped CSS and Playwright tests to `HomeCarousel.astro`. No auth, no user input, no server endpoint, no data handling is introduced. No new disclosure surface. |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs occur in this plan (edits to existing files + tests only). No supply-chain surface introduced. |

ASVS level 1; block-on threshold: high. No `high` or `critical` threats — this phase introduces static homepage CSS/markup plus one outbound link whose only real risk (reverse tabnabbing) is already mitigated by the decided `rel="noopener noreferrer"`.
</threat_model>

<verification>
- `npm run test:unit` passes (no regression in unit tests).
- `npm run test:e2e -- homepage.spec.ts` passes: new Instagram + square-toggle tests green, all pre-existing homepage tests (single toggle, grid hero, wordmark cutout, i18n guard, mobile hero, view-transition) still green.
- `npm run build` succeeds (static build produces no errors from the new markup).
- Manual visual confirmation (developer, non-blocking): Instagram icon reads as clickable among the plain-text nav links in both modes; if it does not, a bordered box is a documented follow-up per D-04 — do not pre-emptively add one.
</verification>

<success_criteria>
- HOME-04: a homepage-header Instagram icon link (not text) opens `https://www.instagram.com/ajs_romanelepont/` in a new tab with `rel="noopener noreferrer"`, in both FR and EN, fitting the mobile header row.
- HOME-05: the mode-toggle button's visible border is a square in both carousel and grid modes, with a ≥44px tappable area and the morph icon proportions preserved.
- Full e2e + unit suites green; static build succeeds.
</success_criteria>

<output>
Create `.planning/phases/07-homepage-quick-fixes-mobile-hero-correctness/07-01-SUMMARY.md` when done.
</output>
