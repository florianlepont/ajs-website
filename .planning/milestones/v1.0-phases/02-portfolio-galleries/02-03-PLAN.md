---
phase: 02-portfolio-galleries
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/GalleryGrid.astro
  - src/components/GalleryCard.astro
  - src/pages/galleries/index.astro
  - src/pages/en/galleries/index.astro
  - src/pages/galleries/[slug].astro
  - src/pages/en/galleries/[slug].astro
autonomous: false
requirements: [PORT-01, PORT-03]
user_setup: []

must_haves:
  truths:
    - "A visitor can open /galleries/ (and /en/galleries/) and see a grid of gallery cover thumbnails with titles in Wild Strawberry panels, in Romane's manual order"
    - "A visitor can click a gallery card and land on its detail page showing the artist statement and a thumbnail grid of that gallery's images"
    - "The artist statement renders in French at /galleries/{slug} and English at /en/galleries/{slug}"
    - "Real migrated content for all known projects is live"
  artifacts:
    - path: "src/components/GalleryCard.astro"
      provides: "cover thumbnail + Wild Strawberry title panel link"
      min_lines: 25
    - path: "src/components/GalleryGrid.astro"
      provides: "responsive 1/3-col thumbnail grid"
    - path: "src/pages/galleries/index.astro"
      provides: "FR gallery listing"
    - path: "src/pages/galleries/[slug].astro"
      provides: "FR gallery detail via getStaticPaths"
      contains: "getStaticPaths"
  key_links:
    - from: "src/pages/galleries/index.astro"
      to: "getGalleries()"
      via: "build-time fetch feeding GalleryGrid"
      pattern: "getGalleries"
    - from: "src/pages/galleries/[slug].astro"
      to: "getStaticPaths + getGallery"
      via: "one static page per gallery slug"
      pattern: "getStaticPaths"
    - from: "GalleryCard.astro"
      to: "thumbnailUrl()"
      via: "cover image (images[0]) 1:1 crop"
      pattern: "thumbnailUrl"
---

<objective>
Build the visitor browsing slice: the `GalleryGrid`/`GalleryCard` components and the FR+EN listing and detail pages, then migrate the real content. After this plan a visitor can browse every migrated project by cover thumbnail, open any gallery, and read its bilingual artist statement — delivering PORT-01 and PORT-03 end-to-end (lightbox for full-size viewing follows in Plan 04).

Purpose: Turns the data contract (Plan 01) and visual identity (Plan 02) into the actual browsable portfolio with real content.
Output: two components, four pages, and a fully populated Sanity dataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-portfolio-galleries/02-CONTEXT.md
@.planning/phases/02-portfolio-galleries/02-UI-SPEC.md
@.planning/phases/02-portfolio-galleries/02-PATTERNS.md
@.planning/phases/02-portfolio-galleries/02-VALIDATION.md

<interfaces>
<!-- Contracts from Plans 01/02. Use directly — do not re-derive. -->

Read layer (src/lib/sanity.ts): `getGalleries(): Promise<Gallery[]>`, `getGallery(slug): Promise<Gallery | null>`. `Gallery = {title: string; slug: string; statement: LocaleString; images: GalleryImage[]}`. `images[0]` is always the cover (D-09).
Image builders (src/lib/image.ts): `thumbnailUrl(img, size=600)` (1:1 crop), `fullSizeUrl(img, maxWidth=2000)` (uncropped, used by Plan 04).
Layout: `BaseLayout.astro` (accepts `title` prop). New tokens available: `--color-dominant` Dawn Pink, `--color-secondary`, `--color-accent` Wild Strawberry, `--color-ink` Woodsmoke; `.sr-only` global utility; Display role = `clamp(2.5rem, 12cqi, 6.5rem)`/900 requiring a `container-type: inline-size` ancestor.
Switcher: `getSwitcherHref` in src/lib/i18n-paths.ts already handles shared gallery slugs (`/rebut` ↔ `/en/rebut`) unchanged — do NOT add new switcher logic (Pitfall 3).
Page pattern: FR page reads `.fr`, EN page reads `.en`, both call the same query fn, differ only in import depth (`../` vs `../../`) and locale key (Pitfall 2 — two files per route, NEVER a `[locale]` segment).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GalleryGrid and GalleryCard components</name>
  <files>src/components/GalleryGrid.astro, src/components/GalleryCard.astro</files>
  <read_first>
    - src/components/LanguageSwitcher.astro (component three-part shape: typed frontmatter + semantic template + scoped `<style>` using tokens)
    - src/layouts/BaseLayout.astro (new token names, `.sr-only`, focus-visible double-ring, 768px breakpoint precedent)
    - src/lib/sanity.ts (Gallery / GalleryImage types) and src/lib/image.ts (thumbnailUrl)
    - .planning/phases/02-portfolio-galleries/02-UI-SPEC.md (Layout Notes: grid, Wild Strawberry panel overlap, 1:1 crop, hover/focus; Copywriting Contract: sr-only CTA suffix)
  </read_first>
  <action>
    Create src/components/GalleryGrid.astro: a presentational wrapper with an `interface Props` declaring a default `<slot />` (it renders whatever cards/thumbnails are passed). Its scoped `<style>` defines a CSS grid: 1 column below 768px, 3 columns at/above 768px (the only breakpoint, matching BaseLayout), `gap: var(--space-xl)`, background `var(--color-dominant)`. This same grid is reused by both the listing page (cards) and the detail page (plain thumbnails), so keep it content-agnostic.
    Create src/components/GalleryCard.astro: `interface Props { gallery: Gallery }`. The whole card is a single `<a>` linking to the gallery detail route — compute the href with `getRelativeLocaleUrl(locale, `galleries/${gallery.slug}`)` (locale derived from `Astro.currentLocale`). Inside: an `<img>` of the cover (`gallery.images[0].image`) via `thumbnailUrl(...)` with explicit `width`/`height` (600×600) and `object-fit: cover` for the 1:1 crop, plus `alt={gallery.images[0].alt?.[locale] ?? ''}` (null-safe). Anchor a Wild Strawberry panel (`background: var(--color-accent)`, padding `var(--space-xl)` horizontal / `var(--space-lg)` vertical) to the bottom edge of the card, overlapping the lower portion of the cover photo (absolute-position over the image, not a block below it). The panel contains the gallery `title` at Display role (`clamp(2.5rem, 12cqi, 6.5rem)`/900, `color: var(--color-ink)`); the panel element MUST set `container-type: inline-size;` so the Display clamp resolves against the card panel width (renders small here vs. large on detail). Append an `.sr-only` span to the link's accessible name with the localized CTA suffix: FR " — Voir la galerie", EN " — View gallery" (e.g. accessible name reads "Rebut — View gallery"). Hover cue: `transform: scale(1.02)` on the cover image and/or `filter: brightness(1.05)` on the panel. Focus-visible on the whole card uses the double-ring (`outline: 2px solid var(--color-ink); outline-offset: 2px; box-shadow: 0 0 0 4px var(--color-accent);`). Use only `var(--*)` tokens, never hardcoded hex/px except the 600 image dimensions.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx astro check 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/GalleryCard.astro` declares `interface Props` with a `gallery: Gallery` field and imports `thumbnailUrl` from `../lib/image`.
    - GalleryCard renders exactly one `<a>` wrapping the cover image and title panel; the panel sets `container-type: inline-size` and uses `var(--color-accent)` fill with `var(--color-ink)` title text.
    - An `.sr-only` span with localized "Voir la galerie"/"View gallery" is present in the link.
    - `grep -c "var(--color-accent)" src/components/GalleryCard.astro` ≥ 1 and no raw `#` hex color appears in either component's styles.
    - GalleryGrid uses a `@media (min-width: 768px)` 3-column rule and `gap: var(--space-xl)`.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Reusable, token-driven grid and card components exist with the Wild Strawberry panel + Display title + accessible CTA suffix + double-ring focus.</done>
</task>

<task type="auto">
  <name>Task 2: FR + EN gallery listing and detail pages</name>
  <files>src/pages/galleries/index.astro, src/pages/en/galleries/index.astro, src/pages/galleries/[slug].astro, src/pages/en/galleries/[slug].astro</files>
  <read_first>
    - src/pages/index.astro and src/pages/en/index.astro (the FR/EN page pair pattern: import BaseLayout + query fn, read locale key, relative import depth)
    - src/pages/404.astro (getRelativeLocaleUrl usage for in-page links; multi-section body precedent)
    - src/lib/sanity.ts (getGalleries, getGallery, Gallery type)
    - .planning/phases/02-portfolio-galleries/02-UI-SPEC.md (Layout Notes: detail page order — Display title panel → statement max-width 640 → thumbnail grid; empty-state copy)
    - .planning/phases/02-portfolio-galleries/02-PATTERNS.md (page analog guidance + getStaticPaths approach)
  </read_first>
  <action>
    Create src/pages/galleries/index.astro (FR) and src/pages/en/galleries/index.astro (EN): each awaits `getGalleries()`, wraps output in `<BaseLayout title="...">`, renders an `<h1>` at Display role ("Galeries" FR / "Galleries" EN — plain, NO panel, its wrapper needs `container-type: inline-size`), then a `<GalleryGrid>` mapping each gallery to `<GalleryCard gallery={g} />`. When the array is empty, render the empty-state instead: heading (Heading role) FR "Galeries à venir" / EN "Galleries coming soon" and body (Body role) FR "De nouvelles œuvres seront bientôt visibles ici. Revenez prochainement." / EN "New work will appear here shortly. Check back soon." FR reads `.fr`; EN reads `.en`; import depth `../` for FR, `../../` for EN.
    Create src/pages/galleries/[slug].astro (FR) and src/pages/en/galleries/[slug].astro (EN): export `getStaticPaths()` that awaits `getGalleries()` and returns one entry per gallery as `{ params: { slug: g.slug }, props: { gallery: g } }` (pass the full gallery as props to avoid a second per-page fetch). The page reads `gallery` from `Astro.props`, wraps in BaseLayout, and renders in this order: the project title (`gallery.title`) at Display role inside a Wild Strawberry panel (same panel treatment as the card, its wrapper `container-type: inline-size` — renders larger here because the container is wider); the artist statement paragraph(s) `gallery.statement?.[locale] ?? ''` at Body role with `max-width: 640px` (no "About this series" label — plain like the homepage); then a `<GalleryGrid>` of that gallery's own images rendered as plain `<img>` thumbnails via `thumbnailUrl` (1:1 crop, explicit 600×600, `loading="lazy" decoding="async"` on below-the-fold thumbnails, null-safe `alt`), with NO panel on individual thumbnails. Add a "back to galleries" link using `getRelativeLocaleUrl(locale, 'galleries')` (never a literal href). Every locale-keyed read uses optional-chaining + fallback (WR-03). Do NOT branch on locale inside a single file and do NOT create a `[locale]` segment (Pitfall 2).
    Leave the thumbnails non-interactive for now — Plan 04 wires the lightbox onto them; structure the thumbnail markup with a stable hook (e.g. a `data-gallery-thumb` attribute and index) so Plan 04 can attach behavior without restructuring.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; test -f dist/galleries/index.html &amp;&amp; test -f dist/en/galleries/index.html &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four page files exist at the exact paths above; the `[slug].astro` pair each `export`s `getStaticPaths`.
    - `getStaticPaths` returns entries shaped `{ params: { slug }, props: { gallery } }` and maps over `getGalleries()`.
    - Listing pages import and render `GalleryGrid` + `GalleryCard`; empty-state copy strings are present in each locale's file.
    - Detail pages render the statement with `max-width: 640px` and a Wild Strawberry title panel with `container-type: inline-size`.
    - `grep "getRelativeLocaleUrl" src/pages/galleries/[slug].astro` matches (back link); no literal `href="/galleries"` string appears.
    - No `[locale]` folder exists anywhere under src/pages; FR reads `.fr`, EN reads `.en`.
    - `npm run build` emits `dist/galleries/index.html` and per-slug detail pages; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Visitors can browse the FR and EN listing grids and open any gallery's detail page (title panel + statement + thumbnail grid). Static build generates one page per gallery slug per locale.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Migrate all known projects into Sanity Studio</name>
  <read_first>
    - .planning/phases/02-portfolio-galleries/02-CONTEXT.md (D-11/D-12/D-13/D-14 content-migration decisions)
    - .planning/phases/02-portfolio-galleries/02-VALIDATION.md (seed-content Wave 0 requirement)
  </read_first>
  <action>A human (Florian/Romane) creates one gallery document per known project (Rebut, Silos, Brume, Adults, The Victorian Tea Room, Paysages, Accumulation, MADO, and any others) in Studio with real original images (D-14), first image as cover (D-09), required FR+EN alt text per image (D-02), FR+EN artist statement adapted from Myportfolio (D-12), and the manual display order (D-10). All projects ship this phase, no subset (D-13). This also seeds the real data the gallery e2e tests run against.</action>
  <acceptance_criteria>
    - All known projects are published in Sanity with real images, bilingual alt text, and bilingual statements, and appear on /galleries/ (both locales) in the chosen order after a rebuild.
    - At least the full project set is present so `tests/e2e/gallery.spec.ts` "listing"/"statement" groups pass GREEN.
  </acceptance_criteria>
  <what-built>The gallery pages now render whatever exists in the Sanity dataset. D-11/D-13 require real content for ALL known projects (Rebut, Silos, Brume, Adults, The Victorian Tea Room, Paysages, Accumulation, MADO, and any others on the current live site) to ship this phase — no placeholder content, no subset-now split. Content entry is Romane/Florian's Studio work (and doubles as the first real exercise of the CMS-01 self-serve flow); the automated gallery e2e/statement tests need at least these galleries present to run against real data.</what-built>
  <how-to-verify>
    1. Source Romane's/Florian's own original high-quality image files for each project (D-14 — not scraped from the live site).
    2. In Sanity Studio (`cd sanity && npx sanity dev`, or the deployed Studio), create one `gallery` document per known project: set the shared `title` (proper noun, D-04), upload images into the array (first image = cover, D-09), give every image its required FR + EN alt text (D-02), and write the FR + EN artist statement adapted from the existing Myportfolio description (D-12).
    3. Drag the galleries into the desired display order in the document list (D-10).
    4. Publish all documents, trigger a site rebuild, and confirm each project appears on `/galleries/` in the chosen order with working detail pages in both locales.
  </how-to-verify>
  <resume-signal>Type "approved" once all known projects are published with real images + bilingual alt text + bilingual statements and verified on the rebuilt site.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Content Lake → Astro build | Gallery content read at build time; token never shipped (unchanged). |
| Static HTML → visitor browser | 100% read-only prerendered output; no form/input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-IV | Tampering (Input) | Gallery content authored in Studio | mitigate | Schema `required()`/`.min(1)` from 02-01 enforces alt text + at least one image before publish (ASVS V5, Studio-side only). |
| T-02-03-NULL | Denial of Service (build) | Partially-populated gallery doc crashing the static build | mitigate | Every locale-keyed read uses optional-chaining + fallback (WR-03) so a mid-edit document cannot fail the build. |
| T-02-03-XSS | Tampering | Statement/alt text rendered into HTML | accept | Astro auto-escapes interpolated text; statements are plain strings (not portable-text/HTML), authored only by the trusted CMS editor. No untrusted-input rendering surface. |
</threat_model>

<verification>
- `npm run build && npx playwright test tests/e2e/gallery.spec.ts -g "listing"` and `-g "statement"` pass GREEN once content is seeded.
- Manual: `/galleries/` and `/en/galleries/` show the grid in manual order; a detail page shows the statement in the correct language.
</verification>

<success_criteria>
- PORT-01: visitor browses all migrated galleries in manual order (both locales).
- PORT-03: each gallery detail shows its bilingual artist statement.
- Real content for all known projects is live.
</success_criteria>

<output>
Create `.planning/phases/02-portfolio-galleries/02-03-SUMMARY.md` when done.
</output>
