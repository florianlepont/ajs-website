---
phase: 04-legal-compliance
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/pages/confidentialite.astro
  - src/pages/en/confidentialite.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [LEGAL-03, LEGAL-05]
must_haves:
  truths:
    - "Visitor can view a privacy policy page describing the contact-form / Sanity / hosting-logs data flows in FR at /confidentialite/ and EN at /en/confidentialite/"
    - "The privacy policy discloses the ajs_locale cookie as strictly functional / CNIL-exempt and explains why there is no accept/reject banner (D-01/D-02)"
    - "The contact-form flow is described as a direct send to Romane's own OVH mailbox with no third-party processor (D-05)"
    - "A 'Confidentialité' / 'Privacy' link appears in the footer on every page, after the legal-notice link"
    - "Loading either legal page sets no cookie; no consent banner element exists anywhere"
  artifacts:
    - path: "src/pages/confidentialite.astro"
      provides: "FR privacy policy (contact form, Sanity, hosting logs, cookies sections)"
      contains: "ajs_locale"
    - path: "src/pages/en/confidentialite.astro"
      provides: "EN privacy policy"
      contains: "ajs_locale"
    - path: "src/layouts/BaseLayout.astro"
      provides: "footer-legal-nav gains the Confidentialité / Privacy link"
      contains: "getRelativeLocaleUrl(locale, 'confidentialite')"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "/confidentialite/"
      via: "getRelativeLocaleUrl(locale, 'confidentialite')"
      pattern: "getRelativeLocaleUrl\\(locale, 'confidentialite'\\)"
    - from: "src/pages/confidentialite.astro"
      to: "ajs_locale cookie disclosure"
      via: "hardcoded Cookies section text"
      pattern: "ajs_locale"
---

<objective>
Deliver the privacy-policy vertical slice (LEGAL-03 + LEGAL-05). After this plan a visitor can reach and read a lightweight, plain-language bilingual privacy notice from every footer, covering all D-05 data flows and folding the CNIL-exempt `ajs_locale` cookie disclosure into a Cookies section — with no banner, because no tracking/analytics cookie exists anywhere on the site.

Purpose: LEGAL-03/LEGAL-05 are launch blockers; the privacy notice is the sole disclosure surface for the site's only client-side data mechanism.
Output: `src/pages/confidentialite.astro`, `src/pages/en/confidentialite.astro`, and the second footer legal link in `src/layouts/BaseLayout.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** French/EU site visitor, **I want to** read the site's legal notice, privacy policy, and cookie disclosure in French or English, **so that** I know who runs the site, how my contact-form data is handled, and that no tracking cookies are used.

*(This plan delivers the privacy-policy + cookie-disclosure half of the phase story; Plan 01 delivered the legal-notice half.)*

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-legal-compliance/04-CONTEXT.md
@.planning/phases/04-legal-compliance/04-RESEARCH.md
@.planning/phases/04-legal-compliance/04-PATTERNS.md
@.planning/phases/04-legal-compliance/04-UI-SPEC.md

<interfaces>
<!-- Contracts extracted this session. Plan 01 already added the footer-legal-nav + legal-notice link; this plan appends the privacy link into that same nav. -->

From src/layouts/BaseLayout.astro (after Plan 01): frontmatter has `legalNoticeLabel`/`legalNoticeHref`; footer has `<nav class="footer-legal-nav" aria-label="Legal">` with the legal-notice `<a>`. Add the privacy label/href beside them and a second `<a>` after the legal-notice link (order: legal notice, then privacy — UI-SPEC).

From src/components/LanguageSwitcher.astro (lines 66-89) — the ONLY cookie on the site, facts the Cookies section must state accurately:
- name `ajs_locale`; written only inside a click handler on the FR/EN switcher link (NOT on page load); `max-age=31536000` (1 year); `SameSite=Lax; Secure`; `path` scoped to the deployed base.

From src/components/ContactForm.astro: contact alias `contact@atelierjacquelinesuzanne.fr` — reuse as the data-controller contact.

Slug constraint (RESEARCH Pitfall 1): EN file MUST be `src/pages/en/confidentialite.astro` (same slug segment as FR), never `privacy-policy.astro`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the bilingual privacy policy pages + append the privacy footer link</name>
  <files>src/pages/confidentialite.astro, src/pages/en/confidentialite.astro, src/layouts/BaseLayout.astro</files>
  <read_first>
    - src/pages/about.astro (FR structural/typography analog — copy the `<style>` block verbatim, rename wrapper to `.legal-page`)
    - src/pages/en/about.astro (EN analog — import depth `../../layouts/BaseLayout.astro`)
    - src/pages/mentions-legales.astro (Plan 01 output — reuse the exact `.legal-page` style block and section conventions for a consistent pair)
    - src/layouts/BaseLayout.astro (the file being modified — the Plan 01 footer-legal-nav to append into)
    - src/components/LanguageSwitcher.astro (authoritative `ajs_locale` cookie facts for the Cookies section)
    - .planning/phases/04-legal-compliance/04-CONTEXT.md (D-01/D-02 cookie treatment, D-04 lightweight depth, D-05 data flows)
    - .planning/phases/04-legal-compliance/04-RESEARCH.md ("GDPR/RGPD Minimum Notice Content" checklist, "CNIL Cookie Guidance", Pitfall 5)
  </read_first>
  <action>
    Create `src/pages/confidentialite.astro` (FR) and `src/pages/en/confidentialite.astro` (EN) as hardcoded Pattern-3 pages wrapped in `<BaseLayout title="...">`, reusing the `.legal-page` style block from the mentions pages. Keep the EN slug segment identical (`confidentialite`). Lead each frontmatter comment with "LEGAL-03 / LEGAL-05" and note it is hardcoded, plain-language (D-04, not a formal Article 13 processor list).

    Section order (UI-SPEC Layout Notes, Privacy Policy):
    1. `<h1>` — FR "Politique de confidentialité" / EN "Privacy Policy".
    2. Optional one-line intro (Body) framing it as a lightweight, plain-language notice (D-04).
    3. `<h2>` "Formulaire de contact" / "Contact form": describe the flow as a DIRECT send of the message to Romane's own OVH mailbox via the host's `mail()` — explicitly state there is NO third-party data processor (D-05; this supersedes Phase 3's interim Web3Forms wiring — do not mention Web3Forms). Include the CNIL minimum-notice elements in plain language: data controller = Romane Lepont / Atelier Jacqueline Suzanne, contact@atelierjacquelinesuzanne.fr; purpose = responding to the visitor's enquiry; legal basis = consent (submitting the form is voluntary); the name/email/message fields are required to reply and the honeypot is a spam trap, not visitor data; retention = plain-language "kept only as long as needed to respond, then deleted"; rights = access, rectification, erasure, objection, PLUS the right to lodge a complaint with the CNIL (do not omit the CNIL-complaint right).
    4. `<h2>` "Contenu du site (Sanity)" / "Site content (Sanity)": one line — gallery/site content is fetched from Sanity at build time only; no visitor data ever reaches Sanity (D-05).
    5. `<h2>` "Journaux d'hébergement" / "Hosting logs": brief — the web host (name OVH, consistent with the mentions-légales host block and D-07's target-state convention) automatically logs visitor IP and user-agent as a normal function of serving pages; this is personal data under GDPR even though the site's own code never touches it (D-05).
    6. `<h2>` "Cookies": disclose the single `ajs_locale` cookie using the exact facts from LanguageSwitcher.astro — name `ajs_locale`, set only when the visitor clicks the FR/EN switcher (not automatically on page load), stores a single language value, `SameSite=Lax; Secure`, ~1-year lifetime. State it is strictly functional / UI-personalization and therefore CNIL-exempt from consent, which is why there is no accept/reject banner; and state plainly that the site uses no analytics or tracking cookies of any kind (Pitfall 5 — disclose the exempt cookie, do not omit it; D-01/D-02).

    Then edit `src/layouts/BaseLayout.astro`: add `const privacyLabel = locale === 'en' ? 'Privacy' : 'Confidentialité';` and `const privacyHref = getRelativeLocaleUrl(locale, 'confidentialite');` beside the Plan 01 legal-notice constants, and append a second `<a href={privacyHref} class="nav-link">{privacyLabel}</a>` inside the existing `.footer-legal-nav`, AFTER the legal-notice link (order per UI-SPEC).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/legal.spec.ts && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `/confidentialite/` renders with `html[lang=fr]`, `h1` "Politique de confidentialité", and body containing all four section headings plus "ajs_locale" and a contact-form direct-send phrase; no occurrence of "Web3Forms".
    - `/en/confidentialite/` renders with `html[lang=en]`, `h1` "Privacy Policy", and contains "ajs_locale".
    - The Cookies section states the cookie is functional/exempt and that no accept/reject banner is used and no analytics/tracking cookie exists.
    - The Contact-form section names the CNIL complaint right and states no third-party processor is involved.
    - `BaseLayout.astro` contains `getRelativeLocaleUrl(locale, 'confidentialite')` and the footer-legal-nav renders two links in order: Mentions légales, then Confidentialité.
    - Full `tests/e2e/legal.spec.ts` suite passes (all groups: mentions, privacy, cookie, switcher) and `npm run test:unit` passes.
    - EN file slug segment is exactly `confidentialite` (`src/pages/en/confidentialite.astro`), not `privacy-policy`.
  </acceptance_criteria>
  <done>Privacy policy live and reachable from every footer; ajs_locale disclosed with no banner; full legal e2e suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser ← static HTML | Prerendered privacy content, no runtime logic, no forms on the page; the Cookies section describes (does not set) the pre-existing `ajs_locale` cookie. |
| privacy disclosure ← actual site behavior | Risk is factual accuracy: the disclosed data flows and cookie facts must match the real code (LanguageSwitcher, ContactForm, Sanity build-time fetch). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Cookies-section accuracy / no-tracking-before-consent claim | mitigate | Cookies section sources exact `ajs_locale` facts from LanguageSwitcher.astro; grep-verified zero analytics/tracking scripts exist site-wide, so the "exempt, no banner" claim is truthful; cookie set only on explicit user action, not on load. |
| T-04-04 | Information Disclosure | privacy-notice completeness (missing CNIL-required element) | mitigate | Include the full CNIL minimum-notice checklist (controller, purpose, legal basis, field necessity, recipients, retention, rights incl. the commonly-omitted CNIL-complaint right). |
| T-04-05 | Repudiation / accuracy | contact-form flow description | mitigate | Describe the D-05 direct-send-to-OVH-mailbox end-state (no third-party processor); explicitly avoid the stale Web3Forms framing so the notice matches the intended architecture. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npx playwright test tests/e2e/legal.spec.ts` — full suite green (mentions, privacy, cookie, switcher).
- `npm run test:e2e && npm run test:unit` — full harness green.
- `grep -rn "Web3Forms" src/pages/*confidentialite* src/pages/en/*confidentialite*` returns nothing.
- `npm run build` succeeds.
</verification>

<success_criteria>
- Bilingual privacy policy live at `/confidentialite/` and `/en/confidentialite/` covering all D-05 flows and the CNIL minimum-notice checklist.
- `ajs_locale` disclosed as CNIL-exempt/functional with a documented no-banner rationale; no consent banner anywhere.
- Both footer legal links present site-wide; full legal e2e suite green.
</success_criteria>

<output>
Create `.planning/phases/04-legal-compliance/04-02-SUMMARY.md` when done.
</output>
