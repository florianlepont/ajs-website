---
phase: 04.1-design-system-homepage-refresh
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [D-08, D-09, D-12]
user_setup: []

must_haves:
  truths:
    - "A failing (RED) e2e spec exists that pins the homepage contract before the homepage is built"
    - "The spec asserts the carousel renders, the grid toggle works, only migrated galleries appear, and auto-advance is observable+pausable via mocked clock"
  artifacts:
    - path: "tests/e2e/homepage.spec.ts"
      provides: "Wave 0 RED contract for D-08/D-09/D-12"
      contains: "clock"
  key_links:
    - from: "tests/e2e/homepage.spec.ts"
      to: "src/pages/index.astro (built in Plan 04.1-04)"
      via: "Playwright navigation to /"
      pattern: "page.goto\\('/'\\)"
---

<objective>
Write the Wave 0 RED end-to-end spec that defines the homepage contract before the homepage exists. It fails until Plan 04.1-04 builds the carousel/grid — that is the point (RED→GREEN), mirroring how `gallery.spec.ts` was authored RED in Phase 2 Wave 0.

Purpose: Give the homepage plan a concrete automated GREEN target and satisfy VALIDATION.md's Wave 0 requirement (D-08 carousel + grid toggle, D-09 auto-advance, D-12 only-migrated-galleries).
Output: `tests/e2e/homepage.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-VALIDATION.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md

<interfaces>
<!-- Selector/behavior contract the homepage (Plan 04.1-04) MUST satisfy. Author the spec against these. -->
- Homepage top bar is a `<header>` element containing the LanguageSwitcher ("FR | EN") and locale nav — do NOT break the pre-existing i18n.spec.ts contract.
- Carousel root: an element with `data-role="home-carousel"`.
- Display-mode toggle buttons labelled "Carrousel"/"Grille" (fr) — assert toggling to grid reveals a 2-column grid and back.
- Migrated galleries are Silos and Brume ONLY (D-12). Unmigrated (must NEVER appear): Rebut, Adults, The Victorian Tea Room, Paysages.
- Auto-advance (D-09): a `setInterval`-driven index change every 6000ms, pausable on hover/focus. Use Playwright's `page.clock.install()` to advance the clock deterministically rather than real wall-clock waits (RESEARCH.md Validation Architecture / Wave 0 Gaps).
- Carousel index label format: "01 / 02" (Label role).

Existing test patterns to mirror: tests/e2e/gallery.spec.ts (goto + role/text assertions), tests/e2e/i18n.spec.ts (header/switcher assertions). Playwright 1.61.1; config auto-runs `npm run preview` against a production build.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/e2e/homepage.spec.ts (RED)</name>
  <files>tests/e2e/homepage.spec.ts</files>
  <read_first>
    - tests/e2e/gallery.spec.ts (RED-authoring precedent, goto + role/text selectors)
    - tests/e2e/i18n.spec.ts (header/switcher/locale contract the homepage must not break)
    - playwright.config.ts (webServer runs `npm run preview` against a production build)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Layout Notes → Homepage carousel/grid; Copywriting Contract)
  </read_first>
  <action>
    Create `tests/e2e/homepage.spec.ts` with a top comment stating it is RED (Wave 0) and targets contracts built in Plan 04.1-04 — do not stub or weaken it to pass early.
    Write these test cases against `/` (French):
    1. Carousel renders: `page.goto('/')`, expect an element `[data-role="home-carousel"]` visible, and expect the first migrated gallery title (case-insensitive `/silos/i`) to be present.
    2. Only migrated galleries appear (D-12): assert the page body text does NOT contain any of "Rebut", "Adults", "Victorian Tea Room", "Paysages" (case-insensitive). Assert exactly the two migrated titles (Silos, Brume) are reachable in the homepage output.
    3. Grid toggle (D-08): click the "Grille" toggle control, assert a 2-column grid of gallery tiles becomes visible; click "Carrousel", assert the hero carousel returns.
    4. Auto-advance + pause (D-09): install `page.clock.install()` before navigation; capture the carousel index label (e.g. "01 / 02"); advance the clock by 6000ms; assert the index label changed. Then hover/focus the carousel root, advance the clock again by 6000ms, and assert the index label did NOT change (paused).
    5. i18n non-regression guard: `page.goto('/')`, expect a `<header>` containing "FR | EN"; assert header innerText differs from the `/en/` header (locale nav differs). This locks the homepage's header contract so it stays compatible with i18n.spec.ts.
    Use resilient selectors (roles, `data-role`, visible text) consistent with the existing specs. Do NOT add watch-mode flags.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/homepage.spec.ts; test $? -ne 0 && echo "RED-as-expected" && test -f tests/e2e/homepage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/homepage.spec.ts` exists
    - The spec references `data-role="home-carousel"`, the "Grille"/"Carrousel" toggle text, `page.clock`, and the negative-assertion list (Rebut/Adults/Victorian Tea Room/Paysages)
    - `npx playwright test tests/e2e/homepage.spec.ts` FAILS (RED) because the homepage does not exist yet — this is expected and correct
    - The spec contains a comment marking it RED / Wave 0
  </acceptance_criteria>
  <done>The RED homepage spec is committed, failing for the right reason (no carousel yet), and ready to turn GREEN in Plan 04.1-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (test-only) | Adds a test file; introduces no runtime code, endpoints, inputs, or dependencies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-02-NA | — | test spec only | accept | Presentation-layer test authoring; no new trust boundary, input surface, auth, or dependency. No mitigation required. |
</threat_model>

<verification>
- `npx playwright test tests/e2e/homepage.spec.ts` fails (RED) prior to Plan 04.1-04.
- The rest of the suite is unaffected (`npm run test:e2e` failures limited to homepage.spec.ts).
</verification>

<success_criteria>
- A RED homepage spec exists covering D-08 (carousel + grid toggle), D-09 (auto-advance/pause via mocked clock), D-12 (only migrated galleries), and an i18n header non-regression guard.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-homepage-refresh/04.1-02-SUMMARY.md` when done.
</output>
