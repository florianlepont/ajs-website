---
phase: 04.1-design-system-homepage-refresh
plan: 04
type: execute
wave: 2
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - src/components/HomeCarousel.astro
  - src/pages/index.astro
  - src/pages/en/index.astro
autonomous: true
requirements: [D-05, D-08, D-09, D-10, D-12]
user_setup: []

must_haves:
  truths:
    - "The homepage shows a full-viewport hero carousel of the migrated galleries (Silos, Brume) with a rotating decorative accent panel"
    - "The carousel auto-advances every 6s and pauses on hover/focus"
    - "A toggle switches the homepage between carousel and a 2-column grid, and back"
    - "Only galleries with real migrated images appear — never Rebut, Adults, The Victorian Tea Room, or Paysages"
    - "The homepage exposes a <header> containing the LanguageSwitcher and locale nav, keeping i18n.spec.ts green"
  artifacts:
    - path: "src/components/HomeCarousel.astro"
      provides: "Vanilla-JS carousel/grid island (display-mode + index + timer state), no framework"
      contains: "data-role=\"home-carousel\""
    - path: "src/pages/index.astro"
      provides: "FR homepage — filtered gallery data + HomeCarousel + headerVariant='none'"
    - path: "src/pages/en/index.astro"
      provides: "EN homepage mirror"
  key_links:
    - from: "src/pages/index.astro frontmatter"
      to: "getGalleries() filtered to images.length > 0"
      via: "build-time Sanity fetch + D-12 filter"
      pattern: "images.*length"
    - from: "src/components/HomeCarousel.astro <script>"
      to: "embedded gallery data (data-* on hidden nodes)"
      via: "DOM read at DOMContentLoaded (no Sanity in client)"
      pattern: "querySelectorAll"
    - from: "src/pages/index.astro"
      to: "BaseLayout headerVariant"
      via: "prop"
      pattern: "headerVariant"
---

<objective>
Replace the placeholder homepage with the real gallery entry point: a full-viewport hero carousel (auto-advancing, pausing on hover, rotating decorative accent panel) and a 2-column grid view, with a toggle between them. Only galleries that already have migrated photos appear. This turns the RED `homepage.spec.ts` (Plan 04.1-02) GREEN.

Purpose: Deliver D-05 (decorative palette usage), D-08 (carousel + grid), D-09 (auto-advance/pause), D-10 (mobile), D-12 (only migrated galleries) as one vertical slice.
Output: New `HomeCarousel.astro` island + fully-replaced `index.astro` / `en/index.astro`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-RESEARCH.md

<interfaces>
<!-- Build-time data source + island pattern + exact copy. -->
From src/lib/sanity.ts (build-time only — never import into a client <script>):
- `getGalleries(): Promise<Gallery[]>` — Romane's manual order. Gallery = { title: string; slug: string; statement: {fr,en}; images: GalleryImage[] } where images[0] is the cover.
From src/lib/image.ts (build-time only):
- `fullSizeUrl(img, maxWidth=2000)` — for the full-bleed hero photo
- `thumbnailUrl(img, size=600)` — for the 2-column grid tiles

D-12 filter: `getGalleries()` then `.filter(g => g.images.length > 0)` in the page frontmatter (NOT a new query function). Today yields Silos + Brume only.

Island pattern to mirror: src/components/Lightbox.astro (hidden data-bearing nodes + typed `<script>`, no client:* directive) and src/components/LanguageSwitcher.astro (script-tag island). RESEARCH.md Pattern 4 gives the exact state shape (displayMode, carouselIndex, timer; ACCENT_VARS = ['--color-accent','--palette-purple','--palette-teal','--palette-lime','--palette-plum']; accent = ACCENTS[index % ACCENTS.length]; 6000ms setInterval; stop on mouseenter/focusin, start on mouseleave/focusout).

Exact copy (verbatim, from UI-SPEC Copywriting Contract / prototype renderVals):
- Wordmark (accent panel): "Atelier Jacqueline Suzanne" (Display role, fixed 32px/600, letter-spacing 0.02em)
- Intro body FR: "Le site présente le travail photographique de Romane Lepont et permet l'achat d'une œuvre directement en ligne." · EN: "This site showcases Romane Lepont's photographic work and lets you buy a piece directly online."
- Byline FR: "Un projet de Romane Lepont" · EN: "A project by Romane Lepont"
- CTA (plain underlined text link, NOT Button — D-15) FR: "Découvrir les autres galeries →" → /galleries/ · EN: "Discover other galleries →" → /en/galleries/
- Toggle FR: "Carrousel" / "Grille" · EN: "Carousel" / "Grid"
- Prev/next accessible names FR: "Galerie précédente" / "Galerie suivante" · EN: "Previous gallery" / "Next gallery"
- Index label format: "01 / 02" (padStart 2)
- ACCENTS array (bg/ink), prototype lines 187-193: [accent/on-accent], [palette-purple/#FFFFFF], [palette-teal/ink], [palette-lime/ink], [palette-plum/#FFFFFF]

Decorative palette (D-05) is used ONLY here (carousel accent panel) — nowhere else on the site.

Prototype reference (structure/inline styles only, NOT shippable): .planning/design-import/homepage-prototype/Atelier Homepage.dc.html lines 51-124 (carousel + grid views), 178-265 (GALLERIES/ACCENTS/state).

CONTRACT — do not break i18n.spec.ts: the homepage top bar MUST be a `<header>` element containing `<LanguageSwitcher />` and locale nav (Accueil/Galeries vs Home/Galleries). Invoke `<BaseLayout headerVariant="none">` so BaseLayout does not also render a header (avoids duplicate `<header>`). BaseLayout still renders the shared `<footer>`.

Intro copy is HARDCODED locale-conditional strings (UI-SPEC decision — do NOT wire to siteSettings.welcomeHeading/welcomeBody).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the HomeCarousel island (carousel + grid + toggle + auto-advance)</name>
  <files>src/components/HomeCarousel.astro</files>
  <read_first>
    - src/components/Lightbox.astro (hidden data-node + typed <script> island precedent)
    - src/components/LanguageSwitcher.astro (script island + scoped style precedent)
    - .planning/design-import/homepage-prototype/Atelier Homepage.dc.html (lines 51-124 carousel/grid markup + inline styles; 195-265 state machine)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-RESEARCH.md (Pattern 4, Anti-Patterns, Don't Hand-Roll)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Layout Notes → Homepage carousel/grid/mobile; Color decorative palette)
  </read_first>
  <action>
    Create `src/components/HomeCarousel.astro`. Props: `locale: 'fr' | 'en'` and the pre-filtered gallery list (title, slug, hero image URL, grid image URL, cover alt) passed from the page frontmatter — the component does NOT call Sanity (RESEARCH.md Anti-Patterns: no getGalleries in the client script). Emit static markup for BOTH modes plus a hidden data-bearing node (mirroring Lightbox's `<ul hidden data-role=...>`) carrying per-gallery slug/title/hero-src/grid-src/alt via `data-*` attributes.
    Root element: `data-role="home-carousel"`. Carousel mode: full-viewport hero (`height:100vh; min-height:680px`) with the current gallery's full-bleed hero `<img>` + dark gradient scrim (`linear-gradient(to top, rgba(0,0,0,0.65) 0%, rgba(0,0,0,0.15) 45%, rgba(0,0,0,0) 70%)`); a `<header>` top bar (logo white PNG left; nav Accueil/Galeries + the Carrousel/Grille toggle buttons + `<LanguageSwitcher />` right, text colored with the current accent per the prototype); bottom-left index label ("01 / 02") + uppercased gallery title (Heading role, white) + byline; bottom-right accent panel `width: min(560px, 52%)` whose background is the current `ACCENTS[index % 5]` entry, containing the Display-role wordmark (fixed 32px/600, letter-spacing 0.02em), intro body, and the underlined CTA link; prev/next arrow buttons (`‹`/`›` glyphs, 44px tap target, rgba(0,0,0,0.35) chip, white glyph) with the localized accessible names.
    Grid mode: same top-bar `<header>` (static position, ink-colored text on white); a full-width pink (`--color-accent`) intro band with the wordmark + intro copy; a `grid-template-columns: repeat(2,1fr); gap: 0` grid of tiles, each a link to the gallery detail page with full-bleed cover + the same scrim + Display-role white title bottom-left.
    Include one typed `<script>` (no client:* directive) implementing RESEARCH.md Pattern 4: read the data nodes at load; state = displayMode ('carousel'|'grid', default carousel) + carouselIndex (0) + timer; `render()` imperatively updates the hero `<img src>`, accent-panel background-color (via the ACCENT_VARS modulo cycle written GENERICALLY for N galleries — RESEARCH.md Open Question 3), index label, and title; toggle buttons switch displayMode by toggling `hidden`/classes; prev/next mutate carouselIndex modulo length; `setInterval` 6000ms auto-advance started on load and on mouseleave/focusout, cleared on mouseenter/focusin of the root. No React/setState patterns — direct DOM mutation like Lightbox.astro.
    Mobile (D-10, breakpoint 768px): below 768px the hero shrinks to `min-height:420px`, the accent panel becomes full-width below the hero in normal flow, grid becomes single column; the top-bar nav wraps to a second line (no hamburger — matches existing BaseLayout behavior). All colors via tokens; decorative palette used only in the accent panel.
  </action>
  <verify>
    <automated>npm run build && test -f src/components/HomeCarousel.astro && grep -q 'data-role="home-carousel"' src/components/HomeCarousel.astro && ! grep -q "getGalleries\|from '../lib/sanity'\|from \"../lib/sanity\"" src/components/HomeCarousel.astro && grep -q "6000" src/components/HomeCarousel.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/HomeCarousel.astro` exists with a `data-role="home-carousel"` root
    - The client `<script>` does NOT import from `../lib/sanity` (no build-time client leak) and does NOT reference `_ds_bundle.js`/`support.js`/`image-slot.js`
    - Accent cycling uses `index % ACCENTS.length` (generic, not hardcoded to 2 galleries)
    - Auto-advance interval is 6000ms with pause on mouseenter/focusin and resume on mouseleave/focusout
    - The top bar is a `<header>` containing `<LanguageSwitcher />`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>A dependency-free carousel/grid island exists with auto-advance/pause, generic accent cycling, both display modes, mobile behavior, and a `<header>`-wrapped top bar; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace index.astro and en/index.astro with the new homepage (turn homepage.spec.ts GREEN)</name>
  <files>src/pages/index.astro, src/pages/en/index.astro</files>
  <read_first>
    - src/pages/index.astro (current placeholder — full replacement target)
    - src/pages/en/index.astro (EN mirror)
    - src/pages/galleries/index.astro (getGalleries() frontmatter usage precedent)
    - src/lib/sanity.ts, src/lib/image.ts (build-time data + URL builders)
    - src/components/HomeCarousel.astro (from Task 1)
    - tests/e2e/homepage.spec.ts (the GREEN target from Plan 04.1-02)
    - tests/e2e/i18n.spec.ts (header/footer/switcher contract that must stay green)
  </read_first>
  <action>
    Fully replace `src/pages/index.astro` (FR): in frontmatter call `getGalleries()`, filter to `images.length > 0` (D-12), and map each surviving gallery to { title, slug, heroSrc: fullSizeUrl(images[0]), gridSrc: thumbnailUrl(images[0], 600), alt: images[0].alt?.fr ?? '' }. Wrap the page in `<BaseLayout title="Atelier Jacqueline Suzanne" headerVariant="none">` and render `<HomeCarousel locale="fr" galleries={...} />` passing the mapped data + all hardcoded FR copy from the interfaces block. Do NOT read welcomeHeading/welcomeBody. Mirror exactly in `src/pages/en/index.astro` with `locale="en"`, `alt` from `.en`, EN copy, and CTA target `/en/galleries/`.
    Confirm the CTA is a plain underlined text link (not Button — D-15) and that gallery-detail links resolve via `getRelativeLocaleUrl(locale, 'galleries/'+slug)` (or the equivalent already used by GalleryCard).
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/homepage.spec.ts tests/e2e/i18n.spec.ts && ! grep -q "welcomeHeading\|welcomeBody" src/pages/index.astro && ! grep -q "welcomeHeading\|welcomeBody" src/pages/en/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/index.astro` and `src/pages/en/index.astro` filter galleries by `images.length > 0` and render `<HomeCarousel>` inside `<BaseLayout headerVariant="none">`
    - Neither homepage references `welcomeHeading`/`welcomeBody`
    - `npx playwright test tests/e2e/homepage.spec.ts` PASSES (RED→GREEN: carousel renders, grid toggles, only Silos/Brume appear, auto-advance pauses on hover)
    - `npx playwright test tests/e2e/i18n.spec.ts` PASSES (homepage `<header>` has "FR | EN", header/footer copy differs fr/en, switcher navigates)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The homepage is the real carousel/grid gallery entry point showing only migrated galleries; homepage.spec.ts is GREEN and i18n.spec.ts stays GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build (Sanity fetch) → static HTML → browser island | Gallery data is fetched once at build time and embedded as static markup; the client island only reads DOM, never Sanity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04-ID | Information Disclosure | HomeCarousel client `<script>` | mitigate | Carousel data is embedded as static markup from build-time `getGalleries()`; the client script must NOT import `src/lib/sanity.ts`, so `SANITY_API_READ_TOKEN` never reaches the browser (existing project constraint). Verified by grep in Task 1. |
| T-04.1-04-NA | — | presentation/interaction only | accept | No new input surface, endpoint, auth, or dependency. No mitigation required. |
</threat_model>

<verification>
- `npm run build` succeeds.
- `npm run test:e2e` fully green — homepage.spec.ts GREEN, i18n.spec.ts still GREEN, no other spec regressed.
- Manual: confirm auto-advance interval feels right (6s is a one-line tuning value, RESEARCH.md A4) and mobile stacking works below 768px.
</verification>

<success_criteria>
- Homepage renders the carousel + grid toggle, auto-advancing/pausing, showing only Silos/Brume with the rotating decorative accent panel.
- Unmigrated galleries never appear on the homepage.
- The homepage keeps a `<header>` + `<footer>` compatible with i18n.spec.ts.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-homepage-refresh/04.1-04-SUMMARY.md` when done.
</output>
