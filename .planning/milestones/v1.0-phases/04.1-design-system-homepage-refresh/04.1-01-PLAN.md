---
phase: 04.1-design-system-homepage-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - public/logos/AJS_Brutalist_Black_Transparent.png
  - public/logos/AJS_Brutalist_White_Transparent.png
  - package.json
  - package-lock.json
  - .planning/phases/02-portfolio-galleries/deferred-items.md
autonomous: false
requirements: [D-01, D-02, D-04, D-05, D-06, D-11, D-13]
user_setup: []

must_haves:
  truths:
    - "Every page renders on the new white (#FFFFFF) / ink (#1A1A1A) / pop-pink (#FF3B94) identity — no Dawn Pink / Woodsmoke / Wild Strawberry values remain in BaseLayout"
    - "Links render as full pink text site-wide (nav, footer, body copy) — not ink-text-plus-underline"
    - "The header shows the AJS wordmark PNG logo (not the text site-title) and inverts to the white variant on hover"
    - "Header and footer are white-on-white, delineated only by a 1px hairline border"
    - "Archivo Black is self-hosted (no fonts.googleapis.com request); the dormant Delight @font-face is gone"
    - "BaseLayout exposes a headerVariant prop (solid | transparent | none) that downstream immersive pages consume"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "New :root token block, Archivo Black import, logo header markup + hover-invert, headerVariant prop, white-on-white chrome"
      contains: "headerVariant"
    - path: "public/logos/AJS_Brutalist_Black_Transparent.png"
      provides: "Default header logo asset"
    - path: "public/logos/AJS_Brutalist_White_Transparent.png"
      provides: "Hover / transparent-header logo asset"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "@fontsource/archivo-black"
      via: "frontmatter import"
      pattern: "@fontsource/archivo-black"
    - from: "src/layouts/BaseLayout.astro :root"
      to: "every component's var(--color-*) references"
      via: "global CSS custom properties"
      pattern: "--color-accent"
---

<objective>
Establish the rebrand foundation: swap BaseLayout's `:root` token block to the imported monochrome + pop-pink identity, self-host Archivo Black, restructure the header/footer chrome to white-on-white, and replace the text site-title with the AJS logo (with hover-invert). This is the single point of change every other Wave-2 plan builds on — new components read these tokens, and the homepage/gallery-detail pages consume the new `headerVariant` prop.

Purpose: The whole phase's visual identity and header contract originate here. Nothing downstream renders correctly until these tokens and the header prop exist (D-01, D-02, D-04, D-05, D-06, D-11, D-13).
Output: A repainted `BaseLayout.astro`, two logo assets under `public/logos/`, `@fontsource/archivo-black` installed, and the Delight deferred-item removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-CONTEXT.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md
@.planning/phases/04.1-design-system-homepage-refresh/04.1-RESEARCH.md

<interfaces>
<!-- Authoritative token values the executor must copy verbatim. -->

From .planning/design-import/design-system/tokens/colors.css (authoritative — CSS file wins over readme prose):
- --gray-0: #FFFFFF; --gray-900: #1A1A1A; --pink-600: #FF3B94
- --palette-purple: #AF3DFF; --palette-teal: #55FFE1; --palette-lime: #A6FD29; --palette-plum: #37013A
- --color-dominant: var(--gray-0); --color-secondary: var(--gray-0); --color-accent: var(--pink-600)
- --color-ink: var(--gray-900); --color-border: #E3E1DE; --color-on-accent: var(--gray-900)
- --color-link / --color-link-hover / --color-focus-ring: var(--color-accent)

From .planning/design-import/design-system/tokens/typography.css:
- --font-sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif
- --weight-regular: 400; --weight-semibold: 600
- --text-label-size:14px / --text-body-size:16px / --text-heading-size:20px / --text-display-size:32px
- matching *-weight and *-leading tokens; --text-heading-family / --text-display-family: var(--font-display)

From .planning/design-import/design-system/tokens/fonts.css (family reference ONLY — do NOT copy its Google Fonts @import, Pitfall 1):
- --font-display: 'Archivo Black', var(--font-sans)

From .planning/design-import/design-system/tokens/spacing.css:
- --radius-none: 0px; --radius-sm: 2px; --border-hairline: 1px; --border-focus: 2px; --focus-ring-offset: 2px; --tap-target-min: 44px
- spacing scale (--space-xs..3xl) is UNCHANGED and already present in BaseLayout — do not duplicate

Current BaseLayout header markup to replace (src/layouts/BaseLayout.astro lines 85-104): `<p class="site-title">`, `.chrome-band` header/footer, `.nav-link`. Current :root is lines 119-135; Delight @font-face is lines 112-117; global `a` + focus-visible rules are lines 164-178.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — @fontsource/archivo-black</name>
  <action>Do NOT run any npm install yet. Verify the legitimacy of `@fontsource/archivo-black` on npmjs.com (org, repo, version, downloads) and pause for human approval before Task 2 installs it. This gate is not auto-approvable (RESEARCH.md Package Legitimacy Audit, threat T-04.1-SC).</action>
  <what-built>Nothing yet — this is a supply-chain gate that MUST clear before any npm install (RESEARCH.md Package Legitimacy Audit; slopcheck was unavailable, so the package is tagged [ASSUMED]). Mirrors Phase 2's precedent for @sanity/orderable-document-list.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@fontsource/archivo-black
    2. Confirm the publisher/org is `fontsource` and the repository is `github.com/fontsource/font-files`
    3. Confirm version 5.2.8 exists and weekly downloads are a plausible multi-thousand count (not a brand-new zero-download typosquat)
    4. Confirm there is no near-identical typosquat name you were about to install instead
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install @fontsource/archivo-black`, or describe the concern to block.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Archivo Black, swap :root tokens, remove Delight, restyle global links/focus</name>
  <files>src/layouts/BaseLayout.astro, package.json, package-lock.json, .planning/phases/02-portfolio-galleries/deferred-items.md</files>
  <read_first>
    - src/layouts/BaseLayout.astro (current :root lines 119-135, Delight @font-face 112-117, body font 147-159, global `a` 164-168, focus-visible 173-178)
    - .planning/design-import/design-system/tokens/colors.css
    - .planning/design-import/design-system/tokens/typography.css
    - .planning/design-import/design-system/tokens/fonts.css
    - .planning/design-import/design-system/tokens/spacing.css
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Color, Typography sections)
  </read_first>
  <action>
    Run `npm install @fontsource/archivo-black` (approved in Task 1), then add `import '@fontsource/archivo-black';` to BaseLayout.astro's frontmatter (this bundles the woff2 + @font-face at build time — do NOT add a Google Fonts `<link>` or `@import`, per RESEARCH.md Pitfall 1).
    Delete the entire Delight `@font-face` block. Replace the `:root` block's color tokens with the exact values from the interfaces block above: base values (--gray-0, --gray-900, --pink-600), the four decorative --palette-* tokens (defined here but used only by the homepage carousel — D-05), semantic aliases (--color-dominant, --color-secondary both = var(--gray-0) per D-04, --color-accent, --color-ink, --color-border, --color-on-accent, --color-link, --color-link-hover, --color-focus-ring). Add the typography role tokens (--font-sans, --weight-regular:400, --weight-semibold:600, all --text-*-size/weight/leading/family) and --font-display: 'Archivo Black', var(--font-sans). Add the radii/border tokens (--radius-none, --radius-sm, --border-hairline, --border-focus, --focus-ring-offset, --tap-target-min). Keep the existing --space-* scale untouched. Remove --color-destructive entirely (D-03 — it is deleted, not replaced; ContactForm's remaining references are handled in Plan 04.1-03).
    Update `body`: font-family to `var(--font-sans)` (system stack — Archivo is display-only), font-weight to `var(--weight-regular)` (400, was 300), keep background var(--color-dominant) and color var(--color-ink).
    Replace the global `a` rule (D-06): `color: var(--color-accent)` (full pink text), `text-decoration: none` by default; on `:hover`/`:focus-visible` add `text-decoration: underline; text-decoration-thickness: 2px; text-underline-offset: 3px`. Remove the old ink-color + accent-underline pattern.
    Replace the `a:focus-visible, button:focus-visible` rule with a single pink ring: `outline: 2px solid var(--color-accent); outline-offset: var(--focus-ring-offset)` — remove the old double ink+accent box-shadow ring.
    Then delete the Delight tracking entry ("Delight variable font sourcing/licensing" section) from .planning/phases/02-portfolio-galleries/deferred-items.md (D-02).
  </action>
  <verify>
    <automated>npm run build && grep -c "Delight" src/layouts/BaseLayout.astro | grep -q '^0$' && grep -c "color-destructive" src/layouts/BaseLayout.astro | grep -q '^0$' && ! grep -rq "fonts.googleapis.com" src/ && grep -q "@fontsource/archivo-black" package.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@fontsource/archivo-black" package.json` matches (dependency added)
    - `import '@fontsource/archivo-black'` appears in BaseLayout.astro frontmatter
    - `grep -c "Delight" src/layouts/BaseLayout.astro` returns 0
    - `grep -c "color-destructive" src/layouts/BaseLayout.astro` returns 0
    - `grep -rn "fonts.googleapis.com" src/` returns no matches
    - `grep -c "F0E7E4\|E4D9D0\|F92D97\|141213" src/layouts/BaseLayout.astro` returns 0 (old Phase-2 hex values gone)
    - BaseLayout.astro `:root` contains `--color-accent: var(--pink-600)`, `--color-border: #E3E1DE`, `--color-on-accent`, all four `--palette-*`, and the `--text-*` role tokens
    - Global `a` rule sets `color: var(--color-accent)`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>New tokens, self-hosted Archivo Black, pink links, and single pink focus ring are live in BaseLayout; Delight is fully removed from code and deferred-items tracking; --color-destructive is gone from BaseLayout; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure chrome (white-on-white + hairline) and adopt the AJS logo with hover-invert + headerVariant prop</name>
  <files>src/layouts/BaseLayout.astro, public/logos/AJS_Brutalist_Black_Transparent.png, public/logos/AJS_Brutalist_White_Transparent.png</files>
  <read_first>
    - src/layouts/BaseLayout.astro (header/footer markup lines 84-104, .chrome-band + .site-title + .nav-link styles lines 195-260)
    - .planning/phases/04.1-design-system-homepage-refresh/04.1-UI-SPEC.md (Layout Notes → Header section)
    - .planning/design-import/homepage-prototype/Atelier Homepage.dc.html (lines 60-73 carousel top bar, 130-141 transparent gallery-detail header — reference only)
    - .planning/design-import/design-system/assets/logos/ (AJS_Brutalist_Black_Transparent.png, AJS_Brutalist_White_Transparent.png)
  </read_first>
  <action>
    Copy `AJS_Brutalist_Black_Transparent.png` and `AJS_Brutalist_White_Transparent.png` from `.planning/design-import/design-system/assets/logos/` into `public/logos/`.
    Add a `headerVariant?: 'solid' | 'transparent' | 'none'` prop to the `Props` interface (default `'solid'`), destructured in frontmatter.
    Render the header conditionally on `headerVariant`:
    - `'none'`: do not render the `<header>` at all (the homepage supplies its own overlaid top bar — Plan 04.1-04).
    - `'solid'` (default, all existing pages): white background, 1px `var(--color-border)` bottom hairline border, NO `--color-secondary` tinted band (D-04). Replace `<p class="site-title">` with a `<a class="logo-mark" href={homeHref} aria-label={siteTitle}>` containing two stacked `<img>`: the black transparent PNG (default, `.logo-mark__img--default`) and the white transparent PNG (`.logo-mark__img--hover`, `opacity:0`, absolutely positioned). On `.logo-mark:hover`/`:focus-within`, cross-fade the white variant to `opacity:1` (150ms ease) over a small dark `#1A1A1A` backdrop chip (logo bounding box + `var(--space-sm)` padding, `--radius-none`) that fades in simultaneously so the white PNG is legible on the white header (D-11, D-13, RESEARCH.md Pattern 3). Keep the existing `<nav class="site-nav">` (Home/Galleries/About/Contact) and `<LanguageSwitcher />`.
    - `'transparent'` (gallery-detail, Plan 04.1-05): header absolutely positioned (position:absolute; top/left/right:0; z-index above hero), transparent/gradient background, no hairline border; show the WHITE logo variant by default (over a dark scrim, no hover swap needed); nav text white. Same nav + switcher.
    Remove the `.chrome-band` background-color declaration from both header and footer; footer becomes white with a 1px `var(--color-border)` top hairline border (D-04). Update `.nav-link` and `.footer-text` font-weight from 300 to `var(--weight-regular)` (400). Delete the now-unused `.site-title` CSS rule.
    Set the logo `<img>` display width to ~48-56px (matches the prototype's 56px top-bar logo); `height:auto`.
    Note: the shared `<footer>` renders for all three variants (only the header branches) so the homepage keeps its footer.
  </action>
  <verify>
    <automated>npm run build && test -f public/logos/AJS_Brutalist_Black_Transparent.png && test -f public/logos/AJS_Brutalist_White_Transparent.png && grep -q "headerVariant" src/layouts/BaseLayout.astro && grep -q "logo-mark" src/layouts/BaseLayout.astro && npx playwright test tests/e2e/i18n.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `public/logos/AJS_Brutalist_Black_Transparent.png` and `public/logos/AJS_Brutalist_White_Transparent.png` both exist
    - BaseLayout `Props` interface includes `headerVariant?: 'solid' | 'transparent' | 'none'`
    - `grep -c "class=\"site-title\"\|class='site-title'" src/layouts/BaseLayout.astro` returns 0 (text title replaced by logo)
    - Header markup contains two logo `<img>` elements referencing the black and white transparent PNGs
    - No `background-color: var(--color-secondary)` remains on `.chrome-band`; footer has a `var(--color-border)` top border and header a `var(--color-border)` bottom border
    - `npx playwright test tests/e2e/i18n.spec.ts` passes — the header/logo change is exercised by `i18n.spec.ts`, which still finds a `<header>` containing "FR | EN" on `/` and `/en/` (solid variant preserves this; the homepage is not yet rebranded in this plan). The full `npm run test:e2e` suite runs at the wave-level gate per VALIDATION.md's Sampling Rate table, not per-task.
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The header shows the AJS logo with a working hover-invert on the solid variant; header/footer are white-on-white with hairline borders; BaseLayout accepts headerVariant with solid/transparent/none branches; the targeted i18n e2e spec stays green (full suite deferred to the wave-level gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | A new third-party package (`@fontsource/archivo-black`) enters the build/supply chain |
| visitor browser → third-party CDN | Avoided: fonts are self-hosted, so no visitor IP is transmitted to Google |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-SC | Tampering | `npm install @fontsource/archivo-black` | mitigate | Blocking `checkpoint:human-verify` (Task 1) before install; package is [ASSUMED] until a human confirms org/repo/downloads on npmjs.com. Not auto-approvable. |
| T-04.1-ID | Information Disclosure | Font loading | mitigate | Self-host via `@fontsource/archivo-black` instead of the design-import's Google Fonts CDN `@import` — keeps the already-shipped privacy policy's data-flow enumeration accurate (RESEARCH.md Pitfall 1 / Security Domain). Verified by `grep -rn fonts.googleapis.com src/` returning zero. |
</threat_model>

<verification>
- `npm run build` succeeds with the new tokens and self-hosted font.
- `npm run test:e2e` — full existing suite green (no hex-value assertions exist; header/footer role + "FR | EN" + locale-differing copy assertions still hold on the solid header).
- Manual visual sanity check (RESEARCH.md Pitfall 4): load a page, confirm headings do not render unexpectedly thin under Archivo Black's single weight. Logo hover-invert chip treatment: RESEARCH.md Open Question 1 is RESOLVED — the UI-SPEC default (dark `#1A1A1A` backdrop chip, logo bounding box + `var(--space-sm)` padding, `--radius-none`) is accepted as final for this phase; no live-site comparison gates this plan. The crossfade-between-two-PNGs mechanism is locked regardless.
</verification>

<success_criteria>
- Every existing page inherits the white/ink/pink identity (no Dawn Pink/Woodsmoke/Wild Strawberry values in BaseLayout).
- Links are full pink text; focus is a single pink ring.
- Header shows the AJS logo with hover-invert; header/footer are white-on-white with hairline borders.
- Archivo Black is self-hosted; no Google Fonts request; Delight is gone from code and tracking.
- `headerVariant` prop exists with solid/transparent/none behavior.
</success_criteria>

<output>
Create `.planning/phases/04.1-design-system-homepage-refresh/04.1-01-SUMMARY.md` when done.
</output>
