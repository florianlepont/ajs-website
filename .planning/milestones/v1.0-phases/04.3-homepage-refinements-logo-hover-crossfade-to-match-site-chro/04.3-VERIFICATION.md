---
phase: 04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro
verified: 2026-07-12T09:00:00Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
---

# Phase 04.3: Homepage Refinements Verification Report

**Phase Goal:** Homepage Refinements — logo hover crossfade, single gallery-browsing entry point (remove standalone /galleries listing, homepage grid is the sole browse surface), icon-based carousel/grid mode toggle, mobile hero-disappears bug fixed, three-line wordmark, clickable gallery names in carousel + grid modes, and the full e2e/unit suite green after the /galleries removal.
**Verified:** 2026-07-12
**Status:** passed
**Re-verification:** No — initial verification

## Verification Method

This report is based on independent evidence gathered directly from the codebase in this session, not on SUMMARY.md claims:
- Fresh `npm run build` run in this environment (SUMMARY.md claimed the sandbox `npm run build` was broken in the executor's worktree — independently re-ran it here and it succeeded cleanly, 17 pages, confirming that was an executor-worktree artifact, not a real defect).
- Fresh `npm run test:unit` (Vitest) and `npm run test:e2e` (Playwright) runs — both green, matching SUMMARY's claimed 47/47 e2e + 23/23 unit.
- Direct inspection of `dist/` build output (grep/regex on rendered HTML) for nav links, wordmark markup, hero-title href, CTA markup, toggle aria-labels, back-link copy.
- Three throwaway Playwright probes (written, run, and deleted in this session — not committed) to behaviorally verify computed CSS state (logo crossfade opacity in both display modes, before/after hover), CTA→grid-mode wiring and `is-active` target, and hero-title href/text sync across a carousel advance.
- `grep` sweeps across modified files for debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) — none found.
- `git status` confirms no stray artifacts left in the working tree from probes/builds run during verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Homepage logo crossfades to white-on-dark-chip on hover/focus in grid mode; carousel mode shows white logo with no hover swap | ✓ VERIFIED | Live Playwright probe on built site: carousel mode `default-img opacity=0, hover-img opacity=1` unconditionally; grid mode default `default=1, hover=0, chip=0`, and after `.home-logo.hover()` (250ms settle) `hover-img=1, chip=1`. CSS at `src/components/HomeCarousel.astro` lines 358-407 matches this exactly. |
| 2 | Carousel/grid toggle renders icon buttons whose accessible names stay Carrousel/Grille (Carousel/Grid on /en/) | ✓ VERIFIED | `dist/index.html` contains `aria-label="Carrousel"` / `aria-label="Grille"`; `dist/en/index.html` contains `aria-label="Carousel"` / `aria-label="Grid"`. Both buttons contain inline `<svg aria-hidden="true">` (HomeCarousel.astro lines 89-102), no visible text. `npm run test:e2e` toggle tests (homepage.spec.ts:49, gallery.spec.ts) resolve these buttons via `getByRole('button', { name: 'Grille'/'Carrousel' })` and pass. |
| 3 | Accent-panel CTA switches the homepage into grid mode instead of navigating to a URL | ✓ VERIFIED | `dist/index.html` has `<button type="button" class="home-hero__cta" data-action="show-grid">` (no href). Live probe: clicking the CTA reveals `[data-role="home-grid"]` and sets `is-active` on the real Grid toggle button (not on the CTA itself), confirming the plan's `setActiveToggle(gridToggleBtn)` wiring (HomeCarousel.astro lines 283-300) works as designed. |
| 4 | Wordmark renders as three lines (Atelier/Jacqueline/Suzanne) in both the carousel accent panel and the grid intro | ✓ VERIFIED | `dist/index.html`: `.home-hero__wordmark` and `.home-grid__wordmark` both contain `Atelier<br>Jacqueline<br>Suzanne` (two `<br>` each), confirmed via regex extraction of the built HTML. |
| 5 | Carousel hero gallery title is a link to that gallery's detail page and its href stays in sync as the carousel advances | ✓ VERIFIED | `dist/index.html`: `<a class="home-hero__title" data-role="gallery-title" href="/galleries/silos/">`. Live probe: clicking `[data-action="next"]` changes both the visible text (SILOS→BRUME) and the href (`/galleries/silos/`→`/galleries/brume/`) together, confirming `render()`'s `setAttribute('href', gallery.href)` (HomeCarousel.astro line 223) keeps them synced on every advance. |
| 6 | On a 375px-wide viewport the homepage hero is visibly rendered, not collapsed/blank | ✓ VERIFIED | `tests/e2e/homepage.spec.ts` "mobile hero visibility (D-08)" test (375×667 viewport) passes in a fresh run: `.home-hero` height > 300px, hero image visible with height > 0, and the CTA is clickable (not obscured) and triggers grid mode. Root cause (accent panel `position:static` dropping its z-index) documented and fixed at HomeCarousel.astro lines 707-716; desktop `height:100vh` preserved (line 481). |
| 7 | The homepage nav no longer contains a Galleries link (Home/Accueil, About/À propos, Contact only) | ✓ VERIFIED | `dist/index.html` has exactly 3 `.home-nav-link` anchors; no `>Galeries<` text found. `galleriesLabel`/`galleriesHref`/`ctaHref` consts removed from HomeCarousel.astro frontmatter. |
| 8 | There is no /galleries or /en/galleries listing page and no Galleries nav item anywhere on the site | ✓ VERIFIED | `src/pages/galleries/index.astro`, `src/pages/en/galleries/index.astro`, `src/components/GalleryCard.astro` all confirmed absent from disk. `dist/galleries/index.html` and `dist/en/galleries/index.html` not produced by the build (verified after a fresh `npm run build`). BaseLayout.astro's site-wide `.site-nav` renders exactly 3 `.nav-link` anchors (Home/About/Contact) — grep confirms `galleriesLabel`/`galleriesHref` consts and the Galleries `<a>` removed. |
| 9 | Gallery detail pages still exist and are reachable; their back-link returns to the homepage with matching copy | ✓ VERIFIED | `src/pages/galleries/[slug].astro` and `en` counterpart exist and build (`dist/galleries/{silos,brume,adults}/index.html` produced). Both set `backHref = getRelativeLocaleUrl(locale, '')`; built detail pages contain "Retour à l'accueil" (FR) / "Back home" (EN) — grepped directly from `dist/`. |
| 10 | The site builds with no unresolved imports after the deletions | ✓ VERIFIED | Fresh `npm run build` in this session exits 0, builds 17 pages, no import errors. (04.3-02-SUMMARY.md flagged this as unverifiable in its own sandbox and asked for confirmation elsewhere — this verification run supplies that confirmation.) |
| 11 | The full Playwright e2e suite and Vitest unit suite pass after the /galleries listing removal | ✓ VERIFIED | Fresh independent run: `npm run test:e2e` → 47/47 passed; `npm run test:unit` → 23/23 passed. Matches 04.3-03-SUMMARY.md's claimed counts exactly, independently reproduced. |
| 12 | gallery.spec.ts discovers a gallery via the homepage grid tile instead of the deleted listing page; grid tiles verified clickable (D-11) | ✓ VERIFIED | `tests/e2e/gallery.spec.ts` contains no `goto('/galleries/')` and no `voir la galerie` matcher; all three describe blocks bootstrap via `/` → `Grille` toggle → `.home-grid__tile`. First block explicitly asserts tile visibility + non-empty accessible name + href matching `/galleries/<slug>/` (D-11). All 3 blocks pass in the fresh e2e run. |
| 13 | About/Contact reachability specs reach their targets from a surviving BaseLayout page (not the deleted /galleries route) | ✓ VERIFIED | `about.spec.ts`'s "header nav links to the About page" test now originates from `/contact/`; `contact.spec.ts`'s "reach Contact from header nav" originates from `/about/`. No `/galleries/` goto remains in either file (grep-confirmed). Both tests pass. |

**Score:** 13/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/components/HomeCarousel.astro` | Logo crossfade, nav trim, CTA→button, icon toggle, 3-line wordmark, synced hero-title link, mobile fix | ✓ VERIFIED | All markup/CSS/script changes present and behaviorally confirmed (see truths 1-7, 5). |
| `tests/e2e/homepage.spec.ts` | New mobile-viewport (375×667) hero-visibility regression test | ✓ VERIFIED | "mobile hero visibility (D-08)" block present (lines 112-140), passes. |
| `src/pages/galleries/index.astro`, `src/pages/en/galleries/index.astro`, `src/components/GalleryCard.astro` | Deleted | ✓ VERIFIED | Confirmed absent from disk and from `dist/`. |
| `src/layouts/BaseLayout.astro` | Galleries nav link + consts removed | ✓ VERIFIED | Grep confirms only Home/About/Contact `.nav-link` anchors remain in `.site-nav`. |
| `src/pages/galleries/[slug].astro`, `src/pages/en/galleries/[slug].astro` | backHref → home, updated copy | ✓ VERIFIED | `getRelativeLocaleUrl(locale, '')` + "Retour à l'accueil"/"Back home" confirmed in built output. |
| `tests/e2e/gallery.spec.ts`, `about.spec.ts`, `contact.spec.ts` | Reconciled off deleted `/galleries/` route | ✓ VERIFIED | Grep confirms no `goto('/galleries/')` remains; all specs pass. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `.home-logo` mode-scoped CSS | BaseLayout `.logo-mark` crossfade mechanism | `data-display-mode` attribute selectors mirroring `.site-header--solid`/`--transparent` | ✓ WIRED | Behaviorally confirmed via live opacity probe in both modes. |
| Repurposed CTA (`data-action="show-grid"`) | Grid toggle button `is-active` state | `setActiveToggle(gridToggleBtn)` captured once, not `setActiveToggle(btn)` | ✓ WIRED | Live probe confirms Grid toggle (not CTA) receives `is-active` on CTA click. |
| Carousel hero title anchor | Data island `data-href` | Client `render()` calls `titleEl.setAttribute('href', gallery.href)` on every advance | ✓ WIRED | Live probe: href and text both update together on `next` click. |
| `gallery.spec.ts` discovery | HomeCarousel `.home-grid__tile` + 'Grille' toggle | Grid-discovery bootstrap pattern reused from `homepage.spec.ts` | ✓ WIRED | Confirmed in spec source and passing test run. |
| Gallery-detail back-links | Homepage | `getRelativeLocaleUrl(locale, '')` | ✓ WIRED | Confirmed in built `dist/galleries/<slug>/index.html`. |

### Requirements Coverage

No formal REQ-IDs were declared for this phase — ROADMAP.md and all three PLAN frontmatters explicitly state `requirements: [TBD]` with the note "UI/UX refinement on top of shipped PORT-01/PORT-02 coverage — no new REQ-IDs." Cross-referenced against `.planning/REQUIREMENTS.md`: PORT-01 and PORT-02 are both already marked Complete (attributed to Phase 2) and are unaffected by this phase's changes (gallery detail/lightbox functionality preserved, only the listing surface removed). No orphaned requirement IDs found mapped to Phase 04.3 in REQUIREMENTS.md.

### Anti-Patterns Found

None. Swept all 8 files modified across the three plans (`HomeCarousel.astro`, `homepage.spec.ts`, `BaseLayout.astro`, both `[slug].astro` detail pages, `gallery.spec.ts`, `about.spec.ts`, `contact.spec.ts`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` — zero matches.

### Human Verification Required

None. All must-haves were verifiable via automated build/test/grep/live-probe evidence; no PLAN.md `<human-check>` blocks were deferred to end-of-phase for this phase.

### Notes on Prior Sandbox-Reported Issues (Resolved by This Verification)

- 04.3-02-SUMMARY.md and its `deferred-items.md` flagged that `npm run build` could not complete in the executor's sandbox and asked for confirmation elsewhere (CI or a healthy local environment) before treating the phase as deploy-ready. This verification session ran a fresh `npm run build` outside that worktree and it succeeded cleanly (17 pages, 0 errors), corroborating 04.3-03-SUMMARY's own finding that the failure was an artifact of that specific worktree never having run `npm ci`, not a real toolchain incompatibility.

### Gaps Summary

None. All 13 merged must-haves (roadmap scope items + PLAN frontmatter truths) are verified against live codebase evidence: file existence, build output inspection, and behavioral Playwright probes — not SUMMARY.md narrative. Full e2e (47/47) and unit (23/23) suites pass on an independent run.

---

*Verified: 2026-07-12*
*Verifier: Claude (gsd-verifier)*
