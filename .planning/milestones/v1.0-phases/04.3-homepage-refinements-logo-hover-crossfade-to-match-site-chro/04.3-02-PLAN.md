---
phase: 04.3-homepage-refinements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/galleries/index.astro
  - src/pages/en/galleries/index.astro
  - src/components/GalleryCard.astro
  - src/layouts/BaseLayout.astro
  - src/pages/galleries/[slug].astro
  - src/pages/en/galleries/[slug].astro
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "There is no /galleries or /en/galleries listing page and no Galleries nav item anywhere on the site."
    - "Gallery detail pages still exist and are reachable; their back-link returns to the homepage with matching copy (Retour à l'accueil / Back home)."
    - "The site builds with no unresolved imports after the deletions."
  artifacts:
    - "Deleted: src/pages/galleries/index.astro, src/pages/en/galleries/index.astro, src/components/GalleryCard.astro"
    - "src/layouts/BaseLayout.astro (Galleries nav link + galleriesLabel/galleriesHref consts removed)"
    - "src/pages/galleries/[slug].astro and src/pages/en/galleries/[slug].astro (backHref → home, back-link labels updated)"
  key_links:
    - "GalleryGrid.astro and the [slug].astro detail routes are preserved — only the listing pages + dead GalleryCard are removed"
    - "backHref uses getRelativeLocaleUrl(locale, '') — the same base-aware home helper already used by existing home links, satisfying the CI base-path regression guard"
---

<objective>
Retire the standalone gallery browsing surface now that the homepage grid is the sole entry point: delete both /galleries listing pages and the now-dead GalleryCard component (D-03), remove the Galleries nav item from the site-wide BaseLayout header (D-04, layout half), and repoint the gallery-detail "back" link from the deleted listing route to the homepage with matching copy (D-06).

Purpose: Eliminate the redundant listing page and every internal link/nav that pointed at it, without breaking the detail pages that remain the browse target.
Output: Three files deleted, BaseLayout nav trimmed, and both `[slug].astro` back-links pointing home.
</objective>

## Artifacts this phase produces (this plan)

- Deleted files: `src/pages/galleries/index.astro`, `src/pages/en/galleries/index.astro`, `src/components/GalleryCard.astro`.
- `src/layouts/BaseLayout.astro`: removed `galleriesLabel` const (lines ~41-42), `galleriesHref` const (line 43), and the `<a class="nav-link">` Galleries link (line 116). Site-wide `.site-nav` now has three links.
- `src/pages/galleries/[slug].astro` + `src/pages/en/galleries/[slug].astro`: `backHref` now = `getRelativeLocaleUrl(locale, '')`; visible back-link text now "← Retour à l'accueil" (FR) / "← Back home" (EN).
- Preserved (explicitly NOT touched): `src/components/GalleryGrid.astro`, both `[slug].astro` route structures, the Sanity `navLabels.galleries` schema field.

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md
@.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-PATTERNS.md
@src/layouts/BaseLayout.astro
@src/pages/galleries/index.astro
@src/pages/galleries/[slug].astro
@src/pages/en/galleries/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the standalone listing pages + dead GalleryCard (D-03)</name>
  <files>src/pages/galleries/index.astro, src/pages/en/galleries/index.astro, src/components/GalleryCard.astro</files>
  <read_first>
    - src/pages/galleries/index.astro (FR listing — imports GalleryCard, GalleryGrid, EmptyState)
    - src/pages/en/galleries/index.astro (EN listing — same imports)
    - src/components/GalleryCard.astro (dead code once both listing pages are gone — imported only by them)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md (D-03 + the code_context integration notes: GalleryGrid must NOT be deleted; [slug].astro detail pages remain)
  </read_first>
  <action>
    Delete these three files entirely: `src/pages/galleries/index.astro`, `src/pages/en/galleries/index.astro`, and `src/components/GalleryCard.astro`. GalleryCard is imported only by the two listing pages being deleted, so it becomes dead code (project convention: delete confirmed-unused components rather than leave them).

    Do NOT delete `src/components/GalleryGrid.astro` — despite the similar name it is a generic layout wrapper still used by the gallery-detail pages' thumbnail grid. Do NOT delete either `[slug].astro` detail page or `src/components/EmptyState.astro` (EmptyState is still used elsewhere; only its use inside the deleted listing pages goes away with them).
  </action>
  <verify>
    <automated>test ! -f src/pages/galleries/index.astro && test ! -f src/pages/en/galleries/index.astro && test ! -f src/components/GalleryCard.astro && test -f src/components/GalleryGrid.astro && npm run build && test ! -f dist/galleries/index.html && test ! -f dist/en/galleries/index.html && ls dist/galleries/*/index.html >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - The three files no longer exist on disk.
    - `src/components/GalleryGrid.astro`, `src/pages/galleries/[slug].astro`, and `src/pages/en/galleries/[slug].astro` still exist.
    - `npm run build` exits 0 with no unresolved-import error.
    - Built output has no `dist/galleries/index.html` and no `dist/en/galleries/index.html`, but retains at least one `dist/galleries/<slug>/index.html` (detail routes still generated).
  </acceptance_criteria>
  <done>Standalone listing routes and the dead GalleryCard removed; GalleryGrid + detail routes intact; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove Galleries nav from BaseLayout (D-04) + repoint detail back-links home (D-06)</name>
  <files>src/layouts/BaseLayout.astro, src/pages/galleries/[slug].astro, src/pages/en/galleries/[slug].astro</files>
  <read_first>
    - src/layouts/BaseLayout.astro (galleriesLabel/galleriesHref consts lines 41-43; nav block lines 114-119, the Galleries `<a>` on line 116; confirm nothing else references galleriesHref/galleriesLabel before removing)
    - src/pages/galleries/[slug].astro (backHref line 35; back-link markup line 54)
    - src/pages/en/galleries/[slug].astro (backHref line 32; back-link markup line 51)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md (D-04, D-06)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-PATTERNS.md (nav removal + backHref update sections)
  </read_first>
  <action>
    Per D-04 (layout half): in BaseLayout.astro remove the site-wide nav anchor `<a href={galleriesHref} class="nav-link">{galleriesLabel}</a>` (line 116) and delete the now-unused `galleriesLabel` (lines 41-42) and `galleriesHref` (line 43) frontmatter consts. Leave `homeLabel`/`homeHref`, `aboutLabel`/`aboutHref`, and `contactLabel`/`contactHref` and their links untouched — the `<nav class="site-nav">` must end with exactly three links (Home/Accueil, About/À propos, Contact). Do NOT modify the Sanity `navLabels.galleries` schema field in sanity/schemas/siteSettings.ts (out of scope; leaving it unused is harmless and avoids a Studio migration).

    Per D-06 (both detail pages): change `const backHref = getRelativeLocaleUrl(locale, 'galleries')` to `const backHref = getRelativeLocaleUrl(locale, '')` in both `src/pages/galleries/[slug].astro` (line 35) and `src/pages/en/galleries/[slug].astro` (line 32). Update the visible back-link text so it no longer says "galleries": in the FR page change "← Retour aux galeries" to "← Retour à l'accueil" (line 54); in the EN page change "← Back to galleries" to "← Back home" (line 51). Keep the `.gallery-detail__hero-back` class and all CSS unchanged.
  </action>
  <verify>
    <automated>npm run build && grep -rq "Retour à l'accueil" dist/galleries/ && grep -rq "Back home" dist/en/galleries/</automated>
  </verify>
  <acceptance_criteria>
    - BaseLayout's `<nav class="site-nav">` renders exactly 3 `.nav-link` anchors; a Playwright `getByRole('link', { name: 'Galeries' })` on `/about/` resolves to 0 (and `{ name: 'Galleries' }` on `/en/about/` to 0).
    - Both `[slug].astro` files set `backHref = getRelativeLocaleUrl(locale, '')`; the FR detail hero back-link reads "Retour à l'accueil" and the EN one reads "Back home" (confirmed via grep of the built detail pages).
    - The back-link href resolves to the locale homepage root and, under the deploy base build, is base-prefixed (same `getRelativeLocaleUrl(locale, '')` helper already used by existing home links) — so the CI `href="/"`/`href="/en/"` base-path guard is not tripped.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Galleries nav removed site-wide; both detail back-links point to the homepage with matching non-stale copy; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static routing surface → visitor | Deleting routes reduces the public surface; the remaining detail routes and nav links are all internal, helper-generated URLs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-03 | Denial of service / broken link | Removed /galleries listing route | low | mitigate | Every internal link to the listing route is repointed in the same phase (BaseLayout nav + HomeCarousel nav/CTA in 04.3-01, both detail back-links here); no dangling `getRelativeLocaleUrl(locale, 'galleries')` remains. |
| T-04.3-04 | Tampering | backHref base-path correctness | low | mitigate | Reuse `getRelativeLocaleUrl(locale, '')` (base-aware) rather than a literal path, keeping the CI base-path regression guard satisfied. |
| T-04.3-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No package installs in this plan (pure deletions + edits). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run build` succeeds after each task with no unresolved-import errors.
- The listing routes are gone from `dist/` while detail routes remain; both detail back-links point home with updated copy.
- IMPORTANT: this plan intentionally does NOT run the full e2e suite. `tests/e2e/gallery.spec.ts`, `about.spec.ts`, and `contact.spec.ts` still navigate to the now-deleted `/galleries/` route and are reconciled in plan 04.3-03 (Wave 2); running the full suite here would fail on those un-migrated specs. Verify with build + route/file/grep checks only.
</verification>

<success_criteria>
D-03, D-04 (layout half), and D-06 are complete: the /galleries listing pages and dead GalleryCard are gone, the site-wide nav no longer offers Galleries, and gallery-detail back-links return to the homepage with matching copy — all while the build stays green and detail routes remain intact.
</success_criteria>

<output>
Create `.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-02-SUMMARY.md` when done.
</output>
