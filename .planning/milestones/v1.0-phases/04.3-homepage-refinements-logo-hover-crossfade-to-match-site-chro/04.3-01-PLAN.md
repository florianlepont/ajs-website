---
phase: 04.3-homepage-refinements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage.spec.ts
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "In grid mode, hovering or focusing the homepage logo crossfades to the white logo over a dark chip; in carousel mode the white logo shows with no hover swap."
    - "The carousel/grid toggle renders icon buttons whose accessible names stay Carrousel/Grille (Carousel/Grid on /en/)."
    - "The accent-panel CTA switches the homepage into grid mode instead of navigating to a URL."
    - "The wordmark renders as three lines (Atelier / Jacqueline / Suzanne) in both the carousel accent panel and the grid intro."
    - "The carousel hero gallery title is a link to that gallery's detail page and its href stays in sync as the carousel advances."
    - "On a 375px-wide viewport the homepage hero is visibly rendered, not collapsed/blank."
    - "The homepage nav no longer contains a Galleries link (Home/Accueil, About/À propos, Contact only)."
  artifacts:
    - "src/components/HomeCarousel.astro"
    - "tests/e2e/homepage.spec.ts"
  key_links:
    - ".home-logo data-display-mode-scoped hover rules mirror BaseLayout .logo-mark solid/transparent behavior"
    - "carousel hero title href stays in sync via a build-time data-island data-href read by render()"
    - "the repurposed CTA reuses the existing data-action=show-grid wiring and drives the Grid toggle is-active state"
---

<objective>
Apply all HomeCarousel.astro homepage refinements: logo hover crossfade (D-01/D-02), remove the homepage's own Galleries nav link (D-04, homepage half), repurpose the accent CTA into a grid-mode switch (D-05), swap the carousel/grid text toggle for icon buttons keeping the locked accessible names (D-07), fix the mobile hero-disappears bug (D-08), render the wordmark on three lines (D-09), and make the carousel hero gallery title a live link to its detail page (D-10).

Purpose: Polish the shipped homepage per Florian's live-use feedback without regressing the locked homepage.spec.ts contracts.
Output: An updated HomeCarousel.astro and a new mobile-hero regression test in homepage.spec.ts.
</objective>

## Artifacts this phase produces (this plan)

New/changed symbols in `src/components/HomeCarousel.astro`:
- CSS classes: `.home-logo__chip`, `.home-logo__img--default`, `.home-logo__img--hover` (replace the removed color-named `.home-logo__img--white` / `.home-logo__img--black`), plus new mode-conditional hover rules scoped to `.home[data-display-mode='grid']` / `.home[data-display-mode='carousel']`.
- Toggle buttons: inline `<svg aria-hidden="true">` icon children + `aria-label` carrying the locked toggle strings.
- CTA: the accent-panel CTA becomes `<button type="button" class="home-hero__cta" data-action="show-grid">` (no `href`).
- Hero title: `[data-role="gallery-title"]` becomes an `<a class="home-hero__title">`.
- Data island: new `data-href` attribute on each `<li>` in `<ul data-role="home-carousel-data">`.
- Client script: `href` field added to the `GalleryEntry` interface + dataset parse; `render()` now sets the title link's `href`; toggle-button references captured to keep is-active correct when the CTA triggers grid mode.
- Removed frontmatter consts: `galleriesLabel`, `galleriesHref`, `ctaHref`.
- Three-line wordmark markup inlined at `.home-hero__wordmark` and `.home-grid__wordmark` (the flat `wordmark` const is kept for the logo `aria-label`).

New test:
- `tests/e2e/homepage.spec.ts` gains a mobile-viewport (375×667) hero-visibility regression test.

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md
@.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-PATTERNS.md
@src/components/HomeCarousel.astro
@src/layouts/BaseLayout.astro
@tests/e2e/homepage.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logo crossfade, nav cleanup, CTA→grid button, icon toggle, three-line wordmark</name>
  <files>src/components/HomeCarousel.astro</files>
  <read_first>
    - src/components/HomeCarousel.astro (whole file — current logo markup lines 81-84 + CSS 327-344, nav lines 85-90 + consts 40/42/62, toggle buttons lines 92-93, CTA line 118 + `.home-hero__cta` CSS 520-525, wordmark const line 56 + render sites 116/124, client script show-carousel/show-grid wiring lines 258-274)
    - src/layouts/BaseLayout.astro (the `.logo-mark` source-of-truth: markup lines 109-113, CSS lines 315-367, including the `.site-header--solid` hover rules and `.site-header--transparent` default-white rules to mirror)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md (D-01, D-02, D-04, D-05, D-07, D-09)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-PATTERNS.md (logo hover, nav removal, CTA repurpose, icon toggle, wordmark sections)
    - tests/e2e/homepage.spec.ts (so the icon+aria-label and CTA→button changes do not break the toggle-name, carousel/grid, auto-advance, or i18n assertions)
  </read_first>
  <action>
    Per D-04 (homepage half): delete the homepage nav anchor with class `home-nav-link` whose href is `galleriesHref` (the Galleries link between the Home and About links). Remove the now-unused frontmatter consts `galleriesLabel` and `galleriesHref`. The `<nav class="home-nav">` must end with exactly three anchors — Home/Accueil, About/À propos, Contact.

    Per D-05: convert the accent-panel CTA from an anchor to a button. Replace the `<a href={ctaHref} class="home-hero__cta">{ctaLabel}</a>` element with `<button type="button" class="home-hero__cta" data-action="show-grid">{ctaLabel}</button>`, keeping the `ctaLabel` const/text. Remove the now-orphaned `ctaHref` const (it referenced the deleted `galleriesHref`). Add button-reset declarations to the `.home-hero__cta` rule so the button renders like the former link: `background:none; border:none; padding:0; cursor:pointer;` alongside the existing font-size 14px / `var(--weight-semibold)` / `text-decoration:underline` / `color:inherit`. In the client `<script>`, capture the two toggle buttons once (a `gridToggleBtn` = `root.querySelector('.home-toggle__btn[data-action="show-grid"]')` and a `carouselToggleBtn` = the show-carousel equivalent); change the existing show-grid click wiring to call `setActiveToggle(gridToggleBtn)` and the show-carousel wiring to call `setActiveToggle(carouselToggleBtn)` (instead of `setActiveToggle(btn)`), so that when the repurposed CTA — which also carries `data-action="show-grid"` and is picked up by the same `querySelectorAll('[data-action="show-grid"]')` loop — is clicked, the Grid toggle button (not the CTA) receives `is-active`.

    Per D-01/D-02: give `.home-logo` the BaseLayout `.logo-mark` opacity-crossfade + dark-chip mechanism, scoped by `data-display-mode` instead of a header-variant class. Restructure the anchor to contain, in order: a `<span class="home-logo__chip" aria-hidden="true"></span>`, then `<img ... class="home-logo__img home-logo__img--default">` using `logoBlackSrc`, then `<img ... class="home-logo__img home-logo__img--hover">` using `logoWhiteSrc` (both keep `alt=""`; the anchor keeps `aria-label={wordmark}`). Delete the old color-swap CSS (`.home-logo__img { display:none }` and the `--white`/`--black` per-mode display rules). Add crossfade CSS mirroring BaseLayout: `.home-logo` position:relative / inline-flex / align-items:center / text-decoration:none; `.home-logo__chip` position:absolute / inset calc(-1 * var(--space-sm)) / background-color var(--color-ink) / border-radius var(--radius-none) / opacity 0 / transition opacity 150ms ease / z-index 0; `.home-logo__img` position:relative / z-index 1 / display block / width 56px / height auto; `.home-logo__img--hover` position:absolute / inset 0 / opacity 0 / transition opacity 150ms ease. Grid mode mirrors `.site-header--solid`: add a rule so `.home[data-display-mode='grid'] .home-logo:hover .home-logo__chip`, `:focus-within .home-logo__chip`, `:hover .home-logo__img--hover`, and `:focus-within .home-logo__img--hover` all set opacity 1. Carousel mode mirrors `.site-header--transparent`: `.home[data-display-mode='carousel'] .home-logo__img--default { opacity:0 }` and `.home[data-display-mode='carousel'] .home-logo__img--hover { opacity:1 }`, with NO hover/focus rule (the white logo already reads over the existing gradient scrim).

    Per D-07: replace the text content of both `.home-toggle__btn` buttons with an inline `<svg aria-hidden="true" width="20" height="20">` icon whose paths use `fill="currentColor"` (or `stroke="currentColor"`) so the icon inherits the header's white (carousel) / ink (grid) color. Use a slideshow/single-frame glyph for the show-carousel button and a 2×2-squares glyph for the show-grid button (inline SVG, dependency-free — no icon library). Add `aria-label={toggleCarouselLabel}` to the show-carousel button and `aria-label={toggleGridLabel}` to the show-grid button. Do NOT change the `toggleCarouselLabel` / `toggleGridLabel` const values (Carrousel/Grille FR, Carousel/Grid EN) and keep `type="button"`, the `data-action` attributes, and the initial `is-active` on the carousel button. If needed for vertical alignment add `.home-toggle__btn svg { display:block }`.

    Per D-09: render the wordmark on three explicit lines at both render sites. Inside `<p class="home-hero__wordmark">` and `<p class="home-grid__wordmark">`, replace the `{wordmark}` expression with the literal text Atelier, Jacqueline, Suzanne each on its own line separated by two `<br />` elements. Leave the `wordmark` frontmatter const unchanged (still the flat single string) because it remains the `aria-label` on `.home-logo`.

    Do not add any import of src/lib/sanity or a `client:*` directive; the component stays a dependency-free island.
  </action>
  <verify>
    <automated>npm run build && npx playwright test homepage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `<nav class="home-nav">` renders exactly 3 `.home-nav-link` anchors; a Playwright `getByRole('link', { name: 'Galeries' })` on `/` resolves to 0 elements (and `{ name: 'Galleries' }` on `/en/` to 0).
    - `.home-logo` contains a `.home-logo__chip` span plus `.home-logo__img--default` (black src) and `.home-logo__img--hover` (white src); the removed `--white`/`--black` display-swap rules are gone.
    - In grid mode, focusing the logo (`:focus-within`) computes `.home-logo__chip` opacity to 1; in carousel mode the computed opacity of `.home-logo__img--hover` is 1 and `.home-logo__img--default` is 0, with no hover rule present for carousel mode.
    - Both toggle buttons have no rendered text but keep accessible names — `getByRole('button', { name: 'Grille' })` and `getByRole('button', { name: 'Carrousel' })` still resolve on `/` (and Grid/Carousel on `/en/`); each button contains an `<svg aria-hidden="true">`.
    - The accent CTA is a `<button type="button" data-action="show-grid" class="home-hero__cta">`; clicking it hides `[data-role="home-carousel"]`, shows `[data-role="home-grid"]`, and adds `is-active` to the Grid toggle button.
    - `.home-hero__wordmark` and `.home-grid__wordmark` each render Atelier / Jacqueline / Suzanne on three lines (two `<br>` children each); the `wordmark` const is unchanged and still supplies the logo `aria-label`.
    - All existing assertions in tests/e2e/homepage.spec.ts pass unmodified.
  </acceptance_criteria>
  <done>D-01/D-02/D-04(homepage)/D-05/D-07/D-09 implemented in HomeCarousel.astro; build green; homepage.spec.ts green with no weakened assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Clickable carousel hero title (D-10)</name>
  <files>src/components/HomeCarousel.astro</files>
  <read_first>
    - src/components/HomeCarousel.astro (server hero title markup line 111; data island `<ul data-role="home-carousel-data">` lines 149-153; client `GalleryEntry` interface + parse lines 156-172; `render()` lines 194-213, esp. the `titleEl.textContent` assignment; `.home-hero__title` CSS lines 478-485)
    - src/components/GalleryCard.astro (the `getRelativeLocaleUrl(locale, 'galleries/' + slug)` href precedent) — note this file is deleted in plan 04.3-02; read it here only as the href-shape reference
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md (D-10)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-PATTERNS.md (clickable hero title section + data-island field-threading shared pattern)
  </read_first>
  <action>
    Thread a build-time-precomputed detail href through the data island so the client script never needs the server-only `getRelativeLocaleUrl`. On each `<li>` inside `<ul data-role="home-carousel-data">`, add `data-href={getRelativeLocaleUrl(locale, `galleries/${gallery.slug}`)}` alongside the existing `data-title`, `data-hero-src`, and `data-alt` attributes (the server-side `GalleryEntry` already carries `slug`).

    Change the server-rendered hero title from an `<h2>` to an anchor: replace `<h2 class="home-hero__title" data-role="gallery-title">{firstGallery.title.toUpperCase()}</h2>` with `<a class="home-hero__title" data-role="gallery-title" href={getRelativeLocaleUrl(locale, `galleries/${firstGallery.slug}`)}>{firstGallery.title.toUpperCase()}</a>`. Keep the `.home-hero__title` class so its white Display styling applies — the class selector out-specifies the global `a { color: var(--color-accent) }` rule, so the title stays white.

    In the client `<script>`: add `href: string` to the `GalleryEntry` interface, and add `href: li.dataset.href ?? ''` to the `Array.from(dataEl.querySelectorAll('li')).map(...)` parse. In `render()`, where it sets `titleEl.textContent = gallery.title.toUpperCase()`, also call `titleEl.setAttribute('href', gallery.href)` so the link target stays in sync with the currently-displayed gallery on every prev/next/auto-advance (`titleEl` already resolves `[data-role="gallery-title"]`, now the anchor).

    Do not import `getRelativeLocaleUrl` or src/lib/sanity into the `<script>` — the href is computed at build time in frontmatter and read from `dataset.href`, preserving the build-time/browser boundary documented in the component header.
  </action>
  <verify>
    <automated>npm run build && npx playwright test homepage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Each `<li>` in `<ul data-role="home-carousel-data">` carries a `data-href` whose value matches `/\/galleries\/[^/]+\/?$/` for its gallery.
    - The hero title element is `<a data-role="gallery-title" class="home-hero__title">` with an initial `href` matching `/\/galleries\/[^/]+\/?$/`.
    - After a carousel advance (Playwright: click the next arrow, or fast-forward the mocked clock), the `[data-role="gallery-title"]` element's `textContent` (uppercased title) and its `href` slug correspond to the same displayed gallery.
    - Clicking the hero title navigates to a URL matching `/galleries/<slug>/`.
    - The client `<script>` reads `li.dataset.href` and calls `setAttribute('href', ...)` on the title; it contains no `getRelativeLocaleUrl` reference (that helper appears only in frontmatter).
    - `npm run build` exits 0 and homepage.spec.ts passes (carousel still shows the first migrated gallery text).
  </acceptance_criteria>
  <done>Carousel hero title is a working, per-render-synced link to the gallery detail page; data island carries data-href; build-time/browser boundary preserved; build + homepage.spec green.</done>
</task>

<task type="auto">
  <name>Task 3: Fix mobile hero-disappears bug + add regression guard (D-08)</name>
  <files>src/components/HomeCarousel.astro, tests/e2e/homepage.spec.ts</files>
  <read_first>
    - src/components/HomeCarousel.astro (`.home` line 292; base `.home-hero` lines 414-418 = `height:100vh; overflow:hidden`; `.home-hero__img` lines 420-426 = `position:absolute; inset:0; height:100%`; mobile `@media (max-width:767px)` block lines 592-620 = `.home-hero { height:auto; min-height:600px; overflow:visible }`; client `render()` lines 194-213 which sets `heroImg.src` at runtime)
    - .planning/debug/resolved/homepage-visual-bugs.md (the PRIOR, resolved fix — 420→560→600 mobile min-height and the desktop 100vh structural fix; do NOT just re-derive this, D-08 is a distinct still-open symptom)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-CONTEXT.md (D-08)
    - .planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-PATTERNS.md (mobile hero visibility bug section + investigation candidates)
    - tests/e2e/homepage.spec.ts (add a new test block; do not weaken existing assertions)
  </read_first>
  <action>
    Reproduce first, then fix. At a 375×667 viewport, load `/` and measure the bounding boxes of `.home-hero` and its `[data-role="hero-image"]` to confirm the hero collapses/blanks before changing anything — do NOT guess another min-height constant (the debug session already exhausted 420/560/600 and removed the desktop floor).

    Root-cause among these concrete candidates and pick the one the live measurement proves: (a) on mobile `.home-hero` is `height:auto; min-height:600px` while `.home-hero__img` is `position:absolute; inset:0; height:100%` — a percentage height on an absolutely-positioned child resolves against the parent's computed height, and when that parent height is `auto` (only `min-height` + the now-static `.home-hero__accent` give it size) the image's `height:100%` can resolve to auto/0 and collapse; (b) the `.home` ancestor (line 292) sets only `position:relative` with no height, providing no definite containing-block height; (c) the hero `<img>` has no intrinsic `width`/`height` attributes and its `src` is assigned by `render()` after paint, so it can momentarily report ~0 height.

    Apply the minimal fix that makes the hero image fill the visible hero box at phone widths, WITHOUT reintroducing any fixed desktop height floor (the desktop `.home-hero` must stay `height:100vh` — the debug session's structural fix). Acceptable fix directions include: ensuring `.home-hero` has a definite resolvable height on mobile so the absolute image's `height:100%` resolves (e.g. sizing the image against the mobile hero via `min-height:100%`/`object-fit`), and/or adding explicit intrinsic `width`/`height` attributes to the hero `<img>`. Choose based on the measurement; record the identified root cause and the exact change in the SUMMARY, explicitly distinct from the prior 600px min-height fix.

    Add a permanent regression assertion to tests/e2e/homepage.spec.ts as a NEW `test(...)` block (do not modify existing assertions): set the viewport to 375×667, navigate to `/`, and assert that `.home-hero` has a bounding-box height greater than 300px AND that `[data-role="hero-image"]` is visible with a bounding-box height greater than 0 (image actually occupies the hero, not collapsed). This closes the debug session's noted blind spot that no automated "hero visible on mobile" guard existed.
  </action>
  <verify>
    <automated>npm run build && npx playwright test homepage.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - homepage.spec.ts contains a new mobile-viewport (375×667) test that navigates to `/` and asserts `.home-hero` height > 300px and `[data-role="hero-image"]` visible with height > 0 — and it passes after the fix.
    - The base `.home-hero` rule still contains `height: 100vh` (positive grep) — no fixed desktop height/min-height floor was reintroduced.
    - The SUMMARY records which root-cause candidate applied and the exact CSS/markup change, called out as distinct from the prior 600px min-height fix.
    - All pre-existing homepage.spec.ts assertions (carousel/grid toggle, auto-advance, i18n switcher) still pass.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Homepage hero renders visibly at phone widths, proven by an emulated-375px automated regression guard; root cause documented; desktop 100vh fix preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time (Sanity/getGalleries) → static markup | Gallery data (title, slug, alt, image URLs) is fetched and filtered by the calling page at build time and baked into static HTML + the hidden data island. |
| Static markup/data island → browser `<script>` | The client island reads only the pre-baked `data-*` attributes; it never imports src/lib/sanity, so SANITY_API_READ_TOKEN never reaches the browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.3-01 | Information disclosure | HomeCarousel client `<script>` (D-10 data-href) | low | mitigate | Keep the build-time/browser boundary: precompute `data-href` in frontmatter, read `dataset.href` in the script; add no src/lib/sanity import or `client:*` directive (asserted in Task 2 acceptance). |
| T-04.3-02 | Tampering | Data-island `data-href`/`data-title` values | low | accept | Values derive from Sanity slugs/titles (already public content) and Astro auto-escapes attribute interpolation; no user-supplied input crosses this boundary. |
| T-04.3-SC | Tampering (supply chain) | npm/pip/cargo installs | n/a | accept | No package installs in this plan — icon toggle uses inline dependency-free SVG (Claude's Discretion, no icon library). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run build` succeeds after each task.
- `npx playwright test homepage.spec.ts` passes after every task, including the new mobile-hero regression test — all pre-existing toggle-name / carousel-grid / auto-advance / i18n assertions remain green and unmodified (D-07 lock).
- The full e2e + unit green gate runs in plan 04.3-03 (Wave 2), after the /galleries removal test reconciliation lands; this plan intentionally scopes its verify to homepage.spec.ts to avoid racing the unrelated gallery/about/contact specs during Wave 1.
</verification>

<success_criteria>
All six HomeCarousel decisions (D-01, D-02, D-04-homepage, D-05, D-07, D-08, D-09, D-10) are implemented in HomeCarousel.astro; the homepage builds and homepage.spec.ts (with the added mobile guard) is green.
</success_criteria>

<output>
Create `.planning/phases/04.3-homepage-refinements-logo-hover-crossfade-to-match-site-chro/04.3-01-SUMMARY.md` when done.
</output>
