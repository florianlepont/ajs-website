---
phase: 03-about-contact
plan: 03
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/pages/about.astro
  - src/pages/en/about.astro
  - tests/e2e/about.spec.ts
  - .planning/phases/03-about-contact/03-VERIFICATION.md
autonomous: false
requirements: [ABOUT-01, ABOUT-02]

must_haves:
  truths:
    - "A visitor reading /about/ (FR) and /en/about/ (EN) learns real, substantive information about Romane's background/artistic approach — the bio paragraph is Romane-supplied copy, NOT the 'sera bientôt disponible' / 'will be available here soon' placeholder — OR the placeholder launch is explicitly recorded as human-approved (accepted_by + accepted_at) in 03-VERIFICATION.md frontmatter."
    - "A visitor learns where Romane works / her atelier practice — the atelier/studio paragraph is real Romane-supplied copy, NOT the 'à venir prochainement' / 'coming soon' placeholder — OR the extended placeholder scope is explicitly recorded as human-approved in 03-VERIFICATION.md frontmatter."
    - "The ABOUT-01 and ABOUT-02 verification gaps are closed by an explicit, human-authored outcome (real content wired FR+EN, or a timestamped override) — never by silently re-accepting the existing unilateral placeholder."
    - "tests/e2e/about.spec.ts asserts the shipped state: real-copy substrings when content is provided, or unchanged placeholder assertions when the override path is chosen; the full e2e suite stays green."
  artifacts:
    - path: "src/pages/about.astro"
      provides: "FR About page carrying the human-resolved bio + atelier/studio content (real copy or explicitly-approved placeholder)"
      contains: "À propos"
    - path: "src/pages/en/about.astro"
      provides: "EN About page mirror of the resolved content"
      contains: "About"
    - path: "tests/e2e/about.spec.ts"
      provides: "Locale-pair assertions matching the shipped (resolved) About copy"
    - path: ".planning/phases/03-about-contact/03-VERIFICATION.md"
      provides: "Override record IF and only IF Florian chose the placeholder-launch path"
  key_links:
    - from: "Task 1 checkpoint outcome (Florian's decision)"
      to: "src/pages/about.astro / en/about.astro paragraphs"
      via: "Task 2 applies the resolved copy or records the override"
      pattern: "À propos"
---

<objective>
Close the two ABOUT verification gaps (03-VERIFICATION.md): the About page currently ships zero real bio/atelier content, and the placeholder scope was widened unilaterally during planning beyond what Florian actually approved (D-06 only covered medium/technique). Research (03-RESEARCH.md Open Questions) confirmed definitively that NO reusable bio or atelier text exists anywhere — not on the live Myportfolio site, not in the codebase — so this gap cannot be closed by "migrating existing content." It can only be closed by an explicit human decision that was never made: either Florian supplies real About copy, or he explicitly signs off on launching with clearly-marked placeholder content.

This plan makes that decision explicit (a blocking human-action checkpoint) and then wires the outcome end-to-end so the About page is a complete, verifiable slice — real content live in both locales, or a recorded, timestamped override. It must NOT silently re-approve the existing placeholder.

Purpose: Delivers Success Criteria 1 and 2 of Phase 3 for real (visitor can actually read who Romane is and where she works), closing the ABOUT-01/ABOUT-02 blockers.
Output: Updated `src/pages/about.astro`, `src/pages/en/about.astro`, `tests/e2e/about.spec.ts` matching the shipped copy, and — only on the override path — an `overrides:` block in `03-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** prospective visitor or collaborator, **I want to** read about Romane's background and artistic/atelier practice on the site, **so that** I understand who she is before reaching out or considering her work.

<context>
@.planning/ROADMAP.md
@.planning/phases/03-about-contact/03-CONTEXT.md
@.planning/phases/03-about-contact/03-VERIFICATION.md
@.planning/phases/03-about-contact/03-UI-SPEC.md

<interfaces>
<!-- Current shipped state the executor is modifying. Extracted from the codebase — no exploration needed. -->

From src/pages/about.astro (FR — the file to edit) — current placeholder structure, D-02 single flowing page, no boxed panels:
- `<h1>À propos</h1>` (Display role — keep unchanged).
- Bio paragraph `<p class="placeholder">Le texte de présentation de Romane sera bientôt disponible ici — en attente de sa version définitive.</p>` — THIS is the ABOUT-01 gap; replace with real copy or keep only if override chosen.
- `<h2>Atelier &amp; pratique</h2>` (Heading role — keep unchanged).
- Atelier/studio paragraph `<p class="placeholder">Informations sur l'atelier et la pratique de Romane à venir prochainement.</p>` — THIS is the ABOUT-02 "where she works" gap that exceeded D-06 scope; replace with real copy or keep only if override chosen.
- Medium/technique paragraph `<p class="placeholder">Précisions sur le médium et la technique à venir — en attente de confirmation avec l'artiste.</p>` — this is the LOCKED D-06 placeholder; it stays placeholder unless Florian also supplies medium/technique text.
- Scoped `<style>`: `.about-page` wrapper (max-width: 640px), Display `.about-page h1`, Heading `.about-page h2`, Body `.about-page p`, and `.about-page .placeholder { font-style: italic; }` — reuse these tokens/classes; do NOT introduce new hex/px where a token exists.

From src/pages/en/about.astro (EN mirror) — byte-identical structure to the FR file, only copy strings + import depth differ:
- Bio: "Romane's biography will be available here soon — pending her final text."
- Atelier/studio: "Information about Romane's studio and practice is coming soon."
- Medium/technique (LOCKED D-06): "Details on medium and technique coming soon — pending confirmation with the artist."

From tests/e2e/about.spec.ts (the suite to update) — asserts the placeholder strings byte-exact today; the copy-differs-between-locales test and the "header nav links to the About page" reachability test must be preserved unchanged. Only the per-paragraph copy assertions change when real content ships.

Convention: `.placeholder` (italic) is the site's sole visual marker for not-yet-final content (03-UI-SPEC.md Layout Notes). Real, final copy must NOT carry the `.placeholder` class — dropping it is how a paragraph signals "this is real content now."
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Florian resolves the About content decision (provide real copy OR approve placeholder launch)</name>
  <read_first>
    - .planning/phases/03-about-contact/03-VERIFICATION.md (the two FAILED gaps and the verifier's override-block template — the exact resolution this checkpoint exists to make)
    - .planning/phases/03-about-contact/03-DISCUSSION-LOG.md (Florian's original "Reuse existing text" choice + D-06's narrow "placeholder for medium/technique only" scope)
    - .planning/phases/03-about-contact/03-RESEARCH.md (Open Questions: confirms no reusable bio/atelier text exists anywhere — this is why a human decision is unavoidable)
  </read_first>
  <what-built>
    The About page (`/about/`, `/en/about/`) is fully built and wired — routes, nav link, bilingual structure, spacing, typography all pass. The ONLY thing missing is real prose: the bio and atelier/studio paragraphs are placeholder text. Verification (03-VERIFICATION.md) found this placeholder scope was widened during planning beyond what you approved: you originally chose "Reuse existing text" for the bio (but research found there is no existing text to reuse), and you only approved a placeholder for the medium/technique detail (D-06) — not for the bio or the broader "where she works" atelier paragraph.
  </what-built>
  <decision-required>
    Choose ONE path. Reply in this checkpoint with the option and, for Path A, the copy.

    PATH A — Provide real About copy (recommended; this is what the phase goal actually promises).
    Supply, in French AND English (FR is authored first; EN is a proper translation, not machine-literal), at minimum:
      1. Bio / background + artistic approach — one or more short paragraphs (ABOUT-01).
      2. Atelier / practice — where you work / your studio practice — at least one paragraph (ABOUT-02, the "where she works" portion).
      3. OPTIONAL: medium / technique specifics (analog vs digital, process). If you don't want to commit yet, leave this one as the clearly-marked placeholder — that part was legitimately approved (D-06) and can stay.
    Paste the FR and EN text directly. No formatting rules — plain paragraphs; the executor will wire them into both pages verbatim.

    PATH B — Explicitly approve launching Phase 3 with the current clearly-marked placeholder About copy.
    This is a real, allowed choice (real content becomes a content follow-up, not a code blocker) — but it MUST be your explicit, recorded sign-off, not a silent re-acceptance of a decision made for you during planning. If you choose this, the executor records a timestamped override in 03-VERIFICATION.md under your name.
  </decision-required>
  <resume-signal>
    Reply either: (A) "Path A" followed by your FR and EN bio + atelier copy, or (B) "Path B — I approve launching with placeholder About content, signed <your name>".
  </resume-signal>
  <acceptance_criteria>
    - The checkpoint blocks execution until Florian replies with an explicit Path A (copy provided) or Path B (signed placeholder approval).
    - No code or override is written before this reply exists — the existing placeholder is never re-accepted implicitly.
    - For Path A: the reply contains distinct FR and EN text for the bio AND the atelier/studio sections.
    - For Path B: the reply contains an explicit approval phrase and a name to record as `accepted_by`.
  </acceptance_criteria>
  <done>Florian has explicitly chosen Path A (with real FR+EN copy) or Path B (with a signed placeholder approval), captured in the checkpoint response.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the resolved outcome — wire real copy end-to-end, or record the override</name>
  <files>src/pages/about.astro, src/pages/en/about.astro, tests/e2e/about.spec.ts, .planning/phases/03-about-contact/03-VERIFICATION.md</files>
  <read_first>
    - src/pages/about.astro (FR page — the placeholder paragraphs and scoped style to edit)
    - src/pages/en/about.astro (EN mirror — keep structure byte-identical to FR, only copy + import depth differ)
    - tests/e2e/about.spec.ts (assertions to update to match the shipped copy)
    - .planning/phases/03-about-contact/03-VERIFICATION.md (override-block YAML template to copy exactly, for Path B)
    - .planning/phases/03-about-contact/03-UI-SPEC.md (Layout Notes → About Page: Body role for real paragraphs, italic-only for remaining placeholders, no box/border/background per D-02)
  </read_first>
  <action>
    Branch on Task 1's outcome.

    PATH A (Florian provided real copy):
    - In src/pages/about.astro, replace the bio placeholder paragraph text with Florian's FR bio copy, and the atelier/studio placeholder paragraph text with his FR atelier copy. Split into multiple `<p>` if he supplied multiple paragraphs. REMOVE the `class="placeholder"` attribute from every paragraph now carrying real, final copy (per the UI-SPEC convention: italic `.placeholder` marks not-yet-final content only). Keep `<h1>À propos</h1>` and `<h2>Atelier &amp; pratique</h2>` unchanged. If Florian ALSO supplied medium/technique copy, replace the D-06 paragraph and drop its `.placeholder`; if he did NOT, keep the D-06 medium/technique paragraph exactly as-is (locked, still `class="placeholder"` italic). Do not infer analog/film/darkroom.
    - Mirror the identical edits into src/pages/en/about.astro using Florian's EN copy. FR and EN files stay byte-identical except copy strings, `<h1>`/`<h2>` labels, and import depth. Reuse existing tokens/classes only — no new hex/px, no boxed panels (D-02), no portrait (D-03).
    - Update tests/e2e/about.spec.ts: in the FR and EN content tests, replace the now-stale bio and atelier placeholder-string assertions with `toContainText` assertions on a distinctive verbatim substring of Florian's real copy (choose a phrase unlikely to collide, ≥ 4 words). Additionally assert the page does NOT contain the old placeholder markers — `await expect(main).not.toContainText('sera bientôt disponible')` (FR) and `await expect(main).not.toContainText('will be available here soon')` (EN) — proving real content shipped. Preserve unchanged: the `<h1>` assertion, the `Atelier & pratique`/`Studio & practice` subheading visibility, the copy-differs-between-locales test, and the "header nav links to the About page" reachability test. Keep the D-06 medium/technique assertion ONLY if that paragraph remained placeholder.
    - Do NOT edit 03-VERIFICATION.md on this path (the gap is closed by real content, not an override).

    PATH B (Florian approved placeholder launch):
    - Do NOT change the page copy — it stays the clearly-marked italic placeholder, now explicitly sanctioned.
    - Append an `overrides:` block to 03-VERIFICATION.md frontmatter using the exact template already drafted in that file's Gaps Summary, one entry per FAILED must-have (bio + atelier/practice). Set `reason` to reference this signed decision, `accepted_by` to the name Florian gave, and `accepted_at` to the current ISO-8601 timestamp.
    - Leave tests/e2e/about.spec.ts unchanged (the placeholder assertions still match reality and stay green).
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/about.spec.ts && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `npx playwright test tests/e2e/about.spec.ts` passes, and the full `npm run test:e2e` suite stays green (nav/switcher/gallery/contact unaffected).
    - Path A — Source: `grep -q "sera bientôt disponible" src/pages/about.astro` returns NON-zero (the FR bio placeholder is gone), and `grep -q "will be available here soon" src/pages/en/about.astro` returns NON-zero (EN bio placeholder gone). The atelier/studio placeholder strings ("à venir prochainement" / "coming soon") are likewise absent unless Florian explicitly left that section as placeholder.
    - Path A — Source: about.astro and en/about.astro each contain a distinctive verbatim substring of Florian's supplied FR/EN copy respectively; paragraphs carrying real copy no longer have `class="placeholder"`.
    - Path A — Source: tests/e2e/about.spec.ts asserts `not.toContainText` on the old bio placeholder markers and `toContainText` on the real-copy substrings; the copy-differs and nav-reachability tests are still present.
    - Path B — Source: 03-VERIFICATION.md frontmatter contains an `overrides:` block with two entries (bio + atelier/practice), each with a non-empty `accepted_by` and a valid ISO `accepted_at`; page copy is unchanged.
    - Either path: neither page introduces a portrait (D-03) or a boxed/bordered panel (D-02); no new hardcoded hex/px where a design token exists.
  </acceptance_criteria>
  <done>The About gap is closed by exactly one path: real bio + atelier copy live in FR and EN with tests updated to match, OR a signed, timestamped override recorded in 03-VERIFICATION.md. The e2e suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → static HTML | About content is hardcoded and prerendered at build; no request-time compute, no untrusted input crosses any boundary on this page. |
| human → repo content | Florian-supplied prose is authored copy pasted into `.astro` templates by the executor; it is static text, not executable input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | about.astro / en/about.astro content | accept | Content is intentionally public bio/marketing copy supplied by the site owner; contains no PII beyond what Romane chooses to publish, no secrets. |
| T-03-03-02 | Tampering | Florian-supplied copy rendered as Astro markup | mitigate | Executor pastes copy as text node content within `<p>` elements (not raw `set:html`), so any stray `<`/`&` is auto-escaped by Astro; no HTML injection surface introduced. |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (zero new dependencies — content/test edits only). Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `npm run test:e2e` green (about.spec.ts updated to match shipped copy + existing i18n/gallery/contact suites unaffected).
- `npm run build` succeeds (static output, no Sanity fetch added for About).
- Manual spot-check: `/about/` and `/en/about/` render the resolved content (real prose on Path A, or unchanged clearly-marked placeholder on Path B).
- Path B only: `03-VERIFICATION.md` frontmatter carries the `overrides:` block with `accepted_by` + `accepted_at`.
</verification>

<success_criteria>
- ABOUT-01: A visitor can read real bio/background content in FR and EN — OR the placeholder launch is explicitly, verifiably human-approved (recorded override), never silently re-accepted.
- ABOUT-02: A visitor can read real atelier/practice ("where she works") content in FR and EN beyond the locked D-06 medium/technique placeholder — OR the extended placeholder scope is explicitly human-approved.
- The gap-closure outcome is traceable to Florian's explicit decision (checkpoint response + code diff or override record).
- About e2e assertions match the shipped state; the full e2e suite stays green.
</success_criteria>

<output>
Create `.planning/phases/03-about-contact/03-03-SUMMARY.md` when done. Record which path (A real copy / B override) was taken and why.
</output>
