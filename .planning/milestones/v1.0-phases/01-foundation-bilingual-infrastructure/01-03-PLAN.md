---
phase: 01-foundation-bilingual-infrastructure
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - src/lib/sanity.ts
  - sanity/package.json
  - sanity/sanity.config.ts
  - sanity/sanity.cli.ts
  - sanity/schemas/index.ts
  - sanity/schemas/siteSettings.ts
autonomous: false
requirements: [I18N-01]
user_setup:
  - service: Sanity
    why: "Headless CMS that stores the locale-aware site-settings singleton (D-09); creating a project requires a Sanity account login Claude cannot perform"
    env_vars:
      - name: SANITY_PROJECT_ID
        source: "Sanity project (created via `sanity init`) → Project ID"
      - name: SANITY_DATASET
        source: "Sanity project → dataset name (e.g. 'production')"
      - name: SANITY_API_READ_TOKEN
        source: "Sanity → API → Tokens → create a Viewer (read-only) token for build-time fetches"
    dashboard_config:
      - task: "Create the Sanity project + dataset via `sanity init` (browser login) and publish the siteSettings singleton document with FR + EN copy"
        location: "Sanity Studio (local `sanity dev`) and sanity.io/manage"

must_haves:
  truths:
    - "A Sanity project + dataset exists and is reachable with a read-only token"
    - "A `siteSettings` singleton schema defines locale-aware fields for site title, nav labels, footer text, and the homepage placeholder welcome (heading + body), each with FR and EN values (D-09)"
    - "The singleton document is published in Sanity with real FR + EN placeholder copy that matches the UI-SPEC copywriting contract"
    - "`src/lib/sanity.ts` exposes a build-time `getSiteSettings()` helper returning the singleton's typed content for both locales"
  artifacts:
    - path: "sanity/schemas/siteSettings.ts"
      provides: "Locale-aware site-settings singleton schema (D-09)"
      contains: "siteSettings"
    - path: "src/lib/sanity.ts"
      provides: "Build-time @sanity/client instance + getSiteSettings() typed fetch helper"
      exports: ["getSiteSettings"]
    - path: ".env.example"
      provides: "Documented env var names for Sanity project/dataset/token (no real values)"
      contains: "SANITY_PROJECT_ID"
  key_links:
    - from: "src/lib/sanity.ts"
      to: "Sanity Content Lake API"
      via: "@sanity/client build-time fetch with SANITY_API_READ_TOKEN"
      pattern: "createClient"
---

<objective>
Stand up the CMS half of the Walking Skeleton: create the Sanity project, define the durable `siteSettings` locale-aware singleton (D-09 — nav labels, footer text, site title, and the homepage placeholder welcome, all with FR + EN values), publish real placeholder copy, and expose a build-time `getSiteSettings()` fetch helper. This is real infrastructure Phase 2+ keeps using, not throwaway scaffolding.

Purpose: Prove the "one real DB read" leg of the skeleton and provide the typed content contract (`getSiteSettings()`) that Plan 04's UI slice consumes to render Sanity-sourced bilingual chrome.
Output: A live Sanity project, a published bilingual singleton, and a typed build-time fetch helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-PATTERNS.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Sanity project + dataset (account login)</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Standard Stack lines 60-102; Assumption A4 line 390 verify v6 schema syntax live; Environment Availability line 420)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md (D-09 singleton)
  </read_first>
  <what-built>
    Claude runs `npm create sanity@latest` / `sanity init` to scaffold the Studio project, but the step pauses at the browser-based Sanity account login, which only Florian can complete. After login, Claude continues with schema work autonomously.
  </what-built>
  <how-to-verify>
    1. Complete the Sanity login in the browser when `sanity init` prompts (free plan, no cost — RESEARCH.md confirms free-tier headroom).
    2. Choose/confirm a project name and a dataset named `production`.
    3. After creation, record the Project ID and dataset name from `sanity.io/manage`.
    4. Create a read-only (Viewer) API token under Sanity → API → Tokens for build-time fetches; capture it for GitHub Secrets in Plan 05 and a local `.env` — do NOT commit the token or paste it into any planning file.
  </how-to-verify>
  <acceptance_criteria>
    - A Sanity project + `production` dataset exist (visible in sanity.io/manage).
    - Project ID and dataset name are recorded for `.env` / GitHub Secrets.
    - A Viewer (read-only) token is created and captured out-of-band.
  </acceptance_criteria>
  <resume-signal>Reply with the Sanity Project ID and dataset name (token stays secret) once the project exists.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Define the siteSettings locale-aware singleton schema and apply it</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-PATTERNS.md (siteSettings.ts assignment lines 121-124 — no vetted excerpt, pull live Sanity v6 singleton pattern)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-UI-SPEC.md (Phase Scope, Copywriting Contract, Layout Notes — which chrome strings the schema must carry)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md (D-09, D-10, D-11)
    - sanity/ project files generated in Task 1 (sanity.config.ts, schemas/)
  </read_first>
  <action>
    Using the current Sanity v6.3.0 singleton-document pattern (verify the exact schema/config API live via Context7 or Sanity's own docs at execution time per RESEARCH.md Assumption A4 — do NOT copy a possibly-stale v3/v4 tutorial), create `sanity/schemas/siteSettings.ts` defining a document type `siteSettings` intended as a singleton (single editable instance). Fields must carry BOTH French and English values for: `siteTitle`, `navLabels` (the header nav labels — even if minimal this phase), `footerText` (copyright/site-name line), and the D-10 homepage placeholder `welcomeHeading` and `welcomeBody`. Represent each localized field as an object with `fr` and `en` string sub-fields (a simple, durable shape Phase 2+ reuses; do not introduce the document-i18n plugin — RESEARCH.md Alternatives judges it overkill for one type this phase).
    Register the schema in `sanity/schemas/index.ts` and wire it into `sanity/sanity.config.ts`. Enforce singleton behavior using the current Sanity approach (e.g. structure/desk customization limiting `siteSettings` to a single document, per live docs).
    Apply/deploy the schema so it is not a silent gap: publish it to the Studio (run the Studio locally with `sanity dev` and/or `sanity deploy` per the live Sanity v6 flow — Sanity applies schema via the Studio, not an ORM migration). Confirm the `siteSettings` type appears in the running Studio.
  </action>
  <verify>
    <automated>grep -q "siteSettings" sanity/schemas/siteSettings.ts && grep -rq "siteSettings" sanity/schemas/index.ts && node -e "console.log(require('fs').existsSync('sanity/sanity.config.ts'))" | grep -q true</automated>
  </verify>
  <acceptance_criteria>
    - `sanity/schemas/siteSettings.ts` defines a document type named `siteSettings` with FR+EN sub-fields for siteTitle, navLabels, footerText, welcomeHeading, welcomeBody.
    - The schema is registered in `sanity/schemas/index.ts` and referenced by `sanity/sanity.config.ts`.
    - Running `sanity dev` shows a `siteSettings` document type constrained to a single (singleton) instance.
    - Schema uses current Sanity v6 API (defineType/defineField or the live-verified equivalent), not a deprecated v3/v4 form.
  </acceptance_criteria>
  <done>The locale-aware `siteSettings` singleton schema exists, is registered, is applied to the Studio, and is editable as a single document.</done>
</task>

<task type="auto">
  <name>Task 3: Publish bilingual content + build the getSiteSettings() fetch helper</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-PATTERNS.md (sanity.ts assignment lines 113-117 — build-time-only fetch, never browser)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md (Architectural Responsibility Map line 55; Standard Stack @sanity/client line 67; Security Domain lines 464-484)
    - .planning/phases/01-foundation-bilingual-infrastructure/01-UI-SPEC.md (Copywriting Contract — homepage welcome FR/EN example tone, footer/site-title roles)
    - .env.example (create) and any existing env handling
  </read_first>
  <action>
    Install `@sanity/client@7.23.0` and `@sanity/image-url@2.1.1` (both Approved in RESEARCH.md Package Legitimacy Audit — no legitimacy checkpoint needed). Create `.env.example` documenting `SANITY_PROJECT_ID`, `SANITY_DATASET`, and `SANITY_API_READ_TOKEN` (names only, NO real values) and ensure the real `.env` is gitignored.
    Create `src/lib/sanity.ts`: instantiate a single `@sanity/client` via `createClient` reading project ID/dataset/token from env, and export a typed `getSiteSettings()` helper that GROQ-fetches the singleton and returns an object exposing the FR and EN values for siteTitle, navLabels, footerText, welcomeHeading, welcomeBody. This helper is BUILD-TIME ONLY — it is called from `.astro` frontmatter, never shipped to or invoked from browser JS (no runtime compute exists on OVH; the token must never reach the client).
    In the running Studio, publish the singleton `siteSettings` document with real FR + EN placeholder copy matching the UI-SPEC copywriting contract (e.g. FR welcome heading/body per the "Bienvenue sur le site de l'Atelier Jacqueline Suzanne…" example tone, with the English counterpart; a footer copyright line; the site title "Atelier Jacqueline Suzanne").
  </action>
  <verify>
    <automated>grep -q "createClient" src/lib/sanity.ts && grep -q "getSiteSettings" src/lib/sanity.ts && grep -q "SANITY_PROJECT_ID" .env.example && ! grep -rq "SANITY_API_READ_TOKEN=" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/sanity.ts` exports `getSiteSettings()` and instantiates `@sanity/client` via `createClient`.
    - The token is read from an env var (not hardcoded): `grep -c "sk[A-Za-z0-9]" src/lib/sanity.ts` returns 0 (no literal token).
    - `.env.example` documents the three var names with NO real values assigned to the token.
    - With real env values present locally, calling `getSiteSettings()` at build time returns FR and EN copy for all five field groups (verify by a temporary build-time log or a throwaway node script using the client, then remove it).
    - `@sanity/client` and `@sanity/image-url` are in package.json dependencies at the RESEARCH.md-pinned versions.
  </acceptance_criteria>
  <done>The singleton is published with bilingual copy and `getSiteSettings()` returns it at build time via a read-only token — the CMS read leg of the skeleton works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build (local/CI) → Sanity Content Lake API | Authenticated read using an API token |
| npm registry → install | @sanity/client, @sanity/image-url, sanity Studio deps pulled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-TOK | Information Disclosure / Elevation of Privilege | Sanity API token | mitigate | Use a Viewer (read-only) token, read from env only, never hardcoded in `src/lib/sanity.ts`, never committed (real `.env` gitignored; `.env.example` carries names only). Token used strictly at build time — never shipped to the browser (RESEARCH.md Responsibility Map: build-time-only fetch). |
| T-01-XSS | Tampering | siteSettings copy rendered into HTML (consumed in Plan 04) | mitigate | Content is plain strings rendered via Astro's default auto-escaping in Plan 04; do not introduce `set:html` for these fields. Flag carried to Plan 04. |
| T-01-SC | Tampering | npm installs (@sanity/client, @sanity/image-url, sanity) | mitigate | All Approved in RESEARCH.md Package Legitimacy Audit (no [ASSUMED]/[SUS]/[SLOP], no postinstall). Pin exact versions; commit lockfile. No blocking checkpoint required. |
</threat_model>

<verification>
- `sanity dev` shows the `siteSettings` singleton with FR+EN fields.
- `getSiteSettings()` returns bilingual copy at build time using the read-only token.
- No token literal appears in tracked files.
</verification>

<success_criteria>
- Sanity project + dataset live; read-only token captured (not committed).
- `siteSettings` locale-aware singleton schema defined, applied, and published with real FR+EN copy (D-09, D-10).
- `getSiteSettings()` build-time helper returns the bilingual content for Plan 04 to consume.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bilingual-infrastructure/01-03-SUMMARY.md` when done — record Project ID, dataset name, and the `getSiteSettings()` return shape for Plan 04.
</output>
