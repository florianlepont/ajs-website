---
phase: 01-foundation-bilingual-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
autonomous: false
requirements: [I18N-01]
user_setup:
  - service: GitHub repository settings
    why: "GitHub Pages must be enabled once (Settings → Pages → Source: GitHub Actions) before Plan 05's workflow can publish to it"
    dashboard_config:
      - task: "Enable GitHub Pages with source 'GitHub Actions' for this repo"
        location: "GitHub repo → Settings → Pages"

must_haves:
  truths:
    - "OVH hosting plan tier and protocol are confirmed and recorded for Phase 5's later production cutover (D-13): Free hosting tier, SFTP enabled on port 22, host ftp.cluster129.hosting.ovh.net, user atelihu, home dir /home/atelihu"
    - "GitHub Pages is enabled for this repo with source set to GitHub Actions"
    - "astro.config.mjs sets base conditionally so the GitHub Pages project-page path (/ajs-website/) works for staging while leaving room for an unprefixed base at the eventual OVH production root"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Conditional base path via an env var so the same build works at GitHub Pages' /ajs-website/ subpath and (later) at OVH's domain root"
      contains: "base"
  key_links:
    - from: "astro.config.mjs base config"
      to: "GitHub Pages project-page URL (https://florianlepont.github.io/ajs-website/)"
      via: "ASTRO_BASE env var read at build time"
      pattern: "base"
---

<objective>
Originally scoped to provision an OVH staging subdomain — superseded mid-execution (see 01-CONTEXT.md D-12/D-13): OVH's Free hosting tier cannot attach any subdomain (multisite requires a paid tier). Rescoped to: (1) record the OVH protocol/doc-root facts already confirmed via the control panel for Phase 5's later use, and (2) prepare the repo for a GitHub Pages staging target instead — enabling Pages and wiring `astro.config.mjs` to emit the right base path for a GitHub Pages project site.

Purpose: Unblock Plan 05's CI wiring with a staging target that actually works on the current OVH tier, without losing the OVH connection details Phase 5 will need.
Output: GitHub Pages enabled, `astro.config.mjs` base path wired conditionally, and OVH connection facts recorded here for Phase 5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-bilingual-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record confirmed OVH connection facts (D-13, for Phase 5) and add conditional base to astro.config.mjs</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md (D-12, D-13 — staging host override)
    - astro.config.mjs (current state from Plan 01-01)
  </read_first>
  <action>
    Record the OVH facts already confirmed via the control panel during this plan's discussion (no further control-panel work needed for this task):
    - Plan tier: OVH "Free hosting" (Hébergement Mutualisé), cluster129, datacenter eu-west-gra, 100MB disk.
    - Protocol: SFTP enabled on port 22 (encrypted), host `ftp.cluster129.hosting.ovh.net`, username `atelihu`, home directory `/home/atelihu`. Plain FTP (port 21) also available but SFTP is preferred.
    - DNS zone for `atelierjacquelinesuzanne.fr` is managed at OVH itself.
    - The domain has an active email service (MX Plan + Zimbra mailbox) — any future DNS cutover must preserve these records.
    These facts are NOT used by this phase's CI (which targets GitHub Pages instead — D-12) but are recorded here so Phase 5's production-cutover plan doesn't need to re-derive them.

    Then update `astro.config.mjs` to set `base` conditionally from an env var, so the same codebase can build for GitHub Pages' project-page subpath now and an unprefixed root later at OVH:
    ```js
    base: process.env.ASTRO_BASE || '/',
    ```
    Leave everything else in the existing i18n config untouched (Plan 01-01 already set `defaultLocale`, `locales`, `prefixDefaultLocale`).
  </action>
  <verify>
    <automated>grep -q "ASTRO_BASE" astro.config.mjs && npm run build && test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` reads `base` from `process.env.ASTRO_BASE`, defaulting to `/` when unset (so local dev/OVH-prod builds are unaffected).
    - `npm run build` still succeeds with no env var set (defaults to root base).
    - `ASTRO_BASE=/ajs-website/ npm run build` succeeds and produces links prefixed with `/ajs-website/`.
  </acceptance_criteria>
  <done>OVH facts are documented for Phase 5, and astro.config.mjs supports building for either GitHub Pages' subpath or a domain root via one env var.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Enable GitHub Pages for this repo (D-12)</name>
  <read_first>
    - .planning/phases/01-foundation-bilingual-infrastructure/01-CONTEXT.md (D-12 — GitHub Pages staging override)
  </read_first>
  <what-built>
    A one-time repo settings toggle Claude cannot perform without elevated GitHub permissions — Florian enables it directly.
  </what-built>
  <how-to-verify>
    In the GitHub repo (florianlepont/ajs-website) → Settings → Pages → under "Build and deployment", set Source to "GitHub Actions" (not "Deploy from a branch"). No branch or folder needs selecting — Plan 05's workflow will publish via the `actions/deploy-pages` action.
  </how-to-verify>
  <acceptance_criteria>
    - Settings → Pages shows Source = "GitHub Actions".
  </acceptance_criteria>
  <resume-signal>Reply "Pages enabled" once the Source is set to GitHub Actions — or describe any permission issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → GitHub Pages staging site | Anyone can reach the staging URL once Pages is enabled |
| developer → GitHub repo settings | Privileged operation (enabling Pages source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-TLS | Information Disclosure | staging transport | mitigate | GitHub Pages provides automatic HTTPS for `*.github.io` — no manual cert provisioning needed, no HTTP-only staging. |
| T-01-CRED | Information Disclosure | OVH SFTP password | mitigate | Password is never pasted into chat, the repo, or any planning file. Not needed until Phase 5's production deploy plan — will be entered directly into GitHub Secrets at that time. Prefer SFTP (encrypted) over plaintext FTP for that eventual transfer. |
| T-01-DNS | Tampering | production domain records | accept | No DNS changes are made in this plan at all (GitHub Pages needs none). Production records untouched; real cutover deferred to Phase 5. |
</threat_model>

<verification>
- `npm run build` succeeds both with and without `ASTRO_BASE` set.
- GitHub repo Settings → Pages shows Source = "GitHub Actions".
- OVH connection facts (protocol, host, user, doc-root) are recorded in the SUMMARY for Phase 5 to consume later.
</verification>

<success_criteria>
- OVH plan tier + deploy protocol + document root confirmed and recorded for Phase 5 (D-13).
- `astro.config.mjs` supports a conditional base path (D-12).
- GitHub Pages enabled with Source = GitHub Actions, ready for Plan 05 to deploy into.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-bilingual-infrastructure/01-02-SUMMARY.md` when done — record the confirmed OVH protocol/host/doc-root (for Phase 5) and confirm GitHub Pages is enabled (for Plan 05).
</output>
