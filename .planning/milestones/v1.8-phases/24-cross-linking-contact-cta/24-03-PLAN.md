---
phase: 24-cross-linking-contact-cta
plan: 03
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - src/components/GalleryDetailBody.astro
  - src/components/GalleryDetailPage.astro
  - tests/e2e/gallery.spec.ts
autonomous: true
requirements: [EDN-12, CONT-04, UI-03]

must_haves:
  truths:
    - "A visitor on a gallery detail page whose gallery has a linked édition sees a cross-link at the top of the content area and can click through to that édition (EDN-12, D-03)."
    - "A visitor on a gallery detail page whose gallery has no linked édition sees no cross-link element at all — not an empty box, not a dead link (D-02)."
    - "A visitor reaching the end of any gallery's photo grid sees the contact CTA and can click through to the contact page (CONT-04, D-04, D-07)."
    - "Both new elements render correctly at phone widths (390px) and desktop widths (1280px), with the existing gallery layout unchanged (UI-03)."
  artifacts:
    - "src/components/GalleryDetailBody.astro — .gallery-detail__related markup+CSS, .gallery-detail__contact-cta-zone/-rule/-arrow markup+CSS, three new Props"
    - "src/components/GalleryDetailPage.astro — relatedLink / contactCtaHref / contactCtaLabel prop threading"
    - "tests/e2e/gallery.spec.ts — new describe blocks for EDN-12 and CONT-04 at both viewports"
  key_links:
    - "buildGalleryDetailModel -> GalleryDetailPage.astro -> GalleryDetailBody.astro: the prop chain; both locale routes (src/pages/galleries/[slug].astro and src/pages/en/galleries/[slug].astro) already delegate to GalleryDetailPage, so no per-locale route edit is needed."
    - "GalleryGrid close -> contact CTA: the CTA must be the LAST child of .gallery-detail__content, after the grid, with no change to GalleryGrid.astro itself."
---

<objective>
Render both of this phase's new gallery-page elements: EDN-12's reverse cross-link to the associated édition (top of the content area) and CONT-04's contact CTA (end of the photo sequence), and cover both with cross-viewport e2e assertions.

Purpose: this is the user-visible half of EDN-12 and half of CONT-04. `GalleryDetailBody.astro` today has no related-link prop at all — this component gains the pattern `EditionDetailBody.astro` has shipped since v1.3, plus the new sketch-018-pinned CTA.

Output: a gallery detail page with a conditional top cross-link and an unconditional closing CTA, both proven at 390px and 1280px.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cross-linking-contact-cta/24-CONTEXT.md
@.planning/phases/24-cross-linking-contact-cta/24-UI-SPEC.md
@.planning/phases/24-cross-linking-contact-cta/24-PATTERNS.md
@.planning/phases/24-cross-linking-contact-cta/24-VALIDATION.md
@.planning/phases/24-cross-linking-contact-cta/24-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the EDN-12 reverse cross-link at the top of the gallery content area</name>

  <files>src/components/GalleryDetailBody.astro, src/components/GalleryDetailPage.astro</files>

  <read_first>
    - `src/components/GalleryDetailBody.astro` — the whole 83-line file: its `Props` interface (14-30), destructure (32-48), the doc comment at lines 51-52, the `.gallery-detail__content` div (69-71), and the two-rule `<style>` block (75-83).
    - `src/components/EditionDetailBody.astro` — lines 26-35 (the `relatedLink` Props declaration being mirrored), lines 96-100 (the conditional render being mirrored), lines 119-150 (the `.edition-detail__related` CSS being copied), lines 152-161 (the mobile override being copied). Note the comment at lines 85-95 explaining WHY this link is placed in-flow rather than absolutely positioned (it avoids a Phase 10 header/logo-overlap regression class).
    - `src/components/GalleryDetailPage.astro` — the whole file: its `Props`, its `buildGalleryDetailModel` call, and the `<GalleryDetailBody ... />` prop list to extend.
    - `.planning/phases/24-cross-linking-contact-cta/24-UI-SPEC.md` — "Component Contract: Reverse Cross-Link (EDN-12)", which gives the exact renamed CSS block to write and the correction note explaining the real shipped treatment is a bordered/framed link (2px `currentColor` border, `10px var(--space-md)` padding, 44px min-height, `::after` arrow), NOT the sketch's simplified placeholder.
    - `src/pages/galleries/[slug].astro` — confirm it delegates to `GalleryDetailPage` and needs no edit of its own.
  </read_first>

  <action>
    In `src/components/GalleryDetailBody.astro`:

    Add `relatedLink: { href: string; text: string } | null;` to the `Props` interface, placed immediately before `gridItems`, matching `EditionDetailBody.astro`'s own field ordering. Add `relatedLink` to the destructure in the same relative position.

    Render it as the FIRST child of `.gallery-detail__content`, before the `GalleryGrid` line (D-03 — top of the content area, mirroring the shipped édition-side convention and sitting at the opposite end of the page from CONT-04's CTA). Use the same conditional-render form `EditionDetailBody.astro` uses at line 96 so that a gallery with no linked édition emits nothing at all — not an empty element, not a disabled link (D-02). The anchor carries class `gallery-detail__related`, `href` from the prop, and the prop's `text` as its only child; do not add an inline arrow span (the arrow comes from CSS generated content, which is already excluded from the accessibility tree).

    Add the CSS to the component's `<style>` block, copying `.edition-detail__related`'s four rules from `EditionDetailBody.astro` lines 119-150 with the class renamed to `.gallery-detail__related`, exactly as spelled out in UI-SPEC.md's Component Contract: the base rule (`inline-flex`, `align-items: center`, `gap: var(--space-sm)`, `min-height: 44px`, `padding: 10px var(--space-md)`, `font-size: var(--text-label-size)`, `font-weight: var(--weight-semibold)`, `line-height: 1.2`, `color: var(--color-ink)`, `text-decoration: none`, `white-space: nowrap`, `border: 2px solid currentColor`, `margin: 0 0 var(--space-2xl)`), the `::after` arrow rule, the combined `:hover, :focus-visible` rule (accent color + 2px accent outline at `var(--focus-ring-offset)`), and a `@media (max-width: 767px)` override setting `max-width: 100%` and `margin: 0 0 var(--space-xl)`. Do NOT add a `translateX` arrow-slide here — that motion belongs exclusively to CONT-04's CTA per UI-SPEC.md's States section.

    Replace the doc comment at lines 51-52. It describes a footer-hiding behavior that PORT-06 (v1.5) deliberately reversed on direct user instruction; `GalleryDetailPage.astro` no longer passes `hideFooter` to `BaseLayout` at all, so the comment is now factually wrong about the current wiring (RESEARCH.md Pitfall 4). Replace it with a comment stating that gallery and édition detail pages have identical footer behavior today, that this component renders EDN-12's optional cross-link at the top of the content area and CONT-04's CTA at the end of it, and citing PORT-06 as the reason no footer-conditional logic exists here.

    In `src/components/GalleryDetailPage.astro`: pass `relatedLink={model.relatedLink}` into `<GalleryDetailBody />`, positioned immediately before `gridItems={model.gridItems}` to match the prop ordering. No other change to this file in this task.
  </action>

  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run build` exits 0.
    - `grep -c "gallery-detail__related" src/components/GalleryDetailBody.astro` is at least 5 (markup class + 4 CSS selectors).
    - `grep -n "relatedLink" src/components/GalleryDetailPage.astro` matches exactly 1 line.
    - `grep -n "border: 2px solid currentColor" src/components/GalleryDetailBody.astro` matches exactly 1 line.
    - `grep -n "focus-visible" src/components/GalleryDetailBody.astro` matches at least 1 line.
    - `grep -c 'gallery-only footer hide' src/components/GalleryDetailBody.astro` equals 0 (stale PORT-06-superseded comment removed).
    - No `translateX` rule appears inside the `.gallery-detail__related` selector block (the arrow-slide belongs exclusively to the CTA added in task 2).
    - `git diff --name-only` lists no file under `src/components/GalleryGrid.astro` or `src/components/EditionDetailBody.astro`.
  </acceptance_criteria>

  <done>
    Gallery detail pages render a bordered cross-link to the associated édition at the top of the content area when one is set, and render nothing there when none is set.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render the CONT-04 contact CTA at the end of the gallery photo sequence</name>

  <files>src/components/GalleryDetailBody.astro, src/components/GalleryDetailPage.astro</files>

  <read_first>
    - `src/components/GalleryDetailBody.astro` — current state after task 1: the `Props` interface, the `.gallery-detail__content` div, and the `<style>` block.
    - `.planning/phases/24-cross-linking-contact-cta/24-UI-SPEC.md` — "Component Contract: Contact CTA (CONT-04)" in full: the markup block, the CSS block, the States section, the Motion note, and the note explaining Astro scopes `<style>` per component so no shared extraction is needed.
    - `.planning/sketches/018-gallery-edition-contact-cta/index.html` — Variant B (`.cta-zone--b`, `.cta--b-rule`, `.cta--b`, `.arrow`) as the confirmed visual reference; note its inline comment saying the mobile hard line break must NOT be reproduced in the real implementation.
    - `.planning/sketches/018-gallery-edition-contact-cta/README.md` — the winner rationale for Variant B.
    - `src/components/GalleryDetailPage.astro` — the `<GalleryDetailBody />` prop list.
  </read_first>

  <action>
    In `src/components/GalleryDetailBody.astro`:

    Add `contactCtaHref: string;` and `contactCtaLabel: string;` to the `Props` interface as the last two fields before `locale`, and add both to the destructure.

    Render the CTA as the LAST child of `.gallery-detail__content`, immediately after the `GalleryGrid` line and before that div closes (D-07 — literally the end of the photo sequence). It renders UNCONDITIONALLY on every gallery: no truthiness guard, no sold-state variant, no conditional wrapper. Structure per UI-SPEC.md: an outer `div` with class `gallery-detail__contact-cta-zone`, containing first an empty `div` with class `gallery-detail__contact-cta-rule` (the hairline), then an anchor with class `gallery-detail__contact-cta` whose `href` is the `contactCtaHref` prop, whose text child is the `contactCtaLabel` prop, and which ends with a `span` of class `gallery-detail__contact-cta-arrow` carrying `aria-hidden="true"` and the right-arrow glyph. Do not hardcode any visible string and do not hardcode the contact path — both arrive as props (the component's header comment states it holds no locale strings of its own). Do not reproduce the sketch's hard line break; let text wrapping happen naturally.

    Add the CSS to the `<style>` block, copying UI-SPEC.md's CONT-04 block verbatim with the `gallery-detail__` class prefix: the zone rule (`padding-top: var(--space-2xl)`), the rule element (`border-top: 1px solid var(--pink-600)`, `width: 48px`, `margin-bottom: var(--space-md)`), the anchor (`inline-flex`, `align-items: center`, `gap: 10px`, `color: var(--color-ink)`, `font-family: var(--font-display)`, `font-weight: 600`, `font-size: 20px`, `line-height: 1.25`, `text-decoration: none`, `border-bottom: 1px solid transparent`), the arrow (`color: var(--pink-600)`, `font-size: 20px`, `transition: transform 0.15s ease`), a combined `:hover, :focus-visible` rule on the anchor setting `border-bottom-color: var(--color-ink)`, and a combined `:hover .arrow, :focus-visible .arrow` rule setting `transform: translateX(4px)`.

    Three fidelity constraints: (a) use the literal `20px` and `600` and `1.25` values, not `var(--text-heading-size)` / `var(--weight-semibold)` / `var(--text-heading-leading)` — UI-SPEC.md pins the sketch's own literals as the reference and notes the tokens are numerically coincidental today, not semantically the same; (b) include the `:focus-visible` half of both combined selectors — the raw sketch had `:hover` only, and this project pairs the two everywhere; (c) add no `prefers-reduced-motion` guard for the 4px arrow nudge, and add no background-color, border-radius, or padding to the anchor — this is a text link with an arrow, not a filled or pill button (D-08).

    In `src/components/GalleryDetailPage.astro`: pass `contactCtaHref={model.contactCtaHref}` and `contactCtaLabel={model.contactCtaLabel}` into `<GalleryDetailBody />`, positioned as the last two props before `locale={locale}`.
  </action>

  <verify>
    <automated>npm run typecheck &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `npm run typecheck` exits 0 and `npm run build` exits 0.
    - `grep -c "gallery-detail__contact-cta" src/components/GalleryDetailBody.astro` is at least 10 (4 markup classes + 6 CSS selectors).
    - `grep -n 'aria-hidden="true"' src/components/GalleryDetailBody.astro` matches at least 1 line.
    - `grep -n "border-top: 1px solid var(--pink-600)" src/components/GalleryDetailBody.astro` matches exactly 1 line.
    - `grep -n "translateX(4px)" src/components/GalleryDetailBody.astro` matches exactly 1 line.
    - `grep -n "contactCtaHref\|contactCtaLabel" src/components/GalleryDetailPage.astro` matches exactly 2 lines.
    - `grep -n "transition: transform 0.15s ease" src/components/GalleryDetailBody.astro` matches exactly 1 line, and that rule is NOT nested inside any `@media` block — the arrow nudge is unconditional, per UI-SPEC.md's Motion note.
    - In the built artifact, `grep -rl "gallery-detail__contact-cta" dist/galleries/` returns at least one file, confirming the CTA is emitted for real galleries.
    - `git diff --name-only` still lists no `src/components/GalleryGrid.astro`.
  </acceptance_criteria>

  <done>
    Every gallery detail page ends its photo sequence with a pink-hairline-topped, 20px Unbounded contact CTA linking to the locale's contact page, styled to sketch 018 Variant B.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cross-viewport e2e coverage for the gallery cross-link and CTA</name>

  <files>tests/e2e/gallery.spec.ts</files>

  <read_first>
    - `tests/e2e/gallery.spec.ts` — the whole file, and specifically the PORT-06 describe block near line 823: it shows the content-fragility-safe navigation idiom (go to `/`, click the "Grille" button, read `a.home-grid__tile` hrefs at runtime) and the `scrollHeight - innerHeight >= 300` scroll-track assertion.
    - `tests/e2e/edition.spec.ts` — lines 623-670, the "editions related-gallery cross-link (EDN-08)" describe block: the walk-until-found loop, the `toHaveAttribute` href-pattern assertion, the `getComputedStyle` evaluate idiom, and the two-viewport structure to mirror.
    - `tests/unit/e2e-content-fragility.test.ts` — lines 1-60: the CI guard that FAILS the unit suite if any e2e spec embeds a literal `galleries/<slug>` or `editions/<slug>` route. Every gallery href in the new tests must be discovered at runtime, never written as a literal.
    - `src/layouts/BaseLayout.astro` — line 309: `--pink-600: #D6327C`, which resolves to `rgb(214, 50, 124)` in computed styles.
    - `playwright.config.ts` — the configured projects (`chromium`, `webkit-mobile`).
  </read_first>

  <action>
    Add two new `test.describe` blocks to `tests/e2e/gallery.spec.ts`.

    **Navigation helper (shared by both blocks):** discover gallery hrefs at runtime exactly the way the PORT-06 block does — set a 1280x900 viewport, go to `/`, click the "Grille" button, then read every `a.home-grid__tile` href via `evaluateAll`. Keep that array and reuse it for direct `page.goto(href)` navigation at both viewport sizes; do not attempt to re-discover hrefs at phone width, where a different homepage renderer is active. Never write a gallery slug as a literal anywhere in the file — the content-fragility guard in `tests/unit/e2e-content-fragility.test.ts` fails the unit suite on that pattern.

    **Block 1 — "gallery contact CTA (CONT-04)".** For the first discovered gallery href, at 390x844 and again at 1280x900, assert: `.gallery-detail__contact-cta` has count 1 and is visible; its `href` attribute matches a contact-route pattern allowing an optional base prefix and optional trailing slash; its text contains the French CTA fragment `Contactez-nous`; and its computed styles are `fontSize: '20px'`, `fontWeight: '600'`, `fontFamily` containing `Unbounded`, and `backgroundColor: 'rgba(0, 0, 0, 0)'` (the D-08 "no filled button" guard). Separately assert `.gallery-detail__contact-cta-rule` has computed `borderTopWidth: '1px'` and `borderTopColor: 'rgb(214, 50, 124)'`, and that `.gallery-detail__contact-cta-arrow` has computed `color: 'rgb(214, 50, 124)'`. Add a DOM-order assertion proving D-07: evaluate `compareDocumentPosition` between `.gallery-grid` and `.gallery-detail__contact-cta` and assert the CTA follows the grid. Add one English-locale check: navigate to the `/en/` counterpart of the discovered href (derive it by inserting the `en` segment programmatically, not by writing a literal slug) and assert the CTA text contains `Get in touch`.

    **Block 2 — "gallery reverse edition cross-link (EDN-12)".** Walk every discovered gallery href at 390x844. For each, read the count of `.gallery-detail__related`. Assert the count is always 0 or 1 — never more. When it is 1, assert the element is visible, its `href` matches an editions-route pattern of the form `/editions/` followed by a wildcard slug segment and an optional trailing slash, its text matches a `Voir l'édition` pattern, and its computed styles are `display: 'inline-flex'` and `borderTopWidth: '2px'` (mirroring the EDN-08 assertion on the édition side). Track whether any gallery had the link, and when at least one did, re-check that same href at 1280x900 and assert the same computed styles hold.

    Do NOT assert that at least one gallery HAS a linked édition in this task. No gallery can carry `relatedEdition` until the Studio field is populated and published, which is plan 24-05's blocking content checkpoint. This task's job is to prove the conditional-render contract (D-02: zero-or-one, never a broken element) and full CTA correctness; 24-05 converts the walk into a hard presence assertion once content exists. State this explicitly in a comment above block 2 so a reader does not mistake it for a weak test.

    Head both blocks with comments citing their requirement IDs (CONT-04 / EDN-12), the decisions they enforce, and UI-03's dual-viewport requirement.
  </action>

  <verify>
    <automated>npx playwright test tests/e2e/gallery.spec.ts &amp;&amp; npx vitest run tests/unit/e2e-content-fragility.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx playwright test tests/e2e/gallery.spec.ts` exits 0 across both configured projects.
    - `npx vitest run tests/unit/e2e-content-fragility.test.ts` exits 0 (no hardcoded gallery/édition slug introduced).
    - `grep -c "CONT-04" tests/e2e/gallery.spec.ts` is at least 1 and `grep -c "EDN-12" tests/e2e/gallery.spec.ts` is at least 1.
    - `grep -n "rgb(214, 50, 124)" tests/e2e/gallery.spec.ts` matches at least 2 lines (hairline rule color + arrow color).
    - `grep -n "compareDocumentPosition" tests/e2e/gallery.spec.ts` matches at least 1 line (D-07 DOM-order proof).
    - `grep -n "390" tests/e2e/gallery.spec.ts` and `grep -n "1280" tests/e2e/gallery.spec.ts` each match inside the two new blocks (UI-03 dual-viewport).
    - The pre-existing PORT-06 describe block is unmodified: `git diff tests/e2e/gallery.spec.ts` shows only additions, no deletions inside that block.
  </acceptance_criteria>

  <done>
    Automated e2e proves the gallery CTA renders correctly and after the grid at both viewport classes in both locales, and proves the cross-link honours its zero-or-one conditional-render contract with correct href, copy, and styling whenever it is present.
  </done>
</task>

</tasks>

## Artifacts this phase produces

Created by THIS plan:

| Symbol / Path | Kind | Notes |
|---|---|---|
| `GalleryDetailBody` prop `relatedLink` | Astro component prop | `{ href: string; text: string } \| null` |
| `GalleryDetailBody` prop `contactCtaHref` | Astro component prop | `string` |
| `GalleryDetailBody` prop `contactCtaLabel` | Astro component prop | `string` |
| `.gallery-detail__related` | CSS class + markup | Conditional bordered cross-link, top of content area |
| `.gallery-detail__contact-cta-zone` | CSS class + markup | Unconditional CTA wrapper, last child of `.gallery-detail__content` |
| `.gallery-detail__contact-cta-rule` | CSS class + markup | 48px pink hairline |
| `.gallery-detail__contact-cta` | CSS class + markup | The anchor |
| `.gallery-detail__contact-cta-arrow` | CSS class + markup | `aria-hidden` decorative arrow span |
| `tests/e2e/gallery.spec.ts` → "gallery contact CTA (CONT-04)" | Playwright describe block | — |
| `tests/e2e/gallery.spec.ts` → "gallery reverse edition cross-link (EDN-12)" | Playwright describe block | Converted to a hard presence assertion by plan 24-05 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Render model → emitted static HTML | Editor-authored édition title becomes visible, clickable link text on a public page |
| Visitor browser → static page | Public, unauthenticated read-only surface; no form, no fetch, no client JS added by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-07 | Tampering | `.gallery-detail__related` link text (editor-authored title) | low | mitigate | The title renders as an Astro expression child, which is HTML-escaped automatically. This plan introduces no `set:html` and no `innerHTML`; task 1's action forbids an inline arrow span in favor of CSS generated content, keeping the anchor's children to a single escaped text node. |
| T-24-08 | Spoofing | `.gallery-detail__contact-cta` destination | low | mitigate | The `href` comes only from `model.contactCtaHref`, built in plan 24-02 from a hardcoded internal route segment via Astro's base-aware URL builder. No editor-controlled value can redirect it off-site. |
| T-24-09 | Repudiation | New anchors bypassing keyboard/assistive access | low | mitigate | Both new elements are real `<a href>` anchors in DOM order with no `tabindex` manipulation; the decorative arrow is `aria-hidden`; both carry an explicit `:focus-visible` state on top of the site's global accent focus ring. Verified by task 3's computed-style assertions and the existing `tests/e2e/accessibility.spec.ts` regression pass. |
| T-24-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs zero packages (RESEARCH.md Standard Stack). Legitimacy gate not applicable. |

ASVS L1 scope note: V2/V3/V4/V6 not applicable. V5 unaffected — this plan constructs no queries and accepts no visitor input. No `high`-severity threat, so `security_block_on: high` is not tripped.
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npm run lint` exits 0.
- `npm run test:unit` exits 0.
- `npm run test:e2e` exits 0 (full Playwright suite — this is also the UI-03 regression pass for the gallery surface).
- `npm run build && npm run test:artifact` exits 0.
- `git diff --name-only` lists only the three files in `files_modified`.
</verification>

<success_criteria>
- A gallery detail page with a linked édition shows a clickable cross-link at the top of its content area, at both viewport classes.
- A gallery detail page without one shows no such element at all.
- Every gallery detail page ends with the contact CTA, after the grid, in both locales.
- `GalleryGrid.astro` and `EditionDetailBody.astro` are untouched.
- The full existing gallery e2e suite still passes (UI-03 no-regression).
</success_criteria>

<output>
Create `.planning/phases/24-cross-linking-contact-cta/24-03-SUMMARY.md` when done.
</output>
