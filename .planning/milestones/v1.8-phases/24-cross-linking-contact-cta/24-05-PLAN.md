---
phase: 24-cross-linking-contact-cta
plan: 05
type: execute
wave: 4
depends_on: [24-03, 24-04]
files_modified:
  - tests/e2e/gallery.spec.ts
autonomous: false
requirements: [EDN-12, CONT-04, UI-03]

user_setup:
  - service: sanity
    why: "EDN-12's reverse cross-link cannot be proven present until at least one published gallery actually references an édition — the field ships empty on every gallery. The Studio also needs a deploy before Romane can self-serve the new field."
    dashboard_config:
      - task: "Open the gallery that pairs with an existing published édition, open its 'Édition liée' tab, set 'Édition liée (optionnel)' to that édition, and Publish. The chosen gallery MUST be homepage-visible (showOnHomePage enabled) so the e2e homepage-grid walk can reach it."
        location: "Sanity Studio -> Collection photo -> <gallery> -> Édition liée"
      - task: "Deploy the Studio so the new reference field appears in Romane's hosted Studio, not only in local `sanity dev`."
        location: "Run `npm --prefix sanity run deploy` from the repo root"

must_haves:
  truths:
    - "At least one published gallery carries a working link to its associated édition, proven by a hard e2e presence assertion (EDN-12, ROADMAP success criterion 1)."
    - "Adding the CTA's height leaves the DetailHero scroll-up-to-return gesture's required scroll room intact (>= 300px) on gallery detail pages."
    - "Both features are confirmed clean by a human at phone width (<=767px) and desktop/tablet width (>=768px), with no regression to either page's existing layout (UI-03, ROADMAP success criterion 4)."
    - "Romane can set the new 'Édition liée' field herself in the deployed Studio."
  artifacts:
    - "tests/e2e/gallery.spec.ts — EDN-12 presence assertion + CTA-present scroll-track regression guard"
    - ".planning/phases/24-cross-linking-contact-cta/24-05-SUMMARY.md recording the UAT outcome at both viewport classes"
  key_links:
    - "Sanity published relatedEdition -> build-time GROQ fetch -> gallery detail page render: the full EDN-12 chain, which only closes once real content exists."
    - "CTA height -> document scrollHeight -> DetailHero ENGAGE_DISTANCE=300 gesture arming: RESEARCH.md Pitfall 3 proves added height is strictly favorable; this plan pins that with an assertion so a future CSS change cannot silently break it."
---

<objective>
Close Phase 24: populate the real content EDN-12 needs, convert the gallery cross-link e2e from a conditional contract test into a hard presence assertion, guard the DetailHero scroll track against the CTA's added height, and run UI-03's human cross-viewport verification.

Purpose: plans 24-01 through 24-04 build and unit/e2e-cover the mechanism, but ROADMAP success criterion 1 ("sees a link to that Édition and can navigate to it") cannot be proven while every gallery's `relatedEdition` is empty. UI-03 is this phase's closing success criterion, and this project has a documented history of phone-specific work leaking into desktop (v1.6 HOME-16), which VALIDATION.md judges makes automated assertions alone insufficient.

Output: real cross-linked content, a non-vacuous EDN-12 assertion, and a signed-off dual-viewport UAT.
</objective>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cross-linking-contact-cta/24-CONTEXT.md
@.planning/phases/24-cross-linking-contact-cta/24-VALIDATION.md
@.planning/phases/24-cross-linking-contact-cta/24-UI-SPEC.md
@.planning/phases/24-cross-linking-contact-cta/24-03-SUMMARY.md
@.planning/phases/24-cross-linking-contact-cta/24-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Populate the reverse cross-link in Sanity and deploy the Studio</name>

  <files>(no repo files — editorial content in the Sanity Content Lake plus a Studio deploy)</files>

  <action>
    Pause and hand off to the developer. Do not attempt to author Sanity content programmatically and do not write a seeding script: the reference target is an editorial judgment about which collection pairs with which édition, and the Studio deploy publishes a build to Romane's hosted Studio. Present the verification steps below verbatim, wait for the resume signal, and record the reported gallery/édition pair in the plan summary so task 2's presence assertion and the phase's UAT can reference it.
  </action>

  <verify>
    <human-check>Developer reports the gallery/édition pair set, a successful `npm --prefix sanity run deploy`, and non-empty output from `grep -rl "gallery-detail__related" dist/galleries/` after a rebuild.</human-check>
  </verify>

  <done>
    At least one homepage-visible published gallery references a published édition, the rebuilt artifact contains the rendered cross-link, and the Studio carrying the new field is deployed for Romane.
  </done>

  <what-built>
    Plan 24-01 added an optional "Édition liée" reference field to the `gallery` document type in Sanity Studio, wired it through the GROQ queries and sanitizers, and plan 24-03 renders it as a cross-link at the top of a gallery detail page's content area. The field ships empty on every gallery — no gallery references an édition yet, so the feature is currently invisible on the live site by design.

    Everything that can be automated is done: the schema, the query, the sanitizer, the helper, the render, and the conditional-render contract test all pass. What remains genuinely requires the Studio, because it is editorial content authorship, not code.
  </what-built>

  <how-to-verify>
    1. Start the Studio locally: `npm --prefix sanity run dev`, then open http://localhost:3333.
    2. In the left sidebar choose **Collection photo** and open the gallery that pairs with an existing published édition. The Rebut collection / Rebut édition pairing is the known example — the forward direction (édition → collection) is already set on that édition today, so setting the reverse there produces a genuine two-way pair.
    3. Open the **Édition liée** tab on that gallery. Confirm the field **Édition liée (optionnel)** is present with its French help text. If the tab or field is missing, the schema change did not load — stop and report.
    4. Set the field to the paired édition, then click **Publish**.
    5. Confirm the chosen gallery is homepage-visible: its **Couleur** tab's "Afficher sur la page d'accueil" toggle must be ON. The e2e walk in task 2 discovers galleries through the homepage grid, so a gallery hidden from the homepage would be unreachable by that test. If it is off, either turn it on or pick a different gallery that is on, and report which gallery you used.
    6. Deploy the Studio so Romane sees the new field in her hosted Studio, not only in local dev: `npm --prefix sanity run deploy`.
    7. Rebuild the site so the newly published reference is fetched: `npm run build`.
    8. Confirm the reference reached the build: `grep -rl "gallery-detail__related" dist/galleries/` should now return at least one file. Before publishing it returned nothing.

    Report back: which gallery and which édition you paired, whether the Studio deploy succeeded, and the output of step 8.
  </how-to-verify>

  <resume-signal>Type "approved" with the gallery/édition pair you set, or describe what blocked you.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Convert the EDN-12 e2e to a hard presence assertion and pin the scroll-track guard</name>

  <files>tests/e2e/gallery.spec.ts</files>

  <read_first>
    - `tests/e2e/gallery.spec.ts` — the "gallery reverse edition cross-link (EDN-12)" describe block added by plan 24-03: its runtime href discovery, its zero-or-one count assertion, and its walk loop. Also read the PORT-06 describe block near line 823 for the shipped `scrollHeight - innerHeight >= 300` assertion and the doc comment explaining why added page height can only make that assertion safer.
    - `src/components/DetailHero.astro` — lines 255-320 (the `REVEAL_DISTANCE = 900` progress computation from `.detail-hero`'s own `getBoundingClientRect().top`) and lines 350-390 (the `ENGAGE_DISTANCE = 300` arming gate and the `atTop()` release check). Both are pure `window.scrollY` / own-rect computations, which is WHY content added after the grid cannot affect them — read this to understand what the guard is protecting, not to change it.
    - `.planning/phases/24-cross-linking-contact-cta/24-RESEARCH.md` — "Pitfall 3", the code-level proof that the CTA's added height is strictly favorable to the gesture rather than a risk.
    - `.planning/phases/24-cross-linking-contact-cta/24-05-SUMMARY.md` inputs: the gallery/édition pair reported in task 1.
    - `tests/unit/e2e-content-fragility.test.ts` — the CI guard forbidding literal slugs in e2e specs; the new assertions must still discover hrefs at runtime.
  </read_first>

  <action>
    Two edits to `tests/e2e/gallery.spec.ts`, both inside the EDN-12 describe block plan 24-03 created.

    1. **Hard presence assertion.** Plan 24-03 deliberately left the cross-link walk unable to fail when no gallery had the link, because no content existed yet. Content now exists (task 1). Add a tracking flag across the walk and, after the loop, assert that at least one visited gallery carried `.gallery-detail__related` — mirroring the `expect(foundRelatedCollection).toBe(true)` shape the shipped EDN-08 block uses in `tests/e2e/edition.spec.ts`. Then, for the gallery that was found, assert click-through actually navigates: click the link and assert the resulting URL matches an editions-route pattern. Replace 24-03's comment explaining why presence was not yet asserted with one recording that the Studio content landed in this plan and that the assertion is now load-bearing — a future content removal SHOULD fail this test, which is the intended signal.

    2. **Scroll-track regression guard.** Add a test at 1280x900 that navigates to a discovered gallery href, asserts `.gallery-detail__contact-cta` is present, and then asserts `document.documentElement.scrollHeight - window.innerHeight` is at least 300 — the same threshold the shipped PORT-06 test uses. Head it with a comment stating that `DetailHero`'s scroll-up-to-return gesture arms at an absolute `scrollY` of 300 and its reveal math reads only `.detail-hero`'s own rect against a fixed 900px constant, so content added after the grid can only ever increase available scroll room; the assertion exists so that a future unrelated CSS change cannot silently erode it, not because this phase's own work is a risk. Cite RESEARCH.md Pitfall 3.

    Do not modify the shipped PORT-06 describe block itself, and do not modify the CONT-04 block from plan 24-03.
  </action>

  <verify>
    <automated>npx playwright test tests/e2e/gallery.spec.ts &amp;&amp; npx vitest run tests/unit/e2e-content-fragility.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx playwright test tests/e2e/gallery.spec.ts` exits 0 across both configured projects.
    - `npx vitest run tests/unit/e2e-content-fragility.test.ts` exits 0.
    - The EDN-12 block contains a post-loop boolean assertion that fails when no gallery carries the cross-link — verified by temporarily inverting it locally, confirming it reports a failure, then restoring it.
    - The EDN-12 block contains a click-through assertion whose resulting URL is matched against an editions-route pattern.
    - `grep -n "scrollHeight" tests/e2e/gallery.spec.ts` matches at least 2 lines (the shipped PORT-06 assertion plus the new CTA-present guard).
    - `git diff tests/e2e/gallery.spec.ts` shows no change inside the PORT-06 describe block and no change inside the CONT-04 describe block.
    - `npm run test:e2e` exits 0 (full Playwright suite).
  </acceptance_criteria>

  <done>
    EDN-12's e2e coverage can no longer pass vacuously: it requires a real cross-linked gallery, proves click-through navigation, and pins the scroll-track headroom the CTA's height sits on top of.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UI-03 cross-viewport verification and sketch-fidelity sign-off</name>

  <files>(no repo files — human visual verification against the built preview)</files>

  <action>
    Run `npm run build && npm run preview` so the developer verifies the REAL build rather than the dev server, then pause and hand off. Present the verification script below verbatim, keeping its four sections (A desktop gallery, B phone gallery, C édition both widths, D no-regression sweep) in order — UI-03 requires both viewport classes on both page types, and this project's v1.6 HOME-16 regression is the documented reason a single-viewport spot check is insufficient. Wait for the resume signal. If the developer reports a mismatch, capture the viewport width and page in the plan summary and treat it as a gap for follow-up rather than closing the phase.
  </action>

  <verify>
    <human-check>Developer confirms all four sections pass: cross-link present and navigable, CTA matching sketch 018 Variant B at both widths, D-09 two-weight relationship reading as intentional, and no layout regression on either page type at either viewport class.</human-check>
  </verify>

  <done>
    UI-03 is signed off at both viewport classes on both page types, with the outcome recorded in the plan summary.
  </done>

  <what-built>
    Both Phase 24 features are implemented and automated-tested: EDN-12's reverse cross-link at the top of gallery detail content, and CONT-04's contact CTA at the end of every gallery's and every édition's photo sequence. The e2e suites assert structure, href, copy, computed styles, and DOM order at 390px and 1280px in both locales.

    What automation cannot settle is visual fidelity to the confirmed sketch and the judgment call in D-09 — whether the two links on an édition page read as two deliberately different weights rather than as a styling inconsistency. VALIDATION.md lists both as manual-only, and this project's v1.6 HOME-16 history is why UI-03 is a first-class criterion rather than an assumption.
  </what-built>

  <how-to-verify>
    Start a preview of the real build: `npm run build && npm run preview`.

    **A — Gallery detail page, desktop (>=768px, use ~1280px wide):**
    1. From the homepage, switch to Grille and open the gallery you cross-linked in task 1.
    2. At the top of the content area, under the hero, confirm the bordered "Voir l'édition « ... »" link is present, sits inline in the flow (not floating over the header/logo), and clicking it lands on that édition's page.
    3. Scroll to the very bottom of the photo grid. Confirm the closing CTA: a short pink hairline rule, generous space above it separating it from the last photo, then "Intéressé·e par une pièce ? Contactez-nous" in the Unbounded display font at a noticeably larger size than the site's small label links, with a pink arrow.
    4. Hover the CTA: an ink-colored underline should appear and the arrow should nudge right. Tab to it with the keyboard: the same reveal plus a pink focus ring.
    5. Click it and confirm you land on the contact page.
    6. Open the same page side by side with `.planning/sketches/018-gallery-edition-contact-cta/index.html` (Variant B, the confirmed winner) and confirm the hairline weight/color, the type size and weight, and the spacing rhythm above the rule match.

    **B — Gallery detail page, phone (<=767px, use ~390px wide):**
    7. Repeat steps 2-5 at phone width. Confirm the CTA text wraps naturally without an awkward orphan, nothing overflows horizontally, and the scroll-up-at-the-top gesture that returns you to the homepage still works.

    **C — Édition detail page, both widths:**
    8. Open an édition that has a related collection set (the reverse of your task-1 pair). Confirm BOTH are visible: the small bordered related-collection link at the top and the heavier CTA at the bottom.
    9. This is D-09's judgment call: confirm the top link still reads as the subtle, secondary affordance it has always been, and the bottom CTA reads as the heavier closing moment — two intentional weights, not an inconsistency. The top link must look exactly as it did before this phase.
    10. Confirm the CTA also appears on an édition that has NO related collection.

    **D — No-regression sweep (UI-03), both widths:**
    11. On both page types, confirm nothing else moved: hero, title, statement, photo grid spacing, footer.
    12. Confirm a gallery with no linked édition shows no empty box, no stray border, and no dead link where the cross-link would be.

    Report any mismatch with the viewport width and page where you saw it.
  </how-to-verify>

  <resume-signal>Type "approved" to close Phase 24, or describe what looks wrong and at which viewport.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

Created by THIS plan:

| Symbol / Path | Kind | Notes |
|---|---|---|
| Published `gallery.relatedEdition` value in Sanity | content | At least one homepage-visible gallery referencing a published édition |
| Deployed Sanity Studio build carrying the `relatedEdition` field | deployment | Via `npm --prefix sanity run deploy`; not CI-invoked |
| `tests/e2e/gallery.spec.ts` → EDN-12 presence + click-through assertions | Playwright assertions | Converts 24-03's conditional contract test into a load-bearing one |
| `tests/e2e/gallery.spec.ts` → CTA-present scroll-track guard | Playwright test | `scrollHeight - innerHeight >= 300` with the CTA rendered |
| `24-05-SUMMARY.md` UAT record | doc | Dual-viewport sign-off for UI-03 |

Note: no database schema push step exists or is appropriate here. Sanity schemas are plain TypeScript bundled by the Studio; there is no migration or push command. `sanity deploy` is a Studio deployment, deliberately manual and never CI-invoked on this project.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Studio editor → Content Lake | A human sets a reference that becomes a public, clickable link on the live site |
| Content Lake → build → static artifact | Newly published content enters the deployed site through the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-13 | Tampering | Editor sets `relatedEdition` to a document that is later unpublished, archived, or deleted | low | mitigate | Plan 24-01's sanitizer reduces a partial dereference to "absent" and records a `relatedEdition.removed` diagnostic; plan 24-01's helper returns `null`; plan 24-03's conditional render then emits nothing. The visitor never sees a broken or dead-end link (D-02). No further control needed. |
| T-24-14 | Elevation of Privilege | `npm --prefix sanity run deploy` publishing a Studio build | medium | mitigate | The deploy runs an already-declared script from the repo's own `sanity/package.json` (`sanity deploy`) — no new dependency, no new credential, and it is executed by the developer at a blocking checkpoint rather than automatically by CI. Task 1 states the exact command so no ad-hoc variant is improvised. |
| T-24-15 | Information Disclosure | Publishing a gallery reference that exposes an unreleased édition | low | mitigate | Both gallery and édition queries already filter on `publicationStatus == "published"`; a reference to an unpublished édition dereferences to partial data under Sanity's published perspective and is dropped by the mitigation in T-24-13. Task 1 instructs the editor to pair with an already-published édition. |
| T-24-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs zero packages (RESEARCH.md Standard Stack: "This phase adds zero dependencies"). `npm --prefix sanity run deploy` invokes an existing declared script; it is not a package-manager install, so the legitimacy gate does not apply. |

ASVS L1 scope note: V2/V3/V4/V6 not applicable to the site surface. V5 unaffected — no new query construction and no visitor-submitted input; the new field is editor-authored in Studio. No `high`-severity threat, so `security_block_on: high` is not tripped.
</threat_model>

<verification>
- `npm run typecheck` exits 0.
- `npm run lint` exits 0.
- `npm run test:unit` exits 0.
- `npm run test:e2e` exits 0 (full Playwright suite — the UI-03 automated regression pass across every page).
- `npm run build && npm run test:artifact` exits 0, and again with `ASTRO_BASE=/ajs-website/` plus `EXPECTED_BASE=/ajs-website/` to match the CI Pages build.
- Task 3's checkpoint returns "approved".
</verification>

<success_criteria>
All four ROADMAP Phase 24 success criteria are true and evidenced:
1. A visitor on a cross-linked gallery detail page sees a link to its Édition and can navigate to it — proven by the hard e2e presence + click-through assertion.
2. A gallery with no associated Édition shows no broken, empty, or dead-end element — proven by the zero-or-one contract assertion and confirmed visually at UAT step 12.
3. A visitor reaching the end of a gallery's and of an édition's photo sequence sees a contact CTA and can click through — proven by the CONT-04 e2e blocks on both page types plus UAT steps 5 and 10.
4. Both features render correctly with no regressions at <=767px and >=768px — proven by the dual-viewport e2e assertions, the full suite regression pass, and the UAT sign-off in task 3.

Plus: Romane can set the new field herself in the deployed Studio.
</success_criteria>

<output>
Create `.planning/phases/24-cross-linking-contact-cta/24-05-SUMMARY.md` when done, recording the gallery/édition pair set in task 1 and the UAT outcome at both viewport classes.
</output>
