---
phase: 20-mobile-navigation-accent-color
plan: 03
type: execute
wave: 2
depends_on: ['20-01', '20-02']
files_modified:
  - src/components/SiteHeader.astro
  - src/components/MobileNavPanel.astro
  - src/components/HomeCarousel.astro
  - tests/e2e/mobile-nav.spec.ts
autonomous: true
requirements: [HOME-13]

must_haves:
  truths:
    - "At 393px on / and /en/, the homepage header shows a hamburger toggle (>=44x44) in place of the inline .site-nav row and the inline language switcher, both of which are hidden (HOME-13, ROADMAP SC #1/#2) — automated: npm run build && npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium"
    - "dialog#mobile-nav exists exactly once on the homepage, is hidden while closed, and is NOT a descendant of [data-role=site-header] — the structural invariant that keeps five pre-existing specs green — automated: same spec"
    - "The panel holds exactly three primary links (Éditions, About, Contact in DOM order) plus exactly one LanguageSwitcher link at Display size in ink, and exactly one small secondary Instagram line (D-04) — automated: same spec"
    - "The panel contains zero a.nav-link elements and zero <header> elements, so .site-nav > a.nav-link stays at 4 and page.locator('header') stays at 1 — automated: same spec"
    - "Every page that does not opt in is byte-for-byte unaffected and About/Contact still serve exactly 4 <script> elements — automated: same spec (plan 20-02's net blocks) plus npx playwright test tests/e2e/visual.spec.ts"
    - "The carousel/grid mode-toggle is still visible at 393px on the homepage — its removal is Phase 21's job, not this phase's — automated: same spec"
  artifacts:
    - "src/components/MobileNavPanel.astro (new) — the full-screen <dialog> panel markup"
    - "src/components/SiteHeader.astro — new optional mobileNav prop, data-mobile-nav attribute, hamburger button, and all .mobile-nav* CSS inside its existing <style is:global> block"
    - "src/components/HomeCarousel.astro — mobileNav={true} at its single <SiteHeader> call site"
    - "tests/e2e/mobile-nav.spec.ts — new homepage structural describe block appended below plan 20-02's net blocks"
  key_links:
    - "HomeCarousel.astro's <SiteHeader variant=\"transparent\"> call site is the ONLY place mobileNav={true} is passed; BaseLayout.astro's call site must keep omitting it entirely"
    - "SiteHeader.astro's <style> block is ALREADY is:global (line 97), so panel rules reaching LanguageSwitcher's .switcher-link need no :global() wrapper — precedent at lines 162-167 and 354-358 of that same block"
    - "the hamburger's color:inherit + currentColor bars hook straight into HomeCarousel.astro's existing per-display-mode header re-skin (lines 1532-1540), so no new mode-specific colour rule is needed"
---

<objective>
Give `SiteHeader.astro` an opt-in mobile-nav mode (D-01), rendered only by the homepage: a hamburger toggle replacing the inline nav row at phone widths, and a full-screen `<dialog>` panel holding the big stacked primary list (Éditions / About / Contact / language switcher) plus a small secondary Instagram line (D-02, D-04).

Purpose: ROADMAP Phase 20 success criteria 1 and 2 — at phone width the homepage's desktop header bar is replaced by a menu control, and the language switcher is reachable from inside that menu rather than shown inline. This plan delivers markup, structure and layout. Opening/closing behaviour and motion are plan 20-04; the halftone accent is plan 20-05.
Output: a new `MobileNavPanel.astro`, an extended `SiteHeader.astro`, a one-line `HomeCarousel.astro` opt-in, and a homepage structural e2e block.
</objective>

## Artifacts this phase produces (this plan's share)

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| component (new) | `src/components/MobileNavPanel.astro` | dialog markup only in this plan; plan 20-04 adds its client `<script>`; it never carries a `<style>` block |
| prop (new) | `SiteHeader`'s `mobileNav?: boolean` (default `false`) | the D-01 opt-in; only homepage call sites pass it |
| HTML attribute (new) | `data-mobile-nav="true"` on `<header data-role="site-header">` | absent entirely when the prop is not passed — this is what makes the mode inert |
| element id (new) | `#mobile-nav` (the `<dialog>`) | referenced by the toggle's `aria-controls` |
| data-role (new) | `data-role="mobile-nav-toggle"` (header hamburger), `data-role="mobile-nav-panel"` (dialog), `data-role="mobile-nav-close"` (in-panel X) | the JS hooks plan 20-04 binds to |
| CSS classes (new) | `.mobile-nav__toggle`, `.mobile-nav__bars`, `.mobile-nav__bar`, `.mobile-nav-panel`, `.mobile-nav-panel__bar`, `.mobile-nav-panel__logo`, `.mobile-nav-panel__logo-img`, `.mobile-nav-panel__close`, `.mobile-nav-panel__nav`, `.mobile-nav-panel__link`, `.mobile-nav-panel__secondary` | all defined in `SiteHeader.astro`'s existing `is:global` block |
| copy (new) | `Ouvrir le menu` / `Open menu`, `Fermer le menu` / `Close menu`, `Menu` (landmark/dialog name) | per 20-UI-SPEC Copywriting Contract; every other label is reused verbatim from existing props |
| test block (new) | `Phase 20 — homepage mobile nav structure` in `tests/e2e/mobile-nav.spec.ts` | |

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-mobile-navigation-accent-color/20-CONTEXT.md
@.planning/phases/20-mobile-navigation-accent-color/20-RESEARCH.md
@.planning/phases/20-mobile-navigation-accent-color/20-PATTERNS.md
@.planning/phases/20-mobile-navigation-accent-color/20-UI-SPEC.md
@.planning/phases/20-mobile-navigation-accent-color/20-VALIDATION.md
@.planning/phases/20-mobile-navigation-accent-color/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the mobileNav prop, the hamburger, and the MobileNavPanel component</name>
  <files>src/components/SiteHeader.astro, src/components/MobileNavPanel.astro, src/components/HomeCarousel.astro</files>
  <read_first>
    - `src/components/SiteHeader.astro` lines 1-95 — the whole frontmatter (the `Props` interface, the destructure, and the header comment stating that every href/label is passed in by the caller and that the component imports only `LanguageSwitcher`) plus the full `<header>` markup. Note the child order: `.logo-mark`, `nav.site-nav`, `<slot name="extra" />`, `<LanguageSwitcher />`.
    - `src/components/LanguageSwitcher.astro` lines 1-36 — it derives its own locale from `Astro.currentLocale === 'en' ? 'en' : 'fr'` and owns its own hrefs, labels and cookie script. This is the in-repo precedent for a nested component reading `Astro.currentLocale` directly, which is what lets the new copy strings be resolved without threading new props through every call site.
    - `src/components/Lightbox.astro` — read the file-top comment and the dialog/close-button markup. The close button lives INSIDE `<dialog id="lightbox">`, not as a shared element outside it. That is the top-layer stacking pattern (20-RESEARCH.md Pitfall 1 / Pattern 3) the panel's duplicated logo + X button must follow.
    - `src/components/HomeCarousel.astro` lines 110-165 — the `<SiteHeader variant="transparent" ...>` call site and the mode-toggle button passed through the `extra` slot. This is the single homepage opt-in point.
    - `src/layouts/BaseLayout.astro` lines 199-216 — the non-homepage `<SiteHeader>` call site that must keep omitting the new prop.
    - `.planning/phases/20-mobile-navigation-accent-color/20-UI-SPEC.md` Copywriting Contract — the exact FR/EN `aria-label` strings and the instruction to reuse `editionsLabel`/`aboutLabel`/`contactLabel`/`instagramLabel`/`instagramNewTabHint` and LanguageSwitcher's own `targetLabel`/`switchHint` verbatim.
    - `.planning/phases/20-mobile-navigation-accent-color/20-02-SUMMARY.md` — the recorded net test count and the `<script>`-count baseline this task must not disturb.
  </read_first>
  <action>
    Extend `SiteHeader.astro`'s `Props` interface with `mobileNav?: boolean` and destructure it with a `false` default. Document in a comment directly above it: this is D-01's opt-in mode; it must be inert — no attribute, no extra element, no extra CSS match, no extra client script — for every caller that omits it, because this component is the project's documented per-page-regression hotspot (Phase 16's `overflow-x` revert, Phase 19's `:global()` scope-hash bug).

    On the `<header>` element add `data-mobile-nav={mobileNav ? 'true' : undefined}`. Astro omits attributes whose value is `undefined`, so the attribute is entirely absent on every non-opted-in page — that absence is what plan 20-02's net asserts.

    Resolve the two new copy strings in SiteHeader's frontmatter, gated behind `mobileNav`, using the same locale derivation `LanguageSwitcher.astro` already uses (`Astro.currentLocale === 'en'`): `openMenuLabel` = `Open menu` / `Ouvrir le menu`, `closeMenuLabel` = `Close menu` / `Fermer le menu`, and `menuLabel` = `Menu` for both locales (the dialog's and its nav landmark's accessible name). Add a comment explaining the deliberate departure from the component's "every label is passed in by the caller" convention: threading three more required props would force an edit at every existing call site, which is exactly the blast radius D-01's inertness requirement exists to avoid — and `LanguageSwitcher.astro`, already a child of this component, sets the precedent for locale-derived copy.

    Add the hamburger as the LAST child inside `<header>`, after `<LanguageSwitcher />`, wrapped in `{mobileNav && (...)}`: a `<button type="button">` with `class="mobile-nav__toggle"`, `data-role="mobile-nav-toggle"`, `aria-controls="mobile-nav"`, `aria-expanded="false"`, and `aria-label={openMenuLabel}`, containing a single `<span class="mobile-nav__bars" aria-hidden="true">` wrapping three empty `<span class="mobile-nav__bar">` elements. Last-child placement puts it at the far right of the flex row, matching the reference image. It must carry no inline styles and no `id`.

    Create `src/components/MobileNavPanel.astro`. It takes props `homeHref`, `siteTitle`, `editionsLabel`, `editionsHref`, `aboutLabel`, `aboutHref`, `contactLabel`, `contactHref`, `instagramUrl`, `instagramLabel`, `instagramNewTabHint`, `logoBlackSrc`, `closeMenuLabel`, `menuLabel` — all `string` — imports `LanguageSwitcher` and nothing else, and renders exactly one top-level `<dialog id="mobile-nav" class="mobile-nav-panel" data-role="mobile-nav-panel" aria-label={menuLabel}>` containing, in order:
    1. `<div class="mobile-nav-panel__bar">` holding an `<a href={homeHref} class="mobile-nav-panel__logo" aria-label={siteTitle}>` wrapping a single `<img src={logoBlackSrc} alt="" class="mobile-nav-panel__logo-img" />` (black logo only — the panel is a white surface, so the header's two-image hover-swap chip mechanic is deliberately NOT reused here), followed by a `<button type="button" class="mobile-nav-panel__close" data-role="mobile-nav-close" aria-label={closeMenuLabel}>` whose inner markup is the SAME `<span class="mobile-nav__bars" aria-hidden="true">` + three `<span class="mobile-nav__bar">` structure as the header hamburger, so one CSS definition drives both glyphs and plan 20-04 can transform this instance into an X.
    2. `<nav class="mobile-nav-panel__nav" aria-label={menuLabel}>` holding `<a class="mobile-nav-panel__link">` items for Éditions, About and Contact in that DOM order (matching the header's own nav priority), then `<LanguageSwitcher />` as the fourth, equal-weight primary item (D-04).
    3. `<a href={instagramUrl} target="_blank" rel="noopener noreferrer" class="mobile-nav-panel__secondary" aria-label={`Instagram ${instagramLabel}`}>` rendering `{instagramLabel}` plus a `<span class="sr-only">{instagramNewTabHint}</span>`.

    Hard structural constraints on `MobileNavPanel.astro`, each of which protects a specific pre-existing spec — state them in a file-header comment with the reason:
    - The `<dialog>` must be rendered as a SIBLING of `<header data-role="site-header">`, never a descendant, because `i18n.spec.ts`, `homepage-carousel-core.spec.ts` and `site-header.spec.ts` all assert header-scoped counts of exactly 1 switcher link and exactly 1 Éditions link.
    - No element inside the panel may use the class `nav-link`, because `site-header.spec.ts`'s cross-page structural-identity test counts `.site-nav > a.nav-link` and expects exactly 4.
    - No `<header>` element may appear inside the panel, because `i18n.spec.ts` clicks through `page.locator('header')` and would trip Playwright strict mode on a second match.
    - The component must contain no `<style>` block (all CSS lands in `SiteHeader.astro`'s existing `is:global` sheet in Task 2) and, in this plan, no `<script>` block.

    In `SiteHeader.astro`, import `MobileNavPanel` and render `{mobileNav && (<MobileNavPanel ... />)}` immediately AFTER the closing `</header>` tag, forwarding the already-available props plus `closeMenuLabel` and `menuLabel`.

    In `HomeCarousel.astro`, add `mobileNav={true}` to its single `<SiteHeader variant="transparent" ...>` call site with a one-line comment citing HOME-13/D-01 and noting that this is the only opt-in in the codebase. Do not touch `BaseLayout.astro`.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/mobile-nav.spec.ts tests/e2e/i18n.spec.ts tests/e2e/site-header.spec.ts tests/e2e/homepage-carousel-core.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `grep -c 'mobileNav' src/layouts/BaseLayout.astro` returns 0 — the non-homepage call site never opts in.
    - `grep -c 'mobileNav={true}' src/components/HomeCarousel.astro` returns 1.
    - In the built output, `[data-role="site-header"]` on `/about/` carries no `data-mobile-nav` attribute and `/about/` still serves exactly the `EXPECTED_SCRIPT_COUNT` recorded in `20-02-SUMMARY.md` — both asserted by plan 20-02's net blocks, which must still pass unchanged.
    - `grep -c 'nav-link' src/components/MobileNavPanel.astro` returns 0 and `grep -c '<style' src/components/MobileNavPanel.astro` returns 0.
    - `tests/e2e/i18n.spec.ts`, `tests/e2e/homepage-carousel-core.spec.ts` and `tests/e2e/site-header.spec.ts` all pass with no edits — proving the sibling-not-descendant placement and the class-name avoidance held.
  </acceptance_criteria>
  <done>The homepage renders a hamburger plus a closed full-screen dialog panel; every other page's header output and client bundle are unchanged; no pre-existing header-scoped spec needed editing.</done>
</task>

<task type="auto">
  <name>Task 2: Structural CSS — full-screen panel, phone-only nav swap, Display-role list, ink switcher override</name>
  <files>src/components/SiteHeader.astro</files>
  <read_first>
    - `src/components/SiteHeader.astro` lines 97-360 — the ENTIRE `<style is:global>` block. Critical: it is already `is:global`, so Astro appends no scope hash to any rule in it. That is why lines 162-167 (`.site-header.site-header--solid .switcher-link { color: inherit; }`) and lines 354-358 (`.site-header .language-switcher .switcher-link { ... }`) successfully reach `LanguageSwitcher.astro`-rendered DOM today. Note the existing `@media (max-width: 767px)` block at lines 301-316 and the `@media (max-width: 400px)` block at lines 322-359, including `.chrome-band { padding: var(--space-sm); }` and `.logo-mark__img { width: 40px; }`.
    - `src/components/Lightbox.astro` — the `dialog#lightbox` rule set: `padding: 0; border: none; max-width/max-height: 100vw/100vh; width: 100vw; height: 100vh;` plus separate `[open]` and `:not([open])` display rules and a `::backdrop` rule. Copy this sizing shape exactly; swap only the background.
    - `src/components/LanguageSwitcher.astro` lines 38-67 — the defaults being overridden: `.language-switcher { font-size: 14px; }` and `.switcher-link { min-height: 44px; padding: 8px; color: var(--color-accent); gap: var(--space-xs); }` plus its own `:focus-visible` outline. Do NOT edit this file.
    - `src/components/HomeCarousel.astro` lines 1486-1545 — the homepage's per-display-mode header re-skin, especially `.home[data-display-mode='grid'] :global(.site-header--transparent) { color: var(--color-ink); }` and the `.nav-link` colour override. This is why the hamburger must use `color: inherit` and `currentColor` bars rather than a hardcoded colour.
    - `src/layouts/BaseLayout.astro` lines 258-320 — the token definitions this task consumes: `--color-dominant`, `--color-ink`, `--color-accent`, `--font-sans`, `--text-display-family/-size/-weight/-leading` (Unbounded / 32px / 600 / 1.2), `--text-label-size/-weight/-leading` (14px / 400 / 1.5), `--space-sm/-md/-lg/-xl/-2xl`, `--tap-target-min`.
    - `.planning/phases/20-mobile-navigation-accent-color/20-UI-SPEC.md` Spacing Scale, Typography and Color sections.
    - `.planning/phases/20-mobile-navigation-accent-color/20-RESEARCH.md` Pitfall 2 and Open Question 2.
  </read_first>
  <action>
    Add every new rule to `SiteHeader.astro`'s EXISTING `<style is:global>` block. Do NOT add a second `<style>` block to this file and do NOT add a `<style>` block to `MobileNavPanel.astro` — a scoped block is exactly where 20-RESEARCH.md Pitfall 2 / Phase 19's scope-hash bug lives, and it is structurally unnecessary here because this block is already global. Do NOT wrap any of the new selectors in `:global(...)`: inside an `is:global` block that wrapper is redundant, and the two pre-existing switcher overrides in this same block prove cross-component reach already works without it. Do NOT edit `LanguageSwitcher.astro`.

    Hamburger and shared bar glyph:
    - `.mobile-nav__toggle` — `display: none` as the BASE state at every width (only the phone media query below opts it in), plus `align-items: center; justify-content: center; min-width: var(--tap-target-min); min-height: var(--tap-target-min); padding: 0; background: none; border: none; color: inherit; cursor: pointer;`. `color: inherit` is load-bearing: it is what makes the bars white over the carousel hero and ink in grid mode via HomeCarousel's existing per-mode header re-skin, with no new mode-specific rule.
    - `.mobile-nav__bars` — `position: relative; display: block; width: 22px; height: 16px;`.
    - `.mobile-nav__bar` — `position: absolute; left: 0; width: 100%; height: 2px; background: currentColor;` with `:nth-child(1)` at `top: 0`, `:nth-child(2)` at `top: 7px`, `:nth-child(3)` at `top: 14px`. Absolute positioning (rather than flex spacing) is deliberate: plan 20-04 morphs these three bars into an X with `transform` alone, which needs a stable, layout-independent origin per bar.
    - `.mobile-nav__toggle:focus-visible` and `.mobile-nav-panel__close:focus-visible` — `outline: 2px solid var(--color-accent); outline-offset: 2px;` (UI-SPEC reserves accent for focus rings inside this phase's markup).

    The phone-only nav swap, inside a NEW `@media (max-width: 767px)` block placed after the existing one (do not modify the existing block's rules — every page that omits the prop must keep them exactly as-is):
    - `.site-header[data-mobile-nav='true'] .site-nav { display: none; }`
    - `.site-header[data-mobile-nav='true'] > .language-switcher { display: none; }` — the direct-child combinator is documentation as much as safety: the panel's own switcher is a sibling of the header, so it can never be caught by this rule.
    - `.site-header[data-mobile-nav='true'] .mobile-nav__toggle { display: inline-flex; }`
    Comment the block with 20-RESEARCH.md Open Question 2's resolution: the existing `max-width: 767px` and `max-width: 400px` trims stay untouched and keep applying to every page that does not pass the prop.

    Panel shell, copying `Lightbox.astro`'s sizing shape:
    - `.mobile-nav-panel` — `padding: 0; border: none; width: 100vw; height: 100vh; max-width: 100vw; max-height: 100vh; background: var(--color-dominant); color: var(--color-ink);`. All four sizing declarations are required: the two `max-*` values are what defeat the UA's default intrinsically-sized, margin-auto-centred dialog box.
    - `.mobile-nav-panel[open]` — `display: flex; flex-direction: column;` (an unconditional `display` on the base rule would beat the UA's own `:not([open])` rule regardless of specificity, which is why the display rules are split).
    - `.mobile-nav-panel:not([open])` — `display: none;`
    - `.mobile-nav-panel::backdrop` — `background: var(--color-dominant);` (the panel is opaque and fills the viewport, so the backdrop only ever shows through during the plan 20-04 transition).

    Panel bar — the logo must land in the SAME screen position as the closed header's logo, which means matching `.chrome-band`'s own padding at both phone breakpoints:
    - `.mobile-nav-panel__bar` — `display: flex; align-items: center; justify-content: space-between; padding: var(--space-lg) var(--space-md);`
    - `.mobile-nav-panel__logo` — `display: inline-flex; align-items: center; text-decoration: none;`
    - `.mobile-nav-panel__logo-img` — `display: block; width: 56px; height: auto;` (matching `.logo-mark__img`)
    - `.mobile-nav-panel__close` — the same box rules as `.mobile-nav__toggle` but `display: inline-flex` unconditionally, since it only ever exists inside the panel.
    - A `@media (max-width: 400px)` block setting `.mobile-nav-panel__bar { padding: var(--space-sm); }` and `.mobile-nav-panel__logo-img { width: 40px; }`, mirroring the existing 400px trims for `.chrome-band` and `.logo-mark__img` so the logo does not visibly jump when the panel opens on a narrow phone.

    Primary list (UI-SPEC Typography "Display" role, Color "ink not accent"):
    - `.mobile-nav-panel__nav` — `flex: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: var(--space-xl);`
    - `.mobile-nav-panel__link` — `display: inline-flex; align-items: center; min-height: var(--tap-target-min); font-family: var(--text-display-family); font-size: var(--text-display-size); font-weight: var(--text-display-weight); line-height: var(--text-display-leading); color: var(--color-ink); text-decoration: none;`
    - `.mobile-nav-panel__link:focus-visible` — the same accent outline as above.
    - `.mobile-nav-panel .language-switcher { font-size: inherit; }` and `.mobile-nav-panel .switcher-link { font-family: var(--text-display-family); font-size: var(--text-display-size); font-weight: var(--text-display-weight); line-height: var(--text-display-leading); color: var(--color-ink); padding: 0; }` — this is the D-04 "equal weight" requirement and the deliberate, scoped reversal of LanguageSwitcher's own accent-pink default. Keep its inherited `min-height: 44px`. Also `.mobile-nav-panel .switcher-link svg { width: 22px; height: 22px; }` so the globe reads proportionate beside 32px Unbounded.

    Secondary line (UI-SPEC "Label" role, `--space-2xl` bottom offset):
    - `.mobile-nav-panel__secondary` — `align-self: center; margin-bottom: var(--space-2xl); display: inline-flex; align-items: center; min-height: var(--tap-target-min); font-family: var(--font-sans); font-size: var(--text-label-size); font-weight: var(--text-label-weight); line-height: var(--text-label-leading); color: var(--color-ink); text-decoration: none;`
    - `.mobile-nav-panel__secondary:focus-visible` — the same accent outline.

    Do not add any motion, transition, `@starting-style`, `transform` or halftone rule in this task — those are plans 20-04 and 20-05. Do not add a rule that hides the panel at `min-width: 768px`: a `display: none` dialog that has been promoted to the top layer would still trap focus, so the desktop guard is a JS concern handled in plan 20-04.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/mobile-nav.spec.ts tests/e2e/visual.spec.ts tests/e2e/site-header.spec.ts tests/e2e/homepage-mobile-responsive.spec.ts tests/e2e/homepage-chrome-nav.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `grep -c '<style' src/components/SiteHeader.astro` returns 1 — exactly one style block, still the `is:global` one.
    - `grep -c ':global(' src/components/SiteHeader.astro` returns 0 — no redundant wrapper was added inside an already-global block.
    - `grep -c '<style' src/components/MobileNavPanel.astro` returns 0.
    - `tests/e2e/visual.spec.ts` passes with `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` empty — `/about/`'s header is pixel-identical (ROADMAP SC #4).
    - Plan 20-02's net blocks in `mobile-nav.spec.ts` still pass, including the `EXPECTED_SCRIPT_COUNT` tripwire.
    - `tests/e2e/site-header.spec.ts`, `tests/e2e/homepage-mobile-responsive.spec.ts` and `tests/e2e/homepage-chrome-nav.spec.ts` pass with no further edits beyond plan 20-02's.
  </acceptance_criteria>
  <done>At phone widths the homepage header shows only logo, mode-toggle and hamburger; the closed panel is a correctly-sized, correctly-typed full-screen surface; every other page is pixel-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Homepage structural e2e block</name>
  <files>tests/e2e/mobile-nav.spec.ts</files>
  <read_first>
    - `tests/e2e/mobile-nav.spec.ts` — the whole file as plan 20-02 left it: the file-header comment, `INLINE_NAV_PATHS`, `EXPECTED_SCRIPT_COUNT`, and net Blocks 1-4. Append below them; do not restructure or weaken any existing block.
    - `src/components/MobileNavPanel.astro` and the new `SiteHeader.astro` markup as written in Task 1 — the exact classes, `data-role` values, `aria-controls`, `aria-expanded` and `aria-label` strings being asserted.
    - `tests/e2e/site-header.spec.ts` lines 133-141 — the `mode-toggle scoping` describe block, the precedent for asserting an element is/is not present on a given page.
    - `.planning/phases/20-mobile-navigation-accent-color/20-CONTEXT.md` `<domain>` — the carousel/grid toggle explicitly STAYS visible on mobile in this phase; its removal is Phase 21.
  </read_first>
  <action>
    Append `test.describe('Phase 20 — homepage mobile nav structure (HOME-13, D-01/D-02/D-04)')` to `tests/e2e/mobile-nav.spec.ts`, looping `for (const path of ['/', '/en/'])` at a 393x852 viewport, with these tests:

    1. "the header swaps its inline nav for a hamburger" — `[data-role="site-header"]` has `data-mobile-nav="true"`; `[data-role="mobile-nav-toggle"]` has count 1 and is visible; its `aria-expanded` is `false` and its `aria-controls` is `mobile-nav`; its bounding box width and height are each at least 44; `[data-role="site-header"] .site-nav` is hidden; `[data-role="site-header"] > .language-switcher` is hidden. Assert the accessible name matches the locale-correct open-menu string (`Ouvrir le menu` on `/`, `Open menu` on `/en/`) — derive it from the loop rather than duplicating two near-identical tests.

    2. "the closed panel exists once, outside the header" — `dialog#mobile-nav` has count 1 and resolves as hidden; and an `evaluate` on that dialog returns `true` for `el.closest('[data-role="site-header"]') === null`. Comment this as the structural invariant that keeps `i18n.spec.ts`, `homepage-carousel-core.spec.ts` and `site-header.spec.ts`'s header-scoped counts intact.

    3. "the panel holds the D-04 two-tier contents" — inside `dialog#mobile-nav`: `.mobile-nav-panel__link` has count 3 and their hrefs contain `editions`, `about`, `contact` in that DOM order; `.switcher-link` has count 1; `.mobile-nav-panel__secondary` has count 1 with `href` equal to the Instagram URL, `target="_blank"`, and a `rel` containing both `noopener` and `noreferrer`; `[data-role="mobile-nav-close"]` has count 1. Use `page.locator('dialog#mobile-nav ...')` — the closed dialog is `display: none`, so assert counts and attributes here, not visibility.

    4. "the panel introduces no selector collisions" — `page.locator('dialog#mobile-nav a.nav-link')` has count 0; `page.locator('dialog#mobile-nav header')` has count 0; `page.locator('[data-role="site-header"] .language-switcher .switcher-link')` still has count exactly 1; `page.locator('[data-role="site-header"] a[href*="editions"]')` still has count exactly 1. Comment each with the pre-existing spec it protects.

    5. "the primary list and switcher render at Display size in ink" — read `getComputedStyle` for the first `.mobile-nav-panel__link` and for `.mobile-nav-panel .switcher-link`, asserting both report `font-size: 32px`, `font-weight: 600`, a `font-family` containing `Unbounded`, and a `color` of `rgb(26, 26, 26)`. Because the dialog is closed, force the read by temporarily setting the dialog's `style.display` to `flex` inside a single `evaluate` and restoring it before returning — or equivalently by reading the resolved values off a cloned node; whichever is used, leave no residual inline style behind. This is the D-04 equal-weight / UI-SPEC Color assertion and it is what would catch a scope-hash failure on the switcher override.

    6. "the secondary line renders at Label size" — same technique, asserting `font-size: 14px`, `font-weight: 400` and a 48px bottom margin.

    7. "the mode-toggle still ships on mobile" — `[data-role="mode-toggle"]` has count 1 and is visible at 393px. Comment that removing it is Phase 21's scope, so a regression here would be silent scope creep.

    8. "no horizontal overflow with the hamburger present" — at 393px and again at 320px, `document.documentElement.scrollWidth <= document.documentElement.clientWidth`, and the header's rendered height is at most 80px (the same budget `homepage-mobile-responsive.spec.ts` already enforces).

    Then add one desktop test outside the path loop: at 1280x800 on `/`, `[data-role="mobile-nav-toggle"]` resolves as hidden and `dialog#mobile-nav` resolves as hidden, while `[data-role="site-header"] .site-nav > a.nav-link` has count 4 and all four are visible — the positive form of plan 20-02's desktop-unchanged net now that the markup actually exists.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium</automated>
    <automated>npm run build &amp;&amp; npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium` passes, with the reported test count strictly greater than the count recorded in `20-02-SUMMARY.md`.
    - `npm run build && npm run test:e2e` — the full suite is green.
    - `npm run typecheck` exits 0 and `npx vitest run` passes (plan 20-01's unit tests unaffected).
    - `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` is empty.
    - `grep -c "closest(" tests/e2e/mobile-nav.spec.ts` returns at least 1 — the sibling-not-descendant invariant is asserted, not assumed.
  </acceptance_criteria>
  <done>The homepage's phone-width structural contract is asserted positively, the pre-existing header-scoped counts are asserted to be intact, and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> `SiteHeader`'s `mobileNav` prop | A build-time boolean from a trusted call site. Its only effect is to render additional static markup; it accepts no user input. |
| Sanity/site-config copy -> panel markup | `editionsLabel`/`aboutLabel`/`contactLabel`/`instagramLabel`/`instagramNewTabHint` and the hrefs are the SAME already-rendered values the header nav uses today; the panel re-renders them through Astro's default HTML escaping with no `set:html` anywhere. |
| shared component -> unrelated pages | The real risk surface of this plan: a shared-component edit leaking onto pages that did not opt in. Not a confidentiality/integrity boundary but the phase's dominant failure mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Tampering | `SiteHeader.astro` shared-component edit reaching non-opted-in pages (Phase 16 / Phase 19 recurrence class) | high | mitigate | Three independent gates, all required green in this plan: (a) plan 20-02's inertness net across six paths at two viewports in both locales; (b) `tests/e2e/visual.spec.ts`'s `shared-site-header.png` snapshot on `/about/`, never re-baselined; (c) the `data-mobile-nav` attribute is emitted as `undefined` when the prop is absent so no new CSS selector can match, and every new rule is either attribute-gated or namespaced under `.mobile-nav`. Additionally no `<style>` block is added to `MobileNavPanel.astro` and no `:global()` wrapper is added inside the already-global sheet, which is what removes the Phase 19 scope-hash failure mode entirely rather than re-litigating it. |
| T-20-07 | Information Disclosure | panel copy rendered from props | low | accept | All strings are the same public site copy already rendered in the header nav on every page; Astro escapes interpolated text by default and no `set:html` is introduced. No new data reaches the client. |
| T-20-08 | Elevation of Privilege | `<dialog>` top-layer promotion covering page chrome | low | mitigate | The panel is opaque, viewport-filling and contains its own duplicated logo plus a close control (20-RESEARCH.md Pattern 3), so no page affordance is left visually present but unreachable behind the top layer. The header hamburger is not relied upon while the panel is open. |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero packages installed (20-RESEARCH.md "Package Legitimacy Audit: Not applicable"). Any executor decision that a package is needed invalidates this acceptance and must stop for a human. |

T-20-06 is rated `high`, matching `security_block_on: high` — its three gates are mandatory acceptance criteria in Tasks 1, 2 and 3 and must all be green before this plan is considered done.
</threat_model>

<verification>
1. `npm run typecheck` exits 0.
2. `npx vitest run` passes.
3. `npm run build && npm run test:e2e` is fully green.
4. `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` is empty.
5. `/about/` and `/contact/` still serve the `EXPECTED_SCRIPT_COUNT` recorded in `20-02-SUMMARY.md`.
6. Confirm the preview server on port 4321 serves THIS worktree's `dist/` before trusting results.
</verification>

<success_criteria>
- ROADMAP SC #1: at phone width the homepage's inline header bar is replaced by a hamburger control (the control's opening behaviour lands in plan 20-04).
- ROADMAP SC #2: the language switcher renders inside the panel as a full-size, equal-weight primary item and is no longer shown inline at phone width on the homepage.
- ROADMAP SC #4 (structural half): every non-opted-in page and the homepage's own desktop header are unchanged, proven by the net plus the un-rebaselined visual snapshot.
- D-01 honoured: one opt-in prop, one opt-in call site, zero edits to `BaseLayout.astro`, zero duplicated `.site-nav`/`.nav-link` CSS.
- D-02/D-04 honoured structurally: persistent top-left logo, top-right toggle, centred big stacked list, small secondary Instagram line near the bottom.
</success_criteria>

<output>
Create `.planning/phases/20-mobile-navigation-accent-color/20-03-SUMMARY.md` when done. Record the new `mobile-nav.spec.ts` test count, confirm the `EXPECTED_SCRIPT_COUNT` tripwire value held, and note the exact class/`data-role` names shipped so plan 20-04 binds to the right hooks.
</output>
