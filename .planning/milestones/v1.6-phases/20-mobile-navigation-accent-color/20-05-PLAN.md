---
phase: 20-mobile-navigation-accent-color
plan: 05
type: execute
wave: 4
depends_on: ['20-04']
files_modified:
  - src/components/MobileNavPanel.astro
  - src/components/SiteHeader.astro
  - tests/e2e/accessibility.spec.ts
  - tests/e2e/mobile-nav.spec.ts
autonomous: true
requirements: [HOME-13, HOME-16]

must_haves:
  truths:
    - "The open panel carries the site's existing halftone dot texture in its top-right corner, reusing PageTitleHeader's exact radial-gradient and background-size values, contained entirely inside the dialog's own box with no overflow rule added anywhere (D-02, UI-SPEC Interaction & Motion) — automated: npm run build && npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium"
    - "The halftone never intercepts taps and never paints over the primary list or the secondary line — automated: same spec"
    - "The open mobile-nav state has no serious or critical axe violations on / or /en/ at a 393px viewport, closing 20-VALIDATION.md's HOME-13 accessibility row — automated: npm run build && npx playwright test tests/e2e/accessibility.spec.ts --project=chromium"
    - "The closed mobile header state has no serious or critical axe violations at a 393px viewport on / and /en/ — automated: same spec"
    - "The whole phase passes the CI blocking-gate sequence in its real order, including the GitHub Pages base build, the un-prefixed-link guard and the static-artifact verifier — automated: the Task 3 command chain"
    - "HOME-16's random starting accent still holds after every Phase 20 change — automated: npm run build && npx playwright test tests/e2e/homepage-accent-random.spec.ts --project=chromium"
  artifacts:
    - "src/components/MobileNavPanel.astro — a decorative .mobile-nav-panel__halftone element as the dialog's first child"
    - "src/components/SiteHeader.astro — halftone CSS with fresh containment geometry, two keyframes, a reduced-motion gate, and explicit stacking so the texture stays behind the content"
    - "tests/e2e/accessibility.spec.ts — mobile-viewport axe passes for the closed header and the open panel, both locales"
    - "tests/e2e/mobile-nav.spec.ts — halftone containment/stacking assertions"
  key_links:
    - "the halftone's radial-gradient and 9px background-size values are copied verbatim from PageTitleHeader.astro's .page-title-header__halftone; its POSITIONING geometry is re-derived for a 100vw/100vh dialog box and its viewport-relative centering constants are deliberately NOT ported (20-RESEARCH.md Open Question 1)"
    - "a modal <dialog> is position: fixed via the UA stylesheet, which is what makes the absolutely-positioned halftone resolve against the dialog's own box — overriding that position value would break the full-viewport panel"
    - "tests/scripts/verify-static-artifact.mjs plus the .github/workflows/deploy.yml base-path grep guard are the deploy-time gates this phase must clear before /gsd-verify-work"
---

<objective>
Finish the panel's visual identity with the site's own halftone dot texture (D-02's resolved discretion item), close the phase's automated accessibility gap, and run the whole phase through the CI blocking-gate sequence in its real order.

Purpose: 20-UI-SPEC.md resolves D-02's texture question in favour of reusing the existing halftone rather than inventing a new decorative element, and 20-VALIDATION.md's HOME-13 accessibility row is still open because the current axe suite only ever runs at a desktop viewport with no dialog open. This plan closes both, then proves the phase deploy-safe.
Output: the halftone layer, mobile-viewport axe coverage for the closed and open states in both locales, and a green CI-order sweep.
</objective>

## Artifacts this phase produces (this plan's share)

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| element (new) | `.mobile-nav-panel__halftone` (first child of `dialog#mobile-nav`, `aria-hidden="true"`) | purely decorative |
| keyframes (new) | `mobile-nav-halftone-fade-in`, `mobile-nav-halftone-drift` | distinct names from PageTitleHeader's own keyframes so the two never collide in the global sheet |
| test (new) | mobile-viewport closed-header axe pass, and open-panel axe pass, in `tests/e2e/accessibility.spec.ts` | both locales |
| test block (new) | halftone containment and stacking assertions in `tests/e2e/mobile-nav.spec.ts` | |

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-mobile-navigation-accent-color/20-CONTEXT.md
@.planning/phases/20-mobile-navigation-accent-color/20-RESEARCH.md
@.planning/phases/20-mobile-navigation-accent-color/20-PATTERNS.md
@.planning/phases/20-mobile-navigation-accent-color/20-UI-SPEC.md
@.planning/phases/20-mobile-navigation-accent-color/20-VALIDATION.md
@.planning/phases/20-mobile-navigation-accent-color/20-03-SUMMARY.md
@.planning/phases/20-mobile-navigation-accent-color/20-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Halftone dot texture in the panel's top-right corner</name>
  <files>src/components/MobileNavPanel.astro, src/components/SiteHeader.astro</files>
  <read_first>
    - `src/components/PageTitleHeader.astro` — the `.page-title-header__halftone` rule (around lines 210-280) in full, plus the long comment block above `.page-title-header` explaining the overflow history. Extract exactly two things to reuse verbatim: the `background-image: radial-gradient(rgba(26, 26, 26, 0.16) 1.4px, transparent 1.6px)` declaration and `background-size: 9px 9px`. Note everything you must NOT port: the `top/bottom: -700px` bleed, the `left/right: calc(50% - 50vw)` viewport-relative centering, and the `--editorial-page-max`/`--editorial-page-padding-inline` mask constants — all tuned for a content-column-relative element, not a full-viewport dialog. Note also its own fade-in + drift keyframes and its trailing `prefers-reduced-motion: reduce` block, which are the structural pattern to mirror under new names.
    - The same file's comment about Phase 16's reverted site-wide `overflow-x: hidden` (which broke `position: sticky` on About's pinned photo and DetailHero's pinned reveal) and Phase 19's replacement of component-level `overflow-x: clip` with geometric containment. That history is why this task uses bounded geometry rather than any overflow rule.
    - `src/components/MobileNavPanel.astro` and `src/components/SiteHeader.astro`'s `<style is:global>` block as plan 20-04 left them — the `.mobile-nav-panel` sizing/display rules and the `.mobile-nav-panel__bar` / `__nav` / `__secondary` layout rules the halftone must sit behind.
    - `.planning/phases/20-mobile-navigation-accent-color/20-RESEARCH.md` Open Question 1 and its recommendation.
    - `.planning/phases/20-mobile-navigation-accent-color/20-UI-SPEC.md` Interaction & Motion Contract, the halftone row.
  </read_first>
  <action>
    In `MobileNavPanel.astro`, add `<div class="mobile-nav-panel__halftone" aria-hidden="true"></div>` as the FIRST child inside the `<dialog>`, before `.mobile-nav-panel__bar`. It is purely decorative and must remain empty.

    In `SiteHeader.astro`'s existing `<style is:global>` block, add:
    - `.mobile-nav-panel__halftone` — `position: absolute; top: 0; right: 0; z-index: 0; pointer-events: none;` sized with `width: min(100vw, 320px); height: min(100vh, 320px);`, plus the two verbatim declarations from `PageTitleHeader.astro`: the `radial-gradient(rgba(26, 26, 26, 0.16) 1.4px, transparent 1.6px)` background image and `background-size: 9px 9px`. Fade the field out toward the panel's interior with a `mask-image` (and its `-webkit-` prefixed twin, matching the existing halftone's pairing) using `radial-gradient(circle 300px at top right, ...)` with the same multi-stop opacity ramp shape the existing halftone uses — but expressed in the dialog's own coordinates, with no `--editorial-page-*` term and no `50vw` centering term. Bounded `min()` sizing anchored at the dialog's own top-right corner is the entire containment strategy: the element cannot overflow a box it is inset within.
    - Two keyframe sets named `mobile-nav-halftone-fade-in` (opacity 0 to 1) and `mobile-nav-halftone-drift` (`background-position` from `0 0` to `90px 45px`), wired as `animation: mobile-nav-halftone-fade-in 0.6s ease 0.15s forwards, mobile-nav-halftone-drift 10s linear infinite;` with `opacity: 0` as the start state. The names must NOT reuse `PageTitleHeader`'s own keyframe names — both live in the global stylesheet and a name collision would silently rebind one component's animation to the other's timing.
    - Explicit stacking so the texture stays behind the content: `position: relative; z-index: 1;` on `.mobile-nav-panel__bar`, `.mobile-nav-panel__nav` and `.mobile-nav-panel__secondary`. Without this the absolutely-positioned halftone paints ABOVE the in-flow flex children (positioned elements with `z-index: auto` paint after non-positioned in-flow content), putting a dot field over 32px Unbounded text.
    - A trailing `@media (prefers-reduced-motion: reduce)` rule setting `animation: none` and `opacity: 1` on `.mobile-nav-panel__halftone`, mirroring the existing halftone's own reduced-motion handling so the texture is still present but static.

    Two hard prohibitions, each with a comment naming the reason:
    - Do NOT add any `overflow` declaration — not to `html`, not to `body`, not to `.mobile-nav-panel`, not to the halftone. Phase 16 shipped and reverted a site-wide `overflow-x: hidden` that broke `position: sticky` on two unrelated pages, and Phase 19 replaced the component-level `overflow-x: clip` with geometric containment for the same class of reason. Containment here is geometric by construction.
    - Do NOT set `position` on `.mobile-nav-panel`. A modal `<dialog>` receives `position: fixed` from the UA stylesheet, which is exactly what makes it fill the viewport and what makes the absolutely-positioned halftone resolve against the dialog's own box. Overriding it with `relative` would drop the panel back into normal flow and destroy the full-screen takeover.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/mobile-nav.spec.ts tests/e2e/page-title-header-bleed.spec.ts tests/e2e/visual.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0.
    - `grep -c 'overflow' src/components/MobileNavPanel.astro` returns 0, and the count of `overflow` occurrences in `src/components/SiteHeader.astro` is unchanged from before this task (record both numbers in the SUMMARY).
    - `grep -c 'position: relative' src/components/SiteHeader.astro` includes the three new content-layer rules, and `grep -cE '\.mobile-nav-panel \{[^}]*position:' src/components/SiteHeader.astro` returns 0 — the dialog's UA `position: fixed` is untouched.
    - `grep -c 'mobile-nav-halftone' src/components/SiteHeader.astro` returns at least 4 (two keyframe definitions plus both animation references).
    - `grep -c 'page-title-header-halftone' src/components/SiteHeader.astro` returns 0 — no keyframe name reuse.
    - `tests/e2e/page-title-header-bleed.spec.ts` (Phase 19's 18-test overflow/sticky regression net) passes untouched, and `tests/e2e/visual.spec.ts` passes with `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` empty.
    - New assertions appended to `mobile-nav.spec.ts`: with the panel open at 393x852, `.mobile-nav-panel__halftone` reports `pointer-events: none`, its bounding box is fully inside the dialog's bounding box on all four edges, `document.documentElement.scrollWidth <= document.documentElement.clientWidth` still holds, and a tap at the coordinates of the first `.mobile-nav-panel__link` still activates that link rather than being swallowed by the texture.
  </acceptance_criteria>
  <done>The open panel carries the site's own halftone texture in its top-right corner, behind the content, tap-transparent, geometrically contained, and static under reduced motion — with Phase 19's overflow regression net still green.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile-viewport accessibility coverage for the closed header and the open panel</name>
  <files>tests/e2e/accessibility.spec.ts</files>
  <read_first>
    - `tests/e2e/accessibility.spec.ts` in full (56 lines) — the static path loop, the `new AxeBuilder({page}).withTags(['wcag2a', 'wcag2aa', 'wcag21aa']).analyze()` call, the serious/critical `violations` filter, and the message-joining assertion shape. Note that every existing test runs at the project's DEFAULT (desktop) viewport, which is exactly why the mobile nav's states have zero coverage today. Note also the two special-case tests at the bottom (dynamic édition slug, the 404 route) and copy their comment-driven style for explaining why a case cannot be a plain array entry.
    - `src/components/MobileNavPanel.astro` and the header hamburger as plans 20-03/20-04 left them — the `aria-label`, `aria-controls`, `aria-expanded` and `aria-hidden` attributes axe will evaluate.
    - `.planning/phases/20-mobile-navigation-accent-color/20-VALIDATION.md` — the HOME-13 row marked "needs new mobile-viewport variant" that this task closes.
  </read_first>
  <action>
    Add two new tests to `tests/e2e/accessibility.spec.ts`, both looping `for (const path of ['/', '/en/'])` and both reusing the file's existing tag list, filter and assertion shape verbatim — do not introduce a second axe configuration or a different severity threshold.

    Test A, closed mobile header: set a 393x852 viewport, go to the path, and run the axe analysis. Comment that the existing suite covers these two paths only at the default desktop viewport, so the phone-width header — where the inline nav and inline switcher are hidden and a hamburger button is the sole nav affordance — was previously unscanned.

    Test B, open mobile-nav panel: set a 393x852 viewport, go to the path, click `[data-role="mobile-nav-toggle"]`, wait for `dialog#mobile-nav` to be visible, then run the axe analysis. Comment that a closed `<dialog>` is `display: none` and therefore invisible to axe, so this state genuinely cannot be reached by any existing test — and that this is the state 20-VALIDATION.md's HOME-13 accessibility row asks for.

    If either test surfaces a serious or critical violation, fix the MARKUP rather than the assertion. The likely candidates and their correct fixes: a duplicated landmark needing a distinguishing accessible name (the panel's `<nav aria-label>` already provides one — verify it survived); insufficient contrast on the panel's ink-on-white text (the tokens are the site's own `--color-ink` on `--color-dominant`, so a failure here means a token was not actually used); or a control lacking an accessible name (the hamburger and the X each carry an `aria-label`, and the bar spans are `aria-hidden`). Never narrow the tag list, raise the severity threshold, or add an axe rule exclusion to make a violation disappear.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/accessibility.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && npx playwright test tests/e2e/accessibility.spec.ts --project=chromium` passes with 4 more tests than before this task (2 paths x 2 states).
    - `grep -c 'disableRules\|exclude(' tests/e2e/accessibility.spec.ts` returns 0 — no rule was silenced.
    - `grep -c "withTags(\['wcag2a', 'wcag2aa', 'wcag21aa'\])" tests/e2e/accessibility.spec.ts` equals the total number of `analyze()` calls in the file — every scan uses the identical tag list.
    - `grep -c 'mobile-nav-toggle' tests/e2e/accessibility.spec.ts` returns at least 1 — the open state is genuinely reached, not merely navigated to.
  </acceptance_criteria>
  <done>Both the closed phone-width header and the open panel are axe-clean at serious/critical severity in both locales, with no rule exclusions.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide regression sweep in CI blocking-gate order</name>
  <files>tests/e2e/mobile-nav.spec.ts</files>
  <read_first>
    - `.github/workflows/deploy.yml` lines 40-131 — the exact blocking-gate order this task reproduces locally: Sanity Studio lint + build, `npm run typecheck` BEFORE the first build, the root-base build, `npm run test:artifact`, the Playwright e2e run, `npm run test:coverage`, then the rebuild with `ASTRO_BASE: /ajs-website/`, the un-prefixed-link grep guard, and finally `npm run test:artifact` with `EXPECTED_BASE: /ajs-website/`. Read the comment above the grep guard: it exists because a hardcoded root-relative link once shipped and bypassed the base path entirely, and no unit test would have caught it.
    - `tests/scripts/verify-static-artifact.mjs` — what the artifact verifier actually checks and how it consumes `EXPECTED_BASE`.
    - `CLAUDE.md` "GitHub Actions pipeline, in actual step order" — the prose restatement of the same sequence.
    - `.planning/phases/20-mobile-navigation-accent-color/20-VALIDATION.md` "Sampling Rate" (phase gate row) and the Manual-Only Verifications table.
    - `.planning/phases/20-mobile-navigation-accent-color/20-03-SUMMARY.md` and `20-04-SUMMARY.md` — the recorded test counts and the `EXPECTED_SCRIPT_COUNT` value to re-confirm.
  </read_first>
  <action>
    Run the phase gate locally in the CI's real order and fix any fallout at the source rather than by relaxing a gate:
    1. `npm --prefix sanity run lint` then `npm --prefix sanity run build` — the Studio is untouched by this phase, so both must pass unchanged; a failure means something unrelated drifted and should be reported, not worked around.
    2. `npm run typecheck`.
    3. `npm run build` (root base) then `npm run test:artifact`.
    4. `npm run test:e2e` — the full suite under both configured projects.
    5. `npm run test:coverage` — the 70/65/70/70 thresholds, which now include plan 20-01's `pickRandomGalleryIndex`.
    6. `ASTRO_BASE=/ajs-website/ npm run build`, then reproduce the workflow's un-prefixed-link guard against `dist/` (the same `grep -rE` pattern the workflow uses for a bare root href and a bare `/en/` href), then `EXPECTED_BASE=/ajs-website/ npm run test:artifact`. The panel introduces four new anchors (logo, Éditions, About, Contact) plus a second `LanguageSwitcher` instance and the Instagram link — every one of them must resolve through an already-base-aware value passed in as a prop or produced by `getSwitcherHref`, so this guard is the specific gate that catches a hardcoded literal slipping into the new markup.
    <!-- planner-discipline-allow: href="/" -->
    <!-- planner-discipline-allow: href="/en/" -->

    Then append a final `test.describe('Phase 20 — phase gate cross-checks')` block to `tests/e2e/mobile-nav.spec.ts` with two durable assertions that outlive this session:
    - At 393x852 on `/` and `/en/`, every anchor inside `dialog#mobile-nav` has an `href` that starts with the value of `import.meta.env.BASE_URL`-equivalent prefix as observed on the page (read the document's own `<base>`-independent convention by comparing against the header logo's own `href` prefix), OR is an absolute `https://` URL (the Instagram link). Comment that this is the in-test companion to the workflow's base-path grep guard, so a future edit that hardcodes a root-relative panel link fails a test rather than only failing a deploy.
    - At 393x852 on `/`, the homepage still satisfies all three of Phase 20's user-visible contracts at once: the hamburger is visible, `--current-accent` read off `.home` is one of the `data-hero-color` values present in the page's own hidden data list, and the mode-toggle is still visible. This is the one assertion that ties HOME-13 and HOME-16 together in a single state and is what a later phase would trip if it regressed either.

    Record in the SUMMARY: every command run with its outcome, the final full-suite e2e and unit counts, the coverage percentages, and the confirmed `EXPECTED_SCRIPT_COUNT`.
  </action>
  <verify>
    <automated>npm --prefix sanity run lint &amp;&amp; npm --prefix sanity run build &amp;&amp; npm run typecheck &amp;&amp; npm run build &amp;&amp; npm run test:artifact &amp;&amp; npm run test:e2e &amp;&amp; npm run test:coverage</automated>
    <automated>ASTRO_BASE=/ajs-website/ npm run build &amp;&amp; EXPECTED_BASE=/ajs-website/ npm run test:artifact</automated>
    <human-check>
      Open the homepage on a real phone-width viewport (or device emulation) in BOTH Chromium and Safari/WebKit and confirm: (1) the menu's open and close motion reads as deliberate rather than instant in both engines, and is instant under `prefers-reduced-motion: reduce` — 20-VALIDATION.md Manual-Only row 1, D-03; (2) the open panel matches `.planning/phases/20-mobile-navigation-accent-color/20-mobile-menu-reference.png` on logo position, hamburger-to-X placement, the big stacked list style, the small secondary bottom line, and the corner halftone accent — 20-VALIDATION.md Manual-Only row 2; (3) reloading the homepage several times visibly shows different starting accent colours — ROADMAP success criterion 3.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Every command in both `<automated>` chains exits 0, run in the listed order.
    - The un-prefixed-link guard finds no match in the `ASTRO_BASE=/ajs-website/` build (the workflow's own `grep -rE` over `dist/` produces no output and therefore no failure).
    - `npm run test:coverage` reports statements >= 70, branches >= 65, functions >= 70, lines >= 70.
    - `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` is empty — no snapshot was regenerated at any point in the phase.
    - `/about/` and `/contact/` still serve the `EXPECTED_SCRIPT_COUNT` recorded in `20-02-SUMMARY.md`.
    - The three human-check items are recorded in the SUMMARY as pending end-of-phase verification (`workflow.human_verify_mode` is `end-of-phase`, so they are not a blocking in-plan checkpoint).
  </acceptance_criteria>
  <done>The phase clears the CI blocking-gate sequence locally in its real order, including the GitHub Pages base build and its two deploy-time guards, and carries two durable cross-cutting assertions for future phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decorative CSS -> layout of unrelated pages | The halftone's containment strategy is the boundary that matters: Phase 16 and Phase 19 both shipped regressions where a containment choice for one component altered unrelated pages sharing the same document. |
| build-time href values -> deployed base path | The panel's six new links cross into a base-prefixed deploy. A hardcoded root-relative literal would 404 on GitHub Pages while passing every local root-base test. |
| rendered HTML data attributes -> client `style.setProperty()` | Carried forward from plan 20-01; re-verified here by the combined HOME-13/HOME-16 cross-check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-10 | Denial of Service | halftone containment altering layout on unrelated pages | high | mitigate | The documented recurrence class for this codebase (Phase 16's reverted site-wide `overflow-x: hidden` broke `position: sticky` on About and DetailHero; Phase 19 replaced component-level `overflow-x: clip` with geometric containment). Mitigated by prohibiting every `overflow` declaration outright, using bounded `min()` geometry anchored inside the dialog instead, and gating on Phase 19's own 18-test `page-title-header-bleed.spec.ts` net plus a per-edge bounding-box containment assertion and a document-overflow assertion. |
| T-20-11 | Tampering | a hardcoded root-relative `href` in the panel bypassing the deploy base path | high | mitigate | Task 3 reproduces the workflow's own base-path grep guard locally against an `ASTRO_BASE=/ajs-website/` build, and adds a durable Playwright assertion that every panel anchor either shares the header logo's base prefix or is an absolute `https://` URL — so the same bug class fails a test in future, not only a deploy. |
| T-20-12 | Repudiation | silencing an axe rule to make the open-panel scan pass | medium | mitigate | Task 2 forbids `disableRules`, `exclude()`, tag-list narrowing and severity-threshold changes, and asserts each prohibition with a count check; the acceptance criteria require every `analyze()` call in the file to use the identical tag list. |
| T-20-13 | Tampering | halftone keyframe name colliding with `PageTitleHeader`'s in the global stylesheet | medium | mitigate | Distinct `mobile-nav-halftone-*` names are mandated and asserted (`page-title-header-halftone` must not appear in `SiteHeader.astro`), because both rule sets live in the same global cascade and a name collision would silently rebind one component's animation to the other's timing. |
| T-20-01 | Tampering | accent colour reaching `style.setProperty()` | low | mitigate | Carried forward from plan 20-01 and re-verified by Task 3's combined cross-check, which asserts the observed `--current-accent` is a member of the page's own already-normalised `data-hero-color` set — i.e. that no second, unvalidated colour path was introduced anywhere in the phase. |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero packages installed across the entire phase (20-RESEARCH.md "Package Legitimacy Audit: Not applicable"). Any executor decision that a package is needed invalidates this acceptance and must stop for a human. |

T-20-10 and T-20-11 are rated `high`, matching `security_block_on: high` — every listed mitigation is a mandatory acceptance criterion above.
</threat_model>

<verification>
1. `npm --prefix sanity run lint && npm --prefix sanity run build` pass.
2. `npm run typecheck` exits 0.
3. `npm run build && npm run test:artifact` pass.
4. `npm run test:e2e` is fully green under both the `chromium` and `webkit-mobile` projects.
5. `npm run test:coverage` meets 70/65/70/70.
6. `ASTRO_BASE=/ajs-website/ npm run build`, the un-prefixed-link grep guard, and `EXPECTED_BASE=/ajs-website/ npm run test:artifact` all pass.
7. `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` is empty.
8. Human checks (end-of-phase, per `workflow.human_verify_mode`): the D-03 motion feel in both engines, visual fidelity against `20-mobile-menu-reference.png`, and visibly different starting accent colours across reloads.
9. Confirm the preview server on port 4321 serves THIS worktree's `dist/` before trusting results.
</verification>

<success_criteria>
- D-02's texture discretion item is resolved by reuse, not invention: the site's exact halftone dot values appear in the panel's top-right corner with containment geometry derived fresh for the dialog's own box.
- 20-VALIDATION.md's HOME-13 accessibility row is closed: the closed phone-width header and the open panel are both axe-clean at serious/critical severity in both locales.
- The phase clears the CI blocking-gate sequence in its real order, including both GitHub Pages deploy-time guards.
- HOME-13 and HOME-16 are asserted together in one homepage state, so a future phase regressing either fails a test.
- Phase 19's overflow/sticky regression net and the `/about/` header visual snapshot are both untouched and green.
</success_criteria>

<output>
Create `.planning/phases/20-mobile-navigation-accent-color/20-05-SUMMARY.md` when done. Record: every phase-gate command with its outcome, the final e2e and unit test counts, the coverage percentages, the confirmed `EXPECTED_SCRIPT_COUNT`, the `overflow` occurrence counts before and after Task 1, and the three human-check items as pending end-of-phase verification.
</output>
