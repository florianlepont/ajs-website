---
phase: 20-mobile-navigation-accent-color
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/mobile-nav.spec.ts
  - tests/e2e/homepage-chrome-nav.spec.ts
  - tests/e2e/homepage-mobile-responsive.spec.ts
  - tests/e2e/site-header.spec.ts
autonomous: true
requirements: [HOME-13]

must_haves:
  truths:
    - "Every page that does NOT opt into the mobile nav (About, Contact, gallery detail, Éditions overview, édition detail, both locales) renders zero mobile-nav markup and keeps its four inline .nav-link items plus its inline language switcher visible at 393px AND at 1280px — automated: npm run build && npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium"
    - "The homepage at 1280px keeps four visible inline .nav-link items, one visible inline switcher link, and no visible mobile-nav affordance — the desktop-unchanged half of ROADMAP success criterion 4 — automated: same spec"
    - "About and Contact each still serve exactly 4 <script> elements, so no new client bundle leaks onto pages that do not render the mobile nav — automated: same spec"
    - "No pre-existing spec asserts a contract Phase 20 deliberately changes (homepage inline nav / inline switcher visible at phone widths), and no pre-existing assertion can pass vacuously on zero-sized rects — automated: npm run build && npx playwright test tests/e2e/site-header.spec.ts tests/e2e/homepage-chrome-nav.spec.ts tests/e2e/homepage-mobile-responsive.spec.ts --project=chromium"
    - "The full e2e suite is green on the UNMODIFIED src/ tree after this plan's test edits, proving the net is real rather than a tautology written to fit a change already made — automated: npm run build && npm run test:e2e"
  artifacts:
    - "tests/e2e/mobile-nav.spec.ts (new — inertness + desktop-unchanged net only; behaviour blocks arrive in plans 20-03/20-04)"
  key_links:
    - "SiteHeader.astro is the shared component every non-homepage page renders via BaseLayout.astro; this net is what proves plan 20-03's new opt-in prop leaves those call sites untouched"
    - "tests/e2e/visual.spec.ts's shared-site-header.png snapshot (taken on /about/) is the pixel-level companion to this net — it is run as a gate here and in every later plan, never re-baselined"
---

<objective>
Write the Phase 20 regression net BEFORE any source change, and prove it green on the unmodified tree.

Purpose: `SiteHeader.astro` is this project's documented per-page-regression hotspot — it has broken unrelated pages twice (Phase 16's site-wide `overflow-x` revert, Phase 19's `:global()` scope-hash bug), both times because a fix targeted one page while the SHARED component affected others (20-CONTEXT.md "Shared-component regression history"). The v1.5 retrospective's own Patterns Established entry is explicit: for any plan touching a component that has already caused a production regression, write the regression-net test file FIRST, prove it passes on the pre-change tree, and only then make the risky change. This plan is that step. It also retires the three pre-existing assertions that encode a contract HOME-13 intentionally changes, so plan 20-03 cannot be blocked by a stale expectation.

Output: `tests/e2e/mobile-nav.spec.ts` (net only, no behaviour assertions yet) plus surgical realignments of three existing specs — every one of them green on the current `src/` tree.
</objective>

## Artifacts this phase produces (this plan's share)

| Kind | Symbol / path | Notes |
|------|---------------|-------|
| test file (new) | `tests/e2e/mobile-nav.spec.ts` | inertness net + desktop-unchanged net; plans 20-03 and 20-04 append behaviour describe blocks to this same file |
| test constant (new) | `INLINE_NAV_PATHS` in `tests/e2e/mobile-nav.spec.ts` | the paths that keep SiteHeader's inline nav at every viewport |
| test constant (new) | `EXPECTED_SCRIPT_COUNT` in `tests/e2e/mobile-nav.spec.ts` | value 4 — the current `<script>` count on `/about/` and `/contact/`; a tripwire for client-bundle leakage |

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-mobile-navigation-accent-color/20-CONTEXT.md
@.planning/phases/20-mobile-navigation-accent-color/20-RESEARCH.md
@.planning/phases/20-mobile-navigation-accent-color/20-PATTERNS.md
@.planning/phases/20-mobile-navigation-accent-color/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the inertness + desktop-unchanged net and prove it green pre-change</name>
  <files>tests/e2e/mobile-nav.spec.ts</files>
  <read_first>
    - `tests/e2e/site-header.spec.ts` lines 1-70 and 133-250 — the canonical `for (const path of [...])` sweep shape, the `[data-role="site-header"]`-scoped locator convention, the `.site-nav > a.nav-link` selector used by the cross-page structural-identity test, and the `mode-toggle scoping` describe block that is this net's closest structural analog.
    - `tests/e2e/accessibility.spec.ts` lines 25-41 — the established way to reach an édition detail page: discover the slug from `/editions/`'s first `.editions-index__row` href, never hardcode a slug and never use the main nav.
    - `src/components/SiteHeader.astro` lines 53-95 — the exact markup the net asserts against today: `<header data-role="site-header">`, `.logo-mark`, `nav.site-nav` with four `a.nav-link` children (Éditions, About, Contact, Instagram), the `extra` slot, and `<LanguageSwitcher />` as a direct child.
    - `src/layouts/BaseLayout.astro` lines 199-218 — `{headerVariant !== 'none' && (<SiteHeader ... />)}` followed by `<main><slot /></main>`; this is why every non-homepage page gets the header from BaseLayout and the homepage (`headerVariant="none"`) gets it from `HomeCarousel.astro` instead.
    - `playwright.config.ts` — `baseURL` `http://localhost:4321`, `webServer` `npm run preview` (so `npm run build` must precede any run), `chromium` full suite, `webkit-mobile` restricted to `**/*.smoke.spec.ts`.
  </read_first>
  <action>
    Create `tests/e2e/mobile-nav.spec.ts`. Open it with a file-header comment stating: this file is the Phase 20 (HOME-13) regression net, written and proven green BEFORE `SiteHeader.astro` gained its opt-in mobile-nav mode, per the v1.5 retrospective's regression-net-first pattern and 20-CONTEXT.md's shared-component regression history; plans 20-03 and 20-04 append the positive homepage behaviour blocks below these net blocks. Do not restate any acceptance-criteria literal in that comment.

    Declare two module constants: `INLINE_NAV_PATHS` = `['/about/', '/en/about/', '/contact/', '/en/contact/', '/galleries/silos/', '/editions/']` (the pages that keep SiteHeader's inline nav at every viewport), and `EXPECTED_SCRIPT_COUNT` = `4` (the current `<script>` element count served on `/about/` and `/contact/`; comment it as a client-bundle-leakage tripwire, and state that a change to this number means a script was added to pages that do not render the mobile nav).

    Block 1 — `test.describe('Phase 20 net — pages without the mobile nav are untouched (HOME-13, D-01)')`. Nest a `for (const path of INLINE_NAV_PATHS)` inside a `for (const viewport of [{ width: 393, height: 852, label: 'phone' }, { width: 1280, height: 800, label: 'desktop' }])`. Each test sets the viewport, goes to the path, and asserts: `[data-role="site-header"]` has no `data-mobile-nav` attribute (use `not.toHaveAttribute('data-mobile-nav', /.*/)`); `page.locator('dialog#mobile-nav')` has count 0; `page.locator('[data-role="mobile-nav-toggle"]')` has count 0; `header.locator('.site-nav > a.nav-link')` has count 4 and every one of them is visible (assert visibility per-index via `nth(i)`); `header.locator('.language-switcher .switcher-link')` has count 1 and is visible; and `document.documentElement.scrollWidth <= window.innerWidth`.

    Block 2 — `test.describe('Phase 20 net — the first published édition detail page is untouched')`. One test: discover the slug from `/editions/`'s first `.editions-index__row` href, then run the same assertion set as Block 1 at 393px. Keep it separate from the static sweep because the slug cannot be a literal array entry.

    Block 3 — `test.describe('Phase 20 net — the homepage desktop header is unchanged (ROADMAP SC #4)')`. For `['/', '/en/']` at 1280x800, assert: four visible `.site-nav > a.nav-link` items inside `[data-role="site-header"]`; exactly one visible `[data-role="site-header"] .language-switcher .switcher-link`; `page.locator('dialog#mobile-nav')` resolves as hidden via `toBeHidden()`; `page.locator('[data-role="mobile-nav-toggle"]')` resolves as hidden via `toBeHidden()`; and no horizontal document overflow. Add a comment explaining WHY `toBeHidden()` rather than `toHaveCount(0)` is used here: Playwright's `toBeHidden()` is satisfied both when an element is absent (the tree today) and when it exists but is not rendered (the tree after plan 20-03), which is precisely what makes this block a net that holds across the change instead of a snapshot of one moment.

    Block 4 — `test.describe('Phase 20 net — no client bundle leaks onto pages without the mobile nav')`. For `['/about/', '/contact/']`, fetch the served HTML for the path (via `page.goto` then `page.content()`) and assert the number of `<script` occurrences equals `EXPECTED_SCRIPT_COUNT`. Comment that this exists because the mobile nav's open/close listener is the first client `<script>` `SiteHeader.astro`'s subtree would ever carry, and Astro hoists component scripts per page — if this count rises, the script must be relocated into a component or inline island that only the homepage renders.

    Do NOT add any assertion about the homepage at phone widths in this plan — that contract does not exist yet and belongs to plan 20-03. Do NOT modify any file under `src/` in this task.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build && npx playwright test tests/e2e/mobile-nav.spec.ts --project=chromium` passes with zero failures and zero skips on the UNMODIFIED `src/` tree. Record the reported test count in the SUMMARY.
    - `git status --porcelain src/` reports no changes after this task — the net is proven against untouched source.
    - `grep -c 'toHaveCount(4)' tests/e2e/mobile-nav.spec.ts` returns at least 1 and `grep -c 'EXPECTED_SCRIPT_COUNT' tests/e2e/mobile-nav.spec.ts` returns at least 2, confirming both non-vacuous positive assertions are present.
    - Re-running the spec a second time passes identically (no flake).
  </acceptance_criteria>
  <done>A green, non-vacuous net exists that will fail if plan 20-03's shared-component change leaks onto any non-opted-in page, onto the homepage desktop header, or into another page's client bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the three pre-existing assertions that encode a contract HOME-13 changes</name>
  <files>tests/e2e/homepage-chrome-nav.spec.ts, tests/e2e/homepage-mobile-responsive.spec.ts, tests/e2e/site-header.spec.ts</files>
  <read_first>
    - `tests/e2e/homepage-chrome-nav.spec.ts` lines 1-56 — the `Instagram nav link (HOME-04)` describe block; specifically the test at lines 40-54 which loads `/` at 393px and asserts `.site-nav a[href="https://www.instagram.com/ajs_romanelepont/"]` is VISIBLE.
    - `tests/e2e/homepage-mobile-responsive.spec.ts` lines 34-50 — the `narrow-phone header regression` test, which loads `/` at 320px and ends with an UNSCOPED `page.getByRole('link', { name: /Passer en anglais/ })` visibility assertion. Note that after plan 20-03 the panel renders a second `LanguageSwitcher`, so this unscoped locator would resolve to two elements and trip Playwright strict mode regardless of visibility.
    - `tests/e2e/site-header.spec.ts` lines 95-132 — the `single-row fit across the mobile range` sweep over `widths = [320, 360, 374, 375, 390, 767]` and `pages = [{'/about/','solid'}, {'/','transparent'}]`. Note that its `evaluate` returns only centre-Y values, so if the nav and switcher become `display: none` both rects collapse to all-zeros, both centres read `0`, and the `<= 5` assertion passes VACUOUSLY — a silent hole this task closes.
    - `src/components/LanguageSwitcher.astro` lines 18-35 — the accessible name is the visible `EN`/`FR` label plus the sr-only `switchHint` (`Passer en anglais` / `Switch to French`), which is why the regex locator above matches.
    - `.planning/phases/20-mobile-navigation-accent-color/20-CONTEXT.md` `<domain>` — HOME-13 is scoped to the HOMEPAGE only; every other page keeps its current inline mobile header. That asymmetry is what these three edits encode.
  </read_first>
  <action>
    Make exactly three surgical edits. Every edit must be green on the UNMODIFIED `src/` tree AND remain green after plan 20-03 — never write an expectation that only becomes true later.

    Edit 1 — `tests/e2e/homepage-chrome-nav.spec.ts`. Split the 393px test into two. Keep a homepage test that sets 393x800, loads `/`, and asserts only `document.documentElement.scrollWidth <= window.innerWidth` (rename its title to describe a homepage mobile overflow guard, dropping the inline-nav visibility claim). Add a second test that sets 393x800, loads `/about/`, and makes the original `.site-nav a[href="https://www.instagram.com/ajs_romanelepont/"]` visibility assertion there — the inline-nav Instagram affordance is the contract for pages that keep the inline nav. Above both, add a comment citing HOME-13: at phone widths the homepage's Instagram affordance moves out of `.site-nav` into the mobile nav panel's secondary line, and that new affordance is asserted in `tests/e2e/mobile-nav.spec.ts`.

    Edit 2 — `tests/e2e/homepage-mobile-responsive.spec.ts`, `narrow-phone header regression`. Keep the `/` 320px test's overflow assertion (`scrollWidth <= clientWidth`) and its `headerHeight <= 80` assertion unchanged. Replace the trailing unscoped `getByRole('link', { name: /Passer en anglais/ })` visibility assertion with a `[data-role="site-header"] .language-switcher .switcher-link` locator asserted via `toHaveCount(1)` — a count that stays 1 both today and after plan 20-03, because the panel's own switcher renders OUTSIDE the `<header data-role="site-header">` element. Then add a sibling test that sets 320px, loads `/about/`, and asserts that same scoped locator is VISIBLE, preserving the switcher-reachability-at-320px contract on the pages that keep the inline nav. Comment both with HOME-13 and the strict-mode reason the unscoped locator had to go.

    Edit 3 — `tests/e2e/site-header.spec.ts`, `single-row fit across the mobile range`. Two changes to the same describe block. First, close the vacuous-pass hole: extend the `evaluate` return with `navHeight` and `switcherHeight` (from the same two `getBoundingClientRect()` calls) and assert both are greater than 0 before the existing same-row and overflow assertions — this makes the block fail loudly if either element ever becomes `display: none`, instead of silently agreeing that `0 === 0`. Second, split the matrix so the phone-range widths `[320, 360, 374, 375, 390, 767]` run only against `/about/`, and add a second loop running `/` against `[768, 834, 1024, 1280]` (where the homepage keeps its inline single-row header permanently). Comment the split with HOME-13: below 768px the homepage no longer has an inline nav row to fit, and its phone-width header contract is asserted in `tests/e2e/mobile-nav.spec.ts`; the non-vacuity guard is what would have caught the silent pass.

    Do NOT weaken any other assertion, do NOT delete a whole describe block, and do NOT modify any file under `src/` in this task.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test tests/e2e/site-header.spec.ts tests/e2e/homepage-chrome-nav.spec.ts tests/e2e/homepage-mobile-responsive.spec.ts tests/e2e/mobile-nav.spec.ts tests/e2e/visual.spec.ts --project=chromium</automated>
    <automated>npm run build &amp;&amp; npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - All four targeted specs plus `visual.spec.ts` pass on the UNMODIFIED `src/` tree; `tests/e2e/visual.spec.ts-snapshots/shared-site-header.png` is NOT re-baselined (`git status --porcelain tests/e2e/visual.spec.ts-snapshots/` reports nothing).
    - `npm run build && npm run test:e2e` — the full suite is green with `git status --porcelain src/` reporting no changes.
    - `grep -c 'Passer en anglais' tests/e2e/homepage-mobile-responsive.spec.ts` returns 0 (the strict-mode-fragile unscoped locator is gone).
    - `grep -c 'navHeight' tests/e2e/site-header.spec.ts` returns at least 2 (returned from `evaluate` and asserted), proving the non-vacuity guard landed.
    - The homepage still appears in the `single-row fit` sweep at widths 768 and above (`grep -c '834' tests/e2e/site-header.spec.ts` returns at least 1).
  </acceptance_criteria>
  <done>No pre-existing spec asserts an inline homepage nav row at phone widths, no assertion can pass on collapsed zero-size rects, and the whole suite is green with `src/` untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production behaviour | This plan writes only test files. It introduces no runtime code path, no new input surface, and no data crossing into the rendered site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Tampering | the regression net itself (`tests/e2e/mobile-nav.spec.ts`) | medium | mitigate | A net that can pass vacuously provides false assurance and is the exact failure mode Phase 16's and Phase 19's regressions slipped through. Mitigated by three structural requirements enforced in the acceptance criteria: every inertness block asserts a POSITIVE count/visibility (four `.nav-link` items, one visible switcher link) rather than only absences; `git status --porcelain src/` must be empty when the net passes, proving it is green against untouched source rather than fitted to a change; and Edit 3 adds explicit non-zero-height guards so collapsed rects can never satisfy the same-row assertion. |
| T-20-05 | Repudiation | later plans re-baselining `visual.spec.ts` snapshots to make a regression "pass" | medium | mitigate | `tests/e2e/visual.spec.ts-snapshots/shared-site-header.png` is asserted as un-modified in this plan's acceptance criteria and is re-run as a gate in plans 20-03, 20-04 and 20-05. Re-baselining it is never an acceptable resolution for a Phase 20 failure — a diff there means the shared header changed on `/about/`, which ROADMAP success criterion 4 forbids. |
| T-20-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero packages installed (20-RESEARCH.md "Package Legitimacy Audit: Not applicable"). Playwright, Vitest and `@axe-core/playwright` are all already present at pinned versions. |

No threat in this plan is rated `high`, so `security_block_on: high` is satisfied.
</threat_model>

<verification>
1. `npm run build && npm run test:e2e` is fully green.
2. `git status --porcelain src/` is empty — no source file was touched by this plan.
3. `git status --porcelain tests/e2e/visual.spec.ts-snapshots/` is empty — no snapshot re-baselined.
4. Confirm the preview server on port 4321 serves THIS worktree's `dist/` before trusting results — concurrent sessions on this repo are the norm and `reuseExistingServer` is on outside CI.
</verification>

<success_criteria>
- A non-vacuous inertness net exists for every non-homepage SiteHeader consumer at both a phone and a desktop viewport, in both locales where routes exist.
- A desktop-unchanged net exists for the homepage (ROADMAP success criterion 4's desktop half).
- A client-bundle-leakage tripwire exists for `/about/` and `/contact/`.
- No pre-existing assertion encodes an inline homepage nav row at phone widths, and none can pass vacuously.
- Everything above is green with `src/` unmodified.
</success_criteria>

<output>
Create `.planning/phases/20-mobile-navigation-accent-color/20-02-SUMMARY.md` when done. Record the reported test count for `mobile-nav.spec.ts`, the full-suite pass count, and the confirmed `EXPECTED_SCRIPT_COUNT` baseline.
</output>
