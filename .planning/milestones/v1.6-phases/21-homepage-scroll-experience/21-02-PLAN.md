---
phase: 21-homepage-scroll-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage-wordmark-peek.spec.ts
autonomous: true
requirements: [HOME-14]
must_haves:
  truths:
    - "On a touchscreen tablet at 768px or wider, tapping a progress dash or the autoplay toggle never navigates away from the homepage."
    - "The existing carousel's touch coverage runs at a viewport width where the carousel still exists after this phase ships."
  artifacts:
    - "An early-return target guard inside the existing .home-hero__photo touchend listener in src/components/HomeCarousel.astro"
    - "A CR-01 regression test in tests/e2e/homepage-wordmark-peek.spec.ts asserting a control tap does not open a gallery"
  key_links:
    - "The guard lives in the OLD carousel handler (HomeCarousel.astro ~line 985), not in the new mobile scroll deck — that handler stays live for touchscreen tablets at 768px and above (phase success criterion 5)"
    - "Re-scoping the touch describes to a tablet-width device is what keeps this coverage meaningful once plan 21-04 hides the carousel below 767px"
---

<objective>
Fix Phase 20's carryover Critical bug CR-01 (D-11) in the existing carousel touch handler, and re-scope this spec file's touch coverage from a phone-width device to a tablet-width touch device so it still exercises live code after plan 21-04 retires the carousel below 767px.

Purpose: `21-RESEARCH.md` Pitfall 1 is explicit that D-11's fix belongs in the EXISTING `.home-hero__photo` touchend handler (`HomeCarousel.astro` lines ~985-1002), not in the new mobile deck — that handler keeps running on touchscreen tablets at 768px and wider, which phase success criterion 5 requires to behave exactly as before. A regression test written against the new mobile markup would pass while the real tablet bug survived. Doing this in wave 1, before any deck markup exists, means the fix is provable against the carousel as it stands today.

Output: one early-return guard in `HomeCarousel.astro`, and a re-scoped + extended `tests/e2e/homepage-wordmark-peek.spec.ts`.

Ordering note: Task 1 re-points the viewport FIRST so Task 2's new regression test is authored inside a describe block that will still be valid after the whole phase lands.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/21-homepage-scroll-experience/21-CONTEXT.md
@.planning/phases/21-homepage-scroll-experience/21-RESEARCH.md
@.planning/phases/21-homepage-scroll-experience/21-PATTERNS.md
@.planning/phases/20-mobile-navigation-accent-color/20-REVIEW.md
</context>

<artifacts_this_phase_produces>
This plan produces no new public symbol. It changes behavior inside one existing listener and re-scopes existing test blocks.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Re-scope this spec's touch coverage to a tablet-width touch device and retire its phone-only carousel assertions</name>
  <files>tests/e2e/homepage-wordmark-peek.spec.ts</files>
  <read_first>
    - `tests/e2e/homepage-wordmark-peek.spec.ts` lines 85-122 — the first `test.describe('mobile', ...)` block (custom-cursor inertness on touch), including its `devices['iPhone 14 Pro']` destructure-and-spread idiom and the comment explaining why `defaultBrowserType` is stripped.
    - `tests/e2e/homepage-wordmark-peek.spec.ts` lines 825-905 — the second `test.describe('mobile', ...)` block (tap-to-open, horizontal swipe) and its synthesized `TouchEvent` helper.
    - `tests/e2e/homepage-wordmark-peek.spec.ts` lines 980-1020 — the grid hero wordmark cutout test that exercises both a 393px half and a 1280px half in one test body.
    - `.planning/phases/21-homepage-scroll-experience/21-RESEARCH.md` Pitfall 1 — the reason this re-scope is required rather than optional.
  </read_first>
  <action>
    Three edits, all inside `tests/e2e/homepage-wordmark-peek.spec.ts`. Every edit must leave the suite green immediately, before any production code in this phase changes — verify that claim by running the spec after the edits.

    1. Both `test.describe('mobile', ...)` blocks: replace the `devices['iPhone 14 Pro']` descriptor with a tablet-width touch descriptor, keeping the existing `defaultBrowserType`-stripping destructure idiom and its explanatory comment intact. Use `devices['iPad (gen 7)']` (810x1080 — comfortably at or above the 768px breakpoint, still `hasTouch`/`isMobile`, so the coarse-pointer path these tests exist to cover is unchanged). Rename each block from `mobile` to something that states the new scope, e.g. `touchscreen tablet (>=768px)`. Add a comment above each renamed block recording WHY: phase 21 retires the carousel below 767px, so a phone-width descriptor would leave these tests asserting against a `display: none` subtree; the behavior they guard (coarse-pointer inertness of the hover cursor, and the carousel's own tap/swipe handling) lives on at tablet widths per phase success criterion 5.

    2. The `grid hero wordmark cutout` test: delete the 393px half (the `setViewportSize({ width: 393, ... })` navigation, the `Grille` click, and the `mobileWordmark` assertions) and keep the 1280px desktop half as the whole test. Rename the describe and the test so neither still claims mobile scope, and replace the deleted half with a comment recording that the mobile grid hero tile's photo-cutout wordmark is retired below 767px by phase 21 (the grid itself no longer renders there), so only the desktop no-cutout guard remains meaningful.

    3. Leave every other test in the file untouched, including the three `setViewportSize({ width: 1600, height: 900 })` desktop tests and the 393px/1280px tests near the end of the file that do not depend on the carousel being interactive below 767px. Do not change any assertion text or threshold.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm run build && npx playwright test homepage-wordmark-peek --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test homepage-wordmark-peek --project=chromium` passes against the CURRENT production code (no `src/` change has happened yet in this task).
    - `grep -c "iPhone 14 Pro" tests/e2e/homepage-wordmark-peek.spec.ts` returns 0.
    - `grep -c "iPad (gen 7)" tests/e2e/homepage-wordmark-peek.spec.ts` returns 2.
    - The file still contains both a tap-to-open test and a horizontal-swipe test (each now under the tablet-scoped describe).
    - `src/` is unchanged by this task.
  </acceptance_criteria>
  <done>The carousel's touch coverage now runs at a width where the carousel still exists after this phase, and no assertion depends on retired phone-width carousel behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the CR-01 regression test, then guard the carousel touchend handler (D-11)</name>
  <files>tests/e2e/homepage-wordmark-peek.spec.ts, src/components/HomeCarousel.astro</files>
  <read_first>
    - `src/components/HomeCarousel.astro` lines 959-1037 — the `touchstart`/`touchend` listeners on `.home-hero__photo` and `openCurrent()`, including the `.is-opening` class it adds synchronously.
    - `src/components/HomeCarousel.astro` lines 1305-1325 — the EXISTING desktop click handler's own caption exclusion. That guard is the shape to mirror; the touch path is the one missing it.
    - `src/components/HomeCarousel.astro` lines 206-248 — the caption markup, confirming the progress dashes and autoplay toggle are both descendants of the caption element.
    - `.planning/phases/20-mobile-navigation-accent-color/20-REVIEW.md` — the CR-01 entry and its suggested fix.
    - The tap-to-open test you re-scoped in Task 1 — its synthesized `TouchEvent` helper is the exact pattern this new test reuses.
  </read_first>
  <behavior>
    New e2e case, authored inside the tablet-scoped touch describe from Task 1:
    - Given the homepage carousel at tablet width, when a `touchstart` and a `touchend` at identical coordinates are dispatched with a progress-dash button as the event target (bubbling up to the photo listener), then the page does not navigate away from the homepage AND the photo element does not acquire the class the open path adds synchronously.
    - A second case with the autoplay toggle as the event target behaves the same way.
    - The pre-existing tap-on-the-photo case (target = the photo itself) still navigates to the current gallery — the guard must not break the real tap-to-open path.
    This test FAILS before the guard exists (the open path fires) and PASSES after.
  </behavior>
  <action>
    RED first: add the two new cases described above to the tablet-scoped touch describe in `tests/e2e/homepage-wordmark-peek.spec.ts`. Reuse the file's existing `page.evaluate` + `new Touch(...)` + `new TouchEvent(...)` construction verbatim, changing only the `target` element resolution: resolve the progress dash via its `data-action` attribute selector and the autoplay toggle via its `data-role` attribute selector, and dispatch on that element so the event bubbles to the photo's listener. Assert both that `page.url()` still resolves to the homepage and that the photo element does not carry the opening-state class. Run the spec and confirm the new cases fail.

    GREEN: in `src/components/HomeCarousel.astro`, add an early-return target guard as the first statement of the EXISTING `.home-hero__photo` `touchend` listener (the one at ~line 985 that runs `detectSwipeDirection` and then the tap check). Resolve `event.target` as an element, walk it with `closest` against the caption selector, and return when it matches — the same exclusion the desktop click handler at ~line 1317 already applies. Add a comment on the guard citing D-11 and `20-REVIEW.md` CR-01, and stating why this belongs here rather than in the new mobile deck: this handler stays live for touchscreen tablets at 768px and wider, which phase success criterion 5 protects.

    Do not add a guard to the `touchstart` listener, do not add a second listener, and do not change `openCurrent()`. Do not touch any other part of `HomeCarousel.astro` — the deck work belongs to plans 21-04 through 21-06.
  </action>
  <verify>
    <automated>cd /Users/florian/Projects/ajs-website && npm run build && npx playwright test homepage-wordmark-peek --project=chromium && npx playwright test homepage-carousel-core homepage-content-display --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test homepage-wordmark-peek --project=chromium` passes, including both new control-tap cases and the pre-existing tap-to-open and swipe cases.
    - `npx playwright test homepage-carousel-core homepage-content-display --project=chromium` still passes (desktop carousel behavior unchanged, UI-02).
    - `grep -n "closest" src/components/HomeCarousel.astro | grep -c "home-hero__caption"` returns at least 2 (the pre-existing click guard plus the new touch guard).
    - `npm run typecheck` and `npm run lint` pass.
    - The diff to `src/components/HomeCarousel.astro` in this plan is confined to the touchend listener and its comment — no CSS, no markup, no other script region.
  </acceptance_criteria>
  <done>A real-coordinate control tap on a touchscreen tablet no longer misfires a gallery navigation, and a regression test proves it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch input -> in-page navigation | A DOM touch event on the homepage decides whether to trigger a same-origin navigation to a gallery detail route. All destinations are build-time-rendered internal hrefs; no user-supplied URL is involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Tampering | `.home-hero__photo` touchend listener in `src/components/HomeCarousel.astro` | medium | mitigate | This IS the tap-hijack pattern `21-RESEARCH.md` names in its Security Domain table: a tap intended for a control (progress dash / autoplay toggle) is silently reinterpreted as "open the current gallery." Task 2 adds the same `closest(...)` exclusion guard the desktop click path already uses, and the two new e2e cases assert a control tap neither navigates nor enters the opening state. |

## ASVS Applicability (level 1)

Per `21-RESEARCH.md` Security Domain: V2 Authentication, V3 Session Management, V4 Access Control, V5 Input Validation, and V6 Cryptography all remain **not applicable**. This plan changes which DOM events trigger an internal navigation; it adds no auth, session, access-control, input-parsing, or cryptographic surface. No new ASVS category becomes in-scope.

## Schema-Push Applicability

**Not applicable, explicitly.** No Sanity schema file, ORM migration, or database schema is touched. No `[BLOCKING]` schema-push task is required.
</threat_model>

<verification>
- `npm run build && npx playwright test homepage-wordmark-peek --project=chromium` — green.
- `npx playwright test --project=chromium` — the full e2e suite must still be green at the end of this plan; this plan changes no visual behavior at any viewport.
- `npm run typecheck`, `npm run lint` — clean.
- Explicit non-goal check: `git diff` for this plan must NOT show any new markup, CSS rule, or second `<script>` block in `src/components/HomeCarousel.astro`.
</verification>

<success_criteria>
- D-11's carryover CR-01 bug is fixed in the handler that survives this phase at tablet widths, with a regression test that would catch its return.
- This spec file's touch coverage runs at a viewport where the carousel still renders after plan 21-04.
- Desktop and tablet carousel behavior is otherwise untouched (UI-02, phase success criterion 5).
</success_criteria>

<output>
Create `.planning/phases/21-homepage-scroll-experience/21-02-SUMMARY.md` when done.
</output>
