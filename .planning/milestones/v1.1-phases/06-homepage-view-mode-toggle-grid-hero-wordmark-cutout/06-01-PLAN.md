---
phase: 06-homepage-view-mode-toggle-grid-hero-wordmark-cutout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage.spec.ts
autonomous: false
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "Homepage renders exactly ONE mode-toggle button (data-role='mode-toggle'), not two; its accessible name is 'Grille'/'Grid' in carousel mode and flips to 'Carrousel'/'Carousel' in grid mode, and clicking it switches modes both ways (HOME-01, D-01/D-02/D-03)"
    - "In grid mode the first child of .home-grid__tiles is a NON-link .home-grid__tile--hero <div> (aspect-ratio 1/1, solid accent background) holding the three-line wordmark + intro paragraph; no separate .home-grid__intro band exists above the grid (HOME-02, D-04/D-05/D-06)"
    - "In carousel mode .home-hero__wordmark uses background-clip:text with transparent fill and background-image from the current gallery's hero photo (--wordmark-photo), revealing the photo through the letterforms, and is never invisible/blank because --wordmark-photo is set at server render (HOME-03, D-07/D-08/D-09)"
    - "The 'Discover other galleries' CTA (.home-hero__cta / ctaLabel) is fully removed; the single toggle is the only mode-switch control (D-10)"
    - "Existing homepage behaviors still pass: auto-advance every 6000ms, pause on hover, FR|EN switcher, clickable gallery title, mobile hero visible at 375px"
  artifacts:
    - "src/components/HomeCarousel.astro (modified)"
    - "tests/e2e/homepage.spec.ts (modified)"
  key_links:
    - "render() updates --wordmark-photo in sync with heroImg.src on every prev/next/auto-advance tick, so the carousel cutout always tracks the visible photo"
    - "The single toggle's aria-label is updated in the same script path that flips root.dataset.displayMode; a stale label breaks the e2e accessible-name locators"
    - "Initial --wordmark-photo is server-rendered from firstGallery.heroSrc so the transparent wordmark is never blank before/without JS"
    - "Removing .home-hero__accent's panel-wide background is required for the cutout; .home-hero__intro must get its own --current-accent backing box to stay legible"
    - "The client <script> is a plain module and cannot read frontmatter vars — the two localized toggle labels must be passed via data-* attributes on the button for the script to reassign aria-label"
---

<objective>
Deliver Phase 6's three homepage refinements as one coherent, polished surface, all inside the already-shipped `src/components/HomeCarousel.astro`:

- **HOME-01 (D-01/D-02/D-03):** replace the two separate carousel/grid toggle buttons with a single unified button whose icon + accessible name reflect the mode it switches *to*.
- **HOME-02 (D-04/D-05/D-06):** move the grid-view hero (wordmark + intro) out of a full-width band and into the first *tile* of the grid — a same-size square, solid accent background, non-link `<div>`.
- **HOME-03 (D-07/D-08/D-09):** give the carousel-mode wordmark a transparent photo-cutout treatment (photo visible through the letterforms), enlarged for legibility.
- **D-10 ripple:** remove the now-redundant "Discover other galleries" CTA.

Purpose: the homepage's view-mode control, grid layout, and hero wordmark should read as one intentional, coherent surface rather than a collection of separate controls and bands.

Output: modified `HomeCarousel.astro` + updated `tests/e2e/homepage.spec.ts`, with the new contracts driven test-first and the visual cutout confirmed live.

Note on prior decisions: this phase's D-01/D-10 **supersede** Phase 04.3's D-05 (CTA-as-toggle) and D-07 (two static-named toggle buttons) — do not re-implement 04.3's two-button/locked-name model.
</objective>

<phase_goal>
## Phase Goal (MVP user story)

**As a** homepage visitor, **I want to** switch between carousel and grid views with a single clear control and see the "Atelier Jacqueline Suzanne" wordmark rendered as a polished, photo-revealing headline, **so that** browsing Romane's work feels like one coherent, intentional surface rather than a set of disjoint buttons and bands.

*(Synthesized from the ROADMAP Phase 6 Success Criteria, which are already written from the visitor's perspective — the ROADMAP `**Goal:**` line is outcome-shaped; the visitor role + capabilities are unambiguous in criteria 1–3, so no story was invented.)*

This phase refines an already-working end-to-end surface. Task ordering is MVP test-first: (1) encode the new contracts as failing e2e tests, (2) make them pass with the real component changes, (3) live-verify the one requirement (HOME-03 cutout) whose "photo visibly reveals through the letters" outcome cannot be asserted from computed CSS alone.
</phase_goal>

<execution_context>
@/home/user/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/home/user/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-CONTEXT.md
@.planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-UI-SPEC.md
@.planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-PATTERNS.md
@src/components/HomeCarousel.astro
@tests/e2e/homepage.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the HOME-01/02/03 contracts as failing e2e tests (RED)</name>
  <files>tests/e2e/homepage.spec.ts</files>
  <read_first>
    - tests/e2e/homepage.spec.ts (current spec — know the existing locators at lines ~36/57/68 and the mobile test at ~112-139)
    - src/components/HomeCarousel.astro (current markup, to know what does/does not exist yet)
    - .planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-UI-SPEC.md ("Layout & Interaction Notes" → "Test impact (D-02)")
    - .planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-PATTERNS.md (tests/e2e/homepage.spec.ts section)
  </read_first>
  <behavior>
    - HOME-01: exactly one button with attribute data-role="mode-toggle" exists; zero elements with class home-toggle__btn remain. On load (carousel default) its aria-label is 'Grille'; after one click grid becomes visible and aria-label is 'Carrousel'; after a second click carousel is visible again and aria-label is back to 'Grille'.
    - HOME-02: after switching to grid, no element with class home-grid__intro exists; the first child of .home-grid__tiles carries class home-grid__tile--hero, contains the text 'Atelier', and its tagName is 'DIV' (it is not an anchor and does not navigate).
    - HOME-03: in carousel mode, the computed style of .home-hero__wordmark reports background-clip (or -webkit-background-clip) containing 'text' and a backgroundImage containing 'url('.
    - Mobile regression (375px): the wordmark inside the accent panel is visible (not buried under the hero photo), and switching to grid via the single toggle works.
  </behavior>
  <action>
Add three new `test.describe` blocks and revise one existing block. Do NOT weaken or delete the existing auto-advance, i18n, or only-migrated-galleries tests — those structural contracts stay.

1. New describe "single unified mode toggle (HOME-01, D-01/D-02)": navigate to '/'. Assert `page.locator('[data-role="mode-toggle"]')` has count 1, and `page.locator('.home-toggle__btn')` has count 0. Capture the toggle locator; assert its aria-label equals 'Grille' on load. Click it; assert `[data-role="home-carousel"]` is hidden, `[data-role="home-grid"]` is visible, and the toggle aria-label now equals 'Carrousel'. Click again; assert the carousel is visible and the aria-label is 'Grille' again.

2. New describe "grid hero-as-first-tile (HOME-02, D-04/D-06)": navigate to '/'; click `getByRole('button', { name: 'Grille' })` to enter grid mode. Assert `page.locator('.home-grid__intro')` has count 0 (the band is gone). Locate `page.locator('.home-grid__tiles > :first-child')`; assert it has class matching /home-grid__tile--hero/, contains text /Atelier/, and evaluate its `tagName` to assert it strictly equals 'DIV' (proves the non-link container of D-06). Additionally assert the first tile has no `href` attribute.

3. New describe "carousel wordmark cutout (HOME-03, D-08)": navigate to '/'. Evaluate the computed style of `.home-hero__wordmark` inside the page, returning `{ clip: getComputedStyle(el).webkitBackgroundClip || getComputedStyle(el).backgroundClip, bg: getComputedStyle(el).backgroundImage }`. Assert `clip` contains 'text' and `bg` contains 'url('. (Whether the photo is legible through the letters is confirmed live in Task 3 per D-08 — do not attempt a pixel assertion here.)

4. Revise the existing "mobile hero visibility (D-08)" describe (~lines 112-139): the block currently interacts with the removed accent-panel CTA via the accessible-name regex for "Discover other galleries". Replace that CTA interaction — because D-10 deletes that button — with: assert `page.locator('.home-hero__wordmark')` is visible at the 375px viewport (this preserves the block's original regression intent: the accent-panel content must not be buried under the opaque hero photo), then click the single toggle `page.getByRole('button', { name: 'Grille' })` and assert `[data-role="home-grid"]` is visible. Keep the existing hero/box-height and hero-image assertions above it unchanged.

Leave the existing toggle assertions at ~lines 36/57/68 as-is: `getByRole('button', { name: 'Grille' })` / `{ name: 'Carrousel' }` continue to resolve against the single button's *current* aria-label (per D-02, the label strings do not change — only the number of buttons does). Add a short code comment at the top of the new HOME-01 describe noting this is the intentional D-02 single-button contract superseding 04.3's two-button locked-name model, so a future reviewer does not "restore" a two-button count assertion.
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/homepage.spec.ts</automated>
    Expected at this task: the spec parses and type-checks, and the three NEW describe blocks (mode-toggle count=1 / grid hero-tile / cutout computed style) FAIL (RED) against the current unmodified component — this failing run is the pass condition for Task 1 (it proves the tests exercise not-yet-built behavior). The existing auto-advance / i18n / only-migrated tests should still pass.
  </automated></verify>
  <acceptance_criteria>
    - `grep -c 'data-role="mode-toggle"' tests/e2e/homepage.spec.ts` returns at least 1
    - `grep -c 'home-grid__tile--hero' tests/e2e/homepage.spec.ts` returns at least 1
    - The spec references `backgroundClip` or `webkitBackgroundClip` in the new cutout assertion
    - The revised mobile describe now clicks the single toggle by name 'Grille' (it no longer depends on the removed accent-panel CTA)
    - Running the spec shows the three new describe blocks RED and the file compiles with no TypeScript/Playwright errors
  </acceptance_criteria>
  <done>Spec compiles; the single-toggle, grid-hero-tile, and cutout contracts are present and RED against current code; the mobile test no longer references the CTA slated for removal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement single toggle + grid hero-tile + wordmark cutout + CTA removal (GREEN)</name>
  <files>src/components/HomeCarousel.astro</files>
  <read_first>
    - src/components/HomeCarousel.astro (read in full — markup, the &lt;style&gt; block, and the client &lt;script&gt;)
    - .planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-CONTEXT.md (all decisions D-01–D-10)
    - .planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-UI-SPEC.md (Typography table + all four "Layout & Interaction Notes" subsections)
    - .planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-PATTERNS.md (HomeCarousel.astro section — exact line ranges and reusable excerpts)
    - .planning/debug/resolved/logo-hover-and-font.md (mode-aware chip-color + Anton font logic in this same component — do not conflict with it)
  </read_first>
  <behavior>
    - Toggling the single button flips display mode both ways and updates its own aria-label to the target mode's localized label each time.
    - Entering grid mode shows a hero tile (wordmark + intro) as the first square tile, solid accent background, non-navigating.
    - In carousel mode the wordmark reveals the currently-displayed gallery's hero photo through its letterforms and updates on every prev/next/auto-advance tick; the intro paragraph keeps a legible accent-colored backing.
    - No "Discover other galleries" CTA and no two-button toggle remain anywhere in the component.
    - All homepage.spec.ts tests (including Task 1's new ones) pass.
  </behavior>
  <action>
Work through four sub-changes in order. Reuse the existing inline-SVG icons, the `[data-display-mode='X'] .selector` CSS mechanic, and the `render()` custom-property pattern rather than inventing new approaches — 06-PATTERNS.md quotes each excerpt to reuse.

**A. Single unified toggle (D-01/D-02/D-03, HOME-01).**
Replace the two toggle buttons (markup ~lines 88-103) with ONE `<button type="button" data-role="mode-toggle" data-action="toggle-mode">` that carries the initial `aria-label={toggleGridLabel}` (carousel is the default mode, so the control's job on load is "switch to grid"). Give the button two data attributes `data-label-carousel={toggleCarouselLabel}` and `data-label-grid={toggleGridLabel}` — the plain module script cannot read frontmatter consts, so it must read these two strings off the DOM. Place BOTH existing inline SVGs verbatim inside the button: the carousel glyph (rect + play `path`) wrapped with class `home-toggle__icon home-toggle__icon--carousel`, and the grid glyph (four rects) wrapped with class `home-toggle__icon home-toggle__icon--grid`. Remove `role="group"` and its localized aria-label from the wrapping `.home-toggle` div (a single control needs no group semantics) — keep the div as a plain layout wrapper or drop it and place the button directly in `.home-header`.
Add CSS driving icon visibility off display mode, using the same mechanic already in the file (e.g. the logo swap at ~lines 426-432): while in carousel mode show `.home-toggle__icon--grid` and hide `.home-toggle__icon--carousel`; while in grid mode show `.home-toggle__icon--carousel` and hide `.home-toggle__icon--grid` — the visible icon depicts the mode you would switch to, mirroring the aria-label.
Script (~lines 275-301): delete the `setActiveToggle()` function entirely (no is-active bookkeeping remains for a single control), delete the `gridToggleBtn`/`carouselToggleBtn` captures, delete both `querySelectorAll('[data-action="show-carousel"]')` / `('[data-action="show-grid"]')` loops, and delete the accompanying explanatory comment block about the accent-panel CTA. Replace with ONE click listener on `root.querySelector('[data-role="mode-toggle"]')`: read `root.dataset.displayMode`; if it is 'carousel' call `showGrid()` then set the button's `aria-label` to its `data-label-carousel` value; otherwise call `showCarousel()` then set `aria-label` to its `data-label-grid` value. Leave `showCarousel()` / `showGrid()` (~lines 247-259) and the `data-display-mode` state model untouched. Also remove the now-unused `.home-toggle__btn.is-active` CSS rule (~line 511).

**B. Grid hero-as-first-tile (D-04/D-05/D-06, HOME-02).**
Delete the `.home-grid__intro` band wrapper `<div>` (markup ~lines 131-135) but keep its two inner paragraphs by relocating them. Insert, as the FIRST child of `.home-grid__tiles` (before the `galleries.map(...)`), a NON-link `<div class="home-grid__tile home-grid__tile--hero">` (D-06: a `<div>`, never an `<a>`, no `href`) containing `<p class="home-grid__wordmark">Atelier<br />Jacqueline<br />Suzanne</p>` and `<p class="home-grid__intro-body">{introBody}</p>`.
CSS: delete the `.home-grid__intro` rule block (~lines 657-661). Add a `.home-grid__tile--hero` rule that inherits `.home-grid__tile` sizing (`position: relative; aspect-ratio: 1 / 1; overflow: hidden;`) and adds `background-color: var(--current-accent, var(--color-accent)); color: var(--color-on-accent); padding: var(--space-md); display: flex; flex-direction: column; justify-content: center;` — solid accent panel, no photo, no cutout (D-05). Resize the existing `.home-grid__wordmark` rule (~lines 663-672) to `font-size: 24px` desktop and add a 16px override in the `@media (max-width: 767px)` block; resize `.home-grid__intro-body` (~lines 674-680) to `font-size: 14px` (Label role). If the intro copy overflows the square at mobile width, add `display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;` to `.home-grid__intro-body` rather than shrinking below 12px.

**C. Carousel wordmark cutout (D-07/D-08/D-09, HOME-03).**
Markup (accent panel ~lines 124-128): remove the inline `backgroundColor: 'var(--color-accent)'` from `.home-hero__accent`'s `style` (keep `color: 'var(--color-on-accent)'`) so no solid panel sits behind the wordmark. Set an initial `--wordmark-photo` custom property at server render from `firstGallery.heroSrc` (e.g. as an inline style on the wordmark `<p>` or on the `.home` section) so the transparent text reveals a photo immediately, before and without JS. Give `.home-hero__intro` its own backing so it stays legible with the panel background gone: wrap it (or style it directly) with `padding` and `background-color: var(--current-accent, var(--color-accent))` — per D-08 only the wordmark shows the photo through; the intro keeps a solid backing.
CSS (`.home-hero__wordmark` ~lines 620-633): bump `font-size` to `64px` desktop and add a `48px` override in the `@media (max-width: 767px)` block; keep `font-family: var(--font-display)` and `letter-spacing: 0.02em`. Inside an `@supports (background-clip: text) or (-webkit-background-clip: text)` block, add `background-image: var(--wordmark-photo); background-size: cover; background-position: center; -webkit-background-clip: text; background-clip: text; color: transparent; -webkit-text-fill-color: transparent;`. Outside `@supports`, keep a solid fallback `color: var(--color-on-accent)` so the wordmark is never invisible on a non-supporting engine. Use the simple full-bleed `cover`/`center` approximation — do not spend time computing an exact offset to align the clip with the photo behind the panel (D-08 explicitly permits this).
Script `render()` (~lines 210-230): remove the `accentPanel.style.backgroundColor = accent.bg;` assignment (~line 226) so the panel no longer paints a solid background over the cutout; KEEP `accentPanel.style.color = accent.text;`; KEEP `root!.style.setProperty('--current-accent', accent.bg);`; and ADD, right beside it, `root!.style.setProperty('--wordmark-photo', \`url(${gallery.heroSrc})\`);` so the cutout photo tracks the visible hero on every gallery change/auto-advance tick.

**D. CTA removal ripple (D-10).**
Delete the accent-panel CTA `<button>` markup line (~line 127). Delete the `ctaLabel` frontmatter const (~line 59). Delete the `.home-hero__cta` CSS rule block (~lines 642-653). After the change, confirm no reference to the CTA class or its label variable remains anywhere in the file (markup, style, or script comments). Optionally trim `.home-hero__intro`'s bottom margin slightly now that nothing follows it and the wordmark is larger (minor balance pass, Claude's discretion).
<!-- planner-discipline-allow: ctaLabel -->
<!-- planner-discipline-allow: home-hero__cta -->
  </action>
  <verify>
    <automated>npm run build && npx playwright test tests/e2e/homepage.spec.ts</automated>
    All homepage.spec.ts tests GREEN. Then confirm the structural gates below.
    Also run: `grep -c 'ctaLabel' src/components/HomeCarousel.astro` returns 0; `grep -c 'home-hero__cta' src/components/HomeCarousel.astro` returns 0; `grep -c 'data-role="mode-toggle"' src/components/HomeCarousel.astro` returns 1; `grep -c 'home-toggle__btn' src/components/HomeCarousel.astro` returns 0; `grep -c "setProperty('--wordmark-photo'" src/components/HomeCarousel.astro` returns at least 1.
  </verify>
  <acceptance_criteria>
    - Exactly one `data-role="mode-toggle"` button; zero `home-toggle__btn`; zero `ctaLabel`; zero `home-hero__cta` occurrences in the component
    - `.home-grid__tiles` first child is `<div class="home-grid__tile home-grid__tile--hero">` (a DIV, no href); no `.home-grid__intro` band element remains
    - `.home-hero__wordmark` computed style has background-clip:text (or -webkit-) with a `url(...)` background-image and a transparent fill; `render()` sets `--wordmark-photo` immediately after `--current-accent`
    - The single toggle's aria-label is 'Grille' on load and reads from `data-label-carousel`/`data-label-grid` when flipping
    - `npm run build && npx playwright test tests/e2e/homepage.spec.ts` is fully GREEN
  </acceptance_criteria>
  <done>Single toggle, grid hero-tile, and carousel wordmark cutout are implemented; the CTA and two-button toggle are gone; the full homepage e2e suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live visual verification of the wordmark cutout + grid hero-tile</name>
  <files>src/components/HomeCarousel.astro (no edits — visual review of the built output)</files>
  <read_first>
    - .planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-UI-SPEC.md ("Carousel wordmark cutout" note — the D-08 live-verification mandate: "letters visibly reveal photo detail... for at least two different galleries")
  </read_first>
  <action>Pause for a blocking human-verify checkpoint (D-08 mandates a live/screenshot check for the cutout because legibility depends on each photo's content — it cannot be asserted from computed CSS). Serve the built site with `npm run build && npm run preview`, then walk the reviewer through the how-to-verify steps below. Do not auto-approve; wait for the resume signal. If the reviewer reports the wordmark is illegible over a given photo or "Jacqueline" wraps, adjust the font size or add a subtle backing per D-09 (Claude's discretion within "larger than 44px") and re-verify.</action>
  <what-built>
    A single unified carousel/grid toggle button, a grid view whose first tile is the hero (solid accent square, non-link), and a carousel wordmark rendered as a transparent photo-cutout. Automated tests confirm the DOM/CSS structure; this checkpoint confirms the one thing computed CSS cannot — that the photo is actually *legible* through the letterforms (D-08 explicitly requires a live/screenshot check because the effect depends on each photo's content).
  </what-built>
  <how-to-verify>
    Run `npm run build && npm run preview`, then open http://localhost:4321/ :
    1. Carousel mode (default): confirm the "Atelier / Jacqueline / Suzanne" wordmark shows the hero photo through the letters (transparent cutout, D-08) and stays legible. Click the next arrow (or let it auto-advance) to a different gallery and confirm the photo revealed inside the letters changes to match the new hero photo (the --wordmark-photo sync). Check at a normal desktop width AND at a 375px mobile width (devtools) for at least TWO different galleries' photos — a photo that is uniformly dark/light near the panel could read as illegible even with correct CSS.
    2. Toggle: confirm there is ONE button in the header (not two); its icon depicts the mode you would switch to; click it to enter grid mode (icon + label flip), then click again to return to carousel (HOME-01).
    3. Grid mode: confirm the FIRST tile is a solid accent-colored square holding the wordmark + intro (no photo, no cutout — D-05), the same size as the gallery tiles, and clicking it does NOT navigate anywhere (D-06). Confirm the old full-width intro band above the grid is gone (HOME-02) and the remaining tiles still open their galleries.
    4. Confirm the "Discover other galleries" CTA is gone from the carousel accent panel (D-10).
  </how-to-verify>
  <acceptance_criteria>
    - The wordmark visibly reveals photo detail through its letters at both desktop and 375px mobile widths, for at least two different galleries, and remains legible (per D-08).
    - "Jacqueline" (the longest line) does not wrap onto a second line inside the accent panel at the ~768px breakpoint; if it does, the desktop font size is stepped down (e.g. 56px) rather than allowed to overflow (D-09, Claude's discretion within "larger than 44px").
    - The grid hero-tile is a solid accent square that does not navigate; no full-width intro band remains; the single toggle flips both ways; the CTA is gone.
  </acceptance_criteria>
  <resume-signal>Type "approved", or describe issues (e.g., "wordmark illegible over the Brume photo" → adjust font size or add a subtle backing per D-09/Claude's discretion, then re-verify).</resume-signal>
  <verify><human-check>The four how-to-verify steps are confirmed by the reviewer against the built preview (cutout legibility at desktop + 375px for ≥2 galleries, single toggle flips both ways, non-navigating grid hero-tile, CTA gone).</human-check></verify>
  <done>Reviewer signals "approved" after confirming the cutout reveals photo detail legibly, the grid hero-tile is a non-link solid square, the single toggle works, and the CTA is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time data → client `<script>` | Gallery data (heroSrc/gridSrc/title/slug) is pre-fetched and pre-filtered at build time by the calling page's frontmatter (getGalleries); this component and its client script never import `src/lib/sanity.ts`, so `SANITY_API_READ_TOKEN` never reaches the browser (existing mitigation, preserved). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `HomeCarousel.astro` client script — new `--wordmark-photo` set from `gallery.heroSrc` | low | accept | `heroSrc` is an already-public build-time image URL, the same value already assigned to the visible `heroImg.src`; the cutout exposes no data that isn't already rendered. No new server/token access is introduced — the component still never imports `src/lib/sanity.ts`. |
| T-06-02 | Tampering | client-only display-mode state + e2e locators | low | accept | Toggle state is cosmetic and entirely client-side; no server state, no persisted data, no user input crosses a trust boundary. There is nothing to tamper with beyond the visitor's own view. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No new package installs in this phase (confirmed via 06-PATTERNS.md "Imports pattern (unchanged) — No new imports needed"). The Package Legitimacy Gate is not applicable; no `[ASSUMED]`/`[SUS]` packages to verify. |

This is a cosmetic/interaction-only phase with no new data flows, auth, or user input; the register reflects that (all dispositions `accept`, all severities `low`), consistent with the security context note for Phase 6.
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|-----|---------------------|------|--------|-------|
| GOAL | — | Single toggle + grid hero-tile + wordmark cutout read as one coherent surface | 06-01 | COVERED | Tasks 1-3 |
| REQ | HOME-01 | Single unified carousel/grid toggle button | 06-01 | COVERED | Task 2A + Task 1 contract |
| REQ | HOME-02 | Grid hero as first tile of the grid (not a band) | 06-01 | COVERED | Task 2B + Task 1 contract |
| REQ | HOME-03 | Wordmark transparent cutout in carousel mode | 06-01 | COVERED | Task 2C + Task 3 live-verify |
| RESEARCH | — | (no RESEARCH.md — research explicitly skipped for this phase) | — | N/A | Well-understood CSS techniques; user accepted no Nyquist Dim-8 coverage |
| CONTEXT | D-01 | One button, flipping icon/label | 06-01 | COVERED | Task 2A |
| CONTEXT | D-02 | e2e queries the single stateful button | 06-01 | COVERED | Task 1 |
| CONTEXT | D-03 | Restructure script around one button | 06-01 | COVERED | Task 2A |
| CONTEXT | D-04 | Hero becomes first grid tile, band removed | 06-01 | COVERED | Task 2B |
| CONTEXT | D-05 | Grid hero-tile stays solid accent (no cutout) | 06-01 | COVERED | Task 2B |
| CONTEXT | D-06 | Hero tile is a non-link `<div>` | 06-01 | COVERED | Task 2B + Task 1 |
| CONTEXT | D-07 | Cutout scoped to carousel mode only | 06-01 | COVERED | Task 2C |
| CONTEXT | D-08 | background-clip:text technique + live verify | 06-01 | COVERED | Task 2C + Task 3 |
| CONTEXT | D-09 | Enlarge wordmark font (> 44px) | 06-01 | COVERED | Task 2C |
| CONTEXT | D-10 | Remove the CTA button + ctaLabel | 06-01 | COVERED | Task 2D |

All source items COVERED. No MISSING rows. Deferred Ideas: none in CONTEXT.md.
</source_audit>

<verification>
Phase-level checks (all run after Task 2, re-confirmed in Task 3):

- `npm run build && npx playwright test tests/e2e/homepage.spec.ts` — full GREEN, including the new single-toggle / grid-hero-tile / cutout contracts.
- Component structural gates: one `data-role="mode-toggle"`; zero `home-toggle__btn`; zero `ctaLabel` / `home-hero__cta`; `--wordmark-photo` set in `render()`.
- Live (Task 3): the cutout reveals photo detail legibly at desktop + 375px for ≥2 galleries; grid hero-tile is a non-navigating solid square; the single toggle flips both ways; the CTA is gone.
</verification>

<success_criteria>
- HOME-01: the homepage shows one toggle button (not two) that visibly reflects and switches the active display mode.
- HOME-02: in grid view the hero (wordmark + intro, no CTA) is the first tile inside the grid, not a separate band.
- HOME-03: in carousel mode the wordmark reveals the hero photo through its letterforms and stays legible at desktop and mobile widths.
- The homepage e2e suite passes and no regression is introduced to auto-advance, i18n, or the clickable gallery title.
</success_criteria>

<output>
Create `.planning/phases/06-homepage-view-mode-toggle-grid-hero-wordmark-cutout/06-01-SUMMARY.md` when done.
</output>
