---
phase: 18-gallery-ditions-display-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/components/DetailHero.astro
  - sanity/schemas/gallery.ts
  - sanity/schemas/edition.ts
  - tests/e2e/gallery.spec.ts
  - tests/unit/statement-length-limit.test.ts
autonomous: true
requirements:
  - PORT-04

must_haves:
  truths:
    - "On a gallery detail page, `.detail-hero__statement` reports computed `-webkit-line-clamp` of `none` — the 4-line cap is gone with no substitute (PORT-04, D-01)."
    - "On a gallery detail page, `.detail-hero__statement` has `scrollHeight <= clientHeight` (no self-clipping) for EVERY published gallery, at 1280x900 and at 390x844."
    - "`.detail-hero__reveal`'s bounding box stays fully inside `.detail-hero__pin`'s bounding box for every published gallery at 1280x900 and 390x844 — the full statement is visible, not clipped by the pin's `overflow: hidden`."
    - "The longest published statement renders in full: its rendered text content length equals the source length from Sanity, with no trailing ellipsis character."
    - "Both `sanity/schemas/gallery.ts` and `sanity/schemas/edition.ts` cap `statement.fr` and `statement.en` at the SAME character limit N — four capped subfields total, one N value across both files (D-02, D-03)."
    - "N is at least as large as the longest statement currently published, so adding the rule invalidates no existing content."
    - "N was chosen from recorded measurements against the real rendered DetailHero, not guessed — the measured overflow thresholds per viewport are written into the SUMMARY."
  artifacts:
    - "src/components/DetailHero.astro — the five truncation declarations deleted from the `.detail-hero__statement` rule"
    - "sanity/schemas/gallery.ts — max-length validation added to both subfields of the local localeTextField helper"
    - "sanity/schemas/edition.ts — the identical max-length validation, same N"
    - "tests/unit/statement-length-limit.test.ts — new unit test proving both schema files carry the same N on exactly two subfields each"
    - "tests/e2e/gallery.spec.ts — new PORT-04 describe block asserting no clamp and no clipping across every published gallery at two viewports"
  key_links:
    - "`.detail-hero__pin`'s `overflow: hidden` (line 427, inherited by the mobile override at 701-705) — the clipping surface that makes the un-clamped statement a real overflow risk rather than a cosmetic one. This is what the Sanity cap exists to bound."
    - "The two SEPARATE local `localeTextField` helpers (gallery.ts line 22, edition.ts line 10) — no shared module exists. Editing one and not the other silently leaves the other content type uncapped; the unit test is the only automated guard against that."
    - "`sanity/schemas/siteSettings.ts`'s THIRD copy of the same helper (line 39, two call sites) — explicitly OUT of scope. Capping it would retroactively invalidate unrelated legacy fields."
    - "`gallery.seo?.description?.[locale] ?? statement` (galleries/[slug].astro line 60) — the statement doubles as the SEO meta description fallback, so N also bounds meta description length. Shortening only; no new exposure."
    - "Mobile `.detail-hero__reveal` is bottom-anchored (`bottom: var(--space-xl)`, `top: auto`) while desktop is vertically centred (`top: 50%` + `translateY(-50%)`) — the two layouts overflow in different directions, so calibration must measure BOTH edges of the pin, not just the top."
---

<objective>
Fix **PORT-04**: gallery and edition detail heroes truncate their description after 4 lines, hiding
published content mid-sentence.

- Remove the CSS line clamp from `.detail-hero__statement` entirely, with no substitute cap (D-01,
  following Phase 17 HOME-12's precedent).
- Because `DetailHero` is a fixed-height `overflow: hidden` panel — unlike HOME-12's freely-growing
  grid tile — bound the content at the SOURCE instead: add a max-length validation to the `statement`
  field in Sanity Studio (D-02), applied identically to the duplicated local helper in both
  `gallery.ts` and `edition.ts` (D-03).

Purpose: the visitor sees the whole description. The Studio cap replaces the CSS clamp as the
overflow defence, moving the constraint from display time (where it silently hides shipped words) to
authoring time (where Romane sees the limit while writing).
Output: one CSS deletion, two schema files carrying the same empirically-calibrated limit, plus unit
and e2e coverage locking both halves.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-gallery-ditions-display-fixes/18-CONTEXT.md
@.planning/phases/18-gallery-ditions-display-fixes/18-PATTERNS.md
@.planning/phases/18-gallery-ditions-display-fixes/18-01-SUMMARY.md
</context>

## Live content measurements taken during planning (2026-08-02)

`N` is Claude's Discretion per CONTEXT.md, and CONTEXT.md requires it be determined empirically
rather than guessed. Planning already gathered the content half of that evidence by querying the live
Sanity dataset (`gwz8iug4` / `production`, public read, no token needed):

```
curl -s "https://gwz8iug4.api.sanity.io/v2024-01-01/data/query/production?query=*%5B_type%20in%20%5B%22gallery%22%2C%22edition%22%5D%5D%7B_type%2C%22slug%22%3Aslug.current%2C%22frLen%22%3Alength(statement.fr)%2C%22enLen%22%3Alength(statement.en)%7D"
```

| type | slug | fr length | en length |
|---|---|---|---|
| gallery | the-victorian-tea-room | **415** | 352 |
| gallery | adult | 217 | 190 |
| gallery | silos | 209 | 194 |
| gallery | brume | 159 | 141 |
| gallery | paysage | 59 | 62 |
| edition | entasse | **453** | 401 |
| edition | rebut | 294 | 300 |
| edition | silos | 295 | 288 |

**Longest published statement: 453 characters** (edition `entasse`, French).

This directly contradicts CONTEXT.md's rough starting estimate of "250-320 characters" — that range
would retroactively invalidate 4 of the 8 documents, so the next time Romane edited "Entassé" or
"The Victorian Tea Room" in Studio she would be blocked from publishing until she cut text she never
asked to cut. CONTEXT.md flagged that estimate as a starting point to be verified, not a decision, so
correcting it upward is within the stated discretion — but it MUST be corrected, not applied.

Re-run the query in Task 2; content may have changed since planning.

## Artifacts this plan produces

**Removed (CSS, `src/components/DetailHero.astro`, the `.detail-hero__statement` rule, lines 571-581):**
- The `display` declaration whose value is the webkit box display type
- The vendor-prefixed line-cap declaration and its unprefixed twin (both currently `4`)
- The vendor-prefixed box-orient declaration
- The `overflow` declaration

Retained in that rule: `margin-top`, `font-size`, `line-height`, `color`.

**Added (`sanity/schemas/gallery.ts`, inside the local `localeTextField` helper, lines 30-45):**
- A max-length constraint of N characters on the `fr` subfield's validation chain
- A max-length constraint of N characters on the `en` subfield's validation chain
- Updated French error messages naming both the required condition and the N-character limit

**Added (`sanity/schemas/edition.ts`, inside its own local `localeTextField` helper, lines 18-33):**
- The identical pair of constraints and messages, with the identical N

**Added (new file, `tests/unit/statement-length-limit.test.ts`):**
- A Vitest suite reading both schema files as source text and asserting: each declares exactly 2
  max-length constraints; every N across both files is identical; N is within a sane band; and
  `siteSettings.ts` declares none.

**Added (`tests/e2e/gallery.spec.ts`):**
- One new describe block for PORT-04 containing 2 tests (desktop and mobile), each looping every
  published gallery.

**Explicitly NOT changed (guard list):**
- `sanity/schemas/siteSettings.ts` — the THIRD copy of `localeTextField` (line 39), used by
  `welcomeBody` and `homepageIntro` (lines 186-187). Out of scope per CONTEXT.md, which names only
  `gallery.ts` and `edition.ts`. Capping it would invalidate unrelated legacy content.
- The `rows: 5` option, the `columns: 2` layout option, the `description` string and the
  `defineField`/`defineType` structure of both edited helpers.
- The `required` constraint on both subfields — the new constraint is ADDED to the existing chain,
  never replacing it.
- Every call site: `localeTextField('statement', 'Texte de présentation', 'content')` at gallery.ts
  line 113 and edition.ts line 95 stays byte-identical. These are each file's ONLY call site
  (verified during planning), which is what makes editing the helper safely scoped to `statement`.
- `.detail-hero__pin`'s `overflow: hidden` (line 427) and both height rules — `height: 100svh`
  (line 426) and the mobile `height: 70svh; min-height: 420px` (lines 701-705). The panel geometry is
  the constraint being measured, NOT a thing to loosen. Do not "fix" overflow by changing the panel.
- `.detail-hero__reveal`'s `max-width: 420px` (line 548) and its mobile bottom-anchored override
  (lines 720-727).
- `src/components/GalleryGrid.astro` and both gallery route files — Plan 01 owns those.
- No package-manager installs: `package.json`, `package-lock.json`, `sanity/package.json` and
  `sanity/package-lock.json` are not touched.

## Source Coverage Audit (PORT-04 rows)

The phase-wide audit lives in `18-01-PLAN.md`. Rows owned by this plan:

| Source | Item | Covered by |
|--------|------|------------|
| GOAL success criterion 1 | full description text, not cut off mid-sentence | Task 1 |
| REQ | PORT-04 | Tasks 1-3 |
| CONTEXT | D-01 remove the clamp entirely, no substitute cap | Task 1 |
| CONTEXT | D-02 bound length via Sanity validation instead of CSS, because the panel is fixed-height | Task 2 (calibration) + Task 3 (rule) |
| CONTEXT | D-03 edit BOTH duplicated local helpers, same N, both fr and en subfields | Task 3 + its unit test |
| CONTEXT | Discretion — N determined empirically at the tightest mobile constraint, not guessed | Task 2 |
| CONTEXT | Discretion — French error message wording matching the existing style | Task 3 |
| CONTEXT | siteSettings.ts third helper copy out of scope | Guard list + unit test asserts 0 constraints there |

No unplanned items.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the 4-line clamp from the detail hero statement and prove no clipping across every published gallery (PORT-04, D-01)</name>

  <files>
    tests/e2e/gallery.spec.ts
    src/components/DetailHero.astro
  </files>

  <read_first>
    - `src/components/DetailHero.astro` lines 416-435 — `.detail-hero` (`height: calc(100svh + 900px)`) and `.detail-hero__pin` (`position: sticky`, `height: 100svh`, `overflow: hidden`). The `overflow: hidden` is the clipping surface everything in this plan is about.
    - `src/components/DetailHero.astro` lines 543-582 — `.detail-hero__reveal` (`top: 50%`, `translateY(-50%)`, `max-width: 420px`, `opacity: 0`), `.detail-hero__caption`, `.detail-hero__reveal-title`, and the `.detail-hero__statement` rule that is the edit target.
    - `src/components/DetailHero.astro` lines 696-746 — the `@media (max-width: 767px)` block: `.detail-hero__pin` becomes `position: relative; height: 70svh; min-height: 420px`, and `.detail-hero__reveal` becomes bottom-anchored (`top: auto; bottom: var(--space-xl); left/right: var(--space-xl); transform: none; opacity: 1`). Note `.detail-hero__caption` and `.detail-hero__format` are hidden here.
    - `src/components/DetailHero.astro` lines 654-680 — the `prefers-reduced-motion` + `min-width: 768px` block, which also pins at `100svh`. A third geometry variant to be aware of.
    - `src/components/DetailHero.astro` lines 127-132 — the reveal markup order: caption, `<h1>` title, statement, format.
    - `src/components/DetailHero.astro` lines 176-230 — the scroll-driven reveal script that animates `.detail-hero__reveal` from `opacity: 0`, so you know why a desktop measurement may need to force opacity or scroll first.
    - `.planning/phases/17-homepage-carousel-intro-fixes/17-01-PLAN.md` Task 2 — the HOME-12 precedent: the same deletion shape and the same "computed clamp is `none` plus no clipping by the parent" test shape. Reuse its structure.
    - `tests/e2e/gallery.spec.ts` lines 1-30 — the homepage-grid discovery convention.
  </read_first>

  <behavior>
    - Desktop test (1280x900), looping every published gallery: `.detail-hero__statement` reports computed `-webkit-line-clamp` of `none`; its `scrollHeight` is less than or equal to its `clientHeight`; and `.detail-hero__reveal`'s bounding rect sits fully within `.detail-hero__pin`'s bounding rect on both the top and bottom edges (a 1px tolerance for sub-pixel rounding).
    - Mobile test (390x844), looping every published gallery: the same three assertions. Mobile is the tighter constraint because the pin shrinks to `70svh` while the reveal is bottom-anchored.
    - Both tests: the rendered `textContent` of `.detail-hero__statement` contains no ellipsis character, proving nothing was visually truncated.
  </behavior>

  <action>
    Step 1 — write the failing tests FIRST, in `tests/e2e/gallery.spec.ts`.

    Append a new describe block at the END of the file named for PORT-04 and the un-clamped hero
    statement. Do NOT use `test.use({ viewport })` here — each test sets its own viewport with
    `page.setViewportSize()` so both viewports live in one block.

    Both tests share a discovery helper: `page.goto('/')`, click the button named `Grille`, then
    collect ALL `a.home-grid__tile` hrefs via `evaluateAll`. Assert the collected array length is
    greater than 0. Looping every gallery rather than only the first is deliberate: the clamp only
    visibly bites on the LONGEST statement, and hardcoding the currently-longest slug would silently
    stop testing anything the moment Romane renames or reorders content.

    For each href: navigate, wait for `.detail-hero__statement` to exist (skip a gallery that has no
    statement element rather than failing — the element is conditionally rendered), then read in one
    `evaluate` call: the statement's computed `webkitLineClamp`, its `scrollHeight` and `clientHeight`,
    its `textContent`, and the top/bottom of both the reveal's and the pin's `getBoundingClientRect()`.
    Assert clamp is `none`; assert `scrollHeight <= clientHeight`; assert the reveal's top is greater
    than or equal to the pin's top minus 1 and the reveal's bottom is less than or equal to the pin's
    bottom plus 1; assert the text contains no ellipsis character. Include the gallery href in each
    assertion's message so a failure names the offending gallery.

    Desktop test: `page.setViewportSize({ width: 1280, height: 900 })` before the loop. On desktop the
    reveal starts at `opacity: 0` and is animated in by the scroll script — opacity does not affect
    `getBoundingClientRect()` or `scrollHeight`, so no special handling is needed, but if the reveal
    measures as a zero-size rect, scroll down a few hundred pixels first and re-measure rather than
    forcing styles.

    Mobile test: `page.setViewportSize({ width: 390, height: 844 })` before the loop. This matches the
    viewport already used by the mobile-responsive specs in this repo, so it is the project's real
    tested mobile constraint.

    Run both tests and confirm at least one gallery FAILS on the current code, reporting a computed
    clamp of `4` (the RED step). Record which galleries failed and the observed values.

    Step 2 — make them pass. In `src/components/DetailHero.astro`, inside the
    `.detail-hero__statement` rule (lines 571-581), delete the five truncation declarations: the
    `display` declaration set to the webkit box value, the vendor-prefixed line-cap declaration, its
    unprefixed twin, the vendor-prefixed box-orient declaration, and the `overflow` declaration.

    Keep `margin-top`, `font-size`, `line-height` and `color`. After the edit the rule has exactly
    four declarations, in that original order.

    Add no substitute: no max-height, no fade mask, no different clamp value, no ellipsis. D-01 is
    explicit that the removal has no CSS-level fallback — the Sanity cap in Task 3 is the replacement
    defence, and adding a CSS cap too would silently re-hide content and defeat the requirement.

    Do NOT touch `.detail-hero__pin`'s `overflow: hidden` on line 427. That property serves the
    photo/scrim layers, not the statement, and removing it would let the sticky hero bleed over the
    rest of the page.

    Step 3 — rerun both tests (GREEN). If a gallery still fails the pin-containment assertion after
    the clamp removal, STOP and do not loosen the test or the panel geometry: that is exactly the
    overflow condition Tasks 2 and 3 exist to bound. Record the failing gallery and its statement
    length, carry it into Task 2's calibration as a real measured data point, and re-run this test at
    the end of Task 3.

    Step 4 — run the whole `gallery.spec.ts` file and then `edition.spec.ts`, since `DetailHero` is
    shared by both content types and this CSS change affects edition detail pages identically.
  </action>

  <verify>
    <automated>npm run build && npm run test:e2e -- gallery.spec.ts --project=chromium</automated>
    <automated>npm run test:e2e -- edition.spec.ts --project=chromium</automated>
    <automated>awk '/^  \.detail-hero__statement \{/{f=1;next} f&&/^  \}/{exit} f{print}' src/components/DetailHero.astro | grep -oE '^ +[a-z-]+:' | tr -d ' :'</automated>
    <automated>npm run typecheck</automated>
  </verify>

  <acceptance_criteria>
    - The third verify command prints exactly four lines, in this order: `margin-top`, `font-size`, `line-height`, `color`. Any fifth line, or any reordering, fails the task.
    - `git diff --stat src/components/DetailHero.astro` shows exactly 5 deleted lines and 0 added lines.
    - The desktop test passes for every published gallery: computed `-webkit-line-clamp` is `none`, `scrollHeight <= clientHeight`, and the reveal rect is inside the pin rect within 1px on both edges.
    - The mobile test at 390x844 passes for every published gallery, with the same three assertions.
    - No gallery's rendered statement text contains an ellipsis character.
    - The RED run failed with a computed clamp value of `4` on at least one gallery; that output is quoted in the SUMMARY along with the list of galleries exercised.
    - `edition.spec.ts` still passes — the shared component change did not regress edition detail pages.
    - No CSS substitute cap was introduced anywhere: `max-height`, `mask-image` and `text-overflow` appear nowhere in the `.detail-hero__statement` rule.
    - `.detail-hero__pin` and `.detail-hero__reveal` rules are unchanged in the diff.
  </acceptance_criteria>

  <done>
    The detail hero statement renders in full with no CSS cap, and Playwright proves across every
    published gallery, at desktop and mobile viewports, that the text neither self-clips nor escapes
    the sticky panel's clipped bounds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Empirically calibrate the statement character limit N against the real rendered DetailHero</name>

  <files>
    (measurement only — produces no committed source change; the temporary probe file is deleted before commit)
  </files>

  <read_first>
    - This plan's "Live content measurements taken during planning" section above — the published length table and the 453-character floor.
    - `src/components/DetailHero.astro` lines 416-435, 543-582, 654-680 and 696-746 — the three geometry variants (desktop pinned, reduced-motion desktop, mobile bottom-anchored). Same regions as Task 1; do not re-read if already in context.
    - `src/layouts/BaseLayout.astro` lines 245-320 — the token values you will reason with: `--text-body-size: 16px`, `--text-display-size: 32px`, `--space-xl: 32px`, `--space-md: 16px`, `--space-2xl: 48px`.
    - `playwright.config.ts` — the `webServer` block runs `npm run preview` on port 4321 with `reuseExistingServer`, and the two configured projects are `chromium` (Desktop Chrome) and `webkit-mobile` (iPhone 15 Pro, smoke specs only).
    - `tests/e2e/gallery.spec.ts` — the discovery convention, so the probe navigates the same way the real tests do.
  </read_first>

  <action>
    This task produces a NUMBER plus recorded evidence, not committed source. Task 3 consumes the
    number. CONTEXT.md requires N be measured against the actual rendered component, so a guess — even
    a well-reasoned one — fails this task.

    Step 1 — re-measure the content floor. Re-run the GROQ query from the "Live content measurements"
    section above against the live dataset. Record the full result table. Set `N_floor` to the single
    largest value across every `frLen` and `enLen` returned. Planning measured 453; if content has
    changed since, the fresh number wins.

    Step 2 — build and serve the site. `npm run build`, then start `npm run preview` in the background
    on port 4321. Confirm it responds before probing.

    Step 3 — write a TEMPORARY probe spec at `tests/e2e/_calibrate-statement.spec.ts`. This file is a
    measurement instrument and MUST be deleted in Step 6 — it is not part of the deliverable.

    The probe:
    - Navigates to the gallery detail page of the gallery with the LONGEST statement (from Step 1's
      table; at planning time `the-victorian-tea-room`). Discover it by slug from the query result
      rather than hardcoding.
    - Builds filler strings from REAL French prose, not repeated single characters — word-wrap
      behaviour depends on word boundaries and `x`-repetition would measure the wrong thing. Fetch the
      longest published French statement via the same GROQ endpoint, then concatenate it with itself
      until longer than the largest candidate, and slice each candidate to its target length trimming
      back to the last space. Record each candidate's ACTUAL resulting length.
    - For each viewport in this list, and for each candidate length in `[300, 350, 400, 450, 500, 550,
      600, 700]`: set the viewport, replace `.detail-hero__statement`'s `textContent` with the
      candidate string via `page.evaluate`, force a reflow, then measure whether
      `.detail-hero__reveal`'s bounding rect escapes `.detail-hero__pin`'s bounding rect on EITHER the
      top or the bottom edge (desktop centres the block so it can escape both ways; mobile
      bottom-anchors it so it escapes upward). Also record whether the statement itself self-clips
      (`scrollHeight > clientHeight`).

      Viewports, in priority order:
      1. `390x844` — the project's real tested mobile viewport, used by the existing mobile-responsive
         specs. **This is the binding constraint.**
      2. `1280x900` — the project's tested desktop viewport, used by every detail-page describe block.
      3. `360x640` — a narrow small-phone floor, INFORMATIONAL ONLY. It is stricter than anything the
         suite tests and is recorded for context, not used to lower N.
    - Prints a table: viewport, candidate length, overflowed yes/no, measured reveal height, measured
      pin height.

    Run it with `npx playwright test tests/e2e/_calibrate-statement.spec.ts --project=chromium` and
    capture the full output verbatim.

    Step 4 — derive N with this rule, in order. Do not improvise around it.
    1. `N_safe_mobile` = the largest candidate that did NOT overflow at 390x844.
    2. `N_safe_desktop` = the largest candidate that did NOT overflow at 1280x900.
    3. `N_binding` = the smaller of those two.
    4. If `N_binding` is greater than or equal to `N_floor` rounded up: set `N` to the largest multiple
       of 50 that is less than or equal to `N_binding` and greater than or equal to `N_floor`. Based on
       planning's `N_floor` of 453 and the panel geometry, the expected landing zone is 500; treat any
       result far outside 450-700 as a sign the probe is measuring the wrong element and re-check it
       before accepting the number.
    5. If `N_binding` is LESS than `N_floor`: do NOT ship a cap below `N_floor`. Capping below already-
       published content would block Romane from re-publishing documents she never asked to shorten.
       Instead set `N` to the next multiple of 50 above `N_floor`, and record the shortfall explicitly
       in the SUMMARY as an open item naming which viewport failed, at what length, and by how many
       pixels — so the end-of-phase human check can decide whether to trim content or adjust the panel
       in a follow-up. Do not silently pick either horn.

    Step 5 — sanity-check the chosen N against the real geometry by reasoning, as a cross-check on the
    probe: at 390x844 the pin is `70svh` (roughly 591px), the reveal is inset 32px from the bottom and
    32px from each side (roughly 326px wide), the statement runs at 16px with `line-height: 1.5`
    (24px per line), and the title above it renders at 32px. If the probe's answer is wildly
    inconsistent with that budget, the probe is measuring the wrong element — fix it and re-run rather
    than accepting the number.

    Step 6 — DELETE `tests/e2e/_calibrate-statement.spec.ts`. It must not reach a commit; leaving it
    would add a slow, content-dependent, mutation-based spec to every CI run. Stop the preview server.

    Step 7 — write the full measurement table, the derived `N_safe_mobile`, `N_safe_desktop`,
    `N_binding`, `N_floor`, the chosen `N`, and which branch of the Step 4 rule was taken into the
    SUMMARY. Task 3 and every future reader depend on this record; an unexplained number in a schema
    file is exactly what CONTEXT.md's "determined empirically, not guessed" instruction forbids.
  </action>

  <verify>
    <automated>test ! -f tests/e2e/_calibrate-statement.spec.ts && echo "GATE PASS: temporary calibration probe deleted"</automated>
    <automated>git status --porcelain tests/e2e/ | grep -c '_calibrate' | grep -qx 0 && echo "GATE PASS: no calibration artifact staged or untracked"</automated>
    <automated>npm run test:e2e -- gallery.spec.ts --project=chromium</automated>
  </verify>

  <acceptance_criteria>
    - The SUMMARY contains the re-run GROQ result table with a per-document fr/en length for every gallery and edition, and states `N_floor` as the maximum across all of them.
    - The SUMMARY contains the probe's full measurement table: for each of 390x844, 1280x900 and 360x640, the overflow verdict at each of the 8 candidate lengths.
    - The SUMMARY states `N_safe_mobile`, `N_safe_desktop`, `N_binding`, the chosen `N`, and names which branch of the Step 4 decision rule was applied.
    - The chosen `N` is greater than or equal to `N_floor` — no currently-published statement is invalidated by the new rule.
    - `N` is a multiple of 50.
    - Filler text used real French prose derived from live content, not repeated single characters; the SUMMARY states this.
    - `tests/e2e/_calibrate-statement.spec.ts` does not exist in the working tree and appears in no commit.
    - If the Step 4 fallback branch was taken, the SUMMARY records the shortfall as an explicit open item with the failing viewport, candidate length and pixel overflow.
  </acceptance_criteria>

  <done>
    A single character limit N is chosen from recorded measurements of the real rendered DetailHero at
    the project's tested mobile and desktop viewports, verified to be at or above the longest published
    statement, with the full evidence trail written into the SUMMARY and the measurement instrument
    removed from the repository.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Apply the calibrated limit to both duplicated Sanity localeTextField helpers, in lockstep (PORT-04, D-02/D-03)</name>

  <files>
    tests/unit/statement-length-limit.test.ts
    sanity/schemas/gallery.ts
    sanity/schemas/edition.ts
  </files>

  <read_first>
    - `sanity/schemas/gallery.ts` lines 17-47 — the doc comment explaining WHY the helper is duplicated (no shared schema-lib module exists) and the helper body with both subfields' existing validation chains. Read lines 108-118 too, to confirm line 113 is the only call site.
    - `sanity/schemas/edition.ts` lines 4-35 — its own copy of the same helper and the same doc comment. Read lines 90-100 to confirm line 95 is the only call site.
    - `sanity/schemas/siteSettings.ts` lines 35-50 and 180-190 — the THIRD copy of the helper and its two call sites (`welcomeBody`, `homepageIntro`). Read this specifically so you can recognise it and leave it alone.
    - `vitest.config.ts` — `include: ['tests/unit/**/*.test.ts']`, `environment: 'node'`, and note that coverage `include` is scoped to `src/lib/**` and `sanity/editorial/**`, so a source-text test adds no instrumented code and cannot move the coverage thresholds.
    - Task 2's recorded `N` from the SUMMARY.
  </read_first>

  <behavior>
    - Unit test 1: `sanity/schemas/gallery.ts` declares exactly 2 max-length constraints.
    - Unit test 2: `sanity/schemas/edition.ts` declares exactly 2 max-length constraints.
    - Unit test 3: every max-length value found across both files is identical — this is the lockstep guard CONTEXT.md D-03 explicitly calls for.
    - Unit test 4: that shared value is within the band 300 to 800 inclusive, catching a fat-fingered digit.
    - Unit test 5: `sanity/schemas/siteSettings.ts` declares 0 max-length constraints, proving the out-of-scope third helper copy was left alone.
  </behavior>

  <action>
    Step 1 — write the failing unit tests FIRST, in a new file `tests/unit/statement-length-limit.test.ts`.

    A source-text test is the right instrument here, not a schema-import test: these modules import
    from the `sanity` package, which lives in `sanity/node_modules` and is not resolvable from the root
    Vitest project. Read each file with `node:fs` `readFileSync` relative to the repo root and match the
    max-length constraint calls with a regex capturing the numeric argument.

    Write the five assertions from the behavior block. Give each a message naming the file, because the
    failure this test exists to catch — editing one schema and forgetting the other — is otherwise
    indistinguishable from a typo.

    Add a file-level comment explaining WHY this test exists: `gallery.ts` and `edition.ts` each carry
    their own private copy of `localeTextField` with no shared module between them, so nothing in the
    type system or the build prevents the two content types' statement limits from silently diverging.

    Run `npm run test:unit` and confirm the new tests FAIL — currently both files declare 0 constraints
    (the RED step). Record the output.

    Step 2 — edit `sanity/schemas/gallery.ts`. Inside the local `localeTextField` helper, on the `fr`
    subfield (line 36) and the `en` subfield (line 43), chain a max-length constraint of N between the
    existing required constraint and the terminal error-message call. The resulting chain reads:
    required, then max of N, then the error message. This is the exact shape 18-PATTERNS.md specifies —
    do not restructure it into an array of rules or split it into two validation entries.

    Update both error message strings so they describe both conditions, since a single terminal error
    call applies its message to the whole chain and the current message would misreport a
    length failure as a missing-value failure. Keep them French and keep the existing sentence style
    (a full sentence, capitalised, ending in a period), naming the limit explicitly so Romane sees the
    number: the French subfield's message states the French text is required and must not exceed N
    characters; the English subfield's message states the same for the English text. Write the numeral
    N literally in both strings — do not interpolate a variable, because the surrounding code is a
    plain literal-valued schema and a variable would be the only one of its kind in the file.

    Step 3 — make the IDENTICAL edit in `sanity/schemas/edition.ts`, on its `fr` subfield (line 24) and
    `en` subfield (line 31), with the SAME N and the same two message strings. CONTEXT.md D-03 is
    explicit that there is no reason for the two content types to differ; divergence here is the
    single most likely defect in this plan and the unit test above is its only automated guard.

    Step 4 — do NOT touch `sanity/schemas/siteSettings.ts`. Its `localeTextField` copy backs
    `welcomeBody` and `homepageIntro`, which are unrelated legacy fields; capping them is unrequested
    scope and would invalidate existing content with no requirement behind it.

    Step 5 — run `npm run test:unit` (GREEN), then the Studio gates that CI runs as blocking steps:
    `npm --prefix sanity run lint` and `npm --prefix sanity run build`. The build is the real proof the
    schema still compiles into a working Studio.

    Step 6 — confirm the rule is live in the actual Studio rather than only in source. Start
    `npm --prefix sanity run dev`, open the `statement` field on any collection and any edition, paste
    text longer than N, and confirm Studio shows the new French error and blocks publishing; then
    confirm the longest ALREADY-PUBLISHED document (from Task 2's table) opens with NO validation error,
    proving N did not retroactively invalidate shipped content. Record both observations. Stop the dev
    server afterwards.

    Step 7 — re-run Task 1's PORT-04 e2e block one final time to confirm the CSS removal and the schema
    cap agree: the un-clamped statement still fits the panel for every published gallery.
  </action>

  <verify>
    <automated>npm run test:unit</automated>
    <automated>npm --prefix sanity run lint</automated>
    <automated>npm --prefix sanity run build</automated>
    <automated>grep -cE '\.max\([0-9]+\)' sanity/schemas/gallery.ts | grep -qx 2 && grep -cE '\.max\([0-9]+\)' sanity/schemas/edition.ts | grep -qx 2 && echo "GATE PASS: exactly two constraints per schema file"</automated>
    <automated>grep -hoE '\.max\([0-9]+\)' sanity/schemas/gallery.ts sanity/schemas/edition.ts | sort -u | wc -l | tr -d ' ' | grep -qx 1 && echo "GATE PASS: both schema files carry the identical limit"</automated>
    <automated>grep -cE '\.max\([0-9]+\)' sanity/schemas/siteSettings.ts | grep -qx 0 && echo "GATE PASS: siteSettings helper untouched"</automated>
    <automated>npm run build && npm run test:e2e -- gallery.spec.ts --project=chromium</automated>
    <automated>npm run typecheck</automated>
    <human-check>Run `npm --prefix sanity run dev`, open a collection's Texte de présentation field, paste text longer than N characters, and confirm the French error message appears and names the character limit, and that Publier is blocked. Then open the longest existing published document and confirm it shows NO validation error. Finally, run `npm run build && npm run preview`, open the gallery with the longest description at desktop width and at a phone width, and confirm the whole description reads to its end with no cut-off and no text spilling outside the hero panel.</human-check>
  </verify>

  <acceptance_criteria>
    - `sanity/schemas/gallery.ts` contains exactly 2 max-length constraint calls; `sanity/schemas/edition.ts` contains exactly 2; `sanity/schemas/siteSettings.ts` contains 0.
    - Every max-length numeric argument across `gallery.ts` and `edition.ts` is the same value, and that value equals the `N` recorded in Task 2's SUMMARY section.
    - In both files, both subfields' validation chains still begin with the required constraint and still end with a terminal error-message call — the new constraint was inserted into the existing chain, not substituted for part of it.
    - Both files' error message strings are French, are full sentences matching the existing style, and state the N-character limit as a literal numeral.
    - `git diff --stat` shows equal and symmetric change counts for `gallery.ts` and `edition.ts` — the two edits are the same edit.
    - The `rows: 5` option, the `columns: 2` option, the helper's `description` string, and both call sites (`localeTextField('statement', 'Texte de présentation', 'content')`) are unchanged in the diff.
    - `npm run test:unit` passes, including all five new assertions, and the tests FAILED before the schema edits with 0 constraints found (RED output quoted in the SUMMARY).
    - `npm --prefix sanity run lint` and `npm --prefix sanity run build` both exit 0.
    - The Studio dev-server check is recorded in the SUMMARY with two observations: over-length input is blocked with the new French message, AND the longest already-published document opens with no validation error.
    - Task 1's PORT-04 e2e block still passes after the schema change.
  </acceptance_criteria>

  <done>
    Both duplicated Sanity helpers cap the statement field's French and English subfields at the same
    empirically-derived limit with clear French messaging, the third out-of-scope helper copy is
    provably untouched, a unit test guards the two files against silent divergence, and the Studio has
    been observed enforcing the new rule without invalidating any published document.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated Studio editor → Sanity Content Lake | Romane's editorial text enters the system here. This plan ADDS a bound on that input (a max-length constraint), which narrows the boundary rather than widening it. |
| Sanity Content Lake → build-time fetch → static HTML | Statement text is fetched at build time and rendered into the static artifact, and also into the `<meta name="description">` fallback. No projection or escaping path is modified by this plan. |
| Static artifact → visitor browser | Presentational CSS only. No request-time compute exists in this project (`output: 'static'`, no adapter). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Denial of Service | `.detail-hero__statement` inside `.detail-hero__pin` (fixed height, `overflow: hidden`) | low | mitigate | Removing the CSS clamp makes statement length unbounded at display time in a fixed-height clipped panel, so over-long text could visually hide the title or escape the panel. Bounded at the source by the Sanity max-length constraint (Task 3), with the specific limit derived from measurements of the real rendered panel (Task 2) and locked by e2e pin-containment assertions at 390x844 and 1280x900 across every published gallery (Task 1). Impact is cosmetic only on a static artifact — no availability or resource-exhaustion consequence. |
| T-18-05 | Tampering | Validation logic in the duplicated `localeTextField` helpers, `gallery.ts` and `edition.ts` | low | mitigate | The change is additive: the new constraint is chained AFTER the existing required constraint, which is preserved and asserted. The real risk is divergence between two copies that no shared module or type binds together — mitigated by `tests/unit/statement-length-limit.test.ts`, which fails if the two files' limits differ or if either declares other than exactly two constraints. `npm --prefix sanity run build` is a blocking CI gate proving the schema still compiles. |
| T-18-06 | Information Disclosure | `gallery.seo?.description?.[locale] ?? statement` (`src/pages/galleries/[slug].astro` line 60) | low | accept | The statement doubles as the SEO meta description fallback, so N also bounds meta description length. Capping can only SHORTEN an already-public string; it exposes nothing new and reaches no new consumer. |
| T-18-07 | Repudiation | Choice of the limit N | low | accept | An unexplained magic number in a schema is unauditable later. Task 2's acceptance criteria require the full measurement table, both derived thresholds, `N_floor`, the chosen N and which decision branch was taken to be recorded in the SUMMARY, so the value is traceable to evidence rather than to preference. |
| T-18-SC | Tampering | npm / pip / cargo installs | high | mitigate | This plan performs NO package-manager installs. `package.json`, `package-lock.json`, `sanity/package.json` and `sanity/package-lock.json` are guard-listed as unmodified. Task 2 uses the already-installed Playwright and Task 3 the already-installed Vitest and Sanity CLI. The executor must not run any install command; if a task appears to require one, stop and escalate. No RESEARCH.md Package Legitimacy Audit is required because there are no install tasks. |

At `security_asvs_level: 1` with `security_block_on: "high"`, the only high-severity entry is the
standing supply-chain control, satisfied by performing zero installs. Every other finding is low and
either mitigated by an automated gate in this plan or accepted with rationale. Zero blocking findings
is the expected and correct outcome for a CSS deletion plus a content-validation tightening.
</threat_model>

<verification>
1. `npm run typecheck` (astro check) exits 0.
2. `npm --prefix sanity run lint` exits 0.
3. `npm --prefix sanity run build` exits 0 — the blocking Studio gate CI runs first.
4. `npm run build` succeeds and `npm run test:artifact` passes.
5. `npm run test:unit` passes, including the five new lockstep assertions.
6. `npm run test:e2e` passes on both configured projects.
7. `git diff --name-only` lists exactly: `src/components/DetailHero.astro`,
   `sanity/schemas/gallery.ts`, `sanity/schemas/edition.ts`, `tests/e2e/gallery.spec.ts`,
   `tests/unit/statement-length-limit.test.ts`. `tests/e2e/_calibrate-statement.spec.ts` must NOT
   appear. Any other file is a scope violation to be explained or reverted.
8. The SUMMARY carries the full calibration evidence trail required by Task 2.
</verification>

<success_criteria>
- PORT-04 (display): `.detail-hero__statement` reports computed `-webkit-line-clamp` of `none`, does
  not self-clip, and stays within `.detail-hero__pin`'s bounds for every published gallery at both
  1280x900 and 390x844 — asserted by Playwright.
- PORT-04 (source bound): both `gallery.ts` and `edition.ts` cap `statement.fr` and `statement.en` at
  the same limit N, with the existing required constraint preserved, asserted by a unit test that
  fails on divergence.
- N is at or above the longest currently-published statement, so no existing document is retroactively
  invalidated — observed directly in a running Studio, not merely inferred.
- N is traceable to recorded measurements of the real rendered component, not to a guess.
- `sanity/schemas/siteSettings.ts` is provably untouched.
- The temporary calibration probe exists in no commit.
- Full unit, e2e, typecheck and Studio lint/build gates pass.
</success_criteria>

<output>
Create `.planning/phases/18-gallery-ditions-display-fixes/18-02-SUMMARY.md` when done.

The SUMMARY must record:
- The re-run GROQ length table and the derived `N_floor`.
- The probe's full overflow-measurement table across all three viewports and all eight candidate
  lengths.
- `N_safe_mobile`, `N_safe_desktop`, `N_binding`, the chosen `N`, and which Step 4 decision branch
  was applied — plus the shortfall detail if the fallback branch was taken.
- The correction of CONTEXT.md's "250-320 characters" starting estimate, with the reason (it would
  have invalidated 4 of 8 published documents).
- Quoted RED output for the e2e clamp tests and the schema unit tests.
- The two Studio dev-server observations from Task 3 Step 6.
</output>
