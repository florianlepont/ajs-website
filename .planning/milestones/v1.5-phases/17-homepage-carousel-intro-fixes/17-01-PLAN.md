---
phase: 17-homepage-carousel-intro-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HomeCarousel.astro
  - tests/e2e/homepage-carousel-core.spec.ts
  - tests/e2e/homepage-content-display.spec.ts
  - tests/e2e/homepage-wordmark-peek.spec.ts
autonomous: true
requirements:
  - HOME-11
  - HOME-12

must_haves:
  truths:
    - "Hovering the pointer over `[data-role=\"home-carousel\"]` does NOT stop auto-advance: the index label still changes across a 6000ms tick while hovered (HOME-11, D-01)."
    - "Focusing `[data-role=\"autoplay-toggle\"]` with the keyboard still freezes auto-advance, and blurring it resumes advancing (D-02)."
    - "The explicit pause/play toggle still pauses and resumes exactly as before — `aria-pressed`/`aria-label` flip and the paused state survives pointer movement (D-03)."
    - "The dashed progress-fill no longer freezes while the pointer merely hovers the hero (same stopAutoAdvance/startAutoAdvance call chain)."
    - "In grid mode `.home-grid__intro-body` renders its full Sanity text: computed `-webkit-line-clamp` is `none`, the paragraph has no vertical overflow, and it stays inside the hero tile's clipped square at both 1280x800 and 375x812 (HOME-12, D-04)."
    - "The gallery-tile hover description keeps its deliberate 3-line clamp — this phase does not touch it."
  artifacts:
    - "src/components/HomeCarousel.astro — pointer-hover pause listeners removed, `.home-grid__intro-body` truncation removed, two stale comments corrected"
    - "tests/e2e/homepage-carousel-core.spec.ts — hover assertion inverted + new keyboard-focus-pause regression test"
    - "tests/e2e/homepage-content-display.spec.ts — new HOME-12 describe block asserting the intro paragraph is neither clamped nor clipped"
  key_links:
    - "`focusin`/`focusout` → stopAutoAdvance/startAutoAdvance on `[data-role=\"home-carousel\"]` — the listener pair that MUST survive (D-02); losing it regresses keyboard a11y with no visible symptom."
    - "`autoplayToggle` click → `autoAdvancePausedByUser` → `startAutoAdvance()`'s early-return guard — the explicit-pause state machine that must stay independent of the listener removal (D-03)."
    - "`.home-grid__tile`'s `aspect-ratio: 1 / 1` + `overflow: hidden` — the parent that can still clip the un-clamped paragraph after the clamp is gone; the real HOME-12 failure mode."
    - "`page.clock.install()` in hover-dependent e2e specs — the seam where removing hover-pause can silently destabilise unrelated tests that previously relied on the carousel freezing under the pointer."
---

<objective>
Fix two shipped homepage bugs, both living in `src/components/HomeCarousel.astro`:

- **HOME-11** — the hero carousel stops auto-advancing when the visitor merely hovers the page with a mouse. Remove the pointer-hover pause only; keep the keyboard-focus pause and the explicit pause/play toggle exactly as they are.
- **HOME-12** — the grid-mode intro paragraph is truncated after 2 lines by a CSS line clamp. Remove the clamp entirely so the real Sanity copy shows in full.

Purpose: restore the intended always-on hero experience and stop hiding published content, without regressing keyboard accessibility (D-02) or the explicit autoplay control (D-03).
Output: three edited files (one component, two e2e specs), with the new behaviour locked by automated Playwright regression tests.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-homepage-carousel-intro-fixes/17-CONTEXT.md
@.planning/phases/17-homepage-carousel-intro-fixes/17-PATTERNS.md
</context>

## Artifacts this phase produces

This phase is dominated by **removals**. The symbols below are the full inventory — a later
source-grounding pass should expect the removed items to be *absent* from the final code.

**Removed (JS, `src/components/HomeCarousel.astro`, lines 823-824):**
- The `hero` listener registration whose event name is `mouseenter` (handler: `stopAutoAdvance`)
- The `hero` listener registration whose event name is `mouseleave` (handler: `startAutoAdvance`)

**Removed (CSS, `src/components/HomeCarousel.astro`, `.home-grid__intro-body` rule, lines 2436-2445):**
- `display: -webkit-box;`
- `-webkit-line-clamp: 2;`
- `-webkit-box-orient: vertical;`
- `overflow: hidden;`

**Added:**
- `tests/e2e/homepage-carousel-core.spec.ts` — one new `test(...)` inside `test.describe('auto-advance + pause (D-09)')` asserting keyboard-focus pause/resume (D-02 regression guard). File goes from 16 to 17 tests.
- `tests/e2e/homepage-content-display.spec.ts` — one new `test.describe(...)` block with two `test(...)` cases (desktop 1280x800 and mobile 375x812) asserting the intro paragraph is neither clamped nor clipped. File goes from 16 to 18 tests.

**Modified in place (not new symbols):**
- `tests/e2e/homepage-carousel-core.spec.ts` — the first test in `test.describe('auto-advance + pause (D-09)')` (currently lines 68-87) is renamed and its final assertion inverted.
- Two stale code comments in `src/components/HomeCarousel.astro` (lines 762-763 above `startAutoAdvance`, lines 828-835 above `goToPrev`).

**Explicitly NOT changed (guard list — these must still exist afterwards):**
- `hero.addEventListener('focusin', stopAutoAdvance);` and `hero.addEventListener('focusout', startAutoAdvance);` (lines 825-826) — D-02
- `stopAutoAdvance()` (756-760), `startAutoAdvance()` (764-773) and its `autoAdvancePausedByUser` guard
- `syncAutoplayControl()` (775-785) and the `autoplayToggle` click handler (787-796) — D-03
- `reduceMotionQuery` change handler (798-804), `showCarousel()`/`showGrid()` (806-821)
- `goToPrev()`/`goToNext()`'s `if (timer !== null) startAutoAdvance();` resync (836-846)
- The `heroPhoto` peek listeners at ~1174/~1184 (desktop wordmark-peek feature, unrelated to auto-advance)
- `.home-grid__tile-description`'s `-webkit-line-clamp: 3` and `min-height: calc(1.4em * 3)` (lines 2560-2580)
- No package-manager installs: `package.json` / `package-lock.json` are not touched.

## Source Coverage Audit

| Source | Item | Covered by |
|--------|------|------------|
| GOAL (ROADMAP Phase 17) | "uninterrupted carousel auto-advance" | Task 1 |
| GOAL (ROADMAP Phase 17) | "full grid-mode intro text" | Task 2 |
| GOAL success criterion 2 | "explicit pause/play toggle still works exactly as before" | Task 1 (guard list + existing toggle test kept green), Task 3 |
| REQ | HOME-11 | Task 1 |
| REQ | HOME-12 | Task 2 |
| RESEARCH | — | No RESEARCH.md for this phase (research deliberately skipped — narrow, fully-diagnosed bugfix). Not a gap. |
| CONTEXT | D-01 remove pointer-hover pause only | Task 1 |
| CONTEXT | D-02 keep keyboard-focus pause | Task 1 (guard list + new focus regression test) |
| CONTEXT | D-03 toggle button unaffected | Task 1 (guard list), Task 3 (full-suite sweep keeps the existing toggle test green) |
| CONTEXT | D-04 remove the 2-line clamp entirely, no substitute cap | Task 2 |
| CONTEXT | Claude's Discretion — spacing polish if the un-clamped text collides | Task 2 step 5 |
| CONTEXT | Deferred Ideas: none | n/a |

No unplanned items. No phase split needed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stop the pointer hover from pausing auto-advance, keep the keyboard-focus pause (HOME-11, D-01/D-02/D-03)</name>

  <files>
    tests/e2e/homepage-carousel-core.spec.ts
    src/components/HomeCarousel.astro
  </files>

  <read_first>
    - `src/components/HomeCarousel.astro` lines 740-850 — `restartFill`, `stopAutoAdvance`, `startAutoAdvance` and its `autoAdvancePausedByUser` guard, `syncAutoplayControl`, the `autoplayToggle` click handler, the reduced-motion handler, `showCarousel`/`showGrid`, the four-line listener block at 823-826, and the `goToPrev`/`goToNext` timer-resync comment at 828-835
    - `src/components/HomeCarousel.astro` lines 360-415 — where `hero` is resolved (`document.querySelector('[data-role="home-carousel"]')`) and where `indexLabel` / `autoplayToggle` are looked up as its descendants
    - `tests/e2e/homepage-carousel-core.spec.ts` lines 1-125 — the whole `test.describe('auto-advance + pause (D-09)')` block plus the mocked-clock pattern it establishes
    - `.planning/phases/17-homepage-carousel-intro-fixes/17-CONTEXT.md` — D-01, D-02, D-03 and the "Verified mechanism" note
    - `.planning/phases/17-homepage-carousel-intro-fixes/17-PATTERNS.md` — the listener-pair analog and the mocked-clock testing pattern
  </read_first>

  <behavior>
    - Test A (rewritten, currently asserts the opposite): with `page.clock.install()`, the index label changes after `fastForward(6000)`; then, with the pointer resting on `[data-role="home-carousel"]`, another `fastForward(6000)` changes it AGAIN. This is the RED test — it must fail against the unmodified component.
    - Test B (new, D-02 regression guard): with `page.clock.install()`, focusing `[data-role="autoplay-toggle"]` freezes the index across `fastForward(6000)`; blurring it and advancing another 6000ms changes the index. This test passes immediately against the unmodified component — it exists to lock behaviour that must NOT change.
    - Unchanged and must stay green: the explicit-toggle-persistence test (lines 89-109) and the reduced-motion test (lines 111-121).
  </behavior>

  <action>
    Do the test edit first, observe RED, then make the component change.

    1. In `tests/e2e/homepage-carousel-core.spec.ts`, rewrite the first test inside `test.describe('auto-advance + pause (D-09)')` (currently lines 68-87). Rename it — its current title claims the carousel "pauses on hover", which is no longer true; use a title naming HOME-11 and stating the carousel keeps advancing while hovered. Keep unchanged: the `page.clock.install()` call, `page.goto('/')`, the `carousel` locator `[data-role="home-carousel"]`, the `indexLabel` locator `carousel.getByText(/^\d{2} \/ \d{2}$/)`, the `toBeVisible()` check, and the first `fastForward(6000)` + `.not.toHaveText(initialLabel)` pair. Change ONLY the tail: after `await carousel.hover()` and the second `await page.clock.fastForward(6000)`, the assertion on `indexLabel` becomes `.not.toHaveText(labelAfterFirstAdvance)`. Rewrite the explanatory comment above it (currently lines 80-82) so it states that the pointer resting on the carousel no longer stops the timer, and reference HOME-11.

    2. Add a sibling `test(...)` in the same describe block for D-02. It must: call `await page.clock.install()` before `await page.goto('/')`; locate `[data-role="index-label"]` and `[data-role="autoplay-toggle"]`; call `.focus()` on the toggle — NOT `.click()`, because clicking flips `autoAdvancePausedByUser` and would exercise the wrong mechanism; `fastForward(6000)` and expect the index label to still equal its pre-focus value; then blur the toggle (`.blur()` on the same locator), `fastForward(6000)`, and expect the label to have changed. Add a comment noting `[data-role="autoplay-toggle"]` is a descendant of `[data-role="home-carousel"]`, which is why focusing it triggers the carousel root's `focusin` listener.

    3. Run `npm run build` then `npx playwright test tests/e2e/homepage-carousel-core.spec.ts --project=chromium` and confirm the RED state: exactly the rewritten hover test fails, and the new focus test plus all other tests in the file pass. Record this observation in the SUMMARY. `npm run build` is mandatory before every Playwright run in this phase — the Playwright `webServer` runs `npm run preview`, which serves the already-built `dist/`, and `reuseExistingServer` is true locally, so skipping the build silently tests stale output.

    4. In `src/components/HomeCarousel.astro`, delete exactly two lines from the four-line listener block at 823-826: the `hero` listener registration whose event name is `mouseenter` (handler `stopAutoAdvance`, line 823) and the one whose event name is `mouseleave` (handler `startAutoAdvance`, line 824). Leave lines 825-826 — the `focusin` and `focusout` registrations — byte-for-byte unchanged (D-02). Per the "Verified mechanism" note in CONTEXT.md this is a self-contained deletion: `startAutoAdvance()` already guards on `autoAdvancePausedByUser` at line 766, so nothing else needs adjusting.

    5. Do not modify anything on the guard list in the "Artifacts this phase produces" section above — in particular `stopAutoAdvance`/`startAutoAdvance` themselves, the `autoplayToggle` click handler (D-03), the reduced-motion handler, or the `heroPhoto` peek listeners near lines 1174 and 1184 (a different element and a different feature).

    6. Correct two now-stale comments (Claude's Discretion, explicitly in scope for accuracy):
       - Lines 762-763, above `startAutoAdvance`: it currently describes the timer as paused by pointer entry and resumed by pointer exit as well as by focus. Reword so it describes focus-only pausing and notes that the pointer no longer pauses it, citing HOME-11. Keep the existing `D-09:` prefix — that label belongs to phase 04.1's decision numbering, not this phase's D-01..D-04.
       - Lines 828-835, above `goToPrev`: it currently justifies the `if (timer !== null) startAutoAdvance();` resync by claiming it preserves pointer-hover pause because "the timer is null during hover". Reword so it refers only to the focus pause and the explicit-pause state. The resync logic itself is unchanged.

    7. Re-run the build + spec from step 3 and confirm GREEN.
  </action>

  <verify>
    <automated>npm run build && npx playwright test tests/e2e/homepage-carousel-core.spec.ts --project=chromium</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF "hero.addEventListener('mouseenter'" src/components/HomeCarousel.astro` prints `0`
    - `grep -cF "hero.addEventListener('mouseleave'" src/components/HomeCarousel.astro` prints `0`
    - `grep -cF "hero.addEventListener('focusin', stopAutoAdvance);" src/components/HomeCarousel.astro` prints `1`
    - `grep -cF "hero.addEventListener('focusout', startAutoAdvance);" src/components/HomeCarousel.astro` prints `1`
    - `grep -cF "heroPhoto.addEventListener('mouseenter'" src/components/HomeCarousel.astro` prints `1` and `grep -cF "heroPhoto.addEventListener('mouseleave'" src/components/HomeCarousel.astro` prints `1` (wordmark-peek feature untouched)
    - `grep -cF "autoplayToggle?.addEventListener('click'" src/components/HomeCarousel.astro` prints `1` (D-03 toggle handler untouched)
    - `grep -c "  test(" tests/e2e/homepage-carousel-core.spec.ts` prints `17` (was 16 — exactly one test added)
    - `npx playwright test tests/e2e/homepage-carousel-core.spec.ts --project=chromium` reports 17 passed, 0 failed, after a fresh `npm run build`
    - The SUMMARY records the intermediate RED result from step 3: the rewritten hover test failing before the component edit, and the new focus test passing before it.
  </acceptance_criteria>

  <done>
    The pointer resting on the hero no longer stops the 6000ms timer or freezes the progress-dash fill; keyboard focus still pauses and blur still resumes; the pause/play button behaves exactly as before; both behaviours are locked by passing e2e tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Show the grid-mode intro paragraph in full (HOME-12, D-04)</name>

  <files>
    tests/e2e/homepage-content-display.spec.ts
    src/components/HomeCarousel.astro
  </files>

  <read_first>
    - `src/components/HomeCarousel.astro` line 92 (`const introBody = siteCopy.homepageIntro;`) and lines 291-300 (the grid hero tile markup — `.home-grid__tile.home-grid__tile--hero` containing `.home-grid__wordmark` and `.home-grid__intro-body`)
    - `src/components/HomeCarousel.astro` lines 2409-2470 — `.home-grid__tile--hero` (padding/flex), `.home-grid__wordmark` (font-size 40px, `margin: 0 0 var(--space-sm)`), `.home-grid__intro-body` (lines 2436-2445, the rule being edited), `.home-grid__tiles`, and `.home-grid__tile` (`aspect-ratio: 1 / 1`, `overflow: hidden`)
    - `src/components/HomeCarousel.astro` lines 2555-2586 — `.home-grid__tile-description`, the OTHER `-webkit-line-clamp` in this file, which must stay
    - `src/components/HomeCarousel.astro` lines 2700-2745 — the `@media (max-width: 767px)` overrides, including `.home-grid__tiles { grid-template-columns: 1fr; }`
    - `tests/e2e/homepage-content-display.spec.ts` lines 18-50 and 125-145 — the established "enter grid mode" pattern (`page.goto('/')` then `page.getByRole('button', { name: 'Grille' }).click()`), the `page.setViewportSize` pattern, and the existing tile-description hover test that must stay green
    - `.planning/phases/17-homepage-carousel-intro-fixes/17-CONTEXT.md` — D-04 and the Claude's Discretion spacing note
  </read_first>

  <behavior>
    - Desktop (1280x800), grid mode: `.home-grid__intro-body` has computed `-webkit-line-clamp` equal to `none`, `scrollHeight <= clientHeight + 1`, and its bounding-rect bottom is within its `.home-grid__tile--hero` ancestor's bounding-rect bottom.
    - Mobile (375x812, single-column grid), grid mode: the same three assertions hold.
    - Both cases first assert the paragraph is non-empty, so an empty Sanity value cannot produce a vacuous pass.
    - Unchanged and must stay green: the existing "grid tile reveals its collection statement on hover" test at lines 18-27.
  </behavior>

  <action>
    Do the test edit first, observe RED, then make the CSS change.

    1. Add a new `test.describe(...)` block to `tests/e2e/homepage-content-display.spec.ts` named for HOME-12 (the grid intro paragraph is not truncated), containing two tests: one at viewport 1280x800 and one at 375x812. In each: call `page.setViewportSize(...)` BEFORE `page.goto('/')` (same order as the existing test at line 30), then click the grid button via `page.getByRole('button', { name: 'Grille' })` exactly as the existing grid tests do.

    2. In each test, first assert the paragraph is non-empty with `await expect(page.locator('.home-grid__intro-body')).toHaveText(/.+/)`. Then read the geometry in a single `.evaluate(...)` on `.home-grid__intro-body` returning an object with: `getComputedStyle(el).webkitLineClamp`, `el.scrollHeight`, `el.clientHeight`, `el.getBoundingClientRect().bottom`, and the `.getBoundingClientRect().bottom` of `el.closest('.home-grid__tile--hero')`. Assert the clamp value is `'none'`, `scrollHeight <= clientHeight + 1`, and the paragraph bottom is `<= tileBottom + 0.5`. The tile-bottom assertion is the one that matters most: `.home-grid__tile` sets `aspect-ratio: 1 / 1` and `overflow: hidden`, so a paragraph can be fully un-clamped and still be clipped by its own tile.

    3. Do NOT hardcode the intro copy — it is Sanity-authored (`siteCopy.homepageIntro`, line 92) and can change without a code deploy. Assert structurally only.

    4. Run `npm run build` then `npx playwright test tests/e2e/homepage-content-display.spec.ts --project=chromium` and confirm RED: both new tests fail on the clamp value (they will read `2`, not `none`). Record this in the SUMMARY. The `npm run build` step is mandatory — see Task 1 step 3 for why.

    5. Edit the `.home-grid__intro-body` rule at lines 2436-2445 so it declares exactly four properties, in this order: `font-size` (14px, keeping its existing `/* Label role — ... */` inline comment), `font-weight`, `line-height`, `margin`. Remove the four truncation declarations that follow them. Per D-04, do NOT substitute any other cap: no higher line-clamp number, no `max-height` paired with clipping, no fade mask. The user explicitly chose full text over a defensive cap.

    6. Do NOT touch the `.home-grid__tile-description` rule at lines 2560-2580 — its `-webkit-line-clamp: 3` plus `min-height: calc(1.4em * 3)` is a different element's deliberate equal-height layout reservation from 260718-rhv, and removing it would reintroduce a fixed bug.

    7. Re-run the build + spec. If the tile-bottom assertion still fails at either viewport, apply the smallest spacing adjustment that makes the text fit (Claude's Discretion, in scope): reduce `.home-grid__wordmark`'s bottom margin (line 2433) or `.home-grid__tile--hero`'s `padding` (line 2412), scoping the change inside the existing `@media (max-width: 767px)` block starting at line 2589 if only the mobile viewport is affected. Do not shrink the intro `font-size` below 14px, and do not reintroduce any line cap. If no adjustment was needed, say so explicitly in the SUMMARY rather than leaving it ambiguous.
  </action>

  <verify>
    <automated>npm run build && npx playwright test tests/e2e/homepage-content-display.spec.ts --project=chromium</automated>
  </verify>

  <acceptance_criteria>
    - `awk '/^  \.home-grid__intro-body \{/{f=1;next} f&&/^  \}/{exit} f{print}' src/components/HomeCarousel.astro | grep -oE '^ +[a-z-]+:' | tr -d ' :'` prints exactly four lines, in order: `font-size`, `font-weight`, `line-height`, `margin`
    - `grep -cF -- "-webkit-line-clamp" src/components/HomeCarousel.astro` prints `1` (down from 2 — only the tile-description clamp remains)
    - `grep -cF -- "-webkit-line-clamp: 3;" src/components/HomeCarousel.astro` prints `1`
    - `grep -c "  test(" tests/e2e/homepage-content-display.spec.ts` prints `18` (was 16 — exactly two tests added)
    - `npx playwright test tests/e2e/homepage-content-display.spec.ts --project=chromium` reports 18 passed, 0 failed, after a fresh `npm run build`
    - The SUMMARY records the intermediate RED result from step 4 and states whether the step-7 spacing adjustment was needed (and if so, exactly which declaration changed).
  </acceptance_criteria>

  <done>
    In grid mode, the pink hero tile shows the whole Sanity intro paragraph with no ellipsis and no clipping, on both a desktop and a narrow mobile viewport; the gallery-tile hover description still clamps at 3 lines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full-suite regression sweep, stabilising hover-dependent specs with a frozen clock</name>

  <files>
    tests/e2e/homepage-wordmark-peek.spec.ts
    tests/e2e/homepage-content-display.spec.ts
  </files>

  <read_first>
    - `tests/e2e/homepage-wordmark-peek.spec.ts` lines 1-100 and 150-200 — every test here parks the pointer on `.home-hero__photo` and then asserts per-slide values (cursor zone/label, and at lines 49-50 the cursor ring background compared against the live `[data-role="accent-panel"]` colour); none of them install a mocked clock
    - `tests/e2e/homepage-content-display.spec.ts` lines 29-60 — 'carousel keeps its navigation fixed and clamps the caption width', which hovers the carousel and then clicks every progress dash in a loop
    - `tests/e2e/homepage-carousel-core.spec.ts` lines 67-121 — the `page.clock.install()` usage that is the stabilisation pattern to copy
    - `playwright.config.ts` — `webServer: npm run preview`, `reuseExistingServer: !CI`, and the two projects (`chromium`, and `webkit-mobile` which only matches `**/*.smoke.spec.ts`)
  </read_first>

  <action>
    1. Run the full gate locally, in this order: `npm run build`, then `npm run test:e2e`, `npm run test:unit`, `npm run typecheck`, `npm run lint`.

    2. Watch specifically for fallout from Task 1. Before this phase, hovering the hero froze the carousel, and several specs unknowingly depended on that freeze to keep per-slide values stable while they asserted. With auto-advance now running under the pointer, a slide can change mid-assertion. The two known-risk files are `tests/e2e/homepage-wordmark-peek.spec.ts` (pointer parked on the hero photo in nearly every test, with per-slide colour assertions) and `tests/e2e/homepage-content-display.spec.ts` lines 29-60 (hovers the carousel, then loops clicking each progress dash).

    3. If a test fails or flakes for that reason, stabilise it by adding `await page.clock.install()` as the first statement of that test, before its `page.goto(...)`. An installed-but-never-fast-forwarded clock freezes time, so the 6000ms interval never fires — this is the same API already used in `homepage-carousel-core.spec.ts`. Add a one-line comment on each such call explaining it freezes the clock because HOME-11 removed the hover pause those assertions used to rely on.

    4. Forbidden stabilisations: do not reintroduce a pointer-hover pause in the component, do not add `page.waitForTimeout` sleeps, do not weaken or delete an assertion, and do not mark a test `.skip`/`.fixme`.

    5. Re-run any spec you touched three times consecutively (`npx playwright test <file> --project=chromium --repeat-each=3`) to confirm the fix is stable, not lucky.

    6. If the sweep passes with no changes needed, leave both files untouched and state that explicitly in the SUMMARY — an empty diff here is a valid, expected outcome.
  </action>

  <verify>
    <automated>npm run build && npm run test:e2e && npm run test:unit && npm run typecheck && npm run lint</automated>
    <human-check>
      Run `npm run dev` and open http://localhost:4321/ in a desktop browser:
      1. Rest the mouse pointer on the hero photo for ~20 seconds — the photo keeps changing every 6 seconds and the dashed progress indicator keeps filling (it must not freeze).
      2. Click the pause button — advancing stops. Click it again — advancing resumes.
      3. Press Tab until the pause button is focused (do not press Enter/Space) — advancing stops while it is focused. Tab away — advancing resumes.
      4. Click "Grille": the pink intro tile shows the whole intro paragraph with no "…" cut-off. Narrow the window to about 375px wide and confirm the paragraph is still complete and not cut off by the tile edge.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `npm run test:e2e` exits 0 with 0 failed and 0 flaky across both Playwright projects
    - `npm run test:unit` exits 0
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `git diff --name-only` for this task lists only files from the `<files>` list, or no files at all
    - If any spec was stabilised, `npx playwright test <that file> --project=chromium --repeat-each=3` passes, and the SUMMARY names the file, the test, and the reason
    - The SUMMARY reports the human-check outcome for all four steps (this project runs `human_verify_mode: end-of-phase`, so the check is reported, not gated inline)
  </acceptance_criteria>

  <done>
    The whole automated gate (e2e, unit, typecheck, lint) is green against a fresh build, with no test weakened or skipped, and any test that silently depended on hover-pause now freezes the clock explicitly instead.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Content Lake → Astro build | Published `siteCopy.homepageIntro` is fetched at build time and interpolated into static HTML. Pre-existing boundary; this phase changes only how the already-fetched text is *displayed*. |
| Static HTML/CSS/JS → visitor browser | Every change in this phase executes client-side in the visitor's own tab. No new input is accepted, no request-time compute exists (static OVH / GitHub Pages hosting). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `.home-grid__intro-body` rendering of Sanity `homepageIntro` (HomeCarousel.astro line 298) | low | accept | Removing the CSS clamp only reveals more of copy that was already in the shipped HTML — the clamp was never a security control. Astro interpolates `{introBody}` as an auto-escaped text node, so no HTML/script injection surface is created or widened. Authorship stays restricted to the authenticated Sanity Studio. |
| T-17-02 | Denial of Service | Homepage hero auto-advance timer | low | accept | Auto-advance now runs continuously rather than pausing under the pointer. This is one 6000ms `setInterval` in the visitor's own tab, swapping DOM nodes for images already present in the document — no per-tick network request and no server-side compute. Reduced-motion visitors still start paused (lines 798-804) and the explicit toggle still stops it permanently (D-03). |
| T-17-03 | Denial of Service (accessibility-adjacent) | Keyboard users navigating the carousel | medium | mitigate | Keep the `focusin`/`focusout` pause (D-02) so a slide cannot change under a keyboard user mid-navigation. Enforced by the new focus-pause regression test added in Task 1, and by the explicit guard list in "Artifacts this phase produces". |
| T-17-SC | Tampering | npm / pip / cargo installs | n/a | accept | No package-manager installs in this phase — `package.json` and `package-lock.json` are not modified, so the Package Legitimacy Gate does not apply. Any diff touching either file is out of scope and must be reverted. |

No threat is at or above the configured blocking severity (`high`) at ASVS level 1. A findings-free register is the expected outcome for a client-side event-listener and CSS removal on an already-public static page.
</threat_model>

<verification>
1. `npm run build` succeeds (static output, no adapter).
2. `npm run test:e2e` — full Playwright suite green, 0 failed, 0 flaky, across `chromium` and `webkit-mobile`.
3. `npm run test:unit`, `npm run typecheck`, `npm run lint` all exit 0.
4. Source guards hold: 0 `hero` pointer-enter/exit listener registrations, exactly 1 `focusin` + 1 `focusout` registration, exactly 1 remaining `-webkit-line-clamp` in `HomeCarousel.astro` (the `.home-grid__tile-description` 3-line one), `.home-grid__intro-body` down to exactly 4 declarations.
5. `git diff --stat` touches only the four files in `files_modified`; `package.json` and `package-lock.json` are untouched.
6. Human check (end-of-phase): hover keeps the carousel advancing; the pause button still toggles; keyboard focus still pauses; the grid intro paragraph is complete at desktop and ~375px widths.
</verification>

<success_criteria>
- HOME-11: hovering the pointer anywhere over the homepage does not pause auto-advance, and the progress-dash fill keeps animating.
- HOME-11: the explicit pause/play toggle pauses and resumes exactly as before, and keyboard focus still pauses (D-02, D-03).
- HOME-12: the grid-mode intro paragraph renders its full Sanity text — no clamp, no ellipsis, no clipping by the hero tile — at 1280x800 and 375x812.
- Every behaviour above is locked by an automated Playwright test that fails if the behaviour regresses.
- The full CI gate (build, e2e, unit, typecheck, lint) is green with no test skipped or weakened.
</success_criteria>

<output>
Create `.planning/phases/17-homepage-carousel-intro-fixes/17-01-SUMMARY.md` when done.
</output>
