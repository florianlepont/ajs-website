---
phase: 15-about-page-editorial-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/sketches/014-about-page-composition/index.html
  - .planning/sketches/014-about-page-composition/README.md
  - .planning/sketches/MANIFEST.md
autonomous: false
requirements: [ABOUT-04]
tags: [about, layout, sketch, editorial, design-exploration]

must_haves:
  truths:
    - "A reviewer can open one HTML mockup and compare 2-3 distinct About-page layout variants side by side, each using the real bio/practice/medium copy."
    - "Each variant resolves the two open decisions D-05 (hero-photo shrink reveal target) and D-07 (numbered-sections structure) in a visibly different way."
    - "A single winner is named with a one-line rationale, so the implementation wave has a concrete, human-approved layout to build."
  artifacts:
    - ".planning/sketches/014-about-page-composition/index.html"
    - ".planning/sketches/014-about-page-composition/README.md"
    - ".planning/sketches/MANIFEST.md (row 014 appended)"
  key_links:
    - "README.md frontmatter `winner` field names one variant → this is the direct input to plan 15-02/15-03 (they build the winner, not a fresh guess)."
    - "Every variant stays inside the locked constraints D-01/D-02/D-03/D-04/D-06 — sketching explores composition/rhythm only, never reopens the locked decisions."
---

<objective>
Produce the mandatory design-exploration deliverable for the About-page redesign: a throwaway HTML sketch presenting 2-3 distinct layout variants, reviewed by the user, resolving into one named winner. This satisfies ROADMAP Phase 15 success criterion 3 and the ABOUT-04 process contract ("explored via multiple layout proposals before implementation, same process as the Contact page redesign") — a single unreviewed guess is an explicit failure condition for this phase.

The two open questions this sketch must resolve (Claude's discretion per CONTEXT.md, but resolved by review here, NOT guessed by the implementer):
- **D-05:** what the pinned hero-photo shrink resolves into — a pure motion "settle" with no text reveal, vs. a reveal into the "01 Atelier & pratique" section using only existing markup/copy.
- **D-07:** the numbered-sections final structure — two-column restyle vs. stacked full-width row-list (Éditions-style) vs. another asymmetric structure.

Purpose: give the implementation waves (15-02, 15-03) a concrete, human-approved composition instead of an unreviewed guess.
Output: `.planning/sketches/014-about-page-composition/` (index.html + README.md) and an appended MANIFEST row.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-about-page-editorial-redesign/15-CONTEXT.md
@.planning/phases/15-about-page-editorial-redesign/15-UI-SPEC.md
@.planning/phases/15-about-page-editorial-redesign/15-PATTERNS.md
@.planning/sketches/013-contact-page-composition/README.md
@.planning/sketches/005-edition-hero-scroll-reveal/README.md
@src/components/AboutPageBody.astro
@src/components/PageTitleHeader.astro
@src/pages/about.astro
</context>

## Artifacts this phase produces (this plan)

- New file `.planning/sketches/014-about-page-composition/index.html` — self-contained HTML mockup with 2-3 named layout variants.
- New file `.planning/sketches/014-about-page-composition/README.md` — frontmatter (`sketch: 014`, `name`, `question`, `winner`, `tags`) + `## Variants` + `## Why {winner}` sections, matching the sketch 005/013 format.
- Appended row `| 014 | about-page-composition | ... | {winner} | ... |` in `.planning/sketches/MANIFEST.md`.
- A named design decision: the winning About-page layout direction (which D-05 reveal target + which D-07 sections structure), consumed by 15-02 and 15-03.

<tasks>

<task type="auto">
  <name>Task 1: Build 2-3 About-page layout variants as a throwaway HTML sketch</name>
  <files>.planning/sketches/014-about-page-composition/index.html</files>
  <read_first>
    - .planning/sketches/013-contact-page-composition/README.md (the sketch → real-feedback → named-winner process this must replicate; note its Round 1/2/3 viewport-ratio + mobile scrutiny)
    - .planning/sketches/013-contact-page-composition/index.html (the tooling pattern: one self-contained HTML file rendering multiple labeled variants for side-by-side comparison, plus its reduced-motion simulation toggle)
    - .planning/sketches/005-edition-hero-scroll-reveal/README.md (the pin+shrink hero mechanism D-04 adapts; and its reduced-motion fallback demonstration)
    - .planning/sketches/005-edition-hero-scroll-reveal/index.html (how the scroll-scrubbed pin+shrink is simulated inside a boxed sub-viewport)
    - .planning/phases/15-about-page-editorial-redesign/15-UI-SPEC.md (Layout & Composition Contract: Locked vs Open; Sketch Exploration Requirement; Typography 4-role table; Color 60/30/10; Component Reuse)
    - .planning/phases/15-about-page-editorial-redesign/15-CONTEXT.md (D-01..D-07 verbatim)
    - src/components/AboutPageBody.astro (current markup/copy structure + existing tokens/breakpoints to reproduce)
    - src/components/PageTitleHeader.astro (the exact eyebrow/giant-title/halftone/divider treatment to reproduce visually in the sketch, including the empty-intro rendering behavior)
    - src/pages/about.astro and src/pages/en/about.astro (the real French/English copy: "À propos"/"About", "Atelier & pratique"/"Studio & practice", "Médium & technique"/"Medium & technique", and the bio/practice/medium fallback strings)
  </read_first>
  <action>
    Create a single self-contained `index.html` at `.planning/sketches/014-about-page-composition/` presenting 2-3 clearly-labeled, visually distinct About-page layout variants for side-by-side review, following the sketch-013 tooling pattern (one file, labeled bordered frames, a "Simuler prefers-reduced-motion" toggle like sketch 005).

    Use REAL production copy throughout (never lorem ipsum): the actual "À propos" / "Atelier & pratique" / "Médium & technique" headings and the real bio/practice/medium paragraph text pulled from src/pages/about.astro's resolved values or its fallback strings, and reproduce the PageTitleHeader visual treatment (pulsing-dot eyebrow "Atelier Jacqueline Suzanne", giant display title at clamp(64px,9vw,140px), drifting dot-halftone, hairline divider).

    Each variant must resolve the two OPEN questions differently so the review is a real choice:
    - D-05 (hero-photo shrink reveal target): at least one variant demonstrates a pure motion "settle" (photo shrinks, no text revealed); at least one demonstrates a "reveal into section 01" (the shrink opens space that section 01 — "01 Atelier & pratique" — visually anchors into, using ONLY existing copy, no new text; if that variant uses the single permitted `--color-accent` pink underline on one format-style line, mirror DetailHero's `border-bottom: 2px solid var(--color-accent)`).
    - D-07 (numbered-sections structure): explore across variants at least two of {two-column restyle, stacked full-width row-list Éditions-style, other asymmetric structure}.

    Stay strictly inside the LOCKED constraints — sketching is for composition/rhythm only, do NOT reopen these: D-01 (biography is a standalone lead paragraph below the header using the `--editorial-page-lead-*` role; PageTitleHeader `intro` stays empty), D-02/D-03 (portrait stays circular at ~112px desktop / ~72px mobile, a small accent, never squared/framed, never scaled into a focal photo), D-04 (hero photo uses the vanilla-JS `position: sticky` pin+shrink mechanism, no new library), D-06 (mobile keeps a plain static photo band; desktop reduced-motion shows the settled end-state immediately). Consume only existing tokens (`--editorial-page-*`, `--border-hairline`, `--color-ink`, `--color-accent`, `--space-*`, `--font-display`) — introduce zero new font sizes/weights/colors; hold exactly 4 type roles (Body/Heading/Display/Lead) and 60/30/10 color.

    Explicitly surface the empty-`intro` watch-out for review: passing `intro=""` to PageTitleHeader renders an empty intro paragraph plus the ≥760px vertical hairline divider in column 2 with nothing beside it. Show in the sketch how each variant handles that right-column space (e.g. an orphaned floating divider vs. a page-scoped suppression vs. the portrait/first-section occupying that space) so the winner's decision is explicit and does NOT require editing the shared PageTitleHeader component.

    Make the sketch reviewable at (a) a normal desktop width, (b) a "short-but-wide" viewport class (mirroring sketch 013's Round 1 ~1241×570 regression scrutiny), and (c) mobile (~375px) — pay explicit attention to the portrait's mobile placement and the hero photo's mobile static-band fallback.
  </action>
  <verify>
    <automated>test -f .planning/sketches/014-about-page-composition/index.html && grep -c 'variant' .planning/sketches/014-about-page-composition/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/sketches/014-about-page-composition/index.html` exists and opens in a browser as one self-contained file (no external build step, matching sketch 013/005).
    - The file renders at least 2 (target 2-3) distinct, individually-labeled layout variants.
    - Real production copy is used: the strings "Atelier & pratique" and "Médium & technique" (or their EN equivalents) and multi-sentence bio/practice/medium paragraphs appear verbatim — no lorem ipsum / no placeholder gibberish.
    - Across the variants, D-05 is shown resolved at least two ways (pure settle AND reveal-into-section-01) and D-07 is shown resolved at least two ways (from {two-column, stacked row-list, other asymmetric}).
    - No new CSS color hex, no font size outside the 4 declared roles, and the portrait is circular at ~112/72px in every variant (locked-constraint adherence is visually verifiable in the mockup).
    - The empty-`intro` right-column behavior is visibly addressed in each variant.
  </acceptance_criteria>
  <done>A single HTML sketch presents multiple real-copy About-page variants that differ on D-05 and D-07 and stay within every locked constraint, ready for user review.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: User reviews variants and names the winning layout direction</name>
  <action>Present the 2-3 sketch variants to the user for review at desktop, short-but-wide, and mobile widths (with the reduced-motion toggle), gather any feedback rounds, and record the variant the user names as winner along with the resolved D-05 and D-07 choices. Block until the user responds — do not pick a winner autonomously.</action>
  <decision>Which About-page layout variant becomes the implemented direction — specifically resolving D-05 (hero reveal: pure settle vs. reveal-into-section-01) and D-07 (sections: two-column vs. stacked row-list vs. other asymmetric)?</decision>
  <context>
    ABOUT-04 and ROADMAP criterion 3 require the implemented layout to be chosen from multiple reviewed proposals, not a single guess. The winner chosen here is the direct build target for plans 15-02 (composition) and 15-03 (scroll-reveal motion). This mirrors the Contact redesign (sketch 013), which went through real feedback rounds before landing on A3.
    Open the sketch: `open .planning/sketches/014-about-page-composition/index.html`. Review at desktop, a short-but-wide window, and mobile widths, and toggle the reduced-motion simulation.
  </context>
  <options>
    <option id="pick-variant">
      <name>Name a winner as-is</name>
      <pros>Fastest path to implementation; one variant already reads as the intended editorial composition.</pros>
      <cons>None if a variant is clearly right.</cons>
    </option>
    <option id="iterate">
      <name>Request a feedback round, then name a winner</name>
      <pros>Matches sketch 013's proven multi-round refinement (title sizing, field craft, mobile tap targets) that caught real regressions before code.</pros>
      <cons>One or more extra sketch-revision cycles before the winner is locked.</cons>
    </option>
  </options>
  <resume-signal>Reply with the winning variant label plus the resolved D-05 and D-07 choices (e.g. "Variant B: reveal-into-section-01 + stacked row-list"), or describe changes for another round.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the named winner and update the sketch manifest</name>
  <files>.planning/sketches/014-about-page-composition/README.md, .planning/sketches/MANIFEST.md</files>
  <read_first>
    - .planning/sketches/013-contact-page-composition/README.md (exact README frontmatter + Variants + "Why {winner}" section format to mirror)
    - .planning/sketches/005-edition-hero-scroll-reveal/README.md (second format reference)
    - .planning/sketches/MANIFEST.md (the table to append to; confirm 014 is the next free number after 013)
  </read_first>
  <action>
    Write `.planning/sketches/014-about-page-composition/README.md` in the same shape as sketch 005/013: YAML frontmatter with `sketch: 014`, `name: about-page-composition`, a one-line `question:` capturing the D-05/D-07 exploration, `winner:` set to the label the user named in Task 2, and `tags: [layout, about, editorial, typography, motion]`. Body sections: `## Design Question`, `## How to View` (`open .planning/sketches/014-about-page-composition/index.html`), `## Variants` (one bullet per variant with its D-05/D-07 resolution), and `## Why {winner}` (the one-line rationale plus any feedback-round notes from Task 2, matching sketch 013's "how it got there" narrative if rounds occurred). Record the resolved D-05 target and D-07 structure explicitly, since 15-02/15-03 read this to know exactly what to build.

    Append one row to the `## Sketches` table in `.planning/sketches/MANIFEST.md`: `| 014 | about-page-composition | {question} | {winner} | [layout, about, editorial, typography, motion] |`.
  </action>
  <verify>
    <automated>grep -q '^winner:' .planning/sketches/014-about-page-composition/README.md && grep -q '^sketch: 014' .planning/sketches/014-about-page-composition/README.md && grep -q '| 014 |' .planning/sketches/MANIFEST.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README.md frontmatter contains `sketch: 014`, `name: about-page-composition`, a non-empty `winner:` naming the variant the user chose, and `question:`/`tags:`.
    - README.md body contains `## Variants` (one entry per variant, each stating its D-05 and D-07 resolution) and a `## Why {winner}` section with a one-line rationale.
    - The resolved D-05 reveal target and D-07 sections structure are stated in plain language downstream plans can act on.
    - MANIFEST.md `## Sketches` table has exactly one new row for `014` and the table stays well-formed.
  </acceptance_criteria>
  <done>The winner is recorded in the canonical sketch README format and registered in the manifest; 15-02/15-03 have an unambiguous build target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan produces throwaway design HTML under `.planning/sketches/`. It is never imported into the site build, never deployed, and receives no runtime or user input. No trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01-SKETCH | Tampering | `.planning/sketches/014-*/index.html` | low | accept | Sketch is a local, non-deployed planning artifact excluded from `src/` and the Astro build; no production surface to tamper with. Documented as trivial, not mitigated. |
| T-15-SC | Supply chain (npm/pip/cargo) | — | n/a | accept | No package installs occur in this plan (self-contained static HTML, no dependencies). No legitimacy gate applies. |

ASVS L1, block-on-high: no high-severity threats — nothing blocks. Risk profile is trivial by design (planning artifact only).
</threat_model>

<verification>
- `.planning/sketches/014-about-page-composition/index.html` and `README.md` exist; README frontmatter carries a named `winner`.
- The sketch presents multiple real-copy variants differing on D-05 and D-07, within every locked constraint (D-01/02/03/04/06, 4 type roles, 60/30/10 color, circular ~112/72px portrait).
- MANIFEST.md has the appended `014` row.
- The user has confirmed a winner (Task 2 checkpoint resolved).
</verification>

<success_criteria>
The implemented layout direction for the About page is chosen from multiple sketched, human-reviewed proposals (ROADMAP criterion 3 / ABOUT-04 process contract), with D-05 and D-07 resolved and recorded, giving 15-02/15-03 a concrete build target.
</success_criteria>

<output>
Create `.planning/phases/15-about-page-editorial-redesign/15-01-SUMMARY.md` when done, recording the named winner, the resolved D-05 target and D-07 structure, and any feedback-round notes, so the implementation plans build exactly what was approved.
</output>
