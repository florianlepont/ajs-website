---
phase: 15-about-page-editorial-redesign
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/components/AboutPageBody.astro
  - src/pages/about.astro
  - src/pages/en/about.astro
  - tests/e2e/about.spec.ts
autonomous: true
requirements: [ABOUT-03, ABOUT-04]
tags: [about, layout, editorial, page-title-header, typography]

must_haves:
  truths:
    - "Visiting /about/ and /en/about/ shows the giant PageTitleHeader title (same display font, clamp(64-140px) size, halftone texture, hairline divider, pulsing-dot eyebrow) as Contact and Éditions — not the old small in-body h1."
    - "Below the title, the biography reads as a standalone lead paragraph, and the supporting composition (circular portrait accent, hero photo band, the two numbered sections) matches the sketch-014 winner's structure."
    - "Every word of the existing bio, practice, and medium copy is still present, in both French and English."
  artifacts:
    - "src/components/AboutPageBody.astro (redesigned markup + scoped styles; PageTitleHeader wired, old eyebrow/h1 removed)"
    - "src/pages/about.astro and src/pages/en/about.astro (props kept in sync if the Props interface changes)"
    - "tests/e2e/about.spec.ts (assertions re-pointed to the new PageTitleHeader-based structure)"
  key_links:
    - "AboutPageBody imports and calls PageTitleHeader with heading + headingId='about-title' + intro='' (D-01) — the shared component is reused unmodified."
    - "about.astro/en/about.astro are the only two call sites — if the Props interface gains a hook for the sketch winner, BOTH must be updated identically."
    - "The former 14px section-number index (.about-page__number) folds into the Body 16px role, keeping weight-600/uppercase/0.08em — the composition holds exactly 4 type roles."
---

<objective>
Deliver the About page's title swap and static editorial composition per the sketch-014 winner. This is the interface-first half of the redesign: get the composition right (title, biography lead, portrait placement, numbered sections, and the hero photo rendered as a static band baseline) before layering scroll motion in plan 15-03.

Covers ABOUT-03 in full (title now renders via the shared `PageTitleHeader`) and the static layer of ABOUT-04 (the coherent editorial composition), implementing the D-01/D-02/D-03/D-07 decisions and the D-05 reveal-target's static markup, exactly as resolved by the 15-01 sketch winner.

Purpose: an About page whose title and resting layout already read as one coherent Contact/Éditions-aligned editorial composition, with all bio/practice/medium copy preserved verbatim.
Output: redesigned `AboutPageBody.astro`, synced call sites, and updated e2e coverage.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/15-about-page-editorial-redesign/15-CONTEXT.md
@.planning/phases/15-about-page-editorial-redesign/15-UI-SPEC.md
@.planning/phases/15-about-page-editorial-redesign/15-PATTERNS.md
@.planning/phases/15-about-page-editorial-redesign/15-01-SUMMARY.md
@.planning/sketches/014-about-page-composition/README.md
@src/components/AboutPageBody.astro
@src/components/PageTitleHeader.astro
@src/components/ContactPageBody.astro
@src/pages/about.astro
@src/pages/en/about.astro
@tests/e2e/about.spec.ts
</context>

## Artifacts this phase produces (this plan)

- Modified `src/components/AboutPageBody.astro`:
  - New: an `import PageTitleHeader from './PageTitleHeader.astro';` and a `<PageTitleHeader heading={heading} headingId="about-title" intro="" />` call.
  - Removed: the `.about-page__eyebrow` element + its markup, the shared eyebrow/number label-size CSS rule, and the now-dead `.about-page h1` rule.
  - Changed: `.about-page__number` restyled to the Body 16px role (weight 600, uppercase, 0.08em) — no longer 14px.
  - Changed: composition markup/scoped CSS restructured per the sketch-014 winner (biography as standalone lead paragraph, circular portrait placement, numbered-sections structure per D-07, hero photo as a static band baseline).
  - Possibly new: one structural prop on the `Props` interface if the winner's D-05 reveal needs a hook (a fixed flag/derived value, never new translated copy).
- Modified `src/pages/about.astro` and `src/pages/en/about.astro` (only if the Props interface changes).
- Modified `tests/e2e/about.spec.ts` (assertions re-pointed to the new structure).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire in PageTitleHeader and retire the old in-body title/eyebrow (ABOUT-03, D-01)</name>
  <files>src/components/AboutPageBody.astro</files>
  <read_first>
    - src/components/AboutPageBody.astro (current hero grid, eyebrow, h1, lead, number-index markup + styles being replaced)
    - src/components/PageTitleHeader.astro (Props interface line 16-21: `intro` is a required string — pass "" not undefined; the eyebrow default "Atelier Jacqueline Suzanne" matches About's current eyebrow; note the empty-intro renders an empty `<p>` + the ≥760px divider in column 2)
    - src/components/ContactPageBody.astro (lines 12-13, 44-45: the exact import + call pattern; line 112: its `.contact-page { --color-accent: var(--color-ink); }` override — do NOT copy this, About keeps the default pink accent)
    - .planning/phases/15-about-page-editorial-redesign/15-UI-SPEC.md (Typography table: Display role inherited from PageTitleHeader; section-number folds into Body role; Component Reuse contract)
    - .planning/sketches/014-about-page-composition/README.md (the winner's decision on the empty-intro right-column space)
  </read_first>
  <behavior>
    - /about/ and /en/about/ each render exactly one `<h1>` inside `<main>`, owned by PageTitleHeader (`h1#about-title`), text "À propos" / "About".
    - The eyebrow text "Atelier Jacqueline Suzanne" renders inside PageTitleHeader's element (`.page-title-header__eyebrow`), not a local `.about-page__eyebrow`.
    - The biography renders as a standalone lead paragraph below the header (D-01), using the `--editorial-page-lead-*` role; PageTitleHeader's own `intro` is empty.
    - `.about-page` does NOT override `--color-accent` to ink (unlike Contact) — the pink accent stays available for the eyebrow dot.
  </behavior>
  <action>
    In `AboutPageBody.astro`, import `PageTitleHeader` and render it as the first child of `<article class="about-page">`, called as `<PageTitleHeader heading={heading} headingId="about-title" intro="" />` — reused unmodified per the Component Reuse contract, with `intro` an empty string per D-01 (its Props type requires a string).

    Remove the old title/eyebrow scaffolding that PageTitleHeader now owns: delete the `.about-page__eyebrow` element and the `.about-page__identity`/`.about-page__hero` header wrapper markup, delete the shared eyebrow/number label-size CSS rule (the current rule that sets those elements to `--text-label-size`/`--text-label-leading`), and delete the now-dead `.about-page h1` rule (its `--editorial-page-title-size` clamp is superseded by PageTitleHeader's own clamp(64px,9vw,140px)). Do not leave two competing title styles.

    Reparent the biography into a standalone lead paragraph below the header (keep the `.about-page__lead` hook so existing lead-copy coverage still resolves), styled with the `--editorial-page-lead-size`/`--editorial-page-lead-leading` role (D-01).

    Restyle the decorative section-number index (`.about-page__number`, `aria-hidden="true"`) to the Body role — `font-size: var(--text-body-size)` (16px) — while keeping its `font-weight: var(--weight-semibold)`, `text-transform: uppercase`, and `letter-spacing: 0.08em` so it stays visually distinct from regular-weight paragraph copy. Do not reference `--text-label-size`/`--text-label-leading` anywhere in `.about-page` after this change (they remain in use only inside the untouched PageTitleHeader).

    Handle the empty-`intro` right-column exactly as the sketch-014 winner resolved it (e.g. leave the divider, suppress it via `.about-page`-scoped CSS with a `:global()` selector, or let the portrait/section occupy that space) — WITHOUT editing `PageTitleHeader.astro` itself (its 760px internal breakpoint and its markup are out of scope to change).

    Do NOT alter any bio/practice/medium copy or add any new copy — presentation only.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `AboutPageBody.astro` contains an `import PageTitleHeader` and a `<PageTitleHeader ... headingId="about-title" intro="" />` call.
    - No `--text-label-size` or `--text-label-leading` reference remains inside `AboutPageBody.astro`'s style block; the section-number index now uses `var(--text-body-size)` with weight 600 / uppercase / 0.08em.
    - No `.about-page h1 { ... font-size: var(--editorial-page-title-size) ... }` rule remains (the local title rule is gone).
    - `.about-page` does not set `--color-accent: var(--color-ink)`.
    - `npm run typecheck` (astro check) passes with 0 errors.
    - No bio/practice/medium string is edited or removed.
  </acceptance_criteria>
  <done>The giant title and eyebrow render via the shared PageTitleHeader; the old local title/eyebrow/label styling is fully retired; the biography is a standalone lead paragraph; type roles hold at 4.</done>
</task>

<task type="auto">
  <name>Task 2: Build the static editorial composition per the sketch-014 winner (ABOUT-04, D-02/D-03/D-07, D-05 static markup)</name>
  <files>src/components/AboutPageBody.astro, src/pages/about.astro, src/pages/en/about.astro</files>
  <read_first>
    - .planning/sketches/014-about-page-composition/README.md (the winner's D-05 reveal target and D-07 sections structure — the exact composition to build)
    - .planning/phases/15-about-page-editorial-redesign/15-01-SUMMARY.md (resolved winner notes)
    - src/components/AboutPageBody.astro (current portrait/exhibition/sections markup + the 767px/480px breakpoints to preserve/extend)
    - .planning/phases/15-about-page-editorial-redesign/15-PATTERNS.md (row-list analog EditionsOverviewBody lines 44-58 IF D-07 = stacked; the current two-column pattern lines 165-178 IF D-07 = two-column restyle; note: About has 2 hardcoded sections, do NOT introduce a data-array/map)
    - .planning/phases/15-about-page-editorial-redesign/15-UI-SPEC.md (Spacing scale, Color 60/30/10, portrait exception 112/72px, breakpoints — no new tokens)
    - src/pages/about.astro and src/pages/en/about.astro (the AboutPageBody call sites and current Props)
  </read_first>
  <action>
    Restructure `AboutPageBody.astro`'s markup and scoped CSS to match the sketch-014 winner exactly, in this reading order below the header: standalone biography lead paragraph (from Task 1) → circular portrait accent → hero exhibition photo → the two numbered sections. Consume only existing tokens (`--editorial-page-max`, `--editorial-page-padding-*`, `--editorial-page-column-gap`, `--border-hairline`, `--color-ink`, `--color-accent`, `--space-*`, `--editorial-page-section-title-size`/`-leading`, `--text-body-size`) — introduce zero new ad hoc values.

    Portrait (D-02/D-03): keep `.about-page__portrait` circular (`border-radius: 50%`, `--border-hairline` ink border), at 112px desktop / 72px mobile, `object-fit: cover`. Place it per the winner (placement is the one open portrait sub-question) — it must stay a small accent that does not compete with the giant title, never squared/framed, never scaled up.

    Numbered sections (D-07): implement the winner's structure — a restyled two-column grid OR a stacked full-width row-list (Éditions-rhythm: full-width rows, leading index number, hairline top border) OR the winner's asymmetric structure. Keep the two sections as two hardcoded `<section>` blocks (studio/practice and medium/technique) — do NOT convert to a mapped data array. Preserve the `<h2>` (`--editorial-page-section-title-size`) and the Body-role paragraph text.

    Hero exhibition photo — STATIC BASELINE ONLY in this plan: render `.about-page__exhibition` as today's plain static full-bleed band (the settled end-state), keeping the `.about-page__exhibition` outer class as the stable hook. Do NOT add the sticky pin / scroll driver here — that is plan 15-03. Structure the markup so 15-03 can layer the pin+shrink on top without re-restructuring (i.e. if the winner's D-05 reveal target introduces a reveal panel using existing section-01 copy, include that markup now in its settled/visible state; 15-03 will add the JS-driven opacity/transform and the `<noscript>`/reduced-motion gating).

    If the winner's D-05 reveal needs a structural hook on the component (e.g. a boolean/label flag — never new translated copy), add it to the `Props` interface and thread it identically through BOTH `src/pages/about.astro` and `src/pages/en/about.astro`, computing it inline from existing resolved strings. If no new prop is needed, leave the call sites untouched.

    Preserve/extend the existing 767px and 480px breakpoints rather than inventing a new scheme; do not reconcile PageTitleHeader's own internal 760px breakpoint (out of scope).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The composition below the header follows the sketch-014 winner's D-07 structure and D-05 static markup, using only existing tokens (no new hex/size/spacing literals introduced in `.about-page` CSS).
    - `.about-page__portrait` is circular, `object-fit: cover`, 112px desktop / 72px mobile, retains the hairline ink border.
    - The two numbered sections remain two hardcoded `<section>` blocks (no data-array/`.map()` introduced); both `<h2>` headings and both body paragraphs render.
    - `.about-page__exhibition` renders as a static band (no sticky/scroll JS in this plan).
    - If a new `Props` field was added, it appears identically in `AboutPageBody.astro`, `about.astro`, and `en/about.astro`; if not, the two call sites are unchanged.
    - `npm run typecheck` passes with 0 errors.
  </acceptance_criteria>
  <done>The About page's resting composition matches the approved sketch winner, portrait stays a small circular accent, sections follow the winner's structure, and the hero photo is a static band ready for 15-03 to animate.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point the About e2e suite at the new PageTitleHeader-based structure</name>
  <files>tests/e2e/about.spec.ts</files>
  <read_first>
    - tests/e2e/about.spec.ts (CURRENT assertions that WILL break: the `About uses the shared editorial type scale and page frame` test reads `.about-page__eyebrow` and `.about-page h1` — both removed in Task 1; the `the portrait belongs to the intro while the exhibition image spans the editorial frame` test reads `.about-page__hero` — removed in Task 2)
    - src/components/AboutPageBody.astro (the new selectors after Tasks 1-2)
    - src/components/PageTitleHeader.astro (new title selector `.page-title-header h1` / `h1#about-title`, eyebrow `.page-title-header__eyebrow`)
    - playwright.config.ts (projects: `chromium` + `webkit-mobile`)
  </read_first>
  <action>
    Update `tests/e2e/about.spec.ts` so its assertions target the redesigned structure — re-point coverage, do NOT delete or weaken it. Specifically:
    - In the type-scale/hierarchy test, read the title from `h1#about-title` (or `.page-title-header h1`) and the eyebrow from `.page-title-header__eyebrow` instead of the removed `.about-page__eyebrow` / `.about-page h1`. Keep the hierarchy invariant (display title font-size > section-title font-size > lead font-size) and the uppercase-eyebrow assertion (now on the PageTitleHeader eyebrow).
    - In the portrait/exhibition layout test, replace the removed `.about-page__hero` container reference with the new composition container introduced in Task 2 (or `.about-page`), keep the portrait circular/≤112px desktop and ≤72px mobile assertions, and assert the exhibition photo's resting/static state (this plan ships the static band; 15-03 adds the motion-specific assertions).
    - Keep the existing content-preservation tests intact: both locales render non-empty bio + "Atelier & pratique"/"Studio & practice" + "Médium & technique"/"Medium & technique" headings, the FR/EN copy differs, and `main h1` contains "À propos"/"About".
    - Add an assertion that the old `.about-page__eyebrow` selector now resolves to 0 elements and the eyebrow text is instead inside `.page-title-header__eyebrow`, proving ABOUT-03 wiring.
    - Add an assertion that PageTitleHeader's structural siblings render on About (the halftone `.page-title-header__halftone` element and the `.page-title-header__divider` element exist), proving the same title identity as Contact/Éditions.
  </action>
  <verify>
    <automated>npm run test:e2e -- about.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/about.spec.ts` no longer references the removed `.about-page__eyebrow` or `.about-page h1` as existing elements (except the count-0 negative-presence check for the eyebrow).
    - New assertions confirm: `h1#about-title` renders in `<main>` on both locales; `.page-title-header__eyebrow` carries the eyebrow; `.page-title-header__halftone` and `.page-title-header__divider` exist; `.about-page__eyebrow` count is 0.
    - Content-preservation and FR≠EN tests still pass.
    - `npm run test:e2e -- about.spec.ts` passes for both the `chromium` and `webkit-mobile` projects. (If the executor's worktree lacks the Sanity build env and cannot build/serve, do NOT weaken the tests — flag the environmental gap in the SUMMARY per the project's established independent-verification pattern.)
  </acceptance_criteria>
  <done>The About e2e suite validates the new PageTitleHeader-based structure and content preservation, and passes on both Playwright projects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Content Lake → build-time render | Bio/practice/medium/portrait/exhibition data is fetched at BUILD time (published perspective only) and rendered to static HTML. This boundary is unchanged by this plan — the same data reaches the same page; only its presentation changes. No runtime input, no user-supplied data, no auth, no endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02-INJ | Tampering (content injection) | `AboutPageBody.astro` render of CMS strings | low | mitigate | Continue rendering bio/practice/medium/alt strings through Astro's default `{expr}` interpolation (auto-escaped), exactly as the current component does. The redesign introduces no raw-HTML render directive for CMS copy. Verified by the content-preservation e2e assertions reading rendered textContent. |
| T-15-02-REG | Tampering (dependency) | build inputs | low | accept | No new npm/component-library/icon/font dependency is added (UI-SPEC forbids it); pure hand-authored Astro/CSS. Nothing to vet. |
| T-15-SC | Supply chain (npm/pip/cargo) | — | n/a | accept | No package installs in this plan. No legitimacy gate applies. |

ASVS L1, block-on-high: no high-severity threats — nothing blocks. This is a read-only editorial page; the risk profile is trivial and documented rather than invented.
</threat_model>

<verification>
- `npm run typecheck` passes (0 errors).
- `npm run test:e2e -- about.spec.ts` passes on `chromium` and `webkit-mobile`.
- Manual/asserted: /about/ and /en/about/ render the PageTitleHeader giant title + halftone + divider; biography is a standalone lead; portrait is a small circular accent; sections follow the winner's structure; all bio/practice/medium copy present verbatim in both locales.
</verification>

<success_criteria>
About's title renders via the shared `PageTitleHeader` (ABOUT-03, ROADMAP criterion 1), the resting supporting layout reads as the approved coherent editorial composition (ABOUT-04 static layer, criterion 2), existing copy is preserved verbatim (criterion 4), and both locales render correctly (criterion 5). Scroll motion is layered next in 15-03.
</success_criteria>

<output>
Create `.planning/phases/15-about-page-editorial-redesign/15-02-SUMMARY.md` when done, noting the final composition structure, any new `Props` field added (and that both call sites were synced), the empty-intro resolution used, and any e2e/env caveats for 15-03.
</output>
