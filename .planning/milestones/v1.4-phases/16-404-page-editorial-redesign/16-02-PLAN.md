---
phase: 16-404-page-editorial-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/404.astro
  - tests/e2e/not-found.spec.ts
  - tests/e2e/accessibility.spec.ts
autonomous: true
requirements: [ERR-01]
tags: [404, error-page, astro, css, scrim, bilingual, accessibility, sanity]

must_haves:
  truths:
    - "The rebuilt 404 page renders a full-bleed background stack of real published gallery photos (D-04), with only the first photo visible by default via opacity/z-index — never display:none (D-03, RESEARCH Pitfall 2) — so the page is a complete, correct, no-JS static fallback on its own."
    - "Centered over a radial dimming scrim (D-07): the AJS logo (white variant, much larger than the header's 56px — D-06), a small '404' marker, the bilingual 'Page introuvable / Not found' phrase, and the 'Retourner à l'accueil' / 'Return home' links laid out side by side (D-06)."
    - "The page still serves HTTP 404 with noindex, shows BOTH French and English content on every load (no per-locale routing), and both home links resolve through getRelativeLocaleUrl so they stay base-aware (CR-01) — verify-static-artifact.mjs still finds href=\"/\" and href=\"/en/\" in the built 404.html."
    - "Every pool <img> is decorative (alt=\"\") and the pool container is aria-hidden=\"true\" (RESEARCH Pitfall 6); the accessible content tree is exactly the marker, the phrase, and the two links — automated axe scan is clean of serious/critical violations."
  artifacts:
    - "src/pages/404.astro (REWRITE — static half): frontmatter photo-pool sourcing + logo assetBase + home links; markup (pool <img> stack, scrim, centered content); full scoped <style> per 16-UI-SPEC.md. No client <script> yet (added in 16-03)."
    - "tests/e2e/not-found.spec.ts (MODIFIED): static assertions realigned to the new markup/copy; HTTP status + robots + href assertions preserved."
    - "tests/e2e/accessibility.spec.ts (MODIFIED): a 404 (nonexistent-path) entry added to the axe scan."
  key_links:
    - "Both home-return links render as plain literal <a href={frHome}>/<a href={enHome}> computed once via getRelativeLocaleUrl('fr','')/('en','') — never a JS-computed href (RESEARCH Pitfall 5 keeps verify-static-artifact.mjs green)."
    - "Logo <img> src uses a locally-computed assetBase from import.meta.env.BASE_URL (the BaseLayout.astro pattern) so it resolves under GitHub Pages' /ajs-website/ base (CR-01 class)."
    - "Markup exposes the exact class/attribute contract the 16-03 client engine will query: container .not-found, background images .pop-photo with the first also carrying .is-active."
---

<objective>
Rewrite `src/pages/404.astro`'s body into the new full-bleed, bilingual, editorial 404 composition — the static half of the redesign: build-time photo-pool sourcing, the stacked background `<img>` markup, the radial dimming scrim, and the centered content block (logo, "404" marker, bilingual phrase, side-by-side home links), all styled per `16-UI-SPEC.md`. This is the pivoted fully-custom concept (D-01) that supersedes the original `PageTitleHeader`-reuse framing still present in stale ROADMAP/REQUIREMENTS text — `PageTitleHeader` is deliberately NOT used here. It is treated with real interaction/accessibility design rigor (D-02), not as a mechanical component reuse. This wave ships a complete, correct page WITHOUT the pop-rate engine: the first pool photo is visible by default, which is exactly the intended no-JS fallback (RESEARCH Pattern 2). The interactive pop-rate `<script>` is added on top in plan 16-03. This plan also realigns the two e2e specs to the new markup so the CI gates stay green the moment the new page ships (RESEARCH Pitfall 4).

Purpose: Deliver the visible, accessible, base-safe, bilingual 404 page and lock the DOM class/attribute contract the client engine depends on — without coupling the static composition to the interaction engine.
Output: Rewritten static `src/pages/404.astro`; updated `not-found.spec.ts` + `accessibility.spec.ts`; build + artifact + e2e gates green.
</objective>

<execution_context>
@/Users/florian/Projects/ajs-website/.claude/gsd-core/workflows/execute-plan.md
@/Users/florian/Projects/ajs-website/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-404-page-editorial-redesign/16-CONTEXT.md
@.planning/phases/16-404-page-editorial-redesign/16-RESEARCH.md
@.planning/phases/16-404-page-editorial-redesign/16-PATTERNS.md
@.planning/phases/16-404-page-editorial-redesign/16-UI-SPEC.md
@.planning/phases/16-404-page-editorial-redesign/16-VALIDATION.md
@src/pages/404.astro
@src/pages/index.astro
@src/components/DetailHero.astro
@src/layouts/BaseLayout.astro
</context>

## Artifacts this phase produces

Phase 16 as a whole produces the following new/changed symbols (this plan owns the static `404.astro` rewrite + both e2e spec updates):

- `src/lib/pop-rate.ts` (NEW — plan 16-01).
- `tests/unit/pop-rate.test.ts` (NEW — plan 16-01).
- `src/pages/404.astro` (REWRITE — this plan writes the static shell + CSS; plan 16-03 appends the client `<script>`) — CSS classes `.not-found`, `.not-found__scrim`, `.not-found__content`, `.not-found__logo`, `.not-found__marker`, `.not-found__phrase`, `.not-found__links`, `.pop-photo`, `.pop-photo.is-active`.
- `tests/e2e/not-found.spec.ts` (MODIFIED — this plan realigns static assertions; plan 16-03 adds the reduced-motion case).
- `tests/e2e/accessibility.spec.ts` (MODIFIED — this plan adds the 404 entry).

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the static 404 page (frontmatter, markup, scoped CSS)</name>
  <files>src/pages/404.astro</files>
  <read_first>
    - src/pages/404.astro — the current implementation; note the existing frontmatter (`getRelativeLocaleUrl` import, `frHome`/`enHome` computation) and `<BaseLayout title="Page introuvable / Page not found" noIndex>` usage that MUST be preserved.
    - src/pages/index.astro (lines 8-56) — the exact build-time gallery-cover-photo sourcing pattern to adapt (getGalleries → filter images.length > 0 → pickHeroIndex → fullSizeUrl/responsiveImageSrcSet).
    - src/lib/image.ts — signatures of `fullSizeUrl(img, maxWidth)` and `responsiveImageSrcSet(img, widths)`.
    - src/lib/image-orientation.ts — `pickHeroIndex(images)` contract (returns 0 for empty/undefined; never throws).
    - src/lib/sanity.ts — `getGalleries()` returns `Gallery[]` (already `?? []`); `Gallery.images: GalleryImage[]`.
    - src/layouts/BaseLayout.astro (lines 33-64, 142-144, 198-219) — the `assetBase`/`logoWhiteSrc` three-line pattern to copy, and the `headerVariant?: 'solid'|'transparent'|'none'` (default 'solid') + `hideFooter?: boolean` (default false) props.
    - src/components/DetailHero.astro (lines 489-531) — the `.detail-hero__scrim`/`.detail-hero__overlay-title` scrim-over-photo technique and white-on-scrim text convention to ADAPT (radial, not the bottom-anchored gradient — RESEARCH Anti-Patterns).
    - .planning/phases/16-404-page-editorial-redesign/16-UI-SPEC.md — the binding spacing/typography/color/copy contract for this exact page (logo clamp(120px,14vw,160px), Display phrase clamp(28px,5vw,40px), radial scrim rgba(26,26,26,0.72→0.55→0.35), accent underline on the marker, tap-target-min on links).
    - .planning/phases/16-404-page-editorial-redesign/16-RESEARCH.md — Pattern 1 (pool sourcing), Pattern 2 (opacity/z-index hard-cut, never display:none), Pitfall 2/5/6, Security Domain (pool-size cap + fetchpriority).
  </read_first>
  <action>
    Rewrite the body of `src/pages/404.astro`. KEEP the file's meta wiring exactly: the `getRelativeLocaleUrl` import, `const frHome = getRelativeLocaleUrl('fr', '')` / `const enHome = getRelativeLocaleUrl('en', '')`, and rendering both as plain literal `<a href={frHome}>` / `<a href={enHome}>` anchors (never a JS-computed/conditional href — RESEARCH Pitfall 5). KEEP `noIndex`; KEEP the `<title>` value (`"Page introuvable / Page not found"`).

    BaseLayout usage — pass `headerVariant="none"` and `hideFooter` in addition to `title` and `noIndex`, so the immersive full-bleed composition owns the whole viewport. Rationale (Claude's Discretion — no locked decision governs chrome visibility; CONTEXT.md only mandates keeping BaseLayout + noIndex + title): the site's solid white header bar and legal footer would undercut the "high-impact, distinctive, different personality" intent (CONTEXT.md `<specifics>`), and the two in-composition home links ARE the escape route (D-06), so no nav is lost. Document this choice inline.

    Frontmatter — add build-time photo-pool sourcing adapted from `index.astro`:
      - Import `getGalleries` from `../lib/sanity`, `fullSizeUrl`/`responsiveImageSrcSet` from `../lib/image`, `pickHeroIndex` from `../lib/image-orientation`.
      - `const pool = (await getGalleries()).filter((g) => g.images.length > 0).map(...)` — deliberately NO `showOnHomePage` filter (RESEARCH A3: the 404 pool wants maximum variety). For each gallery, pick `const cover = gallery.images[pickHeroIndex(gallery.images)]` and produce `{ src: fullSizeUrl(cover, 1920), srcset: responsiveImageSrcSet(cover) }`. Do NOT read per-image alt (pool photos are decorative here — Pitfall 6).
      - Bound the pool for client resource-exhaustion safety (Security Domain / threat T-16-01): cap it to at most 16 entries (e.g. `.slice(0, 16)`). If the current gallery set yields fewer than ~6 photos, you MAY enlarge variety by including a second landscape photo per gallery, still bounded to ≤16 total (Claude's Discretion per CONTEXT.md pool sizing).
      - Compute the logo path locally exactly as BaseLayout does: `const assetBase = (import.meta.env.BASE_URL ?? '/').replace(/\/$/, '')` then `const logoWhiteSrc = ${assetBase}/logos/AJS_Brutalist_White_Transparent.png` (default to the white variant unconditionally per UI-SPEC; the scrim guarantees contrast, so no per-photo color analysis and no `logoBlackSrc`).

    Markup inside the BaseLayout slot:
      - A full-viewport container `<div class="not-found">` (this is the pointer target the 16-03 engine will attach to).
      - A background pool layer: a `<div class="not-found__pool" aria-hidden="true">` wrapping the stacked `<img class="pop-photo">` elements. Render the pool via `pool.map((photo, i) => ...)`. Every `<img>` gets `alt=""`, `loading="eager"`, and for `i > 0` also `fetchpriority="low"` (Security threat T-16-01: bound preload competition). The FIRST image (`i === 0`) additionally gets class `pop-photo is-active` directly in the static markup (RESEARCH Pattern 2 — this default-visible first photo IS the no-JS fallback). Use `src={photo.src}` and `srcset={photo.srcset}` with a `sizes="100vw"`.
      - Empty-pool edge (UI-SPEC): if `pool.length === 0` render no pool layer at all (the `.not-found` container's flat `--color-ink` background stands in) — the centered content must render identically regardless. Guard with a conditional around the pool layer so a build against an empty Sanity dataset never crashes.
      - The scrim: `<div class="not-found__scrim" aria-hidden="true"></div>` layered above the pool.
      - The centered content `<div class="not-found__content">` containing, in reading order (UI-SPEC): the logo `<img class="not-found__logo" src={logoWhiteSrc} alt="" width=... height=... />` (decorative — the phrase conveys the meaning, so `alt=""` per UI-SPEC a11y note); the `<p class="not-found__marker">404</p>` marker; the bilingual phrase as a heading element `<h1 class="not-found__phrase">` containing the two short lines "Page introuvable" and "Not found" — condensed from today's two-sentence-per-language paragraphs (D-12: meaning preserved in both languages, exact verbatim wording not required; the compact marker + phrase + links together carry "this page doesn't exist, here's how home") — (two lines, both at the same Display size — UI-SPEC Typography; use a `<span>`/`<br>` split so both strings are present as visible text); and a `<p class="not-found__links">` (or `<nav>`) with the two literal anchors side by side separated by a middot `·`, each anchor padded to meet `--tap-target-min` (44px) hit height on mobile.

    Scoped `<style>` (all values from 16-UI-SPEC.md; reuse the site `:root` tokens, introduce no new tokens):
      - `.not-found`: `position: fixed; inset: 0;` full viewport, `overflow: hidden`, `background: var(--color-ink)` (prevents white flash before first photo paints), `display: grid; place-items: center`. Set `touch-action: none` on this container (RESEARCH Pattern 4 — the page has no scroll; keep pointer/touch gestures from being hijacked). Also set `overflow: hidden` on `<body>` for this page as the iOS-Safari belt-and-braces measure (RESEARCH notes partial touch-action support) — do this via a scoped rule or a `<style is:global>` targeting body only on this page.
      - `.not-found__pool`: `position: absolute; inset: 0; z-index: 0;`.
      - `.pop-photo`: `position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; opacity: 0; z-index: 0;`. Declare NO `transition` property on this rule (its absence IS the D-05 hard-cut mechanic — do not add an opacity transition to "smooth" it).
      - `.pop-photo.is-active`: `opacity: 1; z-index: 1;`.
      - `.not-found__scrim`: `position: absolute; inset: 0; z-index: 2; pointer-events: none; background: radial-gradient(ellipse at center, rgba(26,26,26,0.72) 0%, rgba(26,26,26,0.55) 45%, rgba(26,26,26,0.35) 100%)` (UI-SPEC Color — radial/center-weighted, NOT DetailHero's bottom-anchored gradient).
      - `.not-found__content`: `position: relative; z-index: 3; display: flex; flex-direction: column; align-items: center; text-align: center; gap: var(--space-sm); padding: var(--space-lg) var(--space-md); color: #FFFFFF;` with a safe edge margin (`--space-xl`) on small screens.
      - `.not-found__logo`: `width: clamp(120px, 14vw, 160px); height: auto;` (UI-SPEC exception — much larger than the header's 56px, D-06).
      - `.not-found__marker`: Label role — `font-family: var(--font-display); font-size: var(--text-label-size); font-weight: 600; text-transform: uppercase; letter-spacing: 0.07em; line-height: var(--text-label-leading);` with an accent underline `border-bottom: 2px solid var(--color-accent)` sized to the text (mirrors `.detail-hero__format`).
      - `.not-found__phrase`: Display role — `font-family: var(--font-display); font-size: clamp(28px, 5vw, 40px); font-weight: var(--weight-semibold); line-height: 1.2; margin: 0;` both lines same size.
      - `.not-found__links`: `display: flex; gap: var(--space-lg); align-items: center; justify-content: center;` — side by side (D-06). Each `<a>`: Label role uppercase letter-spaced, `color: #FFFFFF; text-decoration: none; min-height: 44px; display: inline-flex; align-items: center;` and on `:hover`/`:focus-visible` apply `color: var(--color-accent)` (UI-SPEC accent reservation). Keep a visible focus outline.

    Do not add any `<script>` in this plan (that is 16-03). Do not use `display:none` anywhere in the pool-photo swap CSS (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && EXPECTED_BASE=/ npm run test:artifact</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; `EXPECTED_BASE=/ npm run test:artifact` passes, i.e. the built `dist/404.html` still contains `href="/"` AND `href="/en/"` (CR-01 / Pitfall 5 regression class) and no unprefixed-asset failures.
    - `npm run typecheck` (astro check) reports no new errors.
    - `src/pages/404.astro` renders a `.not-found__pool` of `<img class="pop-photo">` where exactly the first also has class `is-active`, every `<img>` has `alt=""`, and images after the first carry `fetchpriority="low"`; the pool container is `aria-hidden="true"`.
    - The `.pop-photo` CSS rule declares NO `transition` property (source assertion — the D-05 hard cut).
    - The scrim uses a `radial-gradient(... at center ...)` (not `linear-gradient(to top ...)`).
    - The centered content contains: a `.not-found__logo` `<img src={logoWhiteSrc} alt="">`, a `404` marker, a heading with both "Page introuvable" and "Not found" visible, and two literal `<a href={frHome}>`/`<a href={enHome}>` links side by side.
    - `<BaseLayout ... noIndex headerVariant="none" hideFooter>` and `title="Page introuvable / Page not found"` are preserved.
    - The page builds against an empty pool without crashing (conditional guard around the pool layer).
  </acceptance_criteria>
  <done>Static 404 composition renders full-bleed with a working no-JS first-photo fallback, scrim, centered bilingual content, and base-safe links; build + artifact + typecheck gates green.</done>
</task>

<task type="auto">
  <name>Task 2: Realign the e2e specs to the new markup (static assertions + 404 a11y entry)</name>
  <files>tests/e2e/not-found.spec.ts, tests/e2e/accessibility.spec.ts</files>
  <read_first>
    - src/pages/404.astro — the markup just written in Task 1 (the source of the exact roles/text/hrefs the assertions must match).
    - tests/e2e/not-found.spec.ts — the current spec; the `response.status()` (404) and `meta[name="robots"]` (`noindex, nofollow`) and the two `getByRole('link', ...)` href assertions (`/` and `/en/`) that MUST be preserved; the heading assertions that MUST change.
    - tests/e2e/accessibility.spec.ts — the existing static path array (lines 4-14) and the dedicated dynamic-slug test block (lines 25-41) whose pattern the 404 entry may follow.
    - .planning/phases/16-404-page-editorial-redesign/16-RESEARCH.md — Pitfall 4 (update assertions alongside the redesign) and Validation Architecture (the test map).
  </read_first>
  <action>
    Update `tests/e2e/not-found.spec.ts` for the new markup. KEEP verbatim (spirit unchanged): `expect(response?.status()).toBe(404)`, the `meta[name="robots"]` = `noindex, nofollow` assertion, and the two `getByRole('link', {name: /retourner/i})` → `href` `/` and `getByRole('link', {name: /return home/i})` → `href` `/en/` assertions (CR-01 / Pitfall 5 — the hrefs must not change). REPLACE the two `getByRole('heading', {name: 'Page introuvable'})` / `{name: 'Page not found'}` assertions with assertions that match the new DOM: assert the "404" marker text is visible, and assert both `Page introuvable` and `Not found` are visible (via `getByRole('heading')` containing them, or `getByText`, whichever matches the exact element you rendered in Task 1). Both French and English strings must be asserted present on the single load (the no-per-locale-routing structural constraint, success criterion 5). Do NOT add the reduced-motion test here — that belongs to plan 16-03 (the engine doesn't exist yet in this wave).

    Update `tests/e2e/accessibility.spec.ts` to cover the redesigned 404 page. Because the page is reached via a nonexistent path (there is no literal `/404/` route — `not-found.spec.ts` uses `page.goto('/this-page-does-not-exist/')`), either add a genuinely-nonexistent path string to the scanned array, or add a small dedicated `test(...)` block below the loop (mirroring the file's existing dedicated-test pattern at lines 28-41) that navigates to a nonexistent path and runs the identical `AxeBuilder(...).withTags([...]).analyze()` → filter serious/critical → `expect(...).toEqual([])` sequence. This closes RESEARCH Pitfall 4's "zero automated a11y coverage" gap; it will specifically exercise the decorative pool images (`alt=""` + `aria-hidden` container) and the scrim contrast from Task 1.
  </action>
  <verify>
    <automated>npx playwright test not-found accessibility</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test not-found accessibility` passes against the Task 1 page.
    - `not-found.spec.ts` still asserts HTTP 404, `meta[name="robots"]="noindex, nofollow"`, and the two links' hrefs `/` and `/en/`.
    - `not-found.spec.ts` asserts both `Page introuvable` and `Not found` are visible on a single load (bilingual, success criterion 5), plus the `404` marker.
    - `accessibility.spec.ts` scans a nonexistent (404) path and reports zero serious/critical axe violations.
    - No reduced-motion test is present yet in `not-found.spec.ts` (deferred to 16-03).
  </acceptance_criteria>
  <done>Both e2e specs pass against the new static 404 markup; a11y coverage of the 404 page now exists; CI gates stay green with the redesign in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity Content Lake → static HTML (build time) | `getGalleries()` runs only at Astro build; resulting photo URLs are baked into `404.html`. No runtime Sanity call reaches the browser (existing published-perspective boundary, unchanged). |
| Static file → browser (request time) | OVH/GitHub Pages serve a pre-rendered file; no server-side compute, no request-time input processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Denial of Service (client-side) | pool `<img>` stack / preload | medium | mitigate | Cap the pool to ≤16 images at build time; `fetchpriority="low"` on every non-first image; `loading="eager"` + opacity/z-index toggle (never `display:none`) so images load in a bounded, non-blocking way without a swap-time network stall. |
| T-16-02 | Tampering (XSS via CMS content) | pool image alt / rendered text | low | mitigate | All pool `<img alt="">` hardcoded empty (decorative); no per-image Sanity `alt` or any Sanity string is interpolated into an HTML/attribute sink on this page. Copy strings are literal, author-controlled. |
| T-16-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages installed this phase (RESEARCH Package Legitimacy Audit N/A). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm run build` + `EXPECTED_BASE=/ npm run test:artifact` green (base-aware 404.html).
- `npm run typecheck` clean.
- `npx playwright test not-found accessibility` green.
- Manual sanity: open the built page; first photo, scrim, centered logo/marker/phrase/links visible with no JS.
</verification>

<success_criteria>
- ROADMAP success criteria 1 (full-bleed real-photo background, one at a time), 3 (centered logo/marker/phrase/links over scrim), and 5 (bilingual, HTTP 404, noindex, base-aware) are satisfied by the static page; criteria 2 and 4 (pointer-driven rate + reduced-motion drift) are delivered by plan 16-03 on top of this static shell.
</success_criteria>

<output>
Create `.planning/phases/16-404-page-editorial-redesign/16-02-SUMMARY.md` when done.
</output>
