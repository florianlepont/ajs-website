---
// English Contact page (CONT-01, CONT-02): identical structure to
// src/pages/contact.astro; only import depth and copy differ.
import BaseLayout from '../../layouts/BaseLayout.astro';
import ContactForm from '../../components/ContactForm.astro';
import { getContactPage } from '../../lib/sanity';
import { fullSizeUrl } from '../../lib/image';

const locale = 'en' as const;
const contact = await getContactPage();
const intro = contact?.intro?.en ?? 'A question, or an idea for a collaboration? Get in touch.';
const publicEmail = contact?.publicEmail ?? 'contact@atelierjacquelinesuzanne.fr';
const location = contact?.location?.en;
const availability = contact?.availability?.en;
const professionalLinks = (contact?.professionalLinks ?? []).filter((link) => link.url && link.label?.en);
const instagramLink = professionalLinks.find((link) => link.url?.includes('instagram.com'));
const otherProfessionalLinks = professionalLinks.filter((link) => link !== instagramLink);
const seoTitle = contact?.seo?.title?.en ?? 'Contact — Atelier Jacqueline Suzanne';
const seoDescription = contact?.seo?.description?.en ?? intro;
const socialImage = contact?.seo?.image ? fullSizeUrl(contact.seo.image, 1200) : undefined;
const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'ContactPage',
  name: 'Contact',
  description: intro,
  mainEntity: {
    '@type': 'Person',
    name: 'Romane Lepont',
    email: publicEmail,
  },
};
---

<BaseLayout
  title={seoTitle}
  description={seoDescription}
  socialImage={socialImage}
  noIndex={contact?.seo?.noIndex}
  structuredData={structuredData}
>
  <div class="contact-page">
    <h1>Contact</h1>
    <p class="contact-page__lead">{intro}</p>
    <div class="contact-page__details">
      <p><a href={`mailto:${publicEmail}`}>{publicEmail}</a></p>
      {location && <p>{location}</p>}
      {availability && <p>{availability}</p>}
      {instagramLink && (
        <p class="contact-page__social">
          You can also follow me on Instagram:
          <a href={instagramLink.url} target="_blank" rel="noopener noreferrer">
            {instagramLink.label?.en}<span class="sr-only"> (opens in new tab)</span>
          </a>
        </p>
      )}
      {otherProfessionalLinks.length > 0 && (
        <ul class="contact-page__links">
          {otherProfessionalLinks.map((link) => (
            <li>
              <a href={link.url} target="_blank" rel="noopener noreferrer">
                {link.label?.en}<span class="sr-only"> (opens in new tab)</span>
              </a>
            </li>
          ))}
        </ul>
      )}
    </div>
    <ContactForm locale={locale} publicEmail={publicEmail} />
  </div>
</BaseLayout>

<style>
  .contact-page {
    max-width: 640px;
    margin: 0 auto;
    padding: var(--space-2xl) var(--space-md);
  }

  .contact-page h1 {
    font-size: var(--text-display-size); /* Display role */
    font-weight: var(--weight-semibold);
    font-family: var(--font-display);
    line-height: 1.2;
    margin: 0 0 var(--space-xl);
  }

  .contact-page__lead {
    font-size: var(--text-body-size); /* Body role */
    font-weight: var(--weight-regular);
    line-height: 1.5;
    margin: 0 0 var(--space-2xl);
  }

  .contact-page__details {
    font-size: var(--text-body-size); /* Body role */
    font-weight: var(--weight-regular);
    line-height: 1.5;
    margin: 0 0 var(--space-2xl);
  }

  .contact-page__details p {
    margin: 0 0 var(--space-sm);
  }

  .contact-page__social a {
    margin-left: 0.35em;
  }

  .contact-page__links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-md);
    margin: var(--space-md) 0 0;
    padding: 0;
    list-style: none;
  }
</style>
